# Froot — Phase Roadmap & Status

> **Single source of truth for build status.** At the end of every phase, update this table
> (status, commit hash, notes) and commit it along with the phase's code.
> Last updated: 2026-07-22

| Order | Phase | Size | Status |
|---|---|---|---|
| 1 | I-1 Sales item sync + Ingredient library | M–L | ✅ Done (7-4/7-5, v2 rebuild: SalesItem vs Ingredient split, units.ts, CSV import) |
| 1b | I-1b Ingredient parity, lifecycle & duplicates | M | ✅ Done 7-6 (lifecycle, bulk edit, duplicate finder, cost log) |
| 2 | I-2 Vendors, POs & Receiving | M–L | ✅ Done (full PO lifecycle, receiving updates costs; receipts stamp receivedAt so purchases land in the right inventory period) |
| 3 | I-3 Storage areas | S–M | ✅ Done (commit 432a30e) — areas + count sheets, bulk assignment, unassigned triage, re-pointed to ingredientId |
| 4 | I-4 Physical counts | M | ✅ Done (commit eb816db) — count workflow, weigh-to-count, offline-tolerant entry, finalize, corrections + audit trail, count summary |
| 5 | I-5 Sales sync + COGS & analytics | M–L | ✅ Done 7-6 (b3cb378) — Square sales sync (daily/hourly/per-item caches), inventory-period math, Reports suite under Inventory: Summary, Item Sales, Periods/COGS with GL breakdown + negative-usage flags, Valuation, Turnover, Vendor Spend |
| — | D-1 Dashboard redesign (added in pack v2.3) | M | ✅ Done 7-6 (1b8160f) — Sales Performance (today vs same weekday last year, hourly pace), Monthly Goal with extrapolation, real Shift Checklist, mock-backed Team Messages / Corporate Update / Instagram |
| 6 | I-6 Recipes + needs-attention queue + adjustments | L | ✅ Done 7-6 (4947def) — recipes w/ read-time cost engine + loop detection, triage queue, prep/batch items, transfers/loss/adjustments (backdatable, grouped), Variance + Profitability report tabs; fixture: `npx tsx scripts/seed-razz-fixture.ts` |
| 7 | I-7 Reorder points & alerts | M | ✅ Done 7-7 (1020b9e…5380324, 6 commits) — expected-inventory engine (`/inventory/expected`, consumption-mode depletion, degraded mode w/o sales), per-store pars/reorder points + weekly usage on Ingredients, low-stock alerts (`/inventory/alerts` + sidebar badge, 14d staleness guard hardcoded — org-configurable later), Cart Builder + Smart Cart (`/inventory/orders/new`, fill-to-par / N×usage → one draft PO per vendor), vendor minimums/delivery days/standing adjustments (auto-attach at receive, in COGS GL), PO history grouping + CSV + invoice attach (Blob, no OCR) + price-change confirm; fixture: `npx tsx scripts/seed-i7-fixture.ts --seed/--verify --org <id>`; deferred: emailing orders to reps, alert-badge live refresh |
| — | F-1 Forecasting: sales-data foundation | S–M | ✅ Done 7-8 (b7feecb) — GoalPlan/DailyGoal schema (migration `20260708100000_f1_goal_plans`), resumable LY backfill (`POST /api/forecasting/backfill`, ~2-week chunks, verified against real 2025 Square history), nightly 3-day reconciliation cron (`vercel.json` → `/api/cron/sales-reconcile`, verified 9/9 Keva stores); actuals reuse I-5 `SalesPeriodCache`; **⚠ deploy: set `CRON_SECRET` env var in Vercel**; exit test (penny-reconcile a full month vs Square Dashboard) pending Gary's spot-check after first full backfill |
| — | F-2 Goal engine + Forecasting page | M | ✅ Done 7-8 (11f0b47) — weekday-aligned basis (−364d, calendar-date + weekday-avg fallbacks), % scaling with month-exact rounding residuals, `/forecasting` (Goal Settings panel + 12-month calendar, actual-vs-goal tinting, managers read-only, admin writes enforced server-side in every route); fixture: `npx tsx scripts/verify-goal-engine.ts` (15 checks green) |
| — | F-3 Forecasting overrides + import | M | ✅ Done 7-8 (11f0b47) — day-edit dialog (`isOverride`, survives % recalcs), month-total edit redistributed by LY weekday weights, CSV/XLSX import (daily `date,amount` or monthly `month,amount` shapes, server-side parse, preview → commit, raw file to Vercel Blob), "remaining days only" mid-year raise + opt-in override reset; see `FORECASTING.md` |
| — | F-4 Dashboard live pacing | S | ✅ Done 7-10 (96bc048, cd711d7) — Monthly Goal card is plan-driven with goal-weighted extrapolation (`MTD ÷ MTD-goal × month goal`; run-rate fallback, formula now shared via `src/lib/pacing.ts`); Square order webhooks (`/api/webhooks/square`, HMAC-verified, ACK-then-resync of the affected store-day; lazy sync + nightly reconcile stay as backstop); "All locations" dashboard mode (`/api/dashboard/rollup`: summed totals, goal-weighted projection over summed DailyGoal rows, sortable store-ranking table w/ on/behind-pace pills, admin vs assigned-store scoping); fixture: `npx tsx scripts/verify-f4-rollup-webhook.ts` (14 checks green); **⚠ deploy: set `SQUARE_WEBHOOK_SIGNATURE_KEY` in Vercel (per env) + add the webhook subscription in each Square app (prod + Froot Staging) — notification URL `${NEXT_PUBLIC_APP_URL}/api/webhooks/square`, events order.created/updated + payment.created/updated; see FORECASTING.md** |
| — | F-5 Forecasting polish | S | ✅ Done 7-10 (cb3f1bb) — goal-edit audit log (every goal mutation → AuditLog w/ before→after, `GET /api/forecasting/audit` + Edit history panel on /forecasting; admins all stores, managers assigned only), CSV export (`GET /api/forecasting/export`, round-trips through the importer, Export CSV button), behind-pace alerts (daily cron `/api/cron/pace-alerts` 15:00 UTC, threshold `PACE_ALERT_THRESHOLD_PCT` default 90%, pace through yesterday via shared month-goal/pacing helpers, one alert per store-month via PaceAlertLog, migration `20260710220000`; recipients = org admins + assigned managers; email sender is a swappable interface w/ console default — real provider deferred); fixture: `npx tsx scripts/verify-f5-polish.ts` (18 checks green); **⚠ deploy: cron registers from vercel.json automatically (needs existing `CRON_SECRET`); optional `PACE_ALERT_THRESHOLD_PCT` env; alerts log to console until an email provider is wired in `src/lib/notify.ts`** |
| — | I-14 Team Messaging Phase 1 | M–L | ✅ Done 7-9 — TeamMessage/MessageAttachment/MessageReaction/MessageRead/CorporateUpdate schema (migration `20260709090000_i14_team_messaging`), `/messages` store feed (typed composer, photo/doc/YouTube attachments via Blob, emoji reactions, manager resolve/reopen, 15-min edit window, soft delete, cursor pagination, mark-read), checklist handoff notes (top+bottom "Leave a note for the next shift" composer w/ Post-to picker, server-side date resolution by operationalPhase, "Notes from the last shift" banner marks read), dashboard Team Messages + CORPORATE UPDATE boxes live via one `/api/dashboard/comms` call (PREVIEW/sample labels removed, corporate box collapses when none active), bare-bones admin corporate composer on `/messages`; roster sync = existing Square staff import; Phase 2+ deferred: acknowledgments, Twilio notifications, shortage→inventory alerts |
| — | I-14b Shift handoff carry-over & acknowledge | S–M | ✅ Done 7-11 (cde3b86…4c0a1de, 5 commits) — cross-day carry-over now resolves at read time with `postedForDate <= day` (a Saturday closer note for a Sunday-closed store surfaces on Monday's opener automatically), explicit Acknowledge lifecycle (`acknowledgedAt/acknowledgedByUserId` on TeamMessage, migration `20260711165333_add_handoff_ack_expiry`; viewing still only marks read — banner + dashboard clear on Acknowledge everywhere), org-configurable auto-expiry (`Organization.handoffNoteExpireDays` default 2, `expiresAt` stamped at post; legacy rows keep the 7-day createdAt backstop), store-wide notes ("Everyone (store-wide)" composer option, `postedToTemplateId` null; date rule = last-slot semantics), dashboard "NOTES FOR THIS SHIFT" card above the Shift Checklist box (via `shiftNotes` on `/api/dashboard/comms`, store-local day, no template filter so notes to retired templates are never dropped; collapses when empty), stale styling (>2d unacked) in the banner, Acknowledged/Expired badges in the feed, `PHASE_ORDER` fixed to canonical "During the Day" (+ legacy "During Hours" alias — one live row still has it); verified: 14-check DB round-trip incl. closed-day carry-over, expiry, ack (script run ad hoc, not committed) |
| — | IG-1 Instagram integration | M | ✅ Done 7-10 (b1fce8b) — org-level connect via **Instagram API with Instagram Login** (`graph.instagram.com`, scope `instagram_business_basic`; requires a Professional account; Basic Display API is dead), `Organization.instagram*` fields (migration `20260710235000_instagram_integration`), long-lived token w/ lazy 7-day-window refresh, Settings card (connect / on-off switch / disconnect behind AlertDialog / reconnect nudge), conditional sidebar item, `/instagram` grid page (permalink link-outs; IG can't be iframed), dashboard strip live via `/api/instagram/feed` (~60 min in-memory cache, stale-on-error, never calls IG on page load; `media_url` never persisted); **⚠ deploy: set `INSTAGRAM_APP_ID` + `INSTAGRAM_APP_SECRET` (optional `INSTAGRAM_REDIRECT_URI`) in Vercel per env; register `${NEXT_PUBLIC_APP_URL}/api/instagram/callback` as the OAuth redirect URI in the Meta app; until Meta App Review grants Advanced Access for `instagram_business_basic`, only IG accounts with a role on the Meta app can connect — start App Review early**; deferred: posting/comments/insights/Stories, Vercel-cron refresh backup |
| — | HR-0 HR, Training & Compliance foundation | S–M | ✅ Done 7-12 (0233558…8aec033, 4 commits) — full forward-compat HR schema (15 models + 3 enums incl. the HR-0a-confirmed checkpoint/acknowledgment pair w/ signing-time snapshots + fileHash version pin, migration `20260712120000_hr0_hr_training_compliance_schema`), server-side availability gate `hrModuleAvailable()` (`HR_MODULE_AVAILABLE` env flag OR `HR_INTERNAL_ORG_IDS` allowlist — `/hr` 404s while unavailable), per-org `activeModules` "hr" toggle (Settings Integrations card w/ Switch → `POST /api/hr/toggle`, admin-only), Billing add-on card, gated sidebar HR item (hidden until available AND active); no feature code reads the HR models yet; **⚠ deploy: `HR_MODULE_AVAILABLE=true` set on Vercel Preview only — leave production unset until HR launch**; next: HR-1+ (documents CRUD), HR-3 private Blob route |
| — | HR-1 Staff directory + detail shell | S | ✅ Done 7-12 (f85c062…b7d854b, 3 commits) — `/staff` gains a gated Compliance column (stub `getStaffComplianceSummary()` in `src/lib/hr.ts` returns `pct: null` → muted "—" w/ tooltip, never 0%; HR-8 replaces with the real batched rollup) and display-name links to new `/staff/[id]` — both only when `hrModuleAvailable(orgId) && activeModules.includes("hr")`, page byte-identical otherwise; `/staff/[id]` (availability + toggle gate → notFound, non-admins scoped to their assigned stores) renders header (Square badge, member-since, store chips) + tabs Overview (real data) / Documents / Training / Notes / Compliance as read-only shells labeled HR-4/5, HR-6/7, HR-2, HR-8; `/hr` landing now links to the staff directory; read-only phase — no writes, no new API routes |
| — | HR-2 Manager notes | S | ✅ Done 7-12 (30f6e26…2d3d5bb, 3 commits) — Notes tab on `/staff/[id]` is live (first HR phase that writes): add-note box (textarea + 5-category select) + list w/ color-coded category chips, author/timestamp footer, edited marker, empty-state CTA, client-side category filter chips; edit is author-only (dialog), delete is author-or-ADMIN behind AlertDialog; API `POST /api/staff/[id]/notes` + `PATCH/DELETE /api/staff/[id]/notes/[noteId]` share one guard (`access.ts`): 401 → `hrModuleAvailable` 404 → `requireModule("hr")` 403 → `requireManagerOrAdmin` 403 → org 404 / store-scope 403 (mirrors the page's `getStaffMember`); STORE/STAFF never see the tab and never get notes fetched; **no migration** — `ManagerNote.authorUserId` deliberately has no FK to User (notes survive author deletion; authors stitched via a second query, "Former user" fallback); Zod validates the 5-value category + non-empty body; notes are text-only by design (files live in HR-4/5) |
| — | HR-3 Reference Library + private HR file service | M | ✅ Done 7-12 (1a03dca…5161e1e, 3 commits) — the load-bearing HR file layer: dedicated **PRIVATE** Blob store `froot-hr` (iad1, store_7H3toifj0cxlzbAO; connected to all envs w/ `HR_BLOB_` prefix → `HR_BLOB_READ_WRITE_TOKEN`, documented in CLAUDE.md) — uploads via `src/lib/hr-files.ts` `uploadHrFile` (`access:'private'` + random suffix, PDF/PNG/JPG/DOC/DOCX ≤25 MB, sha256 `fileHash`), delivery via `getHrFileDownloadUrl` (cached 10-min read delegation → 5-min presigned GET; `streamHrFile` proxy fallback kept), authorization is the per-kind `canReadHrDocument` policy (Reference ⇒ any org member; HR-4 kinds stubbed) — **verified against the live store: raw blob URL w/o session → 403, signed URL → 200, unauthenticated app routes → sign-in redirect**; `/hr/documents` Reference Library (any role reads/downloads; grouped by category w/ chips, client filter chips, skeleton loader, role-aware empty states) + ADMIN Add/Edit/Archive (`POST /api/hr/documents`, `PATCH /api/hr/documents/[id]` soft archive `isActive:false` — no hard delete, no blob deletion; shared guard `access.ts`: 401 → availability 404 → module 403 → admin 403); Document Library card on `/hr`; public task-attachment store untouched, **no dep bump** (`@vercel/blob` 2.5.0 already ships private access + presign), **no migration**; single-version per doc (re-upload/versioning UI lands w/ HR-4) |
| — | HR-4 Signed-record engine + acknowledgment documents | L | ✅ Done 7-13 (fa94b21…, 4 commits) — the keystone HR phase (HR-5 forms + HR-7 training certs reuse it): new `HrSignedRecord` model (migration `20260713080000_hr4_signed_record`, RESTRICT FKs, unique (version, staff), append-only — no mutate/delete path anywhere); `kind:"Acknowledgment"` docs w/ auto-generated checkpoints from the real PDF page count via **pdf-lib** (new dep; per-page Initials + final Acknowledgment w/ editable attestation) + ADMIN checkpoint editor & re-upload versioning at `/hr/documents/[id]` (re-upload → v(n+1) isCurrent, prior demoted, doc-scoped checkpoints carry forward; signed checkpoints undeletable 409); capture engine `POST /api/hr/documents/[id]/acknowledgments` writes one snapshot-laden acknowledgment per checkpoint (name/store/title/version/`documentFileHash` pin/attestation + ESIGN: consentText/version, signedAt, ipAddress, userAgent, authMethod), idempotent via skipDuplicates; two entry points, one screen at `/hr/acknowledge/[documentId]`: self-serve `ClerkSession` (staff resolved by **org-scoped case-insensitive email match** — User↔StaffMember has no FK; friendly unlinked empty state) and `?staff=` manager-attested (`ManagerAttested`/method `Attested`, ADMIN org-wide, MANAGER store-scoped); completion synchronously triggers `src/lib/hr-signed-pdf.ts` `ensureSignedRecord` (idempotent, race-safe, built ONLY from snapshots): page-1 completion banner + appended Certificate of Acknowledgment pages (source SHA-256, signer/store/org, authMethod, IP, device, consent statement, per-checkpoint UTC timestamp table, per-page initials grid) → own sha256 → private store `hr/{org}/signed-records/` → `HrSignedRecord`; `/staff/[id]` Documents tab live for ADMIN/MANAGER (status "Signed v{n} · date" / "record pending" + generate-record recovery / "Needs current version" w/ old record still downloadable / "In progress x/y" / "Not started"; Record → attested flow); read policy: blank Acknowledgment template = any org member, **signed records = ADMIN / in-scope MANAGER / owning staff only** via `canReadHrSignedRecord` + `GET /api/hr/signed-records/[id]/download`; ADMIN recent-completions list `/hr/signed-records` (full rollup is HR-8); **verified end-to-end against the real 28-page 2026 Employee Handbook + live private store: 11/11 checks — defaults from real page count, duplicate acks skipped, signed PDF hash matches, 28→30 pages w/ correct certificate, raw original+signed blob URLs 403 w/o auth, v1 records/acks intact & hash-pinned after re-upload, status flips needs-current**; in-page per-`pageRef` stamping deferred (pageRef captured; certificate page is authoritative); drawn-signature PNG deferred (typed-name capture per ESIGN); out of scope: HR-5 forms, HR-6/7 training, HR-8 rollup, `/my/*` portal |
| — | HR-5 Fillable agreement forms (Key/Pay) | M | ✅ Done 7-13 (2cff79e…, 3 commits) — second document archetype on the HR-4 engine: `kind:"FillableForm"` docs render **natively** (`bodyText` + ordered `FormField`s incl. Select w/ `options` Json) — no upload; versions snapshot the canonical definition JSON in `definitionSnapshot` w/ its sha256 in `fileHash` (same pin semantics as file bytes; sentinel file columns, migration `20260713160000_hr5_fillable_forms`); definitions edit **in place until a version has submissions**, then edits mint v(n+1) (prior submissions stay pinned); ADMIN builder `/hr/forms` + `/hr/forms/[id]` (body-text editor, field editor w/ reorder, version history, archive) + symmetric `linkedFormId` Check-Out ↔ Check-In pairing (both docs written transactionally, steal-refused 409; seed `scripts/seed-key-agreement.ts`); execution `/hr/forms/[id]/submit?staff=` (ADMIN / store-scoped MANAGER = the supervisor signer): pinned-definition render, ESIGN consent gate, **dual typed-name signatures — submit disabled until BOTH captured**, 409 hash guard if the definition changed mid-fill; non-co-present → `PendingSupervisor` + once-only status-guarded countersign (employee half never rewritten); **re-execution is first-class** (pay changes, key re-issues): NO unique on FormSubmission — each signing is its own append-only record and **the executed PDF lives on the submission** (`signedPdfPathname/Hash/generatedAt`, write-once) via `ensureFormSignedPdf` in the shared `hr-signed-pdf.ts` (certificate scaffolding extracted to `CertificateWriter`, ack output unchanged; typographic-char sanitize fix): title + body + values + both signature blocks + Certificate of Execution (definition SHA-256 pin, both signers' timestamps/IP/device, consent) → private `hr/{org}/signed-records/`; `HrSignedRecord` stays acknowledgment-only, untouched; reads: `GET /api/hr/forms/submissions/[id]/download` behind the same `canReadHrSignedRecord` tier (ADMIN / in-scope MANAGER / owning staff), `canReadHrDocument` FillableForm case = ADMIN/MANAGER + no-file 404 on the doc download route, forms excluded from the Reference library; `/staff/[id]` Documents tab gains **Agreement Forms**: pairs grouped w/ per-side status ("Check-Out signed 7/12 · Check-In pending"), full chronological history per form, per-record download + idempotent generate-recovery, "New execution" always available, archived-form records stay visible; **verified end-to-end against dev DB + live private store: seeded pair hash-recomputes + backlinks both ways, PDF generated/idempotent/hash-matched (visually inspected), in-place-vs-mint matrix all green, prior submission stayed pinned to v1 after mint, 2 accumulated executions for one staff/form, HR-4 ack path regression-tested through the refactored writer**; no new deps; out of scope: HR-6/7 training, HR-8 rollup, `/my/*` portal, conditional fields |
| — | HR-6 Training builder (Trainual-style authoring) | M | ✅ Done 7-13 (1c8b4d1…0dde473, 3 commits) — native training authoring on the HR-0 models (no signed-PDF engine — training is interactive content; certification reuses HR-4/5 in HR-7): ADMIN-only end to end (`requireHrTrainingAccess` guard in `src/app/api/hr/training/access.ts`: 401 → availability 404 → module 403 → admin 403; list/builder pages redirect non-ADMIN to `/hr`); `/hr/training` list mirrors the template library (active/archived tabs, bulk activate/deactivate, archive behind AlertDialog, duplicate — clones lessons+quiz+resource rows sharing the same private blobs since HR never deletes blobs; skeleton loader, empty-state CTA, Training card on `/hr` hub) and `/hr/training/new` + `/hr/training/[id]/edit` mirror `template-form.tsx` (dnd-kit lesson reorder `PointerSensor` distance:8, inline edit, `info` Textarea as the lesson body, `videoUrl` stays a LINK — never uploaded); **resources live on the PRIVATE froot-hr store** (never the public task-attachment route) via the HR-3 three-step browser upload (`/api/hr/training/upload-url` presigned PUT under `hr/{org}/training/` — stricter than the store-wide limits: PDF/JPG/PNG ≤10 MB — then register on the lesson), **4-per-lesson cap + type/size enforced server-side against authoritative blob metadata** (`readHrFileMeta`, `isOrgTrainingBlobUrl` namespace pin), delivery via admin-scoped `GET /api/hr/training/resources/[id]/download` → 5-min signed URL (HR-7 widens this route to assigned staff); existing lessons upload/delete resources immediately, brand-new lessons use the template two-phase pending pattern (upload after save, localId→real id by orderIndex position); quiz authoring persists `TrainingQuiz` (one per module: upsert while questions exist, row deleted when emptied) — `passThreshold` percent 0–100 + `questions` Json `{id, type: boolean|single|multi|written, prompt, options?, correctOptionIds?}` (written = manual review, boolean = implicit true/false ids), Zod-validated in `schemas.ts`, client cleans/blocks empty prompts/<2 options/no correct answer; save = `$transaction` diff (delete removed / update existing / create new lessons + wipe-recreate store assignments, templates pattern); module DELETE 409-blocked once TrainingAssignments exist (archive instead); migration `20260713200000_hr6_training_resource_order` adds `TrainingResource.orderIndex`; **verified end-to-end against dev DB + live private store: 10/10 checks — presigned PUT 200 to training prefix, authoritative meta read, raw blob URL 403 w/o signature, signed URL serves exact bytes, lessons+resources reload in order, videoUrl stored as link, quiz JSON round-trips intact, module delete cascades**; no new deps; out of scope (HR-7): assignment, `/my/training`, quiz taking/grading, certification, rich-text lessons, multi-quiz; **post-review addendum (Gary)**: Export/Import on `/hr/training` mirroring the templates pair — `GET /api/hr/training/export` CSV (one row per lesson/question via `row_type`, module cols repeat; `?format=json` lossless incl. resource metadata) + `POST /api/hr/training/import` (PapaParse dialog w/ preview counts + replace mode archiving same-titled; imports arrive INACTIVE/all-stores; pipe-separated question_options/question_correct matched by text; broken question rows drop the quiz but keep lessons; resource FILES never travel — reattach in builder) + downloadable example CSV in the dialog; shared helpers `csv.ts` verified 16/16 round-trip checks (quotes/commas/newlines, all 4 question types, example file, row-numbered errors) |
| — | HR-7 Training execution + staff self-service | L | ✅ Done 7-13 (de743ca…, 4 commits) — the staff-auth phase (A+B hybrid, routed by email presence, `authMethod` recorded on every completion): **migration `20260713220000`** adds `StaffMember.status/terminatedAt/userId⇄User`, `PendingInvite.staffMemberId`, `TrainingAssignment` cert fields (`certPdfPathname/Hash`, `certifiedByUserId`, `trainerTypedName`), `TrainingLessonProgress.authMethod/completedByUserId`, new append-only `TrainingQuizAttempt` (questions+threshold snapshot pin); **(A) employee logins**: Square import/sync now captures `email_address` (fill-only-when-empty on sync), `POST /api/staff/[id]/invite` (ADMIN/in-scope MANAGER) → Clerk org invitation + `PendingInvite{role:STAFF, staffMemberId}`, webhook links User⇄StaffMember on acceptance; employee logins (STAFF+link, HR gates on) are UI-locked to `/my` via `(app)/layout.tsx` redirect (permission-level split = HR-9); **rule 1**: `POST /api/staff/[id]/terminate` via shared `terminateStaffMember` (status flip + Clerk membership delete + session revoke + pending-invite revoke; never deletes) + new `organizationMembership.deleted` webhook handler (unlink only — losing a login ≠ termination) + `getActiveStaffSelf()` server-side ACTIVE gate on every `/my` surface; **rule 2**: sync-square reconciles Square INACTIVE → TERMINATED (records retained), staff DELETE 409s when any records exist; **manager side**: `requireHrTrainingManageAccess` tier (ADMIN org-wide / MANAGER store-scoped), assignment CRUD (delete blocked once records exist), `/staff/[id]` Training tab live (progress %, quiz/cert status, assign dialog, attested lesson complete + quiz-result capture `ManagerAttested`, log hours, written-answer review, co-sign & certify, cert download); **rule 3 — `/my`**: mobile-first bottom-tab shell (never the admin sidebar), `/my/training` lessons (info/YouTube embed/private resources via widened download route — ADMIN or assigned ACTIVE staff only) + `ClerkSession` completion advancing the %, quiz taking (answer key stripped server-side, snapshot at submit, objective auto-grade vs `passThreshold`, written → `PendingReview` — or immediate Failed when objectively unreachable; retakes allowed, Passed/PendingReview block re-sits), `/my/documents` (own version-pinned sign-off statuses + reference library + self-acknowledge reusing the HR-4 `AcknowledgeClient` w/ full ESIGN, `ClerkSession`; self-serve ack now ACTIVE-gated and resolves via `findStaffMemberForUser` link-first); **certification**: `ensureTrainingCertPdf` in the shared HR-4/5 engine (CertificateWriter; trainee/trainer/hours/quiz result/per-lesson timestamps+authMethods, trainer typed-name co-sign block, write-once pointer, race-safe) behind `POST .../certify` (requires Completed + hoursLogged, once-only co-sign stamp); **rule 5 tightened**: owning-staff branch REMOVED from `canReadHrSignedRecord` (both download routes now ADMIN/in-scope MANAGER only — behavior break vs HR-4/5 by design, Gary-approved), cert download `GET .../certificate` same tier, `/my` shows status only + "ask your manager" copy; **verified against dev DB + live private store: 7/7 identity, 8/8 status engine, 7/7 grading branches, 11/11 cert e2e (hash match, idempotent, raw blob 403, policy matrix)**; no new deps; out of scope: HR-8 rollup/reminders/CSV, EMPLOYEE role (HR-9) |
| — | HR-7.5 Staff editing + resync follow-up (7-14) | S | ✅ Done 7-14 (post-HR-7 test feedback, Gary) — **migration `20260714120000`**: `StaffMember.squareTeamMemberId` global `@unique` → `@@unique([organizationId, squareTeamMemberId])` (multi-tenant fix — the same real Square employee can now be imported by more than one org; NULLs stay distinct so manual members never collide; this was the root of "individual import produced nothing"); **import robustness**: `handleImport` checks each response, keeps the dialog open on failure, and lists who failed + why instead of swallowing errors; **manual+edit**: Email field added to Add Staff dialog; new Edit dialog on `/staff/[id]` (display/full name, email, store assignments, primary store) → `PATCH /api/staff/[id]` extended (ADMIN org-wide / in-scope MANAGER, Zod-validated, storeIds replace-set with primary re-flag, light-touch primaryStoreId-only path preserved for the list chips); **per-member resync**: `POST /api/staff/[id]/resync-square` + `fetchSquareTeamMember` (Square retrieve endpoint, returns status) — authoritative pull of Square-linked fields (name/email/store assignments, primary preserved if still in Square's set), INACTIVE → terminate (rule 2), leaves documents/training/notes/status/login untouched; "Resync from Square" button on the profile behind a confirm spelling out what it replaces; bulk "Sync Locations" stays conservative (never overwrites a manual email — flag 3); verified 5/5 (per-org coexist + dup-still-rejected + resync primary-preservation), `next build` green |
| — | HR-7.6 Manager-uploaded staff documents + visibility toggle (7-14) | S | ✅ Done 7-14 (post-HR-7 request, Gary) — per-member manual document uploads with a team-visibility switch, for scanned/photographed paper forms outside the org-template system (signed pay agreement → shared with the employee; write-up → internal-only): **migration `20260714150000`** new `StaffDocument` (private-blob pointer + sha256, `uploadedByUserId` plain-id, `category?` free label, `visibleToStaff` default false; no cascade — a retained record that also joins the staff delete-guard); reuses the HR-3 private store + presigned browser upload (no new storage/deps) under `hr/{org}/staff-documents/{staffId}/` (namespace-pinned on register, authoritative metadata via `readHrFileMeta`); routes `POST .../documents/upload-url` + `POST .../documents` (register) + `PATCH .../[docId]` (toggle visibility / rename / relabel) + `DELETE .../[docId]` (mis-upload; blob left per HR no-hard-delete) + `GET .../[docId]/download` (two-tier auth: ADMIN/in-scope MANAGER always; owning ACTIVE staff only when `visibleToStaff`) behind shared `requireManageableStaff` guard (`src/app/api/staff/access.ts`: HR gate → ADMIN/in-scope MANAGER → member scope); UI = "Uploaded Documents" section on the `/staff/[id]` Documents tab (Upload dialog w/ title/category/visibility Switch, per-row Eye/EyeOff Switch + download + delete) and a "Shared with you" section on `/my/documents` (visible uploads only, not-visible never fetched); **distinct from rule 5's auto-generated signed-record/cert tier — this class is manager-controlled per-doc visibility by design**; verified 8/8 against dev DB + live private store (namespace pin, hash match, visible-only staff query, toggle, signed-URL serves / raw blob 403, delete-guard); `next build` green |
| — | HR-8 Compliance rollup dashboard | M | ✅ Done — built 7-22 (`61eea85`), **staging pass complete 7-22**: happy path, denominator math, needs-re-sign machinery, Overdue training, terminated exclusion, manager scoping all verified; the pass caught BUG-2 (staff-profile linking, fixed in `3c7d0a0`) and item 4's "mid-signing" state was verified **unreachable by design** (atomic capture + signed-record finality); follow-ups logged as HR-11 (inline signing view + progressive save), HR-12 (living-document re-upload), HR-13 (drill-down + exclude-until-due training %), HR-14 (termination/identity hardening) — computed **live** from existing records: no schema change, no migration, no new deps, no stored snapshots (→ no per-environment data to regenerate). `src/lib/hr-compliance.ts` batched org rollup (fixed query count, never per-staff); definitions per `DECISIONS.md` (Gary 7-22): ack docs current-version-only w/ distinct **"needs re-sign"**, all-required-acked ("pending-record") counts complete, training **Completed = compliant** + Certified as separate badge + **Overdue** (past dueDate, not completed) as loudest gap, **agreement forms OUT of the %** (Agreements panel w/ PendingSupervisor countersigns surfaced), **ACTIVE staff only** (terminated auditable, never counted). Surfaces: `/staff` Compliance column now real (batched `getStaffComplianceSummaries` replaces the HR-1 stub), `/staff/[id]` Compliance tab (per-item statuses, terminated exclusion banner, ADMIN/MANAGER only), new `/hr/compliance` (ADMIN org-wide / MANAGER store-scoped: 4 KPI cards, by-store table grouped by primary store, sortable gaps-first employee table w/ "only gaps" filter, Agreements panel), hub card (ADMIN/MANAGER) + signed-records cross-link. Fixture: `npx tsx scripts/verify-hr8-compliance.ts` (41 checks green vs dev DB); `next build` green |
| — | HR-10 Required agreement forms (deferred from HR-8) | S–M | ⬜ Not started — make agreement forms count toward the compliance %: an admin-set "required" designation (who must hold each form) + a defined **signing cycle** (how often re-execution is due — agreements are re-signed multiple times per year). Needs additive schema; deferred out of HR-8 (Gary, 7-22) because no assignment mechanism exists and cycle semantics are undefined. Until then, forms are tracked on `/hr/compliance` in the Agreements panel outside the % |
| — | HR-9 EMPLOYEE role split (post-HR-8, approved 7-13) | S–M | ⬜ Not started — HR-7's `/my`-only lock for staff logins is a UI redirect in `(app)/layout.tsx`; employee logins keep STAFF-role API permissions (checklists, messages) they never see. This phase adds a dedicated `EMPLOYEE` value to the `Role` enum and cuts those APIs off at the permission layer: invite flow issues EMPLOYEE instead of STAFF, `/users` role management + role-gated routes updated, existing linked STAFF logins migrated. Explicitly deferred out of HR-7 (Gary, 7-13) |
| — | HR-11 DocuSign-style inline signing view (Gary, 7-22) | M | ⬜ Not started — render the acknowledgment PDF inline on the signing screen (pdf.js, client-side canvas from the existing private-Blob file — **no copies, no extra storage**): pages lazy-render as scrolled (mobile-first), each page's "Page N initials" control anchored to its page, confirmed pages (or Initial All) show the typed initials as a display-only overlay badge, final acknowledgments collected on one closing screen. Legal record unchanged (checkpoint captures + generated signed PDF); pdf.js lazy-loaded on this route only, fetched via the existing authed HR file route. Side effect: "scrolled past every page" becomes real. Candidate to fold into the STAFF-1 staff-experience redesign |
| — | HR-12 Living-document updates — re-upload without re-acknowledgment (Gary, 7-22) | S–M | ⬜ Not started — choice at re-upload: "requires re-acknowledgment?" **No** (default for the handbook, which carries a living-document clause): new version becomes current for viewing + new signers, existing signed records STAY compliant (version-pinned to what was actually signed — audit trail unchanged), optional "updated since you signed" note on `/my/documents`. **Yes** (material change): today's needs-re-sign behavior, kept as the lever for policy overhauls. Additive schema (flag on version or last-required-version pointer on document) + compliance computation honors it. Whether a living-document clause legally covers later versions is Gary's policy/legal call — the system supports both modes. Related guard (found in HR-8 pass 7-22): the checkpoint editor silently allows adding checkpoints to a version people already hold signed records for — they are NOT re-prompted (record finality, by design); the editor should warn "N staff already signed this version" and point to the re-upload path |
| — | HR-13 Compliance drill-down + training due-date semantics (Gary, 7-22 HR-8 pass feedback) | S | ⬜ Not started — (a) `/hr/compliance` Items cells ("2/3") and By Store rows become clickable: expand/deep-link to exactly which items are outstanding for that employee/store ("at 100 employees you can't nail down who's the issue" — today only the name click → profile tab and the gaps toggle exist); (b) implement not-yet-due training semantics per decision (Gary 7-22, recorded in DECISIONS.md HR-8 §c amendment): **exclude-until-due** — assignments enter the % denominator only once dueDate passes; completing early counts immediately; Overdue flag unchanged. As-built code counts from assignment until this ships |
| — | HR-14 Termination + identity-lifecycle hardening (Gary, 7-22 HR-8 pass findings) | M | ⬜ Not started — (a) **zombie shell**: terminated staff who sign back in can land in the empty `(app)` admin shell (Messages compose included) — termination's Clerk cleanup is best-effort with silently swallowed failures, and the server-side ACTIVE gate only guards `/my`, not the shell; add a layout backstop (staff TERMINATED or User row without live membership → "access removed" screen), log Clerk cleanup failures instead of swallowing. Interacts with HR-9 (STAFF-role API permissions still live). (b) **staff directory**: terminated members hidden by default behind a Homebase-style "Show terminated" toggle + status badge (today they stay inline forever with "—" compliance). (c) webhook hardening rolled in: no `organization.deleted`/`user.deleted` handlers (5 fossil org rows on staging), User rows survive membership deletion (orphans; BUG-2 aftermath), consider User-row cleanup on `organizationMembership.deleted` |
| — | HR-15 Rehire / reactivate terminated staff | S | 🟢 Built 7-22 (uncommitted — commit on Gary's word), **staging pass pending** (checklist incl. the Tommy Thomas end-to-end, which then unblocks the outstanding UM-1 checks) — no schema change, no migration, no new deps; webhook + sync routes untouched. `POST /api/staff/[id]/reactivate` (ADMIN / in-scope MANAGER, mirrors terminate; 409 unless TERMINATED) flips status → ACTIVE + clears `terminatedAt`, never touches records (terminated-not-deleted holds both directions); `GET` same route = dialog preflight returning live Square status. **Stale-login hygiene**: `terminateStaffMember` now unlinks `userId` + drops the login's StoreUserAssignments inline (staging evidence: webhook-only unlink failed — Tommy kept a stale link that 409-blocked re-invite), webhook stays as backup; reactivate clears stale links defensively for pre-fix rows. `/staff/[id]` Reactivate button + dialog: history-intact copy, **Fork-1 Square warning** when Square still reports INACTIVE (sync reconcile stays absolute-state per Gary; transition-based reconcile documented as follow-up), "send login invite" checkbox chaining the existing invite flow (separable). **Fork-2 (Gary): Policy A — old signatures stand**; version bump remains the compliance lever; additive `rehiredAt` is the documented Policy-B upgrade path (see DECISIONS.md). Directory: "Terminated" badge on `/staff` rows (HR-14(b) badge half early; hide-toggle stays HR-14). Square rehire (INACTIVE→ACTIVE) confirmed NOT to flow into Froot — reactivation is always Froot-side |
| 8 | M-1 Keva data migration (optional) | M | ⬜ Unblocked (I-6 done) |
| 9–11 | N-1/N-2/N-3 Nutrition | M | ⬜ Not started |
| 12 | X-1 Activation & QA | S | ⬜ Not started |
| — | L-4 · Labor assignment layer — named-person shift assignment, GM days-off scheduling, per-person breaks, Square push (Shifts Plus gated) | L | ⬜ Not started (reset roadmap; after L-2) — the interactive assignment layer on top of the L-3 plan; shares the Square OAuth scopes with L-2. Confirm L-3/L-4 order when scoping past L-2. |
| — | L-3 · Weekly Plan report — floor-first daily split (new default), GM 40-hr cap, cross-day rebalancing | L | ✅ **Promoted to PRODUCTION 2026-07-21** (merge commit `9743899`; carried the full HR/Labor backlog — `main` was 53 commits behind staging). Built + verified on staging; docs done; `next build` + `verify-labor-{budget,coverage}` green. Pre-prod decision resolved: **coverage stays sales-inferred for v1**; populating `StoreHours` (real operating hours) deferred as a future additive upgrade (see `DECISIONS.md`). Additive migration `20260721163612_labor3_daily_split_policy_weekly_day_hours` (`LaborDailySplitPolicy` enum + `LaborSettings.dailySplitPolicy` default FLOOR_FIRST; `WeeklyDayHours` per-date override) applied to dev + staging; partial index `LaborSettings_org_default_key` preserved. Shared `getWeeklyDayPlan` (`labor-plan.ts`) turns the weekly budget into a per-day plan: **floor-first split** — guarantee each open day's floor (one body every open hour, minus the GM's capped credit) before distributing the remainder by sales weight (`splitWeeklyHoursToDaysFloorFirst`); **GM 40-hr weekly cap** (`capGmFloorCredits`, floor-math only — the band still renders in full, day-picking is L-4); day weights fall back to the **same sales-derivation the settings editor shows** (no even-split); open windows **inferred from trailing sales** when `StoreHours` is empty (nothing populates `StoreHours` yet — flagged). New `/labor` Weekly Plan page (week strip: forecast, last-year same-weekday +delta, hours, projected %, weather chip, coverage status from the engine; selected-day detail reuses `/api/labor/coverage`, single headcount axis, GM band, peak + break guidance) + `/api/labor/{weekly-plan,day-hours}`; `dailySplitPolicy` toggle + shared info explainer on `/settings/labor` and the coverage-card floor warning; nav item + links from both dashboard cards. Both gates enforced. See `LABOR.md`. |
| — | L-2 · Team/Labor API — actual worked hours + wages → actual labor % vs budget (Coverage "Actual" toggle); Nevada daily/weekly OT warning (warn, don't model) | L | ⬜ Not started (reset roadmap; locked after L-1) — the real actuals loop (nothing shipped uses live worked-shift/wage data yet); additive Square OAuth scopes `TIMECARDS_READ/WRITE` + `EMPLOYEES_READ`. |
| — | L-1 · Full manual staging pass of the shipped labor foundation | — | 🟢 Done (Gary) — end-to-end staging verification of every shipped surface (budget + acceptance case, auto-forecast + override/revert, hero gauge/empty/floor-exceeds warning, coverage step line, GM-on-floor + supervisor-gap, weekly→daily split, weather ±%, 4-week navigator). No code; gated all further build work. |
| — | Labor foundation (pre-reset) — per-store budgets, demand-shaped budget-capped coverage, salaried GM on floor, 4-week forward scheduling | L | 🟢 Built 7-20 (old 0–4 numbering retired per Reset Brief; `next build` + both verify scripts green) — additive migration `20260721010000_labor3_gm_onfloor_window` (2 nullable Int cols on LaborSettings) applied to dev. Per-store `LaborSettings` (`resolveLaborSettings` + scope-picker editor, roll up to org); GM-only-salaried re-seed; coverage rewritten demand-shaped + budget-capped (largest-remainder, floor-1 opener/closer, no fixed minimums) with the salaried GM counted on floor open→14:00 as body+supervisor (`understaffedBudget`/`supervisorGap` flags); future-day demand = 4-week same-weekday average, Budget+Coverage share a day/week navigator (this week…+4); `LaborDaypart` slimmed to supervisor windows (minHeadcount retained-unused); "adjusted from N" label fixed. See `LABOR.md` / `DECISIONS.md`. |
| — | Labor foundation (pre-reset) — auto-forecast, total-sales, daily split, adjustment, coverage engine | L | 🟢 Built 7-20 (`next build` green, `verify-labor-budget.ts` + `verify-labor-coverage.ts` green) — additive migration `20260720230000_labor2_daysplit_daypart_adjustment` (LaborDaySplit/LaborDaypart/LaborDayAdjustment) applied to dev. **Total sales only** — `LaborSettings.denominator` + `SalesForecast.projectedDelivery` deprecated (retained, no drops), removed from UI/IO. **Auto-forecast**: budget derives the week's total from Forecasting `DailyGoal` (`getWeeklyForecast`: TREND default, MANUAL override) — no data entry. **Daily split** (`splitWeeklyHoursToDays`, per-store `LaborDaySplit` weights auto-derived from trailing sales, editable) + **per-day adjustment** (`applyDayAdjustment` / `LaborDayAdjustment`, ±% + reason, scales hourly only, salaried fixed). **Coverage engine** (`computeDailyCoverage`): min-staffing floors + supervisor rule vs `StoreHours` + `LaborDaypart` blocks (seeded Opening/Mid/Close on enable). APIs `/api/labor/{day-split,day-adjustment,daypart}`; budget/coverage cards upgraded; day-split + dayparts editors on `/settings/labor`. See `LABOR.md`. |
| — | Labor foundation (pre-reset) — config + weekly budget & dashboard | L | 🟢 Built 7-20 (`next build` green, `npx tsx scripts/verify-labor-budget.ts` green) — **Config:** additive migration `20260720000000_labor0_positions_settings_forecast` (LaborPosition/LaborSettings/SalesForecast, **money in DOLLARS as Decimal(10,2)** per Gary's F1; partial unique index guarantees one org-default LaborSettings row) applied to dev; two-gate flag `laborModuleAvailable()` (env `LABOR_MODULE_AVAILABLE` + `LABOR_INTERNAL_ORG_IDS`) on sidebar/settings/dashboard; `requireLaborView` (any role, read) + `requireLaborContext` (ADMIN+MANAGER write, unlike Forecasting); `/api/labor/{toggle,settings,positions}` CRUD w/ seed-on-enable + `scripts/seed-labor-positions.ts`; Settings Labor card + `/settings/labor` config page. **Budget engine:** rounding rule locked (floor-to-tier, no full-step-down; spec step-2 + acceptance case corrected to $14,900→18.8%); pure `computeWeeklyLaborBudget` (dollars in, integer-cents internally) reproduces $182 / 18.8% exactly; `/api/labor/forecast` upsert (weekStart→Monday) + `/api/labor/budget` (derived); Labor Budget hero card (total hrs, projected-% gauge vs target, conservative tier/buffer, floorExceedsBudget warning, empty-state forecast entry). **Coverage card:** `/api/labor/coverage` + Labor Coverage card (recharts stepAfter line, single headcount axis, demand shape from the Sales Performance hourly source, floor-1 open hours, peak shaded, "Recommended · guidance", beneath the Sales card, follows its viewed day). See `LABOR.md`. Later (daily templates, Square forecasting, actuals/OT) were out of scope |
