# SESSION PROMPT — HR-20: TrainingCategory ENTITY + ADDITIVE MIGRATION

**Date commissioned:** 2026-08-10
**Session type:** BUILD, one phase (HR-20), no riders beyond the one named on the row. Audit-first with hard stops. Two-commit pattern: work commit, then docs commit citing the work SHA.
**Executor:** Claude Code. **Rulings authority:** Gary. Everything ruled below is settled — build to it, don't re-open it. Anything NOT settled below that smells like a decision is a RULING NOW stop.
**Git:** Commits gated by green `npm run build` (lint is not a gate — DEBT-33). Commit, never push — Gary runs all pushes.

---

## GROUND RULES

- Source of truth is the repo at HEAD. Required reading before any plan: `CLAUDE.md`, `docs/MIGRATIONS.md`, `docs/prompts/2026-08-10_TRAINING_AUDIT.md` (the evidence base for everything here), HR-20/HR-21/HR-22's rows in `docs/ROADMAP.yaml` (filed in 683d33a — note the amendment this session must file, §DOCS below), and the three existing category-pattern implementations (IngredientCategory, LossReason, TemplateType — schema + routes + seed sites) since this is the fourth verse of that song.
- Expected repo state at start: branch staging, HEAD 683d33a or a descendant containing it. Verify and report. `src/generated/` excluded from all greps.
- Migration discipline is absolute: ADDITIVE ONLY — no column drops, no data-destructive statements. The full SQL is presented to Gary and approved BEFORE any migration runs anywhere. Echo the database host before executing. Dev first; staging/production promotion is not this session's job.
- HARD STOP #1: after required reading, present the build plan (schema diff, migration SQL draft, file-by-file change list, seed mechanics) and WAIT for Gary's explicit approval before editing anything.
- No database queries for evidence — the evidence is already gathered and recorded below. If the session believes it needs a query, that is a STOP.

## EVIDENCE OF RECORD (gathered by Gary in the Neon console, 2026-08-10, branch ids verified in-output — do not re-verify, do not contradict)

**§8.1 (category seed source):** Production (br-sparkling-block): Keva org `cf888f2d-...` has 12 modules with 12 DISTINCT subject values, one module each — subjects are subtitles ("MODULE 3: CLEANING & SANITATION BASICS", "How to assemble a Crathco Bubbler from beginning to end."), not categories. Second org has 1 module, null subject. Staging: "Empathy" ×4 + one other (test fixtures). Dev: near-empty.
**Consequence, RULED by Gary (option i):** NO BACKFILL. The migration mints zero categories from `subject`. See SETTLED RULINGS.

**§8.2 (duplicate assignment pairs):** ZERO rows on all three branches (br-sparkling-block, br-square-feather, br-broad-wave), verified 2026-08-10.
**Consequence, per R-b's condition:** the unique constraint on TrainingAssignment (module × staff) SHIPS in this session's migration.

## SETTLED RULINGS THIS SESSION BUILDS TO

1. **No backfill (Gary, 2026-08-10, on §8.1's evidence).** `subject` turned out to be a subtitle field, not a proto-category. Therefore:
   - The migration creates the entity and constraint only — it never reads `subject`.
   - `subject` is LEFT COMPLETELY ALONE: not written, not mirrored, not resolved against, not removed from any form or select. It keeps doing its subtitle job. The TPL-1a "mirror" framing in HR-20's filed row text is AMENDED by this ruling (see §DOCS).
   - All existing modules start uncategorized. Category is OPTIONAL (R-a), so this is a legal resting state; every surface's empty/uncategorized state must render sanely.
   - CSV by-name resolution ships against the NEW category field only (resolve a category name to the org's category by name; unresolvable → module imports uncategorized with the miss reported in the import summary — never a silently invented category unless the audit finds the CSV import's existing convention says otherwise, in which case RULING NOW).
2. **Unique constraint ships** — `@@unique` on TrainingAssignment (trainingModuleId, staffMemberId), justified by §8.2's zero. Future re-assignment, if ever built, uses a cycle column (HR-15b shape) — recorded, not built now.
3. **Starter seed (R-a: convenience, not load-bearing):** six categories for any org with zero TrainingCategory rows — New Hire Training, Manager Training, Procedures Training, Operations Training, Product Training, Smoothie Training — visible, renameable, deletable (DEBT-59 style: offered defaults, never values silently stamped on records; seeding CATEGORY ROWS is fine, stamping MODULES is not — no module gets a category assigned by this session's code, ever). Seed mechanics (migration-time for existing orgs vs. org-creation hook vs. both) follow the existing pattern instances — present the choice with the precedent cited in the plan; if the three prior instances disagree with each other, RULING NOW.
4. **Colors are badge-preset KEYS** from `src/lib/badge-presets.ts` — never class strings in the DB (Tailwind 4 constraint). Assign six distinct existing keys to the starter seed; propose them in the plan.
5. **Entity shape follows TemplateType** (the audit's §7 precedent): per-org, `@@unique([organizationId, name])`, color as preset key. Nullable FK `categoryId` on TrainingModule.
6. **Permissions (R-e):** any new/changed routes use the existing inline pattern via the shared guards in `api/hr/training/access.ts`. Zero registry edits. PERM-5C's boundary comment untouched.

## SCOPE

**In:**
- Schema: TrainingCategory entity; nullable `categoryId` FK on TrainingModule; unique constraint on TrainingAssignment (module × staff). One additive migration.
- Starter seed per ruling 3.
- Module form: category select (optional, clearable), reading the org's categories. The read path this select needs ships here; if that means a minimal list endpoint, it is read-only — category CRUD (create/rename/recolor/delete/reassign) is HR-21's, not this session's. If the form's architecture makes that split unnatural, STOP and present the argument; do not quietly pull HR-21 work forward.
- CSV import/export: category column handled by-name per ruling 1; export carries the category name.
- RIDER (named on the row, closed here since this session touches the write path): module POST/PATCH `storeIds` org-validation gap (audit §9 item 1, PERM-7's class) — validate submitted store ids against the org. HR-21 verifies.
- Wire-only `subject` carry in `/my/training` (audit §9 item 5): this session touches module selects — carry or cut per what the plan proposes; either is fine, say which.

**Out (do not touch):** the /hr/training page markup beyond the form (HR-21 owns badges/chips/toggle/Manage dialog); any assignment write path (HR-22); anything HR-13/HR-16/HR-18/HR-19; `subject`'s future.

## DOCS COMMIT (second commit, citing the work SHA)

- HR-20's row: dated amendment (preserve-and-mark — original text stands, amendment prepended with date): backfill REMOVED from scope by Gary's 2026-08-10 ruling on §8.1's evidence (subjects are subtitles; seed source dry); `subject` mirror framing superseded — subject untouched, not a mirror; constraint condition MET (§8.2 zero on all three branches, 2026-08-10) and constraint shipped in this row's migration. Status flip per board conventions.
- Record the §8.1/§8.2 evidence summary (with branch ids) on the row or per board convention for measured evidence.
- The storeIds rider: marked closed on HR-20's side, HR-21's verify obligation noted.
- DEPLOY_LOG standing note: append this session's commits to the docs-only reconciliation list per the note's own extend-by-dated-line rule.

## DONE CRITERION

Plan approved → schema + migration SQL approved by Gary explicitly (separate approval from the plan — migration SQL is its own gate) → migration run on DEV ONLY (host echoed) → code built → green `npm run build` → work commit → docs commit citing work SHA → report with both SHAs, unpushed count, and the exact migration SQL as run. Staging/production migration and promotion are separate, later, Gary-driven steps.
