# SESSION PROMPT — HR-26: MANAGER TRAINING ACCESS (read + assign, no authoring)

**Date commissioned:** 2026-08-12
**Tier:** 3 — role gates, an API surface, and a scoped write path.
**Session type:** BUILD for items 1–3, with hard stops. **Item 4 (capability toggles) is NOT built in this session — it comes back as a priced packet.** Two-commit pattern (work, then docs citing the work SHA).
**Executor:** Claude Code. **Rulings authority:** Gary.
**Git:** Gated by scoped eslint over touched files && green `npm run build`. Commit, never push.

---

## GROUND RULES

- Required reading: `CLAUDE.md`; `docs/prompts/2026-08-11_TRAINING_ACCESS_AUDIT.md` (§2, §3, §6, and the R-i packet); the HR-24 row and what shipped in `0b1cf51` — **HR-24 is the architectural model for this session**; the HR-22 row and `assignments/bulk/route.ts` + `bulk/recipients` + `shared.ts`; `api/hr/training/access.ts` in full; HR-25's row.
- Expected repo state: branch staging, containing `0b1cf51` and `0ec4f20`. Verify and report. Note whether `main` is still behind and by how much.
- **No schema changes and no database queries.** Either is a STOP.
- HARD STOP #1: present the build plan **and item 4's priced packet** and WAIT for approval before editing anything.

## THE OPERATOR NEED (Gary, 2026-08-12, after testing HR-24 on staging)

STORE read access works. MANAGER now has *less* training access than STORE, which is backwards. Managers need:

1. **Assign training to their team — individually (works today via `/staff/[id]`) and in bulk — for their assigned locations only.**
2. **View training modules**, the same read-only surface HR-24 built for STORE.
3. **No editing, no deleting, no authoring.**
4. *(not built here — see the packet)* ADMIN-toggleable capability overrides for manager training features (create / edit / delete / categorize), **default off**.

## SETTLED — build to these

1. **MANAGER gets the HR-24 read surface.** Same `{kind: "read"}` mode, same trimmed `GET /api/hr/training/library` route, same UI suppression of authoring affordances. Do not build a third variant — extend the tier that exists.
2. **MANAGER gets bulk assign, scoped.** The audit established that `POST /api/hr/training/assignments/bulk` and `bulk/recipients` **already admit store-scoped MANAGER** at `requireHrTrainingManageAccess` — only the page gate kept managers out. Confirm this at HEAD and report it; if true, this item is a gate-and-UI change, not a new write path. Every scope rule HR-22 shipped (`getUserStoreScope`, org ownership, corporate exclusion from store expansion, ACTIVE-only, the counts-sum-to-input response) applies unchanged and must not be weakened.
3. **R-c is now fully superseded.** It ruled `/hr/training` ADMIN-only on 2026-08-10 and said so reversibly-by-row on demonstrated manager need. HR-24 superseded it for STORE-as-reader; this row supersedes it for MANAGER-as-reader-and-assigner. **Record that history on the row** — R-c was not wrong, its condition was met.
4. **No authoring for MANAGER.** All 16 `requireHrTrainingAccess` (ADMIN) sites stay ADMIN-only: create, edit, delete, import, export, upload-url, resources, and all five category routes. The report must confirm each still refuses MANAGER after the change.
5. **Category management stays ADMIN-only.** MANAGER sees category badges and filter chips (read), never the Manage Categories dialog.

## SCOPE (items 1–3)

**In:**
- `/hr` Training card gate — add MANAGER alongside ADMIN and STORE, with copy appropriate to a manager (reads and assigns, does not build).
- `/hr/training` page gate — admit MANAGER; pass the existing `canManage` split so authoring affordances stay hidden. **Determine whether one flag still suffices** now that three tiers view the page with different action sets (ADMIN: everything; MANAGER: read + assign; STORE: read only). If a second flag is needed (e.g. `canAssign`), propose it in the plan — do not invent a role-conditional tangle.
- **Bulk Assign entry point visible to MANAGER in both card and list views** — HR-21's parity rule holds: a feature reachable in one view and not the other is a defect. HR-22 left the slot inside the shared `ModuleActions` cluster precisely so this lands in both at once.
- The bulk-assign dialog for MANAGER: recipients limited to their scope by the existing server rules. **The dialog must not offer what the server will refuse** — if "Everyone in my scope" or the store list needs to render differently for a manager, say how.
- Module read surface for MANAGER: the `{kind: "read"}` mode. **Decide and state what MANAGER gets for attached files.** Note the tension: HR-24 gave STORE nothing (`resourcesAvailable={false}`), but MANAGER already has manage-tier download access through other paths, and HR-25's row records that managers are unaffected by the completion gate. Propose; if the answer changes any guard, STOP.

**Out (do not touch):** item 4 and anything in the capability registry; HR-19; HR-25's file gate; STORE's surface; any authoring or category route; `/my` surfaces; HR-13; HR-18; HR-23.

## ITEM 4 — PRICED PACKET ONLY, BUILD NOTHING

Gary wants ADMIN-toggleable manager training capabilities (create / edit / delete / categorize), **default off**. Unlike R-i (which asked to *grant* STORE a capability and was ruled unrepresentable), this is expressible in a deny-list — but only after several things that do not exist today. Establish and price, with file/line evidence:

- **Nothing in the HR training surface calls `can()`.** The audit found `hr.training.author` and `hr.training.manage` exist in the registry but are absent from `ENFORCED_CAPABILITIES`, and every training route uses R-e's inline guard pattern. So a toggle in the grid today would **do nothing** — which the grid's own rule forbids. Price what it takes to make these capabilities real: which routes must consult `can()`, and whether that is a partial HR-19 (**R-e explicitly ruled that a phase must not quietly begin the registry migration**).
- **"Default off" semantics.** A deny-list expresses "off" only if the denial is written. Price the options: seed denials at user creation; invert the meaning for these specific keys; or treat absence-of-grant differently. Name what each does to existing manager accounts and to the grid's copy ("restrict below what their role allows").
- **Blast radius.** If authoring becomes a MANAGER role baseline that admins subtract from, every existing manager holds it until someone turns it off. State how many manager accounts exist today from the `/users` surface, not from a query.
- **Recommend nothing. Price everything.** This likely becomes its own row (HR-27) and may depend on HR-19 landing first.

## WHAT THE REPORT MUST STATE

- The per-role table (ADMIN / MANAGER / STORE / STAFF) in HR-24's format, updated.
- Confirmation that all 16 ADMIN-only sites still refuse MANAGER, named individually.
- Confirmation that HR-22's scope enforcement is unchanged — quote the guard line and the scope check, and state that no bulk-assign rule was relaxed.
- What MANAGER gets for attached files, and proof no visible affordance 403s.

## DOCS COMMIT

- HR-26's row: rulings as Gary ruled them, R-c's full supersession recorded, what shipped.
- Item 4 filed as its own row (proposed HR-27) carrying the packet's findings — **not started**.
- Promotion-order note if any still applies; DEPLOY_LOG standing note extended by dated line; session prompt rides the commit.

## DONE CRITERION

Plan + item 4 packet presented → approved → build → scoped eslint clean && green `npm run build` → work commit → docs commit citing work SHA → report with both SHAs and the per-role table. Commit, never push. Browser verification is Gary's, after the push.
