# SESSION PROMPT — HR-24: STORE READ ACCESS TO THE TRAINING LIBRARY

**Date commissioned:** 2026-08-11
**Session type:** BUILD, one phase, audit-first with hard stops. Two-commit pattern (work, then docs citing the work SHA).
**Tier:** 1 — role access on a confidential-content surface.
**Executor:** Claude Code. **Rulings authority:** Gary. Settled rulings are listed below and are not re-opened. **Three rulings are NOT settled (R-g, R-h, R-j) — they come to Gary at Hard Stop #1 as priced packets, and the session builds nothing that depends on them until he rules.**
**Git:** Gated by scoped eslint over touched files && green `npm run build`. Commit, never push.

---

## GROUND RULES

- Source of truth is the repo at HEAD. Required reading first: `CLAUDE.md`; `docs/prompts/2026-08-11_TRAINING_ACCESS_AUDIT.md` **in full** — it is this session's evidence base, especially §2 (the page gate), §3 (STORE's current surface), §4 (the preview surface and the download route), and the R-g / R-h / R-j packets; the `/hr/documents` implementation end to end, including `canReadHrDocument` in `hr-files.ts` — **that module is the architectural model for this session**; HR-25's row and what shipped in `f5d2883`; the HR-6 / HR-17 / HR-18 rows.
- Expected repo state: branch staging, staging at `5c01807` or a descendant. `main` is at `ec42265` and is **three commits behind** — reason against staging, not main. Verify and report.
- **No schema changes and no database queries.** Either is a STOP.
- **HR-25 is a prerequisite and is on staging but NOT on production.** This session must not be promoted to production ahead of HR-25 — note that on the row so the promotion order is written down, not remembered.
- HARD STOP #1: after required reading, present the build plan **and the three unsettled ruling packets** and WAIT for Gary's rulings and approval before editing anything.

## SETTLED — build to these, do not re-open

1. **STORE gets a read-only training surface.** Reading the procedure on the floor, on the shared iPad, is the point (Gary's water-heater scenario, 2026-08-11).
2. **STORE never edits, assigns, deletes, or authors.** The audit established these already hold: 14 authoring routes and 11 assignment routes refuse STORE at two guard lines. **This session must not weaken any of them** — the report must confirm each still refuses STORE afterwards.
3. **STORE assignment capability (the old item 5) is NOT built.** R-i's options were all priced above their worth; PERM-5's row already rules that granting a role something it lacks means a different role, not a wider override. Out of scope entirely.
4. **The model is `/hr/documents`, not a new invention:** read access broad, manage ADMIN-only at the API, UI controls hidden for non-managers, and content visibility expressed in a **named policy function** rather than scattered inline conditions.
5. **R-c is superseded, not overturned by convenience.** The audit established `/hr/training`'s ADMIN gate was a consequence of the page being an authoring tool (its own comment says so), and R-c ruled on STORE-as-**assigner**, never STORE-as-**reader**. Record this on the row in those terms.
6. **MANAGER page access is NOT part of this session.** It stays a separate future row. Folding it in re-opens R-c's actual subject inside a session scoped to STORE reading.

## UNSETTLED — present as priced packets at Hard Stop #1, build nothing that assumes an answer

- **R-g — the read surface's shape.** Reuse HR-17's preview (`TrainingModuleView`) for STORE, or a distinct STORE view? Price both, including what each exposes. **Note HR-25 changed the ground here:** `resourcesAvailable` is now a required prop, so whatever STORE's surface is, this session must decide explicitly what STORE gets for attached files — and a visible link that 403s is forbidden (the audit's own §3.3 finding, and the reason HR-25 shipped server refusal and UI suppression together).
- **R-h — scope of what STORE sees.** Every module in the org, or only modules applicable to that store (the `appliesTo` / store-assignment filter that already exists on the assign path)? The water-heater case argues for filtering; an org-wide food-safety module argues against. Price both; state which is cheaper to change later.
- **R-j — content classification.** Does the library need a confidential/operational distinction at all, or does STORE simply get everything readable? If a distinction is wanted, what carries it: HR-20's `TrainingCategory`, a per-module flag, or store applicability. **Note the cost asymmetry:** a per-module flag is a schema change and therefore NOT buildable in this session — if Gary rules that way, this session stops and the flag becomes its own row.

The audit recorded two SQL queries for R-j and R-g's option choice. **Do not run them.** Present them in the plan, formatted for Gary's Neon console with the branch id selected inside each, and state exactly which packet each one decides.

## SCOPE (subject to the three rulings)

**In:**
- The `/hr` Training card's gate (`hr/page.tsx:108`, currently `role === "ADMIN"`) and the `/hr/training` page gate, opened to STORE **read-only**, following the `/hr/documents` shape.
- A named policy function for training readability — the `canReadHrDocument` analogue — so the rule is one function with a name, not conditions scattered across pages.
- UI suppression for STORE on the training surface: no create, edit, duplicate, delete, archive, assign, bulk-assign, Manage Categories, import, or export affordances. **Server guards stay as they are — the UI hiding is in addition to them, never instead of them.**
- The list-view / card-view toggle, category badges, and filter chips work for STORE as they do for ADMIN (HR-21's parity rule: a feature reachable in one view and not the other is a defect).
- **RIDER (audit triage, ROW #1):** `/hr` shows STORE a Staff Directory card that bounces. This session touches that exact card grid — fix it (hide the card for STORE) and report it. If the fix turns out to be non-trivial, STOP rather than expanding.

**Out (do not touch):** any assignment path; any authoring route; HR-25's file gate; the capability registry and HR-19; HR-13; HR-18; MANAGER page access; the `/my` self-service surfaces; `subject`.

## WHAT THE REPORT MUST STATE

- **Per role, what the training surface looks like:** ADMIN, MANAGER, STORE, STAFF — what each sees, what each can click, what each gets. A table, like HR-25's.
- **Confirmation that every authoring and assignment route still refuses STORE**, named individually, after the change.
- **What STORE gets for attached files** — the explicit answer to R-g's file question, and proof no visible affordance 403s.

## DOCS COMMIT (second commit, citing the work SHA)

- HR-24's row: the three rulings as Gary ruled them, what shipped, and R-c's supersession recorded in the terms of settled item 5 above.
- **The promotion-order note:** HR-24 must not reach production ahead of HR-25.
- The ROW #1 rider marked closed.
- DEPLOY_LOG standing note extended by dated line.
- Session prompt file rides this commit.

## DONE CRITERION

Plan + packets presented → Gary rules R-g/R-h/R-j and approves → build → scoped eslint clean && green `npm run build` → work commit → docs commit citing work SHA → report with both SHAs, unpushed count, and the per-role table. Commit, never push. Browser verification on staging is Gary's, after the push.
