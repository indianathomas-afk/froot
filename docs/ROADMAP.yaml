# Froot roadmap — single source of truth for phase status.
# Supersedes docs/ROADMAP.md (now a pointer), the old CLAUDE.md "Phase Status"
# section, and the external sheet. If any of those disagree with this file,
# this file wins. Narrative history (verification fixtures, migration names,
# decision cross-refs) lives in docs/ROADMAP_ARCHIVE.md, frozen 2026-07-24.
#
# UPDATE RULE: every Claude Code session updates its phase's entry here before
# the session is done. See "Session completion rules" in docs/WORKFLOW.md.
#
# status: planned | in_progress | staging | shipped | verified | withdrawn
#   staging  — merged to `staging`, on Vercel preview, NOT in prod
#   shipped  — merged to `main`, live in prod
#   verified — shipped AND smoke-tested against real store data
#   withdrawn — retracted, not fixed; kept for the record. Counts as RESOLVED
#              on /internal/roadmap and carries NO `commits` (nothing was
#              fixed, so there is no SHA to cite). Added 2026-08-01.
#   GAP: no status fits an audit-only phase with no shipped code (SQ-1 sits in "verified") — a future "closed"/"documented" would.
#
# blockers — problems in a phase regardless of status (unset env vars,
#            prod-promotion gates, unverified pixel work).
#   AN ENTRY IS ONE OF THREE THINGS: a BARE STRING (LIVE), or
#   `{narrowed: true, text: >}` (STILL BLOCKS, but on strictly less than it
#   originally asserted), or `{resolved: true, text: >}` (CLOSED, kept for the
#   record). "Anything here blocks" was true when written
#   and stopped being true as soon as the preserve-and-mark convention started
#   keeping closed gates in the array: by 2026-08-01, 8 of 19 entries were
#   closed and /internal/roadmap counted every one of them as live. Corrected
#   by P-4 the same day, which added the flag.
#   NARROWED is DEBT-41's third state, added 2026-08-08 after two entries sat
#   half-closed with nowhere to say so. It is COUNTED SEPARATELY and it STILL
#   BLOCKS: the panel reads "16 live · 2 narrowed", the phase stays in the
#   blocked list and in the "Phases blocked" tile. Use it when part of what an
#   entry asserted is answered and a NAMED REMAINDER is still live — say what
#   remains in the entry's own text, since the flag carries no second field.
#   A MISSING FLAG MEANS LIVE — DEBT-14's "a missing status means OPEN" one
#   level down, and the safe direction: an unclassified entry keeps blocking.
#   NEVER WRITE `resolved: false` OR `narrowed: false`. Each is exactly the
#   value the flag's ABSENCE already carries, and either one destroys the
#   missing-flag-means-live default. The type makes them unwritable (three
#   union members, not one object with optional flags) — keep it that way.
#   NEVER edit an entry's text to mark it resolved or narrowed. The prose is the
#   record and on several rows the ORDER of the entries is the lesson; the flag
#   is additive and sits over text that does not change.
#   DO NOT infer the state from the leading words. Live entries open "VERIFIED
#   STILL TRUE" (F-5) and "CONFIRMED LIVE" (F-4), and PERM-6's two closed
#   entries carry no marker at all — they are closed by the entry prepended
#   ABOVE them. Only the flag is read.
# deferred — scope intentionally cut.
# open     — unresolved questions that aren't yet blockers.
#   Both are still BARE STRINGS ONLY and both still keep resolved entries in
#   the array (PERM-2 `deferred`, and PERM-6/PERM-7/SEC-1 `open`). Known and
#   deliberately not fixed — no count on the page reads either field. DEBT-42.
#   NOTE: blockers/deferred/open/tags exist ONLY on `phases`. `bugs:` accepts
#   id/title/status/shipped/commits/notes; `debt:` accepts
#   id/title/status/commits/notes.
#   Adding `open:` to a debt row type-errors the build (see src/lib/roadmap.ts)
#   — fold it into notes instead. Learned the hard way 2026-07-27.
# tags     — [pre-launch] must close before external merchants.
#
# `shipped:` dates are when the work reached main/prod; where a phase was built
# earlier and promoted later, the build date is in notes.

meta:
  # `updated:` was DELETED 2026-07-30 (DEBT-24, Gary's ruling). It was the
  # fallback for the /internal/roadmap "last updated" label and nothing
  # maintained it, so the one path designed to save you held a stale date.
  # With it gone the chain is git commit date -> unknown, and "unknown" is
  # honest where a stale date is convincing. Nothing else in this block is read
  # by any code — verified 2026-07-29: the generator reads only raw.phases,
  # raw.bugs, raw.debt (and, until now, raw.meta.updated).
  sources_retired: [docs/ROADMAP.md, "CLAUDE.md#Phase Status", external sheet]
  note: >
    Reconstructed 2026-07-24 from docs/ROADMAP_ARCHIVE.md, docs/prompts/*, and
    git log. TEN prod promotions to date: 9743899 (2026-07-21, L-3 + full
    HR/Labor backlog), 942bc59 (2026-07-23, HR-8 through STAFF-1 batch),
    59a6cdc (2026-07-24, HR-11b + HR-11c), 1ba059c (2026-07-25, HR-17),
    c463af3 (2026-07-26, PERM-1 + SEC-1 + the BUG-3 fix), 06b1561
    (2026-07-27, PERM-2 + PERM-3 + BUILD-1 + SQ-1 docs + the P-3 roadmap
    dashboard), 746c1be (2026-07-29 morning, PERM-6 + PERM-7 + DEBT-8 +
    DEBT-10 + DEBT-14), 493175e (2026-07-29 evening, BUILD-2 + its two
    migrations), 63407be (2026-08-01 afternoon, DEBT-1 + DEBT-2 + the 07-30
    debt batch) and 97ed309 (2026-08-01 evening, the DEBT-SWEEP batch).
    The last four were added 2026-08-01 by DOCS-2; only 493175e had a
    DEPLOY_LOG entry before that session, and all four are now logged.
    Two further pushes to main carried DOCS ONLY and are deliberately not
    counted as promotions — 17dc723 (2026-07-27) and 18220bb (2026-07-29);
    they are named inside 746c1be's DEPLOY_LOG entry so main's push history
    still reconciles. NOTE ON METHOD: every promotion from 746c1be onward was
    a FAST-FORWARD, so `git log --merges` finds none of them and there is no
    merge commit to prompt an entry — which is how 746c1be went unlogged for
    three days. See DEBT-38. Both 1ba059c and
    c463af3 were written into docs/DEPLOY_LOG.md at the 2026-07-27 reconcile;
    that log previously stopped at 59a6cdc. The promotion attribution above was
    re-verified from git on 2026-07-27 via `git log --ancestry-path` and
    `git merge-base --is-ancestor`: PERM-1 (6f70465) and SEC-1 (ecee728) rode
    c463af3 on 07-26 and are NOT ancestors of 1ba059c, so they did not ship in
    the 07-25 HR-17 promotion.
    Staging and main are LEVEL as of 2026-08-01 at 97ed309 — verified from git,
    both refs at the same commit. They were previously recorded level at
    2026-07-27 / 06b1561, which was true when written and is kept here as the
    reference point for the backlog that merge cleared (P-3 3902d5c, the SQ-1
    entries 056943f, PERM-2 979da0b, PERM-3 b8f32bb, BUILD-1 6a77e68 — all
    promoted in it, rollback tag `pre-staging-merge-20260727-1427`). The two
    branches have gone level and diverged several times since; the SHA is the
    part that goes stale, not the fact. SQ-2 remains the
    one divergence of record: cherry-picked to main alone as 9dc6dc0 while
    staging carried 81be8d9, so the same change exists under two SHAs and both
    are now ancestors of HEAD. HR LAUNCHED
    in production 2026-07-24 (HR_MODULE_AVAILABLE=true added to the Vercel
    Production scope + redeploy).

phases:

  # ---- Platform / dashboard ----

  - id: P-0
    track: platform
    size: XL
    status: shipped
    shipped: 2026-06-26
    commits: [1cfdf76]
    title: "Initial platform build — Phase 1 core (checklists, templates, stores, users, Square OAuth)"
    notes: >
      From-scratch build per docs/prompts/INITIAL_BUILD_PROMPT.md. The id P-0
      was assigned during the 2026-07-24 roadmap reconstruction (the build
      predates phase ids).

  - id: D-1
    track: platform
    size: M
    status: shipped
    shipped: 2026-07-06
    commits: [1b8160f]
    title: "Dashboard redesign"

  - id: P-1
    track: platform
    size: S
    status: shipped
    shipped: 2026-07-09
    commits: [251c3a6]
    title: "Templates export/import"
    notes: Never on the original tracking sheet or ROADMAP.md.

  - id: P-2
    track: platform
    size: S
    status: shipped
    shipped: 2026-07-09
    commits: [cf9393d, e14f14b, 9c0c8d4]
    title: "Sales-metric accuracy pass"
    notes: >
      Net sales excludes tips; bucket by created_at to match Square's Sales
      Summary; PAID-order counting. Never on the original sheet or ROADMAP.md.
      (Seed said shipped 7-10; commits are 7-09.)

  - id: IG-1
    track: platform
    size: M
    status: shipped
    shipped: 2026-07-10
    commits: [b1fce8b]
    title: "Instagram integration — org-level connect, /instagram page, dashboard strip"
    blockers:
      - >
        POINTER REPAIR 2026-08-13 — THIS ROW QUOTES F-4, AND WHAT IT QUOTES HAS
        BEEN CORRECTED. Prepended; neither preserved passage below is edited,
        per the never-edit-an-entry's-text rule. Session:
        docs/prompts/F-4_FINAL_AUDIT.md.
        TWO ENTRIES BELOW CITE F-4 AND THEY FARE DIFFERENTLY.
        The ORIGINAL FRAMING block calls F-4 "dead since 2026-07-10" and "worse
        than recorded". The severity half is now WRONG: F-4 measured 2026-08-13
        found the 15-minute lazy sync alive in all three dashboard routes, so
        pacing degraded to 15-minutes-plus-one-request, not to the nightly
        backfill. F-4 carries the correction as its first blocker.
        THE STRUCTURAL HALF SURVIVES INTACT, and it is the half this row
        actually relies on: the `narrowed` entry's point — that the env var and
        the callback/subscription registration are two separate things, and
        setting one without the other yields exactly the silence of doing
        neither — is UNAFFECTED and was re-confirmed by the same session. F-4's
        env var is still absent from every environment on 2026-08-13, 34 days
        on, which is that argument's strongest evidence yet.
        SO: keep reading F-4 as the cautionary analogue for the Meta callback.
        Do not carry its "day-old pacing" figure anywhere. A quoted number ages
        differently from the argument it was quoted to support — the argument
        held and the number did not.
      - >
        UNVERIFIED SINCE 2026-07-10 — flagged 2026-07-27 (reconcile Pass 2a),
        re-dated 2026-08-06. TRUE AGE IS NOW 27 DAYS.
        THIS ENTRY ASKED NOT TO BE CARRIED ANOTHER 17 DAYS AND WAS CARRIED
        ANOTHER 10. It is kept, not deleted, precisely because that is the
        finding: a note whose whole content is "assign an owner" cannot assign
        one, so it renews itself at every reconcile and reads as progress. This
        is the third dated stamp on it.
        FOR GARY — A CALL IS OWED, and it is one of two: assign an owner and a
        check-by date to the entries below, or DROP this entry and accept that
        they are carried unowned. Either is fine; the current state is the only
        one that is not, because it costs a paragraph per reconcile and buys
        nothing. Assigning owners is not a bookkeeping session's to make, which
        is why this sweep re-dated it rather than resolving it.
        PROGRESS SINCE, so the entry is no longer describing two unknowns: #2
        is now half-answered — the env vars ARE set (see that entry), leaving
        only the Meta callback registration. #1 is untouched and still needs the
        Meta App Review status from the Meta dashboard, which no repo read can
        supply.
        ORIGINAL FRAMING, PRESERVED: both blockers below had sat 17 days with no
        verification date and no owner — the same shape as F-1 (18 days) and F-4
        (dead since 2026-07-10), both of which turned out to be materially wrong
        when finally checked. F-1 had been satisfied all along; F-4 was worse
        than recorded.
      - >
        Meta App Review (Advanced Access for instagram_business_basic) not yet
        granted — until then only Instagram accounts with a role on the Meta
        app can connect. Start App Review early.
      - narrowed: true
        text: >
          SHRUNK TO ITS SURVIVING HALF 2026-08-06 — STILL LIVE, but half of what
          it asserted is now answered. Updated in place rather than closed and
          re-filed: the entry is genuinely still live, and this is how F-4's and
          F-5's live entries carry their dated re-checks.
          THE ENV-VAR HALF IS SETTLED: INSTAGRAM_APP_ID and INSTAGRAM_APP_SECRET
          are set in Vercel, Sensitive, on Preview AND Production (2026-08-06
          audit; read off the Vercel env list, not from a deploy). Being Sensitive
          they pull as empty, so their VALUES have not been checked against the
          Meta app — only their presence.
          WHAT REMAINS, and it is the whole of this entry now: THE CALLBACK IS NOT
          CONFIRMED REGISTERED in the Meta app. Note this is the same failure
          shape as F-4's, where the env var and the subscription are two separate
          things and setting one without the other yields exactly the silence of
          doing neither.
          ORIGINAL ENTRY, PRESERVED: "INSTAGRAM_APP_ID / INSTAGRAM_APP_SECRET
          (optional INSTAGRAM_REDIRECT_URI) must be set in Vercel per env and the
          callback registered in the Meta app — not confirmed done for every env."
    deferred:
      - Posting / comments / insights / Stories
      - Vercel-cron feed-refresh backup

  - id: UM-1
    track: platform
    size: S
    status: shipped
    shipped: 2026-07-23
    commits: [516b6da]
    title: "User management fixes — names display, STAFF edit option, Clerk role sync, admin/self guards"
    notes: Built 7-22; prod via 942bc59. Outstanding checks were unblocked by the HR-15 staging pass.

  - id: UM-2
    track: platform
    size: S
    status: planned
    title: "Role column on organization members + read-only role-capability matrix page"
    notes: >
      SHRUNK 2026-08-06 (bookkeeping sweep) — RE-SIZED M -> S BECAUSE MOST OF IT
      IS ALREADY BUILT. Both pieces were re-checked at source; neither was built
      by a session that knew this row existed, which is why nothing recorded it.
      PIECE (1) IS DONE. The row's own note asks the right question — whether the
      ACCEPTED-MEMBERS table has the column, or only the pending-invitations one
      — and the answer is that it does. src/app/(app)/users/page.tsx:271 heads
      the "Organization Members" section, its table at :281 carries a "Role"
      header at :284, and :316 renders the role through the same ROLE_STYLES
      badge. Nothing is left of this piece.
      PIECE (2) IS LARGELY SUBSUMED BY PERM-5B, which built the per-capability
      view inside the Edit User modal off the live registry — exactly the
      never-hand-maintain constraint below. src/app/(app)/users/user-actions.tsx:370
      resolves `can({ role }, e.capability)` per capability and :392 renders the
      role baseline against it ("Not granted by this role"). So the modal-linked
      half of the requirement exists and reads from enforcement.
      WHAT ACTUALLY REMAINS: the STANDALONE baseline matrix page only — a global,
      read-only role-capability table rendered from GRANTS + SCOPE_OVERRIDES, for
      reading the whole matrix outside the context of one user. That is the S.
      Original scope follows, unedited.
      Logged by PERM-3 (2026-07-26), explicitly scoped out of it. Two pieces:
      (1) a role / permission-level column on the organization MEMBERS table of
      /users. Check before building: the users page already renders role badges
      via ROLE_STYLES in the PENDING INVITATIONS table, so confirm whether the
      accepted-members table has the same column before treating it as new work.
      (2) a read-only role-capability matrix page, linked from the Edit User
      modal, rendered FROM THE LIVE REGISTRY in src/lib/permissions.ts (GRANTS
      plus the SCOPE_OVERRIDES table PERM-3 added) — never a hand-maintained
      table, which would drift from enforcement the first time a capability
      moves.
      Layout note carried from the PERM-3 prompt: PERM-5's per-user overrides
      belong in the Edit User modal, while the role BASELINE stays on the global
      matrix page. Build the matrix page so a per-user diff can sit beside it.
      Numbered UM-2 because UM-1 is a shipped phase (2026-07-23, 516b6da).

  - id: STAFF-1
    track: platform
    size: L
    status: shipped
    shipped: 2026-07-23
    commits: [2081401, baf4cef]
    title: "Staff-role experience — /my home, nav matrix, staff record viewer (carried HR-11 and BUG-1 step 4)"
    notes: >
      Built 7-23; prod via 942bc59. ROADMAP_ARCHIVE still says "staging pass
      pending" — no prod smoke test recorded, hence shipped, not verified.

  - id: DOCS-1
    track: platform
    size: S
    status: shipped
    shipped: 2026-07-22
    commits: [3ff8720, e01ab8e]
    title: "Docs consolidation into docs/ + session prompt library"

  - id: P-3
    track: platform
    size: M
    status: shipped
    shipped: 2026-07-27
    commits: [3902d5c, 06b1561]
    title: "Live /internal/roadmap dashboard rendered from docs/ROADMAP.yaml"
    notes: >
      Built 2026-07-25 per docs/prompts/ROADMAP_PAGE_SESSION.md. Replaces the
      hand-generated froot-roadmap-dashboard.html snapshot, whose "last updated"
      date was typed in and went stale on the first edit.
      scripts/generate-roadmap.mjs runs on `prebuild`/`predev`: it parses this
      file with the new `yaml` dependency, resolves the file's git commit date,
      and emits src/generated/roadmap.ts (gitignored) — build time rather than
      runtime because the YAML sits outside the import graph and there is no
      .git directory inside a lambda. Timestamp fallback chain, never silent:
      git commit date → meta.updated → unknown, logged to the build output AND
      labelled on the page. The page is a server component (requireAdmin,
      redirect to /dashboard, noindex) passing plain data to a client board:
      summary strip, blockers panel, staging→prod pipeline, three columns,
      search, track chips. Blockers render as the plain YAML strings — no
      severity inferred. Read-only: no writes to ROADMAP.yaml, no DB access.
      Also adds the app's first src/app/robots.ts (allow all, disallow
      /internal/).
      PROD VERIFIED 2026-07-27 (promotion 06b1561): the page renders at
      www.usefroot.com/internal/roadmap with the timestamp "Jul 27, 2026 · from
      the git commit date of docs/ROADMAP.yaml". That specific label is the
      result that mattered — it proves the FIRST link of the fallback chain
      resolved, i.e. Vercel's shallow clone still exposed a usable git commit
      date and the `unknown` fallback did NOT fire. Had it fired, the page would
      have rendered anyway (by design, never silently) and the staleness the
      phase exists to eliminate would have come straight back.

  - id: P-4
    title: >
      The /internal/roadmap blockers panel counts resolved blockers as live —
      blocker entries carry no state for the renderer to read
    track: platform
    size: S
    status: shipped
    shipped: 2026-08-02
    commits: [3a2f7ac]
    notes: >
      SHIPPED TO PRODUCTION 2026-08-02 in de3ba40 (docs/DEPLOY_LOG.md,
      2026-08-02); 3a2f7ac is an ancestor of origin/main. Status corrected from
      `in_progress` by the 2026-08-06 bookkeeping sweep.
      WHY IT SAT AT `in_progress` FOR FOUR DAYS, kept because it is the record
      and not an error: the phase was promoted while deliberately still
      in_progress (Gary, 2026-08-02) — /internal/roadmap is ADMIN-gated and
      internal, with no merchant-facing surface, so an in-progress phase's UI
      reaching production carried no tenant risk. Explicitly NOT a precedent for
      merchant-facing phases. The status was simply never flipped afterwards.
      The rationale paragraph that stood here until 2026-08-06 — that the work
      was committed on local `staging`, unpushed, so `staging` would be false —
      was true when written and stopped being true at the promotion. That is the
      failure mode this sweep exists to catch: a correct note outliving its
      state and reading as current.
      The /internal/roadmap blockers panel counted RESOLVED blocker entries as
      live. Observed 2026-08-01: the panel read "19 across 8 phases" when 8 of
      those 19 were closed, including every entry on PERM-6 and PERM-7 — both
      `shipped`, in production since 746c1be, and still presented as blocked.
      DEBT-14'S DEFECT ONE LEVEL DOWN. That row fixed exactly this for the debt
      list: rows carried a status the UI ignored. Phases, bugs and debt rows all
      gained status handling; blocker ENTRIES never did. They were bare strings
      with no field to carry state, so the renderer had no way to tell a live
      gate from a closed one.
      WHY IT MATTERED beyond cosmetics: this panel is what gets read to answer
      "what is stopping promotion", and the failure direction was OVERSTATING —
      real blockers hiding among resolved ones, in the one moment the answer is
      load-bearing. The preserve-and-mark convention was never the problem; the
      renderer's blindness to it was.
      THE FIX — Option D of five considered, STRUCTURED-MINIMAL. BlockerEntry is
      now `string | {resolved: true, text}`. A BARE STRING MEANS LIVE, which is
      DEBT-14's "a missing status means OPEN" one level down and the safe
      direction. Only the 8 CLOSED entries were migrated; the 9 live ones were
      never touched, so the prose that still matters is untouched prose. Four
      render sites now read the flag rather than the array length: the summary
      tile, the panel headline, PipelinePanel and the PhaseCard badge.
      PREFIX DETECTION WAS REJECTED ON EVIDENCE, NOT ON TASTE — this is the
      durable part of the row. Reading the existing CLOSED / RESOLVED / GATE
      CLEARED / WITHDRAWN markers was the cheap option and it fails in BOTH
      directions on this very file. F-5's LIVE blocker opens "VERIFIED STILL
      TRUE" and F-4's opens "CONFIRMED LIVE" — live entries whose first words
      are resolution words, which a matcher would close, failing toward
      UNDERSTATING, which is worse than the bug being fixed. And PERM-6's two
      closed entries carry NO marker at all: they are closed by a separate entry
      prepended ABOVE them in the same array. The convention operates at the
      ARRAY level as well as inside a string, and prose cannot carry a count.
      THE AUDIT CORRECTED THE PROMPT that opened the session, in three places
      worth keeping: "nearly all" the entries were resolved (no — 8 of 19, with
      9 genuinely live); every entry "opens with" a resolution marker (true of 6
      of the 8); and PERM-6 having "every gate closed" (true of its promotion
      gates, but its second entry is a LIVE coverage note recording four things
      still unexercised, in the wrong field). That entry stays a bare string.
      RESOLVED ENTRIES ARE STILL ON THE PAGE, in array order, toned down and
      MARKED "resolved" — marked rather than merely toned, because PERM-6's last
      two say "STAYS OPEN" in their own text. A phase whose blockers are all
      closed moves into a collapsed "Resolved gates" disclosure, mirroring the
      "Resolved debt" panel DEBT-14 built. Nothing was deleted or reworded.
      THE MIGRATION WAS PROVEN NON-DESTRUCTIVE, which was the one real risk.
      These are folded YAML scalars; nesting them under `text:` reindents every
      line by +2, and a botched reindent changes folded prose SILENTLY. A
      whole-tree identity check parsed the file before and after and compared
      every string with blockers flattened to their text: PASS, byte-identical.
      Also fixed: PipelinePanel, which renders nothing today (zero staging
      phases) and so was a dormant defect rather than an absent one — it re-arms
      the moment anything sits in staging, which is precisely when that panel is
      read. And both summary numbers now name their unit: the tile counts
      PHASES, the panel headline counts ENTRIES, and two bare numbers that
      looked like the same quantity is where the original confusion started.
      Two comments were corrected rather than left to outlive the change that
      falsified them, DEBT-26's exact shape: roadmap.ts's "Free-text strings,
      not structured records" (commit 1) and this file's own header NOTE, which
      asserted "Anything here blocks" (commit 2).
      FILED NOT FIXED: DEBT-41 (no vocabulary for a PARTIALLY closed entry —
      PERM-6[1] and BUILD-1[0] are both evidence) and DEBT-42 (`deferred` and
      `open` carry the same defect, deliberately unfixed by Gary's ruling since
      no count on the page reads them).
      Session prompt: docs/prompts/P-4_blockers_panel_status.md

  - id: UX-1
    track: platform
    size: S
    status: planned
    title: "Role-change ergonomics — Edit User dirty-state guard + role indicator in the sidebar"
    notes: >
      Created 2026-07-27 from friction hit during the PERM-2/PERM-3 verification
      pass. Both items are about the same failure: the person changing a role
      cannot tell what state they are in.
      (a) Edit User modal — "Save Changes" sits BELOW THE FOLD with no
      dirty-state warning, so a role change is silently discarded when the modal
      is closed. The user's mental model says the role changed; the system says
      nothing. This cost ~40 minutes of misdiagnosis on 2026-07-27 — time spent
      debugging permission enforcement that was working correctly, against a
      role that had never actually been saved. Fix: a sticky footer so the
      action is always visible, or a dirty-close guard, or both.
      (b) Role indicator in the sidebar footer, beside the email address already
      rendered there, so the active permission tier is visible at a glance. The
      whole class of confusion in (a) is only diagnosable if you can see which
      tier you are currently acting as. Related to UM-2, which adds a role
      column to the members table and a capability-matrix page — this is the
      same information at a third location, the one you are looking at while
      testing.
      (c) Cosmetic, logged here 2026-07-27: guarded routes answer Next.js RSC
      prefetches (`?_rsc=`) with 404 rather than a clean redirect — 13 observed
      in the Network tab immediately after a role change. NO DATA IS RETURNED,
      so this is not a permission hole; it is noise that makes the Network tab
      harder to read during exactly the permission testing described above,
      which is why it belongs with (a) and (b).

  - id: UX-2
    track: platform
    size: M
    status: planned
    title: "One current-store context across all 14 store pickers + a persistent location indicator"
    notes: >
      Scoped 2026-07-27 from a read-only audit, triggered by Gary's report that
      "I thought I was in one location, however, had to switch. If I logged back
      in, there was a random feel that I always had to double check which
      location I was looking at."
      ROOT CAUSE — TWO INDEPENDENT BUGS, neither of them nondeterministic
      ordering (that theory was tested and refuted; see BUILD-2).
      (1) NO SHARED SOURCE OF TRUTH. There are ~14 store selectors using THREE
      different mechanisms with no coordination: localStorage key
      "froot.dashboard.store" (dashboard + messages, which share it);
      a SEPARATE localStorage key "froot.forecasting.store" (forecasting);
      a URL param `?store=` (checklists); and plain
      `useState(stores[0]?.id ?? "")` in roughly ten more — every inventory
      surface (ingredients, adjustments, counts, expected, storage-areas,
      reports, orders/new), both labor surfaces, and store-view. Concretely: a
      manager who selects Uptown on the Dashboard and clicks into Inventory →
      Ingredients silently lands back on the alphabetically-first store, with no
      indication anything changed, because Inventory never reads the dashboard's
      key. That is the reported bug, exactly.
      (2) localStorage OUTLIVES LOGOUT. It is scoped to the browser ORIGIN, not
      the Clerk session, so dashboard and forecasting show the previous
      selection after re-login while the other ~10 surfaces reset to
      alphabetical. Two surfaces remember, ten forget — that mismatch is the
      "random feel". NOT a data leak: both persisting surfaces validate the
      saved id against the caller's current store list and fall back
      (`stores.find(s => s.id === savedStoreId)?.id ?? stores[0]?.id`). But it
      is worst on a SHARED DEVICE, which is precisely what PERM-7 provisions —
      and worst of all on an ADMIN device account, where every store is in scope
      so a stale selection persists in full.
      THREE SEPARABLE PIECES, deliberately ordered so partial delivery still
      helps:
      (a) SHOW IT — a persistent current-location indicator in the app shell,
      beside the email where UX-1(b) already scopes a role indicator. No schema,
      no refactor, and it directly answers "I always had to double check". Ship
      first regardless of the rest.
      (b) UNIFY IT — one shared context plus ONE storage key replacing all 14
      independent selectors, so a switch propagates everywhere. Mechanical
      rather than hard: ~14 files, each a small swap. KEY IT BY USER ID (or
      clear on sign-out) to close bug (2).
      (c) DEFAULT IT — seed the selection from User.defaultStoreId (BUILD-2)
      instead of "alphabetically first". BUILD-2 is the only piece needing a
      migration; (a) and (b) do not.
      (a) without (b) is honest but still annoying; (b) without (a) is
      invisible. (a)+(c) is a reasonable first release.
      Existing sessionStorage keys "froot.dashboard.salesSelection" and
      "froot.labor.viewedDate" are date/range state, NOT store selectors —
      leave them alone.
      Session prompt: docs/prompts/UX-2_current_store_context.md
      SURFACE MEASURED 2026-07-29 by the BUILD-2 audit — it is NOT 14 uniform
      pickers. 18 UI picker sites hosting 20 selector widgets
      (/inventory/adjustments alone has three: adjustments-client.tsx:399, :576,
      :711), plus 2 non-UI default-store pickers. FOUR BEHAVIOURAL FAMILIES, not
      one:
      (a) plain `useState(stores[0]?.id ?? "")` — 8 sites:
      labor/weekly-plan-client:91, settings/labor/labor-settings-client:72,
      inventory counts:57, expected:51, reports:187, storage-areas:65,
      ingredients:140, orders/new/cart-client:65.
      (b) localStorage-then-stores[0] — 3: dashboard-client:166-169,
      messages-client:174, forecasting-client:135. Dashboard and Messages SHARE
      key "froot.dashboard.store"; Forecasting uses "froot.forecasting.store",
      so switching store on the dashboard silently does not move forecasting.
      (c) sentinel "" or "all" — NEVER reaches stores[0] — 7 sites/9 selectors:
      adjustments x3, recipes-client:376 (whose `initialStoreId` prop is DEAD —
      page.tsx:97 never passes it), alerts-client:44 ("all"),
      purchase-orders-client:63 ("all"), purchase-orders/new/new-po-client:38
      (unconditional "", not even the single-store shortcut),
      store-view/page.tsx:25.
      (d) URL searchParams — 1: checklists/page.tsx:36-45, falls through to ALL
      the user's stores.
      ORDERING IS NOT THE PROBLEM — zero pickers lack an orderBy; all 18 use
      `orderBy: { name: "asc" }`. ONE exception, and it is not a picker:
      src/lib/forecasting-access.ts:103 orders
      [{ storeNumber: "asc" }, { name: "asc" }] and is self-documented at :89-96
      as a BUILD-2 placeholder — so it picks a DIFFERENT store than every UI
      picker. It is the first thing UX-2 should convert to
      resolveDefaultStore().
      BUILD-2 LANDED THE COLUMN, NOT ITS CONSUMPTION. Use
      src/lib/default-store.ts: resolveDefaultStore() (default if visible, else
      alphabetically-first, else null), isDefaultStoreStale() (to decide whether
      to show a "showing X instead" hint — the resolver alone cannot tell "fell
      back" from "was already first").
      SELF-SERVICE IS ALREADY RULED, DO NOT RE-LITIGATE IT. Gary 2026-07-29: a
      user MAY set their own default (DECISIONS.md, "A user may set their own
      default store"). Build it here, alongside the UI that exercises it. Shape
      decided: PATCH /api/users/me, accepting defaultStoreId ONLY, no id param,
      validated with validateDefaultStore against the CALLER'S CURRENT
      assignments. NOT PATCH /api/users/[id] — that route is requireAdmin and
      deliberately blocks self-edits (users/[id]/route.ts:51-53), the lockout
      guard; weakening it to pass a preference field through is the wrong trade.
      RE-CONFIRMED 2026-08-06 (bookkeeping sweep), re-read at source at 411ccd2
      rather than assumed — this row's audit was 2026-07-29 and a scoping
      measurement that old is exactly what this file has been burned by before.
      IT ALL STILL HOLDS. All EIGHT family-(a) sites are exact to the line:
      labor/weekly-plan-client:91, settings/labor/labor-settings-client:72,
      inventory counts:57, expected:51, reports:187, storage-areas:65,
      ingredients:140 (note: `parsStoreId`, not `storeId`), orders/new/cart-client:65.
      The localStorage keys are unchanged and so is the dashboard/messages
      split: "froot.dashboard.store" is declared in BOTH dashboard-client and
      messages-client (they share the key, which is the coupling piece (b) has
      to preserve or deliberately break), and forecasting keeps its own
      "froot.forecasting.store".
      ONE POINTER REPAIRED, not a finding: "reports:187" resolves to
      src/app/(app)/INVENTORY/reports/reports-client.tsx:187. There is also a
      top-level /reports route, which has no picker at all — so the short form
      sends a reader to the wrong file, and the wrong file looks like the
      finding evaporated.
      READY TO START. Its dependency BUILD-2 shipped to production 2026-07-29
      (493175e) and landed src/lib/default-store.ts; BUILD-2's own outstanding
      items are verification, not build, so they gate nothing here. Piece (a)
      ships alone with no schema and no refactor.

  - id: TPL-1
    track: platform
    size: M
    status: shipped
    shipped: 2026-08-07
    commits: ["8461a48", "182ca1c"]
    title: "Template types become a first-class managed entity — create, colour, choose, filter"
    notes: >
      SHIPPED TO PRODUCTION 2026-08-07 in merge "fad9207" — the first promotion
      made under WORKFLOW.md §2's --no-ff rule, so it is also the first with a
      real merge commit to revert. Migration
      20260808103000_tpl1a_template_type_entity replayed on production through
      the build's `prisma migrate deploy`.
      PRODUCTION EVIDENCE, branch named per CLAUDE.md § Database Evidence:
      br-sparkling-block-a620qvg4 — unlinked 0, total 8 (2026-08-07). Every
      production template now carries a real typeId; none fell back to the
      legacy string. That result is also the precondition Gary's Q6 ruling set
      for filing TPL-2, which is now filed.
      ── STAGING VERIFICATION BELOW, AS RECORDED BEFORE THE PROMOTION ──
      VERIFIED 2026-08-07 by Gary on staging — BROWSER WALKTHROUGH PASSED IN
      FULL: the Type select on the template form (pre-selected on edit, colour
      dots on the options), the Manage Types dialog (create, rename with the
      count warning, delete blocked while in use with reassign offered, delete
      of an unused type), the filter chips composing with the Active/Archived
      tabs, and badges rendering from stored colours.
      BACKFILL EVIDENCE, both branches named per CLAUDE.md § Database Evidence:
      preview/staging br-square-feather-a63z92vz — unlinked 0, total 16
      (2026-08-07). dev br-broad-wave-a6vpjdw0 — unlinked 0, total 8. Production
      is NOT yet measured; its evidence lands with the promotion and is the
      precondition for filing TPL-2.
      DATE NOTE, so the sequence is not read as two separate days: the blocks
      below are dated 2026-08-08, taken from the header on Gary's Q1-Q7 ruling
      message. The verification, the promotion and this rider are 2026-08-07.
      The build and the ruling did not span a day; only the labels disagree, and
      they are left as written rather than rewritten.
      TPL-1b SHIPPED TO staging IN "182ca1c" (2026-08-08) — BOTH PHASES ARE NOW
      IN. Management UI, CRUD routes, delete-blocked-with-reassign, filter chips
      and sort, badges from stored colours, CSV type resolution, and the starter
      seed at org creation. TPL-1a ("8461a48") was verified on dev the same day
      — unlinked=0 on br-broad-wave — and pushed to staging 2026-08-07.
      ENTRY POINT RULED 2026-08-07 BY GARY, AND IT SUPERSEDES THE PHASE A PLAN:
      type management lives WITH templates, not in Settings. No Settings tab was
      created. A "Manage Types" button in the /templates header opens a dialog,
      following the precedent of
      src/app/(app)/inventory/ingredients/category-manager-dialog.tsx — the same
      shape one level down, where ingredient categories are managed from the
      list they belong to rather than from global Settings. Dialog over
      subroute because the surface is a short list with inline edits, and a
      subroute would need its own layout and its own capability guard while
      navigating away from the list you are about to filter.
      NO CAPABILITY WAS ADDED AND permissions.ts WAS NOT TOUCHED (Q3).
      templates/layout.tsx already gates the whole /templates surface on
      templates.manage, which is ADMIN_ONLY, so the button inherits ADMIN and a
      second check on it could only ever disagree with the layout.
      THE PRECEDENT WAS COPIED BUT NOT WHOLESALE, recorded because the next
      person to use api/inventory/ingredient-categories as a model should know:
      it calls `Schema.parse(body)` bare (a malformed body is a 500) and has no
      P2002 handling (a duplicate name is a 500). api/template-types uses
      safeParse and returns 409. A comment now sits on the older file pointing
      at the newer pair; its status codes were NOT changed, which is a live
      route and not this row's business.
      RENAME REWRITES HISTORY, KNOWINGLY — DEBT-36's class, not a new trade.
      Checklist holds no snapshot of the type (TYPE-1_AUDIT §3.4), so renaming
      re-renders every historical completed checklist and print copy under the
      new name. DEBT-36 accepted exactly this for section names on 2026-08-07.
      What TPL-1b adds is that the rename is never silent: an AlertDialog shows
      the affected count first (Gary, Q4). If the cost stops being acceptable,
      it is a snapshot column on Checklist and it belongs with DEBT-36's entity
      phase, not here.
      TWO COUNTS, DELIBERATELY DIFFERENT, so nobody "fixes" one into the other.
      The manage dialog's count is org-wide and ARCHIVED-INCLUSIVE because it
      governs deletion — an archived template still holds the FK and still trips
      ON DELETE RESTRICT, so a client-derived count would read 0, enable delete
      and then eat a 409 (Gary, Q1). The filter chips' counts are per-view, so a
      chip's number always matches what clicking it shows.
      TemplateType.active HAS NO WRITER and that is intended (Gary, Q2): the
      ruled model is delete-or-keep with reassignment in front of delete, and no
      deactivate control was built. The field is commented in schema.prisma and
      revisited at TPL-2, not dropped — dropping it would be a schema change.
      ROWS IMPORTED DURING THE TPL-1a WINDOW KEEP typeId = NULL (Gary, Q3 — no
      backfill). They render correctly from the legacy string in neutral grey
      and simply cannot be duplicated until opened. Imports after "182ca1c"
      carry a real typeId.
      ── PHASE A NOTES BELOW, UNEDITED ── They say "TPL-1b — NOT STARTED" and
      place the management UI in Settings. Both were true when written on
      2026-08-08 and are superseded by the two blocks above; the text is left
      exactly as it stood, per the convention DEBT-59 and DEBT-36 follow — the
      prose is the record of what was believed, and corrections are prepended,
      never edited in.
      RULED 2026-08-07 by Gary as option (a), extended, on the TYPE-1
      investigation: operators CREATE types (name + badge colours), pick one on
      the template form, and filter the templates list by type. Q1-Q7 ruled
      2026-08-08. Full audit, re-verified at 2d5b93d:
      docs/prompts/TYPE-1_AUDIT.md.
      THE DEFECT, so nobody re-derives it. Template.type is String NOT NULL with
      no default (prisma/schema.prisma:306) and the template form was shipped
      with the field wired end to end and the CONTROL OMITTED — state at
      template-form.tsx:624, payload key at :798, API handling it, colour map
      rendering it, and no input anywhere in 1239 lines. So
      `type: templateData.type || "Mid-Shift"` (api/templates/route.ts:89) was
      the fallback catching a value that was never collected, and every template
      made through Create became a "Mid-Shift". THE CONTROL IS THE FIX; THE
      DEFAULT WAS THE SYMPTOM. Removing the default without adding the control
      turns creation into a NOT NULL violation, which is why they ship together.
      The eight live values came from scripts/import-keva-templates.ts, a one-off
      seed no operator can reach; the colours were a hardcoded map at
      templates-client.tsx:12-22 written to match that script.
      TPL-1a — SHIPPED TO staging IN "8461a48" (2026-08-08). TemplateType (per-org,
      the third instance of the IngredientCategory/LossReason pattern), nullable
      Template.typeId FK ON DELETE RESTRICT, migration
      20260808103000_tpl1a_template_type_entity seeding + backfilling, the
      required Type select on the form, and typeId resolved org-scoped on both
      POST and PATCH with a 400 for missing/unknown. ADDITIVE ONLY — Template.type
      keeps being written from the chosen type's name, so all six read sites and
      the CSV export are untouched.
      TPL-1b — NOT STARTED. Settings management UI (create/rename/recolor,
      delete blocked while in use with a Reassign action), type filter chips on
      /templates, the list badge reading stored colours, and CSV import matching
      names to the org's types. Gary ruled the split 2026-08-07 (Q7) rather than
      compress: do not start it inside TPL-1a's session.
      COLOURS ARE KEYS, NOT CLASS STRINGS, and this is a build constraint rather
      than a preference. Tailwind 4 runs CSS-first here (globals.css:1, no
      tailwind.config.*, no safelist) and generates utilities by scanning source
      text, so a class string read out of the database is never generated and the
      badge renders unstyled. src/lib/badge-presets.ts holds the literals; the
      column stores the key. Audit §7.
      THE STARTER SET IS NOT THE DEBT-59 FAILURE MODE, and the distinction is on
      the record deliberately (Gary, Q5, 2026-08-08). The migration seeds Opener
      / Mid-Shift / Closer / Cleaning for any org that would otherwise end at
      zero types, and org creation will do the same. Without it a fresh org has
      an empty required select and CANNOT CREATE A TEMPLATE AT ALL. DEBT-59's
      offsets were a value stamped silently onto a record nobody chose; these are
      renameable, deletable defaults offered to an operator who can see them.
      That is the line — visible and editable, versus silent and persisted.
      "Audit" IS NOT SEEDED (Gary, Q1). It is a ninth style in the old colour map
      that no write path has ever produced. If the backfill query finds a row
      carrying it, it rides the normal seed like any other value and the ruling
      is moot.
      TPL-2 — RETIRE Template.type — IS DELIBERATELY NOT FILED YET (Gary, Q6). It
      gets filed only once this backfill is proven on all three branches. Two
      sites will have to be answered for then: api/checklists/route.ts:59 emits
      `templateType` that nothing consumes, and api/stores/[id]/templates/route.ts:60
      emits `type` declared on TemplateOption but never rendered.
      NOTE FOR WHOEVER RUNS THE SQL: TemplateType ids are generated PER BRANCH,
      so unlike the inherited Organization/Store rows they will NOT match across
      dev/staging/production. Never paste one across — the inverse of the trap in
      CLAUDE.md § Database Evidence.

  - id: TPL-2
    track: platform
    size: S
    status: shipped
    commits: ["198d040"]
    title: >
      Retire the legacy Template.type string column — STEPS (1) AND (2)
      CLOSED, the column is no longer read; STEP (3), the drop, REMAINS
      OPEN AND UNAUTHORISED
    notes: >
      STEPS (1) AND (2) SHIPPED TO PRODUCTION 2026-08-10 IN MERGE "ddaa216".
      Status corrected from `staging` by the CHK-4 bookkeeping rider, per the
      DEBT-54 convention; "198d040" is an ancestor of origin/main, confirmed by
      `git merge-base --is-ancestor` after a fetch.
      `shipped` HERE MEANS STEPS (1) AND (2), AND NOTHING ELSE. Read the title
      before the status. STEP (3) — deprecating or dropping `Template.type` — IS
      UNCHANGED BY THIS PROMOTION: still open, still unauthorised, still a
      DESTRUCTIVE migration against production requiring its own ruling and its
      own soak. The DEPLOY_LOG entry for "ddaa216" says the same thing in its own
      words ("Step (3), the destructive column drop, is NOT in this promotion and
      is not authorised"), and the two agree deliberately.
      WHY THE ROW FLIPS ANYWAY RATHER THAN WAITING FOR STEP (3). `status` on this
      board records where a row's SHIPPED CODE has reached, not whether every
      idea attached to the row is finished — CHK-3 sits at `shipped` with an open
      CRON_SECRET rotation by the same reading. Leaving this at `staging` would
      assert something false (that the code is only on the preview) in order to
      avoid asserting something else false (that the row is done), and the fix
      for the second is this paragraph, not a wrong value in the first.
      WHAT STEP (3) NOW INHERITS THAT IT DID NOT BEFORE: the soak it was gated on
      has started. "(1) and (2) have run there" was the condition; they are
      running on production as of 2026-08-10.
      ── PRE-PROMOTION RECORD OF 2026-08-08 BELOW, UNEDITED ──
      VERIFIED 2026-08-08 BY GARY ON STAGING, after "198d040" and "332e6fb"
      reached origin. THE TWO GENUINELY-CHANGED BEHAVIOURS WERE EXERCISED, which
      is the point of this rider — a build gate proves the joins compile, not
      that the right name reaches a screen:
      (1) RENAME OF AN IN-USE TYPE propagated to the list badges and the
      template view THROUGH THE JOIN, with the cascade gone. This is the one the
      deleted block used to do by hand.
      (2) TYPE CHANGE ON A TEMPLATE surfaced the new type everywhere and THE
      STALE MIRROR STRING NEVER RENDERED — the row's `type` column now holds the
      old name by design, and the check is that nothing shows it.
      (3) CSV EXPORT emitted CURRENT type names, which is the surface where a
      stale string would otherwise have escaped into a file.
      ALL THREE PASSED. The regression checklist at docs/prompts/TPL-2_PLAN.md
      §6 is otherwise unrun; these three are the cases where behaviour actually
      changed, and the rest of that list re-exercises paths this commit did not
      alter.
      THIS OBSERVATION DOES NOT MEET CLAUDE.md § BROWSER EVIDENCE, AND SAYING SO
      IS THE POINT OF RECORDING IT THIS WAY. That section requires the
      ORGANIZATION ID and the Clerk INSTANCE named on the same line as the
      observation — "a page reading without a named org id does not count" —
      and NEITHER WAS CAPTURED at the time. The walkthrough is reported here as
      Gary ran it; what is missing is the identifier that would let a later
      reader confirm WHICH org's templates were renamed. Staging holds multiple
      orgs answering to the same names (§ Database Evidence), so the gap is not
      theoretical. Recorded rather than quietly omitted (Gary's ruling,
      2026-08-08) so this rider carries its real weight: a first-hand pass whose
      subject is under-specified, not a citable measurement. Anyone who needs
      the org id must ask Gary; it is not recoverable from this file.
      STEPS (1) AND (2) CLOSED 2026-08-08, commit "198d040". Nothing reads
      Template.type while typeId is set. The three UPDATE paths stopped writing
      it — PATCH /api/templates/[id], the rename cascade in PATCH
      /api/template-types/[id] (deleted outright), and POST
      /api/template-types/[id]/reassign — and all nine read surfaces now resolve
      the name through the templateType relation, keeping the string only as a
      fallback for a row whose typeId is null. NO MIGRATION AND NO SCHEMA
      CHANGE: nothing in this commit is destructive.
      STEP (3) IS EXPLICITLY STILL OPEN AND STILL UNAUTHORISED, and this row
      does not close it. Deprecating or dropping the column is a DESTRUCTIVE
      migration against production, gated on its own soak after (1) and (2) have
      run there, and REQUIRES ITS OWN RULING. The session that did (1) and (2)
      was scoped to exclude it. What step (3) inherits is written out in full at
      docs/prompts/TPL-2_PLAN.md §9 rather than left to be re-derived.
      STATUS IS `staging` AND IS ONE STEP AHEAD OF THE TRUTH AT WRITING —
      DEBT-41's precedent, stated rather than implied. "198d040" is committed on
      the local `staging` branch and NOT PUSHED; the value becomes true on
      Gary's push. Claude never pushes (CLAUDE.md § Git Rules).
      CORRECTION APPENDED 2026-08-08 — THE CAVEAT ABOVE IS NOW FALSE AND THE
      STATUS IS SIMPLY TRUE. Gary pushed; "198d040" and "332e6fb" are both on
      origin/staging, confirmed by `git merge-base --is-ancestor` against
      origin/staging after a fetch, not by assuming the push succeeded. The
      original text is preserved rather than deleted because it records what was
      true at writing and why the value was written ahead of it — the caveat
      was correct, and it expired the way it was designed to.
      TWO CLAIMS ON THIS ROW WERE WRONG AND THE ROW CORRECTS ITSELF HERE. The
      prose below is left exactly as it stood, per the convention DEBT-59 and
      DEBT-36 follow — corrections are prepended, never edited in.
      (a) "MIGRATE THE SIX READ SITES" — there were FIVE. templates-client.tsx
      had already migrated in TPL-1b ("182ca1c"), which the row could not see
      because it was scoped from docs/prompts/TYPE-1_AUDIT.md, audited at
      "2d5b93d" BEFORE TPL-1b landed. Every line number in that audit had moved
      too. The audit was right when written; the row inherited its age.
      (b) "STOP WRITING THE STRING on every path" naming five — there is a
      SIXTH. scripts/import-keva-templates.ts writes `type` with no `typeId`
      and is the ONLY writer left in the tree that manufactures an unlinked row.
      Comment only, no behaviour change (Gary, Q4): it is a one-off seed against
      a hardcoded org id, already run, and no operator can reach it. The comment
      says what to do if anyone re-runs it.
      THE CONSTRAINT WAS RESOLVED ADDITIVELY (Gary, Q1). Template.type is String
      NOT NULL with no default, so the two CREATE paths — POST /api/templates
      and POST /api/templates/import — CANNOT stop writing it: a create that
      omits it fails. They keep writing it from the resolved type's name as a
      MIRROR NO READER DEPENDS ON. It is correct at insert and never maintained
      afterwards, so on a row later renamed or reassigned the column holds a
      STALE NAME BY DESIGN. That is recorded on the column itself in
      prisma/schema.prisma, which previously said "never write one without the
      other" — the contract that note described is the one this row ended.
      The alternative was `@default("")` so creates could omit the column, and
      it was rejected: it spends a migration to make the column hold junk, and
      step (3) is easier to reason about against real names than placeholders.
      THE CSV EXPORT MIGRATED IN THE SAME COMMIT, AND THE COUPLING IS THE POINT
      rather than a tidiness choice. Stopping the rename cascade turns
      Template.type into a snapshot at insert; an export still emitting it raw
      would put STALE TYPE NAMES into a file operators carry between
      environments — the one surface where the divergence escapes the
      application. EXISTING EXPORTED FILES STAY IMPORT-VALID and the import path
      was NOT edited: it resolves template_type BY NAME, case-insensitively,
      creating any type the org lacks (TPL-1b's Q4 behaviour), and never reads
      Template.type from the database or keys on an id. So a CSV exported before
      this change imports exactly as it did, and one exported after carries the
      live name.
      RE-MEASURED BEFORE EXECUTION, NOT CITED — this row's own standing
      instruction, and the reason it is worth having: the filed evidence below
      was taken 2026-08-07, before the session ran. Gary re-ran it 2026-08-08,
      every result naming its branch per CLAUDE.md § Database Evidence:
      dev br-broad-wave-a6vpjdw0 — unlinked 0, total 8;
      preview/staging br-square-feather-a63z92vz — unlinked 0, total 16;
      production br-sparkling-block-a620qvg4 — unlinked 0, total 8.
      Unchanged from the filed figures, so no row can reach the string fallback
      today and it is transition scaffolding rather than a live path. THE
      BLANK-STRING CHECK WAS NOT RUN, and the distinction is deliberate: with
      unlinked 0 everywhere nothing reaches the fallback, so a blank string
      cannot render. That is REASONING, NOT MEASUREMENT, and it is labelled as
      such so a later reader does not cite it as a count that was taken.
      THE TWO WIRE-ONLY CARRIES ARE ANSWERED FOR, MIGRATED NOT DELETED (Gary,
      Q3). api/checklists/route.ts `templateType` (consumed nowhere) and
      api/stores/[id]/templates/route.ts `type` (declared on TemplateOption,
      never rendered) keep their key names; only their SOURCE moved to the
      joined row. Deleting either is a response-shape change no caller asked
      for, and the second would mean editing store-view-client.tsx too. They are
      correct now instead of going stale on the next rename. Deleting them is a
      fair question for step (3), which revisits this surface anyway.
      TemplateType.active STILL HAS NO WRITER. The TPL-1 row named TPL-2 as the
      moment to revisit it; it was NOT revisited, because dropping or changing
      it is a schema change and this row deliberately carries no migration. It
      belongs with step (3)'s migration.
      template-form.tsx WAS NOT TOUCHED, so no DEBT-59 spot-check was owed. It
      still carries a `type: string` on TemplateFormProps and a dead
      initialData.type, both orphaned by TPL-1a; removing them is a two-line
      cleanup that would have dragged the DEBT-59 offset spot-check into an
      S-sized row for no behavioural gain.
      ── FILED PROSE BELOW, UNEDITED. Its "six read sites" and "five write
      paths" are corrected at (a) and (b) above. ──
      FILEABLE AS OF 2026-08-07, and the precondition is why this row did not
      exist sooner. Gary ruled on 2026-08-08 (Q6) that TPL-2 could not be filed
      until the TPL-1a backfill was proven on all three branches. It now is,
      every result carrying its branch per CLAUDE.md § Database Evidence:
      dev br-broad-wave-a6vpjdw0 — unlinked 0, total 8;
      preview/staging br-square-feather-a63z92vz — unlinked 0, total 16;
      production br-sparkling-block-a620qvg4 — unlinked 0, total 8.
      All 2026-08-07, after merge "fad9207" reached production.
      WHAT THE COLUMN IS. Template.type is String NOT NULL with no default
      (prisma/schema.prisma). TPL-1 added TemplateType and the Template.typeId
      FK and made typeId the truth, but kept writing the string on every path
      so nothing had to move at once. Every write path now sets both columns
      from the SAME resolved row, so they cannot disagree — that invariant is
      what makes this row safe to do and must not be broken before it starts.
      SCOPE, IN THREE STEPS, AND THE THIRD IS NOT IN THIS ROW.
      (1) STOP WRITING THE STRING on every path: POST and PATCH
      /api/templates, POST /api/templates/import, the rename cascade in PATCH
      /api/template-types/[id], and POST /api/template-types/[id]/reassign.
      (2) MIGRATE THE SIX READ SITES to templateType.name, keeping the string
      as a fallback ONLY during the transition. They are listed with line
      numbers in docs/prompts/TYPE-1_AUDIT.md §3.1 — note that two of them
      (print/checklist, store-view/checklist-execution-client) reach it through
      `checklist.template.type`, so a grep scoped to the templates directory
      misses them. Two further sites emit the string over the wire and render
      nothing: api/checklists/route.ts (`templateType`, consumed nowhere) and
      api/stores/[id]/templates/route.ts (`type`, declared on TemplateOption,
      never rendered). Both are answered for here.
      (3) DEPRECATION OF THE COLUMN IS A SEPARATE, LATER STEP, gated on its own
      soak after (1) and (2) have run in production — and it is ADDITIVE ONLY
      in this row. A DROP is a destructive migration against production and
      REQUIRES ITS OWN RULING; it is explicitly not authorised by filing this
      row. The precedent to follow is the one both recent DEPLOY_LOG entries
      take on rollback: an unread additive column is harmless, dropping it is
      risk for no benefit.
      KNOWN TRANSITION ARTIFACT, not a blocker: templates imported during the
      TPL-1a window (between "8461a48" and "182ca1c") carry typeId NULL. Gary
      ruled 2026-08-07 (Q3) to leave them — no backfill — because they render
      correctly from the legacy string and heal the moment the template is
      opened and saved. THIS ROW MUST COUNT THEM BEFORE STEP 2: a read site
      that drops the string fallback while any typeId NULL row survives renders
      a blank type. The three-branch evidence above says unlinked is 0
      everywhere TODAY, but it was measured before this row starts and an
      import can create one at any time. Re-measure, do not cite.
      RELATED: TPL-1 (the entity, both phases), and
      docs/prompts/TYPE-1_AUDIT.md, which is the map of every read and write
      site and is the reason this row can be scoped without re-auditing.

  - id: X-1
    track: platform
    size: S
    status: planned
    tags: [pre-launch]
    title: "Activation & QA"
    blockers:
      - >
        UNSCOPED — BLOCKED ON ITSELF. Promoted to a blocker entry 2026-08-06 to
        make this state machine-readable; until now it was invisible, and an
        unscoped row is indistinguishable on the board from a ready one.
        THIS ROW HAS NO NOTES AT ALL — not thin notes, none: `track`, `size`,
        `status`, `tags` and a two-word title are its entire content. Nobody can
        start "Activation & QA" from that, and its `size: S` is an estimate of
        work nobody has described. It carries the [pre-launch] tag, so on its
        own terms it must close before external merchants.
        CLEARS WHEN: someone defines what activation and QA mean here — the
        surfaces covered, what "activated" is true of, and what QA is a gate on.
        That scoping is itself the next action and is a DECISION, not build
        work; it is out of scope for bookkeeping (noted by the 2026-08-06 sweep,
        which is why this entry describes the hole rather than filling it).

  # ---- Checklists (CHK) ----
  # The checklist LIFECYCLE phase, scoped 2026-08-09 from DEBT-36 (sections are
  # implied, not modelled) and DEBT-48 (the availability window). Gary ruled the
  # product design 2026-08-08 (R1-R5) and approved the implementation plan
  # 2026-08-09.
  #
  # THE ARTIFACT IS THE SOURCE; THESE ROWS ARE THE SUMMARY AND THE POINTER.
  # docs/prompts/CHK-1_PLAN.md is the audit, the SQL, the state-derivation
  # contract and the ruling record; docs/prompts/CHK-1_SESSION_SKELETONS.md is
  # the per-session spine. Both committed in "746918c". Do not re-state the plan
  # into these rows and do not edit either file — they are claims wholesale
  # (CLAUDE.md § Where documents live).
  #
  # ONE SESSION PER ROW, IN ORDER, each independently verifiable on staging
  # before the next begins. Schema and migration land first.

  - id: CHK-1
    track: checklists
    size: L
    status: shipped
    commits: ["4fd9152"]
    title: "Sections become a first-class per-template entity, with an as-executed record"
    notes: >
      SHIPPED TO PRODUCTION 2026-08-10 IN MERGE "ddaa216", AND THE SHIPPED GATE
      THIS ROW WROTE FOR ITSELF IS SATISFIED. The gate below said this row does
      not go `shipped` until `unlinked_tasks = 0` comes back from
      `br-sparkling-block-a620qvg4`, because the 2026-08-10 promotion ran
      Migration A's BACKFILL against real production `Task` rows for the first
      time and no staging pass can vouch for a per-branch data migration.
      production `br-sparkling-block-a620qvg4` — unlinked_tasks 0, sections 32.
      MEASURED BY GARY 2026-08-10 and reported to this session in
      docs/prompts/CHK-4_S4_lifecycle_visible.md; Claude queried no database
      (CLAUDE.md § Environment Variables — production reads go through the Neon
      console). The provenance is stated because this row spent two paragraphs
      insisting that a gate nobody ran is indistinguishable from one that
      failed, and "someone else ran it" is the third possibility that deserves
      its own label rather than being folded into the first.
      Status corrected from `staging` by the CHK-4 bookkeeping rider, per the
      DEBT-54 convention: "4fd9152" is an ancestor of origin/main, confirmed by
      `git merge-base --is-ancestor` after a fetch rather than by assuming the
      promotion carried it.
      ── PRE-PROMOTION RECORD OF 2026-08-09 BELOW, UNEDITED ──
      VERIFIED 2026-08-09 BY GARY ON STAGING. The full S1 checklist passed: the
      sections panel; create / rename / reorder round-trips; a rename collision
      REFUSED rather than merged; a non-contiguous section rendering ONE
      heading; duplicate carrying its sections and their order; the CSV
      export→import round-trip; and the DEBT-59 nine-check re-run, owed because
      template-form.tsx was touched.
      THE DEBT-36 TRIGGER WAS FIRED DELIBERATELY AND THE DEFECT IS DEAD. A
      checklist completed AFTER the deploy, its template's section then renamed:
      the print copy rendered the OLD name out of `sectionsSnapshot` while the
      template showed the new one. That is the first time this trigger has ever
      been pulled, and it is the whole point of the phase — the row it closes
      spent months recording it as latent.
      Org/instance: org_3G02wO4QlVVSWppi8aqlnSZnsDa on the Clerk instance
      verified-snapper-7 (CLAUDE.md § Browser Evidence). THE IDENTIFIER'S
      PROVENANCE IS PART OF THE EVIDENCE and is recorded because TPL-2's rider
      could not: the id was read from `window.Clerk.organization.id` on the
      staging site IN THE SAME CONTINUOUS BROWSER SESSION AS THE TESTS,
      2026-08-09 — not reconstructed afterwards from a database row, which is
      the mistake § Browser Evidence's own 2026-08-06 correction records. A
      Clerk org id identifies an INSTANCE, and this one was taken Clerk-side.
      BACKFILL EVIDENCE, each result naming its branch (§ Database Evidence),
      both measured 2026-08-09:
      dev `br-broad-wave-a6vpjdw0` — unlinked_tasks 0, total_tasks 210,
      sections 32, unlinked_logs 0.
      preview/staging `br-square-feather-a63z92vz` — unlinked_tasks 0,
      total_tasks 420, sections 64, unlinked_logs 0.
      production `br-sparkling-block-a620qvg4` — PENDING PROMOTION, and the row
      does not claim it. `preview/main` `br-purple-rain-a6m62xww` is an
      archived fossil, was not queried, and is owed nothing (plan §0a).
      STATUS IS `staging` AND IS SIMPLY TRUE, not one step ahead — the inverse
      of TPL-2's caveat and worth saying for that reason. "4fd9152" and
      "7c99da3" are both on origin/staging, the migration has been applied
      there, and the pass above was run against that deployment.
      WHAT REMAINS BEFORE THIS ROW IS `shipped`: the production promotion, and
      the same verification query on `br-sparkling-block-a620qvg4` returning
      unlinked_tasks 0 with its branch named.
      NOTE 2026-08-10 — THAT DEBT COMES DUE THE MOMENT THE PROMOTION LANDS, and
      it is the SHIPPED GATE for this row, not a formality after it. The
      2026-08-10 promotion carries Migration A to production for the first time,
      where its BACKFILL runs against real `Task` rows — the half that has never
      been exercised there and the only half a clean staging pass cannot vouch
      for (the structural half was proven against `migrate diff`; the data half
      is per-branch by construction). Run this on `br-sparkling-block-a620qvg4`
      AFTER the production build reports the migration applied, and do not flip
      this row to `shipped` until it returns unlinked_tasks 0:
      `SELECT current_setting('neon.branch_id', true) AS branch,
      (SELECT count(*) FROM "Task" WHERE "sectionId" IS NULL AND
      btrim("sectionName") <> '') AS unlinked_tasks, (SELECT count(*) FROM
      "Task") AS total_tasks, (SELECT count(*) FROM "Section") AS sections,
      (SELECT count(*) FROM "TaskLog" WHERE "sectionId" IS NULL) AS
      unlinked_logs;` — plan §2.1. A non-zero unlinked_tasks means a task
      carries a blank sectionName past both API choke points and needs looking
      at; it is NOT a reason to loosen the backfill. A row left at `staging`
      because nobody ran the query afterwards is indistinguishable from one
      whose query failed, which is why the gate is written here rather than
      carried in someone's head through the promotion.
      ── PRE-VERIFICATION NOTE OF 2026-08-09 BELOW, UNEDITED ──
      BUILT 2026-08-09 IN "4fd9152", NOT YET APPLIED AND NOT YET VERIFIED —
      which is why this is in_progress rather than staging. Prompt:
      docs/prompts/CHK-1_S1_sections_entity.md. Migration A is HAND-AUTHORED
      AND UNRUN (prisma/migrations/20260809160000_chk1_section_entity): Gary
      runs it on dev first, then pushes so staging and production take it
      through `migrate deploy` in the Vercel build. Nothing in this row is
      proven until `unlinked_tasks = 0` comes back from the three live
      branches, each result naming its branch. The commit is CODE LANDING, not
      evidence.
      WHAT SHIPPED BEYOND THE PLAN'S LETTER, so the diff is not read as
      narrower than it is: (a) TWO shared helpers rather than six migrated
      call sites — src/lib/sections.ts owns the READ resolution order
      (snapshot -> joined Section -> legacy string -> "General") and
      src/app/api/templates/sections.ts owns the WRITE, shared by POST, PATCH
      and import. Six independent joins would have been the same defect the
      six independent string derivations were (DEBT-26). (b) THE FORM'S TASK
      ARRAY NOW KEEPS EACH SECTION'S TASKS CONTIGUOUS, and that invariant —
      not the adjacency test, which is unchanged and merely follows from it —
      is what ends the double heading and what makes sortOrder agree with
      MIN(orderIndex) by construction. A non-contiguous template is regrouped
      ON LOAD, visibly, rather than silently at save. Consequence worth naming
      in the browser pass: DRAG NOW REORDERS WITHIN A SECTION and a task
      dropped across a section boundary returns to its own block; the section
      input is still what moves a task between sections. (c) The execution
      page reads the snapshot too, not only print, so reopening a completed
      checklist agrees with its print copy instead of contradicting it.
      Every structural statement in the migration was checked against
      `prisma migrate diff --from-empty --to-schema` output and matches it, so
      the schema and the migration cannot have drifted. That check needs no
      database; the BACKFILL half is unproven until Gary runs it.
      S1 of five. Plan: docs/prompts/CHK-1_PLAN.md §§1.2, 2.1, 2.4, 6.3, 6.4.
      Closes DEBT-36's ruled direction.
      SHIPS: Migration A (additive) — `Section` per-template with
      @@unique([templateId, name]) and sortOrder, nullable `Task.sectionId`,
      `TaskLog.sectionId`, `Checklist.sectionsSnapshot`; the backfill recovers
      section ORDER from MIN(orderIndex) rather than inventing it. All six
      section render sites resolve through the join, with `Task.sectionName`
      KEPT AND KEPT WRITTEN as the legacy mirror (the TPL-1a shape exactly —
      retiring it is a later row). Section management in template-form.tsx.
      CSV `task_section` resolved by name within the template. `sectionsSnapshot`
      frozen ONCE, on the first TaskLog write, and never rewritten.
      PROVES IT WORKED: `unlinked_tasks = 0` on the three LIVE branches, each
      result naming its branch; a section rename on a template with completed
      history leaves the historical checklist AND its print copy unchanged —
      DEBT-36's latent trigger, fired deliberately for the first time; a
      non-contiguous section renders ONE heading, not two; duplicate carries
      sections; CSV round-trip; the DEBT-59 nine checks
      (docs/prompts/DEBT-59_AUDIT.md §7), owed because template-form.tsx is
      touched.
      MUST NOT TOUCH: Checklist.status, generation, StoreHours, the offsets, the
      offsets-box copy, any lifecycle behaviour. No cron.
      SNAPSHOT SCOPE IS NAMES ONLY (Gary, 2026-08-09). Task descriptions and
      task additions still resolve live at print time; that hole is real, wider
      than DEBT-36 describes, and is DEBT-60 rather than cargo here.

  - id: CHK-2
    track: checklists
    size: S-M
    status: shipped
    commits: ["597e86d"]
    title: "Day-close inputs — fold the three phase lists into one, and give StoreHours a writer"
    notes: >
      SHIPPED TO PRODUCTION 2026-08-10 IN MERGE "ddaa216". Status corrected from
      `staging` by the CHK-4 bookkeeping rider, per the DEBT-54 convention;
      "597e86d" is an ancestor of origin/main, confirmed by
      `git merge-base --is-ancestor` after a fetch.
      NO GATE OF ITS OWN, AND THAT IS A REAL DIFFERENCE FROM CHK-1 AND CHK-3
      RATHER THAN AN OMISSION: this row carried no migration and no backfill, so
      there is nothing on the production database for a query to confirm. What
      it shipped is a WRITER — /stores is the first thing that has ever written
      `StoreHours` — and the consequence to hold onto is that every production
      store starts with the table empty, so day close falls to midnight + 3h
      everywhere until someone fills the hours in. CHK-4 makes that visible on
      the /stores card rather than leaving it to be discovered.
      ── PRE-PROMOTION RECORD OF 2026-08-09 BELOW, UNEDITED ──
      VERIFIED 2026-08-09 BY GARY ON STAGING — org
      org_3G02wO4QlVVSWppi8aqlnSZnsDa on the Clerk instance verified-snapper-7,
      retrieved via `window.Clerk.organization.id` FIRST, BEFORE ANY
      OBSERVATION. That ordering is the S1 lesson and it is now practice rather
      than a reminder: § Browser Evidence's 2026-08-06 correction records an org
      id read off a database row AFTERWARDS and written down as the wrong
      environment, so the id is taken Clerk-side, up front, in the same
      continuous session as the observations it labels.
      ALL FIVE CHECKS PASSED. (1) Hours round-trip — blank stays blank, cleared
      stays cleared, Closed round-trips. (2) Handoff notes identical to
      pre-fold behaviour: opener→today, closer→tomorrow, ordering unchanged.
      (3) Labor shows the explicit hours for the configured store and 0 for a
      Closed day. (4) A store with no hours renders everywhere unchanged —
      the negative check, and the one that proves NULL breaks nothing. (5)
      Square resync left the hours intact, which the resync route's own comment
      claimed and this is the first time anything has been able to test.
      WHAT THAT MAKES TRUE THAT WAS NOT TRUE BEFORE: a store in a deployed
      environment now has real hours, so labor's explicit branch is running on
      staging rather than only under a local harness, and CHK-3's blocker
      ("at least one store has hours set to verify against") is satisfied on the
      branch S3 will verify against. That blocker is marked resolved on CHK-3
      in this commit.
      `status: staging` IS SIMPLY TRUE HERE. Both commits are on
      origin/staging — Gary pushed them between sessions, which is what made the
      pass possible — and the observations were taken against that deployment.
      Not one step ahead; contrast DEBT-32's closure, which was written while
      they were still unpushed and says so.
      ── PRE-VERIFICATION BLOCK BELOW, UNEDITED ──
      IN_PROGRESS, NOT staging, IN "597e86d" 2026-08-09. Both halves are built
      and committed; nothing is pushed, so no deployment runs this yet and Gary
      has verified nothing. The row moves to `staging` when the checklist below
      passes against the staging deployment.
      WHAT SHIPPED, AND WHAT PROVES IT. The DEBT-32 fold is done and its
      zero-output-change claim was PROVEN, not asserted: the retired
      implementations were pasted verbatim from 7031155 and run beside the
      shipped ones over 2,715 comparisons — every phase either can receive
      (canonical, the legacy "During Hours" string, null, empty,
      whitespace-padded, case variants, junk) crossed with the full source x
      target date rule across month, year, leap-day and both DST boundaries. No
      differences, plus six named cases asserted absolutely rather than only as
      old-vs-new equality. The ordering map now lives ONCE, in src/lib/phases.ts,
      DERIVED from OPERATIONAL_PHASES rather than restated.
      ONE DESIGN NOTE WORTH KEEPING: phaseOrder deliberately does NOT route
      through normalizePhase. That function trims and the old maps did not, so
      using it would reorder a whitespace-padded "  Before Opening  " from MID to
      OPENING — a real behaviour change wearing a cleanup's clothes. The WRITE
      paths already normalize, which is where trimming belongs.
      STOREHOURS HAS A WRITER FOR THE FIRST TIME, and the finding that made this
      row exist was confirmed by MEASUREMENT rather than by grep alone: dev
      br-broad-wave-a6vpjdw0 held 0 StoreHours rows in the entire database
      before this session. PUT/GET /api/stores/[id]/hours, PUT behind
      stores.manage — no new capability, no permissions.ts edit — plus a
      seven-day editor on /stores. Replace-in-one-transaction rather than upsert,
      forced by the schema: StoreHours carries no @@unique([storeId, dayOfWeek]),
      so upsert has no key to target. NO MIGRATION SHIPPED, as ruled.
      BLANK IS A REAL ANSWER AND IT IS THE DEFAULT (DEBT-59). A day nobody has
      decided about gets NO ROW — not a fabricated 9-to-5. Clearing a day deletes
      its row; a one-sided day persists one NULL. Both existing readers already
      treat "no row" and "row with unusable times" identically, so "hours not
      set" stays a queryable state instead of becoming indistinguishable from a
      real decision to open at nine.
      VERIFIED END-TO-END ON DEV, br-broad-wave-a6vpjdw0, Store
      cmqvygqx9000004kzadw4vecg ("Carson"), Organization
      cf888f2d-f234-48c7-8097-fd5b44b5b3dd: seven decided days round-trip, a
      one-sided day keeps its NULL, isClosed round-trips with both times NULL,
      clearing a day removes its row and leaves the other six. LABOR'S
      EXPLICIT-HOURS BRANCH EXECUTED FOR THE FIRST TIME IN ITS LIFE — open hours
      moved from inference [13,13,13,13,14,13,10] to explicit
      [12,12,12,12,14,13,0]. The fixture was removed; the branch is back to 0
      rows.
      TWO PRE-EXISTING /stores DEFECTS WENT LIVE WITH THE WRITER AND ARE FIXED
      HERE, both harmless only because no store could have hours: the hours block
      rendered ONLY when hours existed and its "Edit Hours" button had no
      onClick — a dead affordance inside an unreachable branch — and
      `${openingTime} - ${closingTime}` would have printed the literal "null" for
      a half-filled day, while the Mon-Fri group rendered only when all five
      weekdays were present AND identical, silently showing nothing for a store
      open different hours on Wednesday.
      THE FALLBACK COPY IS NOT HERE, DELIBERATELY. "Hours: not set" is stated
      neutrally; what an unset store falls back to at day close is CHK-3's
      sentence to write, on the session that makes it true (the DEBT-29 lesson).
      THE DEBT-33 HAZARD BELOW HAS MOVED ONE LINE, as a direct result of this
      commit: handoff-notes.tsx's baseline error now sits at :69:25, not :70:25,
      because the fold's edit is one line shorter than what it replaced. The
      exclusion worked as designed — run on its own the file reports exactly that
      one error and no other, so this session introduced none and fixed none.
      DEBT-33 carries the dated correction.
      S2 of five. Plan: docs/prompts/CHK-1_PLAN.md §§3.1, 8. Absorbs DEBT-32.
      SHIPS: DEBT-32 — delete the two hand-copied "During Hours" alias lines
      (messages.ts:41, handoff-notes.tsx:24) and move PHASE_ORDER into
      src/lib/phases.ts so both sides of the client boundary import one list.
      Plus a StoreHours editor on /stores behind `stores.manage`.
      THE STOREHOURS HALF IS NOT A NICE-TO-HAVE, and it is the finding that
      makes this row exist: `StoreHours` HAS NO WRITER ANYWHERE IN THE CODEBASE.
      The table ships in the init migration, `prisma.storeHours` appears in
      exactly one file as a read (labor-plan.ts:170), and that file says so
      outright at :172-174 — "StoreHours is currently never populated, so
      inference is the normal path". Gary's R2 makes day close StoreHours-driven,
      so without this the ruled mechanism has no input on any branch.
      READY NOW — NOT GATED ON CHK-1. Sequenced after it by the ruled order, not
      by a dependency; nothing in this row touches sections or the schema.
      PROVES IT WORKED: handoff-note date resolution unchanged across all three
      phases AND the legacy string (a template carrying "During Hours" still
      orders as mid); hours save, reload and render; Square resync does not
      overwrite them; labor's weekly plan takes the explicit-hours branch
      (labor-plan.ts:195-205) for a store that now has hours — the first row
      ever to reach it.
      MUST NOT TOUCH: checklist surfaces, the offsets, any schema.
      HAZARD, NAMED IN ADVANCE (carried from DEBT-32): handoff-notes.tsx carries
      one of DEBT-33's ten baseline lint errors at :70:25. Scope the eslint gate
      to exclude that file and SAY SO in the commit message — do not appear to
      have fixed a DEBT-33 error this row did not fix.

  - id: CHK-3
    track: checklists
    size: M-L
    status: shipped
    commits: ["d089a7c", "1c907d2"]
    title: "Lifecycle engine — expected windows, overdue on read, missed written at day close"
    notes: >
      ── POST-PROMOTION RIDER 2026-08-10, MERGE "d19cca6". PREPENDED; NOTHING
      BELOW IS EDITED. ──
      "1c907d2" IS IN PRODUCTION. The defect trilogy — the frequency exclusion
      holding at the closing site, the `createdAt` floor, the five split
      counters — reached `main` in merge "d19cca6" and the production build went
      Ready with no migration, which is what the entry predicted and checked
      for. This row's `status: shipped` now describes both of its commits rather
      than only "d089a7c".
      AND THE NO-HEARTBEAT RIDER BELOW HAS ITS ANSWER, FROM THE ONE BRANCH THAT
      COULD GIVE IT. Production, `br-sparkling-block-a620qvg4`, 2026-08-10
      (Gary): 28 rows closed for dates >= 2026-08-09, last close at
      2026-08-10 10:00:28, from the SCHEDULED sweep with nobody triggering it.
      That is the first proof since the engine shipped that the production
      heartbeat runs unattended, and it settles the distinction the rider drew
      — "the job does the right thing when invoked" was all staging could ever
      show, because Vercel fires schedules on Production alone. Both halves of
      the claim are now evidenced on the branches that own them: correctness on
      staging by hand, operation on production by clock.
      ── PROMOTION RIDER 2026-08-10 (CHK phase promotion). PREPENDED; NOTHING
      BELOW IS EDITED. ──
      STAGING HAS NO HEARTBEAT, AND THIS IS PLATFORM BEHAVIOUR RATHER THAN A
      DEFECT. Vercel cron schedules fire ONLY on Production — established in
      CRON-DIAG and now confirmed operationally: no scheduled sweep ran
      overnight on staging between 2026-08-09 and 2026-08-10, and the manual
      beat fired by hand at ~06:45 performed a full and correct close (Carson's
      four to Missed, two marked and two materialised, templatesExcluded 1
      across all 12 stores, beforeTemplateCreation 0, errors 0). RECORD THE
      CONSEQUENCE PLAINLY, BECAUSE IT REACHES BACKWARDS THROUGH THIS WHOLE ROW:
      EVERY STAGING SWEEP EVER RUN — the 22:43 and 22:45 UTC pair of 2026-08-09
      below, and this morning's — WAS MANUAL. Nothing on that branch has ever
      demonstrated the schedule, and the evidence above should be read as
      "the job does the right thing when invoked", never as "the job runs".
      PRODUCTION'S SCHEDULE IS THE REAL ONE AND IT IS ALREADY PROVEN: the two
      hourly sweeps read from the production Runtime Logs at 01:01:25Z and
      02:01:24Z on 2026-08-10 are recorded further down this row, and they
      arrived without anyone asking. Production days close unattended; staging
      days close when someone remembers. The satisfying confirmation — query
      production for yesterday's closed rows and find that they closed
      themselves — is the optional last item on Gary's post-push checklist in
      this promotion's DEPLOY_LOG entry.
      AND THE "NOT PUSHED, NOT DEPLOYED ANYWHERE" SENTENCE IN THE DEFECT RIDER
      BELOW IS NOW SPENT. "1c907d2" was pushed by Gary, has been serving on
      staging since, and is verified there: the Weekly "test" row was left
      Pending and open by this morning's close (the closing-site gate holding)
      and beforeTemplateCreation came back 0 (the `createdAt` floor
      manufacturing nothing). It reaches production on THIS promotion. The
      sentence is left standing per preserve-and-mark — it was true when
      written, and it is the reason the fix was findable at all.
      ── DEFECT RIDER, 2026-08-10. PREPENDED; NOTHING BELOW IS EDITED EXCEPT
      OPEN-CHECK PLACEHOLDER (1), WHICH IS WHAT A PLACEHOLDER IS FOR. ──
      THE LESSON FIRST, BECAUSE IT IS THE PART THAT GENERALISES: A FIELD'S NAME
      IS NOT EVIDENCE OF WHAT IT COUNTS. This row's open-check (1) asked for "a
      body showing frequencySkipped > 0". Response bodies carried that field
      while twenty-four fiction rows sat on staging, and the field was not
      lying — it counted non-Daily templates WITH NO ROW YET, at the
      materialisation site, which is a true and nearly useless fact. The check
      as WRITTEN could have passed on a branch full of fiction. The § Database
      Evidence failure mode, one layer further in: not an under-specified
      identifier that still resolves, but an under-specified METRIC that still
      reports.
      WHAT WAS MEASURED (Gary, 2026-08-10). Staging `br-square-feather`:
      weekly_monthly_missed 24 — template "test", frequency Weekly at time of
      reading, created ~20:00 on 2026-08-09, 12 rows dated 2026-08-07 and 12
      dated 2026-08-08, BOTH PRECEDING ITS CREATION. Production
      `br-sparkling-block-a620qvg4`: non_daily_templates 0, fiction_rows 0 — the
      holes were live there and unexercised, and no production cleanup is owed.
      THE FREQUENCY EXCLUSION WAS NEVER BREACHED AT THE SITE THE CHECK WATCHED.
      Read at "bbb5734": right field, right casing, `Template.frequency` is NOT
      NULL with default "Daily" so the `?? "Daily"` branch cannot fire from a
      row, one enumeration path, and the gate has been in the cron since its
      first commit. WHAT IT NEVER COVERED IS THE CLOSING SITE — and under
      DEBT-61 bulk generate creates a Weekly template's checklist EVERY day, so
      those rows EXIST and were being swept to Missed six days a week without
      frequency ever being consulted. The gate protected only the case that
      barely arises. RULED 2026-08-10 (Gary): Daily-only at BOTH sites.
      THE 24 ROWS THEMSELVES ARE THE OTHER DEFECT, WHICH IS INDEPENDENT OF
      FREQUENCY. Materialisation enumerated store × lookback-day × template and
      never asked when the template began; `createdAt` was not in the SELECT. A
      plain DAILY template created today collected a Missed row for both prior
      days at the next sweep, per applicable store, with an expected window
      frozen onto days it did not exist for. A `createdAt` floor now compares
      against the window's END (`w?.end ?? dc.at`), so a template created 06:00
      for an 08:00 Opener window still misses and one created 14:00 does not.
      No migration; the column already existed.
      WHETHER "test" READ Daily OR Weekly AT SWEEP TIME IS UNRESOLVED AND IS
      RECORDED AS UNRESOLVED. `Template` has `createdAt` but no `updatedAt`, so
      a frequency edit leaves no trace; the walkthrough also recorded TWO
      templates named "test", which is the § Database Evidence ambiguity in
      miniature. It does not gate anything — the floor suppresses those 24 rows
      whatever the frequency said — and the fix closes both paths regardless.
      THE COST OF THE CLOSING-SITE GATE, NAMED BECAUSE IT IS A REAL TRADE AND
      NOT A FREE WIN: a non-Daily checklist somebody genuinely STARTED and
      abandoned is now never closed. It keeps `closedAt` null and therefore
      reads `overdue` indefinitely, because overdue is derived and this job
      writes the only terminal state there is. Against six fictional misses a
      week that is the right trade, and it is not the end state — see the open
      item below. The cron reports those rows as `frequencyLeftOpen` so the
      trade is visible in every response body rather than inferred from an
      absence.
      THE COUNTERS WERE RENAMED AND SPLIT, and `preexisting` is the one that
      matters most: the haveTemplate guard runs BEFORE every rule, so a template
      with a row — including a fictional one — used to vanish from every counter
      at once. `frequencySkipped: 0, materialized: 0` is exactly what a day with
      fiction already on disk looks like. Now: `frequencyExcluded`,
      `frequencyLeftOpen`, `beforeTemplateCreation`, `preexisting`, `raced` —
      all five summed into the TOP-LEVEL totals and into the hourly log line,
      which previously reported only what was written, so a sweep that wrote
      nothing and a sweep that excluded everything read identically in the
      Runtime Logs.
      `materializesMisses` → `dayCloseAppliesTo` for the same reason the
      counters moved: asked at the closing site, the old name poses a question
      about creation and answers one about closing. Its CHK-3 doc block is
      preserved and marked; the call-site pointer was repaired.
      THE FIX IS "1c907d2" — COMMITTED, NOT PUSHED, NOT DEPLOYED ANYWHERE. It is
      in this row's `commits` so it is findable, and this sentence is what keeps
      `status: shipped` honest: that status describes "d089a7c", which reached
      production in "ddaa216". The staging cleanup SQL was presented for Gary to
      run and Claude ran no query — CLAUDE.md § Environment Variables.
      ── THE PRE-DEFECT RECORD IS BELOW, UNEDITED ──
      SHIPPED TO PRODUCTION 2026-08-10 IN MERGE "ddaa216". Status corrected from
      `staging` by the CHK-4 bookkeeping rider, per the DEBT-54 convention;
      "d089a7c" is an ancestor of origin/main, confirmed by
      `git merge-base --is-ancestor` after a fetch.
      MIGRATION B's STRUCTURE ON PRODUCTION — `br-sparkling-block-a620qvg4`: the
      four lifecycle columns present, both unique indexes present. Measured by
      Gary 2026-08-10 and reported to this session in
      docs/prompts/CHK-4_S4_lifecycle_visible.md; Claude queried no database.
      THE ENGINE IS RUNNING ON PRODUCTION AND THIS HALF WAS MEASURED BY CLAUDE,
      from the Vercel Runtime Logs, WITHOUT PRESENTING A SECRET — the log line
      the route already prints is readable to anyone who can read the project's
      logs, which is why it was the right place to look. Deployment
      `dpl_A81kroKm2mQ9wkbJaLuh6RGv3HPq`, commit "bca5df1", aliased to
      www.usefroot.com (confirmed with `vercel inspect` plus
      `vercel ls --meta githubCommitSha=<full 40>`, the CLAUDE.md § Staging
      Verification pair, applied to production). Two SCHEDULED sweeps, both
      HTTP 200:
      `2026-08-10T01:01:25Z — 12 stores, 24 store-days closed, 0 marked missed,
      0 materialized, 0 errors (grace 3h, lookback 2d)`, and the identical line
      at `2026-08-10T02:01:24Z`.
      READ THOSE ZEROS CORRECTLY, BECAUSE THE OBVIOUS READING IS WRONG. They are
      the IDEMPOTENT STEADY STATE, not evidence that the promotion materialised
      nothing: 24 store-days closed with nothing to write is exactly the shape
      staging's second sweep took two minutes after its first wrote 88 rows.
      THE FIRST PRODUCTION SWEEP IS NOT IN EVIDENCE AND THIS ROW DOES NOT CLAIM
      IT. The deployment was created 2026-08-09T23:25Z, so the first hourly tick
      was ~00:01Z, and the retained log window begins 00:08:12Z — it fell off
      the end by seven minutes. Its counts are recoverable only by SQL against
      `br-sparkling-block-a620qvg4` (count `Checklist` rows with `closedAt` set,
      by date), which is Gary's to run.
      ── PRE-PROMOTION RECORD OF 2026-08-09 BELOW, UNEDITED ──
      VERIFIED ON STAGING 2026-08-09 BY GARY, AND THE RECORD IS THE CRON'S OWN
      RESPONSE BODIES. No browser observation and no SQL was taken in this pass.
      This row said what would prove it — "SQL and the cron's own response body,
      NO UI" — and half of that arrived; what the SQL half would still add is
      listed as OPEN CHECKS at the end of this block rather than folded in with
      the passes.
      THE ORG ID DOES NOT LABEL THIS EVIDENCE, AND SAYING SO IS PART OF
      LABELLING IT. GET /api/cron/checklist-day-close authenticates itself,
      renders nothing, and walks EVERY active Store on the branch across every
      organization (route.ts:100-109) — so § Browser Evidence's org id has
      nothing to name here, and quoting one would imply a scope the job does not
      have. What names this evidence is the DEPLOYMENT: the staging alias,
      Preview scope, branch `staging`, Clerk instance verified-snapper-7.
      Twelve stores answered, which is that branch's whole active estate rather
      than one org's.
      FIRST SWEEP, 22:43 UTC 2026-08-09 — 12 stores, 24 store-days closed,
      markedMissed 2 (Las Brisas 08-08, Carson 08-07), materialized 88, errors 0,
      raced 0. Every store reported dayCloseSource `no-hours`: only Carson has
      hours, so the fallback is what almost the whole estate took, and it was
      VISIBLE IN THE BODY rather than inferred — the four-value dayCloseSource
      (note (b) below) earning its keep on its first real run. Per-store close
      instants landed at the right absolute time for each timezone, Denver 09:00Z
      and LA 10:00Z, which is the thing a single daily UTC cron could not have
      done and the reason this one is hourly. strandedOpen reported 4 rows across
      Meadowood and Las Brisas, REPORTED AND NOT TOUCHED — the probe stayed a
      probe.
      SECOND SWEEP, 22:45 UTC, TWO MINUTES LATER — markedMissed 0, materialized
      0, alreadyClosed 3-4 per store per day, stranded counts identical. THE
      ENGINE ACTED ON FIRST CONTACT AND CHANGED NOTHING ON SECOND. R1's
      missed-is-a-written-fact and the idempotency requirement are both proven by
      the machine's own receipts, and proven the only way that separates a
      working writer from a busy one: the same request twice, the second one
      inert. The `closedAt: null` guard at route.ts:214 and the
      have-template/unique-index pair are what produced the zeros.
      MIGRATION B IS APPLIED ON THE STAGING DATABASE, by consequence rather than
      by assertion — 88 creates wrote closedAt, expectedStartAt and expectedEndAt
      (route.ts:250-262) and 2 updates wrote closedAt, none of which a table
      without those columns can accept. `migrate deploy` ran it in the staging
      build, as designed; it has NOT run against production (see the blocker).
      AUTH VERIFIED END-TO-END on the same deployment: 401 with no header, 401
      with a wrong token, 401 with an empty token, 200 with the provisioned
      secret. That closes CRON-DIAG from the other end — its finding was that the
      deployment held a STALE Preview CRON_SECRET because it was built 1h33m
      before the value was edited, and a redeploy past the edit is the whole fix.
      Read docs/prompts/CRON-DIAG_findings.md for the measurement.
      THE TEMP INSTRUMENTATION NEVER FIRED. "df96e9b" logged shapes on the 401
      path only, and no rejection happened once the client-side variable was
      loaded correctly, so the interpretation table written in advance
      (docs/prompts/CRON-DIAG-2_runbook.md) was never consulted. It is REVERTED
      in "32237ee", in this sitting, per that runbook's own not-optional revert
      step. PROOF, and note which grep: `git grep -n "CRON-DIAG-2" -- src`
      returns nothing, and `git diff 411557a HEAD -- src/` is empty, so the
      source tree is byte-identical to the commit before the instrumentation
      landed. The runbook's own form of the check (`-- ':!docs/prompts'`) was
      clean when the revert was made and now matches THIS PARAGRAPH — a doc
      naming the marker is not the marker surviving, and a proof that goes red
      when someone writes down that it passed is the wrong proof to leave
      behind.
      THE FOUR OPEN CHECKS BELOW WERE INHERITED BY CHK-4 AND ARE STILL OPEN
      AFTER IT — RIDER 2026-08-10, "c05365a". S4's prompt folded them into its
      verification plan and required a SQL query for each; the queries are
      written and are in that session's report for Gary to run on staging
      (`br-square-feather-a63z92vz`), and NOT ONE OF THEM IS ANSWERED HERE. S4
      queried no database: CLAUDE.md § Environment Variables forbids pulling a
      deployed environment's credentials to disk, and staging is inside that ban
      by the 2026-07-28 ruling precisely because a carve-out for it is one flag
      away from production.
      SO THE STANDING RESIDUAL AT THE END OF THIS BLOCK IS UNCHANGED AND IS NOW
      ONE SESSION OLDER. Four checks have been carried across two sessions on
      the strength of the writer's own report of what it wrote. That is strong
      evidence and it is not an independent read of the table, and the honest
      thing to record is that the gap did not close rather than that it was
      looked at again. Each item below keeps a result placeholder; whoever runs
      the query fills it in HERE, not in a session report.
      (1) WEEKLY NOT MATERIALISED — result: FAILED ON STAGING 2026-08-10, AND
      THE CHECK AS WRITTEN WOULD HAVE PASSED. 24 non-Daily Missed rows on
      `br-square-feather`; production clean (0/0). The materialisation gate held
      and the closing site had none, so the field this check named was reporting
      truthfully about the site where nothing was going wrong. Fixed in
      "1c907d2"; cause, ruling and the surviving unknown are in the defect rider
      at the top of this row.
      (2) isClosed WEEKDAY MATERIALISES NOTHING — result: __________
      (3) THE TWO 409s (task-log, submit) — result: __________
      (4) PRE-DEPLOY ROWS UNTOUCHED (NULL expectations stay NULL) —
      result: __________
      ── THE 2026-08-09 OPEN-CHECKS BLOCK IS BELOW, UNEDITED ──
      OPEN CHECKS — NAMED, NOT CLAIMED. Each is substantially evidenced by the
      bodies and none is independently confirmed, so none of them is a pass.
      (1) WEEKLY NOT MATERIALISED: the `frequencySkipped` field is present in
      every day result (route.ts:164, 238) and the containment is in the code,
      but no value from it was quoted and nothing confirms a Weekly template
      exists on this branch to be skipped. Confirm with a body showing
      frequencySkipped > 0, or by counting materialised rows against their
      template's frequency for one closed day. (2) AN isClosed WEEKDAY
      MATERIALISES NOTHING (route.ts:223): eleven of twelve stores have no hours
      at all, so the closed-day branch may not have been exercised on either
      sweep. (3) THE TWO 409s on task-log and submit against a closed checklist:
      the cron's body says nothing about them either way — they are route
      behaviour and want two requests against one of the checklists this pass
      closed. (4) PRE-DEPLOY ROWS UNTOUCHED: the 4 stranded rows were reported
      and unchanged across both sweeps, which is the behaviour; what is missing is
      a count of open pre-lookback rows taken before and after. THE STANDING
      RESIDUAL BEHIND ALL FOUR: a response body is the WRITER'S OWN REPORT OF
      WHAT IT WROTE, not an independent read of the table. It is strong evidence
      and it is not the same evidence, which is why this block does not quietly
      upgrade it.
      ── PRE-VERIFICATION BLOCK BELOW, UNEDITED ──
      BUILT 2026-08-09 IN "d089a7c", NOT YET APPLIED AND NOT YET VERIFIED —
      which is why this is in_progress rather than staging. Prompt:
      docs/prompts/CHK-3_S3_lifecycle_engine.md. Migration B is HAND-AUTHORED
      AND UNRUN (prisma/migrations/20260809194500_chk3_checklist_lifecycle):
      Gary runs it on dev first, then pushes so staging and production take it
      through `migrate deploy` in the Vercel build. Nothing in this row is
      proven until the cron has closed a real day on staging and the SQL says
      so, each result naming its branch. The commit is CODE LANDING, not
      evidence.
      THE PRECHECK IS OWED BEFORE THE MIGRATION RUNS, PER BRANCH, AND IT
      GATES TWO STATEMENTS RATHER THAN ONE — Checklist_storeId_templateId_date_key
      and StoreHours_storeId_dayOfWeek_key. It was re-presented, NOT cited: the
      2026-08-09 zero in plan §0a measured a table that grows daily, and
      StoreHours has been taking writes from CHK-2's editor since that morning.
      A non-zero result on either DROPS THAT INDEX — from the migration AND from
      prisma/schema.prisma, or the two drift — and does not delay the migration.
      Neither index is load-bearing: the cron creates read-then-write with a
      P2002 catch, and CHK-2's writer replaces a week in a transaction.
      ITEM (a) IS RESOLVED HERE — StoreHours_storeId_dayOfWeek_key rides
      Migration B in "d089a7c", precheck-gated as above.
      ITEM (b) IS NOT RESOLVED AND WAS NEVER THIS SESSION'S TO RESOLVE. What S3
      owed it was to know before writing expectedWindow(), and to not "align"
      the two by copying labor's `e > s` check — both discharged. The
      disagreement is now WRITTEN DOWN AT THE SINGLE DEFINITION SITE, in the
      hours block of src/lib/checklist-lifecycle.ts, so it is a recorded fact
      rather than a discovery. The labor half stays DEBT-64.
      THE S1 INTEGRITY FINDING IS RESOLVED HERE IN "d089a7c", both ends of it:
      api/checklists/[id]/task-log/route.ts refuses a taskId that does not
      belong to this checklist's template (400), and submit counts DISTINCT logs
      whose task belongs to the template instead of raw log rows. Either half
      alone leaves the hole open. The day-close job recomputes completionRate
      the same way.
      WHAT SHIPPED BEYOND THE PLAN'S LETTER, so the diff is not read as narrower
      than it is: (a) EXPECTATIONS ARE FROZEN AT CREATE, in BOTH creation paths
      in api/checklists/route.ts, not only at day-close materialisation. Without
      it `completedLate` could never be true for a checklist anyone actually
      started, so plan §9's "a checklist completed after its window end has
      completedLate = true" was unreachable. Freezing at completion instead
      would have reopened the hole those two columns exist to close, one step
      later. The shared plumbing is src/app/api/checklists/expectations.ts; the
      window itself is still defined once, in the lib. (b) THE DAY-CLOSE SOURCE
      IS A FOUR-VALUE FACT, not a boolean — hours / closed-day / no-hours /
      no-close-time — because "the operator marked Sunday closed" and "nobody
      has ever set this store's hours" are different facts, and a report that
      merges them tells an operator to fix something they already did. CHK-4/5
      render it; the engine exposes it. (c) expectedWindow() reads
      operationalPhase through normalizePhase(), so a legacy "During Hours" row
      resolves to a window instead of silently getting none. (d) The
      stranded-open probe (a cron outage longer than the lookback) is BOUNDED to
      a stated 7-day window and the bound is in the response body — unbounded it
      would count every unfinished checklist ever created, a number that only
      grows and therefore signals nothing.
      A MATERIALISED MISSED ROW CARRIES NO sectionsSnapshot, AND THAT IS A
      RULING, NOT AN OMISSION. The S1 note on that column said the freeze also
      happens "at day close for a checklist never started"; S3's prompt says a
      materialised row has no task logs and no snapshot, and the prompt is
      right for this column's own reason — it is the AS-EXECUTED record, and a
      checklist nobody touched has no as-executed headings. Freezing today's
      names onto it would make a guess indistinguishable from a record, which is
      plan §12.5's ruling one row-type further on. Recorded as an appended S3
      correction on Checklist.sectionsSnapshot in prisma/schema.prisma; a
      materialised row renders live through src/lib/sections.ts.
      ── PRE-BUILD NOTES OF 2026-08-09 BELOW, UNEDITED ──
      CARRIED IN FROM S2's TRIAGE 2026-08-09 — TWO ITEMS, AND THEY EXISTED ONLY
      IN THAT SESSION'S REPORT UNTIL THIS COMMIT. S2's triage named both as
      ROWs and then did not file them; the S2 report is not the board. That is
      DEBT-37's rule firing on the session immediately after the one that
      recorded it firing (see the block below, carried in from S1's triage for
      exactly the same reason). Twice in two sessions is a pattern, not an
      accident: NAMING a row in a triage is not FILING it.
      (a) STOREHOURS NEEDS @@unique([storeId, dayOfWeek]) AND IT RIDES MIGRATION
      B. The table has never had one — verified at HEAD, prisma/schema.prisma:181
      — which is why CHK-2's writer had to replace the whole set in a
      transaction rather than upsert: there is no key to target. The table can
      therefore hold two rows for one weekday, and both readers resolve that
      with `.find()`, taking whichever comes back first. Add it to Migration B
      alongside Checklist_storeId_templateId_date_key. IT NEEDS THE SAME
      PRECHECK DISCIPLINE as that index and for a stronger reason: CHK-2's
      editor is now writing this table on staging, so a duplicate can be created
      between now and the day S3 runs. Re-measure per branch before applying;
      non-zero drops it rather than delaying the migration.
      (b) LABOR AND DAY CLOSE WILL DISAGREE ABOUT OVERNIGHT STORES, and S3 must
      know before it writes expectedWindow(). A store set 07:00-02:00 fails
      labor's `e > s` check at labor-plan.ts:202 and silently gets sales
      inference instead of its own hours; one-sided hours (open set, close
      blank) fail the same way. Plan §3.5 rules the OPPOSITE for day close —
      `closingTime <= openingTime` means the close instant is D+1, handled
      explicitly. So the same store will be judged on its real window by day
      close and on an inferred one by labor. THIS IS NOT S3's TO FIX — the fix
      is in labor-plan.ts, which S3 must not touch — but S3 must not
      accidentally "align" the two by copying labor's check either. The labor
      half is DEBT-64.
      ── CARRIED IN FROM S1's TRIAGE, UNEDITED ──
      CARRIED IN FROM S1's TRIAGE 2026-08-09 — AN INTEGRITY HOLE IN THE
      COMPLETION RECORD, PRE-EXISTING, AND THIS ROW IS THE RIGHT PLACE FOR IT
      because S3 is already rewriting the two routes involved.
      `POST /api/checklists/[id]/task-log` NEVER CHECKS THAT `taskId` BELONGS
      TO THE CHECKLIST'S TEMPLATE — it takes the id from the body, and the only
      constraint is the FK to Task. `POST /api/checklists/[id]/submit` then
      computes `completedTasks = checklist.taskLogs.length` against
      `template.tasks.length`, counting RAW LOG ROWS rather than logs matching
      the template. So logging task ids from another template inflates
      `completionRate` and can flip a checklist to `Completed` without its own
      tasks being done. No cross-tenant read and not reachable through the UI —
      it needs a hand-crafted request — but it is an integrity hole in the
      exact record CHK-1 exists to protect, and the two ends of it are 30 lines
      apart in files S3 already opens for `completedLate` and the 409.
      NOT FIXED IN S1 DELIBERATELY: CHK-1's MUST NOT TOUCH covers
      Checklist.status and any lifecycle behaviour, and the fix lands in
      `submit`. Scope it here rather than re-deriving it.
      RECORDED HERE BECAUSE IT WAS NOWHERE ELSE. It was raised at S1's triage
      and existed only in that session's transcript — DEBT-37's rule ("an
      observation that lives only in a transcript does not exist") firing on
      the session that was citing DEBT-37. Written down 2026-08-09 on the next
      session that looked for it, which is later than it should have been.
      S3 of five. Plan: docs/prompts/CHK-1_PLAN.md §§2.2, 2.3, 3, 4, 5.4.
      Closes DEBT-48's implement direction.
      THE MECHANISM IS A HYBRID, and the split follows R1's own language.
      OVERDUE IS EVALUATED ON READ — "a live nagging state", so no write, no
      scheduler, and correct the instant an offset changes. MISSED AND
      COMPLETED-LATE ARE WRITTEN — "a closed fact", and a fact needs a writer.
      That answers R2's parenthetical directly: the write is day close, and only
      day close.
      DAY CLOSE MUST MATERIALIZE, NOT MARK. The finding neither parked row could
      see: /store-view lists TEMPLATES, not checklists, and a Checklist row is
      created on "Start Checklist" (api/checklists/route.ts:130) or by the admin
      bulk button (:166). So a checklist nobody touched HAS NO ROW to write
      MISSED onto, and the job creates one.
      SHIPS: Migration B (additive) — closedAt, completedLate, expectedStartAt,
      expectedEndAt, two indexes, NO BACKFILL (the lifecycle starts at deploy;
      inventing expectations for historical rows would manufacture exactly the
      retroactive data DEBT-59 spent a session preventing). Plus
      src/lib/checklist-lifecycle.ts (five predicates, one definition each —
      DEBT-26's discipline), GET /api/cron/checklist-day-close registered hourly
      with a two-day lookback, completedLate written by submit, and 409 on
      task-log/submit for a closed checklist.
      THE UNIQUENESS INDEX IS IN, ON MEASURED EVIDENCE, AND CARRIES A STANDING
      RE-MEASURE: Checklist_storeId_templateId_date_key. Gary's precheck
      returned duplicate_groups 0 on all three live branches 2026-08-09 (plan
      §0a). RE-RUN IT BEFORE APPLYING — the table grows every day a checklist is
      started, by the very findFirst-then-create path the index forbids
      duplicates from, so a clean result in August is not a clean result on the
      day this ships. A non-zero re-measure DROPS the index; it does not delay
      the migration.
      WEEKLY AND MONTHLY TEMPLATES ARE NOT MATERIALIZED, and the exclusion is
      labelled on the report. Template.frequency is display-only — no generation
      path reads it (DEBT-61) — so materialising every active template would
      file a Weekly checklist as missed six days a week.
      PROVES IT WORKED: SQL and the cron's own response body, NO UI — which is
      what makes this session verifiable on its own. Missed marking, missed
      materialisation, idempotency on a second run, the weekly exclusion, an
      isClosed weekday materialising nothing, the 409 refusal, and pre-deploy
      rows untouched.
      MUST NOT TOUCH: any rendering. A chip on a screen in this session means
      the split has failed.
    blockers:
      - resolved: true
        text: >
          ALL THREE PROMOTION GATES RESOLVED BY GARY 2026-08-10, and the entry
          below is kept verbatim rather than rewritten — it records what was
          true on 2026-08-09, which is the only thing that makes the rulings
          legible. Each gate and how it fell:
          (1) CRON_SECRET ROTATION — **DEFERRED BY RULING, NOT DONE, AND NOT
          FORGOTTEN.** Gary assessed the blast radius as low: the endpoint is
          idempotent, it runs hourly on Vercel's schedule regardless of who else
          knows the value, and the worst an unauthorised caller achieves is
          making the same sweep happen sooner. Accepted risk. It is NOT a
          promotion gate any more; it moves to this row's `open` list as a
          low-priority item, to be done at the next secret-touching session by
          the CLAUDE.md § Environment Variables ritual. Recording it as accepted
          rather than closed is the point — a deferred rotation that reads as
          "handled" is how it stops being done at all.
          (2) DEBT-66 / `main` RED — **DOES NOT BLOCK A GIT-PUSH PROMOTION.**
          The measured mechanism is dashboard redeploys not attaching env vars;
          the git-pushed deploy of the SAME commit built clean, which is in that
          row's own evidence. So the failure is scoped to a path this promotion
          does not use. DEBT-66 STAYS OPEN, rescoped to the dashboard-redeploy
          path. PROMOTIONS HAPPEN BY GIT PUSH ONLY — that is now the rule, not
          the preference.
          (3) THE PRECHECK — RUN ON PRODUCTION 2026-08-10, and this is
          measurement, not the August 9 citation: branch
          `br-sparkling-block-a620qvg4`, `checklist_dupes 0`,
          `storehours_dupes 0`. BOTH UNIQUE INDEXES SHIP AS COMMITTED —
          `Checklist_storeId_templateId_date_key` and
          `StoreHours_storeId_dayOfWeek_key`, unchanged in
          prisma/migrations/20260809194500_chk3_checklist_lifecycle and in
          prisma/schema.prisma. The standing re-measure instruction is
          DISCHARGED for this promotion and only for it.
          ── THE 2026-08-09 GATE TEXT IS THE NEXT ENTRY, UNEDITED ──
      - resolved: true
        text: >
          RESOLVED 2026-08-10 BY THE RULINGS IN THE ENTRY ABOVE. Kept verbatim
          as written on 2026-08-09, per the preserve-and-mark convention: it is
          the claim the rulings answer, and editing it would leave the answers
          without their question.
          PRODUCTION PROMOTION IS GATED ON TWO THINGS AND NEITHER OF THEM IS CODE.
          (1) ROTATE CRON_SECRET FIRST. The value now deployed transited a chat
          transcript during today's diagnosis, so it is no longer secret in the
          sense the equality check at route.ts:91 depends on. Rotate it BEFORE this
          work reaches production, by the ritual in CLAUDE.md § Environment
          Variables — save the dashboard value, redeploy the TARGET branch reading
          the branch column, verify the new deployment's created time postdates the
          save, then fire — and put the new value in Gary's password manager AT
          CREATION, because a Vercel Sensitive var can never be read back
          afterwards. Production and Preview are separate rows; rotating one does
          not touch the other. (2) `main` IS RED AT "7ab7106" — DEBT-66. Three
          dashboard redeploys failed 2026-08-09, all `npm run vercel-build` exit 1
          before a single migration ran. Production still SERVES an earlier Ready
          deployment so nothing is down, and the code in this row is unaffected;
          the PROMOTION is what is blocked, and it stays blocked until that build
          is understood.
      - resolved: true
        text: >
          BEHIND CHK-2 — a real gate, not sequencing. Day close is driven by
          StoreHours (Gary's R2) and StoreHours has no writer until CHK-2 ships
          one, so on today's data every store would fall to the midnight+buffer
          fallback and the ruled mechanism would never be exercised at all.
          CLEARS WHEN: CHK-2 ships the StoreHours editor and at least one store
          has hours set to verify against.
    open:
      - >
        CLOSED 2026-08-10 — THE CRON_SECRET ROTATION IS DONE, AND THIS ENTRY IS
        PREPENDED RATHER THAN EDITED INTO THE ONE BELOW, per SQ-3's precedent
        for a bare-string `open` list: the deferral is the record of what was
        accepted and why, and it stays legible only if the answer sits beside
        it rather than on top of it. The value was MINTED 2026-08-10, recorded
        in Gary's password manager AT CREATION (the only moment a Vercel
        Sensitive var can ever be read), and set in BOTH environments —
        Production and Preview are separate rows and both were done, which is
        the half the ritual most often loses. VERIFIED LIVE by the morning's
        manual staging sweep: the endpoint answered 200 to the new value, so
        the deployment in front of it was built after the save, which is the
        CLAUDE.md § Environment Variables ordering check passing rather than
        being recited. The transcript-exposed value from 2026-08-09 is dead.
        NOTE WHAT THIS DOES TO THE ACCEPTED RISK BELOW: it removes it. The
        deferral was sound — an idempotent endpoint that Vercel fires hourly
        anyway — and it is now moot rather than vindicated.
      - >
        THE STAGING CLEANUP IS NOT RUN. 24 fiction rows on `br-square-feather`
        (template "test", dated 2026-08-07 and 2026-08-08, 12 stores each).
        The DELETE was drafted and approved 2026-08-10 and presented for Gary to
        run in the Neon console; Claude pulls no deployed credentials. It is
        signature-guarded — status Missed, closedAt set, startedAt and
        completedAt null, completionRate 0, sectionsSnapshot null, NO TaskLog
        rows, and `date < date_trunc('day', "createdAt")` — so it cannot reach a
        row anybody touched, and it RETURNs what it deleted so the count can be
        asserted against the measurement. CLOSES WHEN: the delete has run and
        returned 24. Production needs nothing (measured 0/0).
      - >
        RULING OWED — SHOULD A STARTED NON-DAILY CHECKLIST STILL CLOSE? The
        2026-08-10 gate is Daily-only at both sites, so a Weekly or Monthly
        checklist somebody genuinely started and abandoned now stays open
        forever and reads `overdue` indefinitely. The refinement that keeps both
        halves is to close a non-Daily row that has task logs or a `startedAt`
        and skip one that was merely generated — a judgement about what a
        started row means, which is why it was not assumed. Watch
        `frequencyLeftOpen` in the sweep bodies: while it stays 0 this is
        theoretical, and DEBT-61 (frequency-aware generation) removes most of
        the pressure behind it by not creating the rows in the first place.
      - >
        THE createdAt FLOOR CANNOT COVER A NEWLY ASSIGNED STORE, and this is a
        schema fact rather than an omission. `TemplateStoreAssignment` has no
        `createdAt`, and PATCH /api/templates/[id] deletes and recreates every
        assignment on each template edit, so "this store was added yesterday" is
        not merely unknown but unknowable. A store newly assigned to an OLD
        template can still take up to DAY_CLOSE_LOOKBACK_DAYS of misses for days
        it was not on the template. Fixing it means a timestamped assignment row
        that survives an edit, which is a migration and its own row.
      - >
        CRON_SECRET ROTATION DEFERRED BY RULING 2026-08-10 — low priority, do it
        at the next secret-touching session, by the CLAUDE.md § Environment
        Variables ritual (save → redeploy the TARGET branch, reading the branch
        column → verify the deployment's created time postdates the save → fire),
        and record the new value in Gary's password manager AT CREATION because a
        Sensitive Vercel var can never be read back. Production and Preview are
        separate rows; rotating one does not touch the other. WHY IT IS HERE
        RATHER THAN CLOSED: the deployed value transited a chat transcript on
        2026-08-09, so it is no longer secret in the sense the endpoint's
        equality check depends on. Gary accepted that risk — the endpoint is
        idempotent and Vercel fires it hourly anyway, so an unauthorised caller
        only makes the same sweep happen sooner — but ACCEPTED IS NOT FIXED, and
        an entry that disappears because it was downgraded is an entry nobody
        does.

  - id: CHK-4
    track: checklists
    size: M-L
    status: shipped
    shipped: 2026-08-10
    commits: ["c05365a", "bbb5734"]
    title: "Lifecycle surfaces — overdue flagged and never hidden, missed closed, offsets gain their meaning"
    notes: >
      ── CALL RESOLVED 2026-08-10 (AFTERNOON): CHECK 5 MEASURED ON STAGING.
      PREPENDED; NOTHING BELOW IS EDITED. ──
      TWO RECORDS DISAGREED, AND THE DISAGREEMENT IS SETTLED BY MEASUREMENT
      RATHER THAN BY CHOOSING A SENTENCE. The BOOKKEEP-3 prompt instructed
      carrying check 5 forward as "FIXED AND NOT YET SEEN in a browser"; the
      rider below, written in "3c8c25d", says it was seen for the first time
      during the production spot-check. Neither was ruled stale. Gary re-ran the
      check on staging on 2026-08-10 (afternoon), and this rider records what
      that run showed.
      THE MEASUREMENT: staging, Clerk instance verified-snapper-7, org
      `org_3G02wO4QlVVSWppi8aqlnSZnsDa` — read from
      `window.Clerk.organization.id` BEFORE testing rather than assumed from the
      org switcher, which is the step that makes the pairing citable. Carson
      hours 07:00-17:00, day close 20:00. A Before Opening template with
      Ends = 14 RENDERED THE CLAMP WARNING, naming Carson and the 8:00 PM day
      close — the exact store-dependent case that failed on 2026-08-09, and the
      first time this fix has been SEEN on a rendered page on a branch and SHA
      this row can name.
      BOUNDARY CASE NOT YET RUN: Ends = 13 lands exactly on the day close, where
      a strictly-after clamp should stay silent. It was not run, and it is now
      this row's one open verification — the difference between "the warning
      fires when it should" (shown) and "the clamp is strictly after, not
      at-or-after" (not shown).
      THE SENTENCE BELOW STANDS UNEDITED. This rider does not correct the
      production spot-check claim; it supplies the staging measurement beside
      it, on a pairing that can be cited.
      ── SHIPPED 2026-08-10 IN MERGE "d19cca6". PREPENDED; NOTHING BELOW IS
      EDITED. ──
      THE FLIP THE PROMOTION RIDER BELOW SAID WAS OWED. That rider set this row
      to `staging` and named `shipped` as a debt to the session after Gary's
      push, for the reason PhaseStatus forced: there is no "staging-verified"
      value, so the verification lived in prose while the status lagged one step
      behind it. The push happened, the production build went Ready, and the
      spot-check passed — so the status is now simply true and the prose no
      longer has to carry the difference. DEBT-54's convention: flip on the
      promotion, cite the merge, date it, edit nothing.
      WHAT REACHED PRODUCTION HERE: the overdue chips and banner that flag
      without ever hiding (R3, DEBT-48), Missed given its own style and a
      read-only execution page, the as-executed print stamp, the (i) explainer,
      DEBT-59's retired copy, and "bbb5734"'s clamp warning rebuilt onto the
      engine's own `endClampsAtDayClose`. CHECK 5 REMAINS THE WEAKEST OF THE
      SEVEN AND STAYS RECORDED AS SUCH — the clamp warning was verified eleven
      cases deep at the predicate level and seen on a rendered page for the
      first time during the production spot-check, not before it.
      ── THE PROMOTION RIDER IS NEXT, UNEDITED. ITS STATUS CLAIM IS ANSWERED
      ABOVE. ──
      ── PROMOTION RIDER 2026-08-10 (CHK phase promotion). THIS ROW MOVES TO
      `staging` AND IS VERIFIED THERE. PREPENDED; NOTHING BELOW IS EDITED. ──
      THE STATUS IS `staging`, NOT "staging-verified", AND THE PROMPT THAT ASKED
      FOR THE LATTER IS NOT WRONG SO MUCH AS UNSPELLABLE: PhaseStatus has five
      live values and that is not one of them (src/lib/roadmap.ts:13), so
      writing it type-errors the build. `staging` is the nearest true value —
      "merged to staging, on Vercel preview, NOT in prod" — and THE
      VERIFICATION IS WHAT THIS RIDER IS. `shipped` is owed to the next session,
      after Gary's push, per DEBT-45's flip-on-promotion pattern and the same
      one-step-ahead honesty TPL-2 and DEBT-41 each wrote down. Claude never
      pushes (CLAUDE.md § Git Rules), so nothing in this row is in production
      until he does.
      ORG AND INSTANCE FOR EVERY BROWSER OBSERVATION BELOW, on both days:
      org_3G02wO4QlVVSWppi8aqlnSZnsDa / verified-snapper-7, read from
      window.Clerk.organization.id BEFORE testing rather than after
      (CLAUDE.md § Browser Evidence, which asks for the instance as well as the
      id). Branch `staging`, database `br-square-feather-a63z92vz`.
      THE SEVEN CHECKS ARE WRITTEN OUT IN FULL HERE BECAUSE THIS ROW HAS NEVER
      CARRIED THEM. The S4 close-out session
      (docs/prompts/CHK-4_S4_closeout.md, committed with this rider) produced
      its code fix "bbb5734" AND NO DOCS COMMIT AT ALL — no rider, and the
      Square-hours row it was told to file was never filed either (it is filed
      now, as SQ-4). So the 2026-08-09 walkthrough has lived in a transcript for
      a day. That is DEBT-37 firing on the session whose ENTIRE PURPOSE was to
      write the record down, which is the third time in this phase (S1's triage,
      S2's triage, now S4's close-out) and therefore a pattern rather than an
      accident. The numbering exists so check 7 can be referred to by number.
      (1) THE (i) EXPLAINER AND THE DEBT-59 COPY RETIREMENT — PASSED
      2026-08-09. The explainer opens beside "Expected window" and reads in
      plain words end to end, including the blank-end case, the clamp rule and
      the no-hours fallback. "Recorded for reference — not yet used to show or
      hide checklists" is GONE from the screen and "Optional. Sets when this
      checklist is expected — it never hides it" is in its place. The sentence
      DEBT-59 left standing was retired by the session that made it false, which
      was the whole condition on retiring it.
      (2) OVERDUE FLAGS ON THE LIVE SURFACES — PASSED 2026-08-09. Store View
      (Carson) showed Overdue badges on Mid-Shift, Opener and both test
      templates, each reading "Expected by <time> — it can still be completed";
      Closer showed NO badge inside its own window, so the discrimination is
      per-window and not per-page. The execution page showed the amber Overdue
      banner and the checklist stayed completable — R3 rendered, and DEBT-48's
      whole argument (flagged, never hidden) observed rather than asserted.
      (3) THE STORES-PAGE FALLBACK SIGNAL — PASSED 2026-08-09. "No hours set —
      checklists for this store close at midnight + 3h…" with a Set Hours link
      on every store without hours, and Carson showing its full week once hours
      were set. The hours dialog's own copy confirms a Square resync never
      overwrites Froot hours. NOTE FOR ANYONE READING STAGING DATA LATER: hours
      were SET on Carson during this test (07:00-21:00 all week, the close later
      moved to 17:00), so Carson's row is a fixture of this walkthrough.
      (4) ADMIN MISSED RENDERING AND THE PRINT SHEET — PASSED 2026-08-09,
      Gary's words "working great"; the print view rendered correctly.
      (5) THE CLAMP WARNING ON THE TEMPLATE FORM — FAILED 2026-08-09, FIXED THE
      SAME DAY IN "bbb5734", AND THE FIX IS PREDICATE-VERIFIED RATHER THAN
      SCREEN-VERIFIED. The failure: Carson at 07:00-17:00 closes its day at
      20:00, and the form accepted Ends = 20 with no warning anywhere — field,
      inline or save. The cause was not a broken condition but an unasked one —
      the warning was gated `phase === "After Closing"`, because a client form
      with no store hours in it could only warn where the arithmetic is
      store-independent, and Gary's template was Before Opening. The fix stops
      re-deriving the clamp in the form: checklist-lifecycle.ts now exports
      `endClampsAtDayClose`, running the same anchor arithmetic against the same
      `dayCloseInstant`, and both template pages join hours and timezone onto
      the store list the form already had. ELEVEN CASES WERE RUN THROUGH THE
      PREDICATE and match the measured case exactly, INCLUDING the boundary
      (ends = 13 lands ON day close and stays silent; the clamp is strictly
      greater-than). WHAT IS STILL NOT DONE IS A RENDERED PAGE: bbb5734's own
      message says so, and the browser confirmation is item 3 on Gary's
      post-push production checklist in this promotion's DEPLOY_LOG entry. This
      check is recorded as FIXED AND NOT YET SEEN, which is a different claim
      from the other six.
      (6) MISSED IS ABSENT FROM THE CREW LIST WHILE ADMIN SHOWS IT — PASSED
      2026-08-10, after the manual 06:45 sweep gave the branch real Missed rows
      to be wrong about. Store View shows today only and misses inherit nothing.
      Note what the pass means and does not: /store-view is scoped to the
      current business day and day close never closes today, so R1 was already
      satisfied structurally and the filter is a GUARANTEE against a later scope
      change rather than a rule anybody watched fire. The S4 rider said exactly
      this before the check ran; the check did not upgrade it.
      (7) A MISSED ROW IS READ-ONLY AND REFUSES TASK INTERACTION (THE 409) —
      CLOSED AS ENGINE-PROVEN, WITH THE BROWSER PATH RECORDED AS A GAP, AND THE
      GAP IS FILED AS CHK-6. What is proven: S3 ships the 409 on both
      /api/checklists/[id]/task-log and /submit for a closed checklist, and the
      2026-08-10 close wrote `closedAt` on every row it closed — Carson's four
      went Missed/closed=true, two marked from started rows and two
      materialised. Every Missed row therefore carries the state the refusal
      keys on. WHAT NOBODY COULD DO IS WALK TO ONE IN A BROWSER: the admin
      Checklists page is today-only with no date navigation, so yesterday's
      Missed rows have no surface except the report's aggregates and a direct
      URL. The refusal is proven at the layer that enforces it and unproven at
      the layer a person uses, and that distinction is the honest closure rather
      than a pass.
      THE THREE CLOCK-DEPENDENT CHECKS CARRIED OUT OF S4 ARE ALL CLOSED, AND ONE
      OF THEM CLOSED SOMEWHERE ELSE. Checks (6) and (7) above are two of them;
      the third was the Weekly-exclusion query rerun, which closed on CHK-3's
      row 2026-08-10 as a FAILURE that the check as written would have passed —
      24 non-Daily Missed rows on staging, a `createdAt` floor and a
      closing-site gate in "1c907d2", and the lesson that a field's name is not
      evidence of what it counts. It is named here so the S4 close-out's list of
      three does not read as two-and-a-missing-one.
      THE RATIFICATION LINE IN THE S4 CLOSE-OUT PROMPT WAS LEFT BLANK, AND THE
      TWO RULINGS IT COVERED ARE THEREFORE PROCEEDED-ON, NOT RATIFIED — the
      drift-marker styling and STORE-role read-only visibility of missed rows.
      Neither has been contradicted by anything since and neither gates this
      promotion; recorded because a blank slot in a saved prompt is invisible
      afterwards, and "nobody objected" is not the same fact as "Gary agreed".
      ── THE 2026-08-10 S5 RIDER IS NEXT, UNEDITED. ITS CLAIM THAT THIS ROW DOES
      NOT FLIP AND ITS THREE CHECKS ARE STILL OWED WAS TRUE WHEN WRITTEN AND IS
      ANSWERED ABOVE. ──
      ── RIDER 2026-08-10 (CHK-5/S5). THIS ROW STAYS in_progress. PREPENDED;
      NOTHING BELOW IS EDITED. ──
      TWO CORRECTIONS AND ONE UNRESOLVED CHECK.
      (1) "NOT PUSHED" BELOW IS NOW STALE. It was true when written. Measured at
      the start of S5: `git log --oneline @{u}..` is empty on staging and
      origin/staging is level at "dc90ff6", which contains "c05365a", "8ff4cce",
      "bbb5734" and CHK-3's "1c907d2"/"dc90ff6". Gary pushed between sessions.
      The sentence is left standing per preserve-and-mark and corrected here.
      (2) THE DEPLOYED-SHA PRECONDITION IS SATISFIED, AND THAT HALF IS DONE FOR
      WHOEVER RUNS THE CHECKS. Measured 2026-08-10 with the CLAUDE.md-corrected
      method, not the dead `inspect --json | grep`: `vercel inspect
      froot-git-staging-indianathomas-2483s-projects.vercel.app` resolves the
      alias to deployment `froot-gbr8setuc-…` (Ready), and `vercel ls --meta
      githubCommitSha=dc90ff61cf3d4e16cd42ece98c4ac26668b43b92` returns that
      same deployment. Staging is serving exactly this code. Anyone running the
      three checks in the next session can skip straight to the browser, PROVIDED
      no further push has landed — re-run both halves if one has.
      (3) THE THREE OPEN CHECKS WERE NOT RUN, AND THE REASON IS A CAPABILITY
      LIMIT, NOT AN OVERSIGHT. Gary's S5 input was "not yet run — run them in
      this session". They could not be: every one of them requires an
      AUTHENTICATED browser session on staging, staging sits behind Clerk (the
      sign-in wall renders "Development mode", the shared dev instance), no
      Chrome with an existing session was connected to this environment, and
      signing in is not something this session may do. The checks are therefore
      still owed, verbatim from the CHK-4 close-out prompt: (a) the crew list
      (store view) does NOT show yesterday's misses while admin does; (b) admin
      shows Missed; (c) open one Missed row → read-only → task interaction
      refused (the 409).
      CONSEQUENCE, STATED SO IT IS NOT LOST: this row does NOT flip to
      staging-verified in the S5 docs commit, because the S5 prompt made those
      three checks the condition for flipping it and they did not happen. The
      defect-trilogy resolution already on this row is unaffected.
      (4) THE "/reports IS UNTOUCHED" DEVIATION BELOW IS NOW SPENT — its
      consequence clause is the stale claim, not the deviation itself. The
      deviation was correct and remains a true record of what S4 did. What is no
      longer true is the sentence that follows it: "/reports's four tiles keep
      under-reporting and its per-store table keeps over-counting, in opposite
      directions". S5 closed DEBT-63 in "5190fd9" — the total is now the row
      count, the five buckets partition it, Missed is in both the tiles and the
      table, and the two halves of the page agree. The deviation's own reasoning
      was vindicated: parking it for S5 is what let the fix be the denominator
      rather than a sixth bucket added to a sum that was already wrong.
      ── THE 2026-08-10 BUILD RECORD IS NEXT, UNEDITED ──
      BUILT 2026-08-10 IN "c05365a", NOT PUSHED AND NOT YET VERIFIED IN A
      BROWSER — which is why this is in_progress rather than staging. Prompt:
      docs/prompts/CHK-4_S4_lifecycle_visible.md. The commit is CODE LANDING,
      not evidence; the S4 walkthrough is in that session's report and it is
      Gary's to run, org-ID-first.
      ONE NEW FILE, AND IT OWNS WORDS AND COLOURS RATHER THAN LOGIC.
      src/lib/checklist-status-display.ts. No predicate was added and none was
      re-derived — src/lib/checklist-lifecycle.ts is still the only definition of
      what overdue/missed/upcoming mean (DEBT-26). What it holds is the shared
      badge vocabulary and ONE rule that genuinely had two available answers:
      A CHECKLIST ROW IS JUDGED AGAINST THE WINDOW FROZEN ON IT; A TEMPLATE WITH
      NO ROW YET IS JUDGED AGAINST THE WINDOW COMPUTED FOR TODAY. The first half
      is what expectedStartAt/expectedEndAt exist for and is the only correct
      answer for a past day, which the execution page and print sheet both
      render; it also keeps the overdue banner from contradicting the
      completed-late badge, since submit judges lateness against that same
      column. The second half is the DEBT-48 scenario the phase exists for — the
      11am employee must see "Opening — Overdue" for a checklist that has no row.
      WHAT SHIPPED BEYOND THE PLAN'S LETTER, so the diff is not read as narrower
      or wider than it is:
      (a) THE MISSED FILTER ON THE CREW LIST IS A GUARANTEE, NOT A FILTER THAT
      FIRES TODAY. /store-view is scoped to the current business day and day
      close never closes today, so no row reachable there can be Missed and R1
      is already satisfied structurally. The filter states the rule anyway so a
      later change to either scope cannot silently put an unactionable card in
      front of a crew. Said out loud because "we shipped the filter" would
      otherwise read as "we saw it work".
      (b) THE SAME FACT MAKES /checklists's MISSED PILL UNREACHABLE IN NORMAL
      USE. That page is today-scoped too, so the pill is correctness insurance
      against the :117 fallthrough (DEBT-63's second finding) rather than a
      surface anyone will see Missed on. THE ADMIN SURFACE THAT ACTUALLY REPORTS
      MISSED IS CHK-5's /reports/operations, and until it ships, production
      Missed rows are visible only by SQL or by direct link to the execution
      page. That is a real gap between S4 and S5 and it is named here rather
      than discovered.
      (c) THE STAFF NAV BADGE WAS CONFIRMED AND NOT CHANGED — the filter is an
      explicit Pending/In Progress allow-list and Missed drops out with no edit.
      Its visible consequence is new though: a STAFF user with nothing open will
      now see the Checklists item DISAPPEAR, because day close sweeps stale rows
      to Missed. That is the badge becoming truthful, not breaking.
      (d) THE CLAMP WARNING COVERS "After Closing" ONLY, and the gap is
      deliberate. That case is store-independent arithmetic (end = close +
      offset, day close = close + grace). "Before Opening" depends on the
      store's open span, which a client form applying to all stores cannot know,
      so the (i) explainer states the clamp unconditionally instead of inventing
      one store's answer.
      (e) TWO CRON-HARDENING ITEMS FROM S3's TRIAGE ARE COMMENTS AT THE SITE,
      NOT CODE — log rejected requests, and .trim() plus a constant-time secret
      comparison. api/cron/checklist-day-close/route.ts is in the diff with no
      behavioural change.
      DEVIATION FROM THE PLAN, FLAGGED RATHER THAN QUIET: /reports IS UNTOUCHED.
      Plan §9 and the S4 skeleton both say to add Missed to its counts; the
      session prompt parks /reports for S5 and forbids fixing report tiles here.
      DEBT-63 is why the prompt is right — that sum already silently omits
      Non-Compliant, so adding one status to it widens the defect while looking
      like maintenance. The consequence is that /reports's four tiles keep
      under-reporting and its per-store table keeps over-counting, in opposite
      directions, exactly as DEBT-63's CHK-3 rider predicted.
      STILL OWED BEFORE THIS ROW MOVES: the staging walkthrough, including the
      NEGATIVE (nothing is ever hidden by a window), blank offsets never overdue,
      AllDay never overdue, and THE DEBT-59 NINE CHECKS A SECOND TIME
      (docs/prompts/DEBT-59_AUDIT.md §7), owed because template-form.tsx was
      touched again.
      ── PRE-BUILD BLOCK OF 2026-08-09 BELOW, UNEDITED ──
      S4 of five. Plan: docs/prompts/CHK-1_PLAN.md §§5, 6.1, 6.2.
      SHIPS: store-view Overdue and Upcoming chips; execution-page overdue
      banner and read-only missed state; `Missed` added to /checklists
      STATUS_STYLES; Missed added to the /reports counts; print renders
      as-executed headings from sectionsSnapshot plus a MISSED stamp; the (i)
      explainer beside the offset fields; and DEBT-59's "not yet used" copy
      retired.
      THE COPY CHANGE IS EARNED HERE AND NOWHERE EARLIER. DEBT-59 left the
      offsets box honest — "Recorded for reference — not yet used to show or
      hide checklists" — and that sentence is retired only by the session that
      makes it false, which is this one. Prepend to the DEBT-29 comment blocks
      at template-form.tsx:1007-1009 and :1048-1055; never edit them.
      PROVES IT WORKED: the NEGATIVE is the load-bearing check — NOTHING IS EVER
      HIDDEN BY A WINDOW (R3, and DEBT-48's whole overdue-not-hidden argument).
      Plus: blank offsets never go overdue; AllDay never goes overdue; the
      Missed style is its own, not Pending's "Not Started" fallthrough; /reports
      totals include Missed; the STAFF nav badge does not count Missed; and the
      DEBT-59 nine checks a SECOND time, owed because template-form.tsx is
      touched again.
      MUST NOT TOUCH: the cron, the predicates, the schema.
    blockers:
      - resolved: true
        text: >
          RESOLVED 2026-08-10 ON ITS OWN TERMS. Both named gates shipped to
          production in merge "ddaa216" — CHK-3 with its four lifecycle columns
          and its hourly sweep (12 stores, 24 store-days closed, 0 errors, read
          from the production Runtime Logs), CHK-1 with Migration A's backfill
          confirmed on `br-sparkling-block-a620qvg4`. The entry below is kept
          verbatim per the preserve-and-mark convention.
          ── THE 2026-08-09 GATE TEXT IS NEXT, UNEDITED ──
      - resolved: true
        text: >
          BEHIND CHK-3 AND CHK-1 — two real gates. CHK-3 writes the state this row
          renders (there is no overdue and no missed to show before it), and CHK-1
          writes the sectionsSnapshot the as-executed print headings read.
          CLEARS WHEN: both ship.

  - id: CHK-5
    track: checklists
    size: M
    status: shipped
    shipped: 2026-08-10
    commits: ["5190fd9", "230f019"]
    title: "Operations report — missed and completed-late, by store, by day, by template"
    notes: >
      ── CALLS RESOLVED 2026-08-10 (AFTERNOON): DEBT-63 MEASURED ON PRODUCTION,
      DEBT-68 RULED, THE DOCS-ONLY MERGE NOTED. PREPENDED; NOTHING BELOW IS
      EDITED. ──
      RATIFIED BY GARY 2026-08-10, ON THE TWO QUERIES THE RIDER BELOW
      RECLASSIFIED: (a) the report reconciling against direct SQL IS softened to
      an optional confirmation — the report absorbed a live day close under
      observation, which is stronger evidence than a static reconcile, because
      it shows the surface moving with the facts rather than agreeing with them
      once; and (b) DEBT-63's partition check IS KEPT, and it has now RUN.
      THE MEASUREMENT, BRANCH NAMED PER CLAUDE.md § DATABASE EVIDENCE, ALL
      RESULTS SUPPLIED BY GARY FROM THE NEON CONSOLE. Production,
      `br-sparkling-block-a620qvg4` — status census: Completed 1, In Progress
      11, Missed 84, Non-Compliant 1, Pending 15 (112 total). Page window
      (/reports "Past Week" = 2026-08-07..2026-08-10): tiles Total 86,
      Completed 0, In Progress 0, Pending 1, Non-Compliant 1, Missed 84. Per-day
      SQL over the same window sums identically: 28+28+28+2 = 86, missed 84,
      pending 1, non-compliant 1. THE FIVE BUCKETS PARTITION THE TOTAL AND THE
      PAGE MATCHES THE TABLE — DEBT-63's fix measured true on production, not
      merely true by construction.
      SHAPE NOTE FOR LATER READERS, so nobody re-derives it from the numbers:
      84 = 28/day x 3 days (Aug 7-9), the first sweeps materialising against a
      branch where only Carson has hours and no store works checklists yet. The
      18 rows outside the window — including production's only Completed row and
      4 In Progress — are pre-lifecycle stragglers, untouched by day close as
      designed.
      DEBT-68 RULED 2026-08-10 (GARY), ANSWERING THE SENTENCE BELOW THAT RECORDS
      THE FLIP INSTRUCTION NAMING "DEBT-68": THE ROW NEVER EXISTED, THE
      REFERENCE WAS A TYPO, AND NOTHING WAS NAMED-AND-UNFILED. The ruling's own
      reasoning is what makes absence conclusive here: "cleared" is not a
      possible state for a debt id — under preserve-and-mark a closed row still
      stands on the board with its status flipped — so an id that appears
      nowhere was never filed, rather than filed and removed.
      THE DOCS-ONLY MERGE, RECORDED HERE BECAUSE IT IS NOT A PROMOTION AND GETS
      NO DEPLOY_LOG ENTRY OF ITS OWN (the 17dc723/18220bb precedent). The
      "3c8c25d" board flips reached `main` on 2026-08-10 (afternoon) in
      docs-only --no-ff merge "65abb74", parents "5c9dbb3" and "3c8c25d",
      touching `docs/ROADMAP.yaml` alone (+116/-2, which is exactly "3c8c25d"'s
      own diff). Build green on that merge, run by Gary this session. TWO PARTS
      OF THIS ARE STILL OWED AND ARE MARKED PENDING RATHER THAN WRITTEN AS DONE:
      the merge was UNPUSHED at the time of writing (`origin/main` still
      "5c9dbb3", local `main` two ahead), and the /internal/roadmap render was
      not observed. THE NEXT REAL PROMOTION'S DEPLOY_LOG ENTRY MUST NAME
      "65abb74" so `main`'s push history reconciles — that obligation is the
      point of recording this at all, and it holds whether or not the two
      pending parts are ever written down.
      A CORRECTION TO THE INSTRUCTION THAT ASKED FOR THIS NOTE, kept because it
      will mislead the next reader otherwise: the /internal/roadmap "last
      updated" label does NOT read the merge date. `scripts/generate-roadmap.mjs`
      resolves it from the git commit date OF `docs/ROADMAP.yaml`, so the
      afternoon build printed "2026-08-10T09:05:46" — "3c8c25d"'s timestamp —
      and not "65abb74"'s 14:25:15. A docs-only merge that changes no file
      beyond what its parent already changed can never move that label. Checking
      the label against a merge SHA would therefore fail forever, for a reason
      that has nothing to do with whether the merge landed.
      ── SHIPPED 2026-08-10 IN MERGE "d19cca6", AND WITH IT THE WHOLE CHK PHASE.
      PREPENDED; NOTHING BELOW IS EDITED. ──
      THE PHASE IS COMPLETE: FIVE SESSIONS BUILT, VERIFIED AND SHIPPED. CHK-1
      sections entity, CHK-2 day-close inputs and CHK-3 lifecycle engine reached
      production in merge "ddaa216" on the morning of 2026-08-10; CHK-4
      lifecycle surfaces and CHK-5 this report reached it in merge "d19cca6"
      that evening, carrying CHK-3's defect fix "1c907d2" with them. Two
      promotions, one day, no migration in the second. The asymmetry this row
      recorded a few hours earlier — three-fifths in production, two-fifths in
      commits nobody had deployed, an engine writing closed facts no production
      user could read — is closed. THE FACTS AND THE SURFACE THAT SHOWS THEM ARE
      NOW ON THE SAME SIDE OF THE PROMOTION.
      PRODUCTION EVIDENCE, GARY 2026-08-10, BRANCH `br-sparkling-block-a620qvg4`
      — TWO QUERIES, AND THE SECOND IS THE ONE THIS PHASE HAS BEEN WAITING FOR.
      (a) TEMPLATE CENSUS: 8 templates, ALL `isActive=true` /
      `isArchived=false`. So the archived-and-inactive generation gate ships as
      PURE PROTECTION — zero behaviour change on day one, nothing stopped
      generating, no cleanup owed. Read that as the gate being INERT AND
      CORRECT, not as the gate being unnecessary: DEBT-65's whole lesson was
      that the first fix guarded a state no operator had ever created, and the
      answer to "which flag do operators actually use" is measured per branch
      rather than assumed. Staging's five deactivated templates did not
      reproduce here, which is exactly why the DEPLOY_LOG entry refused to carry
      that number across.
      (b) THE HEARTBEAT IS REAL AND UNATTENDED: 28 rows closed for dates >=
      2026-08-09, last close 2026-08-10 10:00:28 — the SCHEDULED sweep, with
      nobody triggering it. FIRST PROOF THAT THE PRODUCTION HEARTBEAT OPERATES
      WITHOUT A MANUAL TRIGGER SINCE THE ENGINE SHIPPED, and the answer to
      CHK-3's no-heartbeat rider, which recorded that every staging sweep ever
      run was manual and that staging therefore could never demonstrate this.
      The distinction that rider drew — "the job does the right thing when
      invoked" versus "the job runs" — is now settled on the only branch where
      it could be: production days close themselves.
      WHAT REMAINS OF THIS PHASE IS ITS CHILDREN, NOT ITS WORK. Filed follow-ons,
      as pointers rather than restated scope: CHK-6 (past-day checklists are not
      browsable — date navigation or a drill-through from this report, and the
      reason CHK-4's check 7 closed engine-proven rather than walked); SQ-4
      (import store hours from Square, which is the cheapest cure for the
      midnight-fallback disclosure this report has to print); DEBT-60 (printed
      and historical checklists render LIVE task text — DEBT-36's hole one layer
      out); DEBT-61 (`Template.frequency` read by no generation path — the
      exclusion now holds at three gates and is visible at the third, so what is
      left is generation-time frequency awareness); DEBT-67 (the templates
      "Active" tab counts the wrong flag, which is DEBT-65's two-flags problem
      surfacing in front of operators); and DEBT-64 (labor and day close read
      overnight StoreHours in opposite directions). Each is its own row with its
      own evidence; nothing above needs re-deriving to pick one up.
      NOTE ON A POINTER THAT DOES NOT RESOLVE: the flip instruction for this
      session also named "DEBT-68". NO SUCH ROW EXISTS — DEBT-67 is the highest
      filed id, and this session did not invent one to make the list come out
      even. If something was meant by it, it is unfiled and this sentence is the
      only trace of it.
      ── THE PROMOTION RIDER IS NEXT, UNEDITED. ITS STATUS CLAIM AND ITS
      "FOUR QUERIES OWED" ARE BOTH ANSWERED ABOVE — TWO WERE RUN, AND THE OTHER
      TWO (THE REPORT RECONCILING AGAINST DIRECT SQL, DEBT-63'S TOTAL AGAINST
      ITS TILES) REMAIN OPTIONAL CONFIRMATIONS OF A SURFACE NOW IN USE RATHER
      THAN GATES ON A STATUS. ──
      ── PROMOTION RIDER 2026-08-10 (CHK phase promotion). THIS ROW MOVES TO
      `staging` AND IS VERIFIED THERE. PREPENDED; NOTHING BELOW IS EDITED. ──
      THE STATUS READS `staging` FOR THE REASON GIVEN AT LENGTH ON CHK-4'S
      PROMOTION RIDER — there is no "staging-verified" in PhaseStatus, `staging`
      is the nearest true value, and `shipped` is owed to the session after
      Gary's push. Cited, not restated.
      THE WALKTHROUGH, GARY, 2026-08-09/10, ON STAGING, org
      org_3G02wO4QlVVSWppi8aqlnSZnsDa / verified-snapper-7 (read before testing
      on both days), branch `staging` / `br-square-feather-a63z92vz`.
      /reports/operations IS LIVE AND IT REPORTS: missed and completed-late by
      store, by day and by template, over a date range; the day-close source
      shown per store ("Store hours" for Carson, "Midnight fallback" for the
      eleven stores with none set); and totals that INCLUDE Missed — which is
      DEBT-63's under-count dead on screen rather than by construction. The
      count moved under observation and that is the strongest part of this
      evidence: 68 of 70 at the first read, 110 of 110 after the Aug-9 close
      landed. A report that absorbs a close is reading the table, not a cache.
      THE DISCLOSURE PANEL RENDERS, ALL THREE PARTS. "What this report does not
      cover" carries the Weekly line in the words that matter — "no misses"
      means "not tracked yet", not "all done" — plus the tracking-start
      disclosure and the eleven midnight-fallback stores NAMED rather than
      counted. Each of the three is a place a clean-looking number would
      otherwise mislead, which was the whole reason they were required on the
      page's own face.
      THE Aug-9 DAY CLOSE WAS VERIFIED BY QUERY, NOT BY THE WRITER'S OWN REPORT
      OF ITSELF — the standing residual CHK-3's row names four times. Read after
      a manual sweep at ~06:45 on 2026-08-10: Carson's four rows Missed with
      closed=true, TWO MARKED FROM STARTED ROWS AND TWO MATERIALISED, so both
      halves of the engine are exercised in one day; the Weekly "test" row
      correctly left Pending and OPEN, which is `templateInLifecycle` holding at
      the closing site — the exact hole "1c907d2" was written to close, observed
      shut; templatesExcluded 1 across all 12 stores; beforeTemplateCreation 0,
      so the `createdAt` floor manufactured nothing; errors 0. The report then
      absorbed the close (Carson 6 → 10, headline 110/110).
      THE CORRECTIONS ARC BEHIND THOSE NUMBERS IS CITED, NOT RESTATED, because
      each half already has its own record and re-telling it here would make a
      fourth copy to keep true: the phantom 17/3/14 and the flag-gate fix are on
      DEBT-65's correction rider ("230f019" — the first fix guarded the control
      nobody presses; at Keva archiving is performed with DEACTIVATE, five to
      zero), and the honest DELETE-0 closure is the same rider's finding that
      the cleanup statement matched nothing CORRECTLY. "230f019" is added to
      this row's commits because it ships on this promotion and changes what
      this report will show.
      ── THE CHK PHASE, CLOSED: FIVE SESSIONS BUILT AND VERIFIED. Supersedes the
      accounting block further down this row, which was written before the last
      two rows verified and says so honestly for its moment. ──
      CHK-1 sections entity — shipped, in production since merge "ddaa216",
      Migration A's backfill confirmed on `br-sparkling-block-a620qvg4`
      (unlinked_tasks 0, sections 32). CHK-2 day-close inputs — shipped, same
      merge; its StoreHours editor is what gave Carson real hours to be judged
      against. CHK-3 lifecycle engine — shipped, same merge, plus its defect fix
      "1c907d2" riding THIS promotion; the cron has closed real days on both
      branches. CHK-4 lifecycle surfaces — verified on staging today, seven
      checks, this promotion. CHK-5 operations report — verified on staging
      today, this row, this promotion.
      WHAT SHIPS IN THIS PROMOTION, so the entry is not read as the whole phase:
      S4's lifecycle made visible (overdue flags on store-view and the execution
      page, Missed rendered and read-only, the (i) explainer, DEBT-59's "not yet
      used" copy retired, the clamp warning rebuilt onto the engine's own
      predicate in "bbb5734"); S5's operations report with DEBT-63's under-count
      fixed and the three disclosures on the page's face; DEBT-65's archived-and
      -inactive gate now on BOTH generation paths ("230f019"); and CHK-3's
      defect trilogy — the closing-site frequency gate, the `createdAt` floor,
      and the five split counters ("1c907d2").
      WHAT REMAINS AFTER IT IS PRODUCTION EVIDENCE QUERIES ONLY. No code, no
      migration, no second drop. The three are enumerated further down this row
      — the report reconciling against direct SQL, DEBT-63's total equalling the
      sum of its tiles, and DEBT-65's cleanup exposure — and a fourth is added
      by this promotion: the archived/inactive census on production, stated as a
      query in this promotion's DEPLOY_LOG entry. All four run in the Neon
      console on `br-sparkling-block-a620qvg4`, each naming its branch, no
      credential pulled to disk.
      TWO CLAIMS BELOW ARE NOW FALSE AND ARE CORRECTED HERE RATHER THAN EDITED
      OUT. "NOT PUSHED AND NOT YET VERIFIED IN A BROWSER" was true when written;
      Gary pushed, and the walkthrough above is the browser pass. "STILL OWED
      BEFORE THIS ROW MOVES TO staging: a push; the deployed-SHA match; then the
      walkthrough" is discharged in all three parts — the deployed-SHA match was
      measured on the S5 rider of CHK-4 by the CLAUDE.md-corrected method, and
      re-measuring it is item 0 of any further staging work rather than a claim
      this rider inherits.
      ── THE 2026-08-10 BUILD RECORD IS NEXT, UNEDITED ──
      BUILT 2026-08-10 IN "5190fd9", NOT PUSHED AND NOT YET VERIFIED IN A
      BROWSER — in_progress rather than staging, for exactly the reason CHK-4
      carries the same status: Claude never pushes, so staging does not have
      this code, and CLAUDE.md § Staging Verification makes a matching deployed
      SHA a precondition rather than a checklist item. The session prompt asked
      for `staging` "on completion"; the deployed-SHA precondition is what
      completion means here, and it is not met. The commit is CODE LANDING, not
      evidence.
      WHAT SHIPPED: /reports/operations — missed and completed-late by
      store × day × template over a date range, three views over one query,
      gated by INHERITANCE from reports/layout.tsx (reports.view = MANAGE) with
      NO new capability and no permissions.ts edit, store-scoped through
      getUserStoreScope() with the ?store= param validated against what the user
      may actually see. Plus DEBT-63's fix and DEBT-65's ruling — both rows
      closed on this commit, riders there.
      IT DERIVES NOTHING, AND THAT IS THE LOAD-BEARING PROPERTY. Every state on
      the page comes from src/lib/checklist-lifecycle.ts — checklistState() for
      missed, isCompletedLate() for late, dayCloseAppliesTo() for what is
      tracked at all, frozenWindow() for the window a row was JUDGED against
      (never one recomputed from today's hours), dayCloseInstant().source for
      the fallback column. There is no `status === "Missed"` on the page. CHK-3's
      defect is why this is asserted rather than assumed: a second definition
      site does not disagree loudly, it disagrees quietly under a plausible name.
      THE NO-HOURS FALLBACK COLUMN completes the second visible signal CHK-3
      handed over. There is no `dayCloseSource` COLUMN on Checklist and none was
      added — the engine exposes the fact queryably, and the report asks it per
      store per date. The three fallback reasons stay apart ("you marked Sunday
      closed" vs "nobody has set this store's hours"), because a report that
      merges them tells an operator to fix something they already did.
      THE THREE DISCLOSURES ARE ON THE PAGE'S FACE IN PLAIN WORDS, WITH NO ROW
      IDS ON SCREEN: daily-only tracking, carrying the count of rows the
      exclusion dropped from the range in view; tracking-starts-at-deploy, with
      windowless rows in their own "No expected window" column and never
      reported as on-time; and which named stores are being judged against
      midnight instead of a real closing time.
      EVIDENCE SO FAR, AND WHAT IT DOES NOT COVER. dev
      `br-broad-wave-a6vpjdw0`, org `cf888f2d-f234-48c7-8097-fd5b44b5b3dd`,
      range 2026-06-28..2026-07-23: the page's own aggregation and direct SQL on
      the same branch RECONCILE exactly — total 26, missed 0, late 0,
      completed 1. All 26 dev rows predate Migration B, so all 26 land in the
      "No expected window" column rather than being counted on-time, and all
      six stores measured read "Midnight fallback — hours not set". BOTH
      HEADLINE COLUMNS ARE THEREFORE EXERCISED BY SHAPE AND NOT BY VALUE: dev
      has never run the cron, so missed and completed-late are structurally 0
      there, and a non-zero reconciliation is owed from staging. The staging and
      production numbers come from the Neon console per CLAUDE.md
      § Environment Variables — no deployed credential was pulled.
      NOTE ON THE clerkOrgId FOR THAT DEV ROW: it is
      `org_3FhYUR4l0ue7egug1I0Ig8wxOVn`, which is a PRODUCTION Clerk org id
      sitting in a dev-forked row — dev was branched from production and
      inherited it. Named here the way CLAUDE.md § Browser Evidence's own
      correction demands, so the id is not read back as a dev org later.
      STILL OWED BEFORE THIS ROW MOVES TO staging: a push; the deployed-SHA
      match; then the walkthrough — the report renders the recent misses by
      store, a completed-late row if one exists, the Weekly exclusion line
      carrying a non-zero count, the no-hours column, and /reports's totals
      reconciling against the row count.
      ── THE CHK PHASE, ALL FIVE SESSIONS ACCOUNTED FOR (S5's closing duty) ──
      CHK-1 shipped, in production via merge "ddaa216", Migration A's backfill
      confirmed on `br-sparkling-block-a620qvg4` (unlinked_tasks 0, sections
      32). CHK-2 shipped. CHK-3 shipped, in production via the same merge, with
      its defect fix in "1c907d2" and the day-close cron observed closing real
      days (24 store-days on production, Runtime Logs 2026-08-10). CHK-4 built
      in "c05365a" + "bbb5734", in_progress — its three browser checks are
      still open and its rider says why. CHK-5 built in "5190fd9",
      in_progress, this row.
      SO THE PHASE IS THREE-FIFTHS IN PRODUCTION AND TWO-FIFTHS IN COMMITS
      NOBODY HAS DEPLOYED. That asymmetry is the phase's actual state and it
      should not be smoothed over: the ENGINE is live and writing Missed rows on
      production right now, while both surfaces that would let a human READ them
      are unpushed. Production is currently accumulating closed facts that no
      production user can see.
      WHAT REMAINS BEFORE THE PHASE IS shipped — ONE PROMOTION AND THEN
      PRODUCTION EVIDENCE, in that order:
      (1) ONE PROMOTION. Push staging, verify CHK-4's three open checks and
      CHK-5's walkthrough there, then merge staging → main and promote. Both
      rows move on that one deploy; there is no second code drop in this phase.
      DEBT-66 is the standing hazard on that promotion — `main` was red at
      "7ab7106" with no database env attached to the build — and it must be
      understood before the merge, not during it.
      (2) PRODUCTION EVIDENCE QUERIES, run in the Neon console on
      `br-sparkling-block-a620qvg4` and each naming its branch, no credential
      pulled to disk. Three, and the third is the one nobody has asked yet:
      (a) THE REPORT RECONCILES — the operations report's headline numbers for a
      chosen range against direct SQL over `Checklist` joined to `Template`,
      counting missed as `closedAt is not null and status <> 'Completed'` and
      tracked as `frequency = 'Daily'`. The same query S5 ran on dev, where it
      reconciled exactly at total 26 / missed 0 / late 0 / completed 1.
      (b) DEBT-63's TOTAL IS HONEST — `count(*)` over the /reports window equals
      the sum of the five tiles, which is now true by construction and is worth
      one query precisely because it used to be false silently.
      (c) DEBT-65's CLEANUP EXPOSURE — how many production checklist rows belong
      to an archived template, split by never-started versus human-touched.
      That count decides whether the (a)/(b) SQL in the S5 report is run at all.
      Dev's answer was 0 archived templates and 0 such rows; staging and
      production are unmeasured.
      ── THE 2026-08-09 PRE-BUILD BLOCK IS NEXT, UNEDITED ──
      S5 of five, and the phase's closing session. Plan:
      docs/prompts/CHK-1_PLAN.md §7. Satisfies R4.
      SHIPS: /reports/operations behind the EXISTING `reports.view` capability
      (MANAGE — ADMIN + MANAGER, permissions.ts:171), store-scoped for MANAGER
      through getUserStoreScope the way /checklists already does. NO NEW
      CAPABILITY — TPL-1's Q3 reasoning verbatim: a second check on a surface
      whose existing gate already says the right thing can only ever disagree
      with it, and PERM-5 can already dial reports.view per user.
      THREE VIEWS over one query — by store, by day, by template — carrying
      missed count, completed-late count and completion rate across a date
      range. Completed-late sits BESIDE missed rather than buried (R4).
      THREE DISCLOSURES ON THE REPORT'S OWN FACE, each a place a clean-looking
      number would otherwise mislead: the lifecycle starts at deploy and
      pre-migration rows are excluded rather than counted on-time; Weekly and
      Monthly templates are not tracked, so "no misses" means "not tracked";
      and which stores are running on the midnight fallback because their hours
      are unset.
      PROVES IT WORKED: the role gate at all four roles; counts reconcile
      against direct SQL on the same branch, branch named.
      MUST NOT TOUCH: anything upstream. Pure read surface.
      CLOSES THE PHASE: file nothing new here — DEBT-60 through DEBT-63 are
      already filed — but write the closure riders on DEBT-36, DEBT-48 and
      DEBT-32, and flip DEBT-59's rider to say the copy retired.
    blockers:
      - resolved: true
        text: >
          RESOLVED 2026-08-10 — BOTH HALVES, and the second half is the one that
          mattered. CHK-3 shipped in merge "ddaa216", AND the cron has closed
          real days: 88 rows materialised across 12 stores on staging
          2026-08-09, and 24 store-days closed per hourly sweep on production
          (Runtime Logs, 2026-08-10T01:01:25Z and 02:01:24Z). So this row will
          open onto data rather than onto the empty tables the gate was written
          to prevent. The entry below is kept verbatim.
          WHAT CHK-5 INHERITS FROM S4, newly true: /reports/operations is now
          the ONLY admin surface that will report Missed. /checklists and
          /store-view are both scoped to the current business day and day close
          never closes today, so a materialised Missed row appears on neither —
          S4 mapped the status there for correctness, not for visibility.
          ── THE 2026-08-09 GATE TEXT IS NEXT, UNEDITED ──
      - resolved: true
        text: >
          BEHIND CHK-3 — a real gate. This row reports on closedAt, completedLate
          and the missed rows the day-close job writes; before CHK-3 there is no
          data and the three views would render empty tables that look like a
          clean operation.
          CLEARS WHEN: CHK-3 ships and the cron has closed at least one day.

  - id: CHK-6
    track: checklists
    size: S-M
    status: planned
    title: "Yesterday's misses are reported but not browsable — date navigation on /checklists, or a drill-through from the operations report"
    notes: >
      ── 2026-08-10: "LIVE THE DAY CHK-4/CHK-5 REACH PRODUCTION" BELOW — THAT
      DAY WAS 2026-08-10, IN MERGE "d19cca6". This row's cost is current, not
      future. Prepended; nothing below is edited. ──
      COST OF DOING NOTHING: a manager who reads "Carson missed four" on the
      operations report cannot open one of those four and look at it. LIVE THE
      DAY CHK-4/CHK-5 REACH PRODUCTION — not latent, because the report that
      creates the question ships in the same promotion as the surfaces that
      cannot answer it.
      FILED 2026-08-10 OUT OF THE CHK PROMOTION SESSION, from a gap found while
      closing CHK-4's check 7 rather than from a bug report. THE MEASURED FACT:
      the admin /checklists page is scoped to the current business day and has
      no date navigation, and /store-view is the same by design (R1 — a crew
      list must not show yesterday's unactionable work). Day close never closes
      today. So a Missed row is created by the sweep on a date the admin page
      cannot be pointed at, and its only surfaces are the operations report's
      AGGREGATES and a direct URL to the execution page for anyone who already
      has the id.
      WHAT IT COST ALREADY, WHICH IS WHY THIS IS A ROW AND NOT A COMMENT: CHK-4
      check 7 — a Missed row is read-only and refuses task interaction with a
      409 — could not be walked in a browser and closed as ENGINE-PROVEN with
      the browser path recorded as a gap. The refusal is proven at the layer
      that enforces it (S3 ships the 409 on task-log and submit for any row with
      `closedAt` set, and every row this morning's close touched carries it) and
      unproven at the layer a person uses. A verification that cannot be
      performed by the people who rely on it will not be performed later either.
      FIX SHAPE, EITHER END: (a) a date control on /checklists, which is the
      general answer and also gives the completed-late and Non-Compliant history
      a home; or (b) a drill-through from the operations report's store × day
      cell to the rows behind it, which is cheaper and answers the question at
      the place it gets asked. (b) alone leaves /checklists still unable to
      look backwards; (a) alone leaves the report a dead end. Prefer (b) first
      if only one ships, since the report is where a manager already is.
      MUST NOT: put yesterday's misses in front of a crew. R1 is a ruling, and
      the whole point of the today-scope on /store-view is that unactionable
      work does not belong on a working list. This row is about the ADMIN
      surfaces.
      Disconfirmed by: any admin path that reaches a past-day checklist without
      knowing its id. Related: CHK-4 (check 7, the gap's origin), CHK-5 (the
      report that raises the question), DEBT-48 (flagged, never hidden — the
      same argument one day later).

  # ---- Inventory ----

  - id: I-1
    track: inventory
    size: M-L
    status: shipped
    shipped: 2026-07-05
    commits: [10794e5, 3893e62]
    title: "Sales item sync + Ingredient library"
    notes: 10794e5 original, 3893e62 same-day v2 rebuild (SalesItem vs Ingredient split, units.ts, CSV import).

  - id: I-1b
    track: inventory
    size: M
    status: shipped
    shipped: 2026-07-05
    commits: [2172c83]
    title: "Ingredient parity, lifecycle & duplicates"
    notes: ROADMAP_ARCHIVE says Done 7-6; the commit is dated 7-05.

  - id: I-2
    track: inventory
    size: M-L
    status: shipped
    shipped: 2026-07-05
    commits: [376fe38, 2e610d9]
    title: "Vendors, POs & Receiving"
    notes: 2e610d9 rewired POs onto Ingredient. Receipts stamp receivedAt so purchases land in the right period.

  - id: I-3
    track: inventory
    size: S-M
    status: shipped
    shipped: 2026-07-06
    commits: [432a30e]
    title: "Storage areas"

  - id: I-4
    track: inventory
    size: M
    status: shipped
    shipped: 2026-07-06
    commits: [eb816db]
    title: "Physical counts"

  - id: I-5
    track: inventory
    size: M-L
    status: shipped
    shipped: 2026-07-06
    commits: [b3cb378]
    title: "Sales sync + COGS & analytics"

  - id: I-6
    track: inventory
    size: L
    status: shipped
    shipped: 2026-07-06
    commits: [4947def]
    title: "Recipes + needs-attention + adjustments"

  - id: I-7
    track: inventory
    size: M
    status: shipped
    shipped: 2026-07-07
    commits: [1020b9e, 542cf5e, d8b73e5, 5380324]
    title: "Reorder points & alerts (expected inventory, pars, Smart Cart, vendor minimums)"
    notes: 6 commits total, 1020b9e…5380324; the four listed are the identified parts.
    deferred:
      - Emailing orders to vendor reps
      - Alert-badge live refresh (14d staleness guard hardcoded; org-configurable later)

  - id: I-14
    track: inventory
    size: M-L
    status: shipped
    shipped: 2026-07-09
    commits: [d24d89f]
    title: "Team Messaging (Phase 1)"
    notes: Read acknowledgements (deferred here) shipped later in I-14b.
    deferred:
      - Twilio SMS delivery
      - Shortage alerts routed into the feed

  - id: I-14b
    track: inventory
    size: S-M
    status: shipped
    shipped: 2026-07-11
    commits: [cde3b86, 8a3667c, c26e81a, 270a317, 4c0a1de]
    title: "Shift handoff carry-over & acknowledge"
    notes: >
      Also fixed PHASE_ORDER to canonical "During the Day" with a legacy
      "During Hours" alias — see DEBT-1.

  # ---- Forecasting ----

  - id: F-1
    track: forecasting
    size: S-M
    status: shipped
    shipped: 2026-07-08
    commits: [b7feecb]
    title: "Forecasting — sales-data foundation"
    notes: >
      CLOSED BLOCKER (resolved 2026-07-27, text retained so the history
      survives). The blocker read: "CRON_SECRET not confirmed set in Vercel
      (flagged at ship, never confirmed closed) — nightly sales-reconcile and
      pace-alert crons silently don't run without it." Verified in the Vercel
      dashboard 2026-07-27: CRON_SECRET exists as Sensitive on BOTH Production
      and Preview, **Added Jul 8** — the same day F-1 shipped. It was set at
      ship time and simply never recorded. No backfill is required; the crons
      were never running unauthenticated and never silently skipped for want of
      the secret. This also unblocks F-5's pace-alert cron (F-5's separate
      notify.ts email-provider blocker still stands).
      EXECUTION EVIDENCE ADDED 2026-07-27. DOCS-2 closed this on env-var
      evidence — the secret exists. This supplies the missing half: proof the
      crons actually RUN. Vercel → Observability → Cron Jobs, Production, last
      12 hours:
        /api/cron/sales-reconcile  (0 11 * * *)  1 invocation, P75 14s
        /api/cron/pace-alerts      (0 15 * * *)  1 invocation, P75 30s
      One invocation each is exactly right for daily crons under the Hobby
      once-per-day cap. The DURATIONS are what actually close it: 14s and 30s
      are the profile of routes that did their work — pulling Square orders,
      walking stores. A cron rejected at the CRON_SECRET check would return a
      401 in milliseconds. Tens of seconds therefore rules out the precise
      failure mode this blocker described, crons that "silently don't run".
      Prior evidence, retained: SQ-1 (2026-07-26) gathered evidence against the
      blocker but did not clear it. `vercel env ls production`
      lists CRON_SECRET as Encrypted/Production, added 18 days ago (~2026-07-08,
      the F-1 ship date), so the secret does exist in the Production scope; what
      is still unconfirmed is that the crons actually invoke successfully (check
      the Vercel Cron Jobs page → View Logs). Separately, SQ-1 corrected the
      Pro-plan assumption in the docs: the account is on Hobby, and Hobby caps
      crons at ONCE PER DAY with ±59 min scheduling precision. Both registered
      crons comply — sales-reconcile `0 11 * * *` and pace-alerts `0 15 * * *`
      are daily — but they can fire anywhere inside their hour, and any future
      sub-daily expression will FAIL DEPLOYMENT, not fail quietly. maxDuration
      = 300 does not exceed the Hobby cap: with Fluid compute (on by default)
      Hobby's max duration is 300s, i.e. these routes sit exactly at the ceiling
      with no headroom. Vercel documents no plan-tier difference in
      CRON_SECRET handling, so F-1 is a secrets/verification problem rather
      than a plan-tier one.
      RULING RECORDED 2026-08-09 (Gary) — THE HOBBY CLAIM ABOVE IS STALE, AND
      THE SENTENCES CARRYING IT ARE LEFT STANDING ON PURPOSE. The account is on
      PRO: `GET /v2/teams/team_263mbnLkV4Te2Ca9ssqrV0gl` returns
      `billing.plan: "pro"`, measured 2026-08-09 during CRON-DIAG
      (docs/prompts/CRON-DIAG_findings.md §6). So crons are NOT capped at once
      per day, a sub-daily expression does NOT fail deployment, and CHK-3's
      hourly `/api/cron/checklist-day-close` (`0 * * * *`) is legal — by plan and
      empirically, since the staging deployment carrying that vercel.json built
      clean and the job has since closed real days. SQ-1's reading was CORRECT
      WHEN TAKEN on 2026-07-26 and the plan changed under it, which is why this
      is a dated correction rather than a retraction: the "one invocation each"
      observation above still stands, only its explanation was wrong.
      maxDuration = 300 is unaffected — Pro's ceiling is not lower than Hobby's.
      ONE LINE, PER THE RULING, ABOUT THE REST: Hobby-derived caution appears
      elsewhere — SQ-3 scopes its cron backstop as "viable on Hobby, where
      once-per-day is the cap", and docs/prompts/SQ-1_square_token_refresh_audit.md
      and docs/prompts/verification-smoke-pass.md reason from the same cap. Each
      DESERVES RE-READING against Pro limits when it is next picked up. They are
      deliberately not hunted down and edited here, and the docs/prompts/ ones
      never will be — a saved prompt is a claim wholesale (CLAUDE.md § Where
      documents live).

  - id: F-2
    track: forecasting
    size: M
    status: shipped
    shipped: 2026-07-08
    commits: [11f0b47]
    title: "Goal engine + Forecasting page"

  - id: F-3
    track: forecasting
    size: M
    status: shipped
    shipped: 2026-07-08
    commits: [11f0b47]
    title: "Forecasting overrides + import"

  - id: F-4
    track: forecasting
    size: S
    status: shipped
    shipped: 2026-07-10
    commits: [96bc048, cd711d7]
    title: "Dashboard live pacing — plan-driven Monthly Goal, Square order webhooks, all-locations rollup + store ranking"
    notes: >
      Seed correction: the items the 7-08 partial (96bc048) deferred — order
      webhooks, rollup, store ranking — all shipped 7-10 in cd711d7. Nothing
      remains deferred.
    blockers:
      - >
        RESOLVED 2026-08-16 — THE BLOCKER BELOW IS DISCHARGED ON ITS OWN TERMS.
        Prepended per preserve-and-mark; nothing below is edited. The entry below
        ends "WHAT REMAINS ON THIS ROW, and it is the whole of it: ONE REAL
        TRANSACTION ON A KEVA STORE, IN PRODUCTION, CONFIRMING 'Today so far'
        MOVES WITHOUT A MANUAL PAGE REFRESH. Nothing else closes it." It needed a
        store to be open. NINE WERE.
        THE EVIDENCE, and it is production on both halves. Neon console,
        PRODUCTION branch br-sparkling-block-a620qvg4 (endpoint ep-green-smoke),
        2026-08-16 ~21:10 UTC (2:10 PM PT): nine linked stores — Spanish Springs,
        South Reno, Meadowood Mall, Carson, Las Brisas, UNR, Sparks, University
        Village, Southgate — ALL with last_sync inside the previous ten minutes
        and latest_day = 2026-08-16. Keva Kiosk (17:12 UTC) and Cafe De Keva Cart
        (11:01 UTC) lag, consistent with lower volume or closure. Rohan's
        Restaurant is unlinked (squareLocationId null). Square Developer Console,
        PRODUCTION application sq0idp-UdjqLfkxl0hlbw7b30IiLA ("Froot"): 34,914
        delivery results, ALL 200, to
        https://www.usefroot.com/api/webhooks/square, most recent 2026-08-16
        13:18 PT.
        THE 200s ARE NOT THE PROOF AND THIS ROW ALREADY KNEW THAT — the handler
        ACKs before processing (route.ts:89-94), so a 200 proves acceptance only,
        exactly as the entry below says of the F-4 test event. What proves the
        resync path RAN is the sales caches themselves: nine stores whose
        syncedAt is minutes old on a live business date could not have got that
        way without the webhook, because the 15-minute lazy sync requires a
        request and the cron runs once at 11:00 UTC. Corroborated a third time by
        BUG-7's ~55 `discarded ... superseded by a newer fetch` lines in the same
        afternoon's production logs, every one of them on POST
        /api/webhooks/square — a line that can only be emitted by a sync the
        webhook triggered.
        THE FORM OF THE EVIDENCE, SAID PRECISELY so nobody has to guess later.
        This is server-side evidence — database freshness, delivery logs, and
        function logs — of the mechanism that makes "Today so far" move, taken
        continuously across nine stores over an afternoon. It is not a
        screenshot of the number changing in a browser. Gary ruled it closing,
        and it is stronger than the single-transaction observation the sentence
        below asked for; recording which kind of evidence it is costs nothing and
        stops a future reader inferring a browser pass that was not taken.
        TWO SQUARE PORTALS, DELIBERATELY — recorded here because the docs
        elsewhere carry a "staging Square app points at the production domain"
        gap that this arrangement is the resolution of, not an instance of.
        sq0idp-UdjqLfkxl0hlbw7b30IiLA ("Froot") is PRODUCTION;
        sq0idp-YPgmfGap_oYDRTyYIFG3zw ("Froot Staging") is STAGING. Two separate
        Square applications against the same real merchant account, so staging
        exercises real live data without sharing production's webhook
        subscription, signature key, or notification URL. Anything that reads as
        one app misconfigured is two apps working as designed; check the
        application id before filing it.
        WHAT THIS DOES NOT CLOSE. The entry below also records "PREVIEW IS NOT
        DONE" — Preview's NEXT_PUBLIC_APP_URL unconfirmed, and no subscription on
        the staging Square app, so the staging half still hits the identical 401.
        NONE of that is touched by the evidence above, which is entirely
        production. That half of the entry below stays open and this resolution
        does not reach it.
      - >
        PRODUCTION CONFIGURED 2026-08-13 BY GARY — STILL OPEN, AND WHAT REMAINS
        IS ONE SENTENCE LONG. Prepended; nothing below is edited. The two-part
        fix this row has demanded since 2026-07-27 is DONE for production. What
        it is NOT is closed: the closing evidence is a real transaction moving
        "Today so far" without a manual refresh, which needs a store to be open,
        which is tomorrow. Everything below is configuration; that is evidence.
        WHAT WAS DONE, production Square app sq0idp-UdjqLfkxl0hlbw7b30IiLA:
        webhook subscription registered, notification URL
        https://www.usefroot.com/api/webhooks/square, API version 2024-01-18,
        events order.created, order.updated, payment.created, payment.updated —
        the exact four the handler switches on (route.ts:44).
        SQUARE_WEBHOOK_SIGNATURE_KEY set in Vercel at Production scope, Sensitive,
        and production redeployed AFTER the save (the ordering CLAUDE.md
        § Provisioning insists on — a deployment carries the env values it was
        BUILT with).
        THE 401 THEN 200 IS THIS ROW'S OWN PREDICTION, CONFIRMED. The first test
        event returned 401; it went 200 only after Production's
        NEXT_PUBLIC_APP_URL was changed to https://www.usefroot.com and made
        non-sensitive. That is exactly the byte-for-byte notification-URL
        mismatch the F-4 FINAL audit named as the failure mode and the reason it
        flagged the Sensitive marking as a blocker rather than a nuisance
        (docs/prompts/F-4_FINAL_AUDIT.md § 1d). The handler derives the signed
        URL from the env var and not from the request, deliberately
        (src/lib/square-webhook.ts:9), which is what makes the two values have to
        agree to the byte.
        WHAT THE 200 PROVES, AND PRECISELY WHAT IT DOES NOT. Proves: the env var
        reached the running lambda, the notification URL matches
        NEXT_PUBLIC_APP_URL byte-for-byte, and HMAC verification passes. Does NOT
        prove anything downstream of it — the test event's location_id is
        Square's sample, not a Keva location, so NO SALES DATA MOVED. And note
        the handler ACKs before processing (route.ts:89-94), so a 200 is returned
        whether processEvent succeeds, no-ops or throws. A 200 can never be
        evidence about the resync path. The sample id will have resolved to no
        store and returned early at :107, which is correct behaviour and still
        not a test of anything that writes.
        API VERSION DISCREPANCY, RECORDED SO NOBODY "FIXES" IT LATER. The
        subscription is 2024-01-18. src/lib/square.ts:4 pins SQUARE_VERSION
        "2024-01-17", and Square's dropdown offers no such version — 2024-01-18
        is the nearest. THESE ARE NOT THE SAME KNOB AND DO NOT HAVE TO MATCH: the
        subscription version governs the shape of events Square SENDS US; the
        pinned constant is the Square-Version header on the calls WE SEND SQUARE.
        Different directions, independently versioned. The pin is also not
        broken — every outbound Square call in the product works today — so this
        is an unreconciled oddity, not a defect, and it is written down rather
        than explained because nobody has established WHY 2024-01-17 was chosen.
        L-2 already carries the same pin as a live constraint (it sits below the
        2025-05-21 floor the Labor/Timecard endpoints need); anyone changing it
        should read that row first.
        order.created AND order.updated CARRY SQUARE'S BETA TAG; the two payment
        events do not. Worth knowing because beta events can change shape or be
        withdrawn. The handler degrades rather than breaks if they do: it reads
        the location from `order_created ?? order_updated ?? payment`
        (route.ts:76), so payment.created/payment.updated alone still resolve a
        store and still trigger the resync. Reduced latency, not lost function.
        CHANGING NEXT_PUBLIC_APP_URL MOVED FOUR OTHER URLS WITH IT — FLAGGED, NOT
        DIAGNOSED, AND NOT PART OF THIS ROW. Five things derive from that env var
        besides the webhook. Gary updated one of them the same night (Square
        OAuth Production Redirect URL -> https://www.usefroot.com/api/square/callback).
        The others were not mentioned and are worth a look before anyone reports
        them as new breakage:
          - src/lib/instagram.ts:25 — the Instagram callback defaults to
            ${NEXT_PUBLIC_APP_URL}/api/instagram/callback and must match what is
            registered in the Meta app, unless INSTAGRAM_REDIRECT_URI is set.
          - src/app/api/users/route.ts:112 and
            src/app/api/staff/[id]/invite/route.ts:63 — Clerk invite redirects
            to ${NEXT_PUBLIC_APP_URL}/accept-invite.
          - src/lib/pace-alerts.ts:133 — already falls back to the literal
            "https://www.usefroot.com". THAT FALLBACK IS THE INTERESTING PART: the
            codebase has always assumed www.usefroot.com is production, so the old
            env value was drifted and last night's change corrected a
            long-standing mismatch rather than introducing one. Which also means
            the Square OAuth redirect had been wrong for as long as the env var
            was, and nobody noticed.
        PREVIEW IS NOT DONE. Only Production was configured. Preview's
        NEXT_PUBLIC_APP_URL has not been confirmed changed or made readable, and
        the staging Square app has no subscription — so the staging half will hit
        the identical 401 until both are addressed. The audit's step 4a stands.
        SUPERSEDES A CLAIM BELOW, SAID PLAINLY BECAUSE THE ENTRIES BELOW ARE NOT
        EDITED: the next two entries assert the signature key is "ABSENT from
        EVERY Vercel environment". That was true when written and is now true of
        PREVIEW ONLY. Production has it as of 2026-08-13.
        WHAT REMAINS ON THIS ROW, and it is the whole of it: ONE REAL TRANSACTION
        ON A KEVA STORE, IN PRODUCTION, CONFIRMING "Today so far" MOVES WITHOUT A
        MANUAL PAGE REFRESH. Nothing else closes it.
      - >
        RE-MEASURED 2026-08-13 — THE ENV VAR IS STILL ABSENT, AND THE
        DEGRADATION SENTENCE BELOW IS WRONG. Prepended; nothing below is
        edited. Session: docs/prompts/F-4_FINAL_AUDIT.md.
        STILL LIVE, unchanged: SQUARE_WEBHOOK_SIGNATURE_KEY is absent from
        every Vercel environment (`vercel env ls`, names and scopes only,
        2026-08-13). 34 days after the 2026-07-27 confirmation, nothing has
        been set. The two-part fix in the entry below stands verbatim.
        WHAT IS WRONG IS THE SEVERITY, and it is wrong in the safe direction
        for once. The entry below says pacing "has been silently degraded to
        the nightly sales-reconcile backfill" and that the dashboard "has been
        showing day-old pacing". IT HAS NOT. The 15-minute lazy sync is alive
        at HEAD in all three dashboard routes — /api/dashboard/summary:19,83,
        /api/dashboard/sales:18,192, /api/dashboard/rollup:18,66 — so
        docs/FORECASTING.md:121-124, which asserted the lazy sync survived as
        the fallback, was RIGHT and this row was wrong. Actual degradation
        since 2026-07-10 is 15-minutes-plus-one-request, not 24 hours.
        WHAT WAS GENUINELY LOST is sub-minute latency: "Today so far" no
        longer moves while an operator watches it. That is still a real
        regression of F-4's headline feature. It is not the nightly-backfill
        regression that was written down.
        NOTE WHICH SOURCE WON. Both claims were in the repo the whole time and
        they contradicted each other; the row's own prose was believed over
        the doc's for 17 days because a blocker reads as measured and a doc
        reads as aspirational. Neither is evidence. The routes are.
        A SECOND DEFECT WAS FOUND WHILE PROVING THIS AND IS NOT THIS ROW'S —
        see BUG-6. Gary's dashboard store-switch staleness is a separate code
        regression dating to 2026-07-23 (2081401, BUG-1 step 4), not a symptom
        of the dead webhook. THE TWO COMPOUND: with webhooks live the sales
        cache is rarely stale, so BUG-6's branch would almost never fire.
        THEREFORE ORDER MATTERS — verify BUG-6's fix on staging BEFORE
        registering any Square subscription, or the webhook masks it and
        neither fix can be attributed. Ruled by Gary 2026-08-13.
      - >
        CONFIRMED LIVE 2026-07-27 — not stale, and worse than "never confirmed
        closed" implied. SQUARE_WEBHOOK_SIGNATURE_KEY is ABSENT from EVERY
        Vercel environment. The Square vars that do exist are
        SQUARE_ENVIRONMENT, SQUARE_APPLICATION_SECRET, SQUARE_APPLICATION_ID,
        NEXT_PUBLIC_SQUARE_APP_ID and SQUARE_ACCESS_TOKEN. Consequence: order
        webhooks have NEVER been signature-verified in production, so F-4's
        headline feature — live intraday pacing — has been silently degraded to
        the nightly sales-reconcile backfill since 2026-07-10. The dashboard has
        been showing day-old pacing, not live pacing, CONTINUOUSLY SINCE
        2026-07-10, without erroring.
        THE DECAYING NUMBER WAS REPLACED BY ITS DATE 2026-08-06 (bookkeeping
        sweep). This sentence read "for 17 days" — correct on 2026-07-27, silently
        wrong every day after, and 27 days understated by the time anyone looked.
        A duration is only true on the day it is written; the start date stays
        true. Fixed here rather than left because this figure is quoted onto
        other rows (IG-1's first blocker cites "F-4 (17 days)"), so the stale
        arithmetic propagates.
      - >
        The fix is TWO parts and neither alone is sufficient: (a) set
        SQUARE_WEBHOOK_SIGNATURE_KEY per environment, and (b) register a webhook
        subscription in EACH Square app — the production app and the separate
        "Froot Staging" app, which have different keys. Setting the env var
        against an app with no subscription registered yields exactly the same
        silence as today.
    open:
      - >
        RE-CONFIRMED INDEPENDENTLY 2026-08-13 AT A LATER HEAD (ce036f9), by a
        second full reading of the route rather than by citing the entry below.
        Same answer, same lines: src/app/api/webhooks/square/route.ts:47-50
        returns 500 when SQUARE_WEBHOOK_SIGNATURE_KEY is unset, and :51-54 does
        the same for NEXT_PUBLIC_APP_URL. Both guards precede `await req.text()`
        at :57, so an unconfigured deployment never reads the body. No branch
        anywhere in the file skips verification; :59-62 gates :66's JSON.parse,
        and it parses the same rawBody that was signed. FAIL-CLOSED.
        THE FLAG THIS ENTRY CANNOT CARRY. The F-4 FINAL prompt asked for the
        `resolved` flag on this entry. It is not writable: `open` is typed
        `string[]` (src/lib/roadmap.ts:120) — bare strings only — so
        `{resolved: true, text}` type-errors the build. That is DEBT-42's
        deliberate state, not an oversight, and no count on /internal/roadmap
        reads this field, so the entry inflates nothing. Recorded here because
        the request will recur: the flag lives on `blockers` alone.
        THE GUARD NOW HAS A TEST, which is what actually protects this finding
        going forward. scripts/verify-f4-rollup-webhook.ts asserts the unset-key
        500 (37361ba). Until then the fixture set the key itself and the guard
        could have been deleted with all fourteen checks still green.
      - >
        RESOLVED 2026-08-06 AT SOURCE — FAILS CLOSED, CONFIRMED. Prepended as a
        new entry; the one below stays exactly as written, including its caveat,
        which is the part that turned out to matter.
        THE ROUTE WAS READ, which is what the entry below asked for and did not
        have. src/app/api/webhooks/square/route.ts:47 reads
        SQUARE_WEBHOOK_SIGNATURE_KEY and :48-50 returns HTTP 500
        ("SQUARE_WEBHOOK_SIGNATURE_KEY is not configured") when it is unset.
        That guard is the FIRST statement in the POST handler — it runs before
        `await req.text()` at :57, so with the key unset the body is never even
        read, let alone parsed or written. Fail-CLOSED in the strongest form.
        SO THE F-4 READING HOLDS: this is a BROKEN FEATURE, not an
        unauthenticated write endpoint. There has been NO exposure window since
        2026-07-10 and there is nothing to remediate beyond setting the key and
        registering the subscriptions (the two-part fix in the second blocker).
        The urgency stays at degraded pacing.
        THE DOCS CLAIM WAS RIGHT, AND THAT IS NOT THE LESSON. docs/FORECASTING.md:135
        said exactly this and the entry below still refused to rely on it,
        citing DEBT-3 and STAGING_SETUP.md as live examples of Froot docs
        asserting what the code no longer does. Being right this time does not
        make the caveat wrong — the source reading cost one minute and converted
        strong evidence into proof, and the fail-open branch would have changed
        the remediation ORDER. Superseding a docs claim with a source reading is
        the cheap half of this; refusing to act on the docs claim was the
        correct half.
        every event when the key is unset — the degradation described above) or
        fails OPEN (skips verification when the key is unset). Fail-open would
        make it an UNAUTHENTICATED WRITE ENDPOINT reachable by anyone who knows
        the URL, writing into the sales caches that drive pacing — a materially
        different and more urgent finding than degraded pacing. Logged
        2026-07-27, deliberately NOT investigated in that session; resolve this
        before the env var is set, since fail-open would mean the endpoint has
        been exposed since 7-10.
        LIKELY RESOLVED IN F-4's FAVOUR — FAILS CLOSED (2026-07-27).
        docs/FORECASTING.md:135 states, of SQUARE_WEBHOOK_SIGNATURE_KEY:
        "Without the env var the route returns 500 and Square will keep
        retrying." A 500 on every unverified event is fail-CLOSED. If that
        holds, this is a BROKEN FEATURE, not an unauthenticated write endpoint,
        and the urgency drops back to the degraded-pacing reading above — no
        exposure window since 7-10, nothing to remediate beyond setting the key
        and registering the subscriptions.
        CAVEAT, deliberately not glossed: this is a DOCS CLAIM, not a source
        reading. It has NOT been verified against
        src/app/api/webhooks/square/route.ts, and docs drift — DEBT-3 and the
        STAGING_SETUP.md debt row are both live examples of Froot docs asserting
        things the code no longer does. Treat this as strong evidence, not proof.
        VERIFY IN THE F-4 SESSION, by reading the route, before relying on it —
        and do that before setting the env var, since the fail-open reading
        would still change the remediation order.

  - id: F-5
    track: forecasting
    size: S
    status: shipped
    shipped: 2026-07-10
    commits: [cb3f1bb]
    title: "Forecasting polish — goal-edit audit log, CSV export, behind-pace alerts"
    notes: >
      Seed correction: seed said planned; shipped 7-10.
      CLOSED BLOCKER (2026-07-27): "Pace-alert cron depends on the F-1
      CRON_SECRET blocker" is resolved — CRON_SECRET was verified present as
      Sensitive on Production and Preview, added Jul 8. See F-1 notes. The
      notify.ts blocker below is untouched by this and still stands: the cron
      can now authenticate, but it still has nowhere to send mail.
    blockers:
      - >
        VERIFIED STILL TRUE 2026-07-27 (reconcile Pass 2a) — re-read at source,
        not assumed. src/lib/notify.ts:21 is still
        `console.log("[notify:console] ...")` and :26 documents
        NOTIFY_EMAIL_PROVIDER as "console" (default), with "resend" | "smtp"
        listed as future options. Pace alerts log to console only — no real
        email provider is wired into src/lib/notify.ts (also blocks HR-8
        reminders and HR-16). Re-verify at the next reconcile; a blocker with no
        verification date is a blocker nobody owns.

  # ---- HR / signing ----
  # Built 7-12…7-14, promoted to prod 7-21 (9743899) with HR dark in prod:
  # HR_MODULE_AVAILABLE is set on Preview only, unset in Production until launch.

  - id: HR-0
    track: hr
    size: S-M
    status: shipped
    shipped: 2026-07-21
    commits: [0233558, b185057, 5d44da4, 8aec033]
    title: "HR foundation — schema, availability gate, per-org toggle, gated nav"
    notes: >
      Built 7-12. HR stays dark in prod by design: HR_MODULE_AVAILABLE set on
      Vercel Preview only; Production unset until launch (HR_INTERNAL_ORG_IDS
      is the dogfooding path).

  - id: HR-1
    track: hr
    size: S
    status: shipped
    shipped: 2026-07-21
    commits: [f85c062, d68bd09, b7d854b]
    title: "Staff directory + detail shell"
    notes: Built 7-12.

  - id: HR-2
    track: hr
    size: S
    status: shipped
    shipped: 2026-07-21
    commits: [30f6e26, dd5c19d, 2d3d5bb]
    title: "Manager notes"
    notes: Built 7-12.

  - id: HR-3
    track: hr
    size: M
    status: shipped
    shipped: 2026-07-21
    commits: [1a03dca, 129b081, 5161e1e, eb0a05a, 3756ea0]
    title: "Reference Library + private HR file service (froot-hr Blob store)"
    notes: Built 7-12; eb0a05a/3756ea0 are upload-size follow-up fixes.

  - id: HR-4
    track: hr
    size: L
    status: shipped
    shipped: 2026-07-21
    commits: [fa94b21, 1a80294, 9bce225, 940af81]
    title: "Signed-record engine + acknowledgment documents"
    notes: Built 7-13. In-page pageRef stamping (deferred here) landed via HR-11b.
    deferred:
      - Drawn-signature PNG capture (typed-name capture per ESIGN stands)

  - id: HR-5
    track: hr
    size: M
    status: shipped
    shipped: 2026-07-21
    commits: [2cff79e, 92d5cf5, 530310b]
    title: "Fillable agreement forms (Key/Pay) — dual-signature execution"
    notes: Built 7-13.
    deferred:
      - Conditional fields

  - id: HR-6
    track: hr
    size: M
    status: shipped
    shipped: 2026-07-21
    commits: [1c8b4d1, e4468a7, 0dde473, ec60df1]
    title: "Training builder (Trainual-style authoring) + export/import"
    notes: Built 7-13; ec60df1 is the post-review export/import addendum.

  - id: HR-7
    track: hr
    size: L
    status: shipped
    shipped: 2026-07-21
    commits: [de743ca, 5cea90f, 2392ec8, 5ea03e3]
    title: "Training execution + staff self-service (/my), employee logins, termination rules"
    notes: Built 7-13. EMPLOYEE role split deferred to HR-9.

  - id: HR-7.5
    track: hr
    size: S
    status: shipped
    shipped: 2026-07-21
    commits: [7ce16bc]
    title: "Staff editing, per-member Square resync, import error surfacing"
    notes: Built 7-14.

  - id: HR-7.6
    track: hr
    size: S
    status: shipped
    shipped: 2026-07-21
    commits: [edf100b]
    title: "Manager-uploaded staff documents with team-visibility toggle"
    notes: Built 7-14.

  - id: HR-8
    track: hr
    size: M
    status: shipped
    shipped: 2026-07-23
    commits: [61eea85]
    title: "Compliance rollup dashboard — live-computed, /hr/compliance, real /staff column"
    notes: >
      Built 7-22, staging pass complete 7-22 (caught BUG-2), prod via 942bc59.
      No prod smoke test recorded. Follow-ups spun out as HR-11/12/13/14;
      reminder emails blocked on the notify.ts provider (see F-5 blocker).

  - id: HR-9
    track: hr
    size: S-M
    status: planned
    title: "EMPLOYEE role split — cut STAFF-role API permissions off for employee logins"

  - id: HR-10
    track: hr
    size: S-M
    status: planned
    title: "Required agreement forms — count forms in the compliance % (required designation + signing cycle)"

  - id: HR-11
    track: hr
    size: M
    status: shipped
    shipped: 2026-07-23
    commits: [2081401, baf4cef]
    title: "Inline signing ceremony — 4-phase flow, pdf.js viewer, per-page initialing, real per-interaction timestamps"
    notes: >
      Built 7-23 inside STAFF-1 (shared commit); prod via 942bc59. New dep
      pdfjs-dist, lazy-loaded on document routes only.

  # HR-11a does not exist — numbering goes HR-11 → HR-11b → HR-11c.

  - id: HR-11b
    track: hr
    size: M
    status: shipped
    shipped: 2026-07-24
    commits: [111fb31, 8b0f77d, c2fd8d5, 0547125, 738afa1, b31a309, 01c5ed9]
    title: "Signed-document field anchoring & inline stamping"
    notes: >
      Built 7-23 on staging; prod via 59a6cdc (2026-07-24, no-ff merge,
      rollback tag pre-staging-merge-20260724-2107). Migrations
      hr11b_document_anchors + staff_legal_name_lock, both additive.
      DocumentAnchor model, server-side detection at upload (unpdf), admin
      confirm UI, stamping at anchor coords before the certificate append;
      01c5ed9 adds per-signature checkpoints + timestamps. No prod smoke test
      recorded yet.
    open:
      - Initials-capture validation gap logged to HR-14(d) — ceremony accepts arbitrary text as initials.

  - id: HR-11c
    track: hr
    size: M
    status: shipped
    shipped: 2026-07-24
    commits: [8a4335f, 015d23e, 232d568]
    title: "Signing ceremony fixes — anchor dedup, affordance-at-line, identity chips, legal-name capture"
    notes: >
      8a4335f: dedupeAnchors collapses coincident duplicates (page + normText +
      markType, |Δx|,|Δy| ≤ 3, deterministic survivor), affordances at the
      anchor line, read-only identity chips during review. 015d23e/232d568
      (folded in): signature/certificate capture uses legal Full Name and shows
      name/store/date before signing. Prod via 59a6cdc (2026-07-24); promoted
      with the two blockers below still open — they were NOT re-verified
      pre-promotion and are now prod-verification items. Decisions in
      docs/DECISIONS.md.
    blockers:
      - >
        UNVERIFIED SINCE 2026-07-24 — flagged 2026-07-27 (reconcile Pass 2a),
        re-dated 2026-08-06. TRUE AGE IS NOW 13 DAYS, not the 3 this entry was
        written with.
        ONE OF THE TWO IS NOW CLOSED. The certificate org-name entry cleared
        2026-08-06 on HR-14's 2026-08-03 production observation — see the
        resolved entry directly below. So this note now covers ONE live item:
        the visual-QA/mobile-tuning check. Kept rather than deleted because the
        pattern it describes is the finding, and because the surviving item has
        the same no-owner problem it opened with.
        FOR GARY — A CALL IS OWED, same as IG-1's: assign an owner and a
        check-by date to the visual-QA item, or drop this entry and accept it is
        carried unowned. Not a bookkeeping session's call to make.
        THE CHEAP PATH IS UNCHANGED AND IS NOW CHEAPER: fold the remaining check
        into the next production visit rather than scheduling it. That visit
        already has company — SEC-1's open item asks for a production test
        account, and BUILD-2 needs its production column/index check plus five
        UI checks. One visit settles all of them.
        NOTE ON WHAT CLOSED IT: the answer existed on another row for three days
        before anyone carried it here. That is the same failure this file keeps
        finding — a fact recorded where the question isn't.
      - resolved: true
        text: >
          GATE CLEARED 2026-08-06 by carrying an answer that already existed.
          The entry below asked for a real signing in production; HR-14's gate
          walk did one on 2026-08-03 and recorded the result on its own row,
          where nobody looking at HR-11c would find it. Nothing new was
          observed here — this is a cross-reference, not a verification.
          THE FACT, and only this fact: on branch production
          (br-sparkling-block-a620qvg4), organization
          cf888f2d-f234-48c7-8097-fd5b44b5b3dd, a freshly rendered Certificate
          of Acknowledgment reads "Generated by Froot for Keva Juice" — NOT
          "Microsoft". The dev-instance test-org name does not reproduce in
          production, which is exactly what this entry asked.
          CARRIED NARROWLY, per HR-14's own instruction not to over-claim from
          it. It settles the certificate's org name on production today and
          NOTHING else: not the rename handler (organization.updated still
          writes name and never slug), not the P2025 missing-row hazard (still
          update rather than upsert, still untry/catch'd), and nothing about
          staging, where five rows still answer to "Microsoft". Those remain
          HR-14's substance and are untouched by this closure.
      - resolved: true
        text: >
          Certificate shows 'Microsoft' as org name — was a hard prod-promotion
          gate; shipped un-re-tested. 232d568's certificate dual-name may have
          fixed it ("Microsoft" is the Clerk DEV-instance test org, so prod may
          not reproduce) — verify a real signing in prod.
      - >
        Visual QA unverified — lift offsets (−118% buttons, −90% chips) shipped
        without the mobile tuning pass; check /my signing on a phone in prod.
    open:
      - >
        Does v5 handbook carry persisted duplicate anchors? If yes, upload a
        fresh handbook version to verify dedup (G1: don't touch v5's confirmed
        anchors).

  - id: HR-12
    track: hr
    size: S-M
    status: planned
    title: "Living-document updates — re-upload without forced re-acknowledgment (per-version choice)"

  - id: HR-13
    track: hr
    size: S
    status: planned
    title: "Compliance drill-down + exclude-until-due training semantics"
    notes: >
      SCHEDULING NOTE, ADDED 2026-08-10 (training filing session; ratified by
      Gary the same day). This row was title-only before this — nothing is
      edited; this is the row's first notes text. HR-13 is scheduled ADJACENT
      to HR-22 — immediately after it, its own row, its own audit-first
      session and ruling cycle. REASON ON THE RECORD: an org-wide bulk assign
      with a future due date drops every live compliance number the instant
      the button is pressed; a number that falls off a cliff the day an
      operator did the right thing is the wrong-answer-that-argues-back
      failure mode. HR-13 STAYS OUT OF HR-22'S SESSION — it changes the
      meaning of a live compliance surface, and the row is currently
      title-only: exclude-until-due needs real spec work (does a future-due
      assignment join requiredTotal; does it get its own scheduled state;
      what do store rollups show). Interim behaviour is covered by HR-22's
      disclosure guardrail.

  - id: HR-14
    track: hr
    size: M
    status: planned
    title: "Termination + identity-lifecycle hardening (zombie shell, directory toggle, webhooks, initials validation)"
    notes: >
      Scope items added 2026-07-25 (audit only, nothing fixed).
      Clerk webhook (src/app/api/webhooks/clerk/route.ts), both found while
      checking whether a Clerk org rename reaches the Froot DB — it does, but:
      (1) organization.updated writes name ONLY — it destructures slug from the
      payload and never writes it, so a renamed org keeps its original slug
      forever (only organization.created ever sets slug). (2) That handler uses
      prisma.organization.update, not upsert, and is not wrapped in try/catch —
      unlike organization.created and organizationMembership.created, which
      upsert precisely because Clerk does not guarantee delivery order. A rename
      for a missing Organization row throws P2025, the route 500s, and the
      rename is lost unless Clerk's retries land; live risk, since the DB
      carries fossil org rows while Clerk-side is the truth.
      Staff-record lifecycle, promoted here from PERMISSIONS_INVENTORY §2
      (both still unfixed): (3) item 1 — POST /api/staff has NO role check, so
      any org member incl. STAFF/STORE can create staff members while the
      /staff UI is ADMIN/MANAGER; this is the inventory's worst-first finding.
      (4) item 6 — GET /api/staff exposes the staff directory (names, emails,
      assignments) to any member, store-scoped for non-admins.
      UNBLOCKED as of BUILD-1's ship — struck 2026-08-06 by the bookkeeping
      sweep. This row carried "GATED ON BUILD-1 (2026-07-26): this phase touches
      the schema, and until BUILD-1 lands a local `npm run build` runs migrate
      deploy against the PRODUCTION branch (DEBT-4)" for eleven days after the
      gate lifted. BUILD-1 shipped 6a77e68/06b1561 and was verified in
      production 2026-07-27 (see its own notes, which name SEC-2 and HR-14 as
      "both gated on this landing first — that gate is now lifted"); DEBT-4 is
      shipped. Nothing about the schema now stops this phase starting.
      THE ORG NAME ON A SIGNED DOCUMENT IS CORRECT ON PRODUCTION — measured, not
      argued. Observed 2026-08-03 during the DEBT-9 gate walk, branch production
      (br-sparkling-block-a620qvg4), org cf888f2d-f234-48c7-8097-fd5b44b5b3dd:
      a freshly rendered Certificate of Acknowledgment reads "Generated by Froot
      for Keva Juice". NOT "Microsoft". So the failure mode people have been
      bracing for — a wrong organisation name frozen onto an executed legal
      document — DOES NOT REPRODUCE ON PRODUCTION, and HR's promotion is not
      blocked on it.
      READ THAT NARROWLY, because it is one observation and it is easy to
      over-claim from. It establishes that Organization.name is correct on the
      production row TODAY. It does NOT establish that (1) the rename handler is
      fixed — organization.updated still writes name only and never slug, so a
      renamed org keeps its original slug forever; (2) the missing-row P2025
      hazard is gone — that handler still uses update rather than upsert and is
      still untry/catch'd; or (3) anything about staging, where five rows answer
      to "Microsoft" and the ambiguity that motivated this is untouched. Points
      (1) and (2) above are unchanged and still the substance of this item.

  - id: HR-15
    track: hr
    size: S
    status: shipped
    shipped: 2026-07-23
    commits: [2647680, 0394a2e, 0280cb3]
    title: "Rehire/reactivate terminated staff + HR-15b signing cycles (re-sign on rehire)"
    notes: >
      Built 7-22 (2647680 reactivate, 0394a2e /accept-invite routing); HR-15b
      folded in (0280cb3, 7-23 — Gary reversed Fork 2 to Policy B: rehires
      re-sign; migration 20260723180000_hr15b_signing_cycles). Staging passes
      complete 7-23 (Tommy end-to-end, both cycles); prod via 942bc59. No prod
      smoke test recorded. Follow-up spun out as HR-16.

  - id: HR-16
    track: hr
    size: S-M
    status: planned
    title: "Signed-acknowledgment completion emails + org-level recipient routing"
    notes: Prerequisite — wire a real email provider into src/lib/notify.ts (see F-5 blocker).
    blockers:
      - >
        NO REAL EMAIL PROVIDER EXISTS — promoted from prose to a blocker entry
        2026-08-06 so the board can tell this row from a ready one. Verified at
        source the same day: src/lib/notify.ts:19-24 is `consoleEmailSender`,
        which console.logs the message instead of delivering it, and
        getEmailSender() returns it unconditionally — NOTIFY_EMAIL_PROVIDER is
        documented at :25 as "console" (default) with "resend" | "smtp" as
        future options, and nothing reads the variable. This phase is entirely
        the sending of email, so it cannot start at all, not merely ship
        degraded.
        CLEARS WHEN: a provider integration lands behind getEmailSender().
        THIS ONE GATE BLOCKS THREE THINGS, which is the argument for doing it:
        HR-16 (this row, wholly), F-5's pace alerts (its own live blocker, same
        file, same line) and HR-8's reminders. One integration clears all three.

  - id: HR-17
    track: hr
    size: S-M
    status: shipped
    shipped: 2026-07-25
    commits: [438a9ef]
    title: "Training preview — builder Save & Preview opens the trainee renderer read-only"
    notes: >
      Staging pass complete 2026-07-25 (Gary). Promoted to prod same day.
      Preview renders through the same extracted TrainingModuleView the /my
      execution page uses (no second renderer). Read-only by construction:
      preview has no assignmentId, so neither write endpoint is reachable.
      Gated ADMIN/MANAGER (manager limited to modules applying to their
      stores); resource download route's admin tier widened to the manage
      tier for the same scope. No schema changes.

  - id: HR-18
    track: hr
    size: M
    status: planned
    title: "Supervised (proctored) training completion — executed by the trainee, witnessed by an elevated user"
    notes: >
      FILED 2026-08-06 by the bookkeeping sweep. The row is the only thing new
      here — the work was fully specified on 2026-08-05 in
      docs/prompts/HR-18_supervised_training_completion.md (8fb86bb) and simply
      never got a board entry, so a written-up phase was invisible to every
      surface that reads this file.
      STATUS IS `planned`, AND THAT DIFFERS FROM THE PROMPT ON PURPOSE. The
      prompt asks for the row at `in_progress`. NO SESSION HAS RUN IT — no
      commits, no branch, no audit output — and `in_progress` would assert work
      underway. The prompt is a dated claim and stays unedited (CLAUDE.md
      § Where documents live); this row records what is true today, and the
      discrepancy is written down here rather than silently resolved either way.
      Flip to `in_progress` when a session actually starts.
      WHAT IT IS — A THIRD CAPTURE PATH, NOT A PERMISSION WIDENING, which is the
      distinction the prompt spends its first page protecting. Training
      completions can be captured two ways today: SELF-SERVICE (the team member
      completes lessons in their own portal) and MANAGER-ATTESTED (an elevated
      user presses Mark complete, attesting the training happened, recorded
      against the attester). Both stay exactly as they are — Mark complete is
      explicitly not to be touched.
      The third path is SUPERVISED / PROCTORED: an elevated user (ADMIN, MANAGER
      or STORE) on a shared device opens a proctored session from the team
      member's Staff profile Training tab and hands the device over. The TEAM
      MEMBER personally does the lessons and quiz; the completion records as
      executed by the trainee, supervised by the elevated user.
      WHY THE THIRD PATH HAS TO EXIST SEPARATELY, and it is a legal argument
      rather than a modelling preference: a manager attestation says "I confirm
      this person was trained"; a supervised completion says "this person did
      the work; I watched". THOSE MUST STAY DISTINGUISHABLE IN THE RECORD
      FOREVER. Collapsing supervised into attested loses the stronger of the two
      claims exactly when it is needed.
      Modules: Staff + HR (training) + Users/roles. The prompt is AUDIT-FIRST —
      it requires a plan presented and explicitly approved before anything is
      edited, and forbids fixing anything outside those three modules.
      Related: PERM-7 (the shared store devices this runs on) and HR-19, which
      governs the permission surface these routes sit in.

  - id: HR-19
    track: hr
    size: L
    status: planned
    title: "Migrate the HR permission surface onto the registry — 35 API routes, 13 pages"
    notes: >
      FILED 2026-08-06 by the bookkeeping sweep. Not new scope: PERM-5's
      `deferred` list has named this "its own future row per the audit" since
      that phase closed, and a deferral pointing at a row that does not exist is
      a pointer that does not resolve. This row is that target. PERM-5's own
      deferred list is deliberately NOT edited — it stays the record of what
      PERM-5 chose not to do.
      SIZE VERIFIED, not inherited: 35 route.ts files under src/app/api/hr and
      13 page.tsx files under src/app/(app)/hr, counted at 411ccd2 on
      2026-08-06. The figure PERM-5 recorded reproduces exactly.
      NUMBERING: filed under the `hr` track per the sweep instruction, so it
      takes an HR- id. Its LINEAGE is PERM-, and anyone tracing the permissions
      work will arrive from PERM-5's deferred entry rather than from the HR
      sequence — which is why PERM-5 is cited here by id rather than merely
      alluded to.
      HOW PERM-5C LEFT THE EDGE, and it is guidance for whoever starts: PERM-5C
      deliberately touched this surface's boundary WITHOUT entering it. The
      /staff/[id] HR-derived tabs keep their inline role check, with a comment
      saying why, so that the HR migration would not be quietly begun inside the
      file least likely to be reviewed as part of it. Start from that comment.
      THE OTHER PERM-5 EXCLUSIONS ARE NOT THIS ROW — recorded here because they
      travel together in PERM-5's list and would otherwise be assumed folded in:
      (1) THE UNGOVERNED SURFACES — operational inventory, /messages,
      /store-view, /labor. Not HR, and NOT HOMED ON ANY ROW TODAY.
      (2) USERS/SETTINGS VIEW/MANAGE SPLITS — not HR, also unhomed.
      (3) SETTINGS TOGGLE APIs — not HR, also unhomed.
      Those three are flagged for Gary rather than absorbed: giving them to this
      row would make an L phase unbounded and would file HR work under a
      non-HR heading. They need a call about where they land.
      (4) PER-USER-PER-STORE GRANULARITY is already homed — PERM-5's last
      deferred entry accepts it as a recorded LIMITATION, not a backlog item.
      (5) THE square.manage BASELINE was RULED on 2026-08-06 (R4): the registry
      was wrong, square.manage is now ADMIN_ONLY. The Square ROUTES remain
      unmigrated, but that is PERM-5's square entry and a separate sweep — and
      per R4 it is now a zero-baseline-change one.

  - id: HR-20
    track: hr
    size: M
    status: shipped
    title: "TrainingCategory entity + additive migration"
    notes: >
      ── DEBT-72a BOARD-CURRENCY BACKFILL 2026-08-17 — STATUS in_progress -> shipped.
      Prepended per preserve-and-mark; NOTHING BELOW IS EDITED.

      THIS ROW HAS BEEN STALE FOR SIX DAYS, AND IT SAID SO ITSELF. The amendment
      below reads "status stays in_progress until the push reaches Vercel". The push
      reached Vercel on 2026-08-11. Nothing moved the row.

      EVIDENCE. This row carries no `commits:`, so the ID was resolved from commit
      subjects unanchored: work commit "0a745c3" ("HR-20: TrainingCategory entity +
      additive migration (dev-applied)"). `git merge-base --is-ancestor 0a745c3
      origin/main` returns TRUE, and so does the same test against "882d6c3" — the
      2026-08-11 MORNING promotion (docs/DEPLOY_LOG.md). It did NOT ship in
      "7e77ea6", whose merge subject names "HR-20..HR-28" but whose 47-commit range
      contains none of these commits; the subject names board rows, not its contents.

      ONE OBLIGATION STAYS OPEN AND IS NOT CLEARED BY THIS FLIP. The DEPLOY_LOG
      standing note asked the promotion entry to confirm the applying-line for
      20260810194426_hr20_training_category_entity in the PRODUCTION build log. It
      was not confirmed at the time and is recorded there as DISCHARGED-AS-UNMET.
      NOT `verified` — that call is Gary's.
      Full measurement, method and the whole table:
      docs/prompts/DEBT-72a_BACKFILL.md. The rule this backfill accompanies is
      docs/WORKFLOW.md § Session completion rules item 4 — a row whose code was
      pushed to staging in a session has its status set in that session.
      Cross-refs: DEBT-72 (the promotion gate, still planned, still unbuilt),
      DEBT-72_AUDIT.md "b809e03" (which found this class), DEBT-72b (the gate
      itself, sequenced after this and deliberately not designed here).

      ── AMENDMENT 2026-08-10 (HR-20 build session; work commit "0a745c3";
      preserve-and-mark — the filed text below stands) ── BUILT this date.
      Migration 20260810194426_hr20_training_category_entity applied to DEV
      ONLY (ep-late-water-a6k53nv2 / br-broad-wave-a6vpjdw0, host echoed
      before execution); staging and production receive it via migrate deploy
      on their own promotions — status stays in_progress until the push
      reaches Vercel (the CHK-4 "in_progress at SHA" convention; `staging`
      would claim a preview that does not exist yet).
      THREE CHANGES FROM THE FILED SCOPE, each ruled by Gary 2026-08-10 on his
      own Neon-console evidence (branch ids in-output per CLAUDE.md):
      (1) BACKFILL REMOVED. §8.1 — production br-sparkling-block: Keva org
      cf888f2d-... has 12 modules with 12 DISTINCT subject values, one module
      each; subjects are SUBTITLES ("MODULE 3: CLEANING & SANITATION BASICS",
      "How to assemble a Crathco Bubbler from beginning to end."), not
      categories; second org 1 module, null subject. Staging
      br-square-feather: "Empathy" x4 + one other (test fixtures). Dev
      br-broad-wave: near-empty. Seed source dry — the migration mints ZERO
      categories from subject and never reads the column.
      (2) SUBJECT IS NOT A MIRROR. The filed "subject kept as mirror per the
      TPL-1a additive shape" is SUPERSEDED by the same ruling: subject is
      LEFT COMPLETELY ALONE — not written by category code, not mirrored, not
      resolved against, still on the form doing its subtitle job. Its future
      is its own later row. All existing modules start uncategorized, a legal
      resting state (category OPTIONAL, R-a).
      (3) CONSTRAINT CONDITION MET AND SHIPPED. §8.2 returned ZERO duplicate
      (trainingModuleId, staffMemberId) pairs on ALL THREE branches — dev
      br-broad-wave, staging br-square-feather, production br-sparkling-block
      (Gary, Neon console, 2026-08-10) — so the R-b unique constraint shipped
      in this row's migration. Consequence noted on HR-22's row: the
      untouched single-assign race now surfaces as P2002 rather than a
      silent duplicate.
      BUILD-SESSION RULINGS (Gary, 2026-08-10, at the session's stops): seed
      mechanics = TemplateType's BOTH — the three prior pattern instances
      disagree (IngredientCategory: no seed; LossReason: lazy on first read;
      TemplateType: migration + webhook), ruled to follow TemplateType:
      migration seed for zero-category orgs + ensureStarterTrainingCategories
      at both Clerk org-upsert webhook sites (src/lib/training-categories.ts,
      MUST MATCH the migration's six rows — same names, colours, sortOrder).
      CSV category misses = CREATE-AND-REPORT: resolve module_category
      case-insensitively org-scoped; a missing name is created in gray and
      named in the response's categoriesCreated — the templates-import
      convention; Gary EXPLICITLY OVERRULED the session prompt's never-invent
      default ("it was aimed at the backfill failure mode, not a deliberate
      column"). /my/training's wire-only subject select LEFT IN PLACE (ruled:
      don't cut the dead wire in the session that ruled subject untouched;
      HR-21 cuts it when it reworks that page). Starter colours as proposed:
      green/purple/blue/amber/pink/orange, leaving gray (neutral fallback)
      and red (destructive-coded) unused.
      RIDER CLOSED (HR-20 side): module POST/PATCH now validate storeIds
      against the org (400 on any foreign id) — PERM-7's class, audit §9
      item 1. HR-21 VERIFIES per the shared-rider text below.
      DRIFT FOUND AND FIXED IN PASSING: Template.templateType carried no
      onDelete while TPL-1a's hand-authored FK is ON DELETE RESTRICT on every
      branch, so every migrate diff tried to relax the live constraint to
      SetNull; annotated onDelete: Restrict in schema.prisma — schema-only,
      no DB change. The class is DEBT-68's; the mechanics note is in
      docs/MIGRATIONS.md.
      ── END AMENDMENT; filed text of record follows ──
      FILED 2026-08-10 by the training filing session, from the audit at
      docs/prompts/2026-08-10_TRAINING_AUDIT.md (committed 35a25c6) and the
      rulings ratified the same day. First of three rows — the HR- sequence
      continues; NO TRN- series (ruled R-a). The full R-a..R-f transcription
      and its provenance line are on this row, below; HR-21 and HR-22 cite
      the rulings by id.
      FOURTH INSTANCE of the per-org category pattern — IngredientCategory /
      LossReason / TemplateType.
      SCOPE: TrainingCategory entity; nullable FK on TrainingModule;
      migration seeding categories from distinct `subject` values — the
      backfill is sized by Gary's §8.1 Neon results, which the HR-20 session
      opens with; starter seed for zero-category orgs, DEBT-59 style —
      visible, renameable, never silently stamped — and NOTE: category is
      OPTIONAL per R-a, so the starter seed is convenience, not load-bearing,
      and the empty state must render sanely; module form select; CSV by-name
      resolution; `subject` kept as mirror per the TPL-1a additive shape —
      retirement is its own later step, not this row.
      CONDITIONAL ELEMENT: the unique constraint on TrainingAssignment
      (module x staff) lands in this row's migration ONLY if §8.2 returns
      zero duplicate pairs on all three branches (dev br-broad-wave, staging
      br-square-feather, production br-sparkling-block); any nonzero result
      returns to Gary with the rows before the constraint ships (ruled R-b).
      RIDER (shared with HR-21): close the storeIds org-validation gap in
      module POST/PATCH (PERM-7's class, found unreached by PERM-7 — audit
      §9). No separate debt row; the rider is named here so it cannot
      silently drop.
      ── RULINGS OF RECORD — R-a THROUGH R-f AND THE HR-13 CALL ──
      LOCATION NOTE, so the placement is a decision and not an accident:
      phase-scoping rulings are ROW-EMBEDDED per TPL-1's precedent (its Q1-Q7
      live in its row notes; the rulings: panel is for decisions filed while
      OPEN, per its own header). Three rows share this set, so it anchors
      here on the phase's first row.
      PROVENANCE: all rulings below were made in the Claude.ai planning
      session of 2026-08-10 following the training audit
      (docs/prompts/2026-08-10_TRAINING_AUDIT.md). R-a, R-b, R-d, R-e, R-f:
      ruled by Gary on the audit's evidence packets. R-c page access and the
      HR-13 scheduling call: recommended with reasoning by the audit session,
      explicitly ratified by Gary 2026-08-10 ("ratified"). The distinction
      between ruled-directly and ratified is deliberate.
      R-a: HR-20/21/22, HR- sequence, no TRN- series. Category OPTIONAL on
      modules; starter seed demoted to convenience.
      R-b: duplicates skip-and-report. Unique constraint conditionally
      approved — §8.2 zero on all three branches, else back to Gary with the
      rows. Future re-assignment, if ever built, uses a cycle column (the
      HR-15b shape) — this ruling is what makes the constraint safe.
      R-c: STORE: no. /hr/training stays ADMIN-only; reversible-by-row on
      demonstrated need. (Session-recommended, ratified by Gary 2026-08-10.)
      R-d: ACTIVE-only always; corporate excluded from store expansion,
      individually reachable, in ADMIN's everyone; module-active and
      applicability server-side; every exclusion a named count; response
      sums to its input.
      R-e: inline permission pattern; shared guards in
      api/hr/training/access.ts; zero registry edits.
      R-f: localStorage, key `froot.hr.training.view`, UX-2 caveat on
      HR-21's row with the 2026-08-10 personal-device confirmation.
      HR-13 call: scheduled adjacent to HR-22 with the two guardrails (own
      session; HR-22 discloses regardless). (Session-recommended, ratified
      by Gary 2026-08-10.)

  - id: HR-21
    track: hr
    size: M
    status: shipped
    title: "Training category management UI + badges + filter chips + card/list toggle on /hr/training"
    notes: >
      ── DEBT-72a BOARD-CURRENCY BACKFILL 2026-08-17 — STATUS in_progress -> shipped.
      Prepended per preserve-and-mark; NOTHING BELOW IS EDITED.

      THIS ROW HAS BEEN STALE FOR SIX DAYS, AND IT SAID SO ITSELF — the amendment
      below reads "Status in_progress until the push reaches Vercel", and the push
      reached Vercel on 2026-08-11.

      EVIDENCE. No `commits:` on this row; IDs resolved from commit subjects
      unanchored. TWO work commits: "a56c905" ("HR-21: Category management UI +
      badges + filter chips + card/list toggle") and "4207be0" ("HR-21: colour the
      Category picker's options with their badge preset"). Both return TRUE for
      `git merge-base --is-ancestor <sha> origin/main`. "a56c905" rode promotion
      "882d6c3" (2026-08-11 morning) and "4207be0" rode "ec42265" (2026-08-11
      afternoon) — both tested with --is-ancestor, not inferred from dates. NOT
      `verified`; that call is Gary's.
      Full measurement, method and the whole table:
      docs/prompts/DEBT-72a_BACKFILL.md. The rule this backfill accompanies is
      docs/WORKFLOW.md § Session completion rules item 4 — a row whose code was
      pushed to staging in a session has its status set in that session.
      Cross-refs: DEBT-72 (the promotion gate, still planned, still unbuilt),
      DEBT-72_AUDIT.md "b809e03" (which found this class), DEBT-72b (the gate
      itself, sequenced after this and deliberately not designed here).

      ── AMENDMENT 2026-08-11 (HR-21 build session; work commit "a56c905";
      preserve-and-mark — the filed text below stands) ── BUILT this date.
      Status in_progress until the push reaches Vercel (the CHK-4 "in_progress
      at SHA" convention, as on HR-20's row). No schema change, no database
      queries — UI + CRUD routes over HR-20's entity only, as commissioned.
      SHIPPED: category CRUD routes under api/hr/training/categories
      (collection GET/POST, [id] PATCH/DELETE, [id]/reassign POST), all behind
      requireHrTrainingAccess (R-e, zero registry edits, PERM-5C comment
      untouched), every lookup org-scoped so a foreign id reads as nonexistent.
      Delete blocked while in use with the org-wide archived-inclusive count
      (409; the FK's ON DELETE RESTRICT is the backstop); reassign is its own
      route (the TPL-1b shape) reporting {reassigned: count} — the "how many
      modules moved" number lives on the act that moves them. Manage
      Categories dialog (category-manager-dialog.tsx) — third instance of the
      manager-dialog pattern, copied from the NEWER templates instance
      (safeParse + case-insensitive 409 + P2002 catch); rename-with-count and
      delete-blocked-with-reassign both carried per ruling 4. Colours are
      badge-preset KEYS end to end. Badges render the JOINED category (added
      to moduleInclude — the TPL-2 join-read shape, renames propagate through
      the join alone); filter chips with per-view counts (TPL-1's two-counts
      rule); card/list toggle on localStorage key froot.hr.training.view, read
      once on mount, card fallback on absent/invalid/unavailable (R-f).
      ONE DATA SOURCE HELD, structurally: single fetch GET /api/hr/training →
      single `modules` state → tab filter → chip filter → `visible`; both
      layouts render `visible` and nothing else; shared StatusBadge /
      CategoryBadge / ModuleActions components make field-and-action parity
      by construction; HR-22's Bulk Assign entry point is a marked slot
      inside the SHARED action cluster (appears in both views at once when
      built). Uncategorized modules render with no badge (cards) / an empty
      cell (list) — no placeholder, no layout shift.
      BUILD-SESSION RULINGS (Gary, 2026-08-11, at HARD STOP #1):
      TAB SEMANTICS (audit §9 item 6, deferred to this session, resolved):
      three-tab PARTITION — Active (isActive && !isArchived) / Inactive
      (!isActive && !isArchived) / Archived (isArchived) — replaces the
      DEBT-67-pattern strip whose "Active" counted !isArchived. Counts moving
      is the correction, not a side effect (Gary's words). Rider honoured:
      Archived keeps its exact prior meaning; nothing visible under Archived
      moved. Bulk bar verbs follow the tab: Deactivate·Archive /
      Activate·Archive / Unarchive. Consequence recorded so nobody files it
      as a regression: Duplicate's copies (created isActive:false) now land
      under the Inactive tab, where the old strip showed them under Active.
      UNCATEGORIZED CHIP ruled IN: neutral gray, per-view count, filters
      categoryId=null — the working queue for hand-categorizing the backlog,
      the state 12 of 12 production modules are in (HR-20 §8.1 evidence).
      LIST-WIDTH RIDER honoured: the title column absorbs remaining width and
      truncates (w-full max-w-0); every other cell is nowrap — no horizontal
      overflow at laptop width, no responsive project.
      RIDER VERIFIED (HR-20 storeIds, the shared rider's second half, CLOSED):
      module POST (api/hr/training/route.ts:112-119) and PATCH
      ([id]/route.ts:89-98) both validate submitted store ids against the org
      — owned count compared to the deduplicated submitted Set, 400 "Invalid
      store ids", before any write (PATCH: before the wipe-and-recreate).
      Verified by reading at HEAD bf42bae; correct as shipped, no re-fix.
      HR-20 also org-validates categoryId on both paths, noted in passing.
      /my/training's dead subject select LEFT ALONE per the session prompt's
      own note — HR-21 reworked /hr/training, not /my/training; the wire-only
      carry stays until a session actually touches that page.
      ── END AMENDMENT; filed text of record follows ──
      FILED 2026-08-10 by the training filing session; second of three rows
      scoped from docs/prompts/2026-08-10_TRAINING_AUDIT.md. Rulings R-a..R-f
      are transcribed on HR-20's row; this row cites them by id.
      SCOPE: Manage Categories dialog living WITH the list it categorises
      (the Manage Types precedent); colour badges via badge-preset KEYS only
      (src/lib/badge-presets.ts — never class strings from the DB, the
      Tailwind 4 constraint); filter chips; card view / list view toggle on
      /hr/training.
      ONE DATA SOURCE FEEDS BOTH VIEWS — the list view never fetches its own
      data; a module must not differ in category, status, or action set
      between views; everything this phase adds appears in BOTH views.
      TOGGLE PERSISTENCE: localStorage, single key `froot.hr.training.view`
      (ruled R-f) — UX-2's shared-device caveat stated on this row, with the
      mitigating fact recorded beside it: Gary confirmed 2026-08-10 that
      MANAGER logins at Keva are personal machines and shared iPads run
      STORE, which cannot reach this page.
      PAGE ACCESS: /hr/training stays ADMIN-only (ruled R-c, ratified) —
      HR-6 shipped it that way deliberately, training content is
      confidential, and the org-wide-push persona is ADMIN. Reversible later
      as its own small named row on demonstrated manager need; HR-21 ships
      no role-conditional UI.
      RIDER: carries the storeIds org-validation rider jointly with HR-20
      (whichever session touches the write path closes it; the other
      verifies).

  - id: HR-22
    track: hr
    size: M
    status: shipped
    title: "Bulk training assignment + due date carry-through"
    notes: >
      ── DEBT-72a BOARD-CURRENCY BACKFILL 2026-08-17 — STATUS in_progress -> shipped.
      Prepended per preserve-and-mark; NOTHING BELOW IS EDITED.

      THIS ROW HAS BEEN STALE FOR SIX DAYS, AND IT SAID SO ITSELF — the amendment
      below reads "Status in_progress until the push reaches Vercel", and the push
      reached Vercel on 2026-08-11.

      EVIDENCE. No `commits:` on this row; ID resolved from commit subjects
      unanchored. Work commit "bd63da7" ("HR-22: bulk training assignment + due date
      carry-through") returns TRUE for `git merge-base --is-ancestor bd63da7
      origin/main` and TRUE against promotion "882d6c3", the 2026-08-11 morning
      promotion. NOT `verified`; that call is Gary's.
      Full measurement, method and the whole table:
      docs/prompts/DEBT-72a_BACKFILL.md. The rule this backfill accompanies is
      docs/WORKFLOW.md § Session completion rules item 4 — a row whose code was
      pushed to staging in a session has its status set in that session.
      Cross-refs: DEBT-72 (the promotion gate, still planned, still unbuilt),
      DEBT-72_AUDIT.md "b809e03" (which found this class), DEBT-72b (the gate
      itself, sequenced after this and deliberately not designed here).

      ── AMENDMENT 2026-08-11 (HR-22 build session; work commit "bd63da7";
      preserve-and-mark — the filed text below stands) ── BUILT this date.
      Status in_progress until the push reaches Vercel (the CHK-4 "in_progress
      at SHA" convention, as on HR-20's and HR-21's rows). No schema change and
      no database queries: dueDate and HR-20's (module x staff) unique
      constraint both already existed, and R-d rules the recipient population by
      predicate, not by count.
      SHIPPED: POST /api/hr/training/assignments/bulk (with shared.ts) and GET
      .../bulk/recipients, both behind requireHrTrainingManageAccess (R-e —
      ADMIN + store-scoped MANAGER, STORE 403s at the shared guard; zero
      registry edits, PERM-5C's boundary comment untouched, /hr/training's
      ADMIN-only page gate untouched per R-c). `bulk` as a static sibling of
      `[id]` follows inventory/ingredients/bulk and inventory/sales-items/bulk.
      RESPONSE SHAPE AS SHIPPED — five MUTUALLY EXCLUSIVE buckets over
      `requested`, the deduplicated roster the request NAMES after expansion:
      { moduleId, moduleTitle, dueDate, requested, assigned, alreadyAssigned,
      terminated, notApplicable, corporateExcluded, concurrentlyAssigned,
      recipients[] }. Invariant: assigned + alreadyAssigned + terminated +
      notApplicable + corporateExcluded === requested === recipients.length.
      `concurrentlyAssigned` sits OUTSIDE the sum by design — a diagnostic
      annotating alreadyAssigned (nonzero means HR-20's constraint did its job),
      not a sixth bucket. recipients[] is UNCAPPED: a silent truncation would
      read as "everyone is accounted for" while hiding who was not.
      WORKED EXAMPLE, counts summing to input (ADMIN; module appliesTo
      "selected" -> Downtown + Airport; request = storeIds [Downtown] + three
      individuals): Downtown expands to 14 (11 store-homed + 3 corporate, whom
      Square expands to every store); of the 11, one is TERMINATED and four of
      the ten ACTIVE already hold the module; the three individuals are one
      corporate member (already inside the 14 — the explicit pick de-dupes and
      WINS), one Riverside-only member, one ACTIVE Airport member. requested 16
      = assigned 8 + alreadyAssigned 4 + terminated 1 + notApplicable 1 +
      corporateExcluded 2.
      RECIPIENT RULES AS ENFORCED (R-d, every one server-side, never UI-only):
      ACTIVE-only always; corporate EXCLUDED from store expansion and from a
      MANAGER's all-in-scope (a manager's scope IS a store expansion — the two
      rules are stated together so they cannot contradict), INCLUDED in ADMIN's
      all-in-scope, and always reachable by explicit pick; module isActive +
      isArchived + applicability; every submitted staff id and store id
      validated against org ownership AND caller scope (PERM-6's lesson;
      staff/route.ts:63-84 is the precedent), applied set-wise in one query
      rather than n+1 findManageableStaffMember calls.
      RULED AT HARD STOP #1 (Gary, 2026-08-11), recorded so they read as
      deliberate:
      (1) EXPLICIT PICK BEATS CORPORATE EXCLUSION — which is how R-d's two
      halves both hold: out of store expansion, individually selectable.
      (2) THE STORE PICKER LISTS INACTIVE STORES — deactivating a store does not
      un-employ the people assigned to it, and hiding it would strand them
      behind a picker that cannot reach them. The reason is in the code comment,
      per the same ruling.
      (3) IDENTITY ERRORS REFUSE, ELIGIBILITY RULES SKIP-AND-REPORT. `requested`
      is the RESOLVED roster, so an unknown/foreign/out-of-scope id cannot be in
      it: it fails the whole request before anything is written. Org-foreign and
      out-of-scope collapse into ONE message so neither leaks which ids exist.
      That split is what makes the sum trustworthy — nothing that was written
      down can drop out of the arithmetic.
      (4) RIDER — THE SUM FAILS LOUDLY. Buckets that do not add up to
      `requested`, or an `assigned` count disagreeing with what the database
      says it inserted, return 500 naming the mismatch (requested, sum,
      classified, rowsInserted, every bucket) and state plainly that the rows
      were written but the response cannot be trusted to describe them. A number
      is never quietly reconciled to make the response look right.
      DISCLOSURE LINE, FINAL WORDING (ruling 6; Gary chose option B at the
      stop): "Assigning now adds N required items to compliance. Compliance
      percentages for these N people, and their store rollups, drop immediately
      — a due date does not hold that back." Plus the conditional second line
      whenever any recipient has no login: "N of them have no self-service login
      — a manager records their completions." N is previewed from the recipients
      endpoint and is ADVISORY; the result panel renders the POST's own named
      counts rather than a bare success toast. HR-13's semantics are NOT built
      here — disclosed, not changed.
      DUE DATE: optional, no invented default (DEBT-59), applied to every
      created assignment, noon-normalized client-side exactly as
      staff-training.tsx:156 does it, so a date means the same thing on both
      assign paths.
      BOTH VIEWS BY CONSTRUCTION: the entry point is a button inside HR-21's
      SHARED ModuleActions cluster, so card and list gained it in one edit; the
      dialog is mounted ONCE for the page and keyed by module id. The button is
      disabled with its reason on hover for inactive/archived modules — it says
      what the API would say (409) instead of offering a click that refuses.
      SINGLE-ASSIGN RETROFIT — OUTCOME, both halves:
      skipDuplicates: true LANDED on POST /api/hr/training/assignments'
      createMany, closing the P2002 -> 500 HR-20 knowingly left. Acceptance
      behaviour on that route is otherwise UNCHANGED.
      FINDINGS #2/#3 (it accepts INACTIVE modules the UI never offers; it
      ignores module applicability) RULED TO THEIR OWN ROW — HR-23, filed
      below, NOT absorbed here (Gary, 2026-08-11). The reason is the
      interesting half and is recorded rather than assumed: adopting #2/#3
      alone converts today's loud over-acceptance into a SILENT DROP, and that
      route's { created, skipped } cannot explain a drop — which is finding #4,
      and precisely the CHK-3 failure this phase exists to avoid. HR-22 settles
      the RULE; HR-23 applies rule and response shape together.
      TRILOGY CLOSED: HR-20 (0a745c3), HR-21 (a56c905), HR-22 (bd63da7) — all
      three built. The training phase scoped from
      docs/prompts/2026-08-10_TRAINING_AUDIT.md is complete as scoped; HR-23 is
      a NEW row, not an unfinished part of it.
      ── END AMENDMENT; filed text of record follows ──
      FILED 2026-08-10 by the training filing session; third of three rows
      scoped from docs/prompts/2026-08-10_TRAINING_AUDIT.md. Rulings R-a..R-f
      are transcribed on HR-20's row; this row cites them by id.
      SCOPE: Bulk Assign entry point on /hr/training modules (both views);
      recipient picker — individuals, entire store, or everything in the
      assigner's scope; optional due date applied to every created
      assignment, NO invented default (DEBT-59).
      SERVER-SIDE ENFORCEMENT on the bulk route (ruled R-d): every submitted
      staff/store id validated against org ownership AND caller scope
      (getUserStoreScope — PERM-6's lesson, on day one); ACTIVE staff only,
      always; corporate staff excluded from entire-store expansion
      (individually selectable; included in ADMIN's everything-in-scope),
      because Square sync expands isCorporate staff to every store;
      module-active and applicability-vs-store enforced server-side, not
      UI-only.
      DUPLICATES: skip-and-report (ruled R-b). The response reports its own
      blast radius — assigned / already-assigned / each exclusion as a named
      count — and sums exactly to its input (CHK-3's counter lesson).
      INHERITED FROM HR-20 (noted 2026-08-10, work commit "0a745c3", by
      Gary's instruction — in writing, not by memory): the R-b unique
      constraint on (trainingModuleId, staffMemberId) now EXISTS, and HR-20
      deliberately did not touch the single-assign POST (assignment write
      paths are THIS row's). Consequence: two concurrent assigns of the same
      pair, which previously both passed the read-then-skip and silently
      double-inserted (deflating the member's compliance %), now surface as
      Prisma P2002 → 500 on the loser. Strictly better than the corrupt row,
      but HR-22's build must make the constraint read as ordinary
      skip-and-report: createMany with skipDuplicates: true on the bulk
      route, and retrofit the same flag onto the single-assign path while in
      the file.
      PERMISSIONS (ruled R-e): STORE 403'd; ADMIN + store-scoped MANAGER at
      the API per the existing shared guards in api/hr/training/access.ts —
      inline pattern, zero registry edits, PERM-5C's boundary comment
      untouched; HR-19 migrates this seam later. (Page gating per R-c means
      the button is ADMIN-reachable in practice; the API guard is the
      enforcement layer.)
      DISCLOSURE GUARDRAIL (ratified with the HR-13 call): HR-22's confirm
      dialog states the immediate compliance effect plainly — "this adds N
      required items to compliance now" — so the pre-HR-13 dip is never a
      surprise, even if HR-13 slips.
      OUT OF SCOPE, on the row: notification emails (the gate is HR-16's —
      notify.ts is console-only); any HR-13 semantics change.

  - id: HR-23
    track: hr
    size: S
    status: planned
    title: "Single-assign path adopts the bulk route's module rules + a response that sums to its input"
    notes: >
      FILED 2026-08-11 by the HR-22 build session, at Gary's ruling at that
      session's HARD STOP #1. Carries audit findings #2, #3 and #4 from
      docs/prompts/2026-08-10_TRAINING_AUDIT.md (§4.2, §4.3, §9) — filed as ONE
      row because they cannot safely ship apart, which is the whole reason this
      is a row and not a two-line rider on HR-22.
      SCOPE: POST /api/hr/training/assignments — the /staff/[id] Training tab's
      assign path — and staff-training.tsx, which must render the reasons the
      new response gives.
      #2: the route accepts INACTIVE modules the UI never offers; its module
      filter is isArchived-only (route.ts:35-38) while the page's assignable
      list requires isActive (staff page.tsx:394).
      #3: the route ignores module applicability (appliesTo / store
      assignments); the offer list intersects them with the member's stores
      (staff page.tsx:396-399).
      #4, and the reason the other two waited for this row: the response is
      { created, skipped }, so ids dropped by the route's own filter vanish from
      its arithmetic — created + skipped != requested, and nothing says why.
      TIGHTENING #2/#3 WITHOUT #4 TRADES LOUD OVER-ACCEPTANCE FOR SILENT
      DROPPING (Gary, 2026-08-11): today an odd id is wrongly accepted and
      visible; tightened without a response that can explain a drop, it would be
      wrongly refused and INVISIBLE. That is the CHK-3 failure mode exactly.
      THE RULE IS ALREADY SETTLED — do not re-open it. HR-22's bulk route
      (src/app/api/hr/training/assignments/bulk) enforces isActive + isArchived
      + applicability and REFUSES (409) rather than dropping; its five-bucket
      response and its loud sum invariant are the pattern to copy. This row is
      the retrofit, not a fresh decision.
      NOT A LIVE RISK MEANWHILE: neither gap is exploitable beyond ADMIN/MANAGER
      doing odd things inside their own org, and the UI offers neither (audit
      §4.2) — which is why HR-22 shipped without waiting for it.

  - id: HR-24
    track: hr
    size: M
    status: shipped
    title: "STORE read access to the training library — read-only, no attachments, all 27 guards untouched"
    notes: >
      ── DEBT-72a BOARD-CURRENCY BACKFILL 2026-08-17 — STATUS in_progress -> shipped.
      Prepended per preserve-and-mark; NOTHING BELOW IS EDITED.

      THIS ROW HAS BEEN STALE FOR FIVE DAYS. It reads "Status in_progress until the
      push reaches Vercel"; the push reached Vercel on 2026-08-12.

      CORRECTION TO A STALE SENTENCE BELOW, which is the reason this row mattered
      more than the others: the PROMOTION ORDER paragraph states "Both are on staging
      and neither is on production". That has been false since 2026-08-12 — and THE
      CONSTRAINT ITSELF HELD, which is checked here rather than assumed. Work commit
      "0b1cf51" (HR-24) and "f5d2883" (HR-25) both return TRUE for
      `git merge-base --is-ancestor <sha> b853787`, the single 2026-08-12 MIDDAY
      promotion, and "f5d2883" is the older of the two inside it. One promotion
      carried both, HR-25 first. The wider door did not open ahead of the narrower
      one.

      EVIDENCE. No `commits:` on this row; ID resolved from commit subjects
      unanchored. "0b1cf51" ("HR-24: STORE reads the training library") is an
      ancestor of origin/main. NOT `verified`; that call is Gary's.
      Full measurement, method and the whole table:
      docs/prompts/DEBT-72a_BACKFILL.md. The rule this backfill accompanies is
      docs/WORKFLOW.md § Session completion rules item 4 — a row whose code was
      pushed to staging in a session has its status set in that session.
      Cross-refs: DEBT-72 (the promotion gate, still planned, still unbuilt),
      DEBT-72_AUDIT.md "b809e03" (which found this class), DEBT-72b (the gate
      itself, sequenced after this and deliberately not designed here).

      BUILT 2026-08-11 (work commit "0b1cf51"). Status in_progress until the
      push reaches Vercel (the CHK-4 "in_progress at SHA" convention, as on
      HR-20/21/22/25). No schema change and no database queries by the session.
      PROMOTION ORDER — HR-24 MUST NOT REACH PRODUCTION AHEAD OF HR-25
      (f5d2883). Written here rather than remembered. HR-25 closed the
      employee-side file line (a completed assignment stops being served its
      attachments); HR-24 widens who can read module CONTENT. Promoting the
      wider door first is the ordering R-l's sequence note and HR-25's own row
      both warn against. Both are on staging and neither is on production, so
      one promotion carrying both, or HR-25 first, are the only correct orders.
      WHAT A STORE LOGIN CAN NOW DO: open the Training card on /hr, see the
      org's live training modules in card or list view with category badges and
      filter chips, and open any of them and read it end to end — lesson
      bodies, videos, the procedure. Gary's water-heater case (2026-08-11):
      read the procedure on the shared iPad at the moment the water heater
      needs restarting.
      THE THREE RULINGS, AS GARY RULED THEM AT HARD STOP #1 ON 2026-08-11.
      R-g — OPTION (ii-b) PLUS (c). (ii-b) was NOT one of the audit's options;
      it was added at the stop because the audit's pair was reuse-the-preview
      versus write-a-second-renderer and the middle did not exist. It is a
      third MODE — {kind:"read"} — inside TrainingModuleView, so HR-17's "no
      second renderer" rule holds and STORE cannot drift from what a trainee
      sees. Read mode drops what only makes sense to someone JUDGING a module:
      the progress bar (progress is a property of an assignment; read mode has
      none, so it would sit at 0/N forever and read as "you haven't started"),
      the disabled Mark-complete buttons, and the quiz (inert on a floor
      device, and it invites fourteen answers into a void). (c) is the
      attachment half: resourcesAvailable={false} for STORE.
      R-h — OPTION (i), ORG-WIDE, AND THIS ONE REVERSED ON MEASUREMENT. The
      session's own plan leaned toward the store-applicability filter; the
      staging query killed it. All 5 modules are appliesTo "all", so the filter
      would have filtered NOTHING while permanently promoting appliesTo from an
      ASSIGNABILITY filter to a VISIBILITY one — a one-way semantic change
      bought for a measured zero effect, after which the next person to scope a
      module would silently hide content from a store that needs it. If store
      scoping is ever wanted it is the OR clause already written at
      preview/page.tsx, and it belongs on its own row with evidence.
      R-j — OPTION (i), NO CLASSIFICATION. The library is one bucket; nothing
      about a module's content is consulted. TrainingCategory (HR-20) stays a
      LABEL and is deliberately not a permission input — promoting a cosmetic
      field would be wrong on day one in both directions while 12 of 12 Keva
      production modules are uncategorized. A per-module flag (the audit's
      option iii) was a SCHEMA CHANGE and would have stopped this session; it
      was not chosen and remains available as its own row.
      THE NAMED POLICY FUNCTION SHIPPED ANYWAY, AND THAT IS THE POINT OF R-j(i)
      RATHER THAN A CONTRADICTION OF IT. canReadTrainingModule (lib/training.ts)
      is the canReadHrDocument analogue the session was scoped to build, with
      STORE_LIBRARY_WHERE as its query-shaped twin — the HR-25 pattern of two
      expressions of one rule kept adjacent so they cannot drift. Under
      R-h(i)/R-j(i) the rule that remains is LIFECYCLE: ADMIN owns the builder
      and reaches drafts and archives, STORE reads the live library. When the
      input changes, it changes in one place with a name.
      A NEW ROUTE RATHER THAN A WIDENED GUARD, AND THE REASON IS THE PAYLOAD,
      NOT THE GATE — the finding that shaped the whole build. GET
      /api/hr/training includes `quizzes: true` and lessons.resources verbatim,
      so its response carries correctOptionIds — THE QUIZ ANSWER KEY — and
      every resource's raw private blob fileUrl. Both are safe there because
      its only caller is the ADMIN builder; neither is safe on a shared store
      iPad, and toClientQuizQuestions (which strips the key) guards the RENDER
      paths, not that route. So GET /api/hr/training/library was added behind a
      new requireHrTrainingReadAccess (ADMIN + STORE), selecting counts rather
      than content: what is not selected cannot leak. The list is additionally
      re-filtered through canReadTrainingModule, so if the fragment and the
      function ever disagree the stricter answer ships.
      ALL 27 EXISTING GUARD CALL SITES ARE UNTOUCHED and every one still
      refuses STORE at the same line — 16 behind requireHrTrainingAccess
      (ADMIN) and 11 behind requireHrTrainingManageAccess (ADMIN/MANAGER). The
      access.ts diff has ZERO deletions; it is purely additive. Note the count:
      the training access audit says 14 ADMIN sites and there are 16 (route.ts
      x3, [id]/route.ts x3, categories/route.ts x2, categories/[id]/route.ts
      x2, categories/[id]/reassign, import, export, upload-url,
      lessons/[lessonId]/resources, resources/[id]). The audit is a claim
      wholesale and stays unedited; the correction lives here.
      UI SUPPRESSION IS IN ADDITION TO THE SERVER GUARDS, NEVER INSTEAD OF
      THEM. Hidden for STORE on /hr/training: Create, Import, Export, Manage
      Categories, Edit, Duplicate, Assign, the row checkboxes, Select All, the
      bulk lifecycle bar, the Active/Inactive/Archived tabs, the Active/Inactive
      status badge, and both authoring dialogs (not mounted at all). KEPT, per
      scope: the card/list toggle, category badges and filter chips, working
      identically in both layouts — HR-21's parity rule, which holds by
      construction because one view model (ListModule) is what both roles and
      both layouts render. STORE's chips derive from the categories present in
      its own list (/hr/documents' presentCategories rule) rather than from GET
      /api/hr/training/categories, which stays ADMIN.
      EMPTY STATE, PER GARY'S RIDER: an empty library reads "No training
      modules yet / Training published for your store will appear here." — not
      a blank panel. This is the shared-iPad screen and a blank panel on the
      floor is indistinguishable from a broken one. No CTA, deliberately: the
      reader cannot create a module, and the next step is a person.
      R-C IS SUPERSEDED, AND IN THESE TERMS. R-c ruled /hr/training ADMIN-only
      on the ground that training content is confidential (Keva's handbook).
      The audit established that the gate was a CONSEQUENCE OF THE PAGE BEING
      AN AUTHORING TOOL — training/page.tsx:6-8 said so in as many words — and
      that R-c ruled on STORE-as-ASSIGNER, never STORE-as-READER: the
      2026-08-10 audit's §10.3 priced STORE only under the heading "who can
      bulk assign". R-c was not wrong about what it decided; it did not reach
      this question. The handbook itself is readable by STORE today at
      /hr/documents, by design and with a comment saying why, and that
      asymmetry was undefended anywhere in code. STORE-as-assigner is
      untouched and stays refused.
      MANAGER PAGE ACCESS IS NOT PART OF THIS AND REMAINS A SEPARATE FUTURE
      ROW. MANAGER is absent from requireHrTrainingReadAccess on purpose, not
      by oversight — folding it in would decide it silently, and it re-opens
      R-c's actual subject. MANAGER's existing reach (the HR-17 preview for
      modules applying to its stores, with the store-applicability clause
      intact) is unchanged.
      STORE ASSIGNMENT CAPABILITY (R-i) WAS NOT BUILT AND IS NOT FILED. All
      five of its options priced above their worth, and PERM-5's row already
      rules that granting a role something it lacks means a different role, not
      a wider override. Out of scope entirely.
      EVIDENCE AND ITS LIMITS — R-g's ATTACHMENT CALL AND R-h WERE DECIDED ON
      STAGING DATA. Both queries ran on preview/staging (branch id
      br-square-feather-a63z92vz), org cmr54z65v000105jxczpt72w1: 5 modules,
      all appliesTo "all", 3 of 5 uncategorized, and ZERO resources across the
      entire library — which is what makes (c) cost nothing, since it
      suppresses nothing that exists. PRODUCTION WAS NOT MEASURED FOR
      RESOURCES. HR-20's row records 12 modules in Keva production (org
      cf888f2d-f234-48c7-8097-fd5b44b5b3dd) and those are real content, not
      fixtures. IF PRODUCTION CARRIES PROCEDURE PDFs, option (b) — a third,
      STORE tier inside resources/[id]/download — becomes a future row, priced
      then against real files. Note what (b) costs now that HR-25 has shipped:
      it would give a shared store device permanent, assignment-free access to
      files that HR-25 just took away from employees the moment they complete a
      module. That tension did not exist when the audit priced (b) and must be
      named by whoever files it.
      RIDER, ROW #1 (audit §2.4 / triage #2) — CLOSED IN THIS COMMIT. /hr
      rendered a Staff Directory card to every viewer while /staff requires
      staff.view (MANAGE), so a STORE login tapped it and bounced. Now
      ADMIN||MANAGER, matching staff.view's tier — which ALSO closes the
      identical bounce for STAFF, since the sidebar sends STAFF to
      /my/documents but /hr itself has no role gate of its own. One condition,
      both defects.
      KNOWN AND ACCEPTED — /hr STILL LOOKS THE USER UP BY clerkUserId ALONE
      (hr/page.tsx:21, the DEBT-55 pattern, audit triage #1). Gary ruled
      2026-08-11 to leave it latent and gate on the role string like the rest
      of the file, consistent with R1 (2026-08-06). Every gate behind this page
      refuses independently — the /hr/training page gate and the library route
      both resolve through getCurrentUser(), which carries the cross-org
      refusal — so the card gates are UI-only. Recorded because this session
      ADDED a card gate on top of that lookup, which is exactly the surface
      growth CLAUDE.md's note asks to keep visible.
      TIER — DECLARED TIER 1, EXECUTED AS TIER 3, and the discrepancy is
      recorded rather than resolved silently. The session prompt says Tier 1;
      CLAUDE.md § Session Tiers defines TIER 1 as cosmetic and its guardrail
      names conditionals, queries, permission checks, role gates and anything
      under src/app/api/ — this work touches all five. Ambiguity resolves
      upward and a session may never move down a tier. Nothing about the
      process changed: the prompt already specified audit-first, a hard stop
      before any edit, a row and two commits, which is TIER 3's process.
      Audit and ruling packets:
      docs/prompts/2026-08-11_TRAINING_ACCESS_AUDIT.md §§1-3, §9.1, §9.2, §9.4,
      §9.5; session prompt docs/prompts/2026-08-11_HR-24_SESSION.md.

  - id: HR-25
    track: hr
    size: S-M
    status: shipped
    title: "Completed training stops serving files — content stays readable, attachments stop"
    notes: >
      ── DEBT-72a BOARD-CURRENCY BACKFILL 2026-08-17 — STATUS in_progress -> shipped.
      Prepended per preserve-and-mark; NOTHING BELOW IS EDITED.

      THIS ROW HAS BEEN STALE FOR SIX DAYS. It reads "Status in_progress until the
      push reaches Vercel"; the push reached Vercel on 2026-08-12.

      EVIDENCE. No `commits:` on this row; ID resolved from commit subjects
      unanchored. Work commit "f5d2883" ("HR-25: completed training stops serving
      files (R-m option iii)") returns TRUE for `git merge-base --is-ancestor
      f5d2883 origin/main` and TRUE against promotion "b853787", the 2026-08-12
      MIDDAY promotion. It was the OLDEST of the three access-control commits in that
      promotion, which is what satisfied HR-24's ordering constraint. NOT `verified`;
      that call is Gary's.
      Full measurement, method and the whole table:
      docs/prompts/DEBT-72a_BACKFILL.md. The rule this backfill accompanies is
      docs/WORKFLOW.md § Session completion rules item 4 — a row whose code was
      pushed to staging in a session has its status set in that session.
      Cross-refs: DEBT-72 (the promotion gate, still planned, still unbuilt),
      DEBT-72_AUDIT.md "b809e03" (which found this class), DEBT-72b (the gate
      itself, sequenced after this and deliberately not designed here).

      BUILT 2026-08-11 (work commit "f5d2883"). Status in_progress until the
      push reaches Vercel (the CHK-4 "in_progress at SHA" convention, as on
      HR-20/21/22). No schema change and no database queries.
      THE RULING — R-m OPTION (iii), GARY 2026-08-11: CONTENT STAYS, FILES
      STOP. Once a staff member's own assignment for a module is complete they
      may still READ that module through their own self-service surfaces, but
      are no longer served its attached files. The concern being closed, in
      Gary's words: trade-secret material leaving the building after hours,
      once the training no longer requires it. Option (ii) — closing the
      content too — was rejected on its merits: an employee losing the ability
      to re-read training they completed (refreshers, reference during a shift,
      the food-safety module they want to check) is a real operational loss.
      THE PRE-HR-25 STATE WAS OBSCURITY, NOT A GATE, AND THIS ROW EXISTS
      BECAUSE OF IT. docs/prompts/2026-08-11_TRAINING_ACCESS_AUDIT.md §4
      established that the only completion-aware filter in the whole training
      surface was one line on the /my home card — openItems.filter(i =>
      i.status !== "complete") ((my)/my/page.tsx:72) — written as a TO-DO
      FILTER, for a to-do list, not as an access control. /my/training (:17),
      the assignment page (:29) and the resource download route's self tier
      (download/route.ts:60) carried NO status predicate: completed modules
      rendered in full with working download links, and an employee who
      finished a module in March could still fetch its PDFs in December. The
      one other mitigation was navigational — F7 removed Training from the
      staff portal's tab bar while my-shell.tsx:14 records that the routes
      "stay live", so the module was reachable by URL, history or bookmark.
      The property being preserved did not exist; preserving the behavior
      preserved a card filter.
      SHIPPED — the server gate and the UI half in ONE commit, because they
      cannot ship apart: a visible link that 403s is the failure the audit
      named on the STORE side (§3.3), and a suppressed link over an open route
      is not a gate at all.
      (1) src/lib/training.ts — SELF_FILES_SERVED_WHERE (a Prisma fragment) and
      selfFilesServed() (a boolean), two expressions of one rule kept adjacent
      so they cannot drift. certifiedAt is tested alongside status: normally
      redundant (certification requires Completed), it stops being redundant
      when a module gains a lesson AFTER someone was certified and the
      assignment recalculates to InProgress while certifiedAt stays set —
      belt and braces keeps that person on the refused side.
      (2) resources/[id]/download/route.ts — the SELF tier's assignment match
      now requires an open assignment. A completed holder misses the findFirst
      and takes the route's EXISTING 404 ({ error: "Not found" }), the same
      refusal it already gives for unassigned, terminated and cross-org. No new
      error contract was invented.
      (3) TrainingModuleView — resourcesAvailable is REQUIRED, not defaulted,
      so every viewing tier must answer it rather than inherit links. This
      closes audit triage #6 (the link block was conditioned on the resource
      count and nothing else, so any future tier gained downloads silently).
      When false the lesson renders in full with one muted line: "Attached
      files are no longer available — you've completed this module."
      (4) /my/training/[assignmentId] passes selfFilesServed(assignment); the
      page does not close after completion. (5) The HR-17 preview passes
      resourcesAvailable explicitly — NO behavior change, ADMIN/MANAGER are
      served by the manage tier as before.
      NOT CHANGED, DELIBERATELY: /my/training (the list) renders no download
      links at all, so it needed no edit — completed rows keep their
      Completed/Certified badges and stay clickable. The /my home to-do filter
      stays exactly as it was; it was never the gate. The manage tier of the
      download route and all 25 existing training guards are untouched — a
      manager reviewing training is unaffected.
      THIS ROW IS NOT A COMPLETE GUARANTEE, AND MUST NOT BE READ AS ONE. A
      lesson's YouTube link is an EXTERNAL URL and is deliberately not hidden
      after completion (Gary, 2026-08-11): video is lesson content — for most
      modules it IS the lesson — and hiding it buys no protection, since anyone
      who watched it already had the URL and it was copyable from the DOM
      throughout the assignment. Hiding it was priced (the same flag applied to
      the video block, about five lines) and declined. HR-25 closes what the
      APP SERVES; nothing in this app can claw back an external link.
      CONSEQUENCE FOR WHOEVER BUILDS RE-ASSIGNMENT CYCLES — FINDABLE HERE
      BECAUSE IT IS A CONSEQUENCE OF WHERE THE PREDICATE SITS. Re-assignment is
      unreachable today by two independent mechanisms: HR-22's read-then-skip
      plus skipDuplicates at the route, AND @@unique([trainingModuleId,
      staffMemberId]) in the schema (HR-20/R-b) — which is also what makes the
      predicate inside `some` read as "their assignment, and it is still open"
      rather than "some open one". If HR-15b-shaped cycle rows are ever built,
      FILE ACCESS RETURNS as soon as a not-complete row exists for that module x
      staff pair. Gary ruled that correct 2026-08-11 — reassigned means they
      need the material again — but it follows from the predicate's POSITION,
      not from an explicit decision in the query, so whoever adds cycles must
      confirm it is still what they want.
      HR-18 (supervised completion, unbuilt) IS NOT OBSTRUCTED: a proctored
      session exists to CAPTURE a completion, so it operates on an assignment
      that is by definition not yet complete, and it is launched from the Staff
      profile by an elevated user — resolving through the manage tier, not
      getActiveStaffSelf. Not built for, not blocked.
      SEQUENCE — R-l's note applies and is now live: HR-24 (STORE read access,
      unfiled) widens who can read module content, so the employee-side line
      closed here should stand before it. Audit and ruling packet:
      docs/prompts/2026-08-11_TRAINING_ACCESS_AUDIT.md §4 and §9.7; session
      prompt docs/prompts/2026-08-11_HR-25_SESSION.md.

  - id: HR-26
    track: hr
    size: M
    status: shipped
    title: "MANAGER reads the training library and assigns from it — store-scoped, no authoring, all 27 guards untouched"
    notes: >
      ── DEBT-72a BOARD-CURRENCY BACKFILL 2026-08-17 — STATUS in_progress -> shipped.
      Prepended per preserve-and-mark; NOTHING BELOW IS EDITED.

      THIS ROW HAS BEEN STALE FOR FIVE DAYS. It reads "Status in_progress until the
      push reaches Vercel"; the push reached Vercel on 2026-08-12.

      CORRECTION TO A STALE SENTENCE BELOW: the PROMOTION ORDER paragraph states
      "All four commits are on staging and none is on production". False since
      2026-08-12. THE TRANSITIVE CONSTRAINT HELD: "f5d2883" (HR-25), "0b1cf51"
      (HR-24) and "7048504" (HR-26) all return TRUE for
      `git merge-base --is-ancestor <sha> b853787` — one promotion carried all
      three, with HR-25 oldest.

      EVIDENCE. No `commits:` on this row; ID resolved from commit subjects
      unanchored. "7048504" ("HR-26: MANAGER reads the training library and assigns
      from it") is an ancestor of origin/main. NOT `verified`; that call is Gary's.
      Full measurement, method and the whole table:
      docs/prompts/DEBT-72a_BACKFILL.md. The rule this backfill accompanies is
      docs/WORKFLOW.md § Session completion rules item 4 — a row whose code was
      pushed to staging in a session has its status set in that session.
      Cross-refs: DEBT-72 (the promotion gate, still planned, still unbuilt),
      DEBT-72_AUDIT.md "b809e03" (which found this class), DEBT-72b (the gate
      itself, sequenced after this and deliberately not designed here).

      BUILT 2026-08-12 (work commit "7048504"). Status in_progress until the
      push reaches Vercel (the CHK-4 "in_progress at SHA" convention, as on
      HR-20/21/22/24/25). No schema change and no database queries by the
      session.
      PROMOTION ORDER — UNCHANGED AND NOW TRANSITIVE. HR-24's constraint stands:
      it must not reach production ahead of HR-25 (f5d2883). HR-26 sits on top
      of HR-24 (it extends requireHrTrainingReadAccess and the library route
      HR-24 created), so it cannot promote without it either. All four commits
      are on staging and none is on production, so ONE PROMOTION CARRYING ALL
      OF THEM, or HR-25 first, remain the only correct orders.
      THE OPERATOR NEED, GARY 2026-08-12 AFTER TESTING HR-24 ON STAGING:
      STORE read access worked, and MANAGER then had LESS training access than
      STORE, which is backwards. A manager needed to read the modules for their
      stores and assign them to their team, individually and in bulk, without
      gaining any authoring.
      WHAT A MANAGER CAN NOW DO: open the Training card on /hr, see the live
      modules for their own stores in card or list view with category badges and
      filter chips, open any of them and read it end to end WITH its attached
      files, and bulk assign any of them from either view.
      ITEM 2 WAS A GATE-AND-UI CHANGE, NOT A NEW WRITE PATH — CONFIRMED AT HEAD
      BEFORE ANYTHING WAS EDITED. The audit's claim reproduced exactly: POST
      /api/hr/training/assignments/bulk (:46) and GET .../bulk/recipients (:15)
      have both admitted store-scoped MANAGER at requireHrTrainingManageAccess
      (access.ts:113) since HR-22. The page gate at hr/training/page.tsx:30 was
      the only thing keeping managers out. assignments/bulk/route.ts,
      bulk/recipients/route.ts and bulk/shared.ts ARE NOT IN THE DIFF, and no
      HR-22 rule was weakened: store-id ownership + caller scope
      (bulk/route.ts:73-88), staff-id scope (:94-109), all-in-scope honouring
      isAdmin (:132-134), corporate exclusion from store expansion (:152, :187),
      ACTIVE-only in the picker (recipients:44) and the counts-sum-to-input
      invariant (:251-269) all stand untouched.
      THE RULINGS, AS GARY RULED THEM AT HARD STOP #1 ON 2026-08-12.
      (A) MANAGER'S LIBRARY IS STORE-SCOPED, AND R-h(i) IS NOT REVERSED. R-h's
      org-wide ruling rests on appliesTo never having been a visibility filter —
      true for STORE, and FALSE for MANAGER, which HR-17 has narrowed by exactly
      this OR clause since 2026-07-25 (preview/page.tsx) as has the manage tier
      of the resource download route. An org-wide manager library would have
      WIDENED manager read reach beyond what HR-17 settled, in a session about
      assignment. Store scoping preserves it, and it buys the chain LIST ⊆
      PREVIEW ⊆ DOWNLOAD: no row can appear whose Read 404s, and no file link
      can appear that the download route refuses. THIS IS THE ONE PLACE MANAGER
      AND STORE DIFFER ON THE READ SURFACE, and it differs in the stricter
      direction.
      (B) {kind:"read"} FOR MANAGER, per settled item 1 — extend the tier that
      exists, do not build a third variant. ADMIN is now the only previewer.
      TWO CONSEQUENCES, RULED RATHER THAN ABSORBED. (i) MANAGER loses the quiz
      on that page, since read mode drops it — and nothing anyone clicks
      changes, because at HEAD there was NO in-app link to the preview page for
      MANAGER at all (the only two are training-client.tsx, ADMIN+STORE, and
      the ADMIN save-and-preview at training-form.tsx:679), so the surface was
      URL-only for managers; the list row still carries quiz size and pass mark.
      (ii) MANAGER's URL reach NARROWS to live modules — HR-17 let a manager
      URL-reach an inactive or archived module there, and canReadTrainingModule
      is now authoritative for all three read roles. Gary ruled the narrowing in
      at the stop: drafts and archives belong to the builder.
      FILES FOR MANAGER — resourcesAvailable={true}, AND IT IS NOT NEW REACH.
      MANAGER has held manage-tier download access since HR-17
      (download/route.ts:29-52, scoped by the same appliesTo/store-overlap
      clause) and this session did not touch that route. Because (A) makes the
      library match that route's own clause, EVERY LINK A MANAGER CAN SEE IS ONE
      THAT ROUTE ALREADY SERVES — the audit's §3.3 dead-affordance failure
      avoided by construction rather than by suppression. HR-25 does not bite:
      its narrowing is scoped to the SELF tier (SELF_FILES_SERVED_WHERE at
      download/route.ts:72) and its own row says the manage tier is untouched.
      R-g(c)'s false answer, and its "ask a manager for a copy" copy, remain
      STORE's alone — which is correct, since the reader would otherwise BE the
      manager.
      ONE FLAG NO LONGER SUFFICED, AND THE SECOND IS NAMED FOR AN ACTION SET
      RATHER THAN A ROLE. Three tiers now view /hr/training with three different
      action sets — ADMIN everything, MANAGER read + assign, STORE read — and a
      single boolean expresses two. So: canManage (ADMIN — create, import,
      export, categories, edit, duplicate, lifecycle) and canAssign (ADMIN +
      MANAGER — the Assign button and the bulk dialog). canManage implies
      canAssign, no role name reaches the client, and the alternative the prompt
      warned against (a role-conditional tangle) never arises because every
      branch below the shell asks what the viewer may DO.
      PARITY HELD BY CONSTRUCTION (HR-21's rule). The Assign button went into
      the shared ModuleActions cluster and nowhere else, so card view and list
      view gained it in one edit — the slot HR-22 deliberately left there. The
      two dialogs SPLIT along the two flags: BulkAssignDialog under canAssign,
      CategoryManagerDialog stays ADMIN's. Category badges and filter chips are
      READ and both tiers keep them, deriving from the categories present in the
      viewer's own list, so admitting managers touched NO category route.
      THE BULK DIALOG NEEDED NO CHANGES, AND THAT IS HR-22'S DESIGN RATHER THAN
      LUCK. It computes no rule: stores, staff and eligibility all arrive
      pre-scoped from GET .../bulk/recipients under the same guard the write
      uses, and caller.isAdmin is stated by the server so even the preview count
      and "Everyone in my scope" are the server's reading. The prompt asked
      whether the dialog must render differently for a manager — it does, and it
      already did, because what the picker offers is what the write will accept.
      NO AUTHORING FOR MANAGER, AND NO GUARD MOVED TO MAKE THAT TRUE. All 16
      requireHrTrainingAccess sites are ADMIN-only and untouched, as are both
      authoring PAGES (new/page.tsx:14, [id]/edit/page.tsx:15). The access.ts
      diff touches requireHrTrainingReadAccess ONLY — the ADMIN guard and the
      manage guard have zero deletions between them. Census unchanged at 16 +
      11 + 1.
      R-C IS NOW FULLY SUPERSEDED, AND THE HISTORY IS THE POINT. R-c ruled
      /hr/training ADMIN-only on 2026-08-10, session-recommended and ratified,
      and said so REVERSIBLY-BY-ROW on demonstrated need. HR-24 superseded it
      for STORE-as-reader on the ground that the gate was a consequence of the
      page being an authoring tool. This row supersedes it for
      MANAGER-as-reader-and-assigner, on demonstrated operator need — the
      condition R-c itself named. R-C WAS NOT WRONG; ITS CONDITION WAS MET.
      STORE-as-assigner (R-i) remains unbuilt and unfiled, and PERM-5's rule
      that a role needing something it lacks means a different role still
      stands over it.
      ITEM 4 WAS NOT BUILT — priced only, filed as HR-27 (blocked on HR-19).
      Nothing in the capability registry was touched by this session.
      GARY'S WORKING ASSUMPTION, RECORDED 2026-08-12 SO HR-27 CAN BE
      RECONSIDERED AGAINST IT: admins build trainings, managers read and assign.
      If a manager genuinely needs to author, the answer is a different role,
      per PERM-5.
      NO BROWSER VERIFICATION BY THIS SESSION, deliberately and per the prompt:
      it is Gary's after the push, and the staging-SHA precondition forbids it
      before (staging serves 0ec4f20 at commit time; this work is not on it).
      Audit and ruling packets:
      docs/prompts/2026-08-11_TRAINING_ACCESS_AUDIT.md §§1-3, §5, §6, §9.3;
      session prompt docs/prompts/2026-08-12_HR-26_SESSION.md.

  - id: DOC-1
    track: hr
    size: L
    status: shipped
    title: "Document audience grants — a document is visible to its assigned audience, not to the whole org"
    notes: >
      ── DEBT-72a BOARD-CURRENCY BACKFILL 2026-08-17 — STATUS in_progress -> shipped.
      Prepended per preserve-and-mark; NOTHING BELOW IS EDITED.

      THIS ROW HAS BEEN STALE FOR FIVE DAYS, AND ALL THREE PHASES ARE IN PRODUCTION,
      not just Phase A. The text below reads "PHASE A BUILT ... Status in_progress
      until the push reaches Vercel"; A, B and C all reached production on
      2026-08-12.

      EVIDENCE. No `commits:` on this row; IDs resolved from commit subjects
      unanchored. "d728da4" (A: audience grants + audience-aware read policy),
      "e18dd54" (B: assign dialog, visibility toggle, audience chips) and "9133dcf"
      (C: audience-scoped compliance denominators) all return TRUE for
      `git merge-base --is-ancestor <sha> origin/main`, and all three return TRUE
      against promotion "ce036f9", the 2026-08-12 EVENING promotion. Its migration
      20260812171500_doc1a_document_audience_grants has been on the production
      database since 2026-08-13 04:50:22 UTC — so DOC-1's schema and some of the code
      that reads it reached production in DIFFERENT promotions. Additive; nothing
      broke.

      It did NOT ship in "7e77ea6", whose merge subject names "DOC-1" but whose
      47-commit range contains none of these three commits. NOT `verified`; that call
      is Gary's.
      Full measurement, method and the whole table:
      docs/prompts/DEBT-72a_BACKFILL.md. The rule this backfill accompanies is
      docs/WORKFLOW.md § Session completion rules item 4 — a row whose code was
      pushed to staging in a session has its status set in that session.
      Cross-refs: DEBT-72 (the promotion gate, still planned, still unbuilt),
      DEBT-72_AUDIT.md "b809e03" (which found this class), DEBT-72b (the gate
      itself, sequenced after this and deliberately not designed here).

      PHASE A BUILT 2026-08-12 (work commit "d728da4"). Status in_progress until
      the push reaches Vercel (the CHK-4 "in_progress at SHA" convention, as on
      HR-20/21/22/24/25/26). THREE PHASES: A is the grant table + the policy
      choke point + the ten ruled-in call sites (this commit); B is the assign
      dialog; C is the compliance rollup, the agreements panel and
      audience-scoped signing counters. A ships with NO UI.
      THE AUDIT FOUND THE PHASE WAS NOT THE PHASE THAT WAS SCOPED, TWICE, AND
      BOTH FINDINGS CHANGED THE BUILD.
      (1) canReadHrDocument had ONE caller — api/hr/documents/[id]/download —
      out of fifteen sites that resolve an HrDocument. Its own comment said
      "every future HR read path must resolve version -> document and ask this
      function"; eleven read paths and two write paths were built afterwards and
      not one of them did. The instruction was correct, in the right place, and
      did not propagate, because it lived in hr-files.ts — a Blob-service module
      that a page adding a document list has no reason to import. Phase A
      therefore had to ESTABLISH the choke point, not extend it; the function
      moved to lib/hr-documents-access.ts as part of the fix.
      (2) AN AUDIENCE MECHANISM ALREADY EXISTED AND WAS ALREADY LOAD-BEARING.
      HrDocument.appliesTo + HrDocumentStoreAssignment shipped with the original
      HR model, four call sites already filtered on it, and NOTHING IN src/ HAS
      EVER WRITTEN EITHER ONE — so all four were filtering on a constant, their
      store disjunct never once evaluated against a non-empty set. Measured on
      dev br-broad-wave-a6vpjdw0: 3 documents, 3 appliesTo "all", 0 grant rows.
      CORRECTION TO THAT FINDING, recorded here because the audit artifact is a
      claim wholesale and is never edited (CLAUDE.md § Where documents live):
      the audit said no writer exists and cited a grep across src/. There IS one
      — scripts/verify-hr8-compliance.ts:125, an HR-8 verification fixture — and
      the grep could not have found it because it was scoped to src/. The
      finding's substance is untouched (no APPLICATION writer, so no
      user-created row can exist in any environment) and the build caught the
      omission immediately at the rename. A sweep is only as complete as the
      directory it searched.
      R2, GARY 2026-08-12 — EXTEND, DO NOT DUPLICATE. HrDocumentGrant was NOT
      created as a second table. appliesTo "all" IS the company-wide grant and
      needs no row; the existing table gained granteeType (STORE | STAFF),
      nullable storeId and staffMemberId, createdById and createdAt. The Prisma
      MODEL was renamed to HrDocumentGrant with @@map holding the physical table
      name HrDocumentStoreAssignment — an honest name for the first mechanism,
      at zero SQL, rather than a rival mechanism beside it. Anything reading raw
      SQL still sees the old table name; the four existing call sites stay
      correct instead of being orphaned.
      ADDITIVE, WITH ONE WIDENING NAMED OUT LOUD: storeId went NOT NULL ->
      nullable, because a STAFF grant has no store. No column dropped. appliesTo
      changed its DEFAULT to "selected" and NOT its data, so every pre-existing
      document keeps "all" and keeps reaching everyone. NOTHING GOES DARK
      BETWEEN PHASES — the session prompt and this session's own hard-stop-1
      report both predicted staging would empty for non-admins after Phase A,
      and both were WRONG once R2 replaced a new table with a default flip. Only
      documents uploaded after the migration start ADMIN-only, which is the
      intended Phase B hand-off.
      TWO QUESTIONS, TWO FUNCTIONS, AND THAT IS THE PHASE'S LOAD-BEARING
      DECISION. canReadHrDocument(doc, viewer) answers "may this viewer read it"
      — the viewer may be a STORE login with no StaffMember at all.
      grantedToStaff(doc, staff) answers "does this document apply to this
      person". The manager-attest path is why they are not one function with a
      flag: /hr/acknowledge/[id]?staff=X must ask the second question about X
      and never the first about the manager (ruling 4). Pre-DOC-1 that path
      asked NEITHER — it tested only whether the manager's stores overlapped the
      staff member's, which answers "may this manager act for this person" and
      says nothing about whether the document was ever assigned. A single
      function with a subject flag would eventually be passed the wrong subject
      at one of ten sites; two names cannot be.
      THE WRITES ASK TOO, AND THAT IS THE HALF THAT MATTERS MOST (R1). POST
      .../acknowledgments and POST .../signed-record now refuse a signature for
      a document the staff member was never granted. A read refusal prevents a
      leak; this prevents a PERMANENT ARTIFACT, because completing the required
      checkpoint set mints an HrSignedRecord and ruling 4 makes that permanent —
      there is no path that un-signs it. It also closes the stale-tab and
      hand-rolled-POST cases a page gate cannot reach.
      R3 — STORE GRANTS NEVER REACH CORPORATE STAFF. Square expands corporate
      staff to a StoreStaffAssignment for EVERY store, so a naive roster match
      sweeps them into every store grant, silently, with correct-looking data at
      every step. The exclusion is explicit in both the predicate and the query
      fragment, mirroring the training bulk-assign rule
      (assignments/bulk/route.ts:187). COMPANY grants still reach them and an
      individual STAFF grant always can.
      MANAGER READS STORE-SCOPED — A DECISION THIS SESSION MADE, NOT ONE GARY
      RULED, recorded so it can be overturned cheaply. Managers frequently have
      no StaffMember row and would otherwise have lost read access they hold
      today while still being able to attest for their team — an incoherent
      pair. Store-scoped rather than org-wide follows HR-26's precedent for the
      same question; org-wide reach stays ADMIN's alone. This does NOT touch the
      attest path, which asks grantedToStaff about the staff member.
      SCOPE HELD. #9 (/staff/[id] Agreements) was ruled IN so the two tabs of
      one page share one audience rule. hr-compliance.ts is RENAMED ONLY and its
      rule is still the pre-DOC-1 one, with a comment saying so at the call site
      — audience changes the compliance DENOMINATOR there, not just visibility,
      and that is Phase C with its own verification. The /hr/forms FillableForm
      surfaces are untouched and canReadHrDocument's FillableForm branch is
      unchanged (ADMIN/MANAGER), deliberately, so no out-of-scope behaviour
      change rides in through a shared function. The audit's count of those
      sites is corrected in the artifact itself at §3.1 — eleven query sites
      across seven files, not the four reported at hard stop 1.
      VERIFIED ON DEV (br-broad-wave-a6vpjdw0), 28/28 — the query fragment and
      the predicate return the SAME document set for store staff, other-store
      staff and corporate staff (the fragment/function drift the training.ts
      pattern exists to prevent); the CHECK rejected a STORE row carrying a
      staff member (23514); the partial unique index rejected a duplicate STAFF
      grant (23505); ADMIN saw all four fixtures including the zero-grant one;
      a STORE login saw only its own store's and the company-wide one. All
      DOC-1A-named fixtures removed and the removal asserted.
      A CLAIM THIS SESSION GOT WRONG AND CORRECTED, kept because the correction
      is reusable: the schema comment and the work commit message both said a
      future `prisma migrate diff` would propose DROPPING the two hand-written
      objects. It will not — MIGRATIONS.md § Protected indexes already recorded,
      with evidence from 2026-07-29, that diff is blind to partial indexes in
      both directions, and this session re-measured it and extended the finding
      to CHECK constraints. The reasoning error was "Prisma cannot express it"
      -> "Prisma will fight it". The wrong version would have sent a reader
      hunting a DROP INDEX line that never appears while leaving the REAL hazard
      — the §2 baseline squash, which regenerates from the schema and omits both
      — unwatched. The schema comment was corrected; THE MIGRATION FILE'S HEADER
      STILL CARRIES THE WRONG CLAIM AND WAS LEFT ALONE ON PURPOSE, because
      editing an applied migration changes its checksum in _prisma_migrations
      and breaks migrate deploy on every branch that already ran it. An applied
      migration is frozen even when it is wrong.
      NOT PROMOTED. Applied to dev only; staging and production receive it via
      migrate deploy on Gary's push. BEFORE PRODUCTION, run hard stop 4 on
      br-sparkling-block — SELECT count(*) FROM "HrDocument" with the branch
      column in the same output. Zero → the fresh-start ruling holds and no
      backfill ships. Nonzero → STOP, a COMPANY-grant backfill becomes a ruling
      in planning chat. Note what makes this easy to skip: because existing rows
      keep "all", a forgotten backfill breaks nothing visible — it just leaves
      real production documents company-wide forever, which is the right default
      but was never an explicit decision about real content.
      NO BROWSER VERIFICATION BY THIS SESSION, per the staging-SHA precondition:
      this work is not on staging at commit time. Audit:
      docs/prompts/DOC-1_A_AUDIT.md; session prompt
      docs/prompts/DOC-1_phase_A_audience_grants_audit.md; migration entry
      docs/MIGRATIONS.md § 2026-08-12.
      ────────────────────────────────────────────────────────────────────
      PHASE B BUILT 2026-08-12 (work commit "e18dd54"). The assign dialog,
      the visibility toggle and the audience chips. Phase A shipped the
      audience machinery with NO WRITER anywhere in the application; this is
      the writer plus the two admin surfaces that make an audience legible.
      PUT/GET /api/hr/documents/[id]/audience, ADMIN on the canManage seam,
      scoped to Reference|Acknowledgment by the same guard [id]/route.ts
      uses — a FillableForm id 404s, so forms stay outside the mechanism.
      THE REACH COUNT IS THE POLICY FUNCTION, ASKED A HYPOTHETICAL, AND
      DECLINING TO ADD A HELPER IS THE DESIGN DECISION OF THE PHASE. A
      reachOfStoreGrant() beside grantedToStaff would read as tidier and
      would be a SECOND EXPRESSION OF ONE RULE — precisely what
      lib/hr-documents-access.ts's header exists to prevent, and precisely
      the drift class the session prompt named. Instead the route builds the
      document that WOULD exist and asks the real predicate about it, so the
      corporate exclusion (R3) cannot be forgotten in the dialog because it
      is never restated there.
      LOSSLESSNESS IS A TYPE, NOT A PROMISE. The PUT body is a Zod
      discriminated union on appliesTo; the "all" branch carries no selection
      arrays at all, so the route PHYSICALLY CANNOT delete a grant row on
      that path — the parser refuses the fields that would let it. Rows go
      dormant under "all" and govern again on narrowing. Verified: the flip
      deleted zero rows (3 → 3) and narrowing restored the audience exactly.
      DELTA, NOT REPLACE (ruling (c), Gary 2026-08-12) — surviving rows keep
      createdAt and createdById, provenance a delete-all/insert-all resets on
      every unrelated save. Extracted to lib/hr-document-audience.ts because
      a Next route file may export only handlers, and the delta is the piece
      most needing to be callable by a verification script rather than
      re-typed into one. Testing a copy of a write algorithm proves the copy.
      THE PICKER OFFERS TERMINATED GRANT-HOLDERS, AND THAT IS LOAD-BEARING,
      NOT COURTESY. With an ACTIVE-only population a delta write silently
      revokes anyone terminated since their grant was made: absent from the
      picker, absent from the submitted set, deleted. Both populations were
      measured on the same fixture — ACTIVE-only produces one spurious
      removal, the shipped union produces none.
      RULING (a), GARY 2026-08-12 — reach counts load ACTIVE staff only,
      counted via grantedToStaff, worded "reaches N" per HR-22. NAMED
      DIVERGENCE, NOT DRIFT: THE POLICY HAS NO EMPLOYMENT-STATUS TEST OF ANY
      KIND and reaches TERMINATED staff. PHASE C MUST COUNT ACTIVE STAFF ONLY
      IN COMPLIANCE DENOMINATORS — recorded here by that ruling because the
      divergence is invisible from inside either module.
      RULING (b) — the signed-record recovery path KEEPS its missing isActive
      filter, now with the reasoning written down at the query. It is the only
      one of twelve document read paths without it, and adding it would strand
      a completed signature whose PDF generation failed before an archive:
      archiving is a VISIBILITY decision and must not reach backwards into
      signatures already given. ensureSignedRecord refuses an incomplete
      checkpoint set, so nothing can be minted early.
      VISIBILITY WAS ALREADY BROKEN, NOT MISSING, AND THAT CHANGED WHAT ITEM 2
      WAS. Archive shipped with HR-4 and PATCH has always accepted
      isActive:true — the write half of restore was already there — but the
      library filtered isActive UNCONDITIONALLY, INCLUDING FOR ADMIN, so
      archiving removed the row from the only surface that lists it. An
      archived Reference document was reachable only by typed URL and ruling
      8's "hide, never delete" was in practice "hide forever". ADMIN now sees
      archived rows behind a collapsed disclosure with Restore; every
      non-admin query is byte-for-byte unchanged.
      CHIPS COUNT GRANT ROWS, NOT PEOPLE. A reach count per library row would
      cost either a second read per render or a browser-side copy of the
      corporate rule; "2 stores" is a fact the row already holds. Unassigned
      is the WARNING state because it means visible to admins and nobody else
      — the condition the whole phase exists to surface. NO LIVE CLIENT-SIDE
      TOTAL IN THE DIALOG, deliberately: HR-22 can afford an advisory
      browser-computed preview because its POST reports the server's own
      figures afterwards, and copying that here would put a second copy of R3
      in the client. The aggregate arrives in the PUT response instead. HR-22's
      compliance-dip disclosure has no analogue either — a training assignment
      enters the denominator the instant it is written, whereas an audience
      edit is reversible by ruling, so warning about it would claim a
      consequence that does not exist.
      FIX NOW ADMITTED INTO PHASE B BY RULING — PHASE A CLOSED THE AGREEMENTS
      TAB TO EVERY NEW FILLABLE FORM. createFillableForm (lib/hr-forms.ts:96)
      never set appliesTo, so after Phase A flipped the column DEFAULT to
      "selected" every form built since was born with an EMPTY AUDIENCE.
      /hr/forms never noticed (canReadHrDocument's FillableForm branch does not
      consult the audience) and the org-level Agreements panel never noticed
      (no audience filter) — but /staff/[id]'s Agreements tab did, because
      Phase A ruled it IN so both tabs of one page share one rule, AND NOTHING
      IN THE APPLICATION COULD EVER GRANT A FORM AN AUDIENCE, since the assign
      dialog is scoped to a library that excludes FillableForm by construction.
      New forms were invisible there permanently and unfixably. Gary ruled
      option (i) 2026-08-12: createFillableForm sets appliesTo "all" — one
      line, restoring pre-DOC-1 behaviour exactly (every form predating the
      migration carries it, and the clause that tab used before Phase A WAS
      the appliesTo "all" disjunct) and keeping forms OUT of the grant
      mechanism rather than dragging them in.
      CORRECTION TO PHASE A'S OWN CLAIM, DATED 2026-08-12 AND MARKED RATHER
      THAN EDITED (Gary's instruction; CLAUDE.md § Where documents live).
      Phase A's entry above states "NOTHING GOES DARK BETWEEN PHASES". That
      was TRUE FOR DOCUMENTS and FALSE FOR FORMS, and the exception was found
      by Phase B's audit and fixed in this commit. The original sentence
      stands unedited; this is its correction. Note the shape — the claim was
      reasoned from the default flip preserving existing ROWS, which it did;
      what it missed is a CREATE PATH that never named the column. A default
      is a promise about rows that exist and says nothing about the code that
      makes new ones.
      VERIFIED ON DEV br-broad-wave-a6vpjdw0 / neondb, 32/32, on Organization
      cf888f2d-f234-48c7-8097-fd5b44b5b3dd (clerkOrgId
      org_3FhYUR4l0ue7egug1I0Ig8wxOVn — a PRODUCTION-ORIGINATED id on a
      dev-branch row, dev having been forked from production; the branch label
      is what makes the result evidence). Reach count equals measured policy
      reach; corporate excluded from store grants; other stores unreached; the
      Everyone flip deletes nothing and narrowing restores exactly; provenance
      survives an unrelated edit; hrdoc_grant_shape rejects a STORE row
      carrying a staff member (23514) and the partial unique index rejects a
      duplicate STAFF grant (23505); archived vanishes from both the non-admin
      library query and the /my + /staff audience query, stays listed for
      ADMIN, and returns on restore; six chip states. All fixtures DOC-1B-named,
      removed, removal asserted.
      NOT VERIFIED, AND IT IS THE HTTP SHELL — the guard, the Zod parse, the
      transaction composition and the response shape. Reaching the route needs
      an authenticated ADMIN session and this session may not enter
      credentials. The delta was extracted specifically so a script could call
      the SHIPPED function instead of a copy, and the policy, constraints and
      query fragments were measured directly; but the route was never called
      over HTTP. Chips and the archived disclosure were rendered in a browser
      through a temporary page under the public /menu route (removed before the
      work commit, no console errors). BOTH GAPS BELONG TO THE STAGING PASS.
      FOR THE STAGING CHECKLIST — dev has ZERO FillableForm rows with
      appliesTo='selected' (branch id in the same query output). STAGING HAS
      HAD THE DEFAULT FLIP SINCE GARY'S PUSH EARLIER ON 2026-08-12, so run the
      same query there: any such rows were BORN DARK on the Agreements tab and
      may need a one-time correction at promote time. Also still outstanding
      from Phase A: the STORE-login (shared iPad) staging walk-through has
      never been observed. And unchanged, before any production promotion:
      hard stop 4 — production HrDocument count on br-sparkling-block with the
      branch column in the same output; nonzero stops the promotion.
      Audit: docs/prompts/DOC-1_B_AUDIT.md; session prompt
      docs/prompts/DOC-1_phase_B_assign_dialog.md. NO MIGRATION IN PHASE B —
      every column, constraint and index it writes already existed.
      ────────────────────────────────────────────────────────────────────
      PHASE C BUILT 2026-08-12 (work commit "9133dcf") — DOC-1 IS NOW
      FEATURE-COMPLETE, still in_progress until the push reaches Vercel.
      Compliance denominators answer the audience question.
      THE AUDIT'S FIRST FINDING CHANGED WHAT THE PHASE WAS. The question
      this whole feature exists for — "3 of 5 Colorado people have signed
      the handbook" — IS NOT A QUESTION ANY SURFACE IN THE PRODUCT ASKS.
      There is no per-document completion counter anywhere in src/: not on
      the library, not on the admin detail, not in an API route, not on a
      dashboard. Every completion figure is PER-STAFF-MEMBER ("how many of
      the items that apply to YOU are done"), summed upward into store and
      org aggregates. The only per-document population figure is Phase B's
      assign-dialog "reaches", which was already correct. So Phase C is not
      "build the N-of-M denominator"; it is "the per-staff document set is
      chosen by a rule that knows nothing about STAFF grants or the
      corporate exclusion, and that wrong set propagates into every
      aggregate the product renders". Q1 RULED (Gary, 2026-08-12): correct
      the existing counters only, one phase no riders — the per-document
      counter is DOC-2 below.
      FIFTEEN COUNTER SURFACES ENUMERATED, TWO IN. Everything marked IN —
      the /hr/compliance KPI cards, By Store, the per-employee table, the
      /staff pct column, /staff/[id]'s Compliance tab, /my's open items —
      reduces to ONE call in computeStaffComplianceDetails, which is why
      there was one expression to change and not nine. Two enumerated
      ABSENCES worth keeping: there is no HR-3 acknowledgment-status route
      (49 files under api/hr/**, none returns one), and neither /dashboard
      nor /store-view references HR compliance at all.
      THREE DEFECTS CLOSED, ALL LIVE-ON-FIRST-USE RATHER THAN THEORETICAL,
      and the verification runs the OLD rule side by side so they are
      recorded as measured rather than argued. (i) STAFF GRANTS WERE
      INVISIBLE — the rule tested granteeType "STORE" only, so an
      individually-granted document never entered that person's compliance
      items while /my/documents showed it to them and the signing write
      accepted their signature: two surfaces disagreeing about one person
      and one document. (ii) R3 WAS ABSENT — Square expands corporate staff
      to a StoreStaffAssignment for EVERY store, so any store grant swept
      every corporate member in, permanently dragging a pct for a document
      staffAudienceWhere would never have shown them. (iii) it was a second
      expression of one rule.
      THE PREDICATE, NOT THE QUERY FRAGMENT. staffAudienceWhere narrows one
      query per staff member; this function fetches the org's documents once
      and filters in JS for every member, a fixed set of batched queries by
      construction. Asking grantedToStaff per (doc, member) keeps that and
      still leaves exactly one expression of the rule — Phase B's
      reachOfStoreGrant refusal, again. No new query: the grants relation
      was already loaded one column narrower, and THE MISSING staffMemberId
      IS DEFECT (i)'s MECHANICAL CAUSE, which is why AUDIENCE_INCLUDE (a
      named shape that cannot be half-loaded) replaced the inline select.
      RULING 4 IS SATISFIED STRUCTURALLY, NOT BY A FILTER — recorded because
      it is the ruling that could have implied numerator work and did not. A
      transferred signer's document leaves their items, so requiredTotal and
      completedCount fall together out of the same array; numerator and
      denominator cannot be drawn from different populations. Verified
      anyway: a property that is free today is exactly the kind that stops
      being free without anyone noticing.
      Q2 RULED — THE AGREEMENTS PANEL (Phase A site #11) STAYS UNFILTERED,
      closed as "audited, ruled to stay", reasoning at the call site. It has
      NO DENOMINATOR: executedCount and pendingCount are raw submission
      counts, so a filter could only ever REMOVE ROWS FROM A LIST, never
      correct a number. The row it could remove is a form born appliesTo
      "selected" between Phase A's default flip and Phase B's fix — a form
      no application code can ever grant an audience, since the assign
      dialog 404s FillableForm ids. THAT SET IS EMPTY EVERYWHERE IT COULD
      EXIST, measured not assumed: dev br-broad-wave-a6vpjdw0 zero, STAGING
      MEASURED 2026-08-12 dark_forms=0 (neondb, ep-odd-rain), and production
      has received no DOC-1 code. This closes the Phase B staging checklist
      item. The fixture set still includes that stranded shape and asserts
      it is listed, so the ruling is tested rather than trusted.
      Q4 RULED — ZERO-AUDIENCE DOCUMENTS STAY EXCLUDED from every
      denominator. grantedToStaff returns false for everyone, so this is the
      pre-existing behaviour PRESERVED DELIBERATELY rather than inherited by
      accident. Gary: the Unassigned chip is the annunciator; no second
      alarm. A second one would restate one fact on a page whose contract is
      percentages over PEOPLE, contributing a number that is zero by
      definition — and counting it would manufacture an obligation nobody
      could discharge, the same shape as defect (ii).
      Q5 RULED — ARCHIVING IS A FORWARD-LOOKING VISIBILITY DECISION: it
      removes a document from active compliance and never reaches backward
      into records already made. No code change (isActive already did it),
      but the consequence is now written down.
      TWO NUMBERS THAT WILL MOVE FOR CORRECT REASONS AND WILL BE REPORTED AS
      BUGS. Both are recorded here in plain words because that is the whole
      point of recording them. FIRST: org-wide required totals WILL DROP the
      first time store grants come into real use, as corporate staff stop
      being swept into store-granted documents. That is R3 working exactly
      as ratified, not a regression. SECOND: archiving a document
      IMMEDIATELY CLEARS NON-COMPLIANCE for everyone who never signed it —
      correct, because the obligation was withdrawn rather than met.
      THE :173 RULING, WRITTEN DOWN RATHER THAN "FIXED". The absent ACTIVE
      filter on computeStaffComplianceDetails' staffIds branch is
      deliberate: ruling 2 governs who is counted in a POPULATION, not
      whether a terminated person's own profile renders their auditable
      history, and both id-passing callers depend on it. It looks like an
      oversight sitting next to ruling 2, which is why it now carries a
      comment saying it is not one. The ACTIVE filter that ruling 2 DOES
      mean is named at getOrgComplianceRollup, with the divergence stated:
      the policy module has no employment-status test and reaches TERMINATED
      staff, deliberately, because Phase B's assign dialog depends on it.
      ALSO AUDITED AND RULED OUT: hr/documents/[id]'s per-checkpoint
      acknowledgmentCount stays unfiltered. It drives the edit/delete lock
      and is never rendered as a number; filtering it would let a checkpoint
      become DELETABLE once its only signer left the audience — reaching
      backwards into a signature already given, which ruling 5 forbids. It
      now carries a comment so the next audience sweep does not read it as a
      missed site.
      VERIFIED ON DEV br-broad-wave-a6vpjdw0 / neondb, 43/43, IN A THROWAWAY
      FIXTURE ORG so the six real dev staff and three real dev documents
      were never touched. Every count asserted against expectedTitles(),
      written from the ruling text rather than from the code under test, so
      a bug in grantedToStaff cannot make its own test pass. 7 staff
      (in-audience signer, transferred signer, non-signer, TERMINATED
      member, corporate member HOLDING AN ASSIGNMENT ON THE GRANTED STORE —
      asserted, so the R3 check cannot pass vacuously — other-store member,
      STAFF-grant holder), 5 documents (store, staff, zero-audience,
      archived, company-wide), 2 forms including the stranded shape. The
      transferred signer's HrSignedRecord and both acknowledgment rows are
      asserted to SURVIVE while their document leaves the denominator; the
      company-wide flip is asserted to bring both the corporate member and
      that signature back. All fixtures DOC-1C-named, removed, removal
      asserted BY RE-QUERY rather than by delete count.
      NO MIGRATION — every column, constraint and index already existed; the
      only shape change is a Prisma select widening.
      lib/hr-documents-access.ts is untouched and grantedToStaff gained no
      status test. NO BROWSER VERIFICATION BY THIS SESSION, per the
      staging-SHA precondition: this work is not on staging at commit time.
      Audit: docs/prompts/DOC-1_C_AUDIT.md; session prompt
      docs/prompts/DOC-1_phase_C_compliance_counters.md.
      CARRIED, STILL OPEN AFTER THIS PHASE — the STORE-login (shared iPad)
      staging walk-through has never been observed (Phase A); a live
      store-sweep assignment has not been observed on staging (Phase B); and
      unchanged before any production promotion, HARD STOP 4 — production
      HrDocument count on br-sparkling-block with the branch column in the
      same output, nonzero stops the promotion.

  - id: DOC-2
    track: hr
    size: M
    status: planned
    title: "Per-document compliance view — who hasn't signed this document"
    notes: >
      SPLIT OUT OF DOC-1 PHASE C BY RULING (Gary, Q1, 2026-08-12): "correct
      the existing per-staff counters only. The per-document completion
      counter is NOT built this phase: file it as its own row. One phase, no
      riders."
      THE GAP THIS ROW EXISTS TO CLOSE, in the words the DOC-1 planning used
      throughout: "3 of 5 Colorado people have signed the handbook." DOC-1 C's
      audit established that NO SURFACE IN THE PRODUCT ASKS THAT QUESTION.
      Every completion figure is per-staff-member — "how many of the items
      that apply to YOU are done" — summed upward into store and org
      aggregates. Nothing anywhere pivots the other way, from a DOCUMENT to
      the people who owe it. An admin who uploads a handbook, assigns it to
      Colorado and wants to know who is outstanding has to read the
      per-employee table and filter it by eye.
      WHAT MAKES THIS CHEAP NOW, AND WHY IT WAS RIGHT TO DEFER. The hard part
      is already built and verified: the audience is a policy function
      (grantedToStaff), the ACTIVE-staff rule is ruled and sited at the
      counting layer, and DOC-1 B's assign dialog ALREADY COMPUTES A
      PER-DOCUMENT POPULATION — "reaches N" — through that same predicate
      (api/hr/documents/[id]/audience/route.ts). This row is that population
      joined to signature state, which is a new READ and a new surface, not a
      new rule. Do not restate the audience rule to build it; ask the
      predicate, as Phase B and Phase C both did.
      LIKELY SHAPE, NOT YET RULED — a section on /hr/documents/[id] (already
      ADMIN-only) listing the current audience with each person's status, or
      a per-row "N of M" on the library. The library option costs a reach
      count per row, which DOC-1 B explicitly declined for the audience chips
      (either a second read per render or a browser-side copy of R3); take
      that finding as an input rather than rediscovering it.
      THE RULINGS IT INHERITS, all ratified 2026-08-12 and all already
      enforced in lib/hr-compliance.ts: denominator = the document's CURRENT
      audience; ACTIVE staff only, filtered at the counting layer and never
      in the policy module; STORE grants never reach corporate staff (R3);
      numerator = signatures from people CURRENTLY in the audience, so a
      transferred signer's record survives but stops counting; zero-audience
      documents are excluded (the Unassigned chip is the annunciator);
      archiving removes a document from active compliance and never reaches
      backward into records already made.
      NOT STARTED. No files, no schema expected — DOC-1 C needed none and
      this needs strictly less.

  - id: HR-27
    track: hr
    size: L
    status: planned
    title: "ADMIN-toggleable manager training capabilities (create/edit/delete/categorize), default off"
    blockers:
      - >
        BLOCKED ON HR-19 — ruled by Gary 2026-08-12, at HR-26's hard stop.
        NOT STARTED, and it must not be started as a side effect of a training
        session. There is no quiet third option: HR-27 either lands AFTER HR-19,
        or it is ruled EXPLICITLY to be HR-19's first slice, taken deliberately
        and recorded as such. R-e ruled that a phase must not quietly begin the
        registry migration, and PERM-5C left this exact boundary untouched with
        a comment (staff/[id]/page.tsx:102-106) so that it could not be crossed
        inside a file nobody would review as part of it.
    notes: >
      FILED 2026-08-12 by the HR-26 session as a PRICED PACKET. Item 4 of that
      session's prompt; build-nothing was the instruction and nothing was built.
      All evidence below was read at HEAD 0ec4f20. No database was queried.
      THE ASK: ADMIN-toggleable manager training capabilities — create, edit,
      delete, categorize — DEFAULT OFF.
      (1) THE CONTRADICTION THAT MUST BE RESOLVED BEFORE ANYTHING ELSE IS
      PRICED. The override layer is a DENY LIST WITH NO DIRECTION —
      User.deniedCapabilities: String[], no grantedCapabilities, no sign field.
      can()'s ceiling precedes its only `return true` (permissions.ts:309-320)
      and PATCH /api/users/[id]:104 filters submitted capabilities through
      can({ role }, c) before storage, so a GRANT CANNOT EVEN BE WRITTEN DOWN.
      A toggle for manager authoring is therefore expressible ONLY IF MANAGER
      HOLDS AUTHORING IN ITS ROLE BASELINE — hr.training.author moving from
      ADMIN_ONLY (permissions.ts:258) to MANAGE — and the moment it does, every
      manager account holds authoring from deploy until an admin removes it, one
      login at a time. THAT IS DEFAULT-ON. Unlike R-i (which was
      unrepresentable), this is representable, but only in the direction
      OPPOSITE to the one asked for. Everything below prices closing that gap.
      (2) THE CAPABILITIES ARE INERT TODAY, SO A TOGGLE WOULD DO NOTHING — which
      the grid's own founding rule forbids (permissions.ts:353-357: "A toggle
      that does nothing is WORSE than no toggle"). Zero can() calls exist in the
      entire training surface: 28 guard call sites at HEAD (16 + 11 + 1), all
      resolving to three inline role tests (access.ts:32, :80, :113) — R-e's
      pattern, held exactly. hr.training.author (:258, ADMIN_ONLY) and
      hr.training.manage (:259, MANAGE) are registry entries with no enforcement
      anywhere, and NEITHER IS IN ENFORCED_CAPABILITIES (:399-547), so neither
      is deniable from the /users grid at all.
      (3) ROUTES THAT MUST CONSULT can() FOR THE FOUR NAMED TOGGLES — all 16
      ADMIN sites, grouped, read at HEAD. CREATE: training/route.ts:89 POST,
      upload-url:18 POST, lessons/[lessonId]/resources:22 POST, import:15 POST.
      EDIT: training/[id]/route.ts:58 PATCH, training/route.ts:70 PATCH (bulk
      lifecycle). DELETE: training/[id]/route.ts:194 DELETE,
      resources/[id]/route.ts:7 DELETE. CATEGORIZE: categories/route.ts:45 POST,
      categories/[id]/route.ts:12 PATCH and :65 DELETE,
      categories/[id]/reassign:15 POST. READ-SIDE THAT MUST FOLLOW OR THE
      SURFACES DISAGREE: training/route.ts:57 GET, training/[id]/route.ts:41
      GET, categories/route.ts:14 GET, export:17 GET. PLUS two page gates
      (new/page.tsx:14, [id]/edit/page.tsx:15) and the authoring cluster in
      training-client.tsx, which would need a per-capability flag SET rather
      than HR-26's two booleans.
      (4) IS THAT A PARTIAL HR-19? YES, UNAMBIGUOUSLY — 16 of HR-19's 35 routes
      and 2 of its 13 pages, roughly half its route surface, arriving under a
      non-HR-19 heading. This is why the row is blocked; see the blocker above.
      (5) "DEFAULT OFF" SEMANTICS — FOUR MECHANISMS, PRICED, NONE RECOMMENDED.
      (i) SEED DENIALS AT CREATION — baseline moves to MANAGE and a denial is
      written for every manager at creation, BACKFILLED onto existing ones.
      Needs a data write across every existing manager (its own TIER 3 piece)
      and every creation path must seed it — POST /api/users, the Clerk webhook,
      and any role change to MANAGER. A SINGLE MISSED PATH IS A MANAGER SILENTLY
      HOLDING AUTHORING, and nothing errors. Grid copy stays literally true but
      reads as a grant.
      (ii) INVERT THE MEANING FOR THESE KEYS — an entry means "granted" for
      hr.training.*. Genuine default-off, no backfill. DESTROYS the one-line
      guarantee that can()'s override block contains no `true` literal
      (permissions.ts:296-320); every future reader must know which keys are
      inverted, and the grid copy (user-actions.tsx:355-356, "restrict below
      what their role allows, never above it") is FLATLY FALSE for those rows.
      (iii) A REAL GRANT DIRECTION — grantedCapabilities or a signed entry, plus
      a `return true` above the ceiling. Clean default-off; OVERTURNS A RECORDED
      RULING (PERM-5's row and DECISIONS.md 2026-07-27) including PERM-5's
      advance answer to this exact situation, and the price is paid by every
      capability rather than this one. SCHEMA CHANGE.
      (iv) OUTSIDE THE REGISTRY — a per-user boolean consulted by a new
      requireHrTrainingAuthorAccess. Clean, additive, PERM-5 untouched;
      re-scatters what PERM-1..PERM-5C consolidated and hands HR-19 a bespoke
      flag alongside 35 routes. SCHEMA CHANGE. Cheapest to build, most expensive
      to have built.
      SUMMARY OF (5) AS GARY ASKED IT RECORDED: two of the four are schema
      changes, one overturns PERM-5's recorded ruling, and the cheapest carries
      a fleet-wide backfill with a silent failure mode.
      (6) BLAST RADIUS — THE MANAGER-ACCOUNT COUNT IS OWED AND WAS NOT TAKEN.
      If authoring becomes a MANAGER baseline, every manager account holds it
      from deploy until removed, one login at a time, and the failure is silent:
      nothing errors, rows just appear. The number must be READ OFF /users, not
      queried: that page renders one row per Clerk org member with a role badge
      (users/page.tsx:316-317) and has NO aggregate count and NO role filter, so
      it is a manual count of rows badged "Manager". Two caveats make it a
      per-environment reading rather than a fact — the list is Clerk-membership
      driven and defaults an unresolved member to STAFF (:198), so it is the
      CLERK INSTANCE's truth (production Clerk is not the dev instance staging
      shares); and it shows only the ACTIVE ORG. The figure HR-27 needs is
      managers in the Keva PRODUCTION org, read from production /users, dated,
      with the org id named. A staging count answers a different question.
      (7) NOTHING IS RECOMMENDED HERE, per the commissioning instruction —
      priced, not advised.
      GARY'S WORKING ASSUMPTION, RECORDED 2026-08-12 SO THIS ROW IS RECONSIDERED
      AGAINST IT RATHER THAN IN A VACUUM: admins build trainings, managers read
      and assign. If a manager genuinely needs to author, the answer is a
      different role, per PERM-5.
      Source: docs/prompts/2026-08-12_HR-26_SESSION.md item 4; evidence packet
      docs/prompts/2026-08-11_TRAINING_ACCESS_AUDIT.md §7 and §9.3 (R-i).

  - id: HR-28
    track: hr
    size: S
    status: shipped
    commits: ["142e858", "340f99b", "e9a717c"]
    title: "Training module descriptions are rich text — Tiptap editor, one sanitizer allowlist, three write paths"
    notes: >
      ── DEBT-72a BOARD-CURRENCY BACKFILL 2026-08-17 — STATUS in_progress -> shipped.
      Prepended per preserve-and-mark; NOTHING BELOW IS EDITED.

      THIS ROW HAS BEEN STALE FOR FIVE DAYS. It reads "Status in_progress until the
      push reaches Vercel"; the push reached Vercel on 2026-08-12.

      EVIDENCE. All three recorded work commits — "142e858" (the editor),
      "340f99b" (the bundle follow-up) and "e9a717c" (the sanitize-html swap) —
      return TRUE for `git merge-base --is-ancestor <sha> origin/main`, and all three
      return TRUE against promotion "b853787", the 2026-08-12 MIDDAY promotion. NOT
      `verified`; that call is Gary's.
      Full measurement, method and the whole table:
      docs/prompts/DEBT-72a_BACKFILL.md. The rule this backfill accompanies is
      docs/WORKFLOW.md § Session completion rules item 4 — a row whose code was
      pushed to staging in a session has its status set in that session.
      Cross-refs: DEBT-72 (the promotion gate, still planned, still unbuilt),
      DEBT-72_AUDIT.md "b809e03" (which found this class), DEBT-72b (the gate
      itself, sequenced after this and deliberately not designed here).

      BUILT 2026-08-12 (work commit "142e858"). Status in_progress until the
      push reaches Vercel (the CHK-4 "in_progress at SHA" convention, as on
      HR-20/21/22/24/25/26). NO SCHEMA CHANGE — TrainingModule.description is
      the same text column it always was; it simply holds sanitized HTML now.
      No database was queried by this session, on any branch.
      FILED AS HR-28, NOT HR-23. The session prompt specified HR-23; that id
      was already taken by a planned row (the single-assign path, filed
      2026-08-11 by HR-22). HR-28 is the next free id and the prompt's own
      instruction was to use it if HR-23 was occupied.
      THE PROBLEM: the Description field was a plain textarea and rendered
      through a bare <p> with no whitespace handling, so an admin's paragraph
      breaks collapsed into one run-on line on the trainee's screen. The
      authoring tool accepted structure the renderer discarded. Note the
      LESSON body beside it already rendered with whitespace-pre-wrap
      (training-module-view.tsx:155) — the description was the odd one out.
      WHAT SHIPPED. (1) src/components/ui/rich-text-editor.tsx — Tiptap 3.30
      with a four-button toolbar: bold, italic, bullet list, numbered list.
      (2) src/lib/sanitize-html.ts — ONE DOMPurify allowlist
      (p br strong em b i ul ol li), ZERO attributes, shared by every write
      path and by the renderer. (3) src/lib/rich-text.ts — the pure half.
      (4) The renderer branches: HTML gets sanitized-then-dangerouslySetInnerHTML
      inside a wrapper carrying explicit list classes, legacy plain text gets
      whitespace-pre-wrap so it at least keeps the breaks its author typed.
      THE TOOLBAR AND THE ALLOWLIST ARE ONE DECISION, NOT TWO. StarterKit v3
      ships heading, link, code, codeBlock, blockquote, strike and underline
      ENABLED BY DEFAULT. Left alone, an admin hits Cmd-Alt-1, saves, and
      watches the heading come back as plain text, because the server strips
      what the allowlist does not name — formatting that appears to work and
      then silently doesn't, which is worse than formatting never offered. All
      seven are disabled at the source. trailingNode is disabled too, for a
      different reason: it appends a real empty paragraph and it SERIALISES, so
      a description ending in a list would be stored as "<ul>…</ul><p></p>".
      Adding a toolbar button means adding its tag to the allowlist, in the
      same commit; the rule is written in both files.
      SANITIZED ON THREE WRITE PATHS, AND THE THIRD IS THE FINDING. The prompt
      named POST /api/hr/training and PATCH /api/hr/training/[id]. The audit
      found POST /api/hr/training/import (import/route.ts:98) writes
      module_description from a CSV cell straight to the column, never touching
      the editor or either named handler — so sanitizing only the two would
      have left "the database never stores unsanitized HTML" FALSE for every
      imported module, on the bulk path most likely to carry content pasted out
      of someone else's system. Gary approved closing it in the same commit
      (two lines). Duplicate needed nothing: it re-POSTs through the create
      route and inherits its sanitizer.
      THE PREDICATE IS THE HTML TOKENIZER'S RULE, AND THAT WAS A FIX FOUND BY
      TESTING RATHER THAN BY READING. looksLikeHtml started as a full
      tag-shaped match, which does not match "<svg/onload=alert(1)>" (the "/"
      is followed by a letter, not ">") — so that input was classified plain
      text and STORED WITHOUT SANITIZING. Nothing executed: the renderer
      branches on the same predicate and its plain-text branch is a React text
      node, which escapes the string. But the safety then rested on one regex
      agreeing with every present and future caller rather than on the stored
      string being harmless, and one future component that innerHTMLs a
      description without the branch would turn a storage quirk into a
      vulnerability. Replaced with the browser's own rule — a tag starts at "<"
      followed by an ASCII letter, or "</" — so "stored as plain text" now
      means "no browser can find a tag in here", which is the property the
      text-node branch is entitled to assume. COST, NAMED: a legacy
      description containing something like "the <name> field" now takes the
      sanitizer branch and loses that literal. Correct side to err on, since a
      browser reads it as a tag too, but it is a behavior change on existing
      rows.
      PLAIN TEXT BYPASSES THE SANITIZER, AND THAT IS CORRECTNESS, NOT
      OPTIMISATION. DOMPurify entity-encodes bare ampersands, so running a
      CSV-imported "Tom & Jerry" through it would store "Tom &amp; Jerry" — and
      the renderer, still finding no tag, would take the plain-text branch and
      show staff the entity literally. Tag-free input is therefore returned
      verbatim, which is safe precisely because the predicate above is now as
      permissive as the parser.
      ONE RENDERER, SO BOTH SURFACES WERE FIXED BY ONE EDIT — HR-17's
      no-second-renderer rule paying out. Verified by grep that
      TrainingModuleView:100 is the ONLY site that renders a module
      description; the admin list, the edit page and HR-24's library route
      carry it in payloads without displaying it. It sanitizes AGAIN on the way
      out, which is not theatre: rows written before today were never sanitized
      at all, and HR-24 serves this component to a shared store iPad.
      EMPTY NORMALISES TO NULL. Tiptap serialises an emptied editor as
      "<p></p>", a truthy string that would otherwise render a blank paragraph
      forever. The server tests TEXT (tags stripped, &nbsp; as whitespace), so
      "<p><br></p>" and "<ul><li></li></ul>" are both null while "&amp;" is
      not. The editor emits "" as well, but that half is courtesy — a client is
      never the gate.
      THREE NEW FILES, NOT THE TWO THE PROMPT LISTED, approved before
      implementation. sanitize-html.ts pulls jsdom via isomorphic-dompurify and
      the editor is a CLIENT component that needs the same predicate;
      importing the sanitizer from the browser to reach one regex risks
      dragging jsdom into the client bundle. The alternative was defining the
      rule twice, non-adjacently — the thing HR-25's SELF_FILES_SERVED_WHERE /
      selfFilesServed() pairing exists to avoid. So rich-text.ts holds the pure
      half with no dependencies at all.
      VERIFIED LOCALLY ONLY — NO STAGING, NO PRODUCTION, NO DATABASE. Two
      harnesses, both deleted before the commit. A tsx script over sixteen
      sanitizer inputs: script, img/onerror, javascript: anchor and svg/onload
      all stripped; Google Docs and Word paste reduced to allowlisted tags with
      every attribute gone; empty/junk/br-only all null; plain text verbatim
      including "Tom & Jerry, 5 < 10"; idempotent. And a browser pass on a
      temporary page under the public /menu route: bold-with-live-selection
      works (the onMouseDown preventDefault holds it), aria-pressed tracks
      state, list markers survive Tailwind preflight on both sides, legacy
      plain text loads readable AND does not fire onChange (so opening an old
      module without touching the description leaves it clean), clear emits "",
      hostile input renders as "<p>safe</p>", no hydration errors. ONE DEFECT
      FOUND THERE AND FIXED: Tiptap wraps list-item content in <p>, so real
      output is "<li><p>text</p></li>" and the renderer's [&_p]:mb-2 applied
      inside every bullet and spaced the list out like body copy. [&_li_p]:mb-0
      added to BOTH the editor and the renderer, kept identical so the builder
      is a true preview. Gary's four staging checks are the real gate and are
      unaltered.
      LESSON `info` HAS THE SAME PROBLEM AND IS DELIBERATELY UNTOUCHED — see
      the triage in the audit (§11 item 8). It is the BIGGER surface: info is
      the substance of a module ("what the trainee needs to read and learn"),
      it keeps line breaks today but cannot express a list, and procedures are
      lists. Every piece built here is reusable; the work is the lesson write
      paths in both handlers plus the CSV importer's lesson_info column, and
      the two inline lesson forms in training-form.tsx. It also renders on a
      shared store iPad via HR-24. Not filed as a row by this session.
      FOLLOW-UP "340f99b" (2026-08-12) — STAGING 500'd AT RUNTIME, AND THE
      BUILD WAS NEVER GOING TO CATCH IT. Every /api/hr/training request failed
      with "Failed to load external module jsdom". isomorphic-dompurify loads
      jsdom, whose dynamic requires the bundler cannot trace, so the traced
      function shipped without it. 340f99b added isomorphic-dompurify to
      serverExternalPackages in next.config.ts — the same treatment unpdf
      already carries there (HR-11b, DOMMatrix).
      CORRECTION, WRITTEN 2026-08-12 ALONGSIDE "e9a717c" — THE PARAGRAPH ABOVE
      CLAIMED 340f99b FIXED THIS. IT DID NOT. That claim was committed
      ("985507f") before any request had reached the new deployment, on the
      strength of a green build; the runtime logs for
      dpl_AGvSm4Gner36w4MLoxfFKaAsA9of then showed /api/hr/training still
      500ing. It is corrected here rather than rewritten above, because what
      the wrong version recorded — a fix asserted from a build — is the thing
      worth keeping visible.
      340f99b DID change the error, and that is how the real cause was found:
      "Failed to load external module isomorphic-dompurify: ERR_REQUIRE_ESM:
      require() of ES Module @exodus/bytes/encoding-lite.js from
      html-encoding-sniffer/lib/html-encoding-sniffer.js". The break is inside
      JSDOM'S OWN TREE — jsdom@30 depends on html-encoding-sniffer@6
      (CommonJS), which require()s @exodus/bytes@1 ("type": "module"). No Next
      config fixes a CJS/ESM boundary inside a dependency, and
      serverExternalPackages made it WORSE rather than better: being externally
      required is precisely what forces the runtime require() that throws.
      FIXED IN "e9a717c" BY REMOVING THE DOM, NOT BY CONFIGURING AROUND IT.
      isomorphic-dompurify is gone and so is jsdom; sanitize-html parses with
      htmlparser2 and needs no DOM at all. The serverExternalPackages entry was
      removed with the package — and sanitize-html must NEVER be added there,
      since bundled, the ESM in its tree resolves at build time and never
      reaches a runtime require(). Byte-equivalence was PROVEN BEFORE the swap,
      not asserted after: both implementations run side by side over the
      audit's cases plus real Tiptap output, 19/19 identical, with one
      difference found and normalized (sanitize-html emits "<br />" where
      DOMPurify and Tiptap emit "<br>"; do not reach for `selfClosing: []`
      instead, which emits "<br></br>" and parses as TWO breaks).
      THE LESSON IS ABOUT THE GATE, NOT THE LIBRARY, AND IT COST TWO COMMITS TO
      LEARN. This class of failure builds green locally, in CI and on Vercel,
      and appears only on the first request that reaches the code path. The
      audit predicted the hazard (§8) but prescribed the wrong remedy, and the
      wrong remedy was then reported as a fix on the strength of a build. For a
      server dependency with a native, dynamically-required or ESM-only
      backend: prefer a dependency that does not need the shim at all, verify
      by reading the emitted bundle (is the code INLINED, or is there an
      external-module reference?), and treat "green build" as saying nothing
      whatsoever about runtime module loading.
      Audit: docs/prompts/2026-08-12_HR-28_AUDIT.md — §8's fallback is the part
      this correction supersedes; the file itself is a claim wholesale and
      stays unedited.

  - id: HR-11d
    track: hr
    size: M
    commits: [576fc4e, 6ff7ea6]
    status: shipped
    title: "Hollow signed records — completion reads document checkpoints, stamping reads version anchors; guard, upload reporting, vocabulary, hash carry-forward"
    notes: >
      ── DEBT-72a BOARD-CURRENCY BACKFILL 2026-08-17 — STATUS in_progress -> shipped.
      Prepended per preserve-and-mark; NOTHING BELOW IS EDITED.

      EVIDENCE. Both recorded work commits "576fc4e" (2a-2e) and "6ff7ea6" (2f)
      return TRUE for `git merge-base --is-ancestor <sha> origin/main`. They first
      appear on main in the merge "7e77ea6" (2026-08-17 02:47:13 UTC); that
      promotion's three migrations applied to the PRODUCTION database at
      02:50:05-02:50:06 UTC. Measured against origin/main and origin/staging, never
      local refs.

      RESIDUAL WORK IS NOT CLOSED BY THIS FLIP, and the flip does not claim it is.
      `shipped` means "merged to main, live in prod" (this file's own header) — it
      is a statement about where the code is, not that the row is finished. PHASE 2
      IS STILL NOT DONE: the staging browser walk, and the §3 DOC-1 audience-modal
      non-regression check. Everything below about Phase 2 stands unchanged. This row
      is also NOT `verified` — no verification is recorded and that call is Gary's.
      Full measurement, method and the whole table:
      docs/prompts/DEBT-72a_BACKFILL.md. The rule this backfill accompanies is
      docs/WORKFLOW.md § Session completion rules item 4 — a row whose code was
      pushed to staging in a session has its status set in that session.
      Cross-refs: DEBT-72 (the promotion gate, still planned, still unbuilt),
      DEBT-72_AUDIT.md "b809e03" (which found this class), DEBT-72b (the gate
      itself, sequenced after this and deliberately not designed here).

      PHASE 0 CLOSED 2026-08-14. PHASE 1 BUILT 2026-08-14 ("576fc4e") — FIVE OF
      SIX ITEMS; 2f IS HELD, see below. Audit: docs/prompts/HR-11d_AUDIT.md —
      634 lines, and the diagnosis of record. Phase 0 audit preserved at
      "7fbf618".

      NO `commits:` FIELD UNTIL PHASE 1. The audit SHA above is prose
      deliberately: `commits:` means WORK commits everywhere else in this file,
      and seeding it with a docs commit would have quietly made it mean two
      things the moment Phase 1 appended to it. The field arrives with the
      first work commit — and now has, carrying the two Phase 1 WORK commits
      and nothing else: "576fc4e" (2a-2e) and "6ff7ea6" (2f). The audit SHA
      stays in prose above, where it has always been.

      HANDOFF POINTER — Phase 1 must be handed BOTH
      docs/prompts/HR-11d-phase1-build.md ("2212a1e") AND
      docs/prompts/HR-11d-phase1-build-addendum.md ("82713ea"). The original
      alone reproduces the R3(ii) omission recorded below.

      THIS IS NOT A REGRESSION, and the audit's opening premise was withdrawn
      mid-session. The audit began on a 7-24 artifact stamped correctly and an
      8-12 artifact stamped not at all. Gary's confirmation: "I have never
      formalized a document in production. The 7-24 record is a dev/staging
      observation. This is NOT a regression — inline stamping has never run in
      production." There is no 7-24 to 8-12 production delta to explain. Both
      artifacts are non-production observations of the same system in two
      configurations — one where an admin had confirmed the version's field
      anchors, one where nobody had.

      THE DATING CONFIRMS IT INDEPENDENTLY. Two proofs put the 7-24 artifact
      before 07:50 PDT on 7-24: its certificate labels the name row "Signer",
      predating the dual name rows introduced by "232d568" at 12:29 PDT; and
      its signature sub-lines stack DOWNWARD, predating the reader-up stacking
      for Above placements introduced by "01c5ed9" at 07:50 PDT. The
      HR-11b/HR-11c production merge "59a6cdc" landed at 21:07 PDT the same
      day. The artifact was produced by pre-HR-11c code roughly FOURTEEN HOURS
      before HR existed in production. A third proof drawn from the
      certificate's "Organization: Microsoft" line was drafted and WITHDRAWN —
      that field is a known-suspect open HR-11c verification item and cannot be
      used to identify an org (corrected by Gary, 2026-08-14).

      THE CODE DID NOT CHANGE. git diff "59a6cdc"..HEAD across the two stamping
      libraries: src/lib/hr-anchors.ts is byte-identical — detector, vocabulary,
      line reassembly, fill-gate and placement resolver all untouched.
      src/lib/hr-signed-pdf.ts carries two changes, neither on the
      acknowledgment path: "89c70f7", a let-to-const lint fix with no
      behaviour, and "4adcb13" (DEBT-9), inside ensureTrainingCertPdf — a
      different exported function that ensureSignedRecord does not call. The
      three surrounding HR document routes (versions, anchors, anchors/rescan)
      are byte-identical too. The diff is behaviourally empty on this path. The
      cause is configuration that was never established, not a change in code
      or data.

      THE STRUCTURAL CAUSE. ensureSignedRecord judges COMPLETION from
      DOCUMENT-LEVEL checkpoints and drives STAMPING from VERSION-LEVEL
      confirmed anchors. Two predicates, two sources, two lifetimes, never
      reconciled — so they never have to agree. When the confirmed-anchor set
      is empty the function mints a record claiming completion, draws the
      page-1 banner, appends the full Certificate of Acknowledgment, and stamps
      nothing — silently, with no signal at any point in the chain. The defect
      is therefore not "what broke"; it is what the system does when nobody has
      confirmed anchors. Observed on staging 2026-08-14: the Keva Employee
      Handbook shows "Detected fields (41 across 28 pages)" with a live
      "Confirm & generate (41)" button — 41 proposals, zero confirmed — while
      Checkpoints (37) are fully populated. Completion machinery intact,
      stamping machinery empty.

      RULINGS — RATIFIED BY GARY 2026-08-14, FILED VERBATIM.

      R1 — MOOT. No production signed records exist. Nothing to void, re-issue,
      or explain to Keva. Closing it here so nobody reopens it later assuming
      damage was done.

      R2 — RATIFIED. The upload route reports its scan result. Same rule I made
      on 7-23 for rescan: a scan states which of three things happened — it
      errored, there was no text layer, or it found N fields on M pages. Never
      a bare zero standing in for all three. The upload dialog ends by telling
      the operator what is left to do, not by declaring success.

      R3(i) — RATIFIED. Three layers: flag at upload, refuse to start the
      ceremony, throw in ensureSignedRecord. The backstop is non-negotiable.
      Refusing before the ceremony rather than at mint-time is the point — I am
      not failing a signer after they have typed 27 sets of initials.
      Signer-facing copy is exactly "This document isn't available yet — ask
      your manager," with a download and print option so nobody is stuck
      mid-shift.

      R3(i-a) — RATIFIED, and this CORRECTS the audit. The audit proposed
      firing when a prior version had confirmed anchors and the current one
      does not. That test only works on a document with history; a brand-new
      document whose first version was never confirmed sails through it, and
      new documents are most of what we will upload. The trip-wire is:
      detection matched fields and zero are confirmed. A version that matched
      nothing — image-only, or predating anchoring — is legitimately
      certificate-only and stays signable. This ruling is the only definition
      of R3(i-a) anywhere; it does not appear in the audit or the build prompt.

      R3(ii) — RATIFIED. The Certificate of Acknowledgment states whether marks
      were applied to the document body, and the field count when they were.
      Language stays neutral — no "failed," no "error" — because once R3(i)
      ships, a document with nothing to stamp is a valid document, not a broken
      one. Phase 1 proposes the final copy for both states and I approve it
      before it ships. I am not putting wording on an executed legal artifact
      that I have not read.

      2d — RATIFIED. "Manager Signature:" enters the vocabulary with a discard
      flag: it claims its span so bare "Signature:" cannot steal it, emits no
      candidate, writes no row, and never enters the matched count — so the
      R3(i-a) guard needs no carve-out. It is recognized and left alone, not
      stamped. Manager countersignature is its own row (HR-11h), not this one.
      "Employee:" defaults to SignatureStamp and is fill-gated; page 13 is a
      signature line, and if the default is ever wrong the admin unchecks it at
      confirm in thirty seconds, whereas the reverse error puts a typed name on
      a signature line with no signer act at all.

      2e — RATIFIED. When a new version's file hash matches the version it
      replaces, confirmed anchors carry forward as confirmed, and the upload
      warns that the file is identical. Nine versions of the handbook all carry
      hash 7d60912ccf4b — identical bytes cannot have different coordinates, so
      there is no admin judgment left to exercise. Confirmed anchors only;
      unconfirmed proposals do not carry forward. This moves stamp coordinates
      and nothing else — signed records stay bound to the version signed, and a
      new version still requires new acknowledgment. HR-11f is untouched.

      R3(i-a) CORRECTS THE AUDIT — THE TWO DO NOT AGREE AND MUST NOT BE READ AS
      AGREEING. The audit (§10.1) proposed the trip-wire "prior version had
      anchors and current has none". The ratified condition is "detection
      matched fields and zero are confirmed". The audit is a claim wholesale
      and is not edited; this row is the correction of record. A reader who
      opens the audit alone will find the superseded condition stated as the
      proposal, with nothing in that file marking it as overtaken.

      R3(ii) WAS OMITTED FROM THE PHASE 1 BUILD PROMPT IN ERROR, and is now IN
      SCOPE. It was ruled and then left out of
      docs/prompts/HR-11d-phase1-build.md — present in neither §2 (scope) nor
      §4 (out of scope). Carried instead by
      docs/prompts/HR-11d-phase1-build-addendum.md as §2f ("82713ea"). The
      omission is recorded rather than tidied away: the original prompt is
      unedited, and two of its lines are stale as a result — §2 is headed
      "Scope — five items, no more" (it is now six) and §3 lists "the
      certificate writer" among the things not to disturb (§2f changes it).
      Both stale lines fail in the direction of SKIPPING §2f, which is why the
      handoff pointer above names both files.

      COMMENT — HrAnchorMarkType is a Prisma enum (schema.prisma:1742), not a
      TS union — any future "mark type that stamps nothing" is a migration, not
      a vocabulary edit. The discard flag avoids this entirely. (Filed inside
      notes because `comment:` is not a field on the phase type — see the
      Phase interface in src/lib/roadmap.ts.)

      ──────────────────────────────────────────────────────────────────────

      PHASE 1 BUILT 2026-08-14 — ALL SIX ITEMS. 2a, 2b, 2c, 2d and 2e in
      "576fc4e"; 2f in "6ff7ea6" after Gary ruled the copy. NOT PUSHED.

      2f WAS HELD MID-SESSION AND THEN RULED, which is the shape the addendum
      asks for and not a delay. The copy was presented before anything was
      written into the certificate writer; Gary ruled Option A, FIELD COUNT ONLY
      WITH NO PAGE COUNT, and MARKS ACTUALLY DRAWN rather than anchors
      confirmed. The stamped line reads "Stamped - 41 field marks applied to the
      document body."; the certificate-only line reads "Certificate-only - no
      field marks were applied to the document body. This certificate is the
      complete record of acknowledgment."

      MARKS-DRAWN WAS THE SECOND DECISION HIDING INSIDE THE FIRST, and it is
      what makes the line worth having: the certificate describes THE ARTIFACT
      IN THE READER'S HANDS, not the intent behind it. Its one visible
      consequence is correct rather than incidental — a version whose anchors
      are all confirmed but whose values resolve to null (an all-attested
      initial set, a "Store:" with no store on file) draws nothing and
      truthfully says certificate-only. A signature block counts as ONE mark,
      not three lines.

      Position: a labeled() row under Source SHA-256, with the facts about the
      file and above the signer-identity block, same 8pt/9pt weight as every
      other row. The LABEL IS CONSTANT across both modes so two certificates
      line up when read side by side — that comparison is the entire reason the
      line exists, and a label that changed with the mode would hide the
      difference from anyone skimming a stack. Both modes are legitimate once 2b
      ships, so the fixture asserts neither string contains failure language
      ("failed", "error", "missing", "unable", "skipped", "could not",
      "problem", "invalid") and that the certificate-only line closes on what
      the certificate IS rather than trailing off after what is absent.

      2f is the ONE item on §3's do-not-disturb list that the addendum
      deliberately changes. Everything else on that line is untouched. It
      applies FORWARD ONLY — no certificate already generated is re-issued or
      amended, and R1 is moot because no production signed records exist.

      THE GUARD IS THE PREDICATE, AND IT LIVES IN ONE PLACE. isSigningBlocked
      (src/lib/hr-anchors.ts) is pure and exported, so the fixture asserts the
      shipped function rather than a copy of it, and the ceremony page, the mint
      and the fixture cannot drift. getVersionAnchorReadiness is the one query
      that feeds it.

      TWO PLACES THE PHASE WENT BEYOND THE PROMPT'S LETTER, BOTH FLAGGED FOR
      CHEAP REVERSAL. (1) R2's reporting was applied to POST /api/hr/documents
      as well as the versions route §2a names — that route is what creates the
      brand-new document R3(i-a) was ratified to catch, so reporting only on
      re-upload would leave the flag-at-upload layer silent for exactly that
      population. (2) The attested entry point gets an admin-facing refusal
      instead of the ruled signer copy, because "ask your manager" is addressed
      to a person who, on that path, is the manager. The ruled copy is verbatim
      and untouched on the signer path.

      LAYER (c)'S REFUSAL IS NOW ADMITTED RATHER THAN SWALLOWED. The capture
      route already caught every generator failure and returned complete:true
      with signedRecordId:null — so a bypassed layer (b) would have told the
      signer "executed" while no record existed, which is this row's own defect
      wearing a different coat. It now returns signingUnavailable and both
      clients render the refusal. This is NOT a fourth guard and deliberately
      not a refusal on the write: the ceremony saves progressively, so refusing
      at the write would strand a signer mid-document — the exact failure layer
      (b) is at the door to prevent.

      FIXTURE 55/55 AT "576fc4e", THEN 64/64 WITH 2f — up from 35/35 at the
      pre-phase HEAD. THE PROMPT'S "current 28/28" IS STALE AND CAME FROM
      docs/DECISIONS.md:1142, where it was the HR-11b-era count for
      scripts/verify-hr-anchors.ts; HR-11c's dedup work added seven more checks
      and neither number was revisited. Recorded because the gap reads like a
      regression to anyone comparing 28 against 35 — 35 check() calls are in the
      file at the pre-phase HEAD (git show HEAD:… | grep -c) and all 35 still
      pass. THE 2b REGRESSION ASSERTION NEEDED NEITHER A REAL PDF FIXTURE NOR A
      SECOND HARNESS, which was §5's question: the trip-wire is a pure function
      of two counts, so the synthetic PDFs already in the file supply the real
      `matched` and `confirmed` is 0 by construction (detection writes every
      proposal unconfirmed). Asserting that ensureSignedRecord itself throws
      needs a database, a blob store, a StaffMember and acknowledgment rows —
      a second harness, and it is the Phase 2 staging walk, which already
      requires an authenticated session. 2f's two assertions land the same way:
      the copy is exported pure (certificateModeLine) and asserted against the
      SHIPPED strings, while "a certificate generated for a version with
      confirmed anchors states the stamped mode" is that same Phase 2 walk.

      THE ROADMAP COMMIT'S MESSAGE WAS AMENDED; THE WORK COMMIT WAS NOT. The
      first attempt at the recording commit wrote `commits:` in backticks inside
      a double-quoted zsh -m argument, which command-substituted and swallowed
      the word; the message was amended to restore it. "576fc4e" was never
      touched, so the SHA this row records is still the SHA that was committed —
      the only property the two-commit pattern exists to protect. Amending the
      RECORDER is outside that hazard; amending the RECORDED is the thing the
      pattern forbids. Written down because the history now shows an amend on an
      HR-11d commit, and the next reader should not have to work out which one.
      The reusable half: heredoc every commit message with quoted EOF — a
      backtick in -m is a live command substitution, and it fails SILENTLY, with
      a message that is merely missing a word rather than obviously broken.

      NOT VERIFIED BY THIS SESSION, AND BOTH BELONG TO PHASE 2. The
      non-regression check §3 asks for — that the DOC-1 audience modal still
      saves — needs an authenticated ADMIN session this session may not create.
      What IS evidence: assign-audience-dialog.tsx, api/hr/documents/[id]/audience,
      hr-document-audience.ts and hr-documents-access.ts are BYTE-UNTOUCHED by
      the work commit, and the guard is at the ceremony page and the mint, never
      on a grant path. And no browser observation was taken at all, per the
      staging-SHA precondition: this work is not on staging at commit time.

      SCHEMA: NONE. MIGRATION: NONE. No `shipped:` date yet; that lands at
      promotion.
    open:
      - >
        The anchor rows behind the 8-12 artifact were never read. Close in
        Phase 2 with Q2/Q3a/Q3b/Q4a from the audit's query pack re-pointed at
        staging, branch literal corrected off br-sparkling-block.
      - >
        No production document has ever had its anchors confirmed, so once the
        guard ships Keva cannot sign until someone walks the confirm screen on
        the production handbook — org ID and Clerk instance recorded before the
        click.
      - >
        anchors/route.ts deletes a keep:false anchor with no guard on
        confirmed — hypothesis B representable. File its own row.
      - >
        THE ERRORED-SCAN GAP IS HR-11i, RULED OUT OF THIS ROW (Gary,
        2026-08-14): it needs a column and a migration, and HR-11d is not
        growing one at the end. §2a's loud failure at upload holds the line
        meanwhile. Nothing about it is built here.
      - >
        The §3 non-regression check — that the DOC-1 audience modal still saves
        — was not observed. It needs an authenticated ADMIN session. The
        audience files are byte-untouched by "576fc4e" and the guard is nowhere
        near a grant path, but that is an argument, not the observation the
        prompt asked for. Fold it into the Phase 2 browser pass.

  - id: HR-11j
    track: hr
    size: L
    status: verified
    title: "Signed truth & assignability — R1 completion from the record, R4 assignability gate"
    commits:
      - "32205a4"
      - "2afcf0f"
    notes: >
      SESSION 2026-08-15. Prompt: docs/prompts/2026-08-15-signed-truth-and-assignability.md
      ("887ee14"). Item 1 audit: docs/prompts/2026-08-15_COMPLETION_INVENTORY_AUDIT.md
      (same commit). Reproduction fixtures: docs/test_docs/ ("72e4adc").


      FOUR RULINGS AND TWO SUPERSESSIONS, ALL MADE 2026-08-15 AFTER A FULL
      REPRODUCTION ON STAGING (br-square-feather-a63z92vz / neondb, document
      "Test - Handbook Regression" cmstv3r1s000004jxdcyhkbui). They are recorded
      here as RULINGS — what is true from now on — not as a summary of what was
      changed. The changes are in the commits.


      R1 — SIGNED MEANS THE SIGNED RECORD EXISTS. A document is signed when an
      HrSignedRecord row exists for that version, that staff member, and that
      signing cycle. Not before. Completed checkpoints are PROGRESS, not
      COMPLETION. Every surface that reports completion status reads the record.
      Checkpoint counts may still drive a progress display ("2 of 7
      checkpoints"); they may never produce a "Signed" or "Completed" state. A
      signer holding acknowledgments and no record is IN PROGRESS, not done, and
      the document stays in their TO SIGN list.


      R2 — TWO UPLOAD PATHS, AND THEY MEAN DIFFERENT THINGS. A NEW FILE is a new
      document: everyone signs it. A NEW VERSION is the same document: existing
      signers KEEP their record and are NOT re-prompted, and a read-only notice
      tells them an update is available. FILED, NOT BUILT IN THIS SESSION —
      it needs its own design pass and will likely add a column to
      HrDocumentVersion.


      R2 SUPERSEDES HR-11f (2026-08-14), WHICH REQUIRED UNIVERSAL
      RE-ACKNOWLEDGMENT. Marked here rather than edited there; the HR-11f text
      stands as written and is preserved beneath this marker:


      [SUPERSEDED 2026-08-15 BY R2] HR-11f, ruled 2026-08-14 — a new version of
      a document requires new acknowledgment from everyone; signed records stay
      bound to the version signed, and a version bump re-prompts every signer.
      That is now the NEW FILE path only. The NEW VERSION path no longer
      re-prompts an existing signer.


      R3 — COMPLIANCE SHOWS SIGNED, WITH THE VERSION NOTED. No warning, no flag,
      no amber state for a signature executed against an older version. A
      separate report — "who has acknowledged the current text" — is FILED FOR
      LATER and is the right home for that question. Not built in this session.


      R4 — A DOCUMENT IS NOT ASSIGNABLE UNTIL ITS FIELDS ARE CONFIRMED. The
      guard moves UPSTREAM, from the ceremony to the grant. A document whose
      current version has detected fields and none confirmed cannot be granted
      to anyone.


      R4 SUPERSEDES THE HR-11d §2b CARVE-OUT (2026-08-14), WHICH DELIBERATELY
      LEFT THE GRANT/AUDIENCE PATH UNTOUCHED. The carve-out's own words are
      preserved beneath this marker, and are corrected at their code site the
      same way:


      [SUPERSEDED 2026-08-15 BY R4] "THE GUARD IS AT THE CEREMONY, NEVER AT THE
      GRANT. Assigning an unconfirmed document to the whole company still works
      exactly as before (DOC-1 audience modal, untouched by this phase) — what
      an admin cannot do is have someone sign it."


      Q1 — IMAGE-ONLY PDFs WARN, THEY NEVER BLOCK. isSigningBlocked is
      `matched > 0 && confirmed == 0`, so a PDF with no text layer produces
      matched 0 and is not blocked. That stands. matched == 0 means fields were
      not DETECTED, which is a WARNING condition, not a block condition.
      BLOCKING WOULD CREATE A DEAD END WITH NO ACTION THAT CLEARS IT — there
      would be no fields to confirm — and would silently overturn R3(i-a),
      ratified the day before. One predicate, two outcomes.


      Q2 — ADMIN-FACING COPY DIFFERS FROM SIGNER-FACING COPY, ON EVERY SURFACE
      AND IN THE SIGNED-RECORD ROUTE. "Ask your manager" is nonsense read by the
      manager who is standing there. The three ruled strings, verbatim:
      employee "In progress — ask your manager."; admin "Not signed — ceremony
      incomplete, no record"; assignment refusal "Confirm this document's fields
      before assigning it." The signed-record route's 409 branches on whether
      the caller is acting on their own record or someone else's.


      INVARIANT — THE GRANT GATE BLOCKS GRANTING, NEVER REVOKING. No readiness
      state may trap a document in its current audience. Withdrawing a document
      is exactly what an admin needs when a version goes unconfirmed underneath
      a live grant, and blocking the grant while forbidding the retreat would be
      worse than not blocking at all — the same dead-end shape Q1 rejects.


      ──────────────────────────────────────────────────────────────────────
      ITEM 2 BUILT 2026-08-15 ("32205a4") — R1 ACROSS ALL SIX SURFACES.


      THE DEFECT WAS SIX EXPRESSIONS OF ONE PREDICATE. Six separately written
      derivations of "this document is signed" — hr-compliance.ts, the /my
      documents data layer, /staff/[id], the capture route, and both ceremony
      clients — had to agree by discipline and were not required to agree by
      construction. All six treated a full required-checkpoint set as
      completion, so all six were wrong at the same time; the reproduction
      exercised only two of them. The predicate now lives once, in
      src/lib/hr-completion.ts, as an exported PURE function on the
      isSigningBlocked pattern. It imports nothing, which is load-bearing rather
      than tidy: two of the six sites are CLIENT components, and a predicate
      they cannot import is a predicate they will reimplement.


      THE CAPTURE ROUTE WAS THE ORIGIN. POST .../acknowledgments computed
      `complete` from checkpoints BEFORE attempting the mint and never revised
      it, so the five display surfaces were faithfully reporting what the API
      told them. `complete` is now the mint result. THIS CLOSED A FAILURE
      signingUnavailable NEVER COVERED: that flag narrows one cause, and every
      other generator failure returned complete:true with signedRecordId:null —
      the same lie with a different cause, undetectable by any client.


      PRECEDENCE IS THE PRE-R1 CHAIN ARM FOR ARM, and the first draft got this
      wrong. Deleting the allAcked arm lets that case fall through to
      needs-current whenever the member also holds an older-version record, so a
      row could be BOTH "needs current version" AND record-missing and each
      surface would invent its own rule for which to show. The arm is KEPT and
      only its CLAIM changed. recordMissing therefore always implies
      in-progress and no caller has to choose.


      EXPECT COMPLIANCE PERCENTAGES TO FALL, AND EXPECT IT TO BE FILED AS A
      REGRESSION. Anyone whose checkpoints are all in but whose record was never
      minted moves from complete to in-progress, and their store's pct drops
      with them. Written into hr-compliance.ts in advance, the same as the R3
      corporate-exclusion and archive notes DOC-1 C left there.


      VERIFIED: next build green, eslint clean over 13 files, 23/23 assertions
      against the SHIPPED predicate imported from src/ — the exact reproduction
      shape, the R1 positive, the ambiguous v1-signed/v2-acked case, the vacuous
      zero-required-checkpoint guard, both copy strings verbatim, and a
      96-combination sweep. NO SCHEMA CHANGE, NO MIGRATION.


      ──────────────────────────────────────────────────────────────────────
      ITEMS 3 AND 4 BUILT 2026-08-15 ("2afcf0f") — R4's GATE, THE THIRD
      CEREMONY ENTRY POINT, AND Q2 IN THE SIGNED-RECORD ROUTE.


      THE R4 GATE IS ON THE WRITE. PUT /api/hr/documents/[id]/audience asks
      getVersionAnchorReadiness on the current version and returns 409, after
      the org-scoped lookup so an unknown id still 404s rather than leaking a
      readiness answer, and before BOTH write branches. ONE WRITER, ESTABLISHED
      BY SWEEP: HrDocument.appliesTo and HrDocumentGrant have exactly one writer
      in src/ — every other appliesTo hit is TrainingModule or ChecklistTemplate.
      The dialog's disabled Save is an AFFORDANCE, not the gate, and asks the
      same exported audienceWouldGrant() rather than restating it: two LAYERS
      are intended, two RULES are not, and a mirror that drifts by one condition
      either disables Save on a withdrawal or offers it on a refusal.


      ITEM 4 — LAYER (b) WAS NOT BYPASSED. IT WAS NEVER PRESENT ON THAT ROUTE.
      Three ceremony entry points exist: /hr/acknowledge/[documentId] in self
      mode, the same route in attested mode, and /my/documents/[documentId] in
      the staff portal. HR-11d built the guard on the first file and its comment
      claimed it "covers BOTH entry points" — TRUE OF THE TWO MODES OF THAT
      FILE, FALSE OF THE THIRD ROUTE, which imports SigningClient across the
      (my)/(app) route-group boundary. That is the route the reproduction's
      signer walked in through. All three now call the predicate; the stale
      comment is corrected at its own site under a dated marker with the
      original preserved.


      Q2 ONE LAYER DOWN. POST .../signed-record returned the SIGNER copy to
      every caller, and the only surface rendering it is the admin-facing
      Generate-record button — so "ask your manager" was told to the manager,
      with no remedy named and no link to the screen that clears it. It now
      branches on actingForSelf, named at the permission check that already
      establishes it, and the admin path carries confirmHref — the field name
      SigningUnavailable already uses, rather than a second affordance.


      Q1's WARNING HALF ALREADY SHIPPED AND WAS RE-MEASURED RATHER THAN REBUILT.
      hrScanMessage covers noTextLayer and noFieldsMatched on both upload routes
      (HR-11d 2a), and AnchorsCard renders a persistent "No fields detected yet"
      panel on the document. A third would have duplicated shipped copy.


      VERIFIED: next build green, eslint clean over 7 files, 17/17 assertions
      against the SHIPPED functions — the reproduction refused for both
      all-company and per-store, the WITHDRAWAL permitted at every readiness
      state, image-only permitted per Q1, and a before/after pair proving that
      confirming anchors makes the document assignable. NO SCHEMA CHANGE, NO
      MIGRATION.


      ──────────────────────────────────────────────────────────────────────
      NOTE 1 — THE V1 HASH IN THE SESSION PROMPT IDENTIFIES A BLOB, NOT THE
      LOCAL FILE. The prompt cites 1539cd866058 for v1. That hash came off
      TOMMY'S CERTIFICATE and identifies the v1 in the STAGING BLOB STORE. The
      file committed at docs/test_docs/V1_Test_Employee_Handbook.pdf ("72e4adc")
      measures bec90ef31b1b — a different file. fileHash is plain sha256 of the
      bytes (lib/hr-files.ts), so the two are directly comparable and genuinely
      differ. RECORDED SO A LATER READER DOES NOT REPEAT THE ERROR: anyone
      verifying the R1 positive against the local V1 would be checking the wrong
      bytes and would get a fileHash that matches no version row. Verify against
      the DB row's fileHash, not the committed file. V2 MATCHES (1685fc241b1c)
      and is unaffected — it is the unconfirmed version both R1 and R4 turn on.


      NOTE 2 — YOU CANNOT GREP FOR A MISSING CALL SITE. Grepping isSigningBlocked
      returned a CLEAN result — one definition, correct callers, nothing out of
      place — because the route missing the call had nothing to match on. Note
      the direction: the search produces a PASS, so the session stops looking.
      What found it was enumerating every RENDERER of the shared ceremony client
      and checking each renderer. SAME SHAPE AS THE DEBT-22 MISS, which swept for
      unordered storeAssignments loads and missed a STORE load keyed by
      assignment ids — second time this shape has cost a session, which is why
      it is now a rule rather than a note: CLAUDE.md § Verifying a guard covers
      every path, added in "2afcf0f".


      ──────────────────────────────────────────────────────────────────────
      NOT DONE, AND IT IS THE WHOLE REMAINING RISK ON THIS ROW. EVERYTHING
      ABOVE IS UNIT ASSERTIONS AND A GREEN BUILD. THE 2026-08-14/15
      REPRODUCTION HAS NOT BEEN RE-RUN AGAINST THE FIXED CODE. Predicate tests
      passing is not the same as the bug being gone (Gary, 2026-08-15). Status
      stays in_progress until the acceptance pass lands.


      ACCEPTANCE — run after "2afcf0f" reaches staging, and re-run the
      staging-SHA precondition first or the entire pass is void. PRE-CHECK: the
      test document cmstv3r1s000004jxdcyhkbui must still be on v2 with anchors
      UNCONFIRMED, and Gdogg must still hold his 7 acknowledgment rows and no
      HrSignedRecord — if anyone has confirmed v2's anchors since 8-15 the R1
      and R4 tests both evaporate and the state has to be rebuilt. (1) R1
      REGRESSION — Gdogg's /my/documents shows the document under TO SIGN
      reading "In progress — ask your manager.", not under Completed as "Signed
      v2"; confirmed BY QUERY, signed-record count 0 for (v2, Gdogg, current
      cycle) while the surface reports in progress. (2) R1 POSITIVE — Tommy's v1
      record still reads signed (against the DB row's fileHash, per NOTE 1).
      (3) R4 — the assign dialog refuses with "Confirm this document's fields
      before assigning it."; confirming v2's anchors makes it assignable; and
      WITHDRAWAL was available throughout. (4) ITEM 4 — opening
      /my/documents/cmstv3r1s000004jxdcyhkbui on unconfirmed v2 lands on the
      refusal screen, not the reader. Every DB result carries neon.branch_id and
      current_database() in the same output; every browser observation names the
      org ID and the Clerk instance.


      ──────────────────────────────────────────────────────────────────────
      PUSHED AND DEPLOYED 2026-08-15 09:45 PDT. Gary pushed; the
      staging-SHA precondition was then confirmed independently rather than
      transcribed. Local HEAD and origin/staging both
      717f37bb432778c22c5671edb486a4af1f3ec88b; the alias
      froot-git-staging-indianathomas-2483s-projects.vercel.app serves
      dpl_8dqx2ay7Hx3rWAFxqkXgSvTTmUJj (froot-gthvkp3w8, created 09:45:13 PDT),
      and `vercel ls --meta githubCommitSha=<full 40-char>` returns THAT SAME
      DEPLOYMENT. Matched by deployment id, not by URL similarity, and with the
      full sha — the short form returns "No deployments found", which is
      indistinguishable from a genuine mismatch.


      ACCEPTANCE PRE-CHECK, MEASURED 2026-08-15 ON preview/staging
      (branch_id br-square-feather-a63z92vz, database neondb, both selected in
      the same query as the rows). Read-only SELECTs run by Gary in the Neon
      console — CLAUDE.md § Environment Variables forbids pulling deployed
      credentials, and read-only is explicitly not an exception. Document
      cmstv3r1s000004jxdcyhkbui:


      v1 — isCurrent false, anchors 10 confirmed / 0 unconfirmed, 7
      acknowledgments, 1 signed record. v2 — isCurrent TRUE, anchors 0 confirmed
      / 10 UNCONFIRMED, 7 acknowledgments, 0 signed records. Exactly one
      isCurrent row. Checkpoints: 7 required of 7 total. Per-staff on v2: one
      row only — Gdogg Thomas cmsugvzfn000104l1fl8jcgi3, signingCycle 1, 7 acks,
      0 records; so v1's 7 acks and its 1 record are Tommy's.


      WHAT THE PRE-CHECK ESTABLISHES. isSigningBlocked(10, 0) is TRUE, so R1 and
      R4 are both live and neither test has evaporated. v1's TEN CONFIRMED
      ANCHORS independently corroborate the session prompt's "stamped correctly,
      10 field marks". And 7 required checkpoints against Gdogg's 7 acks makes
      allRequiredAcked TRUE BY MEASUREMENT rather than by inference from the old
      defect having fired — that number was the one input not in the original
      pull, and it is the difference between the ruled copy and an ordinary
      "In progress 7/8" badge.


      PREDICTED SURFACE OUTPUT, computed by feeding those measured numbers
      through the SHIPPED functions. Gdogg: status in-progress, isSigned false,
      recordMissing TRUE — "In progress — ask your manager." on /my, "Not signed
      — ceremony incomplete, no record" on the admin surfaces, Generate record
      offered and the ceremony link withheld. THAT RESULT HOLDS WHETHER OR NOT
      v1'S RECORD IS HIS, which the measured set does not say and does not need
      to: recordMissing implies in-progress, so no surface has to choose between
      two labels. That is the Item 2 arm-ordering decision paying for itself
      against real data. R4: assign-to-all refused, assign-to-store refused,
      WITHDRAWAL PERMITTED.


      TOMMY WILL READ "NEW VERSION TO SIGN", AND THAT IS CORRECT — FLAGGED IN
      ADVANCE SO IT IS NOT FILED AS A REGRESSION. v2 is current and he has no
      acknowledgments on it, so the DOCUMENT ROW is needs-current. His V1 RECORD
      IS INTACT and still renders as signed, in the Signed records section of
      his portal and as a downloadable "Signed record for v1" on /staff/[id].
      Acceptance check (2) is about the RECORD surviving, not about a green
      badge on the document row. This behaviour predates R1 and no arm of the
      chain touching it was changed.


      STILL OUTSTANDING: the four acceptance checks themselves. The pre-check
      establishes only that they are RUNNABLE. Status stays in_progress.


      ──────────────────────────────────────────────────────────────────────
      ACCEPTANCE PASSED 2026-08-15. All four checks verified by Gary on
      staging, deployment dpl_8dqx2ay7Hx3rWAFxqkXgSvTTmUJj (created 09:45 PDT),
      matched by HEAD SHA. Org org_3G02wO4QlVVSWppi8aqlnSZnsDa on Clerk instance
      verified-snapper-7; database branch br-square-feather-a63z92vz / neondb.
      Status moves in_progress -> verified.


      (1) R1 REGRESSION — PASS. Gdogg's /my/documents shows the document under
      TO SIGN reading "In progress — ask your manager." THE DAY BEFORE, THE SAME
      SCREEN SHOWED IT UNDER COMPLETED AS "Signed v2" IN GREEN. That before/after
      on one screen is the closest thing this row has to a direct measurement of
      the defect, and it is why the check was specified as a comparison rather
      than as a lone screenshot.


      (2) A3 GUARD — PASS. Opening the document lands on the refusal screen, not
      the reader. This is the entry point that had no readiness call at all, and
      the one the original signer walked in through.


      (3) R4 GATE — PASS. The assign dialog shows the amber panel with "Confirm
      this document's fields before assigning it.", states that 10 fields are
      detected and none confirmed, links to the confirm screen, and disables
      Save. WITHDRAWAL CONFIRMED AVAILABLE — the invariant held in the browser,
      not only in the unit sweep.


      (4) R1 POSITIVE — PASS. Tommy's v1 signed record is intact and
      downloadable. Nothing in R1 reached backwards into a record already made.


      EVERY PREDICTION IN THE PRE-CHECK ABOVE WAS BORNE OUT, including the one
      flagged in advance as correct-but-surprising: Tommy's row reads "Needs
      current version". See the finding filed on HR-11k — that behaviour is
      right under HR-11f and WRONG under R2, which supersedes it.

  - id: HR-11k
    track: hr
    size: M
    status: shipped
    title: "R2 — two upload paths: new file re-signs, new version does not"
    commits:
      - "457a57f"
      - "6b2054f"
      - "bf7cd28"
      - "4363e3b"
    notes: >
      ── DEBT-72a BOARD-CURRENCY BACKFILL 2026-08-17 — STATUS in_progress -> shipped.
      Prepended per preserve-and-mark; NOTHING BELOW IS EDITED.

      EVIDENCE. All four recorded work commits — "457a57f" (Phase A), "6b2054f"
      (display follow-ups), "bf7cd28" (Phase B / Case A) and "4363e3b" (the Case A
      toggle fix) — return TRUE for `git merge-base --is-ancestor <sha> origin/main`.
      All four first appear on main in the merge "7e77ea6" (2026-08-17 02:47:13 UTC).

      RESIDUAL WORK IS NOT CLOSED BY THIS FLIP. `shipped` states where the code is,
      not that the row is finished. TWO THINGS BELOW STAND UNCHANGED: the 409 is
      STILL OWED (third session), and Phase B / Case A was never observed on staging
      — the code reached production without the staging pass its own text asks for.
      NOT `verified`, deliberately: no verification is recorded and that call is
      Gary's.
      Full measurement, method and the whole table:
      docs/prompts/DEBT-72a_BACKFILL.md. The rule this backfill accompanies is
      docs/WORKFLOW.md § Session completion rules item 4 — a row whose code was
      pushed to staging in a session has its status set in that session.
      Cross-refs: DEBT-72 (the promotion gate, still planned, still unbuilt),
      DEBT-72_AUDIT.md "b809e03" (which found this class), DEBT-72b (the gate
      itself, sequenced after this and deliberately not designed here).

      CASE A VERIFICATION PASS 2026-08-16 ("4363e3b"), TIER 2. Two items were
      asked for. THE FIRST COULD NOT BE REPRODUCED; the second is STILL OWED.
      Two different defects were found on the way and fixed.


      ── (1) THE REPORTED CHECKBOX BUG DID NOT REPRODUCE ──────────────────────
      Reported: on /hr/documents/[id], v7's "Requires everyone to sign again"
      renders UNTICKED while Neon says requiresReacknowledgment = true. Checked
      at every layer named in the brief, and every layer is correct:


      SERVER QUERY — the page's exact query, run against dev
      (br-broad-wave-a6vpjdw0/neondb), returns requiresReacknowledgment=true.
      MAPPING — the page's exact map produces VersionRow with reack=true.
      CLIENT BINDING — the REAL DocumentDetailClient, rendered through a
      temporary public /menu probe page (deleted before commit) so no admin
      session was needed, on a fixture built to mirror v7 exactly (current
      version, flag true, ZERO acknowledgments): the box renders
      aria-checked="true", data-state="checked", enabled, orange with a white
      tick, computed opacity 1. Screenshot taken. A second fixture matching the
      FROZEN case (flag true, 4 acknowledgments) renders checked and disabled.


      DEPLOYMENT WAS NOT THE EXPLANATION EITHER, which is the usual one and was
      checked because of it: the staging alias serves
      dpl_4KPgS2HjS9vE4U88PiAK8jwtegBV, built from a4a87d2, which contains
      bf7cd28. The feature was deployed when the observation was made.


      SO THE OBSERVATION IS UNEXPLAINED AND THE ROW SAYS SO rather than closing
      it. Two candidates worth checking before anyone hunts further, both cheap:
      (a) THE UPLOAD DIALOG'S checkbox, which lives in the versions card header
      (ReuploadButton is rendered there) and ALWAYS defaults unticked by design —
      if that is the box that was read, there is no defect at all; (b) the
      round-trip window in (2) below, which is the only state in this code where
      the box genuinely disagrees with the database. What would settle it: which
      row the box was on, and whether a reload changed it.


      ── (2) TWO REAL DEFECTS FOUND AND FIXED ────────────────────────────────
      THE BOX DID NOT MOVE WHEN CLICKED. It was fully server-controlled, so it
      only changed once the RSC refresh landed — correct on load, correct after,
      and wrong for the entire round trip between. An admin clicks, sees
      nothing, concludes the setting did not take. This is the closest thing in
      the code to the reported symptom. Now optimistic, seeded from the prop and
      re-synced to it IN RENDER (react-hooks/set-state-in-effect refuses the
      effect form, correctly), snapping back on any refusal.


      A LOST SESSION READ AS A SUCCESSFUL SAVE — and the unauthenticated probe
      built for item (1) found it by accident, which is the argument for
      building such probes. With an expired Clerk session the proxy answers this
      PATCH with a 307 to /sign-in; a DEFAULT FETCH FOLLOWS THE REDIRECT, lands
      on the sign-in page, and returns 200 with an HTML body. res.ok is
      therefore TRUE: the save is reported as having worked and the box sits
      there over a database that was never written. Fixed with redirect:
      "manual" — a redirect is never a successful write. SAME DEFECT CLASS AS
      THE FALSE-ASSERTION COPY this row keeps producing, the screen stating the
      opposite of what the app did, arriving through the error path instead of
      through a string. Worth checking wherever else a fetch writes.


      ── (3) THE 409 IS STILL OWED. THIRD SESSION. ───────────────────────────
      Step 6 was skipped in the Phase B session and could not be run in this one
      either, and the reason is structural rather than an oversight: THE ROUTE IS
      SESSION-PROTECTED AND CLAUDE CANNOT CREATE A SESSION. An unauthenticated
      PATCH to /api/hr/documents/[id]/versions/[versionId] returns 307 to
      /sign-in — proof the route is wired and the guard order is right, and proof
      the 409 branch is unreachable without a signed-in admin. Entering
      credentials is not something Claude does. The freeze code is 6 lines and
      re-reads the acknowledgment count inside the write, but IT HAS NEVER BEEN
      EXERCISED, and this row should keep saying so until it has been. Either
      Gary runs the two console commands in the session report, or the project
      decides on a test path for authenticated routes — that second option is
      the one that stops this recurring.


      ── (4) DEBT SWEEP OF THE SIGNING SURFACES, requested, nothing changed ───
      Nothing new found beyond what is already filed. The stale-copy set is
      still the six needs-current surfaces enumerated on this row (awaiting
      Gary's ruling) plus the hr/compliance tile subtitle "Signed an older
      document version", which is FALSE FOR BOTH REHIRE CASES and predates R2.
      Version numbers rendered elsewhere — the ceremony pages, the signed-record
      pages, the form surfaces — all name the version actually being signed or
      the record's own version, and are correct. No other surface was found
      rendering a stored value it does not read.


      PHASE B / CASE A BUILT 2026-08-16 ("bf7cd28"), TIER 2, NOT VERIFIED ON
      STAGING. The per-version re-verification toggle: an admin marks one upload
      as requiring everyone to sign again, and R2 stops applying to that version
      only. THE PHASE'S ONLY SCHEMA CHANGE — migration
      20260816120000_hr_document_version_requires_reacknowledgment, one ADD
      COLUMN, `requiresReacknowledgment` Boolean @default(false) on
      HrDocumentVersion. Purely additive: on dev (br-broad-wave-a6vpjdw0/neondb)
      all 4 existing version rows defaulted false, and HrSignedRecord (1) and
      HrDocumentAcknowledgment (4) were unchanged across it.


      THE DESIGN DOC WAS WRONG AND GARY CORRECTED IT BEFORE ANY CODE WAS
      WRITTEN. DESIGN §10 says that when the flag is set, row 4 is "SKIPPED".
      Implementing that literally is a defect: skipping the arm falls through to
      the acknowledgment arms, so a member who signed v5 and has touched nothing
      on v6 reads NOT-STARTED — the surface telling an admin they had never
      signed anything. THE RECORD DOES NOT STOP EXISTING BECAUSE A RE-SIGNATURE
      WAS DEMANDED. The arm RETURNS needs-current itself, which keeps
      signedVersionNumber populated and lets the row name both facts. Recorded
      here because the design doc is preserved unedited and a future reader will
      meet its wording before this correction.


      THE FLAG IS READ OFF THE VERSION IN FORCE, NEVER OFF THE VERSION SIGNED.
      It is a property of the demand being made now, not of the paper somebody
      signed months ago. And it NEVER REACHES ARM 1 OR ARM 2 (Gary, ruled): a
      member holding a current-version record, or one who has fully acknowledged
      the current version, is untouched by it — they did the newer work on the
      very version demanding it.


      A FIXTURE CAUGHT A REAL DEFECT MID-BUILD, and it is worth the sentence
      because the defect was invisible to the build and to the eye.
      `reacknowledgmentRequired` was first derived from its two inputs alone, so
      it read TRUE whenever the flag was set and an older record existed —
      INCLUDING when arm 1 or arm 2 had already won, which is exactly when Case
      A does not apply. It would have suppressed the version subtitle on rows
      that still needed it. It is now gated on the status that actually
      resulted, derived like its sibling `signedOnEarlierVersion`. A FLAG THAT
      OUTLIVES ITS OWN BRANCH is how a surface renders one state's reason under
      another state's name — the same family as the two-facts-in-one-field
      defect this whole row exists to fix.


      THE DIALOG'S COPY AND COLOUR ARE BOTH CONDITIONAL, ruled by Gary and not a
      nicety. THIS EXACT PARAGRAPH HAS NOW BEEN WRONG TWICE: it asserted the
      HR-11f rule until "6b2054f", and a STATIC version of it under a checkbox
      would be wrong again the instant the box is ticked — telling an admin
      nobody will be re-prompted while they are in the act of re-prompting
      everybody. Muted unticked (nothing is going wrong); amber ticked (people
      who were told they were finished will be asked again). Static text beside
      a control that changes what the text describes is the defect this codebase
      has produced three times.


      THE FREEZE IS ENFORCED SERVER-SIDE, ON A NEW ROUTE. PATCH
      /api/hr/documents/[id]/versions/[versionId] — the one mutable field on an
      otherwise immutable snapshot. Editable while the version has ZERO
      acknowledgments, frozen after the first, REFUSED IN BOTH DIRECTIONS:
      unticking cannot un-collect a signature already given, and ticking late
      demands one from people already told they were finished. The count is
      re-read inside the same request that writes, so what the UI believed when
      it rendered cannot decide what the server allows now — A DISABLED CHECKBOX
      IS NOT ENFORCEMENT (Gary). 409 rather than 403: the caller is permitted,
      the version's state refuses. The control on /hr/documents/[id] surfaces
      that 409 rather than swallowing it.


      /my/documents NEEDED NO CODE. needs-current is already bucketed into TO
      SIGN and already wrapped in the ceremony link, so the move out of COMPLETED
      and the replacement of the read-only notice with a real signing affordance
      are the existing machinery working — the cheapest available proof that
      Case A landed in the predicate rather than in the labels, the same test
      /my/page.tsx served for Phase A. The ceremony itself is untouched: a
      re-verification signer walks the path a new signer walks.


      THE `needs-current` NAMING PROBLEM IS NOW THREE MEANINGS AND IS GARY'S TO
      RULE — REPORTED, NOTHING REWORDED. After Case A the status covers: (a) a
      REHIRE, who may be facing the very same version they already signed; (b)
      RE-VERIFICATION DEMANDED, where the version changed and an admin insisted;
      (c) NEVER SIGNED THE CURRENT VERSION at all. Six surfaces render it and
      most say something false for at least one case:
      staff-documents.tsx:73 "Needs current version" (now conditional — Case A
      gets "Needs v6 · signed v5", the two others unchanged);
      staff-compliance.tsx:19 badge "Needs re-sign" and :75 detail "Signed v5 —
      v6 is now current"; my/page.tsx:182 "New version to sign";
      my/documents/page.tsx:27 "New version to sign"; and the org rollup tile
      hr/compliance/page.tsx:71 "Needs Re-sign" with the subtitle "Signed an
      older document version" — WHICH IS FALSE FOR BOTH REHIRE CASES, since a
      rehire's record can sit on the CURRENT version. The employee strings are
      the worst of it: "New version to sign" is simply untrue for a rehire. The
      admin twin was ruled NOT REWORDED on this row, which is why nothing has
      been touched piecemeal — one decision covering all three, not six edits.
      Proposals are in the session report.


      VERIFICATION OF PHASE B: build passes; a 12-case Case A matrix and a
      12-case R2 regression both pass, including the rehire regression exercised
      under BOTH flag states. STAGING IS OWED, and so is Phase A's own browser
      check — "6b2054f" has still never been confirmed in a browser, because the
      deployment predated it. Nothing in Phase B was observed on staging; these
      commits are not deployed.


      DISPLAY FOLLOW-UPS 2026-08-16 ("6b2054f"), TIER 2, on top of Phase A. Four
      items Gary found on staging once R2 was live; three fixed, one filed as a
      row. All display or copy — hr-completion.ts and the precedence table
      untouched, no schema, no writes.


      (1) THE UPLOAD DIALOG CARRIED A THIRD INSTANCE OF THE HR-11f ASSERTION.
      "Everyone will need to acknowledge this new version" — false under R2, and
      THE ONE THAT MATTERED MOST of the three: the two in versions/route.ts are
      comments read by us, this was the last thing an admin read before
      committing to an upload, so it was actively deterring the revision R2
      exists to make safe. Replaced with what happens to both audiences. Also
      dropped from amber to muted — the sentence now describes nothing going
      wrong, and warning colour on reassurance is the same false claim in a
      different channel. Flagged as a call, not assumed.


      (2) THE DATE IS BACK ON AN R2 ROW, sourced from the record they actually
      signed. Phase A left it null and reported it as an open question; Gary
      ruled 2026-08-16 that DESIGN §6's dateless string was abbreviation, not
      intent. completedAt now resolves from the SAME record object the version
      number and the download link resolve from, at all three sites, so the
      three fields name ONE record and cannot disagree. Only the R2 branch was
      added — the rehire path stays null exactly as before, because no surface
      renders a date on needs-resign and widening it further would have been an
      unruled change wearing a display fix's clothes.


      (3) DUPLICATED VERSION TEXT, BOTH SURFACES, AND BOTH WERE PHASE A'S OWN
      DOING. On /my/documents the notice shipped as a SECOND paragraph beneath
      the existing copy instead of replacing it, so an R2 row said "Signed v5"
      twice in stacked lines. On the staff page the badge gained "· current is
      v6" while the subtitle kept "Current version v6". Both are the same
      mistake — new copy added ALONGSIDE old copy that already said it — and
      neither showed up in a build. Collapsed to one statement each; the badge
      keeps the comparison because it is the half that names both numbers, and
      the subtitle drops out on R2 rows only, since every other status still
      needs it.


      (4) NOT FIXED, FILED AS DEBT-70. The Aug 15 / Aug 16 disagreement Gary saw
      between the staff page and /my/documents is a CLIENT-VS-SERVER TIMEZONE
      SPLIT, not an R2 defect: client components print the viewer's local day,
      server components print Vercel's UTC day, and an evening record falls on
      two different calendar days. R2 only made it visible by putting a date on
      a row that reaches both surfaces. It reaches 11 server components across
      22 call sites — checklists, users, HR, training — so per Gary's own
      instruction it became a row rather than a fix. A partial fix would be
      worse than none: correcting /my/documents alone would make it agree with
      the staff Documents tab and still disagree with the staff Compliance tab
      one scroll away.


      COPY SWEEP for the HR-11f claim, requested with item 1: two more found.
      document-detail-client.tsx:149 ("signed records stay bound to the version
      they were signed against") is TRUE under R2 and more relevant than before
      — kept. The identical-hash guard's comment claimed a duplicate upload
      "makes everyone re-acknowledge for nothing" — corrected, with the reason
      the guard survives R2 recorded on it. REPORTED AND DELIBERATELY NOT
      CHANGED: /my/documents renders "New version to sign" on needs-current,
      which under R2 means a REHIRE who may be facing the very same version. Its
      admin twin "Needs current version" was ruled NOT REWORDED on this row, so
      rewording only the employee side would break that ruling from the other
      end. Both need one decision, not two edits.


      VERIFICATION OF THE FOLLOW-UPS: build passes; documentCompletion exercised
      across all 12 arms of the R2 matrix including the rehire regression,
      deliberately rather than incidentally — all pass. THE LIVE FIXTURE WAS NOT
      EXERCISED and that is the gap: local dev (br-broad-wave-a6vpjdw0/neondb)
      holds 1 signed record and 0 in the R2 state, so there was nothing to drive
      locally, and these commits are not deployed. A screen that reads green for
      a member who was never in the R2 state is a completion screen, not
      evidence — so no screenshot was taken. Gdogg Thomas's row on staging still
      owes the check.


      PHASE A BUILT 2026-08-16 ("457a57f"), NOT VERIFIED. Status is in_progress,
      not verified, and the gap is deliberate: not one observation has been taken
      on staging. Local staging and origin/staging are still DIVERGED (ahead 1,
      behind 1 — local 614dc8d, remote 6e3b9eb, common 7198f77; both carry the
      same HR-11o roadmap record with different messages), so the staging-SHA
      precondition cannot pass and verification may not begin. Gary reconciles
      that by hand — ROADMAP.yaml is append-only and will conflict, and
      origin/staging is what is deployed. Phase A does not close on a build.


      DOCUMENTS. Design spec: docs/prompts/R2-HR11k-DESIGN.md. Step 1 audit:
      docs/prompts/R2-HR11k-PHASE-A_AUDIT.md. Gary's rulings on that audit:
      docs/prompts/R2-HR11k-phase-a-addendum-01.md — WHERE IT DISAGREES WITH THE
      SPEC, THE ADDENDUM WINS. All three committed with the work ("457a57f").
      The base session prompt (suggested path docs/prompts/R2-HR11k-phase-a.md)
      WAS NEVER SAVED TO THE REPO — the audit recorded that at the time and it
      is still true; it is absent from every branch and from git history. The
      spec, the audit and the addendum together reconstruct it, but the
      instruction of record for Phase A is missing and cannot be recovered from
      here.


      WHAT WAS BUILT, AND THE ONE THING THAT DECIDED THE SHAPE. The spec's file
      list was written against main, which does not have R1. Following its §7
      literally would have written the R2 rule into three files and reversed R1
      one day after it shipped. The audit caught it; the addendum ruled the
      corrected shape. THE PRECEDENCE TABLE CHANGES ONCE, IN THE PREDICATE —
      documentCompletion (src/lib/hr-completion.ts) — and the three call sites
      (lib/hr-compliance.ts, staff/[id]/page.tsx, my/documents/data.ts) change
      only in which facts they gather and how they label the result. This is the
      standing trap firing exactly as documented: project knowledge indexes main
      only, so staging-only work is invisible at planning time. The spec said to
      confirm against staging. Doing so is what saved the phase.


      THE SPLIT IS THE WHOLE CORRECTNESS RISK. hasRecordOnEarlierVersion asked
      "did they ever sign an older version, in ANY tenure" — one question
      standing in for two that R2 answers oppositely. It is now
      hasCurrentCycleRecordOnEarlierVersion (signed) and
      hasPriorCycleRecordOnEarlierVersion (rehire, needs-current). The old field
      was REMOVED rather than kept alongside the pair, so every call site is a
      type error until it has answered the cycle question — a caller cannot keep
      the old meaning by accident. Resolved against an any-cycle lookup instead,
      a rehired employee's previous-tenure signature reads compliant and HR-15
      Policy B is destroyed WITH NOTHING FAILING.


      THE TWO ORDERINGS, RULED IN OPPOSITE DIRECTIONS (addendum §2.1). A FULL
      current-version acknowledgment set BEATS the prior-version record — they
      did the newer work, and the fix for their missing record is to mint it. A
      PARTIAL set does NOT: someone who signed v4 and then initialled two pages
      of v6 reads signed at v4, and their partial v6 rows are preserved untouched
      as evidence. Both are recorded in docs/DECISIONS.md under the HR-8 block,
      preserve-and-mark, with the 2026-07-22 text intact beneath.


      A FOURTH DISPLAY SITE, NOT ENUMERATED BY THE ADDENDUM.
      staff/[id]/staff-compliance.tsx:59 hardcoded currentVersionNumber into its
      "Signed vN" string on the COMPLETE path, and carried a comment asserting
      completedAt is non-null whenever status is complete. R2 breaks both: an R2
      signer is complete on an older version, and completedAt upstream carries
      only the CURRENT version's record. It was found by sweeping the RENDERERS
      of currentVersionNumber rather than the callers of the predicate — the
      same lesson as CLAUDE.md § Verifying a guard covers every path, one layer
      out: the addendum's grep was on documentCompletion, and this file consumes
      a TYPE downstream of a call site, so no search for the predicate's name
      could ever have reached it. Fixed in the same commit.


      THE NOTICE LINKS THE DOWNLOAD, NEVER THE CEREMONY (addendum §2.2).
      /my/documents/[documentId] IS the ceremony and computes hasSignedRecord
      against the CURRENT version, which an R2 signer does not have — so linking
      it would open a signing ceremony for the exact person the ruling says must
      never be prompted: the defect R2 exists to remove, reintroduced through the
      notice announcing it. It links /api/hr/documents/[id]/download, the
      audience-aware route the Library already uses on that same page. NOTE: the
      audit transcribed that call site as carrying "?stream=1"; the Library's
      actual href has no query string, which is why the addendum said to confirm
      it from the call site rather than from the audit. Rule 5 is untouched — the
      source document, never a signed PDF.


      NO SCHEMA CHANGE, no migration, no writes to HrSignedRecord or
      HrDocumentAcknowledgment, no touch to signingCycle semantics, retired
      checkpoints, audience logic, the upload dialog, HR_MODULE_AVAILABLE, or the
      ceremony route. Read side only. The "will likely add a column to
      HrDocumentVersion" line in the original row below is superseded for Phase
      A: that column is Case A and waits for Phase B.


      EXPECT PERCENTAGES TO RISE, and expect it to be reported as a bug — the
      mirror image of R1's warning, one day later, in the same file. Anyone
      holding a current-tenure signature on a superseded version moves from
      needs-resign to complete. Account for the movement before accepting it; a
      rise for any OTHER reason is a defect.


      OPEN, FOR GARY, BEFORE VERIFICATION. (1) THE DATE IS OMITTED on an R2 row.
      completedAt upstream is the CURRENT version's record, which an R2 signer
      does not have, so the label renders "Signed v4 · current is v6" with no
      date — exactly the string ruled in DESIGN §6, which shows a date on the
      current-version row and none on the R2 row. Built to the ruled string
      rather than guessed at; if that asymmetry was abbreviation rather than
      intent, the prior record's completedAt is available at all three sites and
      is a small change. (2) THE FIXTURE IS UNMEASURED. Per addendum §2.3,
      verification step 3 does not run against a named person until staging has
      been read in the NEON CONSOLE to find whoever actually holds a completed
      signed record on a NON-CURRENT version in their CURRENT signing cycle. If
      that is nobody, the fixture must be built before verification means
      anything. A screen that reads green for a member who was never in that
      state is a completion screen, not evidence.


      VERIFICATION STILL OWED (addendum §5, spec §9): build passes — done. The
      measured fixture reads green on /staff/[id] and on /my/documents with the
      notice and no signing affordance. THE REHIRE REGRESSION, which is now one
      test against one predicate — that is R1's dividend, but it also means one
      wrong lookup breaks every surface at once, so exercise it deliberately; if
      it appears to pass without being exercised, treat it as unverified. The
      row-4 member has dropped off /my with NO edit to my/page.tsx (the cheapest
      proof the change landed in the predicate rather than in the labels). The
      notice's link opens read-only and no ceremony route is reachable from that
      row. Compliance percentage movement accounted for. Neon counts of
      HrSignedRecord and HrDocumentAcknowledgment unchanged before and after,
      with neon.branch_id and current_database() in the same result.


      HR-11j WAS CHECKED AND DOES NOT MEET THIS (addendum §1). Repo-wide grep on
      documentCompletion: one definition (hr-completion.ts:107) and exactly three
      call sites, matching the audit's enumeration with nothing missed. All four
      HR-11j layers — the R4 assignability gate (api/hr/documents/[id]/audience),
      both ceremony pages, and the mint (lib/hr-signed-pdf.ts) — reach signing
      readiness through getVersionAnchorReadiness / isSigningBlocked in
      lib/hr-anchors.ts, and NONE of them imports hr-completion, hr-compliance or
      my/documents/data. Nothing downstream of the predicate reaches them either:
      every consumer is a display surface (/hr/compliance, /staff, /staff/[id],
      /my, /my/documents). The two ceremony CLIENTS do import from hr-completion,
      but only the recordMissing copy constants — the module boundary is crossed,
      the predicate is not. R2 changes what satisfies the current version for
      DISPLAY; HR-11j's "signed" for grant-blocking is unreached by it.


      IN PLAIN TERMS. Fixing a typo in a handbook currently drags every person
      who already signed it back through the whole signing ceremony. It should
      not. Uploading a NEW FILE means a new document and everyone signs; a NEW
      VERSION of the same document leaves existing signers' records intact and
      just tells them an update is available. Until this is built, no document
      can be revised without re-prompting its entire audience. (Lead paragraph
      added 2026-08-15 for legibility; everything below is the original row,
      unchanged.)


      RULED 2026-08-15 (Gary), in the HR-11j session. FILED, NOT BUILT — it
      needs its own design pass and will likely add a column to
      HrDocumentVersion. The ruling itself is recorded on HR-11j alongside R1,
      R3 and R4; this row is the work.


      THE RULING. A NEW FILE is a new document: everyone signs it. A NEW VERSION
      is the same document: existing signers KEEP their record and are NOT
      re-prompted, and a read-only notice tells them an update is available.


      R2 SUPERSEDES HR-11f (2026-08-14), WHICH REQUIRED UNIVERSAL
      RE-ACKNOWLEDGMENT. HR-11f has never had a row of its own — it exists as a
      ruling cited inside HR-11d — so the marker lives here and on HR-11j rather
      than at a row that could be edited. The superseded text, preserved:


      [SUPERSEDED 2026-08-15 BY R2] HR-11f, ruled 2026-08-14 — a new version of
      a document requires new acknowledgment from everyone; signed records stay
      bound to the version signed, and a version bump re-prompts every signer.


      GARY'S POSITION, WHICH IS THE DESIGN CONSTRAINT: THE VERSION ORIGINALLY
      SIGNED IS THE MASTER DOCUMENT FOR THAT SIGNER. A minor revision does not
      re-prompt someone who has already signed.


      ── THE CONCRETE SURFACE WHERE R2'S ABSENCE IS VISIBLE ──────────────────
      Observed 2026-08-15 during the HR-11j acceptance pass, on staging
      dpl_8dqx2ay7Hx3rWAFxqkXgSvTTmUJj, org org_3G02wO4QlVVSWppi8aqlnSZnsDa
      (Clerk verified-snapper-7), document cmstv3r1s000004jxdcyhkbui.


      TOMMY'S ROW READS "NEEDS CURRENT VERSION". He holds a signed record on v1;
      v2 is current and he has no acknowledgments on it. That is CORRECT under
      HR-11f and INCORRECT under R2. NOT REWORDED (Gary's instruction) — the copy
      is accurate for the rule the code still implements, and changing the words
      without changing the rule would hide the gap rather than close it.


      THE STRUCTURAL STATEMENT OF THE DEFECT. "The current version" and "the
      version this signer is bound to" ARE TWO DISTINCT FACTS, AND ONLY ONE IS
      CURRENTLY SHOWN. Every surface collapses them into a single status, so a
      signer who is fully covered by the document they actually signed is
      rendered as owing something. The fix is not a copy change; it is teaching
      the surfaces to carry both facts, which is why this needs a column and a
      design pass rather than an edit.


      NOTE THE FAMILY RESEMBLANCE TO HR-11j'S OWN DEFECT, which is why it is
      worth writing down here: that one was two predicates reading different
      sources with different lifetimes and never required to agree. This one is
      two facts collapsed into one field. Both are shapes where a surface
      answers a question nobody asked it precisely enough.


      SCOPE NOTE: R3 (compliance shows "signed, version noted", plus a separate
      "who has acknowledged the current text" report) is adjacent and was ruled
      the same day. It is not this row — but whoever builds R2 should read it,
      because the two together decide what a version bump means to a reader.

  - id: HR-11m
    track: hr
    size: S
    status: verified
    title: "Signature checkpoints duplicated on every version — reuse by pageRef + ordinal"
    commits:
      - "974bf49"
      - "eab6254"
    notes: >
      SESSION 2026-08-15. Prompt:
      docs/prompts/2026-08-15-signature-checkpoint-duplication.md ("974bf49").
      Item 1 audit:
      docs/prompts/2026-08-15-signature-checkpoint-duplication_AUDIT.md (same
      commit). Reproduction fixtures V3/V4: docs/test_docs/ ("eab6254").


      ID NOTE: the series skips HR-11l on purpose. A lowercase l reads as a 1 in
      an id that gets typed by hand. Nothing is missing between HR-11k and here.


      THE DEFECT. syncCheckpointsForConfirmedAnchors minted a FRESH Signature
      checkpoint on every version confirmation. The reuse key was the anchor's
      own generatedCheckpointId — and DocumentAnchor is VERSION-scoped (HR-11b
      ruling a) while HrDocumentCheckpoint is DOCUMENT-scoped. A new version's
      anchors are fresh rows with that field null, so the lookup always missed
      and the document grew two ceremony steps per version. Initial never had it:
      its key (pageRef) is document-scoped on BOTH sides.


      SAME DEFECT FAMILY AS HR-11j — two objects with different lifetimes, and
      the reuse key sitting on the one that dies. That is the third time this
      shape has cost a session and it is worth recognising on sight.


      WHAT IT DID TO SIGNERS. Each orphaned Signature checkpoint is a ceremony
      step with no anchor behind it: the signer is prompted to sign and the
      capture has nowhere to be stamped on the document body, so the affordance
      renders at a fallback position over the prose. Gdogg Thomas's v3
      certificate listed SIX Signature checkpoints for TWO signature lines. R2
      (HR-11k) does not fix this — checkpoints are minted at ANCHOR CONFIRMATION,
      an admin action independent of who signs.


      ── THE RULING (Gary, 2026-08-15) ────────────────────────────────────────
      REUSE KEY IS pageRef PLUS ORDINAL. The k-th Signature checkpoint on the
      page, by orderIndex, belongs to the k-th signature anchor on that page, by
      y-descending. When a page carries several, the OLDEST orderIndex wins —
      deterministic, fixed regardless of version history, and what Initial
      already does.


      pageRef ALONE WAS REJECTED. It mirrors Initial and fixes the measured case,
      but it collapses two signature lines on ONE page onto a single checkpoint —
      one signer act and one timestamp for two attestations, which is precisely
      the behavior HR-11j removed.


      THE EMPIRICAL INPUT, and it is worth keeping because the question was open
      when the session started. Running the shipped detectAnchors() against the
      PDFs on disk: the real 2026 Employee Handbook has FOUR signature lines on
      FOUR DISTINCT pages (11, 22, 24, 28) and 27 Initial anchors on 27 distinct
      pages — NO same-page pairs anywhere. The test handbook has two, on pp. 3
      and 4. So on today's data pageRef-alone and pageRef+ordinal are identical;
      the ordinal is only different where pageRef alone is wrong. It was chosen
      for four lines of cost, not because anything currently needs it.


      NOTE WHAT THAT MEANS ABOUT INITIAL: its pageRef key has never been
      STRESSED, only never contradicted. One Initial per page in every document
      in play is why it has held.


      THE MATCH IS POSITIONAL, NOT IDENTITY-BASED, and that is the known
      weakness. A future version that inserts a signature line ABOVE an existing
      one on the same page shifts every ordinal on that page and anchors adopt
      each other's checkpoints. That is a wrong reuse, not a duplicate, and it
      stays inside one page. The identity-based alternative (inherit the pointer
      from the previous version's anchors, matched on page + anchorText +
      ordinal) was weighed and set aside as several times the code for
      robustness nothing currently needs.


      ALSO FIXED: the checkpoint read had no orderBy — a latent bug independent
      of the key. It matters more now, because the Signature lookup indexes into
      that list positionally, so an unordered read would make adoption depend on
      whatever order Postgres happened to return.


      G1 HELD. Nothing in this path deletes or modifies a checkpoint. Existing
      orphans stay: growth stops, the backlog does not clear itself. One orphan
      per page per version is adopted on the next confirm and the rest remain.
      THE ORPHANS HAVE NO REMOVAL PATH — see HR-11n, which is the measured
      consequence and is FILED, NOT BUILT.


      ── VERIFICATION ─────────────────────────────────────────────────────────
      UNIT ASSERTIONS ON ONE VERSION CANNOT SEE THIS DEFECT, which is exactly
      what the earlier fixture missed. Asserted directly against the shipped
      function across four versions on dev (br-broad-wave-a6vpjdw0 / neondb):
      totals held at 2 Signature + 3 Initial from v1 through v4, re-confirm was
      idempotent, every anchor linked to a DISTINCT checkpoint, and a synthetic
      same-page pair kept two checkpoints across three versions. Fixtures tagged
      sigdup-2026-08-15, deleted on the way out.


      NEGATIVE CONTROL, and it is the half that makes the rest mean anything: the
      same assertions run against the PRE-FIX code reproduced the defect exactly
      — 5 / 7 / 9 / 11, +2 per version. A test that cannot fail proves nothing.


      LIVE ACCEPTANCE PASSED on staging (br-square-feather-a63z92vz / neondb),
      deployment built from 974bf494ca06c010759bfdebb7290e18f86e326f, document
      "Test - Handbook Regression" cmstv3r1s000004jxdcyhkbui. V4 uploaded with
      DIFFERENT BYTES (sha256 7d0911b46725 vs V3's aac6a74bc9e4, so the
      hash-match carry-forward never fired and could not mask the result) and its
      anchors confirmed. Checkpoint composition BEFORE and AFTER that confirm was
      IDENTICAL: total 11, Signature 6, Initial 4, Acknowledgment 1. Pass was 11;
      fail would have been 13. Gdogg's v4 signed record stamped 10 field marks
      and the certificate lists 11 checkpoints, 6 Signature.

  - id: HR-11n
    track: hr
    size: M
    status: shipped
    title: "Orphaned Signature checkpoints cannot be removed — DELETE refuses anything acked"
    commits:
      - "72df99a"
    notes: >
      ── DEBT-72a BOARD-CURRENCY BACKFILL 2026-08-17 — STATUS in_progress -> shipped.
      Prepended per preserve-and-mark; NOTHING BELOW IS EDITED.

      EVIDENCE. The recorded work commit "72df99a" (Phase A) returns TRUE for
      `git merge-base --is-ancestor 72df99a origin/main`, first appearing on main in
      the merge "7e77ea6" (2026-08-17 02:47:13 UTC). That promotion also carried the
      row's own migration, 20260815150000_hr11n_checkpoint_retirement, which applied
      to the PRODUCTION database at 2026-08-17 02:50:05 UTC — additive, with a
      default.

      RESIDUAL WORK IS NOT CLOSED BY THIS FLIP. PHASE A ONLY. The anchor column was
      withdrawn to Phase B by Gary's mid-session scope cut, and Phase B is not built;
      the paragraph below naming that as the whole remaining risk on this row stands.
      NOT `verified` — that call is Gary's.
      Full measurement, method and the whole table:
      docs/prompts/DEBT-72a_BACKFILL.md. The rule this backfill accompanies is
      docs/WORKFLOW.md § Session completion rules item 4 — a row whose code was
      pushed to staging in a session has its status set in that session.
      Cross-refs: DEBT-72 (the promotion gate, still planned, still unbuilt),
      DEBT-72_AUDIT.md "b809e03" (which found this class), DEBT-72b (the gate
      itself, sequenced after this and deliberately not designed here).

      IN PLAIN TERMS. The test handbook shows SIX "Sign here" prompts for TWO
      signature lines, four of them landing on top of body text where no
      signature line exists, and an admin currently has no way to delete the
      four extras. They cannot be removed because people have already signed
      them, and the only delete path refuses anything signed. HR-11m stopped
      this getting worse; nothing yet makes it better. Until this is built, any
      document that accumulated duplicates before HR-11m stays visibly wrong to
      every signer who opens it. (Lead paragraph added 2026-08-15 for
      legibility; everything below is the original row, unchanged.)


      FILED 2026-08-15 (Gary's instruction, in the HR-11m session), NOT BUILT.
      Surfaced while auditing HR-11m and CONFIRMED BY MEASUREMENT before filing —
      the original session prompt had framed orphan removal as "a separate admin
      action requiring its own ruling", which assumed a working path exists. It
      does not.


      THE GAP. DELETE /api/hr/documents/[id]/checkpoints/[checkpointId] returns
      409 for any checkpoint with acknowledgments — correct on its own terms,
      since acknowledgment rows are append-only records and the FK is RESTRICT.
      But the signing ceremony selects ALL of a document's checkpoints with NO
      filter against anchors (src/app/(app)/hr/acknowledge/[documentId]/page.tsx),
      so orphans are presented as required steps and ACCUMULATE ACKNOWLEDGMENTS.
      Every orphan therefore becomes undeletable by the only path that exists.


      THE MEASUREMENT (br-square-feather-a63z92vz / neondb, document
      cmstv3r1s000004jxdcyhkbui, 2026-08-15). Six Signature checkpoints for two
      signature lines, EVERY ONE carrying acknowledgments:


      orderIndex 5 "Page 3 signature" acks 4; orderIndex 6 "Page 4 signature"
      acks 4; orderIndex 7 "Page 3 signature" acks 3; orderIndex 8 "Page 4
      signature" acks 3; orderIndex 9 "Page 3 signature" acks 2; orderIndex 10
      "Page 4 signature" acks 2.


      Four of the six are orphans — no confirmed anchor on the current version
      points at them — and all four are refused by the DELETE path. THE FINDING
      IS CONFIRMED, NOT THEORETICAL.


      WHY THIS IS NOT JUST HR-11m'S LEFTOVERS. HR-11m stopped the growth; it
      could not clear the backlog, because G1 forbids that path deleting or
      modifying a checkpoint and the ruling deliberately kept it that way. So the
      test document is stuck at six prompts for two signature lines, and the
      misplaced "Sign here" affordances sitting over body text stay visible to
      signers until something removes the rows. "The fix landed" and "the screen
      is right" are separated by exactly this missing path.


      WHAT IT NEEDS, AND WHY IT IS NOT A ONE-LINER. Its own G1 ruling on what
      removing a SIGNED-but-anchorless checkpoint means for the defensible
      record: the acknowledgments are real signer acts against a step that should
      never have existed. Deleting the checkpoint either orphans or destroys
      them, and neither is obviously right. Decide that before writing any code.


      ────────────────────────────────────────────────────────────── PHASE A
      BUILT 2026-08-15 ("72df99a") — THE CAPABILITY. NOTHING RETIRED.


      THE RULING (Gary, 2026-08-15, verbatim):


      I ruled that we hide these, not delete them. Four leftover signature lines
      got stapled onto the test document by the old version bug, and people
      already signed them, so the delete button refuses. That's the whole problem
      — there was no way to get rid of them.


      Retiring a checkpoint means it stops showing up for anybody signing from
      that point on. It doesn't come off the board. Every signature already
      collected on it stays exactly where it is. I'm not throwing away something
      an employee typed their name into just because I set the document up wrong.


      Certificates already issued don't change and don't get reissued. What a
      certificate says is what the ceremony was on the day that person signed.
      That stays true.


      Retired checkpoints don't get stamped on the page either. If the
      certificate doesn't list a step, there shouldn't be ink on the page for it.


      No script does this. I retire them by hand, one at a time, with a confirm
      that tells me how many signatures are on it. ADMIN only. Managers don't
      touch document setup. And I can un-retire if I get one wrong.


      Anybody in the middle of signing when I retire something doesn't get an
      error and doesn't get their entry quietly recorded against a step I just
      retired. They just finish.


      The test for what to retire is: it's a Signature checkpoint AND no anchor
      backs it. Not just unbacked on its own. Initials checkpoints get made one
      per page at upload, so an unbacked initials line is normal — Page 1
      initials has four real signatures on it and stays.


      [END OF RULING. Everything below is a record of what was BUILT and
      MEASURED, not a statement of what is ruled.]


      Prompt: docs/prompts/hr-11n-phase-a-retire-checkpoints.md ("72df99a").
      Audit: docs/prompts/hr-11n-phase-a-retire-checkpoints_AUDIT.md (same
      commit). Migration ledger entry: docs/MIGRATIONS.md.


      WHAT SHIPPED. Retirement — additive nullable columns retiredAt /
      retiredByUserId / retiredReason on HrDocumentCheckpoint
      ("20260815150000_hr11n_checkpoint_retirement", applied to DEV ONLY at time
      of writing), ADMIN-only retire and unretire routes, and the retire action +
      collapsed Retired section on the document-configuration screen. A retired
      checkpoint is excluded FROM THAT MOMENT FORWARD from the ceremony step
      list, the required-completion denominator at every call site, and the
      checkpoint table, initials grid and INLINE STAMPS of any certificate
      generated after retirement. Its acknowledgments are never deleted or
      modified. No auto-retire anywhere; no backfill migration; THIS SESSION
      RETIRED NOTHING.


      TEN FILTERED SITES, FOUND BY ENUMERATING LOADS RATHER THAN FILTERS. A
      missing filter has nothing to grep for (CLAUDE.md § Verifying a guard
      covers every path), so the audit swept every read of the checkpoint
      relation and checked each consumer individually. The prompt's known list
      named four and was wrong about two more: there is NO checkpoint list API
      (POST/PATCH/DELETE only — the admin list is server-rendered), and the
      HR-11j "staff-portal readiness check" counts ANCHORS, not checkpoints, so
      filtering it would have been a defect rather than an adoption.


      STAMPING IS FILTERED TOO (Gary's ruling, 2026-08-15): a stamp is a claim on
      the page body and must not appear for a step the certificate does not list.
      That path resolves an ack through the ANCHOR's generatedCheckpointId and
      never through doc.checkpoints, so hr-signed-pdf.ts filters ackByCheckpoint
      itself rather than the checkpoint load — one filter covering all five
      consumers. FILTERING THE LOAD WOULD HAVE LEFT THE STAMPS, which is the
      whole reason this is written down.


      THE MID-CEREMONY DROP HAD TO BE WRITTEN, NOT INHERITED, AND THE PROMPT'S
      REASON FOR IT WAS THE WRONG WAY ROUND. §4 asked for a drop instead of a
      400. The 400 could never have fired: the capture route loads checkpoints
      unfiltered, so a retired checkpoint is still in the map and the entry would
      have been INSERTED as an acknowledgment against a retired step. The
      exposure was a silent write, not a spurious refusal. An emptied entry array
      now returns the live-set completion result rather than an error.


      EMPTY-REDUCE GUARD. orderedAcks.reduce has no seed and throws on an empty
      array; retirement makes empty reachable, because `missing` passes vacuously
      when every acknowledged checkpoint has been retired. Refuses now, and the
      completedAt Math.max is seeded (it returns -Infinity rather than throwing —
      a silently wrong date, the worse failure of the two).


      SCOPE CUT MID-SESSION (Gary): THE ANCHOR COLUMN IS WITHDRAWN TO PHASE B,
      and hr-anchors.ts was not touched. §2.3 assumed HR-11m left a reusable
      helper resolving the pageRef + ordinal reuse key. It did not — the key is
      inline in syncCheckpointsForConfirmedAnchors and interleaved with creates
      and updates, so a display-only column cannot call it without refactoring
      the G1-critical file. A SECOND, INDEPENDENT REASON came out of real data:
      an unbacked INITIAL is normal (initials are minted one per page at upload;
      anchors only link to them), so a bare boolean would have rendered "false"
      beside a live initials line carrying four signatures, next to a Retire
      action. RULED: the retire criterion is type = 'Signature' AND
      anchor_backed = false; the anchor flag alone is never the criterion for any
      row type, and Phase B's column must be TYPE-AWARE.


      HR-11m WAS RE-OPENED AND CONFIRMED CLOSED BEFORE ANY FILTER WAS WRITTEN.
      The document held 7 checkpoints at the HR-11k pre-check and 11 now, and
      four Signature checkpoints minted after HR-11m shipped would have meant
      this cleanup tool was being built against a running leak. Dated by proxy
      (HrDocumentCheckpoint has no createdAt) on br-square-feather-a63z92vz /
      neondb: every orphan's FIRST acknowledgment predates deployment
      dpl_3UP4Hu4tHAHQXvgF7a8fYjWp3uH2 (created 2026-08-15 12:46:58 PDT, built
      from 974bf494ca06c010759bfdebb7290e18f86e326f), and acknowledgment
      documentVersionNumber sets are {1,2,3,4} / {2,3,4} / {3,4} with NO
      checkpoint acked on {4} alone — +2 at v2, +2 at v3, 0 at v4. HR-11m holds.


      THE TIMESTAMP COMPARISON NEEDS A TIMEZONE CONVERSION AND READS THE OPPOSITE
      WAY WITHOUT IT. signedAt is TIMESTAMP(3) with no zone (Prisma stores UTC);
      Vercel reports deploy times in PDT. Compared naively, an orphan first
      signed at 19:02 against a 12:46 deploy looks like it appeared six hours
      AFTER the fix — which would have convicted HR-11m and stopped the phase on
      a false finding. The column type was checked only because the answer looked
      wrong. Record the clock alongside any timestamp used as evidence.


      WHERE THE FOUR ORPHANS CAME FROM, since the row above could not say. The v2
      pair was first signed at 12:02 PDT, after the HR-11k pre-check measured 7
      checkpoints at ~09:45 the same morning; the event between them was
      confirming v2's anchors, WHICH WAS HR-11k'S OWN R4 ACCEPTANCE TEST. The
      HR-11k acceptance session minted two of these orphans while verifying
      something else, under pre-fix code, hours before HR-11m was written that
      afternoon. The v3 pair followed nine minutes later in the HR-11m
      reproduction.


      CORRECTION TO THE PROMPT'S §9, made in the audit and NOT in the prompt
      (a saved prompt is a claim wholesale): "page 28" is the real Employee
      Handbook, whose signature lines are pp. 11/22/24/28. THIS TEST DOCUMENT'S
      signature lines are on PAGES 3 AND 4, each currently rendering three
      stacked "Sign here" buttons (orderIndex 5/7/9 on p3, 6/8/10 on p4). The
      rest of §9 stands: the required signature-step count falls from six to two
      and no existing signed record changes state.


      VERIFIED: next build green, eslint clean over 11 files, both new routes
      registered in the build output. Migration applied to DEV ONLY —
      br-square-feather-a63z92vz and production get it via migrate deploy on
      Gary's push. NOT PUSHED.


      NOT DONE, AND IT IS THE WHOLE REMAINING RISK ON THIS ROW. NOTHING HAS BEEN
      EXERCISED IN A BROWSER. Every claim above is a green build, a clean lint,
      and unit-level reasoning over measured data. Status stays in_progress until
      a verification session retires the four orphans by hand and confirms the
      three observable consequences: pages 3 and 4 each render ONE "Sign here"
      button instead of three, the required signature-step count for the document
      falls from six to two, and NO existing signed record changes state. Re-run
      the staging-SHA precondition first or the entire pass is void.

  - id: HR-11o
    track: hr
    size: S
    status: verified
    commits:
      - "7456565"
    title: "Certificate table sorted by ceremony order, Sign here over body text, struck-through date"
    notes: >
      TIER 2, BUILT 2026-08-15 ("7456565"). RENDER LAYER ONLY — no schema, no
      migration, no change to what is recorded. Three display defects found by
      Gary during HR-11n Phase A verification on staging (26aa21d, org
      org_3G02wO4QlVVSWppi8aqlnSZnsDa, Clerk instance verified-snapper-7).
      Evidence: Tommy Thomas's v4 certificate for "Test - Handbook Regression",
      completed 2026-08-16 01:25:58 UTC.


      THE RULING (Gary, 2026-08-15, verbatim):


      Three display bugs found while verifying HR-11n. The certificate was
      sorting by ceremony order instead of by when things were actually signed,
      so it looked like people acknowledged the document before they signed it.
      The Sign here button was covering the acknowledgment sentence, and the date
      had a line through it.


      All three are fixed. The certificate now reports what actually happened, in
      the order it happened. Nothing about what gets recorded changed — this was
      display only.


      Verified on Gdogg's v5 certificate, Aug 15 2026.


      [END OF RULING. Everything below is a record of what was BUILT and
      MEASURED, not a statement of what is ruled.]


      D1 — THE CERTIFICATE TABLE SORTED BY CEREMONY ORDER AND READ AS A FALSE
      SEQUENCE. orderIndex is the order a signer is ASKED for steps. It is not
      the order the steps happened in, and on Tommy's certificate the two
      diverged: the Final acknowledgment (orderIndex 4, signed 01:25:58) sorted
      ABOVE both signatures (01:25:43 and 01:25:41), so the document asserted
      that he acknowledged it BEFORE he signed it. NOT A COSMETIC COMPLAINT ON A
      LEGAL RECORD — the certificate is the defensible artifact, and it was
      stating an order of events that did not occur. Now sorted by signedAt
      ascending with the # column numbering that order. Nothing stored changed;
      orderIndex still drives the ceremony.


      SORTED INTO A SEPARATE ARRAY, AND THAT IS THE LOAD-BEARING PART.
      orderedAcks stays in orderIndex order because the signature-block lookup
      further down takes the FIRST Signature/Acknowledgment row and means
      "earliest CEREMONY step of that kind". Re-sorting in place would have
      silently repointed it at whichever step was signed first — a second,
      quieter defect shipped under cover of fixing the first. orderIndex is the
      tie-break, because one submit stamps a single signedAt across all its rows
      and an unstable sort would otherwise scramble them arbitrarily.


      THE PER-PAGE INITIALS GRID IS DELIBERATELY NOT SORTED BY TIME. Asked to
      check whether it had the same defect; it does not, and the reason is worth
      keeping. Every cell carries its own page label ("p3: GT"), so the block is
      a page-indexed REFERENCE, not a narrative — it asserts no sequence and
      therefore cannot misstate one. Sorted by time it would read p3, p1, p4, p2
      and be materially harder to scan against the document. It IS now sorted by
      pageRef explicitly rather than inheriting orderIndex order: the two
      coincide only while the set is auto-generated (Initials are minted p1..pN
      at upload), and an admin who reorders or inserts a checkpoint breaks the
      correspondence.


      D2 — THE "Sign here" BUTTON COVERED THE SENTENCE BEING SIGNED. Affordances
      were lifted above their anchor (translate(0,-118%)) to clear the signature
      line and its caption. On page 3 of the test document that placed the button
      on the acknowledgment paragraph — "By accepting employment with Keva Juice,
      you acknowledge you have fully read and understand..." — so the signer
      could not read what they were signing under.


      NO VERTICAL OFFSET COULD HAVE FIXED IT, which is why the fix changes the
      axis rather than the number. Prose sits directly above a signature line BY
      CONSTRUCTION — that is what a signature line is — so every upward placement
      collides with text, and downward is the parked Initials-vs-footer
      collision. Affordances now move SIDEWAYS ONLY: placed on the fill line to
      the right of the anchor token, which is an underscore run the document
      itself reserves for a mark and where nothing readable sits, and vertically
      centred on the baseline rather than lifted. This is the rule the identity
      chips already used; signatures and initials now share it. The collision
      nudge goes RIGHT — the old `top -= 46` walked stacked affordances further
      into body text with every step. Overflowing the page width falls back to
      the corner dock that already handles anchorless checkpoints.


      THE PARKED FOOTER COLLISION IS FIXED BY THE SAME CHANGE, and both were done
      rather than one. Nothing moves vertically any more, so an Initials control
      can no longer be pushed down into the page footer.


      D3 — THE STAMPED DATE RENDERED STRUCK THROUGH. The identity chip was
      bg-[var(--color-primary)]/10 — TEN PERCENT ALPHA — and by design it sits on
      the fill line, whose underscores are drawn on the PDF canvas underneath.
      Those underscores read straight through the translucent chip and crossed
      the text, so the date showed as "Aug 15, 2026" struck out. On a signed
      document a struck-through date reads as an alteration. Background is now
      opaque; border and text colour unchanged.


      READER ONLY — CHECKED, NOT ASSUMED. The stamped PDF is unaffected. The
      stamping loop calls page.drawText and nothing else; every drawLine and
      drawRectangle in hr-signed-pdf.ts belongs to the appended certificate pages
      or the first-page banner, none of them near a stamp. Drawn glyphs are
      opaque, so a fill line beneath a stamped date shows as a line UNDER the
      text — which is what a filled-in form is supposed to look like.


      VERIFIED: next build green, eslint clean over both files.


      NOT VERIFIED IN A BROWSER, and that is the whole remaining risk. D2 in
      particular is a geometry change reasoned from the code, not observed. The
      test document and its confirmed anchors live on staging, and standing up an
      equivalent local fixture was disproportionate for a render fix Gary is
      about to verify directly. Status stays in_progress until that pass lands:
      re-sign the test document as a second signer and confirm the certificate
      table reads in true time order, the acknowledgment sentence on page 3 is
      fully readable with the "Sign here" button present, and the stamped date
      has no line through it. Re-run the staging-SHA precondition first.


      ────────────────────────────────────────────────────────────── CORRECTION
      2026-08-15, AFTER A VERIFICATION PASS THAT DID NOT EXERCISE THIS ROW.
      D1, D2 AND D3 REMAIN UNVERIFIED, and the paragraph above understated it:
      it said "not verified in a browser" as a general caution, when the specific
      fact is that BOTH certificates examined in the pass were generated by
      deployments that do not contain these fixes.


      THE TIMELINE, ALL LOCAL (PDT). "7456565" and "68c5a2d" were committed
      18:54:33 and 18:55:52; "68c5a2d" DEPLOYED TO STAGING 18:59:39.


      TOMMY THOMAS's v4 record was minted 18:25:58 PDT — THIRTY-FOUR MINUTES
      BEFORE THAT DEPLOYMENT. His certificate therefore came from "26aa21d",
      which carries HR-11n Phase A's retiredAt filters and NONE of HR-11o. His
      7-checkpoint list is real evidence and it validates PHASE A, not this row.
      If his checkpoint table read in ceremony order rather than time order, that
      is EXPECTED — the D1 sort was not deployed — and must not be filed as a
      regression.


      GDOGG THOMAS's v4 record was minted 13:05:58 PDT, pre-dating "72df99a"
      itself, so his stamped page 3 and his certificate are pre-fix by a wider
      margin still. Signed records are never regenerated, so neither certificate
      can acquire these fixes retroactively; only a NEW signing exercises them.


      SO NOTHING IN THAT PASS TOUCHED D1, D2 OR D3. The three checks named above
      still stand, unrun, and now carry a precondition of their own: the
      certificate must be minted by a deployment built from "68c5a2d" OR LATER,
      confirmed by the staging-SHA precondition BEFORE the signature is given —
      not after, because a signed record cannot be re-made to correct the test.


      The pass was not wasted: it produced
      docs/prompts/hr-11n-ceremony-route-audit.md, which enumerated every
      ceremony route (exactly two renderers, both filtered, no third route),
      confirmed the HR-11n entry-drop intact with one writer in the codebase, and
      established that HR-11n does NOT reopen. It also produced the timezone
      precondition now in CLAUDE.md § Database Evidence, after the UTC/local trap
      fired three times in one day on these same rows.


      ────────────────────────────────────────────────────────────── VERIFIED
      2026-08-15 BY GARY ON STAGING. All three defects confirmed fixed in the
      browser. Status in_progress -> verified.


      EVIDENCE. Gdogg Thomas, v5 of "Test - Handbook Regression", record
      completed 2026-08-16 04:06:52 UTC (= 2026-08-15 21:06:52 PDT — both stated,
      per CLAUDE.md § Database Evidence). Staging deployment built from
      "68c5a2d", org org_3G02wO4QlVVSWppi8aqlnSZnsDa, Clerk instance
      verified-snapper-7. THE PRECONDITION THIS ROW ADDED WAS MET: the record was
      minted by a deployment carrying the fixes, and a NEW signing was used
      rather than an existing certificate, which was the only way to exercise
      them — signed records are never regenerated.


      D1 — PASS. The certificate checkpoint table sorts by signedAt. The column
      reads 04:06:35, :38, :40, :42, :44, :47, :52 with no row out of sequence,
      and the Final acknowledgment is row 7, at the bottom. On Tommy's pre-fix
      certificate the same checkpoint was row 5, ABOVE two signatures signed
      fifteen seconds earlier. The before/after on one document is the direct
      measurement of the defect.


      AND THE FIX SURFACED SOMETHING THE OLD SORT WAS HIDING, WHICH IS THE PART
      WORTH KEEPING. In true time order the signer completed PAGE 3'S SIGNATURE
      BEFORE PAGE 3'S INITIALS, and page 4's signature before page 4's initials —
      out of ceremony order. The orderIndex sort concealed it completely, because
      it rendered every certificate in the order the steps were DEFINED rather
      than the order they were taken. The table now reports what happened instead
      of what was expected, and the first thing it reported was a sequence nobody
      knew was occurring.


      NOT FILED AS A DEFECT, AND DELIBERATELY NOT RULED ON HERE. Whether a signer
      may sign a page before initialing it is a question about the ceremony, not
      about this row's render fix; per-page initialing is gated sequentially
      (nextRequiredInitial) but signatures are not gated on the page's initials.
      Recorded so the observation is not lost — it exists only because D1 shipped.


      D2 — PASS. The acknowledgment text on pages 3 and 4 is fully legible with
      the "Sign here" affordance beside it. Verified in the reader.


      D3 — PASS. The stamped date renders clean, with no line through it.
      Verified in the reader.


      Per-page initials read p1 through p4 in page order, as designed — the
      block that was deliberately NOT time-sorted (see D1 above) behaves as
      argued.


      THE PARKED FOOTER-COLLISION FINDING IS CLOSED BY "7456565". It was parked
      in the HR-11m session prompt's out-of-scope list —
      docs/prompts/2026-08-15-signature-checkpoint-duplication.md line 149,
      "Initials button colliding with the page footer in the reader" — and never
      had a row of its own, which is why it is closed here rather than there (a
      saved prompt is a claim wholesale and is never edited). D2's change removes
      it by construction: affordances no longer move vertically at all, so an
      Initials control cannot be pushed down into the footer. One fix, both
      defects, as intended when the change was made.


      STILL OPEN FROM THAT SAME PARKED LIST: "Initials input hardening" (line
      148). D1 closed the "certificate table sorting by signedAt" half of that
      line; the hardening half is untouched by this row and has no row of its
      own.

  - id: HR-11i
    track: hr
    size: S
    status: planned
    title: "Errored scan is indistinguishable from image-only — per-version scan state"
    notes: >
      Surfaced during HR-11d Phase 1 and ruled OUT of it (Gary, 2026-08-14):
      it needs a column and a migration, and HR-11d was not growing one at the
      end. NOT STARTED. Nothing about it is built.

      IT REACHES HR-11d'S DEFECT THROUGH THE ERROR PATH, AND R3(i-a)'S
      CONDITION DOES NOT COVER IT. The ratified trip-wire fires on
      `matched > 0 && confirmed == 0`. When detection THROWS,
      detectAndStoreVersionAnchors returns matched 0 (src/lib/hr-anchors.ts) —
      so the version presents as `matched == 0`, which R3(i-a) defines as
      LEGITIMATELY certificate-only and therefore signable. A record then mints
      claiming completion with nothing stamped on the body: HR-11d's exact
      defect, arrived at from the other side.

      The two states are not the same and the schema cannot currently tell them
      apart. "No anchors because this PDF has no text layer" is a fact about
      the document. "No anchors because pdfjs threw" is an absence of
      knowledge. Both persist as zero rows on HrDocumentVersion, and the
      ceremony has nothing to ask.

      THE MITIGATION THAT EXISTS, AND WHY IT IS NOT THE FIX. HR-11d 2a (R2)
      makes the failure loud to the operator standing at the upload — the
      dialog says field scanning failed and to scan again before anyone signs,
      instead of a bare zero standing in for three outcomes. That is a human
      holding the line, at one moment, on one screen. It does not survive the
      operator closing the dialog, and the ceremony never learns.

      THE FIX IS PER-VERSION SCAN STATE: record on HrDocumentVersion whether a
      scan has ever SUCCEEDED (and when), so the guard can distinguish "scanned,
      genuinely no fields" from "never successfully scanned" and refuse the
      second. Additive column, its own migration, and its own ruling on what the
      guard does with the third state — refuse, or warn the admin and let the
      document stay signable. Do not fold it into an HR-11d follow-up.

  - id: HR-11h
    track: hr
    size: M
    status: planned
    title: "Manager countersignature — two-party documents where a manager signs after the employee"
    notes: >
      Surfaced during HR-11d. The 2023 handbook's Keva Pay Agreement (p. 9)
      carries a "Manager Signature:" line that HR-11d deliberately recognizes
      and leaves unstamped. Needs: routing to a manager after the employee
      completes, a second signing ceremony, completion semantics while awaiting
      the countersignature, and notification. Not started; HR-11d only keeps
      the line clean for it.

  # ---- Labor ----
  # Old Phase 0–4 numbering retired 2026-07-20 (Reset Brief); L- prefix current.
  # NOTHING IS FILED UNDER `LABOR-`, AND NOTHING SHOULD BE. The 2026-08-02
  # LABOR-1 draft was ABSORBED INTO L-2 (see that row) — it proposed a third
  # numbering of this thread, and its phase (d) was already L-4. Do not re-file
  # it. Noted here as well as on L-2 because the draft is findable in
  # docs/prompts/ and a reader who starts there sees no sign it was absorbed.

  - id: L-0
    track: labor
    size: L
    status: shipped
    shipped: 2026-07-21
    commits: [e907e6f, 07e4597, 4b8ca0a]
    title: "Labor foundation (pre-reset Phases 0–3) — config, budget engine, coverage, per-store budgets, 4-week forward"
    notes: >
      Built 7-20 under the old numbering (e907e6f Phases 0+1, 07e4597 Phase 2,
      4b8ca0a Phase 3); consolidated into one entry at reconstruction. Prod via
      9743899; LABOR_MODULE_AVAILABLE enabled in Production 7-21. Money is
      Decimal dollars; rounding is floor-to-tier.

  - id: L-1
    track: labor
    status: verified
    title: "Manual staging pass of the shipped labor foundation"
    notes: >
      Verification-only phase, no code — Gary's end-to-end staging pass of
      every shipped labor surface. Gated all further labor build work.
      NO `commits` FIELD, DELIBERATELY — read the absence as accurate, not as an
      omission to be filled. This phase shipped no code, so there is no SHA to
      cite; a commit here would assert work that does not exist. Verified
      2026-08-01 that L-1 is the ONLY row in this file carrying a landed status
      (staging | shipped | verified) with no commits, which is why it has been
      flagged twice as an anomaly. It is not one.

  - id: L-2
    track: labor
    size: L
    status: shipped
    shipped: 2026-08-19
    commits: [be705a2, 8a28f61, f47e3bd, fa86bae, 5e2f4d7]
    title: >
      OPTIONAL Square labor integration — schedules and timecards as an overlay
      on forecast-driven labor, per-org toggle, off by default
    notes: >
      SHIPPED — PROMOTED TO PRODUCTION 2026-08-19 in merge 5e2f4d7. Prepended;
      nothing below is edited, INCLUDING the "STATUS STAYS in_progress" and
      "STAGED, NOT PROMOTED" lines in the block directly beneath this one. Both
      were true when written and are superseded here rather than removed — the
      order of the blocks is the record of how this row got to production.
      ALL THREE PHASES ARE LIVE: AL-1 (be705a2), AL-2 (8a28f61 and f47e3bd),
      AL-3 (fa86bae). PRODUCTION VERIFIED THE SAME DAY: the Positions roster
      renders real names and real pay after the production staff import, and
      THE STORE PRIVACY CHECK PASSED — tommy@keva.com sees no wage and no tips.
      That is the check that mattered. AL-3 is the first phase where per-person
      pay reaches a page, and the design rule is that the capability decides
      whether to RUN THE QUERY, never whether to render the cell, so a STORE
      viewer with an empty payload is the only evidence that the rule held
      where it counts.
      STATUS IS `shipped`, NOT `verified`, BY GARY'S 2026-08-19 RULING, and the
      gap is deliberate rather than an oversight to be tidied. The production
      checks recorded above are exactly what a later `verified` flip would
      cite; Gary held the row at `shipped` on the day. Read the absence of
      `verified` as a decision, not as missing bookkeeping.
      ROLLBACK IS `git revert -m 1 5e2f4d7`, and that is worth stating because
      the neighbouring precedent is the opposite one. 5e2f4d7 is a TRUE MERGE
      with two parents (27ae79a, the previous main HEAD, and cd20a45, the
      staging head) — unlike BUILD-2's 493175e, which was a fast-forward and
      therefore had no `-m 1` rollback at all. Verified from `git log --format=%p`
      at recording time, not assumed from the commit subject.
      SQUARE_LABOR_AVAILABLE IS NOW A PRODUCTION VARIABLE. The AL-3 block below
      says production "stays dark regardless" because the flag was Preview-only
      and its activation was a separate future decision. THAT DECISION HAS BEEN
      TAKEN — the promotion's post-deploy steps (docs/DEPLOY_LOG.md, 5e2f4d7)
      set it true on Production, enabled Advanced Labor, ran the staff import
      and the roster/timecard syncs per store, and repeated the privacy check
      there. Anyone reading the older sentence in isolation would conclude the
      overlay is off in production. It is on.
      FIVE SQUARE LOCATION IDS ARE STILL UNIDENTIFIED, and this is the one open
      item this row carries into production: 13KAHQ…, 2K1XTK…, 779FNA…,
      7W133C… and CD2APE… hold 11 team-member assignments between them and map
      to no Froot `Store`. They render as a counted line on the Positions card
      (Q10 ruled: render the count, file nothing). GARY IS CLASSIFYING THEM
      THIS WEEK — closed / test / real-but-unimported — and the classification
      is what decides whether anything gets built. NOTHING IS FILED FOR THIS
      DELIBERATELY: two of the three outcomes need no code, and filing a row
      before the answer would put speculative work on the board. If the answer
      is "real but unimported", the import is the follow-up and it gets its own
      row then. The ids are also listed in docs/ADVANCED_LABOR.md, which is
      where the measurement they came from lives.
      TASK-4 CROSS-NOTE — the 2026-08-19 07:03 build anomaly is NOT filed on
      this row and does not belong to it. It was filed as BUILD-3 in cbc35e1,
      status `withdrawn`, closed as a transient build-environment failure. Said
      here only because the recorder prompt for this session asked for it under
      a different id and a reader comparing the two would otherwise look for a
      row that does not exist.
      ── AL-3 RECORD BELOW, UNEDITED ──
      PHASE 3 IS BUILT — 2026-08-19 (AL-3, work commit fa86bae). Prepended;
      nothing below is edited. STATUS STAYS in_progress — every vision item now
      has a build, but nothing is promoted and production stays dark regardless:
      SQUARE_LABOR_AVAILABLE is Preview-only and its activation is its own future
      decision. STAGED, NOT PROMOTED.
      VISION ITEMS 2, 5 AND 10 SHIPPED: pay rates on /staff and /staff/[id], the
      Square team roster on the Positions card, and a Tips column on All
      Locations. New: migration 20260819210000_al3_team_member_wage (additive —
      one table, SquareTeamMemberWage), src/lib/labor-roster.ts,
      src/lib/labor-costs.ts, canSeeWages/canSeeTips/loadTipBlocks in
      labor-dashboard.ts, computeTipPayout + getTipPayoutForStores in
      labor-actuals.ts, three routes under /api/square/labor/roster/, and one new
      capability, labor.costs.view. NO CRON REGISTRATION, no webhook change, no
      write to Square.
      THIS IS THE FIRST PHASE WHERE PER-PERSON LABOR DATA REACHES A PAGE, so
      DEBT-10 governed ahead of feature completeness. THE GATE DECIDES WHETHER TO
      RUN THE QUERY, NEVER WHETHER TO RENDER THE CELL — Gary's rule was that a
      non-MANAGE viewer's payload must not CONTAIN wage fields, "not hidden in the
      UI, ABSENT from the response". /staff does not build the id list, the
      Positions card does not mount the roster component, and /api/dashboard/rollup
      adds no `tips` key. A wage that is never selected cannot leak through a
      payload, a props tree, an RSC flight payload, or a future JSON route that
      forgets to re-check. STORE accounts were ALREADY structurally safe and stay
      so: /staff redirects without staff.view and /settings/labor redirects without
      labor.manage, so both wage surfaces are unreachable to a shared iPad login.
      labor.costs.view — MANAGE, in ENFORCED_CAPABILITIES, grid row "See pay rates
      and tips". The capability AL-1 deferred and PERM-4 (c) promised. NO PRIOR
      CALL SITES, so no role's baseline moved: before this commit nothing rendered
      a wage anywhere. Deliberately not labor.manage, which matched on tier but is
      held out of the grid by PERM-5C and would have taken /settings/labor with it.
      THE SOURCE IS ONE CALL AND NEEDS NO NEW OAUTH SCOPE, which is what made this
      phase cheap and is the thing to remember. SearchTeamMembers returns
      wage_setting INLINE (Square's own docs recommend it over RetrieveWageSetting)
      under EMPLOYEES_READ — held and consented since long before the labor work,
      so no consent batch and no merchant re-auth, the opposite of the
      TIMECARDS_READ story AL-1 waited on. VERIFIED LIVE against the Keva account
      at Square-Version 2026-01-22: 99 of 99 ACTIVE members carried a wage_setting,
      every one with exactly ONE job assignment, 94 HOURLY all with an hourly_rate,
      5 SALARY all with BOTH annual_rate and weekly_hours, 6 is_overtime_exempt, 7
      distinct job titles. wage_setting is still tagged Beta, so ListTeamMemberWages
      (GA, hourly only) is wired as a CONDITIONAL second tier — zero members needed
      it, so it costs no round trip. The third tier is not code: costing never read
      this table, so a total roster failure degrades the ROSTER and leaves labor %
      untouched.
      THE SALARIED GAP AL-1 DEFERRED IS NOW MEASURED — 5 people ORG-WIDE, not 5 per
      store. Six members are ALL_CURRENT_AND_FUTURE_LOCATIONS and appear under every
      store, which is why per-store roster counts legitimately sum past 99: Sparks
      27/3 salaried, South Reno 25/4, Meadowood Mall 23/4, Spanish Springs 23/3,
      Las Brisas 22/4, Carson 19/3, UNR 18/4, University Village 18/3, Southgate
      16/3. COUNTS ONLY — no cost math, no allocation invented. That discharges the
      second of this row's two long-standing open questions ("whether owner/admin
      accounts who never clock in should appear in labor surfaces") as far as
      MEASUREMENT goes; the allocation decision is still Gary's and still unmade.
      ITEM 10's "REPLACES" IS REJECTED AS LITERAL, and this is the load-bearing
      finding of the phase. LaborPosition is NOT a legend — it is the weekly budget
      engine's rate table: labor-plan.ts:165 feeds active positions into
      computeWeeklyLaborBudget, where the blended hourly rate is the UNWEIGHTED MEAN
      of active hourly rates (labor-budget.ts:88-91). MEASURED: the legend's 4
      archetype hourly rows average $14.50; the Square roster's 94 real people
      average $12.36. Swapping the roster in would drop the blended rate ~15% and
      INFLATE SCHEDULABLE HOURS ~17% at the same budget — and would feed a
      Square-sourced input into a core engine, which seam (b) forbids outright.
      Gary ruled a SEGMENTED CONTROL: the roster is the default view when the
      overlay is on, the legend sits one click away labelled "Rate legend — drives
      the budget", and the label carries the ruling so the next person reads it on
      the control rather than discovering it by editing the wrong table. SEAM (b)
      RE-VERIFIED by grep over all eight core engines — none imports labor-actuals,
      labor-judgment, labor-dashboard, labor-roster or labor-costs.
      TIPS: THE EXPECTED ANSWER WAS REVERSED BY MEASUREMENT. The prompt anticipated
      "declared cash only, file card tips as a follow-up". CARD TIPS ARE ALREADY IN
      THE DATABASE — SalesPeriodCache.tipTotal, populated from order.total_tip_money
      on every sales sync since F-4 (sales-sync.ts:346), on the same store-local
      business day the labor % already uses. Measured 2.4%-7.1% of net across nine
      stores with 573 days of history on the oldest four ($80,378 at South Reno
      alone). So the column is Square-recorded tips PLUS declared cash over
      tip-eligible paid hours, and NO CARD-TIPS INGEST ROW IS FILED because there is
      nothing to build. The fixture pins that declared-cash-only would have read
      $1.25/hr where the true figure is $6.25/hr — wrong in the reassuring
      direction, which is the failure mode this file exists to keep findable.
      THE RESIDUAL DOUBLE-COUNT IS NAMED ON THE CARD rather than assumed away: a
      cash tip rung into the POS lands in total_tip_money and can also be declared
      on a timecard, and the two cannot be separated from data Froot holds, so the
      cell carries an asterisk and its tooltip says the rate is an UPPER BOUND.
      TEN RULINGS, all Gary 2026-08-19, all enforced at their sites and recorded in
      DECISIONS.md: Q1 wage_setting with the three-tier fallback, a missing rate
      reading "Not set in Square" and never $0; Q2 /staff pay gates on the overlay
      PLUS labor.costs.view, so an org with HR but no Square labor sees /staff
      exactly as before; Q3 the segmented control; Q4 sum both tip halves; Q5 a
      costs-denied viewer sees the roster HIDDEN ENTIRELY rather than stripped of
      its pay column; Q6 the Tips column follows the picker and the header names the
      range (diverging from item 5's "MTD" on purpose — it sits beside
      "Labor % · <range>" and two adjacent columns must not silently mean different
      windows); Q7 null tip-eligibility counts as ELIGIBLE with the hours in the
      footnote, since excluding maybe-eligible staff would overstate what a job
      pays; Q8 Tips are MANAGE this phase; Q9 build the WK HRS/SUP editors, labelled
      inert; Q10 render the counted unmapped-locations line and file nothing.
      WK HRS AND SUP PERSIST AND NOTHING READS THEM, and the card says so on its
      face. They are Froot-owned columns the roster sync's ON CONFLICT DO UPDATE
      DELIBERATELY OMITS — the discipline StaffMember.fullNameLocked already
      encodes. IF A LATER EDIT ADDS THEM TO THAT LIST, EVERY SUPERVISORY FLAG IN THE
      ORG IS ERASED BY THE NEXT SYNC, SILENTLY. SUP is not derivable from Square and
      the data says why: "Manager" is 7 people of whom 3 are salaried,
      "Administrator" 4 of whom 2 are, and "Owner" is HOURLY.
      TWO WAGE SOURCES, TWO PURPOSES, NEVER TO BE UNIFIED.
      SquareTimecard.wageHourlyRate is a per-shift SNAPSHOT answering "what did this
      shift cost"; SquareTeamMemberWage.hourlyRate is the CURRENT setting answering
      "what do we pay this person now". A later simplification that collapses them
      would silently re-cost history at today's rates.
      THE NEW TABLE HOLDS NO NAME AND NO EMAIL, EVER, and the reason is structural
      rather than tasteful: GET /api/staff spreads the whole StaffMember row with no
      `select`, so a wage column ON THAT MODEL would ship to every staff.view holder
      the day it was added. Names join from StaffMember.squareTeamMemberId. ~100
      rows/org at ~350 bytes is ~35 KB — not a storage question at all.
      paidMinutesOf is EXTRACTED from computeLaborActuals and SHARED with
      computeTipPayout, so the cost calculation and the tip calculation cannot drift
      on what an hour is; the fixture pins the rule from both sides.
      READ-ONLY TOWARD SQUARE, AND SQ-3 STAYS OWED. labor-roster.ts goes through
      getSquareClient(org) only and DELIBERATELY DOES NOT CALL
      fetchSquareTeamMembers, which still carries the personal-token fallback at
      square.ts:139. Routing those five legacy call sites through the org client is
      SQ-3 item (b); this phase adds no sixth.
      NOT DONE, AND IT NEEDS STAGING: the declared-cash total Gary asked for could
      not be measured here — the dev org's Square grant PREDATES SQ-SCOPE-1 and
      SearchTimecards returns INSUFFICIENT_SCOPES, so there are no timecards to
      total. The Tips cell prints the declared-cash half in its tooltip for exactly
      this reason. Worth keeping: that 403 is also positive evidence that the ROSTER
      half needs no re-consent, since the same token read 99 wage settings fine.
      npm run build green; scripts/verify-labor-actuals.ts 55 -> 73 checks (18 new),
      PASS. Four react-hooks/set-state-in-effect lint errors remain in
      labor-settings-client.tsx — three pre-existing, the fourth the same false
      positive on an async loader the rule cannot see through an await (DEBT-33
      baseline; the build is the gate).
      PHASE 2 IS BUILT — 2026-08-19 (AL-2, work commits 8a28f61 and f47e3bd).
      Prepended;
      nothing below is edited. STATUS STAYS in_progress — Phase 3 (items 2, 5,
      10) has not started. STAGED, NOT PROMOTED, and production stays dark
      regardless: SQUARE_LABOR_AVAILABLE is Preview-only and its activation is
      its own future decision.

      THE DASHBOARD NOW RENDERS THE NUMBER PHASE 1 MADE TRUSTWORTHY. Vision items
      1, 3, 4, 6 and 7: labor % on the Sales Performance card, MTD labor % on the
      Monthly Goal card, and a labor % card, a per-store column and a date-range
      picker on All Locations. New: src/lib/labor-judgment.ts,
      src/lib/labor-dashboard.ts, dashboard/labor-pct.tsx and
      dashboard/date-range-picker.tsx. Labor blocks on /api/dashboard/sales,
      /summary and /rollup; range params on /rollup. NO MIGRATION, no schema
      change, no cron registration, no webhook change, no write to Square.

      THE COVERAGE FIX IS THE LOAD-BEARING CHANGE, NOT THE CARDS, and it is the
      kind of defect this file exists to keep findable. MTD labor % over a
      partly-synced month divided N days of cost by M days of sales: three synced
      days in a nineteen-day month read ~3.2% against a 20% target — GREEN,
      PRECISE AND WRONG, which is the failure mode the seam's honesty rules are
      arranged against. getLaborActuals now derives the store-local days that
      carry timecards from rows it has already loaded and restricts BOTH sides to
      them, returning daysCovered/daysInWindow so every surface says "N of M days
      synced". AL-1's verified 33.7% was a SINGLE-DAY query, where covered and
      window are the same day by construction, so the defect could not have
      surfaced at Phase 1's verification. The fixture asserts what the old
      denominator would have printed, so a later "simplification" back to the
      full range fails there rather than on a card.

      Q-V RULED AND IT COULD NOT BE WIRED WHERE THE RULING FIRST POINTED. Gary
      ruled labor % visible to ADMIN, MANAGER and STORE alike — the tier that
      ALREADY sees the weekly labor BUDGET in dollars on the same page via
      /api/labor/budget's requireLaborView — and DENIABLE PER USER from the
      /users grid, with dollars staying MANAGE. The ruling said to use labor.view
      if it supports overrides and to present the smallest alternative otherwise.
      It does not: an override is only expressible for an ENFORCED_CAPABILITIES
      entry, and labor.view gates the Labor nav, the /labor page and every
      /api/labor route, so denying it to hide one percentage would take the
      module with it — the same shape as labor.manage, which PERM-5C held out of
      the grid saying "Labor governance is its own ruling". The build introduced
      labor.actuals.view (OPERATIONAL) with a grid row: NO PRIOR CALL SITES, so
      no role's baseline moved. Gary's staging pass confirms or reverses it.

      SIX MORE RULINGS, all Gary 2026-08-19, all enforced at their sites and all
      recorded in DECISIONS.md: R1 a disconnect degrades and never removes (the
      2026-08-05 ruling beat the AL-2 prompt's hard rule 3, which contradicted
      it); R2 judge against laborTargetPct AS SET, not the tier-floored effective
      rate; R3 reuse zone()'s three-zone scale; R4 the month-end labor projection
      divides by the goal-weighted sales projection the card already prints, so
      the page carries ONE month-end sales number rather than two, mixed basis
      labelled; R5 no graph recolour (the "green bar" of item 1 is a slim meter
      in the metric row — the chart is a line chart and has no bars); R6 the
      picker drives the ranking table while the three summary cards stay
      month-anchored.

      R3 WAS SUPERSEDED THE SAME DAY, once its consequence was visible (f47e3bd).
      Reusing zone() verbatim put amber ONE POINT BELOW target, so an operator who
      came in at 19.5% against a 20% target — under budget — was shown a warning
      colour, and so was one who landed at exactly 20.0%. A warning for
      succeeding. THE ACTUALS SCALE IS NOW: pct <= target GREEN; target < pct <=
      target+1 AMBER (over budget, inside a grace band); beyond that RED. Vision
      item 3's "green if meets/exceeds" is literal.

      AMBER MOVED SIDES RATHER THAN DISAPPEARING — a store a tenth of a point over
      is not in the same condition as one three points over, and collapsing both to
      red would discard the only signal between them.

      THE LABOR BUDGET CARD IS DELIBERATELY UNCHANGED, SO THE TWO NOW DIFFER, and
      this is the line to find later: a PLAN cutting it fine is worth a nudge, an
      ACTUAL landing under target is a win. On one dashboard a store at 19.5%
      against a 20% target shows AMBER on the Labor Budget card and GREEN on every
      labor % readout. A fixture check names the divergence, so a later
      "harmonisation" of the two scales fails there rather than silently changing
      what managers are told about their week.

      AL-1 Q2's PROMISE IS DISCHARGED: /labor carries the read-only "Advanced
      Labor: On/Off" badge. The FLIP still lives in the Square Integration card
      per seam (a) — this is a label, not a control.

      FRESHNESS WITH THE CRON STILL UNREGISTERED. A database claim on
      SquareLaborSyncState.lastSyncStartedAt — one conditional updateMany, never
      check-then-act, BUG-7's shape — with a 15-minute cooldown matching the sales
      cache, deferred past the response with after(), capped at 3 stores per load
      with THE DROP LOGGED rather than silent. Ceiling is one Square request per
      store per 15 minutes, strictly less than what sales already spends from the
      same surfaces and far less than the ~55 discarded syncs per 2.5 hours
      DEBT-69 measured. A failing store holds its claim and retries once per
      cooldown instead of on every dashboard load.

      SEAM (b) STILL HOLDS, verified by grep over all eight core engines: none
      imports labor-actuals, labor-judgment or labor-dashboard. With the toggle or
      either env gate off, NO `labor` KEY EXISTS on any dashboard payload, so the
      boundary test is structural rather than a render diff. npm run build green;
      scripts/verify-labor-actuals.ts extended 27 -> 55 checks (28 new: the
      coverage pair, the judgment including R2's boundary and BOTH EDGES of the
      flipped band, and the estate roll-up's sum-then-divide against the
      mean-of-ratios it replaces), PASS. NOTE ON TWO STALE COUNTS IN THE COMMIT
      MESSAGES, since a work commit cited by SHA is not amendable: 8a28f61 says
      "27 -> 54" (it was 52 at that commit) and f47e3bd says "52 -> 55" (correct).
      THIS LINE IS THE AUTHORITATIVE FIGURE: 55.

      PHASE 1 IS BUILT — 2026-08-18 (AL-1, work commit be705a2). Prepended;
      nothing below is edited. STATUS MOVES planned -> in_progress, and it is the
      first time this row has been anything but planned. STAGED, NOT PROMOTED.

      THE VISION EXISTS IN THE REPO NOW. docs/ADVANCED_LABOR.md carries Gary's
      twelve-item feature list VERBATIM plus the phase map. That closes the gap
      the deferral-lift blocker below recorded in its own words — "NO ADVANCED
      LABOR FORECASTING DOCUMENT EXISTS IN THE REPO ... the vision the lift names
      is not written down anywhere a build session can read". Phase 2 = dashboard
      cards (items 1, 3, 4, 6, 7); Phase 3 = /staff pay, the Positions roster and
      tips (2, 5, 10); items 9, 11 and 12 are explicit NON-CHANGES, named so a
      later session cannot read silence as permission.

      WHAT PHASE 1 SHIPPED. Migration 20260818223900_al1_advanced_labor_phase1,
      additive only — Organization.squareLaborEnabled plus SquareTimecard and
      SquareLaborSyncState. src/lib/labor-actuals.ts (sync + a pure calculation).
      The toggle inside the EXISTING Square Integration card per seam (a). Three
      ADMIN routes under /api/square/labor/ — toggle, sync, actuals — and an
      UNREGISTERED cron at /api/cron/labor-timecards. Fixture
      scripts/verify-labor-actuals.ts, 27 checks. NO DASHBOARD CARD: Phase 1
      makes the number exist and be trustworthy, Phase 2 renders it.

      THE SEAM WAS FOLLOWED, NOT REINTERPRETED, AND ONE NAME WAS DEFENDED. The
      AL-1 prompt specified the column as `advancedLaborEnabled`; seam (a) says
      `squareLaborEnabled`, and that name was already load-bearing in
      DECISIONS.md:149, four places on this file including L-4's row, and
      docs/prompts/L-2_square_labor_seam.md:59. Gary ruled for the seam. Same for
      placement: the control sits in the Square card, not on the labor settings
      page the prompt suggested.

      SEAM (b)'s BOUNDARY TEST PASSES BY CONSTRUCTION, which is stronger than the
      render diff the seam asks for. No core engine imports labor-actuals.ts —
      verified by grep over labor-budget, labor-plan, labor-coverage,
      labor-forecast, labor-daily, labor-week, labor-settings, labor-positions —
      and the only readers of the two new tables are that module and its own
      routes. A module nothing imports cannot move a render.

      ONE OF THE THREE OPEN QUESTIONS BELOW IS NOW ANSWERED. "Ingest shape:
      webhook-driven, polled via SearchTimecards, or both" — RULED POLLED, and
      not only by AL-1's mandate: the LABOR-0B blocker's finding (4) is the
      argument, since the webhook handler 200-ACKs an unknown type with no log
      line and Square does not retry a 200, so a labor.timecard.* subscription
      added before the handler learns the type would be swallowed without trace.
      The webhook dial was not touched. THE SECOND QUESTION IS STILL OPEN and
      resurfaced in a new place: a salaried GM who never clocks in contributes
      zero labor cost, so "whether owner/admin accounts who never clock in should
      appear in labor surfaces" is now a number problem as well as a display one.
      Phase 1 does NOT measure that gap — establishing the denominator needs a
      Square team-member read, which is Phase 3's roster work.

      DEFERRED WITH A RULING BEHIND EACH, all Gary, 2026-08-18: overtime (Phase 1
      is straight time and RETURNS otApplied:false rather than assuming it, so a
      Phase-2 card cannot render it as if OT were handled); salaried allocation
      (Square's effective hourly rate as-is, nothing allocated); tips excluded
      from labor cost (employee income, not employer cost — and item 5 wants them
      as their own column anyway); net sales as the denominator. ONE COMPLICATION
      SURFACED AND LEFT FOR PHASE 2: SalesForecast.projectedStoreSales is a
      hand-entered figure whose gross/net meaning is defined NOWHERE, so
      actual-vs-budget will compare two things that may not be the same thing.
      Settle it before Phase 2 renders them side by side.

      STORAGE WAS ESTIMATED BEFORE BUILDING, per the prompt's requirement, and
      the estimate found something else. SquareTimecard projects at ~19 MB/year
      (~86 rows/day at nine stores, ~600 bytes/row) against ~90 MB of Neon Free
      headroom — not material. SalesLineCache is 116 MB TODAY, 84% of the
      database, growing ~72 MB/year. Filed as DEBT-74 by Gary's ruling with the
      measurement attached; not this row's problem.

      FROOT IS NOW READ-ONLY TOWARD SQUARE IN FACT, NOT JUST IN INTENT —
      2026-08-18 (SQ-WB-1, work commit 0fd414a, ruling in DECISIONS.md "Froot is
      read-only toward Square; the name write-back dies"). Prepended; nothing
      below is edited. This closes the first of the three things LABOR-0B
      surfaced and left to Gary, recorded on this row's LABOR-0 blocker as (a).

      THE BREACH IS GONE. updateSquareTeamMemberName and
      staff/[id]/square-writeback are deleted whole — no carve-out, which is the
      half of the ruling that matters for this row, because the pending wording
      could have gone either way and a carve-out would have left L-2 arguing from
      a codebase that contradicted its own consent story. src/lib/square.ts now
      issues no write to Square business data; the two remaining POSTs are
      /oauth2/token (OAuth machinery, excepted) and /v2/team-members/search, a
      POST-shaped read.

      WHAT THIS BUYS THE SCOPE-ADDITION SESSION. The consent event for
      TIMECARDS_READ and TIMECARDS_SETTINGS_READ can now tell merchants Froot
      reads Square and never writes it, and the codebase backs the sentence. It
      did not on 2026-08-17. That is the whole point of doing the removal BEFORE
      the batch rather than after: a read-only claim made on a consent screen
      while a live PUT sits in the tree is the kind of thing that is only ever
      discovered by someone who is not on our side.

      AND IT HARDENS THE FIFTH DON'T. This row's standing DON'Ts already say never
      add an OAuth scope opportunistically. The ruling goes further and closes the
      category: no _WRITE scope is EVER added to src/app/api/square/auth/route.ts,
      for any feature, because no write permission will ever be requested from a
      merchant. L-4's prohibited push half is therefore not merely deferred — it
      is ruled out unless Gary reopens it with its own ruling, its own consent
      event, and a real feature behind it. Same treatment landed on the two dead
      Phase 2 inventory write routes in CLAUDE.md § Square Integration.

      UNCHANGED BY THIS: the build deferral (its own blocker below), the scope
      strings, and SQUARE_VERSION. SQ-WB-1 touched none of them by mandate.

      RE-SCOPED 2026-08-05 (Gary's ruling — DECISIONS.md, top entry). Was
      "Team/Labor API — actual worked hours + wages, actual-vs-budget, Nevada OT
      warning". Same work; the ruling changes its STATUS from an upgrade path to
      an OPTIONAL ADD-ON. Keva is migrating to Square Scheduling (all stores by
      September 2026), but Froot serves businesses that do and do not use
      Square, so the integration is a boost and never a floor. BUILD IS
      DEFERRED — this row is the seam design, written so a future build slots in
      without tearing anything down.

      ABSORBS the 2026-08-02 LABOR-1 draft. That draft proposed a NEW row under
      a `LABOR-` prefix, which is a third numbering of this thread — the `L-`
      prefix has been current since the 2026-07-20 reset, and L-2 was already
      this row. Its phase (d), schedule write-back, was already L-4. Nothing is
      filed under `LABOR-`; both prompt files stay untouched as dated claims
      (CLAUDE.md § Where documents live). Superseding prompt:
      docs/prompts/L-2_square_labor_seam.md.

      REVERSED FROM THAT DRAFT: its phase (c) had coverage-from-actuals
      SUPERSEDING the L-3 sales-inferred coverage decision. It does not. Real
      staffing is an overlay ON TOP OF forecast-derived coverage — the
      replacement path is precisely the one that stops working when Square is
      off. L-3 (a) stands; StoreHours remains the coverage upgrade path.

      REGISTRY DEPENDENCY SETTLED 2026-08-06 by R4. The seam below specifies an
      ADMIN-only toggle route, chosen to match what Square routes ENFORCE rather
      than what the registry then said (square.manage was ALL). R4 corrected the
      registry to ADMIN_ONLY, so the seam and the registry now agree and the
      future build inherits no contradiction. Had R4 gone the other way, this
      section would have been the thing to revisit.

      --- SEAM (a) THE TOGGLE ---
      Instagram's species, NOT activeModules (Gary's ruling, fork F3). Three
      parts, matching how Labor and HR are already gated:
        1. Availability gate: SQUARE_LABOR_AVAILABLE env, plus
           SQUARE_LABOR_INTERNAL_ORG_IDS for production dogfooding — the exact
           shape of laborModuleAvailable() / hrModuleAvailable()
           (src/lib/auth.ts:35,53). Server-side only, never NEXT_PUBLIC_.
        2. Per-org toggle: Organization.squareLaborEnabled Boolean
           @default(false) — a dedicated column like instagramEnabled
           (prisma/schema.prisma:28), NOT a fifth entry in activeModules.
           WHY: activeModules is the BILLABLE ADD-ON list that drives the module
           cards at settings/page.tsx:63-70. "Weekly Labor Model" is the product
           the org buys; a Square connection is a DATA SOURCE for a module they
           already pay for. An activeModules entry would advertise it as a
           separate purchase AND would permit the incoherent state
           square-labor-without-labor.
        3. Enforcement: requireSquareLabor() in src/lib/labor-access.ts, layered
           over requireLaborView() — both labor gates first, then the two Square
           gates, each failing to 404 so an org without it cannot probe it.
           ADMIN-only flip via POST /api/square/labor/toggle, mirroring
           /api/labor/toggle and /api/instagram/toggle.
        UI: a control inside the EXISTING Square Integration card on /settings,
        the way InstagramActions sits inside the Instagram card — not a new
        module card.

      --- SEAM (b) THE DATA BOUNDARY ---
      Square-sourced labor lives in clearly-separated storage that no core
      feature reads. New, prefixed tables — SquareTimecard, SquareScheduledShift
      — each carrying its own syncedAt and syncState. The core engines
      (labor-budget.ts, labor-plan.ts, labor-coverage.ts, labor-forecast.ts)
      NEVER gain a Square-sourced input; they keep reading SalesForecast /
      DailyGoal. The integration ADDS a comparison layer on top:
      planned-vs-actual, scheduled-vs-recommended, computed in a NEW module
      (labor-actuals.ts) that the core never imports.
      THE BOUNDARY TEST, and it is a real test to run before shipping: DROP
      EVERY SQUARE-LABOR TABLE AND EVERY EXISTING LABOR SURFACE MUST RENDER
      BYTE-IDENTICALLY. If anything moves, the boundary leaked.
      Mirrored Square data is PER-ENVIRONMENT stored data — it does not migrate
      between Neon branches, exactly like GoalPlan (DECISIONS.md § L-3 (c)).
      Each branch syncs independently; promoting code moves none of it.

      --- SEAM (c) THE FAILURE POSTURE ---
      EXACTLY THREE STATES, and the third is the one that gets skipped:
        OFF — the feature does not exist. No card, no badge, no nav, 404s.
        ON BUT UNHEALTHY — a degraded BADGE or banner on the comparison layer
          ("Square labor unavailable — last synced <ts>"), with every core
          number unchanged and still rendered. The overlay is what degrades.
        ON AND HEALTHY — the comparison layer renders.
      NEVER: a crashed page. NEVER: an integration error surfacing as 401/403 —
      see BUG-1. NEVER: zeros or empty arrays presented as measurements; absent
      data reads "not synced", which is a different sentence from "0 hours".
      BUG-1 IS THE ANTI-PATTERN TO NOT REPEAT, and it is live in the very file
      this seam lands in: src/lib/labor-access.ts:26-31 catches getCurrentUser()
      failures and returns 401 Unauthorized — its own comment says "this catch
      also swallows DB/connection errors for every /api/labor route". An
      infrastructure failure wearing an auth failure's clothes sends the next
      debugger to Clerk. The Square reads must be caught at the CARD BOUNDARY,
      typed distinctly from auth outcomes, and logged with the real cause.
      Also: /api/square/disconnect (which today nulls the three token columns
      and nothing else — the correct shape) must NEVER cascade into labor data.
      A disconnected org keeps its mirrored rows and shows them as stale; it
      does not lose them. Staleness is a badge, not a delete.

      --- SEAM (d) OAUTH SCOPES AND VERSION — THE LONG POLE ---
      Measured 2026-08-05, NOT resolved. Froot requests, verbatim
      (src/app/api/square/auth/route.ts:9):
        MERCHANT_PROFILE_READ ITEMS_READ ORDERS_READ EMPLOYEES_READ
      Every Labor API scope is absent — TIMECARDS_READ (timecards AND scheduled
      shifts), TIMECARDS_SETTINGS_READ (break/workweek config), TIMECARDS_WRITE
      (schedule push, L-4). ADDING ANY OF THEM FORCES EVERY EXISTING MERCHANT
      CONNECTION BACK THROUGH A CONSENT SCREEN. That is a merchant-facing
      re-auth rollout, not a deploy — establish it EARLY, it is the long pole,
      not the code. Second prerequisite: Square-Version is pinned 2024-01-17
      (src/lib/square.ts:4 — one constant, seven call sites, raw fetch, no
      `square` npm package), below the 2025-05-21 floor Labor/Timecard endpoints
      require. Re-verify both at kickoff; this row may sit for months and
      deprecation timelines move.

      LABOR-0 (docs/prompts/LABOR-0_shift_surface_grep.md) HAS NEVER RUN — it is
      an unexecuted prompt, filed with three others in 2e75029, with no results
      record anywhere. RUN IT BEFORE ANY BUILD. A PARTIAL pre-check on
      2026-08-05 covered its Tasks 2-4 only (read-only greps over src/, excluding
      src/generated; Task 1c construction-site count and Task 5 webhook inventory
      NOT done) and found: ZERO hits for deprecated Shift endpoints or
      labor.shift.* events — the surface is clean, so no Shift-migration row is
      warranted, per that draft's own conditional; ZERO hits for Timecard,
      SearchTimecards, WorkweekConfig, TeamMemberWage — no Square labor ingest
      exists today. Those two answers are load-bearing for this row and are
      dated; the audit is still owed the other three tasks.

      WAGE DATA STAYS GATED. Hours are not sensitive; dollars are. Any wage half
      inherits DEBT-10's territory (Square team-member PII — 138 employees'
      emails were exposed in production) and must serve through
      labor.costs.view, the MANAGE-tier capability PERM-4 (c) introduces
      (DECISIONS.md 2026-07-27). Timecard.wage carries a rate ONLY where Square
      team-member wage settings are configured, so the integration must WARN on
      the gap rather than render a silent zero — reuse the staff/[id]
      affordance ("No store assigned — signed documents stamp a blank store").

      DOES NOT INTERACT WITH DEBT-9. Timecards carry their own location, so
      per-store labor is attributed from Square's data, not from
      StaffMember.primaryStore. Primary store stays a payroll/legal label and a
      compliance-rollup bucket. Confirmed 2026-08-02; do not re-derive.

      --- DON'Ts FOR INTERVENING SESSIONS ---
      Pointer line in CLAUDE.md § Square Integration; the list is here.
        1. DON'T hardcode "all labor data is forecast-derived" at the seam sites
           — labor-budget.ts, labor-coverage.ts, labor-plan.ts. Leave the input
           shape open; an overlay has to be able to arrive.
        2. DON'T SPEND THE WORD "SHIFT". It is already double-booked:
           LaborDaypart renders as "Shift blocks" (settings/labor/
           labor-settings-client.tsx:717) and store-view handoff notes say "next
           shift". Never introduce a bare `Shift` model, type or column — Square
           scheduled shifts are SquareScheduledShift, always prefixed.
        3. DON'T add Square OAuth scopes opportunistically for an unrelated
           feature. Every addition re-consents every merchant; batch them
           deliberately or not at all.
        4. DON'T make /api/square/disconnect cascade into labor data.
        5. DON'T extend the labor-access.ts 401-masking catch to cover
           integration errors — that is BUG-1 propagating.

      OPEN QUESTIONS, deferred to kickoff — THREE, of which the third is RULED:
        - Ingest shape: webhook-driven, polled via SearchTimecards, or both.
          Timecards are edited after the fact (manager corrections), so
          labor.timecard.created alone is insufficient — an update path is
          required regardless.
        - Whether owner/admin accounts who never clock in should appear in
          labor surfaces at all.
        - RULED 2026-08-05 (Gary) — THE TOGGLE STAYS ON. A Square disconnect
          does NOT auto-disable squareLaborEnabled. The org keeps the feature
          and the overlay drops to seam (c)'s ON BUT UNHEALTHY state; a
          reconnect restores it with no second admin action. This is seam (c)
          applied to disconnect rather than a new posture, and it is the same
          direction as DON'T 4 — /api/square/disconnect nulls the three token
          columns and touches nothing else, so mirrored rows and the toggle
          both survive it and read as stale. Recorded in DECISIONS.md.
          ORIGINAL ENTRY, PRESERVED: "Whether the toggle should auto-disable
          when Square disconnects, or stay on and show unhealthy. Current lean:
          stay on and show unhealthy, so a reconnect restores the feature
          without a second admin action."
    blockers:
      - >
        THE VERSION BUMP IS DONE AT ALL THREE SITES — 2026-08-18 (SQ-VER-1, work
        commit b846e32). Prepended; nothing below is edited. This closes item (3)
        of the GATE STATUS block in the entry below, which recorded the bump as
        STILL OWED, and it is the last of this row's three gates to close in
        code. STAGED, NOT PROMOTED: it sits on staging and production still calls
        Square at 2024-01-17 until the next promotion.

        `SQUARE_VERSION` is `2026-01-22`, eight months clear of the 2025-05-21
        floor the Timecard endpoints require. THE EDIT WAS THREE LINES BECAUSE
        TWO ROUTES HELD THEIR OWN LITERAL — src/app/api/square/callback/route.ts
        and src/app/api/square/locations/route.ts hardcoded "2024-01-17" and
        never imported the constant, so editing src/lib/square.ts:4 alone would
        have called POST /oauth2/token at two different API versions from two
        different files inside one OAuth flow. BOTH STRAYS NOW IMPORT THE
        CONSTANT rather than carrying a fresh literal, which is the part that
        matters beyond this session: a fourth version string can no longer drift
        into existence, because there is no longer anywhere for one to live.
        Neither import creates a cycle — both files already imported
        squareBaseUrl from the same module.

        SEVEN version-bearing header sites, confirmed by grep at execution:
        square.ts:70 (POST /oauth2/token, refresh_token grant), :141
        (team-members/search), :175 (team-members/{id}), :212 (locations/{id}),
        :251 (getSquareClient headers — the shared client every product call
        uses), plus the two former literals. Five reach the version through the
        constant; site :251 alone carries orders sync, day-report, catalog sync
        and sales-items sync. LABOR-0B counted eight/six before SQ-WB-1 deleted
        the write-back.

        SAFETY RESTS ON LABOR-0B TASK 4, NOT ON THIS SESSION RE-DERIVING IT. All
        eleven dated versions in the window were surveyed against the six APIs
        Froot calls; nothing breaks a call site. The four breaking changes all
        miss the surface: Webhooks management 400 (Froot creates no subscriptions
        by API), RenewToken retired (Froot uses the refresh_token grant it was
        retired in favour of), Location address validation (write-side; both
        Locations sites are reads), Shift deprecation (zero labor surface).
        MERCHANT_PROFILE_READ is held but unexercised — there is no /v2/merchants
        call site in the tree at all.

        THE WEBHOOK SUBSCRIPTION VERSION WAS NOT TOUCHED and is not this row's to
        touch. Separate dial, set per-app in Square's dashboard, governing the
        events Square sends US; production is believed 2024-01-18. Two dials, one
        at a time.

        FIRST L-2 CODE SHIPPED WITH IT: GET /api/square/labor/verify, legitimate
        because the build deferral lifted the same day (see the entry below).
        SearchTimecards, no filter, limit 1, ADMIN-only, returns
        `{ ok, httpStatus, hasData }` and NOTHING else — no timecard fields, no
        names, no wages, which is DEBT-10 territory and stays gated behind
        labor.costs.view when the wage half is built. THE ROUTE MUST NEVER GROW A
        BODY; whoever needs real timecard data builds the ingest behind seam (b),
        with its own toggle and its own failure posture.

        WHAT THE PROBE IS FOR, AND WHY IT IS THE DESIGNED VERIFICATION. A live
        grant's permissions are NOT observable in Square's merchant dashboard, so
        the 2026-08-18 re-consent cannot be confirmed by looking — only by
        calling. One request proves version, scope AND grant together. A 403 from
        Square means the re-consent did not carry the new scopes: re-run the
        consent URL. THAT IS THE FIX, NOT A CODE BUG. Three choices are
        documented at the site and are load-bearing for reading its result: it
        uses the ORG OAUTH TOKEN ONLY via getSquareClient (the
        fetchSquareTeamMembers-style fallback to SQUARE_ACCESS_TOKEN would let
        the probe pass on a personal token and prove nothing about the merchant's
        grant); it gates on square.manage rather than the stores.manage
        /api/square/locations uses, because stores.manage is in
        ENFORCED_CAPABILITIES and deniable per-user, so an unrelated store
        override could 403 the probe and be misread as a missing scope, whereas
        square.manage is ADMIN_ONLY and not deniable; and `hasData` is REPORTED,
        NEVER JUDGED — an authorized empty result is a full pass, `ok` is the
        only criterion (Gary, 2026-08-18).

        GATE STATUS FOR THIS ROW AFTER THIS SESSION — all three code gates
        cleared, two human steps outstanding, in this order: (1) LABOR-0 COMPLETE
        (2026-08-18); (2) SCOPES ADDED AND CONSENTED on staging (SQ-SCOPE-1 plus
        Gary's 2026-08-18 re-consent, still unproven until the probe returns
        ok:true); (3) VERSION BUMPED, this session. OUTSTANDING: Gary's staging
        pass (dashboard sales, Stores → Edit Store location picker — the route
        that held a literal, Staff → Import from Square, then
        /api/square/labor/verify as ADMIN), and the PRODUCTION CONSENT CLICK at
        promotion followed by the same four checks there. Note the callback
        literal's replacement is exercised only by a FRESH OAUTH CONSENT, not by
        the four checks — it gets its real test at the next connect.

      - >
        THE TWO LABOR SCOPES ARE IN THE AUTHORIZE URL — 2026-08-18 (SQ-SCOPE-1,
        work commit 20b4e70). Prepended; nothing below is edited, and the
        "SQUARE OAUTH CARRIES NO LABOR SCOPES" entry below is now true only of
        the GRANT, not of the REQUEST. src/app/api/square/auth/route.ts:9 now
        requests six strings: `MERCHANT_PROFILE_READ ITEMS_READ ORDERS_READ
        EMPLOYEES_READ TIMECARDS_READ TIMECARDS_SETTINGS_READ`. The original
        four are preserved in order; two were appended; nothing was removed or
        reordered. REPORTING_READ stays PARKED (the enum/overview contradiction
        recorded as (c) below is unresolved, and a string absent from the enum
        cannot go in a consent URL). No _WRITE scope was added and none ever
        will be, per the 2026-08-18 read-only ruling.

        GATE STATUS FOR THIS ROW, three items, one closed this session.
        (1) LABOR-0: COMPLETE — both owed tasks run 2026-08-18, per the blocker
        below. (2) SCOPES: ADDED, NOT YET GRANTED — this is the seam that
        matters. The edit changes the AUTHORIZE URL only. The connected token
        still carries its original four permissions and every current sync keeps
        working unchanged; nothing about live behavior moves until Gary
        re-consents on /settings. Whoever reads this row mid-build must check
        whether the re-consent actually happened before assuming a labor read
        will authorize. (3) VERSION BUMP: STILL OWED — its own TIER 3 session,
        next on the track, and it is the THREE-line edit described below
        (src/lib/square.ts:4 plus the two hardcoded "2024-01-17" literals at
        callback/route.ts:43 and locations/route.ts:44), not one.

        WHY THE NEW SCOPES ARE DORMANT ON PURPOSE. No code reads either one, and
        none should until the version bump lands: the Timecard endpoints they
        unlock require Square-Version >= 2025-05-21 and the pin is still
        2024-01-17. So a granted scope alone proves nothing here — this
        session's success criteria are only (a) the authorize URL carries six
        scopes and (b) existing reads still work after re-consent. THE FIRST
        SUCCESSFUL LABOR READ HAPPENS AFTER THE VERSION-BUMP SESSION, and that
        is the moment the grant is fully proven, not before.

        THE REASON IT SHIPPED AHEAD OF THE BUILD IS CONSENT ECONOMICS, NOT
        READINESS (Gary's ruling, 2026-08-18). Adding scopes while exactly one
        merchant — Gary — is connected freezes the re-consent batch at one
        person forever. Every merchant who connects after this commit consents
        to the full six-string list on first connect and never sees a re-consent
        prompt for them. Waiting until the build was ready would have made the
        same batch as large as the merchant list.
      - >
        SQUARE OAUTH CARRIES NO LABOR SCOPES — promoted from this row's prose to
        a blocker entry 2026-08-06 so the board can tell this row from a ready
        one; re-verified at source the same day. src/app/api/square/auth/route.ts:9
        requests exactly `MERCHANT_PROFILE_READ ITEMS_READ ORDERS_READ
        EMPLOYEES_READ` — not one labor scope is present, so every read this
        phase needs 401s today. AND THE API VERSION IS BELOW THE LABOR FLOOR:
        src/lib/square.ts:4 pins SQUARE_VERSION = "2024-01-17". The two move
        together — a scope grant against a too-old version buys nothing.
      - >
        LABOR-0 IS NOW FULLY RUN — BOTH OWED TASKS COMPLETE, 2026-08-18
        (dd7b8603890b47b39287f19234f22da30705468e, results in
        docs/prompts/LABOR-0B_RESULTS.md). Prepended; nothing below is edited.
        Task 1c (construction-site count) and Task 5 (webhook inventory) are
        DONE, so this blocker's own condition is met and the "RUN IT BEFORE ANY
        BUILD" sentence below is discharged. The same session ran two riders:
        the exact OAuth scope strings for the consent batch, and the changelog
        delta for the SQUARE_VERSION bump. WHAT THE SURVEY CHANGES FOR THIS ROW,
        four things. (1) THE CONSENT BATCH IS TWO STRINGS, NOT THREE OR FOUR:
        TIMECARDS_READ and TIMECARDS_SETTINGS_READ. Scheduled-shift READS share
        TIMECARDS_READ (Square documents both under the same permission), and
        team wage / job / pay-rate reads are ALREADY COVERED by the held
        EMPLOYEES_READ — the wage half needs no new scope at all. Neither of the
        prompt's RULING NOW triggers fired: no read capability requires a WRITE
        scope, and scheduled-shift reads are NOT gated by a Shifts Plus
        subscription (only certain updates are, which bites L-4's prohibited
        push half, not this row). EMPLOYEES_READ is NOT deprecated — the
        Employees API is, the scope is the live one governing Team API reads,
        and there is no TEAM_READ to migrate to. (2) THE VERSION BUMP IS
        LOW-RISK BUT IT IS A THREE-LINE EDIT, NOT ONE. Eleven changelog dates
        2024-01-17 -> 2026-01-22 were surveyed and NOTHING breaks a Froot call
        site; all four breaking changes miss the surface (Webhooks management
        400 — Froot creates no subscriptions by API; RenewToken retired — Froot
        uses the refresh_token grant; Location address validation — write-side,
        Froot only reads; Shift deprecation — zero surface). BUT the pin is not
        one constant with seven call sites as the prose above says: there are
        EIGHT version-bearing header sites and only SIX reach the version
        through src/lib/square.ts:4. src/app/api/square/callback/route.ts:43 and
        src/app/api/square/locations/route.ts:44 HARDCODE "2024-01-17" and never
        import the constant. Editing line 4 alone would have Froot calling POST
        /oauth2/token at two different API versions from two different files
        inside the same OAuth flow. No changelog surfaces this; only the grep
        does. (3) THE SHIFT RETIREMENT ALREADY HAPPENED AND THIS ROW SURVIVED
        IT. Retirement was 2026-05-21, 89 days before the survey ran — LABOR-0
        was written 2026-08-02 to get ahead of a deadline that was already
        behind it. Zero labor hits re-verified 2026-08-18 (Shift endpoints,
        labor.shift.*, Timecard, SearchTimecards, BreakType, WorkweekConfig,
        TeamMemberWage), so the surface is still clean and no Shift-migration
        row is warranted. (4) THE WEBHOOK HANDLER SWALLOWS UNKNOWN EVENT TYPES.
        src/app/api/webhooks/square/route.ts:71-73 returns HTTP 200 with NO log
        line for any type outside the four it handles. This row's leading ingest
        option is webhook-driven: add labor.timecard.created/updated to the
        subscription before the handler learns them and every delivery is
        200-ACKed and dropped with no trace, and Square does not retry a 200.
        Whoever builds the ingest owns that. THREE THINGS THE SURVEY SURFACED
        AND DELIBERATELY DID NOT RESOLVE, all Gary's: (a) FROOT DOES WRITE TO
        SQUARE TODAY — updateSquareTeamMemberName (src/lib/square.ts:227-254)
        issues PUT /v2/team-members/{id}, live via
        src/app/api/staff/[id]/square-writeback/route.ts:46, and it is not OAuth
        machinery, so the pending "Froot never writes to Square" ruling must
        either carve it out by name or the route is in breach. (b) That write
        cannot be authorized by Froot's OAuth token — EMPLOYEES_WRITE is not
        requested — so on the code path as written it can only land via the
        SQUARE_ACCESS_TOKEN personal-token fallback (src/lib/square.ts:236),
        a credential not constrained by the merchant's consent grant. FLAGGED,
        NOT DIAGNOSED; not executed at runtime. (c) REPORTING_READ MAY NOT
        EXIST: the Reporting API overview mandates it, while the OAuth
        Permissions Reference and the OAuthPermission enum both omit it
        entirely. A string absent from the enum cannot go in a consent URL and
        may hard-fail the whole authorize request for every merchant, so this
        must be settled BEFORE the batch if Reporting is in scope at all. Also
        unresolved: Square documents the scope/Square-Version relationship in
        NEITHER direction, so the planning lean that grants are version-
        independent is neither confirmed nor refuted at source — the survey
        declined to assert it. What IS documented is that timecard and
        scheduled-shift endpoints require 2025-05-21 or later regardless, so
        both remain necessary. NOTE FOR THE NEXT SESSION: LABOR-0B's own summary
        of Task 1c ("sites where the word shift is spent") CONTRADICTED the
        LABOR-0 file, whose Task 1c is the Square CLIENT construction-site
        count. The file won, per its own Task 0, and this row's wording at the
        prose above is the one that matched. NONE OF THIS LIFTS THE BUILD
        DEFERRAL, which is a separate ruling recorded in its own blocker below,
        and none of it is acted on — the survey was read-only by mandate.
        ORIGINAL ENTRY, PRESERVED VERBATIM:
        LABOR-0 HAS NEVER FULLY RUN (docs/prompts/LABOR-0_shift_surface_grep.md)
        — an unexecuted prompt filed in 2e75029. The 2026-08-05 partial covered
        Tasks 2-4 only and answered two load-bearing questions in this row's
        favour; Task 1c (construction-site count) and Task 5 (webhook inventory)
        are still owed. RUN IT BEFORE ANY BUILD.
      - >
        CLEARS WHEN all four are done: LABOR-0 Tasks 1c and 5 run; SQUARE_VERSION
        bumped past the Labor floor; the labor scopes added; and EVERY MERCHANT
        RE-CONSENTS. The re-consent is the expensive part and is why CLAUDE.md
        forbids adding a scope opportunistically — one addition re-consents every
        connected merchant, so L-2's and L-4's scope additions must be batched
        into ONE consent moment (see L-4).
        THE ONLY DATED PRESSURE ON THIS ROW: Keva moves all stores to Square
        Scheduling by SEPTEMBER 2026. That argues for scoping the work, not for
        starting it — the build is deferred by ruling, not by the clock.
      - >
        THE BUILD DEFERRAL IS LIFTED — 2026-08-18 (Gary, DECISIONS.md "The L-2
        deferral is lifted — Advanced Labor is the build", recorded in commit
        8410023). Prepended; the original entry is preserved verbatim below and
        is now HISTORY, not a live blocker. Gary's words: the 2026-08-05 deferral
        did its job — the seam got designed, the survey ran, the scopes were
        batched and consented, and the app went read-only before any merchant was
        promised that. THE ADVANCED LABOR FORECASTING VISION IS WHAT L-2 BUILDS.

        THE LIFT CHANGES WHAT MAY BE BUILT, NOT THE ORDER. The gates Gary
        restated with it still stand and are sequenced: version bump FIRST (done
        2026-08-18, SQ-VER-1 work commit b846e32, staged not promoted),
        PRODUCTION CONSENT at promotion, then BUILD SESSIONS ONE PHASE AT A TIME.
        A lifted deferral is not a licence to build the row in one pass.

        WHAT DOES NOT CHANGE WITH THE LIFT: the strictly-optional ruling of
        2026-08-05 stands untouched — Square labor is an OVERLAY and can never
        become a floor under forecast-driven labor, seam (b)'s boundary test
        still has to pass, and the seam design below is still the design. The
        lift removed the "do not start" clause and nothing else. Note also that
        the original entry's pointer to "DECISIONS.md top entry" now resolves to
        the WRONG entry — the 2026-08-05 ruling has three newer entries above it,
        of which the lift is one; read it by title, not by position.

        NO ADVANCED LABOR FORECASTING DOCUMENT EXISTS IN THE REPO. `grep -rli
        "advanced labor"` over docs/ and froot_docs/ returned nothing on
        2026-08-18, so the vision the lift names is not written down anywhere a
        build session can read. Filing it was outside SQ-VER-1's scope; whoever
        opens the first build phase needs it to exist, or the phase will be
        scoped from chat memory.

        ORIGINAL ENTRY, PRESERVED VERBATIM: "BUILD IS DEFERRED BY RULING (Gary,
        2026-08-05, DECISIONS.md top entry), which is a DIFFERENT thing from the
        three gates above and is recorded here so the two are never conflated.
        Even with every gate cleared this row does not start until the deferral
        lifts: the Square labor integration is strictly optional and can never
        become a floor under forecast-driven labor. This row is the seam design,
        written so a future build slots in without tearing anything down."

  - id: L-3
    track: labor
    size: L
    status: shipped
    shipped: 2026-07-21
    commits: [a599aea, ea00f28, 0a7c1f1]
    title: "Weekly Plan report — floor-first daily split, GM 40-hr cap, cross-day rebalancing"
    notes: >
      Promoted to prod 7-21 via merge 9743899 (carried the full HR/Labor
      backlog; main was 53 commits behind). Coverage stays sales-inferred for
      v1 per decision.
      THAT DECISION WAS RE-AFFIRMED 2026-08-05, not superseded. A 2026-08-02
      draft had Square actuals replacing the sales-inferred proxy; Gary's ruling
      makes the Square labor integration optional, so actuals are an OVERLAY and
      the proxy stays the always-on path. StoreHours below remains the coverage
      upgrade path — Square is not it. See L-2 and DECISIONS.md (top entry).
    deferred:
      - Populating StoreHours with real operating hours (open windows inferred from trailing sales until then) — still the upgrade path; NOT replaced by L-2's Square actuals (2026-08-05 ruling)

  - id: L-4
    track: labor
    size: L
    status: planned
    title: "Labor assignment layer — named-person shifts, GM days off, per-person breaks, Square push"
    notes: >
      Confirm L-3/L-4 order when scoping past L-2. Shares Square OAuth scopes
      with L-2.
      CROSS-NOTE 2026-08-05 — THE SQUARE PUSH HALF INHERITS L-2's WHOLE SEAM.
      Gary's ruling (DECISIONS.md, top entry) makes the Square labor integration
      strictly optional, and write-back is the same integration pointed the other
      way: it sits behind the same squareLaborEnabled toggle, off by default, and
      the assignment layer must remain fully usable as a Froot-only scheduler
      with the toggle off or Square down. Write-back needs TIMECARDS_WRITE on top
      of L-2's read scopes — the same re-consent event, so scope additions for
      L-2 and L-4 should be batched into ONE consent moment rather than two.
      Naming: SquareScheduledShift, never a bare `Shift` — see L-2 DON'T #2, the
      word is already spent twice in this codebase.
    blockers:
      - >
        BEHIND L-2 — promoted from this row's prose to a blocker entry
        2026-08-06 so the board can tell this row from a ready one. The Square
        push half inherits L-2's whole seam: the same squareLaborEnabled toggle,
        off by default, and the assignment layer must stay fully usable as a
        Froot-only scheduler with the toggle off or Square down. So every gate
        on L-2 is a gate here — read them there rather than restated here, which
        is how they drift.
        THIS ROW ADDS ONE OF ITS OWN: write-back needs TIMECARDS_WRITE on top of
        L-2's read scopes. That is the SAME re-consent event, which is the whole
        argument for batching L-2's and L-4's scope additions into one consent
        moment rather than re-consenting every merchant twice.
        CLEARS WHEN: L-2 clears, with L-4's write scope included in that single
        consent. The Froot-only assignment layer — everything except the Square
        push — is not gated by any of this and could be scoped independently.

  - id: CRON-1
    track: labor
    size: S
    status: planned
    title: "Register the labor-timecards cron on production"
    notes: >
      FILED 2026-08-19 BY RULING, NOT BY NEED — this row exists so the
      deliberate absence stays findable, and its whole content is a decision to
      not act yet.
      THE ROUTE IS BUILT AND IS DELIBERATELY ABSENT FROM vercel.json.
      `/api/cron/labor-timecards` shipped with AL-1 (be705a2) and went to
      production in 5e2f4d7 with everything else, so it is live and callable —
      there is simply no schedule pointing at it. That is the AL-1 Q7 ruling
      (Gary, 2026-08-18): build the route, register no cron, and pick the
      cadence once Phase 2 gave the number something to be fresh FOR. Read the
      missing vercel.json entry as this ruling, never as an oversight to be
      helpfully corrected.
      GARY'S 2026-08-19 RULING — BACK BURNER. Dashboard-triggered sync is
      sufficient. The sync runs when someone opens the labor surfaces, which is
      also when anyone cares about the number, so a schedule would mostly warm
      data nobody is looking at.
      REVISIT TRIGGER, and it is two specific events rather than a date: the
      FIRST STALENESS COMPLAINT from the team, or the FIRST ALERTING FEATURE.
      Both describe the same underlying change — a consumer that reads labor
      data when no human just opened a page. Alerts and end-of-day reports need
      always-warm data by construction; dashboards do not. Until one of those
      exists there is nothing for a cron to serve.
      SIZE S IS THE REGISTRATION ONLY. Picking the cadence is the actual
      decision (AL-1 Q7 left hourly-during-business-hours and nightly both on
      the table) and it should be made against whichever trigger fires, since
      an alerting feature and a staleness complaint would not want the same
      schedule.
      SCOPE NOTE — THE ROSTER HAS NO SCHEDULED REFRESH EITHER, and it is a
      separate question from this one. AL-3's roster sync is a manual "Sync
      roster" action by design: a wage setting moves when somebody gets a
      raise, not on a clock. Do not fold it into this row's cadence decision.
      ID NOTE: `CRON-` is a NEW id prefix as of this row — no other row carries
      it — and the track is `labor` rather than `build`, because what is parked
      is a labor-data freshness decision and not a tooling change. Same
      reasoning the `staff` track note on SQ-4 records.
      STATUS NOTE, AND IT IS A DEVIATION FROM WHAT WAS ASKED. Gary specified
      status `parked`. There is no such value — PhaseStatus in
      src/lib/roadmap.ts is a closed union (planned | in_progress | staging |
      shipped | verified | withdrawn) and src/generated/roadmap.ts is typed
      against it, so `parked` fails the build typecheck rather than rendering
      oddly. `planned` is the closest true statement: the work is real, it is
      not started, and it is not blocked by anything except the absence of a
      reason. THE PARKING LIVES IN THIS PROSE, which is weaker than a status
      value would be — a board reader sees "planned" and has to open the row to
      learn it is on the back burner behind a named trigger. If `parked` should
      exist as its own value, that is Gary's call and it is the same shape as
      the ruling that added `withdrawn` on 2026-08-01; it would touch the
      union, BOARD_COLUMNS and the board's column mapping, which is code and
      out of scope for a recorder commit. BUILD-3 hit the identical wall the
      same day with `closed` and resolved it the same way, so this is now a
      pattern rather than a one-off.

  # ---- Schedule/actual overlay (labor-schedule) ----
  # Session split ratified 2026-08-20: S1 discovery -> S1b probe -> S2 ingest
  # -> S3 card overlay -> S4 /labor comparison. One phase per session.

  - id: OVL-S1
    track: labor
    size: S
    status: verified
    shipped: 2026-08-20
    commits: [de8e7cf, 6e1510f]
    title: "Schedule/actual overlay — S1 seam audit and scope rulings (discovery, docs only)"
    notes: >
      BACKFILLED 2026-08-20 during S2, because the board had no row for the two
      discovery sessions the whole overlay track rests on and their rulings were
      only findable in docs/DECISIONS.md. Filed after the fact rather than
      pretended into existence earlier — the dates below are when the work
      happened.
      NO CODE SHIPPED. A read-only session that audited the three seams against
      HEAD and took Gary's scope rulings before any build was scoped. `verified`
      is the closest the board's vocabulary comes to "audit-only, no shipped
      code" — the same gap SQ-1 sits in, recorded at the top of this file.
      WHAT IT DECIDED, and the decisions are in docs/DECISIONS.md § "Schedule/
      actual overlay, scope rulings" (2026-08-20) rather than restated here:
      ingest is sync-and-cache cloned from timecards and never fetch-on-view;
      position colours are deterministic-by-default with a settings override;
      the overlay is STORE-visible and shows counts by position, never wages,
      with a PERM-5 capability override to turn it off; scheduled and actual
      curves are DISPLAY OVERLAYS on coverage output only and never enter the
      demand shape, budget or recommendation path (seam b); a sync failure
      renders last-synced and LABELLED STALE rather than a blank pretending no
      schedule exists (seam c).
      IT ALSO RULED THE SHAPE OF THE TRACK: scout first, one phase per session,
      S1 -> S2 -> S3 -> S4. Everything filed under OVL- follows from that.
      The commits carry the rulings and the S1b session prompt; there is no
      application code in either.

  - id: OVL-S1b
    track: labor
    size: S
    status: verified
    shipped: 2026-08-20
    commits: [0f8706c]
    title: "Schedule/actual overlay — S1b ScheduledShift API probe against the real Square account"
    notes: >
      BACKFILLED 2026-08-20 during S2, same reason as OVL-S1. THE REPORT IS THE
      DELIVERABLE and it is committed at
      docs/prompts/Overlay_S1b_Probe_RESULTS.md (LABOR-0B_RESULTS.md precedent) —
      read it rather than this summary, because S2's schema doc-comments cite it
      by count and a future session changing a nullability needs the measurement,
      not a paraphrase.
      RAN UNDER A NAMED ONE-TIME EXCEPTION to the Neon-console-only rule
      (docs/DECISIONS.md § "Staging probe exception, schedule payload
      discovery", 2026-08-20): ONE read-only SELECT against the staging database
      for the organization id and access token, solely to make read-only Square
      calls with the org token. No other staging read, no staging write, no
      getSquareClient (it writes tokens back), no production contact, and every
      pulled env file and scratch artifact deleted before the session ended. The
      exception covers that probe only; any future need is its own ruling.
      WHAT IT MEASURED — 462 distinct shifts, real Keva Square account,
      Square-Version 2026-01-22, 19 locations, window 2026-08-06 -> 2026-09-17.
      THE HEADLINE FINDING IS THAT SQUARE'S CURSOR IS UNSAFE HERE. Against a
      ground truth of 976 shifts (one request per location per day, each
      verified single-page): all 18 locations batched following the cursor
      retrieved 224 and missed 752 (77%); per-location following the cursor
      retrieved 751 and missed 225 (23%); per-location with weekly windows
      retrieved all 976. The cursor decodes to (start_at, shift_id) and page 1
      of a sweep is an interleaved merge across the whole window rather than a
      time-ordered slice, so resuming skips earlier rows PERMANENTLY and Square
      then reports the cursor exhausted. The same defect duplicates — one
      location returned 114 rows over 3 pages, 76 unique. The timecard sync's
      do…while(cursor) loop must NOT be cloned, and that inversion became a
      binding ruling rather than a note.
      FOUR OTHER FINDINGS THAT CHANGED THE DESIGN. (1) An UNKNOWN FILTER FIELD
      RETURNS 200 WITH THE FULL RESULT — a typo does not fail, it silently
      widens the window, which is why S2 builds its filter from typed constants
      and asserts at runtime that the filter was applied. (2) "No schedules" and
      "nonexistent location" are byte-identical 200 {}, so seam (c) cannot be
      served from the response and needs its own sync-state table — eight of
      eighteen active locations answer exactly that today, mid Square-Scheduling
      rollout. (3) limit caps at EXACTLY 50; the timecard endpoint's documented
      200 does not transfer. (4) job_id is the sole position reference, GET
      /v2/labor/jobs 404s, and only 1 of 7 estate job ids matches a
      LaborPosition name — which killed the colour-on-LaborPosition idea and
      produced the SquareJobColor ruling.
      NO BETA HEADERS EXIST on the endpoint — no Warning, Deprecation, Sunset or
      rate-limit header — so there is nothing to detect a future breaking change
      from. That is the argument for pinning SQUARE_VERSION and re-testing on
      every bump, and it is the one finding with no code attached to it.
      The rulings it produced are docs/DECISIONS.md § "Schedule overlay, S1b
      rulings". CLAUDE.md's Square-scopes section was corrected in the same
      commit: scheduled-shift reads ride TIMECARDS_READ, verified live.

  - id: OVL-S2
    track: labor
    size: M
    status: in_progress
    commits: [a56a2e8]
    title: "Schedule/actual overlay — S2 ingest: SquareScheduledShift tables, sync module, fixtures"
    notes: >
      BUILT 2026-08-20, work commit a56a2e8. STATUS IS in_progress AND NOT
      staging, DELIBERATELY: the commit is local and NOT PUSHED (pushes are
      Gary's, CLAUDE.md Git Rules), so nothing has reached the staging branch or
      a Vercel preview yet. Flip to `staging` when the push happens — BUG-9's
      precedent, where the row was filed against the built work and promoted
      afterwards rather than claiming a deploy that had not occurred.
      NO UI IN THIS PHASE. The card toggle, the colour rendering and the
      settings editor are S3; the /labor comparison is S4. This session is
      ingest, tables and fixtures only.
      THREE ADDITIVE TABLES, migration
      20260820170000_ovl_s2_scheduled_shift_ingest, applied to the DEV branch
      only — staging and production ride the normal deploy path.
      SquareScheduledShift carries draft, published and effective columns.
      EVERY NULLABILITY IS AN OBSERVED FACT from the S1b report, not a guess:
      draft_shift_details was present on 462/462, so the draft location, job,
      start, end, timezone and is_deleted columns are NON-NULL and only
      draftTeamMemberId (450/462) and draftNotes (24/462) are nullable; the
      published columns are all nullable because published_shift_details is
      ABSENT — not null, not empty — until a manager publishes (399/462).
      The effective columns denormalize published ?? draft at write time so the
      coalesce cannot be forgotten by one caller and remembered by another, and
      effectiveSource records WHICH SIDE WON so the S3 legend can say "this is
      the manager's draft" without inferring it from a null check that will one
      day be null for another reason. There is deliberately no effectiveNotes.
      SquareScheduleSyncState is a clone of SquareLaborSyncState with its own
      storeId @unique rather than a shared row: the two syncs fail independently
      and one lastError column cannot hold two sentences. Its purpose is seam
      (c) — lastSyncOkAt set with lastShiftCount 0 is the ONLY way "we asked and
      Square said nothing" is distinguishable from "never synced".
      SquareJobColor is keyed (organizationId, squareJobId) and holds OVERRIDES
      ONLY. A row's absence is not missing data: deterministicJobColor() gives
      every job id a stable colour from the badge palette (grey excluded — grey
      is what badgePreset() returns for an unrecognised key, so a job
      legitimately assigned grey would be indistinguishable from a broken one),
      which means the overlay is fully coloured with the table empty and the
      writer can wait for S3.
      THE FETCH PROTOCOL IS THE LOAD-BEARING PART, and it INVERTS the timecard
      sync. One location_id per request, weekly windows, limit 50. A returned
      cursor means the window was too big: the page is DROPPED (its 50 rows are
      an interleaved sample of the whole window, not its first 50, so keeping
      them would duplicate some and still miss others), the window is halved,
      and both halves are re-queried from scratch. A ONE-DAY WINDOW THAT STILL
      PAGINATES THROWS — SQUARE_SCHEDULE_WINDOW_UNSPLITTABLE — rather than
      storing a partial day, because a truncated schedule renders as "nobody is
      scheduled", which is exactly the sentence seam (c) exists to forbid.
      assertFilterApplied() is the runtime tripwire for S1b's other trap: an
      unknown filter field returns 200 with the full result, so a
      request-construction mistake would otherwise land silently in the
      database. Location is checked exactly; dates carry one day of slack in
      each direction, because a draft/published pair straddling a boundary is
      real (22 of 399 differ) whereas an ignored filter misses by weeks.
      NOTES ARE SYNCED AND NEVER SELECTED. The columns exist on the table;
      getScheduledCoverage's select list omits them. Enforced by NOT FETCHING,
      never by filtering afterwards — observed live notes already carry a
      person's name and the overlay reaches a STORE-visible surface.
      THE TRIGGER IS A SECOND, SEPARATE ONE — its own 30-minute cooldown, its
      own cap of 2 stores per load and its own deferral log, wired to the same
      three dashboard sites the timecard refresh uses and behind the SAME
      existing laborOverlayOn gate (no new ruling). Separate rather than a
      branch inside scheduleLaborRefresh because a schedule sync costs ~5 Square
      requests to the timecard sync's 1, and a shared claim column would make
      either one's failure look like the other's. Its window is the card horizon
      (-3/+28 days), not today: a schedule is about days that have NOT happened,
      so syncing today only would leave the week ahead blank however often it
      ran.
      /api/cron/labor-scheduled-shifts is built and DELIBERATELY ABSENT from
      vercel.json — the same ruling, and the same shape, as
      /api/cron/labor-timecards (see CRON-1). Read the missing entry as the
      ruling, not as an oversight to be helpfully corrected.
      SEAM (b) VERIFIED BY GREP, not by assertion: labor-plan, labor-coverage,
      labor-budget, labor-forecast, labor-week and labor-daily contain no
      reference to labor-schedule. The only importers are labor-dashboard, the
      cron route and the fixture.
      scripts/verify-labor-schedule.ts — 44 checks, pure, no DB and no network.
      The protocol itself is under test, not just its helpers:
      collectScheduledShifts takes an injected page-fetcher so the fixture can
      prove that a cursor is dropped rather than followed, that the paginated
      page is discarded rather than merged, that both halves are re-queried in
      calendar order, that an unsplittable day throws, and that a
      silently-ignored filter throws. Also: effective = published ?? draft
      including the differs and draft-only cases and the case where a published
      shift legitimately drops its team member; an unassigned shift still
      counting toward coverage; overnight shifts splitting at store-local
      midnight; the upsert guard's predicate; colour stability; and a structural
      assertion that no notes and no team-member field appears anywhere in the
      read function's serialized output.
    open:
      - >
        THE LITERAL REQUEST BODY IS THE ONE THING S1b's REPORT DOES NOT RECORD.
        The endpoint is confirmed — POST /v2/labor/scheduled-shifts/search on
        TIMECARDS_READ, verified live 2026-08-20 (CLAUDE.md § Square scopes) —
        but the report captures the payloads, the pagination behaviour and the
        error matrix without ever quoting the query object the probe sent. S2
        therefore builds the filter as
        `query.filter.location_ids` plus `query.filter.start.{start_at,end_at}`
        from typed constants. That is the documented ScheduledShiftQuery shape
        and it is consistent with everything the report DOES record — windows
        expressed as date ranges, and a cursor keyed on start_at — but it is
        reconstructed rather than copied, and S1b § 4 proved a wrong filter key
        fails SILENTLY with 200 and the wrong data.
        WHY THIS IS NOT LEFT AS A HOPE: assertFilterApplied() turns exactly that
        failure into a thrown SQUARE_SCHEDULE_FILTER_IGNORED on the first page
        that comes back wrong, so a bad guess surfaces as an unhealthy store
        with a named error rather than as silently wrong rows. CLOSES WHEN S3
        runs the sync against staging and the log shows real shifts landing in
        the right windows — which S3 needs anyway before it can build a card
        against synced data.

  # ---- Permissions ----

  - id: PERM-1
    track: permissions
    size: M
    status: shipped
    shipped: 2026-07-26
    commits: [6f70465]
    title: "Permission capability shim — enforcement inventory, capability registry, can()/scope() shim, sidebar pilot"
    notes: >
      Audit-first session (docs/prompts/PERM-1_permission_capability_shim.md).
      Zero behavior change by definition. Enforcement inventory + capability
      registry delivered 2026-07-25 in docs/PERMISSIONS_INVENTORY.md (also
      records the security-gap and contradiction lists feeding later PERM
      phases). Shim built: src/lib/permissions.ts (can()/scope(), deny-by-
      default, hardcoded role grants). Pilot: sidebar nav filter migrated to
      can(); parity verified — 192/192 role×gate permutations byte-identical
      to the old roles-array filter; next build green. Sidebar alerts-badge
      role check deliberately NOT migrated (pilot = nav filter only).
      Staging pass clean 2026-07-25 (Gary — no nav differences across all
      four roles). Built 7-25; prod via c463af3 (2026-07-26). The two blockers
      below were NOT cleared by the promotion — they gate PERM phase 2, not
      this one.
      Promotion path re-verified 2026-07-27: `git branch -a --contains 6f70465`
      returns main and origin/main, and `git log --ancestry-path
      6f70465..origin/main` shows the first merge on the path is c463af3
      (2026-07-26) — NOT 1ba059c, the 2026-07-25 HR-17 promotion.
      `git merge-base --is-ancestor 6f70465 1ba059c` returns false. The
      shipped date below (2026-07-26) is correct as written; a 2026-07-25 /
      HR-17 attribution was considered at the 7-27 reconcile and rejected on
      this evidence.
      BOTH BLOCKERS CLOSED 2026-07-27 (reconcile Pass 2a). Text retained here
      rather than deleted, per this file's convention — the history is the
      point.
      CLOSED #1 — was: "Phase 2 (call-site migration) must not start until Gary
      rules on each contradicted call site in PERMISSIONS_INVENTORY.md §3 — the
      shim encodes one answer per capability; migrating a contradicted site
      silently picks a side." SATISFIED BY PERM-2, which shipped to production
      2026-07-27 (979da0b, promoted in 06b1561) and whose own notes state it was
      "clearing PERM-1's phase-2 blocker: Gary ruled on every contradicted call
      site before migration." The condition was met on 07-26 and nobody returned
      to close the blocker — precisely the drift Pass 2a exists to catch.
      CLOSED #2 — was: "Square OAuth callback state validation (§2 gap #3) is
      SEC-1, its own phase — logged here, unfixed in PERM-1." This was a pointer,
      never a gate on PERM-1's own work, and SEC-1 shipped 2026-07-26 (ecee728,
      via c463af3). Nothing outstanding.

  - id: PERM-2
    track: permissions
    size: L
    status: shipped
    shipped: 2026-07-27
    commits: [979da0b, 06b1561]
    title: "Resolve permission contradictions — checklists, templates, staff, inventory split, dashboard goal, weekly plan"
    notes: >
      Audit-first session (docs/prompts/PERM-2_resolve_permission_contradictions.md),
      clearing PERM-1's phase-2 blocker: Gary ruled on every contradicted call
      site before migration. Task 1 audit found POST /api/checklists
      INSTANTIATES a store's daily checklist from a template (it does not
      create a definition) and is the ONLY way an instance is ever created —
      no cron generates them — so the proposed ADMIN-only lock would have
      stopped stores from opening. Resolved §3 #2 (templates ADMIN-only, layout
      closed), #3 (GET /api/staff ADMIN/MANAGER; POST /api/staff was entirely
      unguarded — security fix), #4 (checklist instantiation ADMIN/MANAGER/
      STORE + StoreUserAssignment scope, bulk fan-out ADMIN-only), #5
      (inventory split by data sensitivity — operational vs commercial, 21
      routes gated), #6 (dashboard goal PUT ADMIN-only, matching Forecasting)
      and #8 (Weekly Plan nav for all roles). #1, #7 and #9 deliberately
      untouched. Two new capabilities: inventory.costs.view and
      inventory.nav.view — the latter keeps nav visibility separate from API
      access so STAFF-1's sidebar exclusion survives operational grants going
      to ALL. Every decision goes through can(); no inline role check added,
      two dead ones removed. One deliberate scope exception, logged in
      DECISIONS.md: a cross-tenant templateId hole inside the function already
      being changed was fixed here rather than deferred.
      STAGING VERIFICATION PASS COMPLETE 2026-07-27 (Gary), Tommy Thomas driven
      as STORE and then MANAGER, full results in
      docs/prompts/verification-smoke-pass.md. Negative checks all passed:
      S1 `POST /api/staff` as STORE → 403 (this route was ENTIRELY UNGUARDED
      before PERM-2 — the inventory's worst-first finding, now closed by
      observation and not just by code review); S2 `GET /api/staff` as STORE →
      403. Regression guards passed: a STORE can still start AND complete a
      checklist — the instantiation path Task 1 identified as the one that would
      have stopped stores opening had the proposed ADMIN-only lock shipped — and
      a STORE sees Weekly Plan in the nav, confirming the §3 #8 widening.
      PARTIAL: the inventory pars and storage-areas routes returned 400
      "storeId is required" rather than a role denial. Not denied, but not
      proven either — role-check ORDERING on those two routes is unverified,
      since a 400 that precedes the role check would look identical from the
      client. Worth a targeted re-test with a valid storeId.
      NOT COVERED: the STAFF role was not exercised at all in this pass. Known
      coverage gap, recorded rather than glossed; pick it up with HR-9, which
      splits EMPLOYEE off STAFF and has to re-walk these same routes anyway.
    deferred:
      - >
        RESOLVED 2026-07-27 by PERM-6 Task 1. POST /api/staff now runs both
        checks, widest blast radius first: an org-ownership count (closing the
        CROSS-TENANT hole this text had understated) and then, for non-admins,
        a caller-scope check against getUserStoreScope().storeIds — org
        membership is the floor, not the ceiling, which was this entry's
        original point. Body is Zod-parsed and storeIds deduped. ORIGINAL TEXT
        FOLLOWS:
        POST /api/staff accepts a storeIds array with no check that those
        stores are in the caller's scope — a MANAGER can assign a staff member
        to stores they do not manage. Tier is now correct; the scope hole is
        its own fix. Found in this session, deliberately not fixed.
        SHARPENED 2026-07-27 — this text UNDERSTATES the bug, confirmed by
        reading src/app/api/staff/route.ts:46-68. There is no org-ownership
        check on storeIds AT ALL, not merely a missing caller-scope check. The
        request body is the sole source of truth, so the hole is CROSS-TENANT
        (a storeId belonging to another organization is accepted) and not just
        cross-store. The route is MANAGER-reachable via
        can(role, "staff.manage"), so this is not admin-only. Fixed in PERM-6.

  - id: PERM-3
    track: permissions
    size: M
    status: shipped
    shipped: 2026-07-27
    commits: [b8f32bb, 06b1561]
    title: "MANAGER forecast read window (current + next month), forecasting store scoping, affordance visibility"
    notes: >
      Audit-first session (docs/prompts/PERM-3_forecasting_read_window.md).
      NOTE: this entry previously said MANAGER *and STORE* get a current+next
      month window on forecast AND sales data. Task 1 established that was
      wrong on both counts, and Gary's ruling (option c, 2026-07-26) replaced
      it: STORE and STAFF get NO forecasting access at all (the APIs already
      403'd them), and MANAGER's window applies ONLY to forward forecast goals —
      historical actual sales stay fully visible, because a manager needs last
      July's actuals to budget this July. ADMIN unrestricted. Writes unchanged
      and still ADMIN-only (§3 #7 deliberately not harmonized).
      Task 1 found the phase was a pure RESTRICTION with no grants: managers
      could read an unbounded number of forward years, and the two dashboard
      "Forecasting →" links (Monthly Goal card, rollup Store Ranking header)
      rendered for every role with no capability check at all, dead-ending in a
      redirect for STORE/STAFF — the PERM-2 bug class, UI-only.
      Enforced server-side by per-FIELD masking, never by a 4xx: rejecting or
      clamping the requested range would also withhold the historical actuals
      the ruling grants. /calendar and /export null `goal` outside the window
      (and `variance` with it, since actual − goal hands the goal back);
      /audit filters entries to window months. plan GET's annual aggregates stay
      visible to MANAGER by ruling. No new capability — forecasting.view gained
      a scope() window via the new SCOPE_OVERRIDES table, which is also where
      PERM-5's per-user overrides will hook in.
      Window is computed per request from Store.timezone in the new pure
      src/lib/forecast-window.ts, shared by the API guards AND the client year
      selector so they cannot drift; inclusive 1st-of-current-month through
      last-day-of-next, crossing the year in December. The year selector's
      forward edge derives from the window, so a manager in December can still
      select next January's year.
      Also fixed in scope (Gary): every forecasting read was org-scoped but not
      assignment-scoped, so a MANAGER could read any sibling store's forecast by
      passing its storeId — now mirrors requireLaborStore. Migrated
      forecasting-access.ts and forecasting/page.tsx off their inline role
      checks onto can(). See DECISIONS.md for Q1–Q4 and the accepted
      basis-reconstruction property.
      STAGING VERIFICATION PASS COMPLETE 2026-07-27 (Gary), Tommy Thomas driven
      as STORE and then MANAGER, full results in
      docs/prompts/verification-smoke-pass.md. Negative checks: S3 forecasting
      calendar as STORE → 403; S4 cross-store calendar as MANAGER → 403 (the
      assignment-scoping fix Gary added in scope — a MANAGER can no longer read
      a sibling store's forecast by passing its storeId); S5 `PATCH
      /api/forecasting/day` as MANAGER → 403 "Admin access required", confirming
      writes stayed ADMIN-only.
      MASKING CONFIRMED WORKING AS RULED, and this is the load-bearing result:
      the calendar returned `days: Array(365)` with the window computed as
      2026-07-01 → 2026-08-31, and June 15 came back with `goal` null while its
      `actual` of 2582.93 was PRESENT. That is per-FIELD masking, not range
      clamping — exactly the Q1 ruling, and the observable difference between
      the two implementations. `/api/stores` as MANAGER returned 1 of 12 stores.
    deferred:
      - >
        forecastWindowForCaller (used only by /audit, whose storeId is optional)
        picks the window's timezone by sorting the caller's assigned stores on
        storeNumber then name. Placeholder pending BUILD-2 (deterministic
        primary store), which is blocked on BUILD-1.

  - id: PERM-4
    track: permissions
    size: M
    status: planned
    title: "Strip cost/valuation fields from operational inventory payloads"
    notes: >
      PERM-2 §3 #5 split inventory by ROUTE; this splits it by FIELD. Several
      operational payloads still carry commercial data to callers without
      inventory.costs.view: counts GET (draftValue), purchase-orders reads and
      receive (unit costs, line totals), and ingredients GET (purchaseCost,
      costPerReportingUnit, vendorPriceDisplay — the route deliberately left
      open in PERM-2 because the shortage-message picker in messages-client
      depends on it). Deferred out of PERM-2 by Gary because field-level
      redaction is a new pattern across three surfaces and a missed field is a
      silent leak that looks closed; these fields are visible to any member
      today, so deferring is not a regression. BUILD IT AS ONE SHARED
      SERIALIZATION HELPER, not per-route logic.
      SCOPE ADDED 2026-07-27 — three more surfaces plus one hardening idea.
      For (a) and (b) the rule is API FIRST, then hide the affordance: hiding a
      button while the endpoint still answers is the PERM-2 bug class inverted,
      and PERM-3 already had to clean up two links that dead-ended in redirects.
      (a) `/api/forecasting/audit` → 403 for MANAGER, and the audit panel
      hidden. Before building, GREP FOR ALL AUDIT CONSUMERS — F-5 shipped a
      goal-edit audit log and it may render in more than one place, so a single
      panel is an assumption, not a finding. Note this SUPERSEDES PERM-3's
      window-filtering of /audit for MANAGER: if the route 403s outright, the
      filtering becomes dead code and should be removed rather than left as a
      second, weaker gate.
      (b) `/api/forecasting/export` → 403 for MANAGER, export button hidden.
      See the CSV-export entry in DECISIONS.md (2026-07-27) before scoping this:
      removing the export is exfiltration FRICTION, not a confidentiality
      boundary, because masked goals stay derivable from `basis` × 1.03. Build
      it knowing that, so the phase is not mis-sold as closing the leak.
      (c) Labor cost split — add `labor.costs.view` at the MANAGE tier,
      mirroring `inventory.costs.view` from PERM-2. Touch points verified in
      source 2026-07-27: src/app/(app)/dashboard/page.tsx:51 (`laborEnabled`)
      and src/lib/labor-access.ts:19 (`requireLaborView`). NOT the `labor.view`
      tier — that governs only the sidebar link, and narrowing it would reverse
      the §3 #8 nav widening the 7-27 pass just confirmed working for STORE.
      (d) Hardening idea: export a separate `requireForecastWriteContext()`
      alongside the existing context helper, so a write path that omits
      `{write:true}` becomes IMPOSSIBLE to express rather than merely unlikely
      to be written. Cheap, and it removes a whole class of silent-permissive
      mistake at the type level.

  - id: PERM-5
    track: permissions
    size: L
    status: shipped
    shipped: 2026-08-04
    # B's two, then Session C's range endpoints — the eleven between them are
    # contiguous and named in the notes rather than listed here.
    commits: [5119a6b, 24eb289, 6fb4b4f, 2a7e044, 72bcf30]
    title: "Per-user capability overrides — the toggle layer"
    notes: >
      Grant or remove a capability for ONE user against their role baseline —
      e.g. one staff member gets inventory-count access, or the inventory nav,
      while others do not. PERM-1's can()/scope() layer plus PERM-2's
      discipline (every decision routed through a capability, nav visibility
      kept separate from API access) is the substrate this depends on: there is
      one place to hook the override. Must keep the shim's rule that a stored
      permission set RESTRICTS BELOW the Clerk role ceiling and never elevates
      above it.
      SCOPE SHARPENED 2026-07-27 — three things changed, none of them the core
      idea.
      (1) RESTRICT-ONLY IS NOW A RECORDED RULING, not just an implementation
      note. See DECISIONS.md 2026-07-27: role choice at provisioning is
      unrestricted, while overrides stay restrict-only. Those are DIFFERENT
      MECHANISMS and were argued as if they were the same during scoping — an
      ADMIN device account is not an "elevated STORE user", it is a user whose
      role is ADMIN. If a future session finds itself asking PERM-5 to grant
      something a role does not already have, the answer is a different role,
      not a wider override.
      (2) PERM-7 DEVICE ACCOUNTS ARE THE FIRST REAL CONSUMER. The motivating
      case is no longer hypothetical: an admin dials an individual store's
      device DOWN from the STORE baseline — this location's iPad does not get
      labor, that one does not get inventory counts. Restrict-only is exactly
      the right direction for that, so PERM-5 and PERM-7 fit together without
      either bending. Design the override UI knowing the population may be
      one row per store rather than a handful of exceptional humans.
      (3) MANAGEMENT SURFACE GREW. With a device account per location, twelve
      stores means twelve override targets. That raises the value of UM-2's
      capability-matrix page rendered FROM THE LIVE REGISTRY, and makes a
      per-user diff beside the role baseline close to required rather than
      nice-to-have.
      SEQUENCE: after PERM-7. The devices have to exist before dialling them
      down is a real task, and PERM-6 (which clears the blocker below) is
      already a PERM-7 prerequisite — so the ordering falls out naturally as
      PERM-6 → PERM-7 → PERM-5.
      SPLIT INTO THREE SESSIONS 2026-08-03/04. A = the toggle-inventory audit
      (read-only, docs/prompts/PERM-5A_toggle_inventory_audit.md). B = the
      machinery (docs/prompts/PERM-5B_override_machinery_build.md). C = the
      call-site migration sweep that makes it reach.
      SESSION B BUILT 2026-08-04 — 5119a6b (schema + can() seam) and 24eb289
      (grid UI + footer). User.deniedCapabilities String[], additive; migration
      20260804123449_perm5_user_denied_capabilities. THE ONE RULE is structural
      rather than conventional: in can() the ceiling is evaluated FIRST and the
      only `return true` sits after it, so the override block below can subtract
      and nothing else. Three-state CapabilityOverride ({loaded,denied} |
      {loaded:false}) because a bare array's load failure yields [] and silently
      restores the full role baseline; overridesFrom() reads an UNSELECTED
      column as a failure, so a `select` that omits the field fails closed. The
      set loads once per request on the row getCurrentUser() already fetches and
      is threaded as `actor` through all 41 existing can()/scope() call sites.
      The grid renders a maintained ENFORCED_CAPABILITIES list (15), not the
      registry — 24 registry entries have no call sites and 15 are nav-only.
      Session C adds grid rows by appending to that one array.
      SESSION B VERIFIED ON STAGING 2026-08-04, against deployed SHA
      21a80e7c36c1cb2095b86a3ccf81496ed384c33f. Migration 20260804123449
      reached staging through the pipeline's own `prisma migrate deploy`, never
      by hand — which is what cleared the blocker below rather than any local
      action. The Task B4 principal was gary@keva.com (MANAGER, Carson),
      SUBSTITUTED for the prompt's kevajuice06@icloud.com: that is a production
      device account and B4 was never going to be run against production. Note
      the substitution rather than reading the pass as covering the account the
      prompt named. Result: denying inventory.analytics.view removed the nav
      entry, the pages and the APIs TOGETHER — PERM-2's discipline held on the
      first capability that ever exercised it — and forecasting was untouched,
      which is the whole point of a per-capability grid rather than a per-area
      one. Restoration verified by re-granting. The three negatives verified
      too, and they are the load-bearing half: there is NO control anywhere,
      UI or API, that grants a capability above the role ceiling; an
      unregistered capability is refused 400; and a denial of something the
      role does not grant stores nothing. The principal ended at zero denials.
      SESSION C BUILT 2026-08-04 — 11 work commits from 6fb4b4f to 2a7e044 plus
      this record, on staging. 39 inline role checks migrated
      across eight areas (staff, templates, stores, reports, dashboard,
      purchase orders, users, settings) plus one security fix. BOTH MOTIVATING
      EXAMPLES ARE NOW EXPRESSIBLE, which is the only thing this session was
      for: (a) staff.view on the /staff layout kills nav, page and API
      together; (b) the /staff/[id] page ran on ONE inline ADMIN||MANAGER flag
      driving edits, notes, documents, training and compliance alike — there
      was no seam between reading the page and acting on it, so no override
      could have expressed one. It splits into staff.manage / staff.notes.use /
      staff.documents.manage and an unchanged role check for the HR-derived
      tabs (HR is its own phase).
      NO BASELINE MOVED anywhere. Two disagreements were found and neither was
      resolved silently: /settings/labor is ADMIN+MANAGER and C2 proposed the
      ADMIN_ONLY settings.access for it — migrated to labor.manage instead
      (Gary's ruling), which matches exactly and is already what the sidebar
      asks for that link; and square.manage is ALL in the registry while every
      Square route enforces ADMIN, so those routes stay UNMIGRATED pending a
      ruling. Where a page affordance was deliberately STRICTER than the API it
      calls (the /staff list buttons are ADMIN-only over MANAGE-tier routes) the
      capability was AND-ed with the existing isAdmin rather than replacing it —
      swapping would have widened.
      THE GRID GREW 15 → 21 by appending to ENFORCED_CAPABILITIES, one place,
      exactly as B's protocol said: users.manage, templates.manage,
      staff.documents.manage, staff.notes.use, reports.view,
      inventory.po.manage. B's two held-out rows are both released.
      FOUR CAPABILITIES ARE MIGRATED BUT DELIBERATELY HELD OUT, each with its
      blocking gap named at the capability — stores.view (GET /api/stores is
      the shared store list and serves any member by design), dashboard.view
      (a denied user would have no landing page; /dashboard is where every
      other guard redirects, so the redirect target must change first),
      labor.manage (Labor governance is its own ruling), settings.access (its
      gap is the intended architecture — the toggle APIs belong to module
      governance, not to this page, so promotion needs a different argument
      rather than a finished migration). PATCH /api/users/[id] enforces
      ENFORCED_CAPABILITIES as the DENIABLE set, so none of the four can be
      denied by a hand-rolled request either — the hidden-page/answering-API
      state is not merely avoided, it is inexpressible.
      C4 DISCHARGED. B left a placeholder asking for a re-check once
      users.manage became load-bearing. It did; the argument holds; the
      placeholder is replaced by the argument in five steps at the site. The
      invariant is that at least one ADMIN always holds users.manage, resting
      on the self-edit refusal and the last-admin removal guard — both named in
      the comment so weakening either is visible.
      SESSION C VERIFIED ON STAGING 2026-08-04 (Gary), against deployed SHA
      bf87743b6135751aa90699fad4c4104e4e3a1a2e — local HEAD at the time of the
      pass, so the § Staging Verification precondition held. Principals:
      gary@keva.com (MANAGER, Carson) and karson@keva.com (ADMIN) on the
      staging org, org id recorded with the observations.
      BOTH MOTIVATING EXAMPLES CAME BACK YES, which is the sentence this whole
      phase existed to be able to write. (a) Denying the staff capabilities
      removed Staff from the nav, bounced /staff and /staff/[id], and refused
      /api/staff — with /inventory fully intact, which is the half that proves
      the grid is per-capability rather than per-area. (b) With ONLY
      staff.manage denied, /staff rendered and listed, and detail pages opened
      with no edit, invite or legal-name affordances while Notes and Documents
      stayed. That is the split of the single canSeeNotes flag paying off: the
      seam between reading a staff record and acting on it did not exist before
      this session, so (b) was not merely unimplemented, it was inexpressible.
      THE NEW TIERS ISOLATE. staff.notes.use denied alone removed the Notes tab
      and nothing else; staff.documents.manage denied alone removed the uploads
      block and nothing else. Both had zero call sites before C.
      SPOT-CHECKS: templates.manage denied on an ADMIN principal took effect —
      the caveat B's grid carried is gone. inventory.po.manage denied removed PO
      creation while list and receiving kept working, which is the view/manage
      split doing its job on the one surface where a coarse denial would have
      taken receiving away with it.
      THE NEGATIVES PASSED, and they are the load-bearing half. A denial of
      stores.view — migrated onto can() but deliberately ungridded — was refused
      400, so the deniable-list rule is live and the hidden-page/answering-API
      state is not reachable even by hand. An unregistered capability was
      refused 400 too.
      REGRESSION: at zero denials, MANAGER and ADMIN behaviour was byte-
      identical to pre-sweep. That is ruling 3's proof and the point of the
      whole session — it changed who ENFORCES, never who is ALLOWED. C4's
      lockout argument was verified in code at users/[id]/route.ts:100; no
      guard needed. All principals ended at zero denials.
      Session prompt: docs/prompts/PERM-5C_migration_sweep.md
    deferred:
      - >
        SHIPPED 2026-08-04: promoted in 999cbdc; migration 20260804123449
        applied to production via migrate deploy (build log confirmed); smoke
        test passed 2026-08-04 — grid renders on production, zero cross-org log
        lines, baselines unchanged at zero denials. DEPLOY_LOG entry same
        date. Still explicitly excluded from this phase: HR migration, the
        ungoverned surfaces (operational inventory, /messages, /store-view,
        /labor), Users/Settings view/manage splits, Settings toggle APIs,
        per-user-per-store granularity, the square.manage baseline ruling.
        The promotion was a FAST-FORWARD of 32 commits (de3ba40..999cbdc), so
        no merge commit exists and `git revert -m 1` does not apply — the
        DEPLOY_LOG entry carries the reverse-order revert list. Smoke test ran
        on www.usefroot.com under org org_3FhYUR4l0ue7egug1I0Ig8wxOVn.
      - >
        RESOLVED 2026-08-06 by R4 (Gary): THE REGISTRY WAS WRONG. square.manage
        is now ADMIN_ONLY in permissions.ts, matching the enforcement every
        Square route already applies. This closes the entry BELOW, which stays
        for the record per the house convention — a resolved `deferred`/`open`
        entry is closed by the entry prepended above it (DEBT-42), the same
        shape as the SHIPPED entry at the top of this list.
        NO BEHAVIOUR CHANGED. Nothing calls can(_, "square.manage") — verified
        at head — and it is not in ENFORCED_CAPABILITIES, so it was never
        deniable from the /users grid either. The old value was documentation
        that lied, and the cost of the lie was exactly what the entry below
        describes: the routes could not be migrated, because migrating onto ALL
        would have handed every member the org's Square connection.
        THE SQUARE ROUTES ARE STILL UNMIGRATED — they keep their inline
        requireAdmin(). What changed is that migrating them is now a
        zero-baseline-change sweep rather than a decision. Consistent with L-2,
        whose optional Square-labor toggle is specified ADMIN-only.
      - >
        SQUARE ROUTES UNMIGRATED, pending a ruling. square.manage is ALL in the
        registry while /api/square/{auth,disconnect,catalog/sync,sales-items/
        sync} all enforce ADMIN. Migrating onto the registry value would hand
        every member the Square connection; migrating the registry down to
        ADMIN_ONLY is a restriction, not a migration. Named in permissions.ts
        since PERM-1 and still the honest answer: the whole surface needs one
        decision.
      - >
        HR migration (35 routes, 13 pages) — its own future row per the audit.
        PERM-5C touched its edge deliberately without entering it: the
        /staff/[id] HR-derived tabs keep their inline role check, commented, so
        the HR phase is not begun in the file least likely to be reviewed as
        part of it.
      - >
        Per-user-per-store granularity is OUT and recorded as a limitation:
        kevajuice14@icloud.com is one MANAGER device across Las Brisas + South
        Reno, so "no labor at one, yes at the other" is not expressible per
        user. The v1 answer is two device accounts; the grid says so in copy.
    blockers:
      - resolved: true
        text: >
          RESOLVED 2026-08-04 — 20260804123449 reached staging via the Vercel
          build's `prisma migrate deploy`, and the Task B4 pass then ran green
          against 21a80e7 (see the verification note above). PRODUCTION HAS NOT
          APPLIED IT and will not until staging is promoted; that is the normal
          pipeline, not an outstanding blocker. Original text kept below,
          because the shape note in its last sentence is still live guidance for
          anyone writing the next blocker. ORIGINAL TEXT FOLLOWS:
          MIGRATION NOT YET APPLIED ANYWHERE. 20260804123449 is committed but has
          not been run against the local dev DB, and staging/production apply it
          via `prisma migrate deploy` on their next Vercel build. Any page or
          route that reads a User row will error until it lands, so the Task B4
          staging pass cannot run before it. Migration SQL:
          ALTER TABLE "User" ADD COLUMN "deniedCapabilities" TEXT[].
          (Written as a plain string, not `resolved: false` — the generator
          infers `resolved: true` as a literal type from the existing entries and
          a false one fails `next build`. Same defect P-4 tracks from the other
          side: blocker state is not modelled.)
      - resolved: true
        text: >
          RESOLVED 2026-07-27 by PERM-6 Task 5 — kept in full below as the record
          of what was wrong, because the trap it describes is exactly the kind
          PERM-5 can re-arm. ForecastContext no longer has an `isAdmin` flag at
          all: it carries `canEdit` (can(forecasting.edit)) and `unscoped`
          (can(forecasting.scope.all), a new ADMIN_ONLY capability), and the
          THREE sites that made a scoping decision off the fused flag —
          requireForecastStore, forecasting/audit's store filter, and the
          /forecasting store picker — all ask `unscoped`. Granting a MANAGER
          forecasting.edit now grants goal editing and nothing else. Note the
          original blocker text below named only the first of those three sites;
          see the PERM-6 row for why the "one-file" scoping was wrong.
          PERM-5 is no longer blocked on this. ORIGINAL TEXT FOLLOWS:
          PERMISSION FUSION — split "may write" from "may read any store" BEFORE
          building the override layer. Verified in source 2026-07-27:
          `ForecastContext.isAdmin` is literally `can(actor,
          "forecasting.edit")` (src/lib/forecasting-access.ts:51), and
          `requireForecastStore` SKIPS the StoreUserAssignment check entirely
          whenever `isAdmin` is true (`if (!ctx.isAdmin) { ...assigned check... }`
          in the same file). One flag is doing two unrelated jobs: "may edit
          goals" and "is exempt from store scoping". These coincide correctly
          TODAY only because forecasting.edit is ADMIN_ONLY, so the only callers
          with isAdmin true are admins, who are legitimately unscoped.
          The moment PERM-5's override layer grants forecasting.edit to a single
          MANAGER — the exact per-user grant this phase exists to enable — that
          user ALSO silently gains unscoped cross-store forecast READS. Nobody
          reading the grant "give Maria goal-edit access" would predict it hands
          her every sibling store's forecast, and it would defeat the
          assignment-scoping fix PERM-3 shipped and the 7-27 pass verified via S4.
          This is not a bug today; it is a trap armed for the first override.
          Fix direction: give requireForecastStore its own capability (e.g. a
          `forecasting.scope.all` or an explicit unscoped flag on the context)
          rather than reusing isAdmin, so the two powers are grantable
          independently.
          SCHEDULED: PERM-6 fixes this. Once PERM-6 lands, clear this blocker.
          PERM-5 must not start before it does.

  - id: PERM-6
    track: permissions
    size: S
    status: shipped
    shipped: 2026-07-29
    commits: [d4a6bdc]
    blockers:
      - resolved: true
        text: >
          GATE CLEARED 2026-08-01 — ONE REAL INVITE RAN START TO FINISH ON
          STAGING. Both entries below are closed by this and kept unedited,
          because the ORDER is the lesson. Gary ran it and supplied the evidence;
          DOCS-3 recorded it and independently checked only what the repo can
          check.
          THE RUN, on staging (froot-git-staging-…vercel.app), Clerk org
          org_3GO2wO4QlVVSWppi8aqlnSZnsDa. /users -> Invite,
          indianathomas@gmail.com, role MANAGER — DELIBERATELY NON-ADMIN, which is
          the whole point of the test: POST /api/users maps every non-ADMIN role
          to the same Clerk role, org:member (users/route.ts:106), so a MANAGER
          arriving as a MANAGER is only possible if the app role was recovered
          from PendingInvite. One location, University Village. Clerk logged
          organization_invitation.created at 19:22:47 PDT, source backend-api,
          event 019fc047-ee89-7d77-8b99-5c8512eeb9cd. Invitation accepted, account
          created. THREE OBSERVATIONS, and they are not of equal weight — the
          discrimination argument is stated here because it is what makes two of
          them mean anything.
          (i) ADMIN VIEW, ROLE: the /users members table shows
          indianathomas@gmail.com as MANAGER. That Role column reads User.role off
          the DB row (users/page.tsx:135), so a User row exists at the invited
          role rather than at the STAFF floor. THIS OBSERVATION DOES NOT
          DISCRIMINATE: since DEBT-17's fix (84437e5) the page's own create branch
          resolves role from PendingInvite exactly as the webhook does
          (users/page.tsx:95), so MANAGER is what EITHER writer produces. It rules
          out the failure mode; it does not identify the writer.
          (ii) ADMIN VIEW, LOCATIONS: the same row rendered Location Access =
          "University Village". THIS ONE DISCRIMINATES. storeAssignments are
          written only by the Clerk webhook's if(pending) block — the page writes
          role and never assignments — so the chip rendering is PERM-6 Task 2's
          storeUserAssignment.createMany having run, on the validated storeIds.
          This is the first end-to-end confirmation of that half outside a build.
          (iii) THE INVITED ACCOUNT'S OWN SESSION, which is the strongest of the
          three: signed in AS the invited user, the dashboard header read
          "Saturday, August 1 · University Village — Colorado Springs, CO".
          Stronger than (ii) because it is not an admin page rendering a row about
          someone else — it is the new principal's own session resolving its own
          store scope from the assignment the webhook wrote. Role recovery,
          assignment materialisation and scope resolution, end to end, in the
          account they were provisioned for.
          THE DEPLOYMENT CONTAINED THE CODE — the check the 2026-07-28 pass
          failed, and here it is repo-derivable rather than taken on trust.
          origin/staging was pushed to 7b590b3 at 18:52:54 PDT, THIRTY MINUTES
          before the invite, and no commit exists after it; d4a6bdc (PERM-6),
          6530d8b and bae09ed (PERM-7) and 84437e5 (DEBT-17) are all ancestors of
          that tip. Stated precisely: this is PUSH-TIME evidence, not `vercel
          inspect` evidence — the deployed SHA was not read back.
          OUT OF ORDER, SAID PLAINLY. This gate existed to hold the promotion, and
          the promotion went first: 746c1be, 2026-07-29, and that much is checkable
          — all three phase commits are ancestors of it. So the run confirms the
          path works; it did not gate anything. The bill was paid after delivery.
          THE ORG WAS NOT RECONCILED, deliberately. The Clerk org id above is as
          Gary gave it; PERM-7's re-run target on this row's sibling is recorded
          as DB organizationId cmr54z65v000105jxczpt72w1 on branch
          preview/staging. NOBODY CHECKED THAT THESE ARE THE SAME ORG, and this
          line exists so no future reader assumes it was checked. Note also that
          `org_3GO2wO...` carries an O/0 ambiguity in transcription. Identifying
          the org by NAME is not a way out — see DEBT-19: nine orgs on
          preview/staging, five of them called "Microsoft".
          DELIVERABILITY, worth the line it costs: the invite email landed in
          Gmail's JUNK folder. Clerk DEVELOPMENT-instance mail gets spam-filtered,
          and the development instance is what serves staging. CHECK JUNK BEFORE
          CONCLUDING AN INVITE WAS NEVER DELIVERED — several minutes went into the
          wrong conclusion before the mail was found.
          THE SAVED PROMPT IS A PARTIAL RECORD. docs/prompts/DOCS-3_perm6_gate_
          cleared.md was truncated in transmission mid-way through its evidence
          list, and per CLAUDE.md § Where documents live a saved prompt is a claim
          wholesale and is never edited. THIS ROW IS THE COMPLETE RECORD; the
          prompt file is not. What the truncation cost: observations (ii) and
          (iii) above, and the /users 409 note now on DEBT-16.
          THE ORG WAS NOT RECONCILED — IT IS NOW, 2026-08-01 (evening), by query
          rather than by inference. DB organizationId cmr54z65v000105jxczpt72w1
          and Clerk org id org_3G02wO4QlVVSWppi8aqlnSZnsDa ARE THE SAME ORG. The
          line above exists so no future reader assumed it had been checked; it
          had not been, and now it has. Both ids stay on this row, since either
          one alone is the ambiguous half of a pair.
          AND THE TRANSCRIPTION ABOVE IS WRONG, which is why the check mattered.
          The character after `3G` is a ZERO, not a letter O. Correct in full:
          org_3G02wO4QlVVSWppi8aqlnSZnsDa. Note the SECOND `O` (in `2wO`) IS a
          letter in both spellings — exactly one character was wrong, which is the
          whole hazard: a one-character error in a 28-character opaque id looks
          like a correct id. The original spelling is preserved unedited above,
          and the "carries an O/0 ambiguity" note stands as the flag that caught
          it. docs/prompts/DOCS-3_perm6_gate_cleared.md:27 also carries the
          letter-O spelling and STAYS WRONG BY DESIGN — a saved prompt is a claim
          wholesale and is never edited (CLAUDE.md § Where documents live). THIS
          ROW IS THE AUTHORITY for the spelling; the prompt file is not.
          WHY THE ID AND NOT THE NAME, restated because the temptation recurs:
          DEBT-19. Nine orgs on preview/staging, five of them called "Microsoft",
          including this one. Identifying it by name resolves to A row — silently,
          and not necessarily this one. An unverifiable id is still safer than a
          name that resolves to the wrong org without erroring.
      - resolved: true
        text: >
          WHAT THE CLEARED GATE DOES NOT COVER — recorded 2026-08-01 beside it, so
          "the gate is cleared" is never read as "the phase is verified".
          (1) THE WEBHOOK HALF IS COVERED — was briefly carried as open by DOCS-3
          and CLOSED the same day, on evidence that already existed. The reasoning
          is worth keeping because it is the reusable part: role MANAGER does not
          discriminate which writer created the row (both resolve from
          PendingInvite post-84437e5), LOCATIONS does — the page never writes
          assignments. Observations (ii) and (iii) in the entry above are that
          discriminating evidence, and (iii) is the invited account's own session
          rather than an admin page. Task 2's accepting half is CONFIRMED. The
          failure here was one of RECORDING, not of coverage: the run had the
          right evidence and it was not transmitted as such.
          (2) CLOSED 2026-08-01 (evening) by PERM-6c — THE REJECTING BRANCH HAS
          NOW EXECUTED, for the first time in this phase's life, by the SECOND of
          the two routes this item itself named: a store deleted between invite
          and acceptance. Gary ran it and supplied every query result; this
          session had NO database access and checked only what the repo can check.
          ON BRANCH preview/staging (neon.branch_id br-square-feather-a63z92vz,
          returned by the query itself per the convention PERM-7's Task 7 closure
          produced), org cmr54z65v000105jxczpt72w1 BY ID per DEBT-19.
          THE ARMING, which is the part nobody had ever done. Scratch store
          "PERM-6b" (id cmsbe0giw000004l7e3zhm22n) created via /stores —
          deliberately a throwaway rather than an existing test store, because
          deleting a real store silently destroys its assignments, goals and
          caches and /stores' handleDelete swallows the failure. Staff member
          "PERM-6b Staff" created via /staff with two stores (Carson + PERM-6b),
          primary Carson, then invited to self-service. Pre-delete: n_store_ids 2,
          owned_now 2, stores {Carson, PERM-6b}. Scratch store deleted via
          /stores, absence confirmed by reload. Post-delete, the ARMED state:
          n_store_ids 2, owned_now 1, stores_still_existing {Carson} — THE STALE
          ID STAYED IN THE ARRAY. Observed now rather than predicted:
          PendingInvite.storeIds is a bare String[] with no FK
          (prisma/schema.prisma:195, read at HEAD), so nothing cascades it, which
          is exactly why the webhook re-filters instead of trusting.
          POST-ACCEPTANCE: user_id cmsbeaqn3000504l7uy2dd3wl, role STAFF,
          n_assignments 1 against an invite carrying 2, assigned_stores {Carson},
          defaultStoreId cmrd1a3es000104kzao4mf69v -> Carson,
          linked_staff_member_id cmsbe40q9000104l7fm3t6bp4,
          surviving_pending_invite 0, name null.
          THE DEFAULT STORE IS THE DISCRIMINATING SIGNATURE, and n_assignments
          alone is not. The default write keys off `owned.length === 1` — the
          FILTERED array — and not off pending.storeIds.length
          (webhooks/clerk/route.ts:199-204 at HEAD), so a two-store invite ending
          with ONE assignment AND a default set is a state only the filtering code
          can produce. Note precisely what that means: the write itself is
          BUILD-2/PERM-7 code, and it discriminates because it reads PERM-6's
          variable.
          CORROBORATION FROM THE SCHEMA, offered as inference and not as
          observation. StoreUserAssignment.storeId carries a real FK
          (schema.prisma:185), so pre-PERM-6 code — which passed pending.storeIds
          straight to createMany — would have violated it on the stale id and
          thrown. createMany is ONE statement, so that outcome is ZERO assignments
          and a 500 back to Clerk, not one assignment. And
          surviving_pending_invite 0 says the whole `if (pending)` block ran to
          completion, since the delete is its last statement — the filter behaving
          as its own comment demands: "Filter, never throw".
          A SECOND SET OF ASSIGNMENTS ALSO LOST THE STORE, AND PERM-6 DID NOT DO
          IT — recorded because conflating the two would credit this phase with
          something the database did on its own. Observed in the app rather than
          in any query: the /staff profile for "PERM-6b Staff" now shows Store
          Assignments = CARSON ONLY. Those are StoreStaffAssignment rows (staff
          member -> store), NOT the StoreUserAssignment rows (login -> store) the
          webhook writes, and StoreStaffAssignment.store is onDelete: Cascade
          (prisma/schema.prisma:267, read at HEAD). So the STORE DELETION removed
          that row directly, at the database, before the invite was ever accepted
          — no application code chose it and the webhook's filter never saw it.
          Two different tables lost the same store by two different mechanisms in
          one run, and only the login side is evidence for this item. It is also
          the hazard this entry's own ARMING paragraph names, observed live: a
          store delete silently rewrites assignments elsewhere.
          THE LIMIT, and it is half of what this item's own words claim. The path
          is `filter-and-warn`. The FILTER is proven from the database. The
          console.warn STATEMENT (route.ts:174) is INFERRED from the same branch
          and was NOT OBSERVED — the Vercel runtime log line was not captured, and
          no database query can witness a log line. Record it as FILTERED PROVEN,
          WARNED INFERRED. If the warn ever matters — it is the only
          operator-facing signal that ids were dropped — it needs its own look at
          the runtime log.
          `name null` is consistent and is not a defect: PERM-7's Task 7 closure
          established that no sign-up path collects a name, and the store-name
          derivation fires only for role STORE carrying exactly one store. This
          invite was neither.
          ORIGINAL TEXT, unedited:
          (2) THE REJECTING BRANCH IS STILL NOT COVERED, unchanged by this run.
          One valid, org-owned storeId went in, so the webhook's filter-and-warn
          path (`owned.length !== pending.storeIds.length`) has still never
          executed. Covering it needs a foreign-org storeId, or a store deleted
          between invite and acceptance.
          (3) CLOSED 2026-08-01 (evening) by PERM-6c AS WRITTEN — AND THAT IS
          NARROWER THAN IT LOOKS. A staff member WAS created on staging through
          the route this item names: "PERM-6b Staff", via /staff -> Add Staff
          Member, email indianathomas+staff@gmail.com, two stores (Carson +
          PERM-6b), primary Carson, fullNameLocked — the manual-add branch, not a
          Square import — and it is the same fixture the run in (2) was built on.
          The item's literal claim ("no staff member was created") is false from
          this date.
          WHAT IT DOES NOT ESTABLISH, said plainly rather than left to be assumed.
          This is the ACCEPTING PATH ONLY and it is NON-DISCRIMINATING. Task 1's
          guard is two rejections — an org-ownership count (400) and, for
          non-admins, a caller-scope check (403) at staff/route.ts:75-83 — and
          NEITHER FIRED. The Add Staff dialog cannot construct the inputs that
          would fire them: it offers only this org's stores, the route dedupes,
          and a primary outside the selection is not submittable. So PRE-PERM-6
          CODE PRODUCES THE IDENTICAL ROW for this input. It is the same
          discrimination test observation (i) in the entry above FAILED and (ii)
          passed: it rules out the guard having broken the ordinary case; it does
          not show the guard rejects anything.
          THE DISCRIMINATING CALLS WERE NOT RUN. Two console calls against POST
          /api/staff — a foreign-org storeId (expect 400) and, driven as a
          MANAGER, an in-org but out-of-scope storeId (expect 403) — were planned
          as B-2 and B-3 and NEITHER WAS EXECUTED. Task 1's rejecting behaviour
          therefore still rests exactly where it did on 2026-07-27: the local
          dev-DB count query recorded in the NOT COVERED block below, plus the
          build. Do not read this item's closure as covering it.
          ORIGINAL TEXT, unedited:
          (3) TASK 1 (POST /api/staff) is still unexercised on staging — no staff
          member was created.
          (4) CLOSED 2026-08-01 (evening) by PERM-6c — THE STAFF ROLE WAS DRIVEN,
          in PERM-2's sense of the word, which is the sense this row has used
          since that pass. Signed in AS the STAFF principal (username
          perm-6-staff). Two routes, both 403 Forbidden.
          GET /api/square/locations -> 403. This is the load-bearing one and it
          DISCRIMINATES. Item (d) in the notes below records that this route had
          NO role gate at all before this phase, only auth(); the gate that now
          stands is `can({ role }, "stores.manage")` at
          square/locations/route.ts:34-35, read at HEAD. Pre-PERM-6 code cannot
          return 403 to an authenticated org member — it had nothing to return it
          from. A STAFF principal would have received the full location list,
          which is the finding that named STAFF explicitly.
          GET /api/staff -> 403. A control from a DIFFERENT enforcement point —
          PERM-2 §3 #3's guard, not this phase's — so the denial is not an
          artefact of one route's wiring. PERM-2's own pass drove that route to
          403 as STORE (S2); this is its STAFF half, five days later.
          WHAT IT DOES NOT ESTABLISH: two denials are two denials. No positive
          STAFF capability was exercised, so nothing here says a STAFF principal
          can still do what a STAFF principal should. PERM-2 pointed that work at
          HR-9, which has to re-walk these routes anyway; that pointer stands.
          THE PRINCIPAL DRIVEN HERE IS THE ACCOUNT CREATED IN (2) — SETTLED
          2026-08-01 (evening) by Gary, on two sources named separately rather
          than on an assumption, which is the whole reason it is written down.
          DB, branch preview/staging: User id cmsbeaqn3000504l7uy2dd3wl, email
          indianathomas+staff@gmail.com, role STAFF, createdAt 2026-08-02
          06:04:19.167 UTC. APP: the /staff detail page for StaffMember "PERM-6b
          Staff" (cmsbe40q9000104l7fm3t6bp4) renders the badge "Self-service
          login" against that same address, where it read "Invite pending" before
          acceptance — the profile's own record of the binding, and the visible
          face of the linked_staff_member_id in (2). The principal that produced
          the two 403s signed up through that invite. One account, two roles in
          this entry: it is the row the rejecting branch wrote, and the role the
          gates denied.
          ORIGINAL TEXT, unedited:
          (4) The STAFF role is still unexercised.
          WHAT REMAINS UNCOVERED ON THE PAIR after 2026-08-01 (evening), recorded
          here because three items closing at once is precisely when a reader
          concludes more than happened. FOUR things, none closed by that session:
          (i) B-2 and B-3, the discriminating POST /api/staff calls — item (3).
          (ii) DEBT-16's collision PRE-CHECK, still untested. The /stores device
          dialog's check has never run on staging and was not opened here; this
          run went through /staff -> Add Staff Member, on a fresh address. Three
          fresh addresses across PERM-7's runs, a fourth here — no collision has
          ever been presented to any dialog.
          (iii) PERM-7's rewritten Clerk error branch in POST /api/users, STILL
          DORMANT on its own code: every invite to date has succeeded. DEBT-15
          records that branch green in PRODUCTION under promotion 97ed309, but
          carrying DEBT-15's own guard on top, so it is not a clean test of what
          PERM-7 shipped.
          (iv) The webhook's console.warn STATEMENT — the limit in item (2).
          THE PERM-7 SIDE SAYS OTHERWISE AND WAS TRUE WHEN WRITTEN. PERM-7's Task
          7 closure states "PERM-6's second blockers entry stands untouched" and
          lists (2), (3) and (4) as open. Correct on 2026-08-01 (afternoon),
          superseded by this entry the same evening. THIS ROW IS THE AUTHORITY on
          items (2)-(4); that sentence is dated prose and is deliberately not
          edited.
      - resolved: true
        text: >
          THE GATE BELOW WAS NEVER CONFIRMED, AND BOTH PHASES SHIPPED ANYWAY —
          recorded 2026-08-01 by DOCS-2, Gary's ruling. PERM-6 and PERM-7 reached
          main in the 746c1be promotion on 2026-07-29 WITHOUT the real-invite run
          being confirmed. Gary has no clear recollection of it running end to end
          and unknown is not satisfied, so this blocker STAYS OPEN rather than
          being cleared on a hazy memory — he will run the invite and clear it
          with real evidence. The practical state: the invite -> PendingInvite ->
          webhook-acceptance path is now IN PRODUCTION still unexercised end to
          end. That is why the status is `shipped` and not `verified`. The gate as
          originally written follows, unedited.
      - resolved: true
        text: >
          SINGLE PROMOTION UNIT WITH PERM-7 — Gary, 2026-07-28. NEITHER PHASE
          REACHES MAIN UNTIL ONE REAL INVITE RUNS START TO FINISH ON STAGING.
          PERM-7 modified the same two files this phase changed — POST
          /api/users and the Clerk webhook — and that path (invite ->
          PendingInvite -> webhook acceptance) has NEVER been exercised
          end-to-end. PERM-6 shipped it build-green only; see the NOT COVERED
          list below. Promoting either phase alone would put an unexercised
          write path into production, and promoting PERM-6 without PERM-7 would
          do it while splitting the two changes to those files across releases.
    title: "Store-assignment integrity — validate storeIds on every write, gate the locations route, split the forecasting isAdmin fusion"
    notes: >
      STATUS RATIONALE — WHY PERM-6 IS `shipped` AND NOT `verified`. RELOCATED
      HERE 2026-08-01 (later the same day) FROM THE `blockers` ENTRY IT WAS
      WRITTEN IN, REVERSING THE RULING ITS OWN LAST PARAGRAPH RECORDS. That
      paragraph ruled the entry should stay a bare string so the rationale would
      not sit behind a fold, and called over-reporting by one "the cheaper
      error". The evidence against it arrived within hours: Gary opened
      /internal/roadmap, saw PERM-6 listed under "Blockers & gates — what's
      stopping promotion", and concluded the session had failed. Nothing is
      stopping PERM-6 — all four items in that entry are closed. A panel that
      makes its owner misread his own project is not paying for itself, so the
      cheaper error was the other one. The rationale is a note ABOUT STATUS, and
      `notes` is where notes go.
      THE REVERSAL IS THE DURABLE PART, which is why it is recorded here rather
      than quietly applied: a ruling that changed within hours should say so
      rather than look like it was always this way.
      WHAT MOVED AND WHAT DID NOT. The three paragraphs immediately below are
      the relocated text, word for word. Everything else in that entry — items
      (1)-(4), their closures, the ORIGINAL TEXT blocks, and the WHAT REMAINS
      UNCOVERED list — stayed where it was, unedited. The entry is now
      `resolved: true` and renders inside the "Resolved gates" disclosure with
      its record intact. NOTHING WAS DELETED ANYWHERE.
      THE COST OF THIS MOVE, named rather than absorbed: the entry's WHAT
      REMAINS UNCOVERED list is the only LIVE content left in it, and folding it
      is what this relocation buys the honest count with. FOUR things, still
      unexercised, now findable only inside the resolved entry under that
      heading — (i) B-2 and B-3, the discriminating POST /api/staff calls (a
      foreign-org storeId expecting 400, and an in-org but out-of-scope storeId
      driven as MANAGER expecting 403), neither ever executed; (ii) DEBT-16's
      collision PRE-CHECK in the /stores device dialog, which has never been
      presented a collision; (iii) PERM-7's rewritten Clerk error branch in POST
      /api/users, still dormant on its own code because every invite to date has
      succeeded; (iv) the webhook's console.warn STATEMENT — filtered proven,
      warned inferred. Open "Resolved gates" to read them in full.
      THE RELOCATED TEXT STILL POINTS AT THE ARRAY IT WAS WRITTEN IN — "the
      entry below", "this entry", "WHAT THIS ENTRY NOW IS" — and those pointers
      are DELIBERATELY NOT REPAIRED. They resolve against PERM-6's `blockers`,
      which is where the words were written and where the entry still is.
      Repairing them would rewrite the record to look as though it had always
      lived here. See DEBT-41, whose exemplar (a) this relocation removes.
      STATUS STAYS `shipped`, NOT `verified`, and the reason has changed: the
      entry below gave the unconfirmed gate as the reason, and that reason is
      gone, but (1)-(4) are not. The flip is Gary's ruling to make, not a
      bookkeeping consequence of clearing a blocker.
      THE REASON HAS CHANGED AGAIN, 2026-08-01 (evening). The paragraph above
      gives "(1)-(4) are not [gone]" as the reason; all four are now closed,
      so that reason is spent too. What survives is its LAST SENTENCE, which
      never depended on the four. Everything in this entry — every query in
      items (2) and (4) — is preview/staging, and staging evidence is not
      production evidence (DOCS-3's precedent, restated in PERM-7's Task 7
      closure). BOTH PERM-6 AND PERM-7 STAY `shipped`, unchanged by PERM-6c.
      WHAT THIS ENTRY NOW IS: a STATUS RATIONALE wearing the blockers field.
      That is DEBT-41(a) in a sharper form than when P-4 filed it — at filing
      it held four live coverage gaps AND a rationale; it now holds only the
      rationale, and there is no longer any live gate in it. It STAYS A BARE
      STRING by Gary's ruling 2026-08-01: `resolved: true` would collapse it
      into the "Resolved gates" disclosure, putting the one thing a reader
      needs from this phase behind a fold. The panel over-reports by one,
      which is the cheaper error. See DEBT-41.
      SIZE: scoped S, DELIVERED ABOVE S — recorded honestly rather than left at
      the estimate (Gary, 2026-07-27). It landed as 15 files. Two things grew:
      Task 5 was scoped as a one-file change and was actually three sites (see
      the correction under (e)), and the phase acquired real doc work —
      PERMISSIONS_INVENTORY §1j/§5, DECISIONS.md, three new DEBT rows, and two
      status entries cleared on other phases. The estimate was not unreasonable
      given what the scoping audit had established; it was wrong because that
      audit asked a narrower question than it answered. NOTHING WAS ADDED TO
      SCOPE — still five fixes, still no schema, still no migration. The
      correction is to the size vocabulary, not to the work: an S that reaches
      15 files should say so, or S stops meaning anything on the next estimate.
      Scoped 2026-07-27 from a read-only audit of every store-assignment read
      and write path. Five independent fixes, no schema change, ONE new
      capability (forecasting.scope.all — the original "no new capability"
      claim did not survive Task 5). Deliberately sequenced BEFORE PERM-7, so
      the new provisioning flow is not built on top of unvalidated write paths.
      (a) POST /api/staff (src/app/api/staff/route.ts:46-68) — storeIds from the
      request body flow straight into the nested storeAssignments.create with NO
      org-ownership query anywhere in the route, and the route is
      MANAGER-reachable via can(role, "staff.manage"). This is CROSS-TENANT, not
      just cross-store: a storeId belonging to another organization is accepted.
      This is PERM-2's deferred item, and PERM-2's own wording understated it.
      Highest severity item in this phase.
      (b) POST /api/users (src/app/api/users/route.ts:88-91) — the invite path
      writes storeIds into PendingInvite.storeIds with no org-ownership check,
      and the Clerk webhook later materialises them via
      storeUserAssignment.createMany with NO re-check
      (src/app/api/webhooks/clerk/route.ts:126). ADMIN-only, so lower severity
      than (a) — but note its sibling PATCH route DOES validate correctly at
      src/app/api/users/[id]/route.ts:56-63. Same file family, opposite
      behaviour; copy the working check.
      (c) PATCH /api/staff/[id] (src/app/api/staff/[id]/route.ts:55-63) —
      validates org-ownership but NOT caller scope. The target staff member must
      be in the caller's scope to reach the edit, but once inside, a MANAGER can
      set storeIds/primaryStoreId to ANY org store, including ones they do not
      manage. Narrower than (a) but the same class.
      THE FIX IS DELTA-BASED, AND MUST STAY THAT WAY — do not "simplify" it to
      the obvious "every storeId must be in caller scope". Found during
      implementation 2026-07-27: that flat subset check would have 403'd
      LEGITIMATE MANAGER SAVES. The edit dialog seeds its selection from the
      member's FULL assignment set (staff-edit-actions.tsx:55) while rendering
      only the manager's own stores (staff/[id]/page.tsx:120-131), so a staff
      member who is also assigned to a store the manager does not manage
      submits that foreign storeId straight back on every ordinary save — the
      manager cannot see it and cannot remove it. So the check compares the
      DELTA: a non-admin may only ADD or REMOVE stores within their own scope,
      and pre-existing out-of-scope assignments pass through untouched.
      For the same reason primaryStoreId is validated against the resulting
      assignment set ONLY, never against caller scope — the dialog seeds it
      from the member's existing primary, which may be an out-of-scope store,
      so scope-checking it would 403 an untouched field. The chip path that
      sets primary directly is already ADMIN-only (staff/page.tsx:169).
      (d) GET /api/square/locations (src/app/api/square/locations/route.ts:7-8)
      — NO role gate at all, only auth(). Any authenticated org member including
      STORE and STAFF can read the org's full Square location list. Worse than
      it first appears because the route spreads the entire Square object
      (`...loc`), so business_email, coordinates and every other field go to the
      client. Not on any prior list; found 2026-07-27. Cheapest fix here.
      (e) Split the forecasting isAdmin fusion — PERM-5's blocker. Give
      requireForecastStore its own capability or an explicit unscoped flag
      instead of reusing `isAdmin`, which is literally
      can(actor, "forecasting.edit") at src/lib/forecasting-access.ts:51.
      SCOPE CORRECTED 2026-07-27 during implementation — this row previously
      said "This is a ONE-FILE fix, not a pattern-wide refactor", and that was
      WRONG. The audit behind that claim swept SIBLING HELPERS IN OTHER MODULES
      (labor-access.ts, requireLaborStore, requireManageableStaff,
      findManageableStaffMember, requireNoteAccess, requireCount) and correctly
      found them clean — labor-access.ts derives LaborContext.isAdmin from
      dbUser.role === "ADMIN", not from a capability, so it genuinely does not
      have this coupling, and those helpers still must NOT be harmonised. What
      that audit never swept was FORECASTING'S OWN OTHER CONSUMERS of the same
      fused flag. It established the pattern had not spread outward and
      concluded the fix was one file; those are different questions. The flag
      was public on ForecastContext, so its blast radius was every reader of
      that context. THREE sites read it for a SCOPING decision:
      (1) src/lib/forecasting-access.ts — the flag + requireForecastStore;
      (2) src/app/api/forecasting/audit/route.ts — the audit-log store filter;
      (3) src/app/(app)/forecasting/page.tsx — which stores the picker lists.
      Fixing only (1) would have left PERM-5's trap half-armed (a MANAGER
      granted forecasting.edit would still get org-wide audit reads and every
      store in the picker), so the blocker could not honestly have been
      cleared. Gary ruled 2026-07-27: fix all three. The fix NAMES THE TWO
      POWERS APART rather than renaming one — ForecastContext now carries
      `canEdit` and `unscoped`, and `isAdmin` is gone, because a single flag
      answering two questions is what invited this. New capability
      forecasting.scope.all (ADMIN_ONLY) in PERMISSIONS_INVENTORY.md §5.
      Session prompt: docs/prompts/PERM-6_store_assignment_integrity.md
      STAGING VERIFICATION PASS 2026-07-27 (Gary), driven as
      corporate@keva.com at role MANAGER (assigned Carson + Las Brisas, 2 of
      12 org stores) and as indianathomas@live.com at ADMIN. Evidence quoted,
      not summarised — a row saying "verified" is not proof.
      NEGATIVE CHECK, Task 5 scoping (this is the one that matters — it is
      PERM-3's S4 re-run against the split flag). As MANAGER,
      GET /api/forecasting/plan?storeId=cmrd1a4hw000004jrsgj5qah1&year=2026
      (South Reno, NOT assigned) →
      `STEP 3 → 403 {error: 'Forbidden'}`.
      CONTROL, same session, same route, same year, only the store differs:
      storeId=cmrd1a4hz000004jjl91v23nm (Las Brisas, assigned) →
      `STEP 4 → 200 {plan: {…}, canEdit: false}`.
      The control is what makes the 403 mean something: without it a 403 could
      have come from a malformed request. Note `canEdit: false` in the 200 body
      — that field was `isAdmin` before this phase, so the payload itself shows
      the two powers now expressed separately: a MANAGER gets a successful
      scoped READ and canEdit false in the same response.
      TASK 5 STORE PICKER, the regression this could have caused: /forecasting
      as MANAGER listed exactly `Carson` and `Las Brisas` in the dropdown, and
      GET /api/stores in the same session returned exactly those two names.
      Picker and API agree at 2 of 12 — the picker is NOT over-listing. Page
      also rendered "· read-only" in the subtitle with the Plan Summary card
      instead of Goal Settings, and "goals shown for Jul 2026–Aug 2026",
      confirming PERM-3's window survived.
      TASK 3 DELTA CHECK, the case the naive fix would have broken. Staff
      member Jordan Pippenger (/staff/cmri2mpd6000304jol6u5r3ht) carries SEVEN
      assignments — UNR (primary), Carson, Las Brisas, Meadowood Mall, South
      Reno, Spanish Springs, Sparks — of which FIVE are outside this MANAGER's
      scope. The edit dialog renders only Carson and Las Brisas as checkboxes
      while submitting all seven ids. Opened Edit, changed nothing, saved:
      dialog closed with no error, and a reload showed ALL SEVEN still
      assigned with UNR STILL PRIMARY. Both halves matter — a flat subset check
      would have 403'd this save, and a wrong delta could have succeeded while
      silently dropping the five invisible stores, which is worse than a
      refusal because nothing would have told anyone.
      TASK 4 on the trimmed payload, as ADMIN: the Square import dialog listed
      locations with name + address and greyed out the already-imported ones,
      and Edit Store's picker read "Las Brisas — 5020 Las Brisas Blvd Ste A2".
      Both consumers render on the allow-list.
      NOT COVERED — recorded rather than glossed. (1) TASK 1 (POST /api/staff)
      was NOT exercised on staging; no staff member was created during the
      pass. Its guard was verified locally against the dev DB by running the
      exact count query (a storeId owned by another org → count 0/1, rejected;
      own ids → 3/3, accepted), which proves the query discriminates but is
      NOT an end-to-end test of the route. (2) TASK 2 was NOT exercised AT ALL
      — no invite was created and none accepted, so neither the invite-half
      check nor the webhook re-filter has ever run outside a build. Both are
      the highest-value targets for the next staging visit. (3) The STAFF role
      was not exercised, same gap PERM-2's pass recorded. (4) The staff
      VISIBILITY path was not tested; that gap is how DEBT-13 was found.
      GAP (2) NARROWS — THE ACCEPTING PATH IS NOW COVERED END-TO-END,
      2026-07-28. During PERM-7's staging attempt an invite was created as role
      STORE with one store and accepted for real. Verified by query ON BRANCH
      preview/staging (branch named deliberately — see the retraction note
      below): User id cms55ln6v000004jrtgs8gpx1, email gary@keva.com, role
      STORE, clerkUserId user_3H9HEV5iN8vkRDMb71JDSjB37cm, org "Microsoft",
      StoreUserAssignment count = 1. One valid storeId in, exactly one
      assignment out, and the app role recovered correctly from PendingInvite
      rather than defaulting from the Clerk org role. That is the invite half
      and the webhook half both running for the first time outside a build.
      THE REJECTING BRANCH IS STILL NOT COVERED. No invalid or foreign-org
      storeId was ever sent, so the webhook's filter-and-warn path
      (webhooks/clerk/route.ts, the `owned.length !== pending.storeIds.length`
      console.warn) has still never executed. Covering it needs an invite
      carrying a storeId from another org, or a store deleted between invite
      and acceptance. GAP NARROWED, NOT CLOSED.
      GAP (2), SECOND RUN 2026-08-01 — a MANAGER invite this time, on a
      deployment that provably contained this phase. The full record is the
      leading entry under `blockers`, kept in ONE place so there is one copy to
      correct if it turns out wrong. It adds what the 2026-07-28 run could not:
      DISCRIMINATING evidence that the webhook half ran — a rendered Location
      Access chip, and the invited account's own dashboard resolving to that
      store — rather than a role that either writer could have produced. It does
      not change the SHAPE of the gap: the accepting path has now run twice, the
      rejecting path still zero times.
      RETRACTION OF A RETRACTION — recorded because the churn is the lesson. An
      earlier version of this row claimed gap (2) was untouched, on the basis
      of a query returning ZERO assignments and no User row. That query was run
      against the PRODUCTION branch while being read as staging; the branch was
      never named beside the result. The staging row had existed and been
      correct the whole time. See CLAUDE.md § Database Evidence.
    open:
      - >
        RESOLVED AND MOVED 2026-07-27 — kept here as a pointer only. The
        `StoreStaffAssignment.isPrimary` constraint question now lives on
        BUILD-2, which is already the migration phase; that keeps PERM-6
        no-schema and size S, and batches both DDL changes into one deploy.
        BUILD-2 carries the hard gate: the duplicate pre-check runs against
        PRODUCTION and its results go to Gary before anything migrates.
        TWO CORRECTIONS to how this was originally written here, so the next
        reader does not chase the wrong thing. (1) The original text said a
        RACE between two concurrent PATCHes on the two-step false-then-true
        branch could produce two primaries. That is OVERSTATED — the two-step
        runs inside `$transaction`, and Postgres row locks serialise the second
        writer behind the first, so it resolves to a single primary correctly.
        Do not spend a session hunting that race; it cannot happen as written.
        (2) The real risk is a FUTURE WRITER, and it is mundane: a new call
        site doing the obvious
        `storeStaffAssignment.update({ where: {...}, data: { isPrimary: true } })`
        sets the new primary without clearing the old one. The existing
        chip-click path avoids this only because someone deliberately wrote the
        two-step; that reasoning lives in code, not in the schema, so a new
        writer cannot inherit it — and PERM-7 plus any bulk-edit or import
        feature are exactly such new writers.

  - id: PERM-7
    track: permissions
    size: M
    status: shipped
    shipped: 2026-07-29
    commits: [6530d8b, bae09ed]
    title: "Store device logins — provision a store account from /stores, Square-seeded email, role-aware badge"
    notes: >
      Scoped 2026-07-27 with Gary.
      REFRAMED 2026-07-28 AGAINST PRODUCTION — this phase ESTABLISHES a
      convention; it does not formalise an existing one. The original framing
      ("makes an EXISTING CONVENTION first-class") rested on a single live
      example that does not survive contact with the production database. Every
      detail of it was wrong. Verified read-only on 2026-07-28 against the
      production Neon branch, org "Keva Juice" org_3FhYUR4l0ue7egug1I0Ig8wxOVn
      (the Square-connected one, 9 stores):
      the account assigned to Las Brisas is kevajuice14@icloud.com, NOT
      corporate@keva.com — no corporate@keva.com User row exists in production
      at all; its role is MANAGER, NOT STORE; it is assigned to TWO stores
      (Las Brisas AND South Reno), not one; Las Brisas has storeNumber NULL, so
      "#0014" does not exist; and User.name is null for all 8 production users,
      so nothing is named "Tommy Thomas" (the /users page composes its name
      column from Clerk's first/last name, not from the DB — see the Task 7
      note below, which turns on the same fact).
      THE LOAD-BEARING FINDING: there are ZERO users with role STORE anywhere
      in production. Role histogram across all orgs is ADMIN 5, MANAGER 2,
      STAFF 1. The pattern this phase claimed to make first-class has never
      been built once, and the nearest live account is a two-store MANAGER —
      a direct counterexample to "one store per device account".
      That does not invalidate the design, and one-store-per-device REMAINS
      THE RULE for accounts this flow creates (it is what sidesteps BUILD-2 for
      devices). It changes what the phase is: net-new convention, so the
      enforcement in (f) is the point rather than a tidy-up, and no existing
      account is evidence for the shape.
      Today a store device login would be built by hand: an admin invites a
      user on /users, picks role STORE, and ticks one store. Nothing names the
      pattern, nothing enforces it, and nothing provisions it in one step.
      NOT A SECOND PERMISSION MODEL — verified there is no direct
      user-creation path anywhere (provisioning is Clerk-invite-only), and Clerk
      authenticates an identity, not a database row. So "assign a Store as a
      User" necessarily mints a normal User with role STORE plus one
      StoreUserAssignment. Same objects the invite flow already produces,
      reached in one click from the page the admin is already on. Making Store a
      second permission-holding principal was considered and REJECTED: it would
      fork the capability layer PERM-1 deliberately unified, forcing every
      can() call site to ask "User or Store?" forever.
      SCOPE:
      (a) "Create device login" action on /stores, ADMIN-only, creating a User +
      a single StoreUserAssignment to that store. One store per device account —
      which also sidesteps BUILD-2 entirely for devices, since a one-store
      account has no primary-store ambiguity.
      (b) Role is CHOOSABLE at provisioning (Admin/Manager/Store), defaulting to
      STORE, with MANAGER marked recommended. See DECISIONS.md 2026-07-27 — this
      is role choice, not a capability override, so PERM-5's restrict-only
      invariant is untouched.
      (c) COUNT-AWARE warning AlertDialog when the chosen role is above STORE.
      The store picker is hidden for ADMIN
      (src/app/(app)/users/user-actions.tsx:104,230 — "Admins have access to all
      locations automatically"), so an ADMIN device account sees EVERY store.
      The warning must state the actual blast radius — "this gives the shared
      device at Carson access to all 12 of your locations" — so it is
      unalarming for a single-location operator and stopping for a twelve-store
      one, without the product judging who is sophisticated. Name concrete
      consequences: Square disconnect is ADMIN-gated by SEC-1 and drops the live
      org-wide token; dashboard goal PUT is ADMIN-only per PERM-2; F-5's audit
      log will record the device address, not a person.
      (d) Email SEEDED from Square, never live-synced. See DECISIONS.md.
      (e) Replace the ambiguous "Has Account" badge
      (src/app/(app)/stores/page.tsx:109-114), which is merely
      `store.userAssignments.length > 0` — a COUNT, not a concept. A MANAGER
      assigned to three stores lights it up on all three. Make it role-aware
      ("Device login" vs "Manager access"), show who, and LINK to /users. The
      first thing this phase buys is the ability to answer "does this store have
      a login, and whose is it?" on the page where the question is asked.
      (f) Enforce the STORE-is-a-device convention through the flow: default the
      account name to the store, not a person. Do NOT retro-repoint existing
      accounts; surface the drift instead.
      CORRECTED 2026-07-28 — the "Tommy Thomas on a corporate address" evidence
      cited here was wrong (see the reframe above; production User.name is null
      for every user). The argument for enforcing the convention in the flow
      SURVIVES the correction and is in fact stronger: with zero STORE accounts
      in production there is no precedent to inherit, so whatever this flow
      produces IS the convention.
      HOW, given no schema change (Gary's Ruling 4, 2026-07-28): the invite
      carries only email + role, and User.name is written by the Clerk webhook
      from the invitee's own first/last name
      (src/app/api/webhooks/clerk/route.ts:119) — i.e. whatever a human types at
      sign-up. PendingInvite has no name column and schema is frozen, so
      provisioning cannot set the name directly. The webhook instead derives it:
      when pending.role === "STORE" AND the invite carries exactly one storeId,
      User.name defaults to that store's name. CREATE-ONLY — the create branch
      of the upsert, never the update branch, or every later Clerk event for
      that user would reset a name someone has since corrected. If that cannot
      be cleanly confined to create, Task 7 is CUT and filed rather than bodged.
      NOT BLOCKED ON BUILD-2 (corrected 2026-07-27 — an earlier read said it
      was). BUILD-2 solves default-store for multi-store HUMANS; a device
      account has one store.
      Session prompt: docs/prompts/PERM-7_store_device_logins.md (carries DEBT-8
      as its Task 0).
      AS BUILT, 2026-07-28. Task 0 (DEBT-8) landed as its OWN commit per Ruling
      1 and is bankable independently — see that row. Tasks 1-7 followed.
      NEW SHARED MODULE src/lib/device-login.ts — pure, no server imports, so the
      provisioning dialog, the /stores badges and the /users badges all read one
      definition of "device login" instead of three. Holds DEVICE_ROLE_OPTIONS,
      isDeviceLogin, isAboveStore, blastRadius, deviceConsequences, plusAddress.
      NO NEW CAPABILITY. Provisioning goes through POST /api/users (requireAdmin,
      users.manage); the resync route is can(role, "stores.manage"). Inventory
      §1j gains SQ-5c and §5 cross-references it.
      TASK 6's badge needed the /stores query widened to pull the assigned USER
      through (page.tsx getStores include), since role and identity are exactly
      what the old count could not express.
      TASK 4 on /users identifies a device by the one CHECKABLE signal available
      without a schema flag: the account signs in as a location's own
      contactEmail. Deliberately NOT inferred from role + assignment count — an
      ADMIN device account has ZERO assignments (the picker is hidden for
      ADMIN), so counting would miss the case that matters most.
      TASK 7 STAYED CREATE-ONLY and was not cut. The derivation sits in the
      `create` branch of the webhook's user upsert
      (src/app/api/webhooks/clerk/route.ts) and is ABSENT from `update`, so no
      later Clerk event can reset a corrected name.
      NOTHING IN THIS PHASE HAS EVER EXECUTED, ANYWHERE. Established
      2026-07-28. Not verified, not falsified — UNTESTED. The staging
      deployment used for the verification pass was created at 11:59:10; the
      earliest PERM-7 commit (6530d8b) is 12:31:21 and the feature commit
      (bae09ed) is 12:40:55. Staging was running pre-PERM-7 code, confirmed
      against `git show 6530d8b~1`: stores/page.tsx:112 still reads "Has
      Account" (the badge Task 6 replaces) and users/route.ts still has the
      blanket `err.message` catch. Every observation from that pass — the two
      "failures" AND the "passes" — is about code that does not contain this
      phase. Treat the whole phase as unexercised until a pass runs against a
      deployment whose SHA matches. See CLAUDE.md § Staging Verification.
      RE-RUN TARGET, BY ID NOT NAME: organizationId
      cmr54z65v000105jxczpt72w1 on branch preview/staging. It is the ONLY org
      on that branch with Square connected, it has 4 users, and gary@keva.com
      (role STORE, 1 store) already lives in it — so Tasks 0 and 5, which need
      Square, ARE exercisable, and the existing account covers several checks
      without a fresh sign-up. Its display name is "Microsoft", which is NOT
      identifying: five orgs on that branch share it. Always address this org by
      ID. See DEBT-19.
      Ordered re-run plan: docs/prompts/PERM-7_store_device_logins.md
      § Re-run plan.
      STILL TRUE AFTER THE 2026-07-28 RE-QUERIES. Correcting the branch labels
      resolved the role and assignment questions and vindicated PERM-6, but it
      tested NOTHING in this phase — the deployment still predated every PERM-7
      commit. Not verified, not falsified: UNTESTED.
      NARROWED 2026-08-01, AND ONLY ON THE READ SIDE. The invite gate ran
      against a deployment that provably contained this phase (see the resolved
      single-promotion entry under `blockers`), so "NOTHING HAS EVER EXECUTED,
      ANYWHERE" is no longer literally true: /users rendered Task 4's badge
      logic, which correctly emitted no badge for a non-contactEmail account.
      EVERY WRITE-SIDE CHANGE THIS PHASE MADE IS STILL DORMANT — the rewritten
      Clerk error branch (the invite succeeded) and Task 7's name derivation
      (the role was MANAGER, not STORE) both went unentered, and the /stores
      device-login dialog was not the entry point used. Read the paragraph above
      as still standing for everything this phase actually writes.
      NARROWED AGAIN 2026-08-01 (EVENING), AND NOW ON THE WRITE SIDE — the three
      device-login runs recorded in the Task 7 blocker. Two claims immediately
      above are SUPERSEDED and preserved unedited: the /stores device-login
      dialog HAS now been the entry point, three times, so "was not the entry
      point used" is true only of the earlier /users run; and Task 7's
      derivation HAS executed, three times, so "every write-side change is still
      dormant" no longer holds. The same correction applies to the line in the
      resolved invite-gate blocker below reading "ALSO STILL UNEXERCISED: the
      /stores device-login dialog" — that entry point is exercised; what is
      still uncovered there is narrower and still real, DEBT-16's collision
      pre-check, since all three runs used fresh addresses. STILL GENUINELY
      DORMANT after these runs: the rewritten Clerk error branch in POST
      /api/users — all three invites succeeded, so it went unentered again.
      CITATION CORRECTED, same date, no text deleted: the HOW paragraph above
      cites src/app/api/webhooks/clerk/route.ts:119 as where User.name is
      written. At HEAD :119 is a comment line; the write is :151-153, and the
      derivation that feeds it is :126-137. The line drifted when Task 7's own
      comment block was added above it.
      THE EMPTY User.name IS THE EXPECTED RESULT, NOT A FAILURE. Task 7's
      derivation does not exist in the build staging was running, so the create
      branch fell through to Clerk's first/last name, and the invitee supplied
      a username instead. An empty name is exactly what pre-PERM-7 code
      produces. It was logged as failure "4. NAME DERIVATION — FAILED"; that
      line should be read as "Task 7 absent", and it is the one observation of
      the night that is fully consistent with correct behaviour on both sides.
      COLLISION CASE — RECORDED AS VERIFIED ON AN INVALID TEST, THEN
      MIS-DIAGNOSED TWICE. Corrected 2026-07-28. The original note read:
      "reproduced live 2026-07-28 against the development Clerk instance:
      re-inviting an existing org member returns HTTP 400
      already_a_member_in_organization." That statement is TRUE and it did not
      test this code. It called Clerk's REST API with `fetch` and asserted on
      the HTTP RESPONSE; the handler branches on the ERROR OBJECT the SDK
      throws, which that test never constructed. It proved the half that was
      never in doubt and skipped the half that broke.
      WHAT STAGING SHOWED: the admin saw a raw "Bad Request".
      FIRST DIAGNOSIS, WRONG: that isClerkAPIResponseError returned FALSE in the
      deployed runtime because ClerkAPIResponseError is defined 5x across bundle
      chunks. Withdrawn — see DEBT-15, which has been demoted to an unverified
      hazard. The SDK reproduction had already returned TRUE, and that
      disconfirming result was reasoned past rather than believed.
      ACTUAL CAUSE: the 409 handler was NOT DEPLOYED. Staging was running the
      pre-PERM-7 catch block, which is `err instanceof Error ? err.message` with
      a blanket 400 — and ClerkAPIResponseError.message is verbatim "Bad
      Request". The failure is fully explained by the absence of the feature,
      with no bundler behaviour involved.
      STILL TRUE AND UNAFFECTED: the dialog on /stores pre-checks against the
      org's known emails (Users + PendingInvite), so for the DEVICE-LOGIN flow
      the suggestion appears before the Clerk call. The failure above is on the
      /users invite dialog, which has no pre-check and depended entirely on the
      409. Not verified either way on staging.
      ONE STORE PER DEVICE ACCOUNT IS A CONVENTION OF THE DIALOG, NOT A
      REQUIREMENT OF THE PRODUCT. Established from code 2026-08-02 by DEBT-44a,
      which was asked whether anything actually REQUIRES a STORE account to have
      exactly one store. Nothing does. The hardcode is real and is scope item
      (a) working as designed — create-device-login-button.tsx:78 submits
      `storeIds: [store.id]` with the rationale at :23-26 — but everywhere else
      the count is a PREDICATE or a CONDITION, never a validator:
      device-login.ts:65-67 (isDeviceLogin CLASSIFIES for badge rendering; a
      three-store STORE user simply fails it and gets no badge),
      webhooks/clerk/route.ts:127 (Task 7's derivation is SKIPPED, not refused),
      checklists/page.tsx:37-38 and :71 (a picker instead of a lock — graceful
      degradation). And the multi-store state is reachable through the supported
      UI for STORE role: users/user-actions.tsx:104-117 and :236-249 render
      CHECKBOXES for every non-ADMIN role in both the invite and the edit
      dialog, and neither POST /api/users (route.ts:118) nor PATCH
      /api/users/[id] (users/[id]/route.ts:162-165, a full replace) applies any
      cardinality rule for any role. Recorded as a LINE not a row (Gary's
      ruling, 2026-08-02): there is no defect here, only a premise this phase
      overstated. THIS ROW ALREADY CARRIED THE COUNTEREXAMPLE — the 2026-07-28
      reframe found production's nearest account was a two-store MANAGER — and
      the reframe drew the boundary at "one-store-per-device REMAINS THE RULE
      for accounts this flow creates", which is true of the FLOW and was read as
      true of the ROLE. If the GAP wants tracking — that nothing enforces the
      convention this phase claims to establish — that is a separate row and a
      separate question.
      isDeviceLogin() CURRENTLY MATCHES ZERO USERS ON preview/staging, and a
      classifier with nothing to classify is worth knowing about. Queried
      2026-08-02, branch id br-square-feather-a63z92vz: exactly ONE STORE-role
      user exists on the branch — gary@keva.com, cms55ln6v000004jrtgs8gpx1 — and
      it has THREE assignments (Carson, Las Brisas, Meadowood Mall), so the
      predicate is false for it. The three device logins this phase created were
      deleted in DEBT-44's cleanup, and no user of ANY role on the branch has
      exactly one assignment (ADMIN x7 at zero, MANAGER x1 at two, STAFF x1 at
      two, STORE x1 at three). Task 6's badge therefore has nothing to render on
      staging today. Not a regression and not evidence against the phase — the
      accounts it was built for were removed on purpose — but any future staging
      pass that expects to SEE a device-login badge must provision one first.
      gary@keva.com's THREE assignments do not contradict PERM-6's recorded
      count of ONE; see DEBT-44 for why both readings are correct at different
      moments.
      DEBT-11 was NOT fixed here (Ruling 2) and DEBT-14 was filed rather than
      fixed — neither route is one this phase touches.
    blockers:
      - resolved: true
        text: >
          CLOSED 2026-08-01 — NOT BY A PASSING OVERRIDE TEST. THE OVERRIDE IS
          UNREACHABLE THROUGH THE PRODUCT AS CONFIGURED. Gary ran the evidence;
          this session verified the code and the record and wrote neither query
          nor row from the database.
          THE THREE RUNS, all on branch preview/staging (neon.branch_id
          br-square-feather-a63z92vz, 9 orgs on the branch), org
          cmr54z65v000105jxczpt72w1 BY ID per DEBT-19, all entered from /stores ->
          Carson -> Create device login, role STORE, exactly one store:
          RUN 1, indianathomas+store@gmail.com, username/password sign-up —
          User.name = "#0034 — Carson", n_assignments 1, is_default_store true,
          surviving_pending_invite 0.
          RUN 2, indianathomas+store2@gmail.com, username/password sign-up,
          username "kevacarson2026" — identical on every field.
          RUN 3, indianathomas+store3@gmail.com, Google OAuth attempted
          SPECIFICALLY to obtain a non-null first/last name. Clerk routed through
          Google and then returned to a Froot sign-up card asking for a USERNAME
          and nothing else. No name was ever collected. This run is the load-
          bearing one: it is the path most likely to carry a name, and it did not.
          WHAT THE RUNS PROVE ABOUT THE CODE: the derivation
          (webhooks/clerk/route.ts:126-137, written into the upsert's create
          branch at :151-153) executed three times and produced the store's name
          each time — including the NUMBERED form `#${storeNumber} — ${name}`,
          which had never executed anywhere before these runs.
          THE GROUND OF CLOSURE, which is not the same as the fill passing.
          `first_name`/`last_name` being ENABLED on the instance does not mean the
          sign-up flow RENDERS them: Clerk renders REQUIRED fields, and enabled
          means optional. Observed three times, including through OAuth, the flow
          presents username and password only. So no name is ever collected,
          `[first_name, last_name].filter(Boolean).join(" ")` is always "" and
          therefore null, and `deviceName ?? (…)` at :151-153 never has a
          competing value to lose to. The hazard the ruling defends against — a
          person's name landing on a shared store-device account — CANNOT OCCUR BY
          THIS PATH. That is a stronger result than a passing override test, which
          would have shown only that the override wins when contested; this shows
          the contest cannot arise.
          A THIRD OUTCOME THE RECORDING RULE BELOW DOES NOT CONTEMPLATE. That rule
          offers two labels — "Task 7 verified" and "fill verified, override
          UNTESTED" — and both are wrong here. It presupposes a tester who CHOSE
          not to type a name, making the gap a test-design debt a better-run pass
          would pay off. The gap is not payable: the tester cannot type a name
          because the form does not ask. The correct record is FILL VERIFIED,
          OVERRIDE UNREACHABLE. Do not force this into either bucket.
          CORRECTION, and it is worth more than the result — the reasoning error,
          not the conclusion, is the durable part. The paragraph below beginning
          "WHAT WOULD PROVE THE OVERRIDE" is WRONG, and is preserved unedited so
          the error stays legible. It infers TESTABLE from ENABLED, on a correct
          observation (GET /v1/environment, 2026-07-28, first/last enabled on both
          instances) that does not support it. ENABLED is a statement about the
          instance's schema; RENDERED is a statement about the sign-up flow, and
          only the second one governs what a tester can type. The earlier note it
          overrode — the one calling the override structurally impossible — was
          closer to right than the correction that replaced it, though not for a
          reason anyone had checked. Read ENABLED as RENDERED and you will write a
          test plan for a form that does not exist.
          THE EVIDENCE WAS ALREADY ON THIS ROW, and that is the pattern rather
          than the apology. The paragraph above beginning "THE EMPTY User.name IS
          THE EXPECTED RESULT" recorded on 2026-07-28 that "the invitee supplied a
          username instead". Nobody asked WHY. The answer — the form offers
          nothing else — was available that day and cost three staging runs four
          days later to rediscover. Same shape as DEBT-15 (a disconfirming SDK
          result in hand, reasoned past in favour of a bundler theory) and DEBT-23
          (a row built on a file whose ordering was never checked). The common
          move is accepting an observation as an outcome and declining to ask what
          produced it.
          REOPENING CONDITION — CHECK THIS BEFORE TRUSTING THIS CLOSURE. This row
          closes on a CONFIGURATION, not on a proof about the code, and the
          configuration is not pinned by anything in this repo. The closure no
          longer holds the moment EITHER of these becomes true:
          (1) `first_name` and/or `last_name` are made REQUIRED on either Clerk
          instance (Dashboard -> User & Authentication -> Personal information;
          re-checkable in one call via the public Frontend API GET /v1/environment
          at clerk.usefroot.com and verified-snapper-7.clerk.accounts.dev — the
          same call that produced the mistaken evidence above, read correctly this
          time: look at REQUIRED, not at enabled).
          (2) A sign-up path is added that collects a name by any other means — a
          custom sign-up component, an OAuth provider mapped to give
          first/last, an admin-created Clerk user, or a Clerk-side profile edit.
          If either fires, the override becomes REACHABLE and remains UNTESTED,
          the blocker below is live again as written, and DEBT-39 unmasks with it.
          WHAT THIS DOES NOT CLOSE — this is not "closing the pair". PERM-6's
          second blockers entry stands untouched: (2) the webhook's rejecting
          branch (`owned.length !== pending.storeIds.length`) has still never
          executed, (3) Task 1 (POST /api/staff) is still unexercised on staging,
          (4) the STAFF role is still unexercised. On this row, the rewritten
          Clerk error branch in POST /api/users is STILL DORMANT — all three
          invites succeeded — and DEBT-16's collision pre-check is still
          uncovered: the /stores dialog ran three times, but on three fresh
          addresses, so no collision was ever presented to it.
          STATUS: PERM-7 STAYS `shipped`, NOT `verified`. This is staging
          evidence, and staging evidence is not production evidence — the
          precedent DOCS-3 set on 2026-08-01 holds here without exception.
          BRANCH IDENTITY, a convention improvement these runs produced and the
          one place it is recorded. `current_setting('neon.branch_id', true)`
          returns a real value on Neon — br-square-feather-a63z92vz above came
          from the query itself, not from the person pasting it. CLAUDE.md
          § Database Evidence requires the branch be NAMED, which is an assertion
          by the reporter, and the 2026-07-28 failure that section documents was a
          correct query wearing a wrong label. Selecting the branch id ALONGSIDE
          the result makes the label a product of the same query as the row, so
          the two cannot drift. RULED 2026-08-01 (Gary): belt and braces — select
          the branch id AND name the branch, since the id is not the
          `preview/staging` name and the mapping still lives in a human's head.
          Recommended where available, not mandatory: it is Neon-specific and
          returns null on local dev, which is itself a useful signal rather than a
          failed check. Recorded here ONCE, deliberately, rather than at each
          § Database Evidence reference site — four copies is four things to
          drift. DEBT-19 carries the pointer.
          ORIGINAL TEXT, unedited:
          TASK 7: A GREEN STAGING RESULT DOES NOT MEAN "TASK 7 VERIFIED". READ
          THIS AT PROMOTION TIME — Gary, 2026-07-28.
          Task 7 has two halves and they are not equally testable. The FILL — an
          empty name gets the store's name — is what a normal staging sign-up
          exercises. The OVERRIDE — a name the invitee TYPED is replaced by the
          store's name — is the actual ruling (DECISIONS.md 2026-07-28: "the
          premise is not trusting the sign-up form"), and it is NOT exercised by
          the default path. On the development instance that serves staging,
          `username` is required and `first_name`/`last_name` are merely enabled,
          so an invitee who types nothing leaves the name blank and `deviceName`
          fills a hole rather than overwriting anything. That is the weaker half
          passing while the half the ruling is about goes untouched.
          WHAT WOULD PROVE THE OVERRIDE — it is a test-design requirement, NOT a
          structural impossibility, and an earlier note calling it structurally
          impossible was wrong. `first_name`/`last_name` are enabled on BOTH
          instances (verified 2026-07-28 via Clerk's Frontend API
          GET /v1/environment). So: invite as role STORE with EXACTLY ONE store,
          and at sign-up DELIBERATELY TYPE a first and last name — a person's
          name, e.g. "Tommy Thomas", the exact shape the convention exists to
          prevent. Then query, naming the branch. PASS = User.name is the STORE's
          name and the typed name is gone. FAIL = the typed name survives, which
          means the derivation is filling blanks rather than overriding and the
          ruling is not implemented.
          If a pass is run without typing a name, record it as "Task 7 fill
          verified, override UNTESTED" — never as "Task 7 verified".
      - resolved: true
        text: >
          RESOLVED 2026-08-01 — THE INVITE RAN START TO FINISH ON STAGING. The
          full record, the evidence and the deployment-SHA check live on PERM-6's
          row (its leading `blockers` entry) and are deliberately NOT duplicated
          here, so there is one copy to correct if any of it turns out wrong.
          WHAT IT MEANS FOR THIS PHASE, WHICH IS LESS THAN IT SOUNDS. The gate is
          satisfied as written, and what it exercised is PERM-6's Task 2 path —
          exactly what the rationale below predicted it would test. But NEITHER OF
          THE TWO CHANGES THIS PHASE MADE TO THOSE FILES EXECUTED. The invite
          SUCCEEDED, so POST /api/users never reached the Clerk error branch this
          phase rewrote — that branch WAS exercised the same day, but elsewhere
          and on other code: DEBT-15 records it green in PRODUCTION under
          promotion 97ed309, carrying DEBT-15's own payload-based guard (6f33427)
          on top of what this phase shipped, so it is not a clean test of this
          phase's handler either. And the role was MANAGER, not STORE, so the
          webhook's
          Task 7 name derivation — which fires only when pending.role === "STORE"
          AND the invite carries exactly one storeId — did not run. What DID
          execute is this phase's READ side: /users rendered with Task 4's device
          badge logic (users/page.tsx:149) and correctly produced NO badge, since
          indianathomas@gmail.com is not any location's contactEmail. A page that
          renders is weak evidence and is recorded here as weak.
          ALSO STILL UNEXERCISED: the /stores device-login dialog. This run went
          through the /users invite dialog. The two share a server path (POST
          /api/users) but not a client, so the device dialog's email-collision
          pre-check has still never run on staging — see DEBT-16.
          THE TASK 7 BLOCKER ABOVE IS UNTOUCHED by this run, neither the fill nor
          the override. A cleared promotion gate is not progress on it.
          ORIGINAL TEXT, unedited:
          SINGLE PROMOTION UNIT WITH PERM-6 — Gary, 2026-07-28. NEITHER PHASE
          REACHES MAIN UNTIL ONE REAL INVITE RUNS START TO FINISH ON STAGING.
          This phase modified POST /api/users (Clerk error-code handling) and the
          Clerk webhook (Task 7's create-only name derivation) — the same two
          files PERM-6 changed, and the same path PERM-6 shipped build-green
          without ever exercising. A staging pass of PERM-7's provisioning IS the
          first end-to-end test of PERM-6 Task 2's webhook validation, so the two
          phases share one verification and must share one promotion.
          NOT YET DONE at the time this row was written: the staging pass requires
          a deploy, which requires a push, which is Gary's alone.
          WHEN RUNNING IT, expect a shape difference that is NOT a bug: the
          development Clerk instance (which serves staging) has `username` as a
          REQUIRED identity attribute and production does not, so the acceptance
          exercises the BUG-2 path where Clerk's `identifier` is a username rather
          than an email. getClerkPrimaryEmail already guards it. A surprise
          anywhere else on that path is a PERM-6 bug worth reporting, not
          something to work around.
      - resolved: true
        text: >
          RESOLVED 2026-07-27 — PERM-6 is in staging (see its row). The write
          paths this phase builds on now validate storeIds. ORIGINAL TEXT:
          Do not start before PERM-6. This phase adds a NEW write path that
          creates users and store assignments; building it on top of the
          unvalidated storeIds paths in PERM-6 (a)/(b) would replicate the hole
          into a new surface rather than closing it.
      - resolved: true
        text: >
          WITHDRAWN 2026-07-28 (Gary) — THE ORIGINAL RATIONALE WAS WRONG, not
          merely superseded. This blocker was written onto the row on 2026-07-27
          claiming "PERM-7 is exactly the caller that trips [DEBT-11]". It is
          not, and the audit that opened PERM-7 caught it. DEBT-11 is in PATCH
          /api/staff/[id], which writes storeStaffAssignment. PERM-7 provisions
          storeUserAssignment through POST /api/users -> PendingInvite -> the
          Clerk webhook. Different table, different route, no overlap: PERM-7
          never calls the route DEBT-11 lives in. The claim was reasoning by
          analogy ("a new programmatic writer of storeIds") without checking
          which write path PERM-7 actually uses.
          The path PERM-7 does use is already safe at both ends, which is the
          other half of why this was never a blocker: POST /api/users dedupes at
          src/app/api/users/route.ts:86-88 (`[...new Set(...)]`, added by PERM-6
          Task 2), and the webhook builds its createMany from a findMany result
          with skipDuplicates: true at
          src/app/api/webhooks/clerk/route.ts:144-148.
          DEBT-11 REMAINS OPEN AND UNFIXED — deliberately, Gary's Ruling 2,
          2026-07-28. It stays debt under the PERM-2 scope precedent: a hole
          inside a function already being changed gets fixed inline, anything
          else gets written down. PERM-7 does not touch that route, so the test
          fails and it is left filed. Size was not the criterion — "four lines"
          is not a reason to widen a phase.
          ORIGINAL TEXT, kept so the error is legible: "READ DEBT-11 BEFORE
          WRITING THE PROVISIONING CALL. Cross-referenced here 2026-07-27 (Gary)
          because PERM-7 is the next phase and is exactly the caller that trips
          it: PATCH /api/staff/[id] dedupes storeIds for the ownership COMPARISON
          but builds finalStoreIds from the RAW array, so a body carrying the
          same storeId twice passes validation and then 500s on the
          (staffMemberId, storeId) unique constraint in createMany. Not reachable
          from today's UI — the dialogs hold selections in a Set — which is
          precisely why a new programmatic writer is the one that finds it. POST
          /api/staff already dedupes correctly after PERM-6 Task 1; copy that,
          and fix DEBT-11 rather than routing around it."
    open:
      - >
        RESOLVED 2026-07-28, both unknowns, with evidence rather than reasoning.
        (1) PLUS-ADDRESSING SURVIVES — Clerk never canonicalises subaddresses;
        it only optionally BLOCKS them, via the instance setting
        `block_email_subaddresses` (Dashboard -> Restrictions -> "Block email
        subaddresses"). That flag reads FALSE on both instances, checked
        2026-07-28 against Clerk's public Frontend API GET /v1/environment at
        clerk.usefroot.com (production) and verified-snapper-7.clerk.accounts.dev
        (development, which also serves staging). Backend API GET
        /instance/restrictions is 405 (PATCH-only), so /v1/environment is the
        one-call re-check for a future session. See DECISIONS.md 2026-07-28 —
        this is a dashboard setting nothing in the repo pins.
        (2) THE MAILBOXES ARE REACHABLE, on the best evidence available without
        the production Clerk key (Sensitive; pulls as [SENSITIVE]):
        kevajuice14@icloud.com and kevajuice06@icloud.com are both already live
        production Clerk identities with User rows and store assignments, so
        those per-location addresses have demonstrably completed a sign-up.
        Not distinguishable from a Clerk-dashboard add without that key.
        INCIDENTAL, recorded because it shapes the staging pass: the development
        instance has `username` as a REQUIRED identity attribute and production
        does not. A staging sign-up therefore exercises a different shape than
        production — and it is exactly the BUG-2 hazard (identifier is the
        username, not the email) that the webhook already guards against.
      - >
        Square's business_email is NOT guaranteed unique per location — it is
        free text, and many operators put one address on every location. Clerk
        requires a unique email per user, so N locations sharing an address
        means exactly ONE device login can be provisioned and the rest hard-fail.
        Keva's per-location convention (kevajuice14@) is good practice, not a
        Square guarantee.
        CONFIRMED LIVE 2026-07-28, not hypothetical: the production Square
        account returns 19 locations, and corporate@keva.com is the
        business_email on FOUR of them (Cafe De Keva Cart, Cafe De Keva Cart 2,
        Keva Juice, Keva Kiosk), gary@kevajuice.com on two more. All NINE
        currently-imported stores happen to hold distinct addresses, so the
        collision is latent today and fires on the next import.
        HANDLING (Gary, 2026-07-28): prefill the plus-addressed variant as an
        editable SUGGESTION rather than demanding the operator invent an
        address — "require a distinct address" is easy to write and may be
        impossible for them to satisfy. Detection must switch on the Clerk error
        CODE (already_a_member_in_organization, organization_invitation_not_unique
        — both HTTP 400) via isClerkAPIResponseError, not on message text.

  # ---- Security ----

  - id: SEC-1
    track: security
    size: S
    status: shipped
    shipped: 2026-07-26
    commits: [ecee728]
    title: "Square OAuth callback org binding + state nonce; ADMIN gate on connect/disconnect"
    notes: >
      VERIFIED IN PRODUCTION 2026-08-17, 20:01 Pacific. As kyla@keva.com —
      MANAGER, non-ADMIN — in org cf888f2d-f234-48c7-8097-fd5b44b5b3dd (Keva
      Juice), production Clerk instance ins_3FhV0TLHXvctLIxrPW0fjJe0uYw, app
      footer showing deployed commit 7e77ea6~main:
      fetch('/api/square/auth', {redirect:'manual'}) returned 403 Forbidden,
      logged as 403 'basic'. This closes the half that had been untestable since
      2026-07-26 — no non-ADMIN account existed in the production Clerk instance
      until the HR launch put six there. The ADMIN half was verified 2026-07-27
      (0 opaqueredirect, legitimate connect path unaffected).
      /api/square/disconnect was deliberately NOT tested — pressing it revokes
      Keva Juice's live Square token. It carries the same gate and is covered by
      inspection, not observation; that distinction is recorded rather than
      papered over. Affordance gating also held (no Settings link in her nav),
      but that is supporting evidence only — PERM-2 established a hidden link is
      not a guard. Session obtained via a Clerk actor token pasted onto
      /sign-in; see the impersonation row.
      (Gary's ruling, prepended verbatim per preserve-and-mark; NOTHING BELOW IS
      EDITED. Status deliberately untouched — whether this row moves to
      `verified` is Gary's call, not the recording session's.)

      TWO MEASURED RIDERS ON THE RULING ABOVE, added by the recording session
      2026-08-17 and kept separate from Gary's words rather than folded into
      them. (1) THE TIMESTAMP CROSSES MIDNIGHT: 2026-08-17 20:01 Pacific =
      2026-08-18 03:01 UTC. Recorded both ways per CLAUDE.md § Database
      Evidence — this is an 08-17 observation in Pacific and an 08-18 one in
      UTC, and the two dates do not reconcile if only one is written down.
      (2) THE DISCONNECT INSPECTION WAS RE-RUN AT HEAD rather than cited from
      memory: src/app/api/square/auth/route.ts and
      src/app/api/square/disconnect/route.ts carry the IDENTICAL gate — the same
      `await requireAdmin()` in the same try/catch returning the same
      { error: "Admin access required" } at status 403, under a comment naming
      SEC-1 Part C on both. Same gate, same shape, one of the two observed.
      RECORDED IN "22b8f26" — carried here as prose, not in commits:, because
      commits: means WORK commits on this row and the only one is ecee728
      (the 5ecceb3 convention from HR-11d). NOT PUSHED at the time of writing.

      Closes PERMISSIONS_INVENTORY.md §2 items 2 and 3 (found by PERM-1).
      Part A: callback resolves the token-write org from the SESSION — state
      is never a write address (beyond-spec improvement, kept by Gary; see
      DECISIONS.md). Part B: state is a 32-byte crypto-random single-use
      nonce via double-submit httpOnly cookie (Option 1, Gary's fork ruling —
      no schema; Square's one-time auth codes already cover the replay gap a
      table would close). Part C (the one deliberate behavior change):
      /api/square/auth and /api/square/disconnect now ADMIN, matching
      Instagram; only in-app callers are on /settings (already ADMIN).
      Existing connected orgs unaffected — no token columns touched, no
      reconnect needed. Instagram's own missing nonce logged as SEC-2.
      Staging pass clean 2026-07-25 (Gary; details not itemized in the
      pass report). Built 7-25; prod via c463af3 (2026-07-26). No prod smoke
      test recorded, hence shipped, not verified.
      Promotion path re-verified 2026-07-27, same method and same result as
      PERM-1: `git branch -a --contains ecee728` returns main and origin/main,
      `git log --ancestry-path ecee728..origin/main` puts the first merge at
      c463af3 (2026-07-26), and `git merge-base --is-ancestor ecee728 1ba059c`
      is false. SEC-1 did NOT ride the 2026-07-25 HR-17 promotion; the
      2026-07-26 date below stands.
    open:
      - >
        RESOLVED 2026-08-17 — THE NEGATIVE HALF IS DONE, WHICH CLOSES THE FIRST
        ENTRY BELOW IN FULL. Their text is untouched: `open:` takes bare strings
        only and carries no resolved flag (DEBT-42), so a closure arrives as an
        entry prepended ABOVE the one it closes — the mechanism PERM-6's two
        closed entries already use. Read this entry first.
        (1) CLOSED. kyla@keva.com, MANAGER and non-ADMIN, org
        cf888f2d-f234-48c7-8097-fd5b44b5b3dd, production Clerk instance
        ins_3FhV0TLHXvctLIxrPW0fjJe0uYw, deployed commit 7e77ea6~main read off
        the app footer on the same screen: fetch('/api/square/auth',
        {redirect:'manual'}) returned 403 Forbidden, logged as 403 'basic'.
        2026-08-17 20:01 Pacific = 2026-08-18 03:01 UTC. Both halves of Part C
        are now observed in production.
        (2) THE SECOND ENTRY BELOW IS NOT CLOSED BY THIS, AND SAYING SO IS THE
        POINT. Its capability half IS delivered — the HR launch put six
        non-ADMIN principals into the production Clerk instance, so a production
        test identity now exists. Its stated purpose is broader: the STAFF-role
        coverage gap on PERM-2 and the outstanding prod smoke tests on STAFF-1,
        HR-8, HR-11b/c and HR-15 are all still owed and are untouched by this
        entry. A capability is not a result; that entry stays open on the work,
        not on the account.
        STILL TRUE AND CLOSED BY NEITHER: the prohibition on exercising
        /api/square/disconnect in production. It revokes Keva Juice's live
        Square token. It is covered by inspection — the identical requireAdmin()
        gate, re-read at HEAD in both routes — and NOT by observation.
      - >
        HALF-CLOSED 2026-07-27. Part C's ADMIN gate was partially smoke-tested
        in production.
        POSITIVE HALF — DONE. As ADMIN in production,
        `fetch('/api/square/auth', {redirect:'manual'})` returned
        `0 opaqueredirect`, i.e. the redirect to Square still fires. That
        confirms the deny-by-default change did NOT break the legitimate connect
        path — the regression risk of a tightening change, and the one that
        would have stranded Gary without a way to reconnect Square.
        NEGATIVE HALF — STILL UNTESTED, AND CURRENTLY UNTESTABLE. Confirming a
        non-ADMIN gets 403 requires a non-ADMIN production account, and none
        exists in the production Clerk instance that Gary controls. This is not
        an oversight to be scheduled away; it is blocked on the item below.
        The standing prohibition remains: DO NOT verify via the Disconnect
        button — that revokes and clears Keva Juice's live Square token (and per
        SQ-1 the stored refresh token with it), which is a production outage,
        not a test. Note this is now more costly than before: the 2026-07-26
        refresh moved the live token to a 2026-08-26 expiry, and a disconnect
        would discard that.
      - >
        CREATE A PRODUCTION TEST ACCOUNT — a STORE or MANAGER user assigned to
        one store. Currently blocking the SEC-1 403 check above, but the gap is
        broader than one item: EVERY role-based verification to date has run
        through Tommy Thomas on STAGING's Clerk DEV instance
        (verified-snapper-7). Production has never had a non-ADMIN identity at
        all, so nothing in PERM-1/PERM-2/PERM-3 has been observed behaving in
        the environment that actually holds customer data — and per the
        DECISIONS.md ruling of 2026-07-27, prod is also where the STORE-is-a-
        device model has real consequences. Standing capability, not a one-off:
        the same account unblocks the STAFF-role coverage gap recorded on
        PERM-2 and the prod smoke tests still outstanding on STAFF-1, HR-8,
        HR-11b/c and HR-15.

  - id: SEC-2
    track: security
    size: S
    status: planned
    title: "Instagram OAuth state nonce — same double-submit hardening as SEC-1"
    notes: >
      Logged 2026-07-25 during SEC-1: Instagram's callback org-equality check
      blocks cross-org token planting but is not CSRF protection — state is
      still the predictable orgId, no nonce. Deliberately untouched in SEC-1
      (Instagram was the reference implementation); now the weaker of the two.
      Also in this phase's scope (Gary, 2026-07-25): SEC-1's session-org
      resolution means a multi-org admin who switches orgs mid-flow gets
      Square tokens silently written to whichever org their session is on at
      callback time — Instagram's equality check would error instead. Not a
      problem at one org; fix alongside the nonce work (e.g. bind the
      initiating org into the state cookie payload and reject on mismatch).
      UNBLOCKED as of BUILD-1's ship — struck 2026-08-06 by the bookkeeping
      sweep. This row carried "GATED ON BUILD-1 (2026-07-26): if the nonce work
      adds any schema field, it must not start before BUILD-1 is verified" for
      eleven days after the gate lifted. BUILD-1 shipped 6a77e68/06b1561,
      verified in production 2026-07-27, and its own notes name SEC-2 by id as
      one of the two rows that gate released; DEBT-4 is shipped. Ready to start.

  - id: SEC-3
    track: security
    size: M
    status: planned
    title: "Forecasting budget-file uploads are world-readable in Vercel Blob"
    notes: >
      Found by PERM-3's Task 1 audit (2026-07-26), recorded not fixed on Gary's
      instruction — it is a live data exposure, not a permissions tier, so it
      needs its own session.
      SEVERITY: a tenant's uploaded budget file is retrievable by URL with NO
      authentication whatsoever. POST /api/forecasting/import writes it to
      Vercel Blob with access: "public" at
      forecasting/${orgId}/${storeId}/${year}-${Date.now()}.${ext}, persists
      that URL on GoalPlan.importFileUrl and in the audit log, and
      /api/forecasting/plan GET hands it to any MANAGER. The only thing standing
      between the file and the public internet is the guessability of a
      millisecond timestamp — and anyone who has ever legitimately seen the URL
      keeps access forever, including after losing their role.
      Fix direction: move to a PRIVATE Blob store with signed, short-lived reads,
      the pattern HR documents already use (HR_BLOB_READ_WRITE_TOKEN +
      src/lib/hr-files.ts passing the token explicitly on every call). Existing
      blobs must be migrated or re-uploaded; the persisted importFileUrl values
      become stale and need a backfill or an on-demand signing indirection.
      Not a schema change in itself, but the backfill may want one — if so it is
      UNBLOCKED as of BUILD-1's ship — the trailing "GATED ON BUILD-1 like
      SEC-2 (a local build runs migrate deploy against the PRODUCTION branch
      until then, DEBT-4)" was struck 2026-08-06 by the bookkeeping sweep.
      BUILD-1 shipped 6a77e68/06b1561, verified in production 2026-07-27;
      DEBT-4 is shipped. If the backfill wants a schema change it can now have
      one.
      THE SEVERITY ABOVE IS UNCHANGED AND IS NOT WHAT WAS STRUCK. The exposure
      is LIVE and UNFIXED: tenant budget files remain retrievable by URL with no
      authentication at all. Clearing the schema gate makes this row READY TO
      START, which raises its urgency rather than lowering it — nothing now
      stands between this finding and a session.

  - id: SEC-4
    track: security
    size: S
    status: planned
    title: "Verify and rotate a possible plaintext GitHub PAT in the repo's git remote"
    notes: >
      Logged 2026-07-27 from a SECONDHAND claim, deliberately unverified.
      docs/STAGING_SETUP.md's housekeeping note states that this repo's git
      remote has a GitHub personal access token embedded in plaintext in the
      remote URL, and therefore sitting in .git/config.
      NOT CHECKED IN THIS SESSION, BY INSTRUCTION: the DOCS-3 session was told
      not to run `git remote -v` or read .git/config, and did not. So the claim
      is recorded as a claim. Note the source is the same file as DEBT-7, which
      is materially wrong in at least three other places — its accuracy here is
      not established either way, and it may equally describe a state that was
      already remediated.
      WHY IT STILL MATTERS IF TRUE: a PAT in .git/config is not encrypted, is
      readable by any process running as the user, and survives in any copy of
      the working tree. Depending on scope it may grant push access to this repo
      or to every repo on the account.
      HANDLING RULES for whoever picks this up: never echo, paste, log or commit
      the token value — not into a terminal, a report, this file, or a commit
      message. Establish presence without printing the secret. If present:
      rotate the token on GitHub FIRST (rotation invalidates it regardless of
      where copies have spread), then switch the remote to SSH or a credential
      manager, then confirm .git/config no longer carries it. Rotating before
      cleaning is the right order — cleanup without rotation leaves a live
      credential in any copy already made.
      Related: DEBT-7 (same source file).

  - id: SEC-5
    track: security
    size: S
    status: planned
    title: "Clerk impersonation redirects to / which has no SignIn component, so the actor token is never consumed"
    notes: >
      Found 2026-08-17 while obtaining a non-ADMIN session for SEC-1. Clerk's
      "Impersonate user" redirects to
      https://www.usefroot.com/?__clerk_ticket=<jwt> — the public marketing
      page, which renders and ignores the param. The token is NOT stripped; it
      survives in the URL. Pasting the same ticket onto
      /sign-in?__clerk_ticket=… signs in correctly on the first try, which is
      the documented workaround. Likely fix is one setting: point Clerk's
      impersonation redirect at /sign-in.
      MATTERS BEYOND TESTING — "sign in as this merchant to see what they see"
      is table stakes for supporting other Square merchants.
      This is the row SEC-1's 2026-08-17 verification note points at for the
      method it used to get a non-ADMIN production session.
      FILED IN "22b8f26". No commits: field — nothing is built, and commits:
      would imply it was. NOT PUSHED at the time of writing.

  # ---- Migration / nutrition ----

  - id: M-1
    track: migration
    size: M
    status: planned
    title: "Keva data migration (optional)"
    notes: >
      Unblocked since I-6. scripts/import-keva-templates.ts migrates checklist
      templates only — not inventory, recipe, or vendor data.

  - id: N-1
    track: nutrition
    size: M
    status: planned
    title: "Nutrition — schema & menu item manager"
    notes: MenuItem/MenuItemNutrition/MenuItemAllergen models and module flag scaffolded; no UI or routes.

  - id: N-2
    track: nutrition
    size: M
    status: planned
    title: "Nutrition — facts editor + label preview"
    blockers:
      - >
        BEHIND N-1 — promoted to a blocker entry 2026-08-06 so the board can
        tell this row from a ready one. N-1 is `planned`: per its own note the
        MenuItem / MenuItemNutrition / MenuItemAllergen models and the module
        flag are scaffolded, but there is NO UI and there are NO routes. This
        phase is a facts editor over exactly those models, so there is nothing
        to edit and nothing to edit it with until N-1 builds the surface.
        CLEARS WHEN: N-1 ships its routes and UI.

  - id: N-3
    track: nutrition
    size: M
    status: planned
    title: "Nutrition — public menu page (/menu/[orgSlug], SSR, iframe-embeddable)"
    blockers:
      - >
        BEHIND N-1 — promoted to a blocker entry 2026-08-06, same gate as N-2.
        N-1 is `planned` with models scaffolded but no UI and no routes, and a
        public menu page renders the nutrition data those routes would serve.
        CLEARS WHEN: N-1 ships. In practice this row wants N-2 as well, since a
        public page with no editor behind it has nothing to publish.

  # ---- Square integration ----

  - id: SQ-1
    track: square
    size: S
    status: verified
    shipped: 2026-07-27
    commits: [f93b906, 9bd61c7, 056943f, 06b1561]
    title: "Audit — Square OAuth access-token refresh (read-only)"
    notes: >
      SQ-1'S OPEN QUESTION IS ANSWERED AND THE ANSWER IS "NOT ZERO" — 2026-08-18
      (SQ-WB-1, work commit 0fd414a). Prepended; nothing below is edited. This
      row asked whether anything still READS the static SQUARE_ACCESS_TOKEN. The
      2026-08-06 bookkeeping sweep answered it at five sites; SQ-WB-1 removed one
      of them and re-counted at HEAD.

      FOUR READERS REMAIN, all in src/lib/square.ts, and the count did not fall to
      zero: :51 (the redactSquareSecrets redaction list), :130
      (fetchSquareTeamMembers), :169 (fetchSquareTeamMember) and :206
      (fetchSquareLocation). The fifth, :236 inside updateSquareTeamMemberName, is
      GONE with the write-back. Each of the three live ones is the same
      const tokens = [org.squareAccessToken, process.env.SQUARE_ACCESS_TOKEN]
      pattern — a fallback to the static credential when the per-org OAuth token
      is absent or empty.

      SO THE ENV VAR IS NOT A DELETION CANDIDATE, and SQ-WB-1 deliberately did not
      touch it (flag only, by its own scope). Deleting it today would break three
      READ helpers for any org relying on the fallback. The prerequisite is
      unchanged and is SQ-3's item (b): route the bypassing helpers through
      getSquareClient first, THEN the variable can go. What DID change is the
      severity, and it changed in the direction of lower: the three survivors all
      read, so the static credential no longer backstops any WRITE. The specific
      exposure LABOR-0B flagged — a PUT executing outside the merchant's consent
      grant on a credential they never granted — is closed at the source rather
      than at the credential.

      NOTE ON THE COUNT ITSELF: :51 is a redaction list, not a request path. It
      reads the value only to keep it out of logs, so a reader count of four is
      three call sites plus one safety net, and whoever finally deletes the
      variable must update :51 too or the redaction silently stops covering a name
      that no longer exists.

      Refresh EXISTS and is wired: refreshTokenIfNeeded() at src/lib/square.ts:37,
      called only from getSquareClient() (src/lib/square.ts:202), which refreshes
      when squareTokenExpiresAt is inside 7 days. Lazy only — no refresh cron.
      Production DB confirms Keva Juice has both an access and a REFRESH token
      stored, expiring 2026-08-06T09:48:55Z, so the 8-06 cliff is survivable
      without a reconnect provided one getSquareClient path runs on/after
      2026-07-30. Residual risk is silence, not absence: a failed refresh
      returns the stale org with no log (square.ts:56), the settings UI reports
      "Connected" off token presence alone, and dashboard sync errors are caught
      and served from cache. Four Square helpers bypass the wrapper entirely
      (fetchSquareTeamMembers / fetchSquareTeamMember /
      updateSquareTeamMemberName in square.ts, plus /api/square/locations) and
      no oauth.authorization.revoked webhook is handled. Audit only — no code
      changed. See docs/prompts/SQ-1_square_token_refresh_audit.md.
      Docs reached prod 2026-07-27 via promotion 06b1561; `shipped:` is recorded
      for that date even though this phase changed no product code, which is the
      status GAP noted in this file's header. The three listed SHAs carry the
      same commit message — they are amend/re-commit iterations of one audit
      write-up, not three separate pieces of work.
      SQ-1's central prediction was CONFIRMED in production 2026-07-26 21:39:15:
      the lazy refresh fired on a real request and moved the token off the
      2026-08-06 cliff. See SQ-2, which shipped the observability that made the
      confirmation legible. The residual risks SQ-1 flagged and SQ-2 did not
      close — the four helpers bypassing getSquareClient, the missing
      oauth.authorization.revoked handler, and the settings UI reporting
      "Connected" off token presence alone — are SQ-3.

  - id: SQ-2
    track: square
    size: S-M
    status: shipped
    shipped: 2026-07-26
    commits: [9dc6dc0]
    title: "Square token-refresh hardening — observability + refresh-ahead"
    notes: >
      Cherry-picked onto main alone (staging 81be8d9), ahead of the rest of the
      staging queue: the 2026-08-06 production token expiry made this the one
      time-critical item. BEHAVIOR CHANGE — the refresh trigger moved from
      "expires within 7 days" to "expires within 23 days". Square's guidance is
      to refresh every 7 days or less; with a 30-day token life and no issuedAt
      column, 23 days of remaining life (30 − 7) stands in for "older than 7
      days". Consequence: on the first production run, any token already inside
      that window refreshes immediately rather than at the old cliff. Also
      replaces the silent `if (!res.ok) return org` in refreshTokenIfNeeded with
      attempt/success/failure logging; no token value is ever logged, and any
      secret echoed back in a Square error body is redacted. VERIFIED on staging
      2026-07-26: runtime logs recorded `[square] token refresh success` against
      a real Square token (GET 200, org cmr54z65v000105jxczpt72w1) — the refresh
      call itself, not just the logging. Production's token (expiry 2026-08-06)
      was already inside the 23-day window at promotion, so the first Square
      request after deploy refreshes immediately rather than waiting; confirm it
      in the production runtime logs. Remaining SQ-1 follow-ups (cron backstop, helpers bypassing
      getSquareClient, oauth.authorization.revoked, settings UI showing expiry)
      are SQ-3 and stay on staging.
      CONFIRMED IN PRODUCTION 2026-07-27 — the follow-up above is CLOSED. Vercel
      Logs, Production, 2026-07-26 21:39:15, on both /api/dashboard/summary and
      /api/dashboard/sales:
        [square] token refresh success org=cf888f2d-f234-48c7-8097-fd5b44b5b3dd
        expiresAt=2026-08-06T02:48:55.000Z -> 2026-08-26T04:39:18.000Z
      The refresh fired on the FIRST Square-touching request after the SQ-2
      promotion, exactly as the 23-day window predicted. Production's token moved
      off the 08-06 cliff to 08-26, so **the 2026-08-06 expiry risk is CLOSED** —
      the deadline that made this the most time-critical item on the board is
      gone, and nothing now depends on a getSquareClient path running before
      2026-07-30.
      Worth recording WHY this was legible at all: that log line IS the
      observability SQ-2 added. Under the pre-SQ-2 silent
      `if (!res.ok) return org`, a successful refresh and a failed one both
      produced no output, so a failure would have been indistinguishable from a
      no-op until the token expired. The phase verified itself.
      That two dashboard routes both logged it is consistent with DEBT-6 —
      ordinary GET reads are what reach getSquareClient today.
      Note text carried verbatim from main at the 2026-07-27 reconcile to
      pre-resolve the known merge conflict on this row; staging's own SQ-2
      commit is 81be8d9, recorded inline above rather than in `commits:` so
      both sides of the merge match byte-for-byte.

  - id: SQ-3
    track: square
    size: S-M
    status: planned
    title: "Square integration follow-ups — cron backstop, bypassing helpers, revocation webhook, expiry in settings"
    notes: >
      Created 2026-07-27. The SQ-1 findings SQ-2 deliberately did not carry;
      main's SQ-2 note already names SQ-3 as their destination, so this row
      makes an existing forward reference real rather than opening new scope.
      Scope: (a) a daily refresh cron as a BACKSTOP to the lazy path, never a
      replacement — viable on Hobby, where once-per-day is the cap (see F-1);
      (b) route the four helpers that bypass getSquareClient through it —
      fetchSquareTeamMembers / fetchSquareTeamMember /
      updateSquareTeamMemberName in src/lib/square.ts plus
      /api/square/locations — since a bypassing call never triggers a refresh;
      (c) handle the oauth.authorization.revoked webhook, so a revoked grant
      stops presenting as "Connected"; (d) surface token expiry in the settings
      UI, which today reports "Connected" off token presence alone.
    open:
      - >
        ANSWERED 2026-08-06 (bookkeeping sweep), at source. Prepended as a new
        entry rather than edited into the one below, which stays as written —
        the question was correctly posed and its two branches are the record.
        IT IS READ. SQUARE_ACCESS_TOKEN appears five times in src/lib/square.ts:
        :130, :169, :206 and :236 each build `const tokens = [org.squareAccessToken,
        process.env.SQUARE_ACCESS_TOKEN].filter(Boolean)`, and :51 is the
        redaction list. Those four ARE the bypassing helpers item (b) names, so
        the overlap the entry below anticipates is total, not partial.
        THE FORK COLLAPSES TO BRANCH ONE: this is a SECOND, UN-REFRESHED
        CREDENTIAL PATH, not a stale credential to delete. Each site falls back
        to the env token when the per-org OAuth token is absent or empty, so the
        static credential silently backstops every one of them — it is not
        covered by SQ-2's refresh work and it would outlive a disconnect.
        Deleting the variable is therefore NOT the fix on its own: it would
        break these four call sites for any org relying on the fallback. Item
        (b) — routing the helpers through getSquareClient — has to land first,
        which makes (b) a prerequisite of the deletion rather than a sibling.
        NOT DONE HERE: the fix is code and belongs to this row's session.
      - >
        A static SQUARE_ACCESS_TOKEN exists in Vercel Production AND Preview
        (and in local .env). Determine whether anything still READS it — i.e. a
        helper bypassing the per-org OAuth tokens on Organization.
        squareAccessToken — or whether it is a stale credential to delete.
        These are opposite conclusions with opposite fixes: if it is read, it is
        a second, un-refreshed credential path that SQ-2's refresh work does not
        cover and that would outlive a disconnect; if it is unread, it is a live
        Square secret sitting in two environments for no reason. Note the
        overlap with (b) — the bypassing helpers are the most likely readers.

  - id: SQ-4
    track: square
    size: S
    status: planned
    title: "One-directional, on-demand import of business hours from Square Locations into StoreHours"
    notes: >
      SCOPE: an admin-initiated "Import hours from Square" action on the store
      hours dialog (or the store card) that reads a linked store's business
      hours from Square's Locations API and writes them into StoreHours for that
      store. One direction, on demand, per store. NOT a sync — nothing in this
      row runs on a schedule or on connect.
      GROUND: Square already holds real hours for every linked store, and
      CHK-2's editor requires them to be typed in by hand, per store, per
      weekday. Eleven of Keva's twelve staging stores had no hours set at the
      2026-08-10 walkthrough, which is exactly the condition that puts a store
      on the midnight fallback — so this is the cheapest available fix for the
      disclosure CHK-5's report has to print. Hand entry does not scale past
      Keva.
      THE CONSTRAINT IS ALREADY SATISFIED BY DESIGN AND MUST STAY THAT WAY:
      FROOT HOURS ARE NEVER OVERWRITTEN BY A SQUARE RESYNC. The S2/S4 hours
      dialog states this to the operator in its own copy, so the import has to
      be a deliberate action a human takes, and a second press must not silently
      undo an edit made in between — confirm before replacing an existing week.
      READ THE L-2 SEAM BEFORE TOUCHING SCOPES. Locations is already granted
      (src/app/api/square/locations/route.ts), so this should need NO new OAuth
      scope; per the L-2 DON'Ts, never add one opportunistically, because every
      addition re-consents every merchant. If the hours field turns out to sit
      behind a scope Froot does not hold, that is a ruling, not an
      implementation detail.
      FILED 2026-08-10 BY THE CHK PROMOTION SESSION, AND LATE. The S4 close-out
      prompt (docs/prompts/CHK-4_S4_closeout.md, Step 2) instructed this row to
      be filed on 2026-08-09; that session committed its code fix "bbb5734" and
      no docs at all, so the instruction survived only in the saved prompt. It
      is DEBT-37's rule one more time — see CHK-4's promotion rider, where the
      same omission swallowed the seven-check walkthrough.
      Related: CHK-2 (the editor this fills), CHK-3/CHK-5 (the midnight fallback
      this reduces), and S2's COMMENT 4 on timezone overwrite — the same
      "imported field vs operator field" question one column over.

  - id: SQ-ACK-1
    track: staff
    size: S
    status: planned
    title: >
      Acknowledgeable name divergence — let an admin mark a Froot/Square name
      difference as intentional, quieting the banner to a note
    notes: >
      FILED 2026-08-18 BY SQ-SCOPE-1 AS A RIDER, authorized by Gary in the same
      breath as the scope addition. Not built, not scheduled — filed. Gary's
      words on it: file, do not rush.
      SURFACED BY SQ-WB-1's REMOVAL OF THE WRITE-BACK (work commit 0fd414a,
      ruling in DECISIONS.md "Froot is read-only toward Square; the name
      write-back dies"). While the write-back existed, a name difference had an
      obvious resolution — push Froot's name to Square and the banner cleared
      itself. With the write-back gone and Froot read-only toward Square by
      ruling, that escape hatch is closed on purpose, and the banner has no
      terminating state.
      THE PROBLEM: the divergence banner is a PERMANENT NAG WHENEVER THE
      DIFFERENCE IS DELIBERATE. A staff member who goes by a different name in
      Froot than the legal or payroll name Square holds is a normal, correct
      state — not a data error. Today nothing distinguishes "these disagree and
      someone should look" from "these disagree and that is the point", so the
      second case is indistinguishable from an unresolved problem forever, and a
      warning that never clears is a warning people stop reading. That erosion
      is the actual cost, because the banner still has a real job in the first
      case.
      SHAPE (sketch, not a spec — the build session designs it): an admin marks
      a given divergence as intentional; the banner degrades from a warning to a
      quiet note that the names differ by choice; the note still shows both
      values so the difference stays visible rather than hidden. Worth settling
      at build time: whether the acknowledgement is pinned to the pair of values
      it was made against, so that a LATER change on either side re-raises the
      banner instead of staying silently acknowledged against names nobody
      approved.
      NOT A SCOPE QUESTION AND NOT A WRITE. Nothing in this row asks Square for
      anything or sends Square anything — the acknowledgement is Froot-side
      state about a Froot-side display decision. It is consistent with the
      read-only ruling and needs no new OAuth permission.
      TRACK NOTE: `staff` is a NEW track value as of this row — no other row
      carries it. Filed under the Square integration section because the SQ-
      id and the surfacing session both live here, and because the divergence
      it describes only exists where the two systems meet.

  # ---- Build / tooling ----

  - id: BUILD-1
    track: build
    size: S
    status: shipped
    shipped: 2026-07-27
    commits: [6a77e68, 06b1561]
    title: "Separate local builds from production migrations (vercel-build split)"
    notes: >
      Config only, no product code. Before: `build` was
      `prisma generate && prisma migrate deploy && next build`, so every local
      `npm run build` ran migrate deploy against whatever DATABASE_URL resolved
      to — which per DEBT-4 is the PRODUCTION branch. After: `build` is
      `prisma generate && next build` and a new `vercel-build` is
      `prisma migrate deploy && npm run build`. Verified at source, not assumed:
      @vercel/next resolves the build script via
      getScriptName(pkg, ["vercel-build", "now-build", "build"]), so vercel-build
      wins; and the project's Vercel Build Command is the default, NOT overridden
      (checked with `vercel project inspect`), which is what makes the approach
      valid. vercel-build delegates to `npm run build` rather than duplicating
      its body BECAUSE npm only fires pre-hooks matching the exact script name —
      verified empirically that `npm run vercel-build` runs prevercel-build, not
      prebuild. A copy-pasted vercel-build would skip
      scripts/generate-roadmap.mjs, and src/generated/roadmap.ts is gitignored
      and imported by the /internal/roadmap page, so the Vercel build would fail
      outright. Side benefit of the delegation: prisma generate now runs after
      migrations apply, not before. No CI exists (no .github/), and nothing else
      in the repo depended on `build` running migrate deploy.
      Local `.env` repoint is Gary's edit, not this session's — recommended a
      dedicated Neon dev branch over reusing staging, so local work cannot
      corrupt a staging verification run. Both DATABASE_URL (pooled) and
      DATABASE_URL_UNPOOLED (direct) need to point at it.
      CORRECTED 2026-07-27 — the sentence here previously read that
      DATABASE_URL_UNPOOLED was "ABSENT from local .env entirely", so
      prisma.config.ts had been falling back to the pooled production URL for
      every CLI command since BUG-3. That is now stale on BOTH counts. Verified
      2026-07-27: DATABASE_URL_UNPOOLED IS present in local .env, host
      `ep-late-water-a6k53nv2` with no `-pooler` suffix, and DATABASE_URL is the
      same endpoint's `-pooler` host. There is no fallback happening and
      prisma.config.ts emits no warning. Note the endpoint id: `ep-late-water`
      is neither production (`ep-green-smoke`) nor staging (`ep-odd-rain`), so
      the recommended dedicated dev branch appears to have been created and
      wired up — see DEBT-4, whose "NOT done yet" text is stale for the same
      reason.
      VERIFICATION STEP 3 SATISFIED 2026-07-27, IN PRODUCTION — stronger than
      the staging log this phase asked for. Production deployment EHfDfAKJR
      (14:49) logged:
        Running "npm run vercel-build"
        > prisma migrate deploy && npm run build
        Loaded Prisma config from prisma.config.ts
        31 migrations found; No pending migrations to apply.
      Both halves of the risk are answered by those four lines. `vercel-build`
      WAS picked up — Vercel invoked it by name, confirming the @vercel/next
      getScriptName precedence held against a real deployment rather than
      against a source reading. And migrate deploy ACTUALLY RAN — it enumerated
      31 migrations. The failure mode this phase feared was migrations silently
      ceasing to apply while builds stayed green, and a run that finds and
      reports the ledger is the opposite of that.
      STATUS CALL (Claude, 2026-07-27 — flag for Gary to overrule): flipped to
      `shipped`, blocker retained below. Reasoning: the code is in production
      via 06b1561 and this phase's own stated exit criterion is now met. The one
      remaining item is a Vercel-console configuration change that BUILD-1
      explicitly scoped OUT of itself ("out of scope for BUILD-1"), so holding
      `in_progress` would leave the phase permanently unclosable on work it
      disclaimed. Per this file's header, `blockers` are live problems "in a
      phase regardless of status" — a shipped row carrying an open blocker is
      the schema working as designed, not a contradiction. If Gary would rather
      the row stay open until the Preview vars exist, `in_progress` is the
      defensible alternative and only this line changes.
      Related: DEBT-4 (the finding this addresses), BUG-3 (closed by the same
      pair of build logs), SEC-2 and HR-14 (both schema-touching, both gated on
      this landing first — that gate is now lifted).
    blockers:
      - narrowed: true
        text: >
          CHANGED SHAPE 2026-07-27 — HALF CLOSED, HALF DEFERRED. This blocker is
          NOT the same problem it was this morning; read the new state before
          acting on it.
          CLOSED — step 1 of the two-step fix is DONE. Gary edited the Jun 26
          `DATABASE_URL` row in the Vercel console from **Production and
          Preview** to **Production only**. Preview deploys from non-staging
          branches therefore no longer resolve to the production database, and
          the original danger — a PR preview build running `migrate deploy`
          against production through pgbouncer, which was also the BUG-3 P1002
          condition — is gone. The edit was proven non-destructive: production
          redeployed successfully afterward with the Datasource still resolving
          to ep-green-smoke, so scoping the row did not disturb its value.
          STILL OPEN — step 2 is deliberately deferred. No generic
          Preview-scoped `DATABASE_URL` or `DATABASE_URL_UNPOOLED` exists, so a
          preview build from any branch other than `staging` now has NO database
          and FAILS AT BUILD TIME. That is the intended fail-closed posture and
          is strictly better than the previous fail-open one — a loud build
          failure beats a silent write to production. It costs nothing today
          because Gary pushes only `staging` and `main`.
          WHAT IT BLOCKS: the deferred second-developer plan. PR previews are
          preview deploys, so the first collaborator's first PR gets a failed
          build. FIX WHEN COLLABORATION WORK STARTS: create a throwaway Neon
          branch, then add BOTH vars Preview-scoped with NO branch restriction.
          UNTOUCHED: the staging branch-scoped override still exists and still
          wins for `staging`, which is why the 13:14 staging build resolved to
          ep-odd-rain rather than failing.

          ADDED 2026-08-17 (DEBT-72a) — WHICH NEON BRANCH THE THROWAWAY IS
          FORKED FROM, so this reasoning does not have to be rediscovered.
          Nothing above is edited. Fix when a second developer's first PR is
          expected. The Preview database must NOT be `preview/staging` — an
          unreviewed PR migration would apply to the branch used for promotion
          verification. Prefer a throwaway Neon branch forked from `dev`, not
          from `production`, so preview deploys never hold a copy of real
          customer data.

  - id: BUILD-2
    track: build
    size: S
    status: shipped
    commits: [118a02d, 944dfa3, f480568]
    title: "Deterministic default/primary store for multi-store users"
    notes: >
      PROMOTED TO PRODUCTION 2026-07-29 as 493175e. FAST-FORWARD, not a merge —
      origin/main HEAD has a single parent (f480568), so there is no merge
      commit and no `git revert -m 1` rollback. All four commits are ancestors
      of main; both migrations applied via `prisma migrate deploy` in the Vercel
      build. See docs/DEPLOY_LOG.md, 2026-07-29.
      SHIPPED, NOT VERIFIED — and the distinction is the point. What was
      actually confirmed:
      (a) Query A (duplicate primaries) returned ZERO ROWS on branch
      `production`.
      (b) The defaultStoreId column and the
      StoreStaffAssignment_one_primary_key index were confirmed PRESENT on
      branch `preview/staging`.
      What was NOT done: the column and index have NOT been confirmed on branch
      `production`, and NONE of the five UI checks below were run. (b) is
      staging evidence and says nothing about production — dev and staging are
      separately seeded (docs/MIGRATIONS.md, "Which branch am I actually
      reading?"). This row moves to `verified` when Gary has confirmed the
      column and index on branch `production` AND driven the five checks. Held
      at `shipped` deliberately rather than claiming more than was checked
      (Gary, 2026-07-30).
      RE-CONFIRMED 2026-08-06 (bookkeeping sweep) — STILL SHIPPED, STILL NOT
      VERIFIED, and that is bookkeeping-correct rather than a stale status. The
      build work reached production 2026-07-29 as 493175e and nothing about it
      is outstanding. WHAT REMAINS IS VERIFICATION WORK, NOT BUILD WORK: the
      column/index check on branch `production` plus the five UI checks below.
      So this row is neither ready-to-start nor blocked — it is done, awaiting a
      production visit. Cheapest path is to fold it into the next one alongside
      HR-11c's two prod-verification items and SEC-1's test-account request,
      which is the same visit.
      CORRECTED 2026-07-29 — THE NONDETERMINISM RATIONALE BELOW IS REFUTED. The
      row claims primaryStoreName() "picks on an unordered query" and "may
      return a different one per request", and that "the codebase is already
      split" with only hr-signed-pdf.ts:764 deterministic. FALSE against
      current code. The function has exactly TWO callers and BOTH load
      storeAssignments with the same
      `orderBy: [{ isPrimary: "desc" }, { store: { name: "asc" } }]`:
      api/hr/documents/[id]/acknowledgments/route.ts:172 (staff loaded at
      :94-102, orderBy :99; self path via src/lib/hr.ts:23) and
      api/hr/forms/[id]/submissions/route.ts:143 (via
      api/hr/forms/shared.ts:48). The codebase is UNIFORM, not split.
      WHAT SURVIVES: the index still earns its place, on the "the database
      refuses two" rationale rather than the nondeterminism one — it converts
      "every writer happens to write at most one primary" into an enforced
      invariant. All four writers were checked and all already comply
      (api/staff/route.ts:105; api/staff/[id]/route.ts:126-133 and :136-144;
      sync-square:77-86; resync-square:93-100). Verified ENFORCING on branch
      dev 2026-07-29: setting all 9 of Kelton Thomas's assignments primary was
      rejected with "duplicate key value violates unique constraint
      StoreStaffAssignment_one_primary_key", statement aborted atomically, no
      rows written. Task 8 additionally gave primaryStoreName() its own
      internal tie-break so the guarantee stops depending on caller discipline
      (17/17 assertions, verified no-op against both callers).
      STALE DEFAULTS — FIRST-CLASS FINDING, NOT A FOOTNOTE. Nothing nulls
      defaultStoreId when a user's assignments are removed. The Clerk webhook
      drops every StoreUserAssignment on organizationMembership.deleted
      (webhooks/clerk/route.ts:221-235, deleteMany at :234) and does not touch
      User columns; an admin edit replaces the whole set
      (users/[id]/route.ts:164-167). The FK targets Store, so onDelete: SetNull
      fires ONLY when the STORE is deleted — losing access to a store that
      still exists leaves the column pointing at it. Read-time revalidation is
      therefore LOAD-BEARING, not defensive: it is the only thing standing
      between a stale default and a user landing on a store they have lost.
      NOTE THE PROMPT FILE'S CITATION WAS WRONG — it cited
      webhooks/clerk/route.ts:180 for the deletion; :180 is a createMany in the
      pending-invite path. Correct citation is :221-235, deleteMany at :234.
      RESURRECTION IS INTENDED BEHAVIOUR, decided rather than inherited.
      Because the column persists, a user who loses a store and later regains
      it silently gets their old default back. This is harmless, and harmless
      STRUCTURALLY rather than by luck: a stale default is INERT because access
      is decided by the assignment set and the default only ranks WITHIN it.
      resolveDefaultStore() selects from visibleStores, so the column can never
      widen reach — the worst case is an unexpected landing page, never an
      unreachable store becoming reachable. For a PERM-7 device account it
      cannot even do that: a device login is role STORE with exactly one
      assignment (device-login.ts:66-68), so visibleStores has one element and
      the resolver returns it whatever the column says.
      THE DESIGN PREMISE IS A UI CONVENTION, AND THAT STRENGTHENS THIS DESIGN.
      Admins CAN acquire StoreUserAssignment rows: users/[id]/route.ts:164-167
      creates them unconditionally and the client always sends storeIds
      (users/user-actions.tsx:189), so promoting a two-store MANAGER to ADMIN
      writes two admin assignment rows. READ THIS AS CONFIRMING THE isPrimary
      REJECTION, NOT UNDERMINING IT: if admins have assignment rows SOMETIMES,
      depending on role history, then isPrimary on StoreUserAssignment is an
      even worse home for a default than the original argument said — it would
      have worked for some admins and not others, with the difference invisible
      and determined by whether someone was once a MANAGER. A nullable column
      on User is unconditional. Filed for PERM, not fixed here; also recorded
      in PERMISSIONS_INVENTORY §3 item 10.
      DEBT-17 INTERACTION. The device-account default is set inside the
      webhook's `if (pending)` block, which DEBT-17's 2026-07-28 audit
      established fires whenever a PendingInvite matched, regardless of which
      writer won the race (webhooks/clerk/route.ts:158-196). So a device login
      gets its defaultStoreId even when /users wins and persists the wrong
      role. DEBT-17's chosen fix DIRECTION B changes the page only and cannot
      disturb this. OPTION C (a shared syncOrgMembership()) WOULD have to
      preserve the default-setting deliberately — noted on DEBT-17's row too.
      Minor side effect: DEBT-17's forensic signature "role STAFF holding store
      assignments" gains a field — such a row will now also carry a
      defaultStoreId when exactly one store was provisioned.
      STAGING VERIFICATION REQUIRED — could not be done locally (the Edit User
      modal is behind an authed admin session; Claude does not sign in). Local
      evidence is `next build` green, tsc/eslint clean, and 17/17 logic
      assertions on the pure helpers. STILL UNEXERCISED until driven on
      staging:
      (1) Default Location select RENDERS in the Edit User modal.
      (2) Options FILTER to the locations ticked in that same modal.
      (3) Deselecting the chosen default's store CLEARS it.
      (4) A role change (ADMIN → STORE with the default unselected) does not
      submit a stale value — the select shows "" and the PATCH sends null.
      (5) A newly provisioned SINGLE-STORE device login comes out with its own
      store as defaultStoreId.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      Gary's rule is that a multi-store manager's default store is "the first
      store on their profile" — but StoreUserAssignment has no primary flag and
      no ordering column, so "first" is currently whatever Postgres happens to
      return and can vary between queries. Making it deterministic requires a
      schema change (mirror StoreStaffAssignment.isPrimary, which the staff side
      already has). Raised and deliberately deferred in PERM-2. Any code needing
      to pick a store before this lands should sort deterministically (store
      number, then name) and say so in a comment.
      RESCOPED 2026-07-27 — THE isPrimary DESIGN ABOVE DOES NOT WORK. Adding
      isPrimary to StoreUserAssignment cannot express a default store for an
      ADMIN, because admins have NO StoreUserAssignment rows at all: every page
      does `...(isAdmin ? {} : { id: { in: storeIds } })` (src/lib/auth.ts:137-142
      and every consuming page), and the UI hides the store picker for ADMIN
      entirely ("Admins have access to all locations automatically",
      src/app/(app)/users/user-actions.tsx:230). There would be no row to flag.
      That is disqualifying rather than inconvenient, because the ADMIN device
      account in PERM-7 is exactly the case that most needs a default store.
      NEW SHAPE: a nullable `defaultStoreId` FK on **User** (onDelete: SetNull),
      which works for every role including ADMIN. One additive column.
      Set by the admin at provisioning — for a PERM-7 device account it defaults
      to that account's own store — and editable by the user themselves.
      ALSO CORRECTED: the premise that "first store" is nondeterministic is
      FALSE for store pickers. Every page selects with
      `orderBy: { name: "asc" }` and takes `stores[0]`, so today's default is
      deterministic — just arbitrary (alphabetically first). The unspecified
      ordering of `dbUser.storeAssignments` is real (no orderBy at
      src/lib/auth.ts:137-142) but is only ever used as a
      `WHERE id: { in: storeIds }` filter, never indexed into. So this phase is
      about giving users a CHOSEN default, not about fixing a random one.
      Consumed by UX-2, which is where the user-visible payoff lands; BUILD-2 is
      the schema half only. UNBLOCKED as of 2026-07-27 — BUILD-1 shipped.
      SECOND MIGRATION FOLDED IN 2026-07-27: the partial unique index on
      StoreStaffAssignment.isPrimary, moved here from PERM-6 so that PERM-6
      stays a no-schema size-S phase and both DDL changes ride one deploy.
      WHY IT MATTERS — the consequence is a legal document, not a pixel.
      `primaryStoreName()` (src/lib/hr.ts:44-49) picks with
      `.find(a => a.isPrimary) ?? storeAssignments[0]` on an unordered query, so
      with TWO primaries it returns an arbitrary one, and may return a different
      one per request. That value is persisted at signing time
      (api/hr/documents/[id]/acknowledgments/route.ts:172,
      api/hr/forms/[id]/submissions/route.ts:143) and stamped into the signed
      PDF, including the certificate line at src/lib/hr-signed-pdf.ts:615. Two
      documents signed by the same person the same afternoon could therefore
      name different stores, permanently. Note the codebase is already split on
      this: hr-signed-pdf.ts:764 queries with
      `orderBy: [{ isPrimary: "desc" }, { store: { name: "asc" } }]` and IS
      deterministic under duplicates; primaryStoreName() has no tie-break.
      The realistic cause is a future writer, not concurrency — see PERM-6.
      Session prompt: docs/prompts/BUILD-2_default_store.md
      PRE-CHECK RUN AND CLEARED 2026-07-27 — the blocker below is CLOSED, text
      retained per this file's convention because the method and the trap it
      documents apply to EVERY future production check, not just this one.
      RESULT: Query A (duplicate primaries) returned ZERO ROWS. Run by Gary in
      the Neon console, project `froot` (Keva Brands), database `neondb`,
      **branch `production`**, compute Primary/Active — branch confirmed from
      the console UI, not inferred from an env var. 72ms, "Statement executed
      successfully", no rows. The partial unique index can therefore be created.
      WHAT THAT ALSO ESTABLISHES: across all four write paths the "at most one
      primary" invariant has held in production. There is no existing
      corruption, which independently confirms the correction recorded on
      PERM-6 — the risk here is a FUTURE writer, not a race and not damage
      already done. A clean table is the cheapest moment to lock an invariant,
      and every new write path (PERM-7, any bulk edit) narrows that window.
      STALENESS IS SAFE, not a hazard: if a duplicate appears between now and
      the migration, index creation FAILS rather than corrupting anything. The
      cost is a failed deploy, not data damage. Re-run only if a long gap opens.
      SEPARATE FINDING FROM THE SAME PASS — see DEBT-9. Query B returned two
      ACTIVE staff (Gary Thomas, Kelton Thomas) with 9 store assignments and
      ZERO primary. NOT a migration blocker — a partial unique index permits
      zero — but a live data-quality problem feeding signed documents.
      CLOSED BLOCKER, retained for method (was: the hard gate requiring this
      check). THE TRAP, still true and still worth reading before any future
      production check: local `.env` DATABASE_URL resolves to
      `ep-late-water-a6k53nv2` — the DEV branch. Production is
      `ep-green-smoke-a6xthq4r`; staging is `ep-odd-rain-a6gr4xmm`. So
      `npx prisma studio`, `prisma db execute`, or any local tsx script reads
      DEV and returns a FALSE ALL-CLEAR. Before BUILD-1/DEBT-4 local pointed AT
      production, so this mistake was not possible; the repoint — otherwise
      correct — created it. A result with no host recorded beside it does not
      count. PREFERRED METHOD: the Neon console against the production branch,
      which keeps production credentials out of the working tree (DEBT-4's own
      guidance) and shows the branch in the UI. The two queries used are
      preserved in docs/prompts/BUILD-2_default_store.md Task 1a.

  - id: BUILD-3
    track: build
    size: S
    status: withdrawn
    title: "2026-08-19 07:03 production redeploy of 27ae79a failed — prisma migrate deploy: datasource.url required"
    notes: >
      CLOSED — transient build-environment failure. Filed for the record rather
      than fixed: nothing was changed, and nothing is outstanding.
      STATUS NOTE, AND IT IS A DEVIATION FROM WHAT WAS ASKED: Gary specified
      status CLOSED. There is no `closed` value — PhaseStatus in
      src/lib/roadmap.ts is a closed union (planned | in_progress | staging |
      shipped | verified | withdrawn) and src/generated/roadmap.ts is typed
      against it, so `closed` fails the build typecheck rather than rendering
      oddly. `withdrawn` is the only value that means resolved-and-not-built:
      it counts as resolved in isResolvedDebt and claims no board column
      (DEBT-37), which is where a closed incident belongs. The imperfect fit is
      its documented sense of "retracted" — this was investigated and dismissed,
      not retracted. If CLOSED should exist as its own value, that is Gary's
      call and it is the same shape as the ruling that added `withdrawn` on
      2026-08-01; it would touch the union, BOARD_COLUMNS and isResolvedDebt,
      which is code and was out of scope for a recorder commit.
      WHAT HAPPENED. A production redeploy of 27ae79a — the DEPLOY_LOG entry for
      the 90a8eca Square/labor promotion, committed 2026-08-18 21:57:26 local —
      failed at 07:03 on 2026-08-19 (time as reported from the Vercel dashboard,
      i.e. local, not UTC) with `prisma migrate deploy` reporting that
      datasource.url is required.
      THREE OBSERVATIONS FROM THE FAILED BUILD, recorded because together they
      are what makes it environmental rather than a defect: the build ran WITHOUT
      CACHE; npm allow-scripts BLOCKED THE PRISMA POSTINSTALL SCRIPTS; and the
      environment variables were VERIFIED UNCHANGED in Vercel that morning. A
      blocked postinstall means the Prisma client was never generated for that
      build, which is the reading under which "datasource.url required" is a
      symptom of the missing generate step rather than of a missing or malformed
      URL — and the unchanged env vars are what rule out the other reading.
      RESOLUTION: transient build-environment failure. The same-machinery
      production promotion build 5e2f4d7 SUCCEEDED THE SAME DAY (promote:
      Advanced Labor Phases 1-3, 2026-08-19 14:39:04 local), on the same project,
      the same branch target and the same migration machinery — so whatever
      blocked the 07:03 run was not present seven hours later and required no
      change to fix. REOPEN ONLY IF THE ERROR RECURS.
      NO COMMITS FIELD, deliberately, and it is the `withdrawn` convention
      rather than an omission: nothing was fixed, so there is no SHA that
      resolves this row. 5e2f4d7 is cited above as EVIDENCE that the failure did
      not persist, which is a different claim from a fix, and putting it in
      `commits` would assert the stronger one.
      PRESERVE-AND-MARK: BUILD-2 is not edited by this row. The two are
      unrelated beyond the shared `build` track — BUILD-2 is the
      default/primary-store work and carries its own separate, still-open
      verification debt.

  - id: POLISH-1
    track: labor
    size: S
    status: shipped
    shipped: 2026-08-20
    commits: [0e9d6cb]
    title: "Monthly Goal card — the MTD labor % reads at the same size as the extrapolated month-end figure"
    notes: >
      FILED 2026-08-20, AFTER THE CODE, and the row is the point: 0e9d6cb was
      committed 2026-08-19 naming POLISH-1 in its own message while no POLISH-1
      existed on the board, so the promotion merge 248a80b was carrying UI to
      production with nothing here describing it. Caught in the pre-push read
      of that merge and filed at Gary's instruction. This is DEBT-72a's failure
      mode arriving from the other direction — not a row left stale after its
      code moved, but code moved with no row at all.
      WHAT IT DID, display-only. The Monthly Goal card's "<month> labor %" row
      now sits on the same visual rung as the EXTRAPOLATED TO MONTH END pair
      above it: value text-xl font-extrabold, label the small-caps/tracking
      treatment, both COPIED from that block's classes rather than assumed.
      HOW IT AVOIDED TOUCHING THE OTHER TWO SURFACES. The classes live in
      LaborPctLine (src/app/(app)/dashboard/labor-pct.tsx), which three surfaces
      share, so the change is an opt-in `prominent` prop DEFAULTING TO FALSE and
      passed at exactly one call site — the Monthly Goal card in
      dashboard-client.tsx:639. The Sales-goal-unset state of that same card and
      the All Locations summary render byte-identically to before. Verdict
      colours, the "over" bold-red state, the asterisk and the footnotes are
      untouched; `prominent` is typography and nothing else.
      NOT VERIFIED IN PRODUCTION — `shipped` states where the code is, not that
      anyone has looked at it. It is display-only on a card that is on the
      dashboard's first screen, so the check is a glance, not a procedure.


bugs:
  - id: BUG-1
    title: "Dashboard cards intermittently fail to load"
    status: shipped
    shipped: 2026-07-23
    commits: [6a14fb6, 2081401]
    notes: >
      Steps 1–3 in 6a14fb6 (7-22): card failure states with manual retry, 12s
      fetch timeout, API error/duration logging, maxDuration on sales-sync
      routes. Step 4 shipped inside STAFF-1 (2081401): request-path Square
      sync moved behind after().
      APPENDED 2026-08-13, NOT A CORRECTION — step 4 above is accurate and the
      trade it made was the right one. But it had a consequence nobody wrote
      down: moving the sync behind after() means the response that TRIGGERS the
      refresh is composed before it runs, so the dashboard serves numbers one
      request old whenever the cache is stale. That is BUG-6, found 2026-08-13
      and fixed in 37361ba by having the client re-fetch until the cache
      timestamp advances — NOT by reverting step 4, which would restore exactly
      the hung card this row was raised to fix.
      THE LESSON IS ABOUT WHAT MADE IT INVISIBLE FOR THREE WEEKS: step 4's own
      comment says "the next load sees the refreshed cache", which was true and
      read as harmless because Square order webhooks were assumed to be keeping
      the cache fresh. They were not — F-4's signature key has been absent from
      every environment since 2026-07-10. A deferral is only safe while the
      thing it defers to is actually running; this one leaned on a feature that
      had been dead for thirteen days when it shipped.

  - id: BUG-2
    title: "Staff-profile linking failed on sign/acknowledge — Clerk identifier vs email"
    status: shipped
    shipped: 2026-07-23
    commits: [3c7d0a0]
    notes: Found during the HR-8 staging pass (7-22); resolve the real Clerk email, never trust the identifier.

  - id: BUG-3
    title: "Migrations bypass Neon pooler via directUrl"
    status: shipped
    shipped: 2026-07-27
    commits: [f6818f1]
    notes: >
      Intermittent P1002 on deploy — Prisma's migration advisory lock leaked
      onto recycled pgbouncer backends (hit ecee728, 7-25, no migration in it).
      Prisma 7 removed schema directUrl; fix is prisma.config.ts datasource.url
      = DATABASE_URL_UNPOOLED ?? DATABASE_URL (loud console.warn on fallback).
      No migration, no schema change. DATABASE_URL_UNPOOLED pre-exists in all
      Vercel envs (Neon integration); local .env gets it manually. See
      DECISIONS.md BUG-3. Staging proof required: build log Datasource "db"
      host without -pooler; a green deploy alone is not proof (P1002 is
      intermittent).
      History: f6818f1 reached main via c463af3 (2026-07-26), but status was
      deliberately held at in_progress through the 2026-07-26 reconcile because
      the required proof had never been recorded. Code shipped ≠ bug closed.
      PROOF SATISFIED 2026-07-27 — flipped to shipped. Both build logs show the
      Datasource resolving to the DIRECT endpoint, neither host ending
      `-pooler`:
        staging    (13:14): Datasource "db": ... at "ep-odd-rain-a6gr4xmm..."
        production (14:49): Datasource "db": ... at
                            "ep-green-smoke-a6xthq4r.us-west-2.aws.neon.tech"
      The production log (deployment EHfDfAKJR) also reads "31 migrations found;
      No pending migrations to apply."
      The NEGATIVE evidence is what closes it, and it is the half a green deploy
      could never supply: NEITHER log contains the
      `[prisma.config] DATABASE_URL_UNPOOLED is not set` warning that
      prisma.config.ts emits on fallback. Because the fix is a `??` fallback
      rather than strict, a pooled connection would still have deployed
      green — so "no warning" is the only thing that distinguishes the fix
      working from the fix silently degrading. That is precisely why the notes
      above insisted a green deploy was not proof.

  - id: BUG-4
    title: "Labor Budget card and Weekly Plan disagree on schedulable hours when no weather adjustment exists"
    status: planned
    notes: >
      Logged 2026-07-27, not fixed. The dashboard Labor Budget card shows
      `totalSchedulableHours` unless a weather adjustment exists, while the
      Weekly Plan ALWAYS shows `adjustedTotalSchedulableHours`. Confirmed in
      source: src/app/(app)/dashboard/labor-budget-card.tsx:179 reads
      `adjusted ? res.adjustedTotalSchedulableHours ?? budget.totalSchedulableHours
      : budget.totalSchedulableHours`, whereas
      src/app/(app)/labor/weekly-plan-client.tsx:206 renders
      `adjustedTotalSchedulableHours` unconditionally. On a week with NO weather
      adjustment the two values still differ — they are computed by different
      paths (src/lib/labor-budget.ts:100 sums salaried + hourly; the adjusted
      figure at src/lib/labor-plan.ts:285 re-sums salaried + the per-day
      hourlyHours AFTER the floor-first 0.5-step flooring), so the adjusted
      total carries flooring residue even with no adjustment applied.
      The card's behavior is DELIBERATE per its own inline comment, so this is
      not a straightforward bug — but two screens showing different
      schedulable-hour totals for the same store and the same week is a
      correctness problem for the operator regardless of which number is
      defensible. Decide which figure is canonical and make both surfaces say
      it, or label them differently. Numbered BUG-4 because BUG-1…BUG-3 exist;
      docs/prompts/verification-smoke-pass.md already referenced a "BUG-4" in
      its holds list before any BUG-4 row existed — this fills that reference.

  - id: BUG-5
    title: "Duplicating a template creates it ACTIVE — the client's isActive:false is dropped"
    status: planned
    notes: >
      Logged 2026-07-30, found while auditing DEBT-1a (21dd712); NOT fixed, that
      session was audit-only and its writable set was two docs files.
      src/app/(app)/templates/templates-client.tsx:137 POSTs `isActive: false`
      when duplicating a template, but the create block in
      src/app/api/templates/route.ts:60 never reads `isActive` — it is absent
      from the `data` object — so Prisma applies the schema default
      `isActive Boolean @default(true)` (prisma/schema.prisma:283). The copy is
      live the moment it is created.
      IT THEN PROPAGATES: the bulk generator at
      src/app/api/checklists/route.ts:138 selects every `isActive: true` template
      for the org, so the duplicate starts generating checklists for every
      applicable store on the next generation run — the original's checklists
      silently double. The admin who clicked Duplicate to draft a variant gets a
      second live checklist per store instead.
      This is a DROPPED FIELD, not a design choice: the client sends the flag
      explicitly, and the intent (a copy you review before going live) matches
      the import route, which does hardcode `isActive: false`
      (src/app/api/templates/import/route.ts:134). Fix is one line in the POST
      route's `data`. Before changing it, check each branch for duplicates
      already created live — the current behaviour has shipped since the field
      existed, so someone may be relying on a copy that went live immediately.

  - id: BUG-6
    title: "Dashboard store switch serves the previous request's sales — the lazy refresh lands after the response"
    status: staging
    commits: [37361ba, b77082e]
    notes: >
      VERIFIED ON STAGING BY GARY 2026-08-13 — THE DEFECT IS CLOSED. Prepended;
      everything below stays exactly as written. Org
      cf888f2d-f234-48c7-8097-fd5b44b5b3dd on Clerk instance verified-snapper-7,
      staging preview, deployed SHA 92de25f read off the dashboard's own build
      stamp ON THE SAME SCREEN as the observations — which is the § Staging
      Verification precondition satisfied by construction rather than by a
      separate command whose answer could drift.
      WHAT WAS OBSERVED, all four:
        1. Cold load of Las Brisas — chart flatlined at zero, then populated at
           ~1s WITHOUT a manual refresh. The poll firing as designed, and the
           clearest possible demonstration of the defect being gone: the zero
           state IS the pre-refresh payload this row is about.
        2. Store switch across 4-5 stores — current sales on each, no refresh.
        3. Return trip to a previously visited store — fresh. THIS IS THE LATCH
           CASE b77082e FIXED, and it is the one that could not have passed
           before: the ever-started guard would have suppressed the poll on a
           revisit whose baseline had not moved.
        4. BUG-1 not regressed — cold dashboard load paints promptly, no eternal
           skeleton. The negative check, and the one that proves the fix did not
           buy freshness back by reintroducing the hang.
      TIMINGS SUPERSEDED, NOT CORRECTED — the 969-1179ms figure further down is
      preserved and was never wrong, it was just measured in the wrong place (a
      dev machine against the dev branch, and the row said so). THE AUTHORITATIVE
      FIGURE IS NOW ~800ms typical for /api/dashboard/summary on Preview across
      the session, spot values 796/1032/1162/1184ms, measured from a real lambda.
      Against a 3x2s poll window the first attempt catches it with room to spare,
      which is what the duration log added in 37361ba was for.
      STATUS IS `staging`, NOT `verified`, AND THAT IS DELIBERATE — read this
      before changing it. Gary's instruction was "set status verified"; the
      observations fully support the closure and none of this qualifies them.
      But `verified` is grouped into the "In production" board column
      (src/lib/roadmap.ts:229, statuses ["shipped","verified"]), and neither
      37361ba nor b77082e is on origin/main — measured 2026-08-13 with
      `git merge-base --is-ancestor`, origin/main at ce036f9, which is where this
      session started. So `verified` would render this row as in production while
      its code has never left staging. `staging` is simply true, the way CHK-4's
      row says of itself, and the verification lives in the prose above where no
      column mapping can overstate it. Flip to `shipped` when it reaches main and
      to `verified` after a production observation.
      CARRIED FORWARD, NOT PART OF THIS CLOSURE: see BUG-8 (one store returns
      ~6704ms where every other store returns ~800ms). Observed in this same
      session and deliberately not diagnosed in it.
      CLOSURE RECORDED IN 728638d — the commit carrying the verification prose
      above, per the two-commit pattern. The code it verifies is 37361ba and
      b77082e, both already in `commits:`; this third SHA is the bookkeeping, and
      it is a separate commit rather than an amend because amending would
      invalidate the SHA this sentence just recorded.
      ── PRE-VERIFICATION RECORD BELOW, UNEDITED ──
      Found 2026-08-13 while proving F-4's second symptom; FIXED IN CODE, NOT
      YET VERIFIED ON STAGING, which is why this is in_progress and not
      `staging`. Audit: docs/prompts/F-4_FINAL_AUDIT.md.
      COST OF DOING NOTHING: LIVE. Every operator on the single-store dashboard
      reads sales one request out of date, and has since 2026-07-23.
      THE DEFECT. src/app/api/dashboard/summary/route.ts:83-95 (and its twin
      src/app/api/dashboard/sales/route.ts:192-202) registers the stale-cache
      Square refresh in after(), then composes the response at :103-127 from the
      rows it read BEFORE that callback runs. after() fires once the response
      has flushed, so the payload can never contain its own refresh. The route
      is one REQUEST behind, not one sync behind. A manual page refresh is a
      second request, which is why refreshing "fixed" it and nothing else did.
      WHY THE STORE PICKER IS WHERE IT SHOWS: a store not viewed in the last 15
      minutes is always past STALE_MS, so switching to it always takes the
      deferred branch. The same staleness is on first page load; it is just
      invisible there because there is no previous number to contradict.
      RULED OUT BY SOURCE READING, NOT BY ELIMINATION — both alternatives were
      checked and are false. The client DOES refetch: saveStoreId dispatches
      STORE_EVENT (dashboard-client.tsx:145-148) into a useSyncExternalStore
      (:137-143), storeId changes, the useCallback deps invalidate and the
      effect refires with the new URL; results are keyed by storeId (:177,:210)
      so a previous store's payload cannot render. And NOTHING CACHES: grep over
      src/app/api/dashboard and src/app/(app)/dashboard returns zero hits for
      dynamic/revalidate/fetchCache/unstable_cache/revalidatePath, and every
      route is dynamic via req.url searchParams plus getCurrentUser().
      IT IS A REGRESSION OF BUG-1's OWN FIX. git log -L 79,95 on the summary
      route returns exactly two commits: 1b8160f (2026-07-06, D-1) where the
      stale branch was an INLINE await, and 2081401 (2026-07-23, BUG-1 step 4)
      which moved it behind after(). So 07-10 to 07-23 the dashboard was fresh
      and slow; from 07-23 it has been fast and one request stale.
      NOT THE SAME DEFECT AS F-4, AND THE DISTINCTION IS LOAD-BEARING. F-4 is a
      CONFIGURATION gap from 2026-07-10 (an env var and two Square
      subscriptions never created). This is a CODE regression from 2026-07-23.
      They compound: with webhooks live the cache is rarely stale, so this
      branch would almost never fire — meaning FIXING F-4 WOULD LARGELY MASK
      THIS WITHOUT REPAIRING IT, and it would resurface on any store not
      transacting, any webhook outage, and the first load after any quiet
      15 minutes. Hence Gary's ruling 2026-08-13: VERIFY THIS ON STAGING BEFORE
      REGISTERING ANY SQUARE SUBSCRIPTION, so the result is attributable.
      THE FIX (37361ba). Both routes report `salesRefreshing` and the
      `salesSyncedAt` their numbers were read at; both cards re-fetch until
      syncedAt advances — at most 3 attempts 2s apart, one sequence per
      (request key, baseline syncedAt), returning the moment it moves. Bounds
      live once in card-fetch.ts. after() is deliberately NOT reverted to an
      inline await: that is BUG-1 verbatim, and a card hung against
      card-fetch.ts's 12s timeout is worse than a stale one.
      THE WINDOW IS SIZED AGAINST A MEASUREMENT. A full store-day sync took
      969-1179ms across four stores — branch br-broad-wave-a6vpjdw0 (dev), org
      cf888f2d-f234-48c7-8097-fd5b44b5b3dd, 76-144 orders each, 2026-08-13 —
      so 6s is generous and the FIRST poll should normally catch it. Measured
      from a dev machine, not from a lambda; the routes now log the duration on
      every deferred refresh so each environment reports its own figure rather
      than inheriting this one.
      THE ROLLUP ASYMMETRY IS PART OF THIS ROW (folded in on Gary's ruling
      2026-08-13 rather than opened as BUG-7). /api/dashboard/rollup:66-68 never
      received BUG-1 step 4 and still syncs INLINE, so today the all-locations
      view is fresh-and-slow while the single-store view is fast-and-stale, and
      the two surfaces of the same dashboard can disagree about the same store's
      "today" seconds apart. NOT TOUCHED BY 37361ba — deciding which posture is
      canonical is this row's remaining work, and it is a real decision rather
      than an oversight: the rollup loops every store serially, so giving it the
      same treatment means N syncs, not one.
      NO AUTOMATED COVERAGE OF THE FIXED PATH EXISTS, said plainly rather than
      implied. scripts/verify-f4-rollup-webhook.ts cannot reach it twice over —
      the dashboard routes need a Clerk session, and store switching is
      localStorage plus a window event, so a Node fixture has neither. The
      fixture staying green (15/15) is a no-regression signal, not proof.
      Closure is Gary switching stores on staging and seeing current numbers
      without a manual refresh.
      APPENDED 2026-08-13 — THE FIRST FIX SHIPPED THE BUG'S OWN SHAPE, caught by
      Gary reading the guard before pushing, corrected in b77082e. pollGuard was
      a Set nothing deleted from, so a (store, baseline) key latched for the
      component's lifetime: inert in the healthy case, because syncedAt advances
      and a revisit presents a new baseline, and PERMANENT in the failure case,
      because a failed background sync never moves the baseline. Since each poll
      re-enters the route and registers a FRESH after() sync, what latched was
      the RETRY OF A FAILED SYNC, not a redundant read — and the manual Retry
      button was blocked by the same key. Now cleared in a finally, so the Set
      means "in flight" rather than "ever started": StrictMode's double effect
      invocation is still deduped, a later revisit polls again. The bound was
      never the guard's job and is unchanged — sequences are spawned only by the
      effect and by Retry, a poll writes state directly rather than re-entering
      loadSummary, and POLL_ATTEMPTS bounds each one.
      NOTE THE SHAPE THAT REPEATED. BUG-6 is a deferral whose result nobody
      came back for; the first fix added a guard whose expiry nobody came back
      for. Both are cheap-at-write, invisible-until-the-unhappy-path, and both
      were found by reading rather than by anything failing.
      CHECKED AND CORRECT, so it is written down rather than re-derived next
      time: a poll CANNOT paint one store's data under another's label. Guarded
      independently at two levels — the in-sequence checks before the fetch and
      after it resolves (dashboard-client.tsx:211,213 on activeStoreRef;
      sales-performance-card.tsx:301,303 on requestSeq, which is tighter because
      requestSeq is bumped synchronously in the effect body while activeStoreRef
      lags one commit) — and the render-time keying BUG-1 introduced for exactly
      this (`current` at dashboard-client.tsx:255, `loading` at
      sales-performance-card.tsx:321). Worst case if both in-sequence checks
      went stale is a flicker back to the skeleton, never wrong-store numbers.
      ONE LATENT EDGE, now commented at the declaration rather than left
      implicit: `data` in sales-performance-card is UNKEYED, and its safety
      rests entirely on every read sitting inside the `loading ?` ternary.
      Rendering it outside that ternary — moving the unconfirmed teaser into the
      header, say — reintroduces the cross-store paint.
      APPENDED 2026-08-13 — THIS FIX SURFACED BUG-7, AND THE ROW IS STILL NOT
      VERIFIED. Observed by Gary on staging, preview env, branch 92de25f, org
      cf888f2d-f234-48c7-8097-fd5b44b5b3dd, 2026-08-13 18:48:50 UTC:
        prisma.salesPeriodCache.createMany() — P2002, unique constraint failed
        on ("storeId", date), store=cmrd1a4hz000004jjl91v23nm
      followed 3.5s later by a clean 796ms refresh on the same store. A later
      poll attempt recovered it, so the symptom is invisible to the operator.
      THE POLL IS NOT THE DEFECT AND IS NOT TO BE TOUCHED (Gary, 2026-08-13).
      Lambda timings 796/1032/1162/1184ms confirm the 3x2s window empirically,
      from a real deployment rather than the dev-machine figure this row cites
      above. The defect is in the WRITE — see BUG-7 and
      docs/prompts/BUG-7_AUDIT.md.
      WHAT THIS FIX ACTUALLY DID TO IT, stated precisely because the first
      framing overstated it: the race was ALREADY REACHABLE and this fix raised
      its frequency. A single dashboard load has always issued two concurrent
      syncs for the same store-day — dashboard-client.tsx:225 fetches
      /api/dashboard/summary while SalesPerformanceCard:291 fetches
      /api/dashboard/sales, whose default preset is `today`, so both evaluate
      the same STALE_MS test and both register an after() sync milliseconds
      apart. That has held since 2081401 (2026-07-23). What the poll changed is
      the MULTIPLIER: each attempt re-enters the route and, because an in-flight
      sync has not yet advanced syncedAt, schedules another. One store switch
      can now schedule up to 8 syncs for one store-day where it scheduled 2.
      BUG-6 REMAINS in_progress AND UNVERIFIED. The store-switch behaviour has
      not been confirmed on staging, and this row does not close on a P2002.
      ORDERING NOW STACKS THREE DEEP: verify BUG-6 on staging -> fix BUG-7 ->
      register the Square subscriptions -> verify F-4. BUG-7 before the
      subscriptions is not optional; see that row's blast-radius finding.
      CROSS-REFS: F-4 (the dead webhook that hid this for three weeks, and whose
      fix must NOT be applied first), BUG-1 (whose step 4 introduced it),
      BUG-7 (the write race this fix's extra requests made frequent).

  - id: BUG-7
    title: "Concurrent syncs raced on the sales caches — P2002, and the older fetch could win"
    status: verified
    commits: [00881dc]
    notes: >
      VERIFIED IN PRODUCTION 2026-08-16, NEVER VERIFIED ON STAGING. Status
      in_progress -> verified. Prepended per preserve-and-mark; nothing below is
      edited. In particular "FIXED IN CODE 2026-08-13, NOT YET VERIFIED ON
      STAGING" still stands below and was true when written — it was OVERTAKEN,
      not corrected. Session:
      docs/prompts/BUG-7_CLOSURE_AND_DEPLOY_LOG_RECONCILIATION.md and its
      Addendum A. WORK COMMIT 165bcb8 — one docs-only commit carrying all six of
      that session's tasks: this block, F-4's resolved blocker, the five
      retroactive docs/DEPLOY_LOG.md entries and their standing-note discharge,
      DEBT-38's second recurrence, DEBT-69's measured rate, DEBT-72's
      placeholder, and the docs/DECISIONS.md mechanism entry. `commits:` on this
      row still names 00881dc alone, deliberately — that is the FIX; 165bcb8 is
      the RECORD of its verification and does not belong in the same field.
      THE ROW CLOSES ON THE CRITERION IT SET FOR ITSELF, which is worth saying
      before the evidence rather than after. The closure condition written on
      this row 2026-08-13 and still standing below reads: deploy 00881dc, then
      EITHER observe the `[sales-sync] ... discarded N superseded by a newer
      fetch` line OR observe sustained concurrent load with no P2002. The first
      disjunct is satisfied, ~55 times over. That is not an after-the-fact
      justification assembled to fit what was found; it is the test this row
      specified three days before the evidence existed.
      WHY A PRODUCTION-ONLY VERIFICATION IS SUFFICIENT HERE, STATED PLAINLY
      BECAUSE IT IS UNUSUAL AND SHOULD NOT BECOME A PRECEDENT. Staging never ran
      this code under contention and still has not. What closed the row is that
      production exercised the guard against REAL concurrency — nine distinct
      stores, live webhook load, collisions 70 ms and 270 ms apart — which the
      staging fixture can only simulate. The fixture DID simulate it (24/24, see
      below) and that is what made shipping defensible; simulation is not the
      same evidence as a race that actually happened on real money. The order
      this arrived in was the inverse of what this row recommended, which is
      recorded below as fact rather than objection. It came out right. It was not
      made right by coming out right.
      EVIDENCE 1 — THE GUARD FIRES UNDER REAL CONTENTION. This is the direct
      proof and the only one; everything else is corroboration. Vercel PRODUCTION
      function logs, 2026-08-16, ~11:53-14:11 PT (18:53-21:11 UTC), filtered on
      `discarded`: approximately 55 occurrences of `[sales-sync] store=<id> wrote
      0/1 day(s); discarded 1 superseded by a newer fetch: 2026-08-16`, all on
      POST /api/webhooks/square, across at least nine distinct store ids
      (cmqvygqz2..., cmqvygq1m..., cmqvygque..., cmqvygqx9..., cmqvygr1l...,
      cmqvygqxb...000004l45ajpg1la, cmqvygqxb...000004l4v0im0byu, cmqvygpze...,
      cmqvygr36...). Two collisions are tight enough to name, because they are
      the exact shape that used to throw P2002: 12:52:27.19 and 12:52:27.26 PT,
      same store, 70 ms apart; and 13:22:58.94 and 13:22:59.21 PT, same store,
      270 ms apart. That line can only be emitted by the post-fix code, and it
      means a race was caught and RESOLVED CORRECTLY rather than avoided by luck.
      HOW TO READ "0/1", AND THIS IS THE SINGLE MOST IMPORTANT SENTENCE ON THIS
      ROW FOR ANYONE WHO FINDS THAT LOG FILTER LATER. The log statement is
      wrapped in `if (discarded.length > 0)`, so A FULLY SUCCESSFUL WRITE EMITS
      NO LOG LINE AT ALL. Filtering on `discarded` can therefore only ever return
      losers. The winners are invisible BY CONSTRUCTION — not missing, not
      failing, just not logged — and their existence is proven by Evidence 3
      below, where every store's syncedAt is minutes fresh. A reader who finds
      this filter and reads a 100% discard rate as a defect will be wrong, and
      this paragraph exists to stop them.
      WHY P2002 IS NOT THE PROOF, and why looking for it was looking for
      something that could only return zero. ON CONFLICT makes P2002
      STRUCTURALLY IMPOSSIBLE in the post-fix code. An empty P2002 search is
      therefore confirmation that the OLD PATH IS GONE — which is worth knowing —
      and is not, and can never be, evidence that the guard works. Only the
      discard line proves the guard.
      EVIDENCE 2 — THE WEBHOOK IS DELIVERING. Square Developer Console,
      PRODUCTION application sq0idp-UdjqLfkxl0hlbw7b30IiLA ("Froot"): 34,914
      delivery results, ALL 200, to https://www.usefroot.com/api/webhooks/square,
      most recent 2026-08-16 13:18 PT. Square retains webhook logs 28 days, so
      that window is not the whole history — it is the whole RETAINED history.
      A 200 DOES NOT PROVE THE SYNC RAN: the handler ACKs before processing
      (route.ts:89-94), so a 200 proves signature verification and acceptance
      only. This row already said that about F-4's test event and it is still
      true at 34,914 deliveries. What follows the ACK is proven separately, by
      Evidence 1 and Evidence 3.
      EVIDENCE 3 — EVERY PRODUCTION STORE IS SYNCING LIVE. Neon console,
      PRODUCTION branch br-sparkling-block-a620qvg4 (endpoint ep-green-smoke),
      2026-08-16 ~21:10 UTC (2:10 PM PT). Nine linked stores — Spanish Springs,
      South Reno, Meadowood Mall, Carson, Las Brisas, UNR, Sparks, University
      Village, Southgate — all with last_sync inside the previous ten minutes and
      latest_day = 2026-08-16. Keva Kiosk (17:12 UTC) and Cafe De Keva Cart
      (11:01 UTC) lag, consistent with lower volume or closure rather than
      failure. Rohan's Restaurant is unlinked (squareLocationId null). This is
      what makes the invisible winners of Evidence 1 real: a store whose writes
      were all being discarded could not have a syncedAt ten minutes old.
      EVIDENCE 4 — THE ID-FORMAT BOUNDARY, AND WHAT IT BRACKETS. Same production
      branch br-sparkling-block-a620qvg4, 2026-08-16. SalesPeriodCache rows carry
      two id formats: Prisma generates cuids client-side, and the post-BUG-7 raw
      INSERT supplies its own randomUUID(). ON CONFLICT DO UPDATE does not
      rewrite id, so a row BORN under the old code keeps its cuid forever even
      after the new code rewrites its numbers. THE BOUNDARY THEREFORE FALLS ON
      BUSINESS DATE, NOT ON syncedAt — syncedAt is overwritten on every write and
      was never a creation time. By business date: 2026-08-16 uuid, 11 rows;
      2026-08-15 uuid, 11 rows; 2026-08-14 cuid, 11 rows; 2026-08-13 cuid, 11
      rows; 2026-08-12 cuid, 11 rows. BUSINESS DATE 2026-08-15 IS THE FIRST UUID
      DAY. Eleven rows on every date with no gaps is a second reading of
      Evidence 3: every linked store is syncing, none silently missing. This is
      the fingerprint the fix predicted in advance (the comment at the `values`
      map in src/lib/sales-sync.ts, and the RAW SQL paragraph below) showing up
      exactly where it was predicted to.
      THE ROLLOUT WINDOW IS A BRACKET AND IS LABELLED AS ONE. Two independent
      bounds, neither of them a deploy instant: (a) the id-format boundary above
      puts the rollout between the creation of business date 2026-08-14's rows
      and 2026-08-15's; (b) `git reflog show main --date=iso` shows main moving
      to 06dc830 — the promotion carrying 00881dc — at 2026-08-14 08:05:16 PT =
      2026-08-14 15:05:16 UTC, so THE FIX CANNOT HAVE BEEN SERVING PRODUCTION
      TRAFFIC BEFORE THAT INSTANT. Bound (b) is a LOWER BOUND ONLY: the reflog
      records a local merge on Gary's machine, not the push and not the Vercel
      build, both of which follow it. The two agree — Aug 14's rows were created
      before 15:05:16 UTC and kept their cuids — and neither is the exact moment.
      The Vercel production deployment timestamp for 00881dc would replace the
      bracket with an instant; it was not supplied to this session, so the
      bracket stands as a bracket. Stating it tighter than that would be
      inventing precision the evidence does not carry.
      NO BACKFILL, NO CLEANUP — RULED. The mixed cuid/uuid ids are not a defect
      to repair. `id` on these three tables is a bare primary key: no foreign key
      references it, no response exposes it, and nothing reads it. Rewriting a
      column nothing reads would be churn against the additive-only rule, and it
      would destroy the only artifact that dates the rollout (Evidence 4). Leave
      them.
      THE GAP IN THE RECORD, STATED PLAINLY, BECAUSE IT IS THE REASON THIS ROW
      NEEDED A BRACKET AT ALL. 801dceb records 00881dc's commit SHA, because the
      two-commit pattern captures commit SHAs and it worked. NOTHING CAPTURED
      WHEN THAT COMMIT REACHED PRODUCTION, because the promotion that carried it
      was a fast-forward with no docs/DEPLOY_LOG.md entry — one of five. So the
      row can say exactly when the fix was WRITTEN and had to reconstruct, from
      row ids and a reflog, when it was RUN. Same root cause as DEBT-38; the five
      retroactive DEPLOY_LOG entries written in this session close the hole for
      these five promotions and not for the next one.
      CROSS-REFS ADDED BY THIS SESSION: F-4's first blocker closes on the same
      Evidence 2 and 3 (its own remaining requirement was one real transaction
      moving "Today so far", and nine stores did it); DEBT-69 gains the measured
      discard rate this evidence produced; DEBT-38 gains this promotion as one of
      its five; docs/DECISIONS.md 2026-08-16 records the mechanism that let
      00881dc ship past this row's own gate.
      ── EVERYTHING BELOW IS THE PRE-VERIFICATION RECORD, UNEDITED ──
      Observed by Gary on staging, preview env, branch 92de25f, org
      cf888f2d-f234-48c7-8097-fd5b44b5b3dd, 2026-08-13 18:48:50 UTC:
      prisma.salesPeriodCache.createMany() — P2002 on ("storeId", date),
      store=cmrd1a4hz000004jjl91v23nm, followed 3.5s later by a clean 796ms
      refresh. FIXED IN CODE 2026-08-13, NOT YET VERIFIED ON STAGING.
      Audit: docs/prompts/BUG-7_AUDIT.md. Ruled Option 2 by Gary the same day.
      COST OF DOING NOTHING: LIVE, but low today and rising sharply — see the
      webhook paragraph. Today the constraint and the transaction hold, so a
      collision costs discarded work, never corruption.
      THE DEFECT. sales-sync.ts wrote each day as deleteMany-then-createMany in
      one transaction — check-then-act, the delete standing in for the check,
      with nothing holding it true until the insert. Under READ COMMITTED (the
      Postgres default, and unset anywhere in this repo) two writers for one
      store-day collided on @@unique([storeId, date]).
      THE SECOND DEFECT, INDEPENDENT OF THE RACE, and the reason Gary ruled
      Option 2 over the cheaper Option 1: syncedAt was @default(now()), stamped
      at INSERT. The winner was therefore whichever COMMITTED last, which is not
      whichever READ SQUARE last. An older fetch could overwrite a newer one and
      stamp the result as FRESHER — the day's sales tick DOWNWARD, and the
      fresh-looking stamp then suppresses the 15-minute refresh and the webhook
      burst absorber for a further 15 minutes. Silent and self-sustaining, on
      the screen F-4 exists to make trustworthy. Gary declined to accept that in
      writing, which is the whole reason this row is not a one-line upsert.
      THE FIX (00881dc). syncedAt now carries fetchStartedAt, taken before the
      first Square page — start-time UNDERSTATES freshness, which is the safe
      direction (an extra refresh, never a skipped one) and is the only instant
      true of the whole window rather than of its last page. The write is one
      INSERT ... ON CONFLICT DO UPDATE ... WHERE stored.syncedAt <
      EXCLUDED.syncedAt, doing three jobs at once: it cannot collide, because ON
      CONFLICT takes the row lock and the second writer re-evaluates against the
      COMMITTED value; the older fetch loses even when it commits later; and a
      row whose WHERE is false is skipped and not RETURNED, which is exactly the
      discard signal. Winning the period row also holds the store-day for the
      rest of the transaction, so the hourly/line rewrite needs no guard of its
      own — hence the period upsert MUST come first.
      THE INVARIANT THAT MAKES DISCARDING SAFE, worth keeping because it is what
      justifies the whole design: every sync re-reads the WHOLE day from Square,
      so the winner's read covers everything the loser saw. Discarding a
      superseded write can never lose information.
      SYNCEDAT READER AUDIT — all six functional readers re-checked against the
      changed meaning, and the notable result is that NONE broke and ONE was
      already wrong and is now right:
        1. dashboard/summary/route.ts:91 — 15-min staleness test. HOLDS, and is
           more correct: the test asks how old the DATA is, and fetch time is
           when the data was read. Insert time overstated freshness by the sync
           duration. Rows now read ~1s staler against a 900s threshold.
        2. dashboard/sales/route.ts:201 — identical test. HOLDS, same reasoning.
        3. dashboard/rollup/route.ts:66 — identical test. HOLDS, unchanged and
           deliberately not touched (out of scope).
        4. webhooks/square/route.ts:119 — the burst absorber,
           `cached.syncedAt >= eventAt`, whose comment claims "that sync saw the
           post-event order state". THIS WAS FALSE BEFORE AND IS TRUE NOW. With
           insert-time stamps, a sync that read Square at T0 and inserted at
           T0+1s stamped T0+1s, so an event emitted at T0+0.5s was skipped even
           though the sync's read never saw it — a genuinely dropped event, for
           the whole duration of every sync. Fetch-time stamping closes that.
           Effect: slightly FEWER skips, i.e. more syncs, the safe direction.
        5-6. summary:131 and sales:238 — `salesSyncedAt`, BUG-6's poll baseline,
           stopped on `!== baseline`. HOLDS and is STRENGTHENED: the guard makes
           syncedAt monotonic by construction, where before monotonicity was
           accidental and could be violated by this very race. New behaviour to
           know: a DISCARDED write does not move syncedAt, so the poll spends
           its remaining attempts and stops — harmless, because the numbers on
           screen already belong to the newer winner. Bounded at 3 either way.
        getSyncedThrough (sales-sync.ts) reads `date`, not syncedAt. Unaffected.
      FIXTURE: 24/24, nine new. Covers the ruled case — newer fetch commits
      FIRST, older fetch commits LAST, newer totals survive — plus fetch-instant
      stamping, a discarded write leaving the winner's hourly and line rows
      intact, and two genuinely concurrent writers not throwing and leaving one
      row with the newer numbers. CONFIRMED IT CAN GO RED, and how it fails is
      the useful part: removing ONLY the WHERE clause leaves the P2002 fixed
      (one row, no throw — that is Option 1 exactly) while netSales goes
      500 -> 1 and 222 -> 111. The fixture reproduces Option 1's going-backwards
      hazard as a failing assertion rather than as an argument.
      RAW SQL WAS NOT A PREFERENCE. No typed Prisma API expresses a conditional
      ON CONFLICT, and the obvious alternative — updateMany, then create on zero
      rows, catching P2002 — cannot work inside an interactive transaction: once
      a statement errors, Postgres puts the transaction in a failed state and
      every later statement fails with 25P02. Measured on br-broad-wave-a6vpjdw0:
      `id` has NO database default on any of the three cache tables (Prisma
      generates cuid client-side) and syncedAt DOES default to CURRENT_TIMESTAMP,
      so the raw insert supplies both explicitly. New rows therefore carry uuid
      ids beside older cuids; the column is a bare primary key that no foreign
      key references and no response exposes, so the formats never meet.
      NO SCHEMA CHANGE — every constraint relied on already existed. That is
      worth stating because Option 2 sounds like it needs one.
      WHY THIS BLOCKS THE SQUARE SUBSCRIPTIONS. Registering a webhook today adds
      a writer at transaction rate — 76-144 paid orders/store/day measured, x3-5
      events per order across the four subscribed types — against a ~1s sync.
      The burst absorber collapses most of one order's cluster but fails exactly
      on events arriving within the ~1s a sync takes, because the in-flight sync
      has not committed; it is the same check-then-act one layer up. And
      processEvent swallows the failure after the route already returned 200, so
      Square never retries: a raced webhook sync is simply lost. The recovery
      paths that hide this today are the poll and the cron — precisely what the
      webhook is meant to make unnecessary.
      ORDERING: verify BUG-6 on staging -> verify BUG-7 on staging -> register
      the Square subscriptions -> verify F-4.
      ZERO P2002 OBSERVED 2026-08-13 during BUG-6's staging verification — org
      cf888f2d-f234-48c7-8097-fd5b44b5b3dd, Clerk instance verified-snapper-7,
      staging preview, deployed SHA 92de25f. RECORDED SEPARATELY FROM BUG-6'S
      CLOSURE AT GARY'S INSTRUCTION, AND IT DOES NOT CLOSE THIS ROW.
      Read what it is and is not. 92de25f PREDATES the fix — the guarded upsert
      is 00881dc, which is not deployed anywhere — so this session exercised the
      OLD delete-then-insert write. Zero P2002 across it is therefore evidence
      about the RACE's FREQUENCY on a quiet single-operator session, not evidence
      that the fix works. The original sighting was one collision in one session,
      so a session with none is exactly what a rare race looks like either way.
      IT ALSO IS NOT NOTHING. It is the closest thing to a baseline this row will
      get: the same screens, the same store set, the same poll behaviour, one
      operator, no collision. A post-fix session showing the same absence proves
      little on its own; what would prove the fix is the `[sales-sync] ...
      discarded N superseded by a newer fetch` line appearing in Preview logs,
      because that line can only be emitted by the new code and means a race was
      caught and resolved correctly rather than avoided by luck.
      SO THE CLOSURE CONDITION IS UNCHANGED: deploy 00881dc, then either observe
      that discard line or observe sustained concurrent load with no P2002. This
      row stays in_progress.
      PRODUCTION WEBHOOK WENT LIVE 2026-08-13 — THIS ROW'S BLAST-RADIUS
      PARAGRAPH IS NO LONGER HYPOTHETICAL. Gary registered the subscription on
      the production Square app (F-4 carries the configuration record). So the
      next store-day write race is a REAL one, in production, on real money.
      THE ORDER THIS SHIPPED IN IS THE INVERSE OF WHAT THIS ROW RECOMMENDED, and
      that is recorded as fact rather than as objection — Gary read the audit and
      made the call. The consequence to hold onto is precise: 00881dc IS DEPLOYED
      NOWHERE. Not production, not staging. Production is therefore running the
      OLD delete-then-insert write with a webhook writer attached to it, which is
      exactly the combination the audit described as converting a rare,
      self-healing, invisible race into a frequent one — while removing the poll
      and the 15-minute lazy sync that were hiding it, because a fresh cache is
      the whole point of the webhook.
      WHAT THAT ACTUALLY COSTS, unchanged and still bounded: the unique
      constraints and the single transaction still hold, so a collision discards
      one sync's work and can never corrupt a day, half-write it, or duplicate
      rows. The two live risks are the going-backwards hazard (an older fetch
      overwriting a newer one and stamping itself fresher, then suppressing the
      refresh and the burst absorber for 15 minutes) and silent loss, since
      processEvent logs and swallows after the route has already returned 200, so
      Square never retries.
      THE CHECK RIDES ALONG WITH F-4'S TRANSACTION TEST (Gary): when the real
      transaction is rung tomorrow, read the production runtime logs for P2002 on
      ("storeId", date) alongside watching "Today so far" move. One store, one
      operator, one transaction is the LIGHTEST possible load, so a clean read
      there is weak evidence — the race needs two writers within ~1s. Absence of
      P2002 in that window means very little; PRESENCE of it means the hazard is
      live in production and this row becomes urgent.
      CROSS-REFS: BUG-6 (whose extra requests made this frequent), F-4 (blocked
      behind it, and now the source of production's webhook writer), DEBT-69
      (coalescing, filed not built).

  - id: BUG-8
    title: "One store returns ~6704ms from /api/dashboard/summary where every other store returns ~800ms"
    status: planned
    notes: >
      OBSERVED 2026-08-13 BY GARY, NOT INVESTIGATED — this row is the
      observation and deliberately nothing more. Carried forward out of BUG-6's
      staging verification at Gary's instruction; no diagnosis was done in that
      session and none should be read into this row.
      THE OBSERVATION, verbatim in substance. Org
      cf888f2d-f234-48c7-8097-fd5b44b5b3dd, Clerk instance verified-snapper-7,
      staging preview, deployed SHA 92de25f. One store — THE ONE WITH NO LABOR
      BUDGET SET — reproducibly returns ~6704ms from /api/dashboard/summary,
      where every other store in the same session returns ~800ms typical (spot
      values 796/1032/1162/1184ms). REPRODUCIBLE, NOT A COLD START; that was
      checked at the time and is the reason this is a row rather than a shrug.
      WHY IT MATTERS, which is Gary's framing and is the whole point of filing
      it: A STORE WITH NO LABOR BUDGET IS THE STATE EVERY NEWLY ADDED STORE
      ARRIVES IN. So if that path costs 6s, every Keva onboarding hits it — the
      slow case is the DEFAULT case, not an edge one, and the population that
      hits it is exactly the population with nobody watching yet.
      IT CAN EXHAUST BUG-6'S POLL WINDOW. That window is 3 attempts at 2s
      (POLL_ATTEMPTS/POLL_INTERVAL_MS, src/app/(app)/dashboard/card-fetch.ts),
      i.e. 6s of cover sized against ~800ms responses. A 6704ms response is
      outside it. So on a brand-new store the store-switch staleness BUG-6 just
      fixed could reappear — not because that fix is wrong, but because its
      window was sized against the healthy case. Do not read that as a BUG-6
      regression; the two interact, and BUG-6's staging verification stands.
      NOT DIAGNOSED. No file, no line, no cause is asserted here on purpose. The
      labor-budget correlation is Gary's observation of WHICH store, not a claim
      about which code path — writing a suspected cause into the row before
      anyone has read the code is how a guess becomes a fact by being quoted.
      COST OF DOING NOTHING: LIVE for any newly added store, LATENT for existing
      ones, on the named trigger of adding a store.
      CROSS-REFS: BUG-6 (the poll window this can exceed, and the session this
      was observed in).

  - id: BUG-9
    title: "Coverage crams the whole day's budget into the morning — today shaped by its own partial-day cache"
    status: shipped
    shipped: 2026-08-20
    commits: [7f1fb3b]
    notes: >
      RULED BY GARY 2026-08-19 BEFORE ANY CODE WAS WRITTEN, and the ruling is in
      docs/DECISIONS.md § "Same-day coverage shape", not in this row: the
      current day's demand shape uses the future-day template, past days use
      their own complete cache, the elapsed-day flip back to actuals is
      INTENDED, hybrid intraday shapes are rejected, and "today" is the
      STORE-LOCAL day. This row records what was built against it.
      THE DEFECT. getDemandShape (src/lib/labor-plan.ts) classified with
      `if (date <= today)`, so today fell in with the past and took its own
      SalesHourlyCache. That cache holds only ELAPSED hours, and
      computeDailyCoverage is pure — it spreads the day's ENTIRE hourly budget
      across whatever demand array it gets. So the whole day's budget landed on
      the hours that had already happened, post-now hours sat at floor-1, and
      the curve mutated hour by hour as sales came in. Same-day only: past days
      and future days both have complete shapes and always rendered correctly.
      THE FIX. `date < today` -> that date's own cache; `date >= today` -> the
      4-week same-weekday template future days already used. The boundary is
      store-local because both callers derive `today` as
      localDateStr(new Date(), store.timezone) — /api/labor/coverage:26 and
      /api/labor/weekly-plan:46 — and the comparison is a yyyy-mm-dd string
      compare against that value. NOTHING about how hours are budgeted or split
      changed; this is a shape-source change only.
      THE TEMPLATE WINDOW MOVED TOO, AND THAT IS A DEVIATION FROM THE SESSION
      PROMPT, which asked for future-day behavior byte-identical. The
      same-weekday walk-back anchored at `today`, so when the target weekday
      was today's own the window was [today, -7, -14, -21] — today's partial
      day inside the average. Left there it would have re-entered today's shape
      through the template and the fix would have been self-defeating. It also
      contradicted the determinism the ruling asks for: yesterday's future view
      of today averaged [-7,-14,-21,-28], today's view averaged
      [today,-7,-14,-21]. The walk now starts at YESTERDAY, so both views
      average the same four COMPLETED same weekdays. Future days whose weekday
      matches today's therefore changed as well — they were carrying the same
      contamination, and the change moves them the same direction.
      WHERE THE CLASSIFIER LIVES. demandShapeSource(date, today) is pure and
      sits in src/lib/labor-coverage.ts, the module that already declares
      itself PURE with no DB import, so the verify script exercises it with no
      database. addDaysStr/jsDowOf moved there with it and are RE-EXPORTED from
      labor-plan, so every `@/lib/labor-plan` import site is unchanged and the
      date math is not duplicated a second time.
      VERIFIED: scripts/verify-labor-coverage.ts section 9, with "today"
      injected (2026-08-19) and never read from the clock — today takes the
      template, today is not in its own window, a past day still takes its own
      cache, and today's window equals the one yesterday's future view drew.
      Both verify scripts green, npm run build green. No migration, no deps.
      NOT PUSHED — the commit is on local `staging` only, which is why this row
      reads in_progress rather than `staging` (WORKFLOW.md § Session completion
      rules 4 keys on the push). It moves to `staging` in the session that
      pushes it.
      STATUS MOVED 2026-08-20 — the paragraph above is kept as written and is no
      longer the state. It skipped `staging` entirely: the promotion merge
      248a80b took this straight to `main`, so the row went in_progress ->
      shipped in one step, dated the day of the push it rides.
      TWO FINDINGS THIS FIX DID NOT TOUCH, both filed as text per the triage:
      (1) the demand-shape template has NO last-year fallback — the ruling and
      the prompt both describe one, but getDemandShape returns [] when the four
      weekday dates are all empty and the card renders "No sales shape to
      project"; the -364 last-year read that DOES exist is
      /api/labor/weekly-plan:58, and it feeds the sales comparison column, not
      the shape. (2) No UI labels the shape source, so nothing today says
      "projected shape" — the Coverage card's only shape sentence is the
      "Demand-shaped and capped by the conservative budget" footnote
      (labor-coverage-card.tsx:208) and the Weekly Plan day detail carries none.
      Both are Gary's calls, not defects introduced here.
      CROSS-REFS: DECISIONS.md § Same-day coverage shape (the ruling), L-3 (the
      plan engine this rides), BUG-6/BUG-8 (the today-cache refresh path that
      makes today's cache fill in over the day: /api/dashboard/summary,
      /rollup and /sales sync today inline on a first-ever load and refresh it
      behind after() past a staleness TTL, with Square order webhooks and the
      11:00 sales-reconcile cron as the primary freshness).


# COST OF DOING NOTHING — THE DEFINITION, so nobody re-derives a different one.
# Ruled by Gary 2026-08-02, applied by DEBT-TRIAGE-2 to every open row below.
#
#   LIVE   — a user OF THE PRODUCT is affected today. A merchant, an operator
#            running a store, someone using the app.
#   LATENT — it bites on a NAMED trigger. Name the trigger, always.
#   NONE   — no consequence to any user or operator. NONE IS A LEGITIMATE AND
#            USEFUL ANSWER; say it where it is true rather than inflating it.
#
# THE LINE THAT MATTERS IS PRODUCT vs PROCESS. Costs that fall on Gary, on
# Claude, or on the tooling — a red lint baseline, an over-reporting panel
# count, staging fixtures that make a query ambiguous — are LATENT with the
# trigger named, HOWEVER REAL THEY ARE. They are not LIVE.
#
# The reason is the whole point of the field: a list where six things are LIVE
# is a list you cannot read a priority off, which is the list this existed to
# fix. DEBT-TRIAGE-2 first derived six LIVE rows and Gary moved three of them
# (DEBT-33, DEBT-41, DEBT-44) to LATENT on exactly this distinction.
#
# IF A ROW'S COST IS GENUINELY PRODUCT-FACING AND HAS BEEN CALLED PROCESS, THAT
# IS WORTH ARGUING AGAIN. The definition is settled; its application to any
# individual row is not.
#
# Every cost line below is marked DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 because it
# was derived by reading the code and the files at HEAD, NOT inherited from the
# DEBT-TRIAGE audit — whose own 22 assessments exist nowhere in this repo. That
# hole is DEBT-45.
debt:
  - id: DEBT-69
    status: planned
    title: "Coalesce concurrent sales syncs per store-day — Square quota, not correctness"
    notes: >
      MEASURED 2026-08-16 — THE PRECONDITION THIS ROW SET FOR ITSELF IS
      DISCHARGED. Prepended per preserve-and-mark; nothing below is edited. The
      entry below says "REVISIT WHEN WEBHOOKS ARE LIVE ... until then there is
      nothing to measure." Webhooks are live — production Square application
      sq0idp-UdjqLfkxl0hlbw7b30IiLA ("Froot"), 34,914 delivery results all 200 to
      https://www.usefroot.com/api/webhooks/square — and there is now something to
      measure. THE REVISIT IS NOT A BUILD DECISION; it is this measurement. The
      row stays `planned`.
      THE NUMBER. Vercel PRODUCTION function logs, 2026-08-16, ~11:53-14:11 PT
      (18:53-21:11 UTC), filtered on `discarded`: approximately 55 discarded
      syncs across at least nine distinct store ids, all on POST
      /api/webhooks/square. That is roughly ONE DISCARDED SYNC EVERY 2-3 MINUTES,
      sustained, across the nine-store production estate during a normal
      afternoon.
      WHAT EACH ONE COST. Every discarded sync completed a FULL PAGINATED Square
      orders/search before losing at the write — the round trip is already spent
      by the time the guard discards it, which is the entry below's own
      "WHY IT IS WORTH ANYTHING AT ALL" argument with a number attached. So this
      is ~55 complete Square API read cycles and ~55 lambda invocations held for
      their full duration in 2.5 hours, thrown away. Square publishes no rate
      limits, so it remains unquantified spend against an unknown ceiling — but
      the SPEND is no longer unquantified, only the ceiling is.
      THIS IS THE AUDIT'S ESTIMATE, MEASURED RATHER THAN PREDICTED.
      docs/prompts/BUG-7_AUDIT.md § 1d predicted 3-5 events per order against
      76-144 paid orders per store per day, and reasoned from there to "a
      continuous stream of writers where today there are none". The ~55 figure is
      that multiplier observed. Recorded as corroboration of the audit's method,
      not as a new argument.
      NOTHING HERE CHANGES THE RULING BELOW, AND THE MEASUREMENT SHOULD NOT BE
      READ AS RE-OPENING IT. Option 4 was rejected as a FIX because in-process
      coalescing is per-lambda-instance and therefore cannot help across
      concurrent instances or across users — that is a property of the mechanism,
      and no volume of measured waste makes it false. BUG-7's guarded upsert
      remains THE correctness fix and is now verified in production; coalescing
      remains a COST AND QUOTA optimisation. A bigger number makes this row more
      worth building and no more of a race fix than it was on 2026-08-13.
      READ THE ~55 CORRECTLY — it is a floor, not a total. The log line is
      wrapped in `if (discarded.length > 0)`, so successful syncs emit nothing
      and the filter can only return losers. ~55 is the count of DISCARDED syncs
      in that window, not the discard RATE; the denominator is invisible by
      construction. Anyone sizing this work should not divide by anything.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      FILED 2026-08-13 BY GARY'S RULING, DELIBERATELY NOT BUILT. It was Option 4
      in the BUG-7 audit (docs/prompts/BUG-7_AUDIT.md § 1c) and was rejected as a
      FIX because it is per-lambda-instance and therefore closes nothing: it
      lowers the probability of a race without removing it, and cannot help
      across concurrent instances or across users. BUG-7's guarded upsert is the
      correctness fix and is already in (00881dc).
      COST OF DOING NOTHING: NONE today, in this file's own sense — no user of
      the product is affected. This is spend, not behaviour.
      WHAT IT WOULD BE. Single-flight coalescing keyed on (storeId, date) so
      concurrent callers await one in-flight sync instead of each starting their
      own; and/or stopping /api/dashboard/summary and /api/dashboard/sales from
      independently scheduling a sync for the same store-day, which they have
      done on every stale dashboard load since 2081401's after() deferral.
      WHY IT IS WORTH ANYTHING AT ALL. Every racer completes a full paginated
      Square orders/search before losing at commit, so a discarded sync is a
      round trip already spent. One store switch can currently schedule up to 8
      syncs for one store-day (2 initial + 6 polled, BUG-6), times every user and
      every open tab. Square publishes no rate limits — sales-reconcile/route.ts
      says so, which is why that cron walks stores serially — so this is
      unquantified spend against an unknown ceiling.
      REVISIT WHEN WEBHOOKS ARE LIVE (Gary, 2026-08-13). The webhook adds a
      writer at transaction rate, which is when the wasted-call volume starts to
      matter; until then there is nothing to measure. Do NOT build it as a race
      fix — that question is settled and belongs to BUG-7.

  - id: DEBT-70
    status: planned
    title: "Server-rendered dates print UTC calendar days — two surfaces disagree about one signature"
    notes: >
      SERVER HALF CLOSED 2026-08-16, CLIENT HALF STILL OPEN — THIS ROW IS NOW
      THE 42 CLIENT SITES. Prepended per preserve-and-mark; nothing below is
      edited, including the line below that says this row is "NOW THE 22 SERVER
      DISPLAY SITES ONLY", which was true when written and has been overtaken:
      that scope was built as DEBT-70b ("cc144dc") and verified on staging the
      same day, alongside the PDF half DEBT-70a ("623acb6").


      WHAT IS DONE. The 22 server-rendered date displays now resolve
      store-local through displayTimeZone (lib/hr.ts) with no bare-UTC arm, and
      the one civil date moved to formatCivilDate, which takes no zone argument
      at all. Verified in a browser on branch br-square-feather-a63z92vz /
      neondb, org org_3G02wO4QlVVSWppi8aqlnSZnsDa: the two staff tabs that
      filed this row now agree on Aug 15, and /checklists still reads Sun,
      Aug 16 unmoved. Evidence is on the DEBT-70b row.


      WHAT IS NOT DONE, AND IT IS THE HALF THAT MAKES THIS STRUCTURAL. THE 42
      CLIENT SITES REMAIN VIEWER-ANCHORED while the converted server surfaces
      are now STORE-anchored. They agree whenever the viewer sits in the store's
      timezone — which is the normal case, was the acceptance case, and is
      therefore the case that CANNOT DETECT THE REMAINING GAP. A manager reading
      from another zone can still see one signature carry two calendar days.
      CONVERGENCE IS NOT STRUCTURAL UNTIL THE CLIENT SITES MOVE.


      SO THE ROW STAYS OPEN, AND THE REASON IS WORTH STATING PLAINLY: the
      original complaint — two surfaces disagreeing about one signature — is
      no longer reproducible for a same-zone viewer, and is STILL REACHABLE for
      a cross-zone one. Closing this row on tonight's pass would record a
      same-zone observation as proof of a cross-zone property. The remaining
      decision is the one the WHAT THE FIX PROBABLY IS paragraph below already
      names and DEBT-70a/70b have now half-answered: the server half chose the
      STORE's day, so the client half is no longer an open design question but
      a conformance job — move the 42 to the same resolver, or rule explicitly
      that viewer-anchored is correct for non-signature dates and write down
      where the line falls.


      NOT URGENT, UNCHANGED, AND FOR THE SAME REASON AS BEFORE: no record is
      wrong, no compliance state is wrong, nothing is miscounted. The residue is
      a cross-zone display disagreement on evening records.


      FILED 2026-08-16 BY GARY'S INSTRUCTION, during the HR-11k Phase A display
      follow-ups ("6b2054f"). Observed by Gary on staging: the SAME signed record
      renders "Aug 15" on the staff profile's Documents tab and "Aug 16" on
      /my/documents. Two compliance surfaces telling different stories about one
      signature. Not an R2 defect — R2 only made it visible by putting a date on
      a row that reaches both surfaces.


      THE MECHANISM, MEASURED. Every DateTime column is TIMESTAMP(3) with no
      time zone, so Prisma returns UTC. `format()` from date-fns renders in the
      TZ OF WHATEVER RUNS IT. A "use client" component runs in the browser and
      prints the viewer's local day; a server component runs on Vercel, where
      TZ is UTC, and prints the UTC day. Same instant, two calendar days,
      whenever the record falls in the evening:


        record 2026-08-16 04:06 UTC = 2026-08-15 21:06 PDT
        TZ=America/Los_Angeles -> "Aug 15, 2026"    (staff-documents.tsx, client)
        TZ=UTC                 -> "Aug 16, 2026"    (my/documents/page.tsx, server)


      Reproduced locally against the shipped formatter, both zones, 2026-08-16.


      IT IS ALSO WITHIN ONE PAGE. The staff profile's Documents tab is a client
      component and reads Aug 15; its Compliance tab (staff-compliance.tsx) is a
      server component and reads Aug 16. Same profile, same record, one scroll
      apart.


      SCOPE, AND WHY IT WAS NOT FIXED IN THAT SESSION. 11 server components
      render dates across 22 call sites — checklists, users, hr/compliance,
      hr/signed-records, staff/[id], staff-compliance, my, my/training,
      my/documents, my/documents/records/[recordId], training-module-view — set
      against 20 client components doing it correctly by accident. Gary's
      instruction was explicit: contained, fix it; reaches beyond those files,
      file a row. It reaches. A PARTIAL FIX IS WORSE THAN NONE HERE: correcting
      only /my/documents would make it agree with the staff Documents tab and
      still disagree with the staff Compliance tab, trading one visible
      disagreement for a subtler one.


      THIS IS CLAUDE.md § Database Evidence's UTC/local precondition ONE LAYER
      OUT. That precondition governs what WE compare while investigating; this
      is the same offset reaching the PRODUCT, where the reader has no way to
      know which zone they are looking at and no suffix tells them. The
      precondition was written after the offset fired three times in one day on
      four rows. Same failure, rendered for a customer.


      WHAT THE FIX PROBABLY IS, not ruled: one shared formatter, and a decision
      about WHOSE day is authoritative — the viewer's browser, or the store's.
      They are different questions and the second may need a timezone on Store
      or Organization (schema, so TIER 3). The viewer's-browser answer is
      additive and needs no column. Note that a SIGNATURE arguably belongs to
      the store's day rather than the reader's, which is the argument for the
      harder option; a signed certificate is a legal artifact and its date
      should not change depending on who opens it.


      SURFACE LIST EXTENDED 2026-08-16, BY GARY'S INSTRUCTION, AFTER IT SURFACED
      AGAIN ON THE VERSIONS CARD of /hr/documents/[id]: v6 and v7 are 15:02 and
      17:02 UTC in the database and render as 8:02 AM and 10:02 AM — UTC-7, PDT.
      AND THAT CARD IS ON THE CORRECT SIDE OF THIS BUG, which is the thing to
      understand before anyone "fixes" it: document-detail-client.tsx is a
      client component, so it renders the VIEWER'S day, which is the day a human
      means. It looks wrong only when read against a raw UTC value in the Neon
      console. The defect is the ELEVEN SERVER components that print Vercel's
      UTC day at a reader who is not in UTC.


      FULL COUNT, re-measured 2026-08-16 across src/app and src/components:
      31 files render dates, 64 format() calls. 20 files / 42 calls are CLIENT
      (viewer-local — correct today, correct by accident). 11 files / 22 calls
      are SERVER (Vercel UTC — the defect). The "22" recorded above is the
      server half only; the denominator it should be read against is 64, and any
      fix has to decide what the other 42 do, not just repair the 22. That is
      most of why this is TIER 3 rather than a sweep.


      SPLIT 2026-08-16: DEBT-70a CARRIES THE PDF HALF AND IS BUILT ("623acb6").
      The audit (docs/prompts/DEBT-70_AUDIT.md §5.2) found an instance this row's
      own sweep could not see — hr-signed-pdf.ts does not import date-fns, so the
      `format(` search missed a bare `toISOString().slice(0,10)` stamped into the
      document BODY as the `Date:` line. Gary split it off and took it first
      because it is the only part producing PERMANENT artifacts: a screen
      re-renders after a fix, a minted PDF never does. THIS ROW IS NOW THE 22
      SERVER DISPLAY SITES ONLY, and DEBT-70a settled two things it inherits —
      Organization.timezone exists as of migration
      20260816180000_organization_timezone, and primaryStoreTimeZone (lib/hr.ts)
      is the ruled resolver. Neither needs re-deciding here.


      NOT URGENT, AND SAY WHY: no record is wrong, no compliance state is wrong,
      nothing is miscounted. It is a display disagreement, visible only on
      evening records, and it has been there since the first server-rendered
      date. It becomes urgent if anyone ever reads a date off a screen into a
      legal or HR process.

  - id: DEBT-70b
    status: verified
    title: "The 22 server-side date displays now render the store-local day"
    commits:
      - "cc144dc"
    notes: >
      VERIFIED ON STAGING 2026-08-16 IN A BROWSER. Status in_progress ->
      verified. Prepended per preserve-and-mark; nothing below is edited,
      including the "NOT YET VERIFIED ON STAGING" line, which was true when
      written. Verified in the same pass as DEBT-70a ("623acb6").


      THE ANCHOR CASE, WHICH IS THE ONE THAT MATTERS. Gdogg Thomas's v5 record
      reads AUG 15 on all four surfaces: the staff profile's Documents tab, the
      staff profile's Compliance tab, /my/documents, and /hr/signed-records.
      Branch br-square-feather-a63z92vz / neondb, org
      org_3G02wO4QlVVSWppi8aqlnSZnsDa, Clerk instance verified-snapper-7.


      THE TWO STAFF TABS PREVIOUSLY DISAGREED BY A DAY, ONE SCROLL APART — the
      Documents tab (client) read Aug 15 and the Compliance tab (server) read
      Aug 16. THAT WAS THE DEFECT AND IT IS CLOSED. This is the exact
      observation that filed DEBT-70, re-taken on the same record after the fix,
      which is why it is the acceptance rather than a spot check.


      THE CIVIL-DATE REGRESSION PASSED. /checklists reads "Sun, Aug 16",
      UNMOVED. That is the trap this row handled before any edit — a civil date
      is a store-local day already stored at UTC midnight, so converting it to
      Pacific would have moved it a day BACKWARD, silently, with no screen
      showing it. formatCivilDate takes no zone argument and the browser
      confirms what the six-boundary-day fixture asserted.


      SCOPE OF THE PASS, STATED SO IT IS NOT READ AS MORE THAN IT IS: four
      surfaces on one anchor record, plus the civil-date regression. Not all 22
      call sites were individually exercised in the browser. The 72/72
      byte-for-byte fixture below is what covers the rest, and the four surfaces
      here are the ones the defect was reported on.


      CONVERGENCE IS STILL NOT STRUCTURAL, and this pass is precisely the case
      that cannot show it. The verified viewer was in the store's timezone, so
      the STORE-anchored server sites and the VIEWER-anchored client sites
      agreed — they agree whenever those two zones match. A manager reading from
      another zone can still see the split. The client half is open on DEBT-70;
      this row's own "argument for the client row" paragraph below is unchanged
      by tonight's pass and is if anything strengthened, since the pass could
      not have detected the remaining gap.


      BUILT 2026-08-16 ("cc144dc"), TIER 2, NOT YET VERIFIED ON STAGING. The
      display half of DEBT-70; the PDF half shipped as DEBT-70a ("623acb6").
      Audit of record: docs/prompts/DEBT-70_AUDIT.md.


      THE RULING (Gary, 2026-08-16): THE CLIENT WAS RIGHT. Gdogg's record is
      2026-08-16 04:06 UTC = 2026-08-15 21:06 PDT; the staff profile's Documents
      tab (client) read Aug 15 and its Compliance tab (server) read Aug 16, one
      scroll apart. The signature happened at 9:06 PM on August 15 and the server
      printed a day nobody lived, so the 22 server sites move to store-local.


      THE TRAP, AND IT WAS HANDLED BEFORE ANY EDIT. Two kinds of DateTime pull in
      OPPOSITE directions. A CIVIL DATE — Checklist.date, written through
      dbDate() — is a store-local day already stored at UTC midnight: rendering
      it in UTC is CORRECT and converting it to Pacific moves it A DAY BACKWARD,
      silently, with no screen showing it. An INSTANT is a real moment and
      rendering it in UTC is wrong. All 22 were classified against their WRITE
      PATHS first: 21 instants, 1 civil date (checklists/page.tsx:191), and zero
      of the other 21 columns are written through dbDate(). terminatedAt,
      rehiredAt, certifiedAt and publishedAt are all `new Date()`; dueDate is
      `new Date("<day>T12:00:00")`, an instant whose NOON PIN is what keeps it
      drift-proof either way.


      THE CIVIL DATE WAS NOT LEFT ALONE — it moved to formatCivilDate, WHICH
      TAKES NO ZONE ARGUMENT AT ALL. Behaviour is byte-identical; what changed is
      that being right became STATED rather than accidental, and the one function
      that could break it can no longer be handed a store zone by a future edit.
      Exercised deliberately on six boundary days: all unmoved, and the test
      prints what each WOULD have become if converted (Aug 15 -> Aug 14, Jan 1 ->
      Dec 31) so the regression is visible rather than asserted.


      ONE MECHANISM. lib/display-time.ts is Intl.DateTimeFormat with an explicit
      timeZone — the same primitive localDateStr (reports.ts:51),
      businessDayWindow (:71) and formatWindowTime (checklist-status-display.ts
      :141) already are, given a shared home so 22 call sites do not each
      hand-roll options. STYLES ARE NAMED RATHER THAN FORMAT STRINGS, and a
      fixture asserts that with the zone forced to UTC all nine reproduce their
      date-fns pattern BYTE-FOR-BYTE across eight awkward instants — 72/72 pass.
      That property is what makes the diff reviewable: the only behavioural
      change in the whole phase is which zone is asked for.


      @date-fns/tz WAS CONSIDERED AND REJECTED. It would have preserved every
      format string with a one-token edit per site, but it is present only
      TRANSITIVELY via react-day-picker — an undeclared dependency that an
      unrelated upgrade could remove — and the brief's "use the existing
      mechanism" points at Intl regardless.


      THE ZONE comes from displayTimeZone (lib/hr.ts): subject's primary store ->
      Organization.timezone -> the schema default. DEBT-70a established that
      chain and spelled it out inline at its single mint site; this row FACTORS
      IT OUT rather than copying it 22 times, because 22 copies of a fallback
      chain is how a fallback becomes three different chains. THERE IS NO
      BARE-UTC ARM and `org` is REQUIRED, not optional — a site that cannot
      supply one has not loaded enough to render a date, and a silent UTC
      fallback would reproduce this bug while looking fixed. /users legitimately
      has no staff subject (Clerk identities, not StaffMembers) and enters at the
      org step; that is the same chain, not a second one.


      StaffComplianceDetail and PendingCountersign now CARRY `timeZone`, on the
      object the timestamps already travel on, so a surface cannot render one
      without the other — four of the 22 needed no new query because of it.


      SCOPE HELD: server sites only, 42 client sites untouched (verified by
      count, still 42). AND THE CONSEQUENCE IS WORTH WRITING DOWN: the three
      converted signature surfaces are now STORE-anchored while the staff
      Documents tab remains VIEWER-anchored. They agree for a viewer in the
      store's zone — the normal case, and the acceptance case — but CONVERGENCE
      IS NOT STRUCTURAL until the client sites move too. A manager reading from
      another zone can still see the split. That is the argument for the client
      row, and it should be made with this sentence.


      LOGIC SITES UNTOUCHED per the brief. hr-compliance.ts:564 (dueDate < now)
      is instant-vs-instant and safe; dueDate's noon pin is the guard against
      exactly this bug and was left alone. No logic site was found beyond the
      audit's.


      STILL OWED ON STAGING: the anchor case on all four surfaces using a record
      whose UTC and local dates actually DIFFER (3 of 10 qualify); the
      civil-date regression exercised deliberately on /checklists; HrSignedRecord
      and HrDocumentAcknowledgment counts unchanged. Nothing was observed on
      staging — these commits are not deployed.

  - id: DEBT-70a
    status: verified
    title: "Inline Date: stamp on minted PDFs rendered a bare UTC day — now store-local"
    commits:
      - "623acb6"
    notes: >
      VERIFIED ON STAGING 2026-08-16 IN A BROWSER, IN THE WINDOW WHERE THE TWO
      DATES DIFFER — which is the only window that can prove anything here.
      Status in_progress -> verified. Prepended per preserve-and-mark; nothing
      below is edited, including the "NOT YET VERIFIED ON STAGING" line, which
      was true when written. RECORDER COMMIT f7723fe carries this block and
      DEBT-70b's, DEBT-70's server-half closure, and DEBT-73. `commits:` still
      names 623acb6 alone — that is the FIX; f7723fe is the record of its
      verification and does not belong in the same field.


      THE EVIDENCE. Tommy Thomas, v7 of "Test - Handbook Regression". Completed
      2026-08-17 02:05:01 UTC = 2026-08-16 19:05:01 PDT — both stated, per
      CLAUDE.md § Database Evidence. Branch br-square-feather-a63z92vz / neondb,
      org org_3G02wO4QlVVSWppi8aqlnSZnsDa, Clerk instance verified-snapper-7.
      The body "Date:" line on page 1 reads 2026-08-16 — THE DAY THE SIGNER WAS
      AT WORK. The certificate's "Completed at" and the SIGNED AT (UTC) column
      read 2026-08-17 ... UTC, still labelled, unchanged.


      THAT IS THE WHOLE RULING DEMONSTRATED IN ONE RECORD: the certificate is
      EVIDENCE and stays UTC; the body stamp is a FIELD A PERSON READS and now
      names their day. The two disagreeing by a calendar day is not a defect
      here — it is the design, and this record is the case that shows both
      halves behaving as ruled at the same instant. Note that the mint happened
      after 7 PM Pacific, which is exactly the post-5-PM condition this row's
      STILL OWED list below asked for; the owed item is discharged, not waived.


      WHAT CANNOT BE FIXED, AND THE NUMBER IS AN UPPER BOUND. 3 of 10 signed
      records on staging carry a stamp minted before the fix. UPPER BOUND
      because it was measured as records whose UTC and store-local dates DIFFER
      — the true count can only be SMALLER, since a record only carries a wrong
      stamp if a DateStamp anchor was actually confirmed on it. All three are
      staging test records. Signed records are frozen and NONE WAS REGENERATED;
      this row could not repair them and did not try.


      PRODUCTION IS NOT AFFECTED — MEASURED, NOT ASSUMED. HR is LIVE in
      production, not dark: HR_MODULE_AVAILABLE=true was added to the Vercel
      Production scope 2026-07-24 (docs/DEPLOY_LOG.md, the "HR LAUNCH" entry),
      and org cf888f2d-f234-48c7-8097-fd5b44b5b3dd carries activeModules =
      {inventory,labor,hr} with 5 STORE / 1 MANAGER / 3 ADMIN / 1 STAFF. ANY
      NOTE ON THIS ROW OR ELSEWHERE CLAIMING HR IS DARK IN PRODUCTION IS STALE
      BY NINETEEN DAYS AND MUST NOT BE RELIED ON.


      Production nonetheless carries ZERO wrongly-stamped records. Branch
      br-sparkling-block-a620qvg4, measured 2026-08-16: three HrSignedRecord
      rows exist — 2026-07-13 21:47:43.527 UTC (14:47 PDT), 2026-08-03
      19:24:48.709 UTC (12:24 PDT), 2026-08-03 21:51:06.908 UTC (14:51 PDT).
      All three completed in the early afternoon Pacific, so UTC and store-local
      calendar dates agree on every one and dates_differ is false across the
      board. THE STAGING COUNT OF 3-OF-10 DOES NOT TRANSFER TO PRODUCTION.


      DEBT-70b VERIFIED IN THE SAME PASS ("cc144dc") — recorded on its own row;
      noted here because the two shipped as one thought and a reader arriving at
      either should know the other passed.


      DEBT-70 IS NOT FULLY CLOSED BY THIS. The converted server surfaces are
      STORE-anchored; the 42 client sites remain VIEWER-anchored. They agree for
      a viewer in the store's timezone — the case verified tonight — but
      CONVERGENCE IS NOT STRUCTURAL UNTIL THE CLIENT SITES MOVE. The server half
      is closed; the client half is open, and is on DEBT-70.


      ONE LOOSE END FILED, NOT CHANGED: the page-1 banner ("Completed by ... on
      2026-08-17 02:05:01 UTC") is a body element a person reads, rendered in
      UTC. Consistent with the certificate and arguably correct, but it sits on
      the same page as the stamp that now reads local. Filed as DEBT-73 for a
      ruling. DELIBERATELY NOT TOUCHED by this session.


      BUILT 2026-08-16 ("623acb6"), TIER 2, NOT YET VERIFIED ON STAGING. Split
      out of DEBT-70 by Gary because it is THE ONLY PART THAT PRODUCES PERMANENT
      ARTIFACTS. Audit of record: docs/prompts/DEBT-70_AUDIT.md (same commit),
      which is DEBT-70's Step 1 audit — this row is §5.2 of it.


      THE DEFECT. formatDateStamp's `dateOnly` branch — the ORG DEFAULT — was
      `d.toISOString().slice(0, 10)`: a bare UTC calendar date, no zone marker,
      stamped into the document BODY on the `Date:` line a person reads as the
      day they signed. Gdogg signed at 21:06 PDT on 2026-08-15 and his handbook
      says 2026-08-16 — a day he was not at work, on his own signature line.


      WHY IT WENT AHEAD OF DEBT-70'S 22 SCREENS. A screen re-renders after a fix.
      A MINTED PDF NEVER DOES: records are append-only and ensureSignedRecord
      returns the existing row rather than regenerating. Every certificate
      produced before this landed keeps its wrong date permanently, so this
      defect's cost grew every day it stayed open, which no display bug does.


      IT WAS INVISIBLE TO THE SWEEP THAT FOUND DEBT-70'S 64 SITES, and that is
      the transferable lesson. hr-signed-pdf.ts does not import date-fns, so a
      `format(` search could not see it; it was found by reading the mint path
      instead. CLAUDE.md § Verifying a guard covers every path, one layer out —
      a sweep is only as complete as the shape it searches for, and the shape is
      chosen from what you already know exists.


      BOTH BRANCHES WERE WRONG, which Gary asked to have confirmed either way.
      `dateOnly`: wrong day AND unlabelled. `dateTime`: `utc(d)`, so labelled and
      technically honest — but still the WRONG DAY on a body field, which is the
      whole complaint. Same function, same mint path, so in scope. It now renders
      store-local date+time carrying its zone ("2026-08-15 21:06:00 PDT"),
      keeping the label that made it defensible without lying about the day.


      NO SECOND MECHANISM WAS INVENTED (Gary's constraint). The zone comes from
      the app's settled Store.timezone convention and its EXISTING helper —
      localDateStr in lib/reports.ts, the same primitive checklists, forecasting,
      labor and reports already trust. The resolver, primaryStoreTimeZone in
      lib/hr.ts, is deliberately a SIBLING of primaryStoreName rather than a
      second sort: that function sorts INTERNALLY because its value is frozen
      into a legal record and one caller forgetting the orderBy would make a
      signed document depend on row order. The identical argument holds for the
      zone, which decides a DATE on the same document, so it gets the identical
      internal sort. The two must always pick the same assignment.


      IT RETURNS NULL FOR CORPORATE STAFF rather than a constant — the one place
      the siblings legitimately diverge. "Corporate" is a true, printable answer
      to "where do they work"; there is no equivalent for a zone, so null hands
      the question up to the org column instead of burying a fallback where
      callers cannot see it.


      SCHEMA — ONE ADDITIVE COLUMN. Organization.timezone String
      @default("America/Los_Angeles"), migration
      20260816180000_organization_timezone. SAME DEFAULT AS Store.timezone
      DELIBERATELY: the two are read in one chain, so a different default here
      would make the fallback silently CHANGE the answer rather than preserve it.
      Follows hrDateStampFormat's precedent on that model.


      MOST-COMMON-STORE-ZONE WAS REJECTED BY GARY as the no-schema alternative,
      and the reason belongs on the row: it changes what day an ALREADY-SIGNED
      signature displays as soon as a store is added in another zone. An explicit
      column cannot drift under a record that is supposed to be frozen.


      RESOLUTION CHAIN, WITH NO BARE-UTC ARM ANYWHERE: signer's primary store ->
      Organization.timezone -> the default Store.timezone already carries.


      THE CERTIFICATE IS UNTOUCHED, per Gary's item 3. `utc()` (hr-signed-pdf.ts
      :66) and every caller of it — the page-1 banner :381, the signature
      validation line :474, "SIGNED AT (UTC)" :570, the capture rows :628, and
      every certificate field — stay exactly as they are. CONFIRMED BY READING
      THE WHOLE MINT PATH RATHER THAN ASSUMING: formatDateStamp's dateOnly branch
      was the ONLY unlabelled date rendering in the file; all 15 others go
      through utc() and carry the label.


      VERIFIED AGAINST THE SHIPPED RESOLVER AND PRIMITIVES: Nevada and New Mexico
      stores both read 2026-08-15 with correct distinct clock times (21:06 PDT /
      22:06 MDT) — the multi-tenant case, which is the one a single org zone
      would have broken; a multi-store member with UNSORTED assignments picks the
      primary; corporate falls to the org column, not UTC; DST handled both ways
      (PDT/MDT in August, PST/MST in January); output is ASCII-safe for pdf-lib's
      WinAnsi fonts. Dev (br-broad-wave-a6vpjdw0/neondb): migration applied, 1
      HrSignedRecord, 0 in the affected window, 0 wrongly stamped.


      STILL OWED ON STAGING: mint a fresh PDF after 5 PM Pacific (when UTC has
      rolled over) and read the body Date: line against the certificate's SIGNED
      AT; confirm a corporate member resolves through the org column; confirm
      HrSignedRecord and HrDocumentAcknowledgment counts unchanged. AND THE COUNT
      OF WHAT CANNOT BE FIXED — how many already-minted records carry a wrong
      stamp. Dev answers 0; staging holds Gdogg and needs the Neon console. The
      query is in the session report and runs safely BEFORE the migration
      deploys, since it uses the literal default rather than the new column.


      NOTHING ALREADY MINTED WAS TOUCHED, and nothing here can touch it: this row
      changes only what FUTURE mints stamp. Signed records are frozen.

  - id: DEBT-71
    status: planned
    title: "Name-mismatch banner fires on correct initials — compares Initial checkpoints to fullName"
    notes: >
      FILED 2026-08-16 BY GARY'S INSTRUCTION, observed by him on staging during
      the HR-11k Phase A follow-ups. NOT FIXED — explicitly out of scope for that
      session ("Real defect, pre-existing, needs its own row. Do not fix here"),
      and untouched by it.


      THE DEFECT AS OBSERVED. The name-mismatch banner fires on the value "GT".
      It compares Initial-type checkpoints against the staff member's `fullName`,
      so a correctly-executed INITIAL — which is by definition not the full name —
      trips a warning that says the signer typed the wrong name. The certificate
      confirms name-as-executed matches exactly, so the artifact is right and the
      banner is wrong.


      WHY IT MATTERS MORE THAN IT LOOKS. The banner is a fraud/identity signal.
      One that fires on the correct value trains every reader to dismiss it, and
      the whole worth of the check is that it is rare. A warning nobody believes
      is worse than no warning, because it still costs a look.


      PRE-EXISTING, NOT R2. R2 changed nothing on this path; it was visible in
      the same pass only because that pass was looking at a signed record.


      WHAT TO CHECK FIRST, not ruled: whether the comparison should be
      checkpoint-KIND aware (Initial checkpoints compared against derived
      initials, Signature checkpoints against fullName), and what "derived
      initials" means for names the app has not seen — middle names, particles,
      single-word legal names, non-Latin scripts. That last question is the
      reason this is its own row rather than a one-line fix: the naive
      first-letters rule is wrong for a large minority of real names, and a
      banner that fires on THOSE is the same defect with a worse blast radius.

  - id: DEBT-72
    status: planned
    title: "PLACEHOLDER — the promotion procedure never reads the roadmap, so a roadmap gate cannot stop a promotion"
    notes: >
      ── DEBT-72a SPLIT OUT AND DONE 2026-08-17 — THIS ROW IS UNCHANGED AND
      STILL planned. Prepended per preserve-and-mark; NOTHING BELOW IS EDITED,
      AND NO MECHANISM IS PROPOSED HERE EITHER.
      The audit below found something the ruling had not: the failure mode is
      not promotion, it is THE BOARD GOING STALE AFTER WORK LANDS, and then a
      decision being made from it. That half needs no gate and was ruled and
      executed separately as DEBT-72a on 2026-08-17
      (docs/prompts/DEBT-72a_board_currency.md). It added item 4 to
      docs/WORKFLOW.md § Session completion rules — a row whose code was pushed
      to staging in a session has its status set IN THAT SESSION, with an
      explicit corollary for debt rows, where a missing status already means
      OPEN — and it backfilled the board: TWELVE rows corrected, ELEVEN of them
      reading in_progress while their code was live in production, eight of
      those stale for five to six days. Evidence and the full measured table:
      docs/prompts/DEBT-72a_BACKFILL.md. DEBT-72a's work commit is "5c6419f",
      carried here as prose rather than in commits: — the same convention 5ecceb3
      set on HR-11d, since commits: means WORK commits for THIS row and DEBT-72
      itself still has none. NOT PUSHED at the time of writing: 5c6419f is on
      local staging only, which is why this row does not read staging either.
      WHAT THAT DOES AND DOES NOT DO TO THIS ROW. It does not build the gate and
      does not answer any of the four open decisions listed below; they are
      still Gary's. What it does is remove the precondition that made the gate
      unshippable — a gate built against the board as it stood on 2026-08-16
      would have refused every promotion on day one, which is what the audit's
      §3b replay measured. THE GATE IS DEBT-72b, sequenced after this row and
      deliberately after Gary has lived with the new rule through two or three
      promotions.
      DEBT-72's OWN STATUS STAYS planned, and that is measured rather than
      assumed: its only commits, "b809e03" (the audit) and "039e7a2" (that
      audit's recorder), are on LOCAL staging and are NOT on origin/staging.
      Tested against the remote refs, this row's code is nowhere. planned is
      what the board should say.

      AUDITED 2026-08-17 — THE RULED GATE WAS NOT BUILT. §3a'S OWN STOP CONDITION
      FIRED. Prepended per preserve-and-mark; nothing below is edited, and NO
      MECHANISM IS PROPOSED HERE — the four open decisions are Gary's and are
      listed in the audit, not in this row. Full evidence:
      docs/prompts/DEBT-72_AUDIT.md, committed as b809e03 — the audit SHA is
      carried here as prose rather than in commits:, per the convention 5ecceb3
      set on HR-11d, because nothing shipped and commits: would imply it did.
      WHAT WAS MEASURED. The prompt's row-ID regex is anchored to the start of the
      commit subject, and the repo completed a move to Conventional Commits on
      2026-08-15 — so on 06dc830..7e77ea6 it recognised ONE row ID across 47
      commits (43 subjects, 91.5%, matched nothing), and on the 21-commit range
      493175e..63407be it recognised NONE AND RETURNED PASS. A gate that passes by
      not looking is the false-PASS class CLAUDE.md § Verifying a guard covers
      every path already warns about, and it is worse than no gate. Unanchored,
      81% of subjects resolve. Two further defects the prompt did not anticipate:
      28 of 75 debt rows carry NO status: field (which §2.3 fails closed on, so
      37% of the debt board becomes an automatic blocker), and ROADMAP.yaml has a
      FOURTH collection — rulings, R1..R6, all status "ruled" — which §4 does not
      resolve against, whose IDs appear in real commit subjects, and for which no
      reading of the ruling lets a commit naming R2 pass.
      WHAT PASSED. The §5 acceptance criterion was MET: replayed against
      ce036f9..06dc830 with the board as it stood at that moment, the gate BLOCKS
      on "BUG-7 — in_progress", using the spec's own anchored regex. The thing
      DEBT-72 exists to catch, it catches. The §5 criterion that a clean older
      range must PASS could NOT be met — eleven ranges swept, and PASS appears
      only where the regex found zero IDs.
      CONTRADICTS §1 OF THE PROMPT. WORKFLOW.md §2 does NOT read "git merge
      staging --no-edit". It has read "--no-ff --no-edit" since 2026-08-07, with a
      "Why --no-ff" paragraph citing DEBT-38 and the DEPLOY_LOG add/commit steps
      inside the copyable block. Ruling §2.2 is therefore already satisfied in
      prose, and DEBT-38's candidate (a) with it. The genuinely new thing this row
      buys is the STATUS GATE, which exists nowhere. DEBT-38 was NOT closed and
      NOT annotated this session — its open question is not yet answered, because
      the half that would answer it was not built. Also: the promoted range is 47
      commits, not the 48 the prompt states.
      STATUS STAYS "planned". §8 asked for "staging" once the work was on staging;
      there is no work. Nothing was written to scripts/, package.json or
      WORKFLOW.md. The row remains an accurate description of an open problem.
      PLACEHOLDER ROW, FILED 2026-08-16. THE REMEDY IS DELIBERATELY NOT DRAFTED
      HERE. Gary reserved the WORKFLOW.md promotion gate as his own ruling, to be
      written and ratified in his own words — "a recommendation is not a ruling
      until I have written and ratified it" (session prompt
      docs/prompts/BUG-7_CLOSURE_AND_DEPLOY_LOG_RECONCILIATION.md § 3). This row
      exists so the finding is not lost while it waits, and it holds the problem
      statement ONLY. DO NOT ADD A PROPOSED MECHANISM TO IT.
      THE FINDING, which is the whole content of this row. 00881dc (BUG-7's
      guarded upsert) reached production while its own ROADMAP row read
      `in_progress` and carried "NOT YET VERIFIED ON STAGING", and while the same
      file recorded an explicit ordering gate — verify BUG-6 on staging -> verify
      BUG-7 on staging -> register the Square subscriptions. BUG-6 was verified.
      BUG-7 was not. The subscriptions were registered about two hours later
      anyway. One missing step in a three-step chain, and NOTHING FAILED: no
      check was bypassed, no warning was dismissed, no rule was overruled.
      Promotion is `git merge staging` plus a push, and neither command has ever
      read docs/ROADMAP.yaml. A gate written in a file nothing in the procedure
      opens is a note, not a gate.
      SCOPE, so this does not grow into a general CI proposal: this row is about
      the ROADMAP-to-PROMOTION seam specifically. The adjacent failure — five
      promotions that skipped WORKFLOW.md §2 entirely and so left no DEPLOY_LOG
      entry and no merge commit — is DEBT-38's second recurrence and stays there.
      Same shape, different document, and they may well share one remedy; that is
      for the ruling to decide, not for this row to assume.
      COST OF DOING NOTHING: LATENT (trigger: the next promotion carrying a row
      whose own status or ordering gate says it is not ready — which is every
      promotion, since staging is currently 42 commits ahead of production and
      carries rows in every state). NOT LIVE: the one time this fired, the code
      that shipped early turned out to be correct and is now verified in
      production. That is luck, and the row exists because the next one may not
      be.
      CROSS-REFS: BUG-7 (the row that shipped past its own gate, now verified),
      DEBT-38 (the DEPLOY_LOG half of the same seam), docs/DECISIONS.md
      2026-08-16 (the mechanism entry this row is the backlog counterpart of).

  - id: DEBT-73
    status: planned
    title: "The minted PDF's page-1 banner reads UTC on the same page as a Date: stamp that now reads store-local"
    notes: >
      FILED 2026-08-16 BY GARY'S INSTRUCTION, during the DEBT-70a/70b staging
      verification. AWAITING A RULING. DELIBERATELY NOT CHANGED — Gary's
      instruction was to file it, and the whole question is whether it should
      move at all.


      WHAT IT IS. The page-1 banner on a minted signed PDF —
      "Completed by ... on 2026-08-17 02:05:01 UTC" (hr-signed-pdf.ts:381, one
      of the utc() callers) — is a BODY ELEMENT A PERSON READS, rendered in UTC
      and labelled as such. Observed on Tommy Thomas's v7 of "Test - Handbook
      Regression", branch br-square-feather-a63z92vz / neondb, org
      org_3G02wO4QlVVSWppi8aqlnSZnsDa.


      WHY IT IS A QUESTION RATHER THAN A DEFECT. It is consistent with the
      certificate and it carries its zone label, so it is arguably correct on
      both of the tests DEBT-70a applied. It was also explicitly left alone by
      that row, on Gary's item 3 — "THE CERTIFICATE IS UNTOUCHED" — and that
      decision was made by reading the whole mint path, not by missing this
      line.


      WHY IT IS WORTH A ROW ANYWAY. It now sits ON THE SAME PAGE as the Date:
      stamp that DEBT-70a moved to store-local. So page 1 of a signed handbook
      can show 2026-08-16 on the signature line and 2026-08-17 in the banner
      above it, both correct under their own rules, both unexplained to the
      person holding the document. The tension is not that either value is
      wrong; it is that one page now answers "when was this signed" twice, in
      two zones, for a reader with no reason to expect that.


      THE RULING NEEDED, and it is genuinely open in both directions: whether
      the EVIDENCE/FIELD line DEBT-70a drew — certificate is evidence and stays
      UTC, body fields name the signer's day — puts this banner on the body side
      because it is on page 1 in prose, or on the evidence side because it is a
      completion attestation. The same argument decides it either way; nobody
      has picked which side the banner is on.


      COST OF DOING NOTHING: LATENT (trigger: anyone reading a minted PDF whose
      completion instant falls after 5 PM Pacific, when the two dates diverge —
      the same window DEBT-70a was verified in). No record is wrong and nothing
      is miscounted. NOTE THE ASYMMETRY THAT ARGUES FOR DECIDING SOONER RATHER
      THAN LATER: a minted PDF is never regenerated, so every document produced
      while this sits open keeps whatever the banner says permanently. That is
      the same argument that made DEBT-70a jump ahead of DEBT-70's 22 screens,
      and it applies here at lower severity — the value is labelled, so a reader
      can at least tell which zone they are in.


      CROSS-REFS: DEBT-70a (moved the Date: stamp; left this deliberately),
      DEBT-70b (the 22 server displays), DEBT-70 (the open client half).

  - id: DEBT-74
    status: planned
    title: "SalesLineCache is 84% of the database and grows ~72 MB/year — the Neon Free headroom is its conversation, not Advanced Labor's"
    notes: >
      FILED 2026-08-18 BY GARY'S RULING at the AL-1 hard stop (docs/ADVANCED_LABOR.md
      § Open questions Q8). AL-1 was required to estimate its own new table's
      growth before building it; the measurement taken to produce that estimate
      found something four times larger already running, and Gary's instruction
      was to file it citing the measurement rather than to fold it into the
      labor phase. NOT ACTED ON — a retention policy and a Neon plan are both
      his, and neither was AL-1's to decide.

      THE MEASUREMENT, all taken 2026-08-18 against the LOCAL DEV DATABASE
      (endpoint ep-late-water-a6k53nv2, the one DATABASE_URL points at — branch
      label per CLAUDE.md § Database Evidence). Production was NOT queried;
      production reads go through the Neon console and no deployed credential
      was pulled.

        table               rows       total bytes    bytes/row
        SalesLineCache      254,087    115,990,528    456
        SalesHourlyCache     31,894     12,648,448    397
        SalesPeriodCache      2,440      1,302,528    534
        database total                145,547,264    (138.8 MB)

      SalesLineCache is 116 MB of a 138.8 MB database — 84% of every byte. Its
      date range runs 2025-01-01 to 2026-08-13, 590 days, which puts it at ~431
      rows/day across nine stores (~48 per store per day) and therefore
      ~72 MB/YEAR at the current estate.

      THE CONTEXT THAT MAKES IT A ROW. Neon's Free plan is 0.5 GB per project
      (neon.com/docs/introduction/plans, read 2026-08-18), the project was at
      ~82% on 2026-08-17 (docs/prompts/FROOT_SESSION_HANDOFF_2026-08-17.md), and
      that leaves roughly 90 MB of headroom. One year of SalesLineCache at the
      measured rate is 80% of what remains. Child branches are billed on the
      minimum of their accumulated delta or their logical size, so staging and
      dev — both forked from production — pay for divergence rather than for a
      full copy; the root branch is where this lands.

      WHY IT IS FILED AGAINST THIS TABLE AND NOT AGAINST THE PLAN. AL-1's own
      new table, SquareTimecard, projects at ~19 MB/year (~86 rows/day at nine
      stores, ~600 bytes/row) — roughly a quarter of SalesLineCache's rate, and
      the estimate Gary asked for. Ruling the plan upgrade on the smaller of the
      two would have been answering the wrong question. If the headroom is worth
      money, the first thing to look at is the table that is 84% of the problem.

      WHAT THIS ROW DOES NOT DECIDE, deliberately, because all three are Gary's:
      whether to upgrade the Neon plan; whether SalesLineCache needs a retention
      window at all (it feeds the Item Sales report now and theoretical usage /
      variance in I-6, so old rows are not obviously disposable); and if it does,
      how far back. The measurement is the contribution here, not the remedy.

  - id: DEBT-75
    status: planned
    title: "Demand-shape last-year fallback — build the ruled-but-unbuilt fallback: fewer than 4 completed same-weekdays in SalesHourlyCache → last-year same-weekday hourly shape (−364)"
    notes: >
      FILED 2026-08-19 OUT OF BUG-9'S TRIAGE, ruled the same day (DECISIONS.md
      § "Same-day coverage shape, addenda"): the last-year same-weekday
      fallback of original Advanced Labor decision #7 REMAINS BINDING but is
      not built, and is tracked here rather than fixed inline.
      THE STATE TODAY. getDemandShape (src/lib/labor-plan.ts) averages the four
      completed same-weekday dates from SalesHourlyCache and returns [] when
      none of them has rows — there is no −364 step under it. The card then
      renders "No sales shape to project" (labor-coverage-card.tsx:151) and the
      day has no coverage curve at all. The −364 read that DOES exist,
      /api/labor/weekly-plan:58, feeds the sales COMPARISON column and not the
      shape; do not mistake it for the fallback.
      WHAT TO BUILD: fewer than 4 completed same-weekdays in the cache → fall
      through to the same weekday one year prior (date − 364, the alignment
      goal-engine already uses) for the HOURLY shape. Contained — one helper
      plus a fixture case in scripts/verify-labor-coverage.ts, alongside the
      pure demandShapeSource that BUG-9 added.
      COST OF DOING NOTHING: LATENT, on two named triggers — a newly onboarded
      store whose cache has no four completed same-weekdays yet, and any store
      after 4+ dark weeks. On either, the Coverage card and the Weekly Plan day
      detail show no curve rather than a degraded one.
      CROSS-REFS: BUG-9 (the same-day shape fix this was found in), DECISIONS.md
      § Same-day coverage shape + addenda (the binding ruling), L-3.


  - id: DEBT-1
    status: verified
    commits: ["c17ccc1", "c01a2b1"]
    title: "operationalPhase string inconsistency"
    notes: >
      PROMOTED TO PRODUCTION 2026-08-01 in 63407be, and VERIFIED THERE by Gary:
      the Mid-Shift template form shows "During the Day" — the canonical value,
      rendered by the promoted code over the already-backfilled production data.
      This satisfies the "READ THIS BEFORE PROMOTING" condition below in the
      order it demanded: the promotion first, the flip after. Production no
      longer runs unplugged writers. Debt rows carry no `shipped:` field
      (DebtItem in src/lib/roadmap.ts has none, and adding one type-errors the
      build), so the promotion date lives here.
      CLOSED 2026-07-31 by DEBT-1b — writers plugged in c17ccc1, data backfilled
      on all three branches, record in c01a2b1. Full evidence:
      docs/prompts/DEBT-1_AUDIT.md § DEBT-1b remediation record.
      DATA: zero non-canonical rows remain, verified per branch with branch-named
      before/after counts — branch dev 1 row changed, branch preview/staging 2,
      branch production 1, all three then returning
      non_canonical_remaining = 0. Mechanism was one-off approved SQL per branch
      in the Neon console (ruled 2026-07-31), NOT a committed data migration:
      prisma/ was outside the session's writable set, and a migration file would
      have fired unattended in a Vercel build, taking the operator's hand off a
      production mutation this row has always required be approved per statement.
      Logged in DEPLOY_LOG.md under 2026-07-31 — the entry is explicitly NOT a
      promotion.
      CODE: one shared rule at every write path, src/lib/phases.ts — trim, map
      the single legacy "During Hours" to canonical "During the Day", reject
      anything else BY NAME. That is a deliberate change from the audit's own
      "reject, do not silently coerce" recommendation, ruled by Gary: CSVs
      exported before the backfill still exist on disk, the mapping is
      unambiguous, and unknown values still fail loudly per row on import. All
      six writer sites from the audit go through it, including the seed script
      that originated every dirty row.
      WHY status:staging AND NOT verified — READ THIS BEFORE PROMOTING. The data
      is clean in production but c17ccc1 is on the staging branch only. Until
      staging → main is merged, PRODUCTION RUNS UNPLUGGED WRITERS OVER CLEAN
      DATA. The practical risk is nil (after the backfill there is no legacy row
      for Duplicate to copy, and the origination path was a hand-edited script),
      but it is a real state and it is why this row is not verified. Flip it
      after the promotion, not before.
      NOT DONE, deliberately: both I-14b alias copies stay as belt-and-braces —
      DEBT-32 retires them and folds the three phase lists into one. DEBT-30's
      CHECK constraint is now UNBLOCKED by this backfill and is the durable fix
      for the class; app-layer validation is the ceiling until then.
      ORIGINAL AUDIT MARKER, preserved:
      AUDIT LANDED 2026-07-30 in 21dd712 — docs/prompts/DEBT-1_AUDIT.md. STILL OPEN, and
      deliberately so: an audit does not close the debt. The writer fix and the
      backfill are DEBT-1b, whose prompt is docs/prompts/DEBT-1b_operationalPhase_remediate.md
      and whose hard precondition is that audit file.
      WHAT IT FOUND, in the order it matters:
      (1) THE TAP IS OPEN by inspection — six paths reach the column, two of them
      easily: the Duplicate button (templates-client.tsx:133) copies the raw value
      into a NEW row, and the documented CSV export→import round trip carries it
      between environments (export/route.ts:94 emits it raw; import/route.ts:44
      accepts any string). POST and PATCH on /api/templates validate nothing, and
      the column has no CHECK and no enum. The ORIGIN of every dirty row is
      scripts/import-keva-templates.ts:83, which still hardcodes "During Hours".
      (2) BUT IT HAS NOT FIRED SINCE THE FIX. The newest dirty row on any branch
      was created 2026-07-11 19:44:15 UTC, about 4h16m BEFORE I-14b's alias commit
      8a3667c (2026-07-12 00:00:13 UTC). The timezone step was verified, not
      assumed — reading the Neon console as browser-local would invert it.
      (3) THE BLAST RADIUS IS SMALL, and this is the finding that reframes the row.
      The availability window this field configures is NEVER ENFORCED — see
      DEBT-29. The only functional consumer of operationalPhase is handoff-note
      date resolution, which the I-14b alias already covers. A dirty row's whole
      visible effect is in the template EDIT FORM. This is cleanup, not an
      incident — but it must be done BEFORE DEBT-29 is resolved by implementing
      the gate, or the string starts deciding when checklists appear.
      (4) SCOPE, measured 2026-07-30, each naming its branch: dev 1 row,
      preview/staging 2, production 1 — four rows total, all named "Mid-Shift
      Checklist", all still isActive. NO THIRD VARIANT on any branch.
      Template.operationalPhase is the only carrier, proven by a schema-wide
      sweep rather than assumed. "During the Day" appears in ZERO rows on ANY
      branch — the canonical value has never been written to a database.
      (5) The I-14b alias is BEHAVIOURALLY INERT: phaseOrder falls back to `?? 1`,
      the same value the alias maps "During Hours" to. It is documentation, not
      logic, and can be retired after the backfill.
      CORRECTION to the original text below: the audit-then-fix prompt IS in
      docs/prompts/ now. The DEBT-1a half is committed with the audit in 21dd712;
      the DEBT-1b half exists at docs/prompts/DEBT-1b_operationalPhase_remediate.md
      but is UNCOMMITTED as of 4fdccc5 — it lands with the session that runs it.
      The "at least one live row" claim is confirmed and quantified above. Related
      rows opened by this audit: BUG-5, DEBT-29, DEBT-30, DEBT-31.
      ORIGINAL TEXT, preserved verbatim:
      "During the Day" vs "During Hours". I-14b fixed PHASE_ORDER to the
      canonical "During the Day" with a legacy alias; at least one live row
      still carries "During Hours". An audit-then-fix session prompt was
      drafted but is NOT in docs/prompts/ yet. Neon is the source of truth;
      SQL approval required before any mutation — backfill is the risk, not
      code replacement.

  - id: DEBT-2
    status: shipped
    commits: ["bceca47", "5003d65"]
    title: "Task.sectionName is unvalidated free text with no Section entity"
    notes: >
      REACHED MAIN 2026-08-01 in promotion 63407be. Status flipped staging -> shipped 2026-08-01 by DOCS-2. Debt rows carry no
      `shipped:` field (DebtItem in src/lib/roadmap.ts has none, and adding
      one type-errors the build), so the promotion date lives here.
      CLOSED 2026-08-01 by DEBT-2b in 5003d65. Audit is bceca47
      (docs/prompts/DEBT-2_AUDIT.md); the hardening pass is 5003d65.
      NO DATA STEP WAS RUN, BECAUSE NONE WAS NEEDED. The audit measured all
      three branches and all three were clean — 29 distinct spellings, 29
      distinct lower(btrim()), zero empty, zero untrimmed, zero
      tab/newline/CR/NBSP, zero near-duplicates, zero within-template drift on
      dev, preview/staging AND production. So unlike DEBT-1 there was no
      backfill, no UPDATE, no Neon console, and DEBT-2b touched no database at
      all. A remediation session that runs no SQL is the correct outcome when
      the audit says the data is clean; recorded explicitly so nobody later
      reads the absence of a data step as an omission.
      THE ENUM WAS DELIBERATELY NOT ADDED. sectionName is legitimately free
      text — an operator names their own sections — so there is no canonical
      list to validate against and src/lib/phases.ts is NOT the template.
      The fix is trim plus non-empty, nothing more. This is the single most
      important thing to carry forward: the DEBT-1 shape does not transfer, and
      anyone reaching for a phases.ts equivalent here is solving the wrong
      problem.
      WHAT 5003d65 ACTUALLY CHANGED, four items:
      (1) THE UNGUARDED WRITER, which was the whole of the realised risk.
      template-form.tsx's inline per-row section input persisted "" with no
      guard while addTask and saveEditTask beside it both blocked empty. Those
      two gate their own commit action, so the row input's commit action — the
      form's Save button — is what now gets gated, plus a message naming how
      many rows are blank. TRIM HAPPENS ON BLUR, NOT ON CHANGE, and that
      departure from the session prompt was ruled correct by Gary: trimming per
      keystroke makes a multi-word section name impossible to type, and 23 of
      the 29 live section names are multi-word. An outbound trim in the save
      payload covers the bulk-set, add-task and edit-drawer paths in one place.
      (2) BOTH API CHOKE POINTS now trim and reject blank with a 400 — POST
      /api/templates and the shared taskData() in PATCH /api/templates/[id].
      Re-verified at the checkout that writers 3-9 still funnel through those
      two; the only other writes of the column are the CSV import (already
      trims) and the seed script, both out of scope. The guard precedes the
      trim because .trim() alone throws a TypeError on a request omitting the
      field, turning a 400 into a 500.
      (3) ONE EMPTY-VALUE STRING. The execution client rendered a BLANK heading
      for an empty section while the template detail page, both print pages and
      the CSV import default all used "General". It now groups on the same
      fallback, and the form's section header row reads "General" rather than
      "No section". DISPLAY ONLY — "General" is never written to the database.
      (4) DEBT-1 FOLLOW-THROUGH, not a reopening.
      docs/TEMPLATES_IMPORT_EXPORT.md listed only two operational phases; it now
      lists all three canonical values plus the legacy mapping. DEBT-1b
      recommended this and did not carry it out. DEBT-1 stays closed at
      status:staging and was not touched.
      DEBT-36 CARRIES THE ENTITY QUESTION FORWARD and is deliberately NOT part
      of this row's closure. A Section entity — id, name, order, per-template —
      is the durable answer to everything this row could not fix: no canonical
      list, no rename, no independent ordering, and the finding that because
      Checklist does not snapshot task data, renaming a section retroactively
      rewrites the headings on every historical completed checklist. DEBT-2b
      hardened the writers; it did not model the thing.
      WHY status:staging AND NOT verified: 5003d65 is on the staging branch
      only. Flip it after the promotion, not before. NOTE that main is 19
      commits behind at the time of writing, so this rides with DEBT-1's own
      unpromoted writer fix (c17ccc1) whenever staging → main next happens.
      STILL OPEN AND UNTOUCHED BY THIS SESSION: DEBT-30 (DB-level CHECK),
      DEBT-33 (red lint baseline), DEBT-36 (the entity).
      ── ORIGINAL AUDIT MARKER, preserved ──
      AUDIT LANDED 2026-08-01 in bceca47 — docs/prompts/DEBT-2_AUDIT.md. STILL OPEN,
      and deliberately so: an audit does not close the debt. Remediation is
      DEBT-2b, whose prompt is written after this audit, not before.
      RETITLED BY THIS AUDIT. The old title — "sectionName vs section field
      ambiguity" — named a field that does not exist. That is not pedantry: the
      whole first half of the audit went to establishing it, and the next reader
      should not repeat the hunt.
      (1) THERE IS NO `section` FIELD. A catalog sweep of every column in public
      named %section% returned, on branch dev, exactly ONE row —
      Task.sectionName, text, NOT NULL, no default. No Section model among 79
      Prisma models, no sectionId, and no migration has ever altered the column
      since 20260627002005_init created it. The identifier `section` lives only
      in code, in three unrelated roles: a local key in the seed script's data
      literal (mapped to sectionName at import-keva-templates.ts:363), a
      destructuring variable in three render sites, and <section> tags plus
      .section CSS in the print pages. `task_section` is the CSV wire-format
      column name, not a field.
      (2) SO IT IS ONE PIECE OF DATA WITH FOUR NAMES, one per layer boundary —
      DB/Prisma sectionName, API JSON sectionName, CSV task_section, seed literal
      section. Not an unfinished rename, not a denormalised copy, not an API
      field mapped to a different column. With no Section entity there is no
      id-and-text pair that can disagree, so the disagreement queries are N/A BY
      CONSTRUCTION, measured rather than assumed.
      (3) THE REAL AMBIGUITY, and it is a modelling gap not a dirty-value
      problem: sections are free text with nothing behind them — a GROUP BY over
      a string, recomputed independently at five render sites. Tracked forward as
      DEBT-36.
      (4) THE TAP IS OPEN, WIDER THAN DEBT-1's. Nine write paths, zero validation
      at any of them, and only NOT NULL at the database. Worst is
      template-form.tsx:526-527 — the inline per-row section input has NO guard
      at all, while addTask and saveEditTask beside it both block an empty
      section, so "" is reachable through the UI with no typing skill. Trimming
      is inconsistent INSIDE ONE COMPONENT: bulk-set trims (:405-407), the row
      input next to it does not. GOOD NEWS: seven of the nine funnel through two
      API routes, so the plug surface is three choke points, against DEBT-1's six.
      (5) DATA IS CLEAN ON ALL THREE BRANCHES, each measured separately and each
      naming itself. Branch dev: 29 distinct spellings, 29 distinct
      lower(btrim()), 210 tasks. Branch production: IDENTICAL to dev on all 29
      census rows — every value, every length(), every count, orgs=1 throughout,
      210 tasks / 8 templates / 38 task logs. Branch preview/staging: the SAME
      29 values over exactly 420 tasks, every row matching dev on length()
      exactly and on count at precisely 2x, because the seed script was run
      against a second org there on 2026-07-11 (DEBT-1). All three return zero
      empty, zero untrimmed, zero tab/newline/CR/NBSP, zero near-duplicates,
      zero within-template drift, zero non-contiguous runs, one carrier column,
      and no CHECK beyond NOT NULL. THE BRANCH MOST LIKELY TO HOLD DRIFT HOLDS
      NONE. So there is NOTHING TO BACKFILL — see (6).
      EVIDENCE QUALITY DIFFERS BY BRANCH AND THE AUDIT SAYS SO. dev and
      production are the strong form: identity captured BEFORE the data, same
      session. preview/staging is reconstructed — S1-S7/S9 ran ~18:20 UTC with
      the breadcrumb screenshotted but S0 NOT captured, S0 came at 20:43 UTC,
      and in the gap S0 was run once against PRODUCTION by mistake and discarded
      (disclosed by Gary unprompted). What closes it is structural, not
      testimonial: orgs=2 with counts at exactly 2x dev is a fingerprint only
      preview/staging can produce. THAT ARGUMENT WAS STATED WITH ITS OWN
      FALSIFIER — production returning orgs=2 or 420 tasks would have broken it
      — AND THE FALSIFIER WAS THEN RUN AND CAME BACK NEGATIVE (orgs=1, 210
      tasks). The attribution rests on structure, not on the breadcrumb alone.
      See docs/prompts/DEBT-2_AUDIT.md §4.
      (6) NOT A BACKFILL — SETTLED, not predicted. All three branches measured
      clean, so DEBT-2b is a small hardening pass with NO data step: close the
      unguarded writer (template-form.tsx:526-527), trim uniformly at the two
      API choke points, and collapse the FOUR different empty-value behaviours
      across five render sites onto one string ("General" is the incumbent).
      DO NOT ENUM THIS FIELD: it is legitimately free text, which is exactly
      where DEBT-2 differs from DEBT-1, and src/lib/phases.ts is NOT the
      template to copy. DEBT-1 was a dirty-data cleanup with a writer fix
      attached; DEBT-2 is a writer fix with no cleanup at all.
      ── ORIGINAL TEXT BELOW, UNCHANGED ──
      Next candidate for the same audit-then-remediate treatment as DEBT-1.

  - id: DEBT-3
    status: shipped
    commits: ["6b36471"]
    title: "WORKFLOW.md §3 contradicts the migration policy"
    notes: >
      REACHED MAIN 2026-08-01 in promotion 63407be. Status flipped staging -> shipped 2026-08-01 by DOCS-2. Debt rows carry no
      `shipped:` field (DebtItem in src/lib/roadmap.ts has none, and adding
      one type-errors the build), so the promotion date lives here.
      CLOSED 2026-07-30 in 6b36471. §3 now states that `migrate dev` is broken
      here, gives the hand-authored flow as a six-step SHAPE only (edit schema →
      migrate diff → read the SQL → db execute + migrate resolve → generate →
      commit with the code), and sends the reader to MIGRATIONS.md §3 for the
      exact commands. The §3 heading always promised "see MIGRATIONS.md for full
      detail"; it now honours that instead of carrying a second copy of the
      commands to drift.
      WHY POINT RATHER THAN RESTATE: the commands must connect via
      DATABASE_URL_UNPOOLED (BUG-3), which prisma.config.ts does automatically
      for `--from-config-datasource` and `db execute`. The way a reader bypasses
      that routing is by improvising an inline `DATABASE_URL="…" npx prisma …`
      prefix — so §3 contains no command a reader could follow to do it, and one
      clause telling them to copy from MIGRATIONS.md rather than retype.
      THE :50 PROHIBITION SURVIVED, STRENGTHENED — deliberately, and it is the
      subtle part of this fix. "Never run `db push` or `migrate dev` against
      staging or prod databases" was always TRUE and was never the bug; the bug
      was :43 prescribing migrate dev LOCALLY. But once §3 opens by saying
      migrate dev is broken locally, that line's staging/prod-only scope starts
      to read as implying local is fine — the exact inversion of the warning. It
      now ends "— and `migrate dev` is not available locally either, see above."
      Worked example cited in the doc and verified present in prisma/migrations/
      before citing: 20260729124105_build2_user_default_store and
      20260729145504_build2_staff_one_primary_store (BUILD-2, 2026-07-29). The
      future note is half a sentence: once the baseline squash lands and
      SHADOW_DATABASE_URL is set, steps 2–5 collapse back to migrate dev —
      not today. Verified this session that .env still has no
      SHADOW_DATABASE_URL (key names only; docs-only session, no DB access).
      Fixed together with DEBT-25 in one commit: same file, same section-level
      staleness, and splitting them would have meant two builds of one doc.
      ── ORIGINAL TEXT BELOW, UNCHANGED ──
      docs/WORKFLOW.md §3 still instructs `npx prisma migrate dev` for schema
      changes, but that flow is retired: migrate dev is broken (baseline squash
      never done, P3018) and the current policy is the hand-authored
      `migrate diff` flow in CLAUDE.md / MIGRATIONS.md. Recorded 2026-07-24;
      fix the doc in a future docs session — do not fix inline.

  - id: DEBT-4
    status: shipped
    title: "Local .env DATABASE_URL points at the PRODUCTION Neon branch"
    notes: >
      REACHED MAIN 2026-08-01 in promotion 97ed309. Status flipped staging -> shipped 2026-08-01 by DOCS-2. Debt rows carry no
      `shipped:` field (DebtItem in src/lib/roadmap.ts has none, and adding
      one type-errors the build), so the promotion date lives here.
      CLOSED 2026-08-01 by Gary's confirmation, delivered in the DEBT-SWEEP task
      order. The row's own notes had ended "Gary to confirm and close" since
      2026-07-27; every closing condition was already recorded as met.
      THE TITLE IS WRONG AND HAS BEEN SINCE 2026-07-27 — read it as the
      historical filing, not the state. Local `.env` has not pointed at
      production for months. Re-verified 2026-08-01, by hostname only, no value
      read and no connection opened: DATABASE_URL resolves to
      ep-late-water-a6k53nv2-pooler and DATABASE_URL_UNPOOLED to
      ep-late-water-a6k53nv2 — the dev branch (br-broad-wave-a6vpjdw0), which is
      neither production (ep-green-smoke) nor staging (ep-odd-rain). That was
      the last checkable fact on the row and it checks out.
      NO `commits` FIELD, DELIBERATELY — same shape as L-1, and read the absence
      as accurate rather than as an omission. This row closed on VERIFICATION,
      not on code: no commit in this sweep or before it changed anything for
      DEBT-4. The `.env` repoint was a local edit to an untracked file and the
      build-script half shipped under BUILD-1 (06b1561), which is its own row.
      Citing a SHA here would assert work that does not exist.
      THE `vercel env pull` GAP IS NOT CLOSED BY THIS AND CANNOT BE CLOSED BY
      CONFIGURATION. `DATABASE_URL_UNPOOLED` is integration-managed and Vercel
      rejects the Sensitive type change on it, so there is no platform setting
      behind the rule. The mitigation is PROCEDURAL and stays live reading:
      CLAUDE.md § Environment Variables (never run `vercel env pull` in this
      repo; production reads go through the Neon console; no deployed credential
      is written to disk) and DECISIONS.md 2026-07-28 for the reasoning. Because
      the control is a convention rather than an enforced boundary, it is worth
      re-stating at review time rather than assumed to hold.
      THE STRUCTURAL-GUARD QUESTION MOVES TO DEBT-34, which is the successor row
      and is OPEN. That row was filed 2026-08-01 after a production connection
      string was found sitting in froot/.env.backup — the exact artefact this
      convention exists to prevent, three days after the ruling landed. Its fix
      shape is the guard DEBT-4 only ever gestured at: a pre-commit or
      session-start check that greps the working tree for a non-dev Neon
      endpoint and fails loudly. Anyone reading DEBT-4 for the live version of
      "can this still happen?" should read DEBT-34 instead.
      ── ORIGINAL TEXT BELOW, UNCHANGED ──
      Found during SQ-1 (2026-07-26). `ep-green-smoke-a6xthq4r` is the
      PRODUCTION branch — it is the host in Vercel's Production scope; staging
      is `ep-odd-rain-a6gr4xmm`. The repo's local `.env` DATABASE_URL resolves
      to ep-green-smoke, and `npm run build` runs `prisma migrate deploy`, so a
      local build applies pending migrations to production outside any
      promotion — the exact bypass the migration policy in CLAUDE.md /
      MIGRATIONS.md exists to prevent. It also means the local dev server reads
      and writes live Keva Juice data. Not fixed in SQ-1 (audit only). Fix
      shape: point local `.env` at a dev/staging branch and keep production
      credentials out of the working tree; consider guarding the build script
      against a production host. Related: BUG-3 (DATABASE_URL_UNPOOLED),
      DEBT-3 (WORKFLOW.md §3 still documents the retired migrate dev flow).
      See also DEBT-5, DEBT-6.
      BUILD-1 (2026-07-26) addresses the build-script half: migrate deploy now
      lives in `vercel-build`, so a local `npm run build` no longer touches a
      database at all.
      .ENV HALF NOW APPEARS DONE (observed 2026-07-27, not yet ruled closed by
      Gary): local DATABASE_URL no longer resolves to ep-green-smoke. Both keys
      point at `ep-late-water-a6k53nv2` (pooled for DATABASE_URL, the same host
      without `-pooler` for DATABASE_URL_UNPOOLED) — an endpoint that is neither
      production (`ep-green-smoke`) nor staging (`ep-odd-rain`), i.e. the
      dedicated dev branch BUILD-1 recommended. The prior text saying this was
      "NOT done yet" is stale. Left OPEN rather than closed because the closing
      condition was both halves true AND BUILD-1 in production.
      STALE REFERENCE CORRECTED 2026-07-27 (reconcile Pass 2c) — this row went
      on to say "BUILD-1 is still `in_progress` and staging-only". That is no
      longer true: BUILD-1 flipped to `shipped` on 2026-07-27, promoted in
      06b1561, and its vercel-build split was verified IN PRODUCTION (deployment
      EHfDfAKJR logged `Running "npm run vercel-build"` and `31 migrations
      found`). So the second half of DEBT-4's closing condition is now MET.
      STILL LEFT OPEN, deliberately and narrowly: the 2026-07-27 session
      verified the local env vars by NAME AND HOST ONLY. It did not confirm the
      dev branch's contents, nor that no tooling still reaches production by
      another path — and the local→dev repoint has since been shown to create
      its own hazard (see BUILD-2's blocker: a local pre-check now reads
      ep-late-water and returns a false all-clear). Gary to confirm and close.
      "ANOTHER PATH" FOUND, 2026-07-28 — and it CANNOT be closed with a platform
      setting. The PERM-7 pre-flight audit reached the production Neon branch
      from this machine via `npx vercel env pull --environment=production`. The
      guard that was assumed to prevent this does not: `DATABASE_URL` is marked
      Sensitive and pulls as the literal `[SENSITIVE]`, but its sibling
      `DATABASE_URL_UNPOOLED` is NOT Sensitive and pulls a live production
      connection string. So the Sensitive marking on `DATABASE_URL` is not the
      boundary it appears to be, and the memory note claiming "you cannot read
      the prod Neon DB from this machine via pulled env" was simply wrong.
      WHY THE OBVIOUS FIX IS UNAVAILABLE: `DATABASE_URL_UNPOOLED` is
      INTEGRATION-MANAGED by the Neon integration, not a hand-created project
      var, and Vercel REJECTS the type change on it. Marking it Sensitive is
      therefore not an option — the platform will not allow it.
      THE MITIGATION IS PROCEDURAL, NOT A SETTING (Gary, 2026-07-28): a standing
      rule in CLAUDE.md § Environment Variables — never run `vercel env pull` in
      this repo; production reads go through the Neon console; no production
      credential is written to disk, including a scratchpad file intended to be
      deleted. Recorded in DECISIONS.md 2026-07-28 with the reasoning. Because
      the control is a convention rather than an enforced boundary, it is worth
      re-stating at review time rather than assumed to hold.
      This does NOT close DEBT-4 — it converts one of its open questions into a
      known, documented, unfixable-by-configuration gap.
      ROW TITLE IS NOW STALE (noted 2026-07-29, not edited). The title still
      reads "points at the PRODUCTION Neon branch"; the notes above already
      record the repoint to dev and end "Gary to confirm and close." Verified
      2026-07-29: local .env DATABASE_URL and DATABASE_URL_UNPOOLED both resolve
      to ep-late-water-a6k53nv2 (dev). The title is the part a reader skimming
      the roadmap sees first, and it now says the opposite of the truth. Left for
      the close-out that resolves this row rather than edited in passing.

  - id: DEBT-5
    status: shipped
    commits: ["838ad99"]
    title: "10 of 12 stores have storeNumber: null, degrading PERM-3's deterministic store sort"
    notes: >
      REACHED MAIN 2026-08-01 in promotion 63407be. Status flipped staging -> shipped 2026-08-01 by DOCS-2. Debt rows carry no
      `shipped:` field (DebtItem in src/lib/roadmap.ts has none, and adding
      one type-errors the build), so the promotion date lives here.
      CLOSED 2026-07-30 in 838ad99 — THE LABEL HALF ONLY. Both /users chips now
      render `#0014 — Las Brisas` instead of a bare `#0014`, using the
      prefix-ternary form `{s.storeNumber ? `#${s.storeNumber} — ` : ""}{s.name}`.
      The number-absent branch is untouched and still renders name-only, so ten
      of the twelve stores are unchanged by a single character.
      THE BACKFILL HALF REMAINS OPEN AND NOW HAS ITS OWN ROW: DEBT-27. It did
      not have one before — this row's notes were the only place in the file
      that mentioned backfilling storeNumber, so closing this row would have
      dropped the data half off the open list with nothing tracking it. DEBT-4
      was considered as its home and REJECTED: DEBT-4 rules on local `.env`
      hosts and `vercel env pull`, and says nothing about this backfill. The
      gate named in the original text below — the migration policy in
      CLAUDE.md / MIGRATIONS.md, not BUILD-1 — moves to DEBT-27 with it. The
      PERM-3 sort degradation this row is TITLED for is part of that half and is
      NOT fixed here.
      ROW TITLE IS THEREFORE STALE ON CLOSE, deliberately, same shape as DEBT-4's
      (see that row, "ROW TITLE IS NOW STALE"). The title describes the null
      storeNumbers and the sort; what closed is the label. A reader skimming the
      resolved list will see a title whose subject is still open at DEBT-27. Left
      rather than rewritten because the original wording is preserved by
      convention below the marker; DEBT-27 carries the live version.
      THE EIGHT PRECEDENTS WERE NOT UNANIMOUS — the session stopped and Gary
      ruled. Six use EM DASH U+2014 (user-actions.tsx:112,244,271;
      template-form.tsx:995; training-form.tsx:813;
      create-device-login-button.tsx:103) and two use HYPHEN-MINUS U+002D
      (staff-buttons.tsx:116; staff-edit-actions.tsx:201). Four further sites
      outside the audited eight were found — staff/page.tsx:127,
      stores/page.tsx:162, staff/[id]/page.tsx:623 all hyphen, and
      store-view-client.tsx:100 inverted to `Carson (#0034)` — making the
      repo-wide split 6 em dash / 5 hyphen / 1 parenthesised. RULING (Gary,
      2026-07-30): match user-actions.tsx exactly, em dash. Site :301 is now
      byte-identical to user-actions.tsx:112. The inconsistency itself is logged
      as DEBT-28; none of those sites were touched here.
      NOTE THE ROW'S OWN SUGGESTION WAS NOT TAKEN: the original text below
      proposes `#0014 · Las Brisas` with a MIDDLE DOT U+00B7. That character
      appears nowhere in the codebase in this idiom. The em dash form did, eight
      times, which is why it won.
      REAL FILE:LINE, the row's citation having drifted: the chips are at
      src/app/(app)/users/page.tsx:252 (members table, `a.store.storeNumber`)
      and :301 (pending invites, `s.storeNumber`). The original text below cites
      :199 and describes ONE site; there are TWO, with different data shapes.
      Both were re-verified against the checkout before editing. Note :252 and
      :301 are themselves now stale by construction — the edit is on those lines.
      NO DATA PLUMBING WAS NEEDED, as predicted: storeNumber and name are both
      already in scope at both sites (page.tsx:34,104 include the store on
      storeAssignments; :162 maps storeIds to whole store rows). Label strings
      only. No database was read at any point this session — the fix renders
      correctly whatever storeNumber holds, so none was required.
      LAYOUT VERIFIED, NO CAP ADDED. Both chip containers are `flex flex-wrap
      gap-1` (page.tsx:249, :298) with no fixed width, max-width, truncate or
      whitespace-nowrap on the chip spans, inside a `w-full` auto-layout table in
      an `overflow-x-auto` wrapper — so longer labels wrap, they do not squash.
      The members table caps at 5 chips with `+N more` (:250, :255); the pending
      invites list is UNCAPPED (:299). Judged fine today on arithmetic: only two
      stores carry a number, so exactly two chips lengthen. The bulk arrives with
      DEBT-27's backfill, when all twelve lengthen at once and the uncapped list
      is where it shows — that is the moment to reconsider a cap, as a decision,
      not now as a silent behaviour change.
      ── ORIGINAL TEXT BELOW, UNCHANGED ──
      Logged 2026-07-27. Only Carson #0034 and Las Brisas #0014 carry a
      storeNumber; the other ten are null. PERM-3's no-storeId fallback
      (`forecastWindowForCaller`, used by /api/forecasting/audit, whose storeId
      is optional) sorts the caller's assigned stores by storeNumber THEN name,
      so with ten nulls it degrades to a name-only sort. Harmless today —
      name-only is still deterministic, and the sort only decides which store's
      timezone computes the window, where every store is currently in one
      timezone. It stops being harmless the moment stores span timezones, since
      the window boundary would then turn on a tiebreak nobody chose.
      The real fix is BUILD-2; backfilling storeNumber is the cheaper interim
      and is a DATA change, not a code change — so it is gated on the same
      migration policy, not on BUILD-1. (BUILD-2's shape was rescoped
      2026-07-27 to User.defaultStoreId; the isPrimary framing this row
      originally referenced is retired.) Related: BUILD-2, PERM-3's `deferred:`
      entry.
      "HARMLESS TODAY" IS WRONG — CORRECTED 2026-07-27. This row was logged as a
      latent sort-order issue. It has a live, user-visible consequence that cost
      Gary a real double-take. /users renders each store chip as
      `{a.store.storeNumber ? `#${a.store.storeNumber}` : a.store.name}`
      (src/app/(app)/users/page.tsx:199) — store number IF PRESENT, otherwise
      the name, never both. Because only two stores have a storeNumber, the
      Location Access column labels those two BY NUMBER and the other ten BY
      NAME, inconsistently, in the same table. So Las Brisas — one of the only
      stores with a number — displays as a bare "#0014" with its name nowhere on
      screen, which made its device login look absent when it exists (Tommy
      Thomas, corporate@keva.com, role STORE). Fix the label to carry both
      (`#0014 · Las Brisas`) or drop the number preference; that is independent
      of the backfill and of BUILD-2, and it is the cheaper of the two.

  - id: DEBT-6
    status: shipped
    commits: ["9508d4c"]
    title: "Square sync fires from ~11 GET routes — a write path that greps as read-only"
    notes: >
      REACHED MAIN 2026-08-01 in promotion 97ed309. Status flipped staging -> shipped 2026-08-01 by DOCS-2. Debt rows carry no
      `shipped:` field (DebtItem in src/lib/roadmap.ts has none, and adding
      one type-errors the build), so the promotion date lives here.
      CLOSED 2026-08-01 in 9508d4c — BY RELOCATION, not by a code change. This
      row was always a method warning rather than a defect, and a warning filed
      as debt is a warning nobody reads at the moment it applies. Its content
      now lives in docs/PERMISSIONS_INVENTORY.md, under its own "Audit method"
      heading immediately after the enforcement-type list — i.e. in front of the
      person actually conducting the audit it is about. Nothing in the codebase
      changed; the GET routes still sync, deliberately.
      THE COUNT WAS WRONG AND THE ROW'S OWN REMEDY WAS INSUFFICIENT — both
      re-verified at HEAD before writing the note. It is 13 GET handlers, not
      ~11. Worse, grepping `syncSalesForStore` / `ensureSalesCached` by name —
      which is the entirety of the fix this row prescribed — finds only 8 of the
      13. The other five (inventory/order-guide, /expected, /alerts,
      /alerts/count, /pars) never mention a sync helper: they call
      computeExpectedInventory / computeWeeklyUsage / computeLowStockAlerts in
      src/lib/expected-inventory.ts, which calls ensureSalesCached on their
      behalf. So the row diagnosed a blind spot and then prescribed a remedy
      with the same blind spot one layer down.
      THAT IS THE DURABLE PART, and it is what the inventory note leads with: a
      by-name grep is defeated by exactly one level of indirection, so the
      question has to be asked again of any new computation helper. The relocated
      note names both layers and both greps. Three further call sites are
      non-GET and are not the concern: square/sales/sync, forecasting/backfill,
      webhooks/square.
      STILL NOT A PERMISSION HOLE, restated so the move does not change how the
      finding reads: the routes are gated and the cache is org-scoped.
      ── ORIGINAL TEXT BELOW, UNCHANGED ──
      Logged 2026-07-27. `syncSalesForStore` / `ensureSalesCached` are invoked
      from roughly 11 GET routes — dashboard/summary, dashboard/rollup,
      dashboard/sales, and four inventory reports among them. Any org member's
      READ therefore triggers outbound Square API calls and SalesPeriodCache
      writes.
      Pre-existing, not goal data, and NOT a permission hole — the routes are
      gated and the cache is org-scoped. It is recorded for what it does to
      AUDIT METHOD: a write-path audit that greps only for non-GET handlers has
      a blind spot exactly here and would report these routes as read-only. Any
      future permissions or data-exposure audit should grep for the sync helpers
      BY NAME, not just for POST/PATCH/PUT/DELETE.
      Secondary consequence, relevant when SQ-3 is scoped: because these GETs
      reach getSquareClient, ordinary page loads are what currently triggers the
      lazy token refresh. That makes SQ-3's backstop cron insurance against a
      quiet week rather than a redundant addition.

  - id: DEBT-7
    status: shipped
    commits: ["705584f"]
    title: "docs/STAGING_SETUP.md describes an environment that was never built — and now gives dangerous advice"
    notes: >
      REACHED MAIN 2026-08-01 in promotion 63407be. Status flipped staging -> shipped 2026-08-01 by DOCS-2. Debt rows carry no
      `shipped:` field (DebtItem in src/lib/roadmap.ts has none, and adding
      one type-errors the build), so the promotion date lives here.
      CLOSED 2026-07-30 in 705584f, via the header option — the cheap one this
      row recommended. The file was NOT rewritten to describe the environment
      that exists: that is a bigger job, and a half-corrected file reads as
      authoritative (Gary's ruling, this session). What landed is a header above
      the H1 marking the file ASPIRATIONAL/HISTORICAL, naming all four
      falsehoods in one line each, and pointing at this row and BUILD-1 for the
      real state — plus FOUR inline warnings, not the two originally scoped.
      THE TWO EXTRA WARNINGS, and why the scope moved: the decided shape was
      header + warnings at §2 step 4 and §5's DATABASE_URL row (the dangerous
      pair in (2) below). Two more were added on the money side of (3), on the
      argument that a false safety guarantee is most dangerous to the reader who
      skimmed the header. §4's "no real charges can happen from staging even by
      accident" (:48 pre-edit) is the false promise, and §8's checkbox "confirm
      a test 'charge' on staging hits Square's sandbox, not live" (:86 pre-edit)
      is an INSTRUCTION TO ACT ON IT — against, per (3), a real Square account.
      The imperative is the one that gets acted upon; marking the promise while
      leaving the instruction unmarked would have been the wrong half.
      §5's warning is TWO-PART deliberately: a blockquote cannot live inside a
      markdown table cell, and a reader scanning the env-var table with the
      Vercel console open in the other window reads neither the prose above nor
      below it. So the cell itself now carries "FALSE, see warning below the
      table" and the full warning sits under the table.
      NOT CLOSED BY THIS: §8 remains unpassable as written, by design — that is
      falsehood (4) and fixing it means describing the real environment. The
      file is now safe to read, not correct. A future session may still take the
      rewrite; this one deliberately did not.
      Line references in the original text below were re-verified against the
      checkout before editing and had NOT drifted (:26 and :56 exact). They are
      now stale by construction — the edit moved them.
      ── ORIGINAL TEXT BELOW, UNCHANGED ──
      Logged 2026-07-27. Not merely stale: one of its instructions would now
      REVERSE the fail-closed fix recorded on BUILD-1.
      (1) §2 claims the Neon-Vercel integration auto-creates an isolated
      database branch per Preview deployment. It does not, and never did here —
      Neon's own console reports "You don't have any preview branches yet."
      Staging is served by one long-lived branch, ep-odd-rain, wired by hand.
      (2) §2 step 4 instructs the reader to "remove any manually-set
      DATABASE_URL under Preview scope so there's no conflict", and §5's table
      lists Preview DATABASE_URL as "auto-injected by Neon integration". This is
      the DANGEROUS line. Following it today would delete the branch-scoped
      staging override — the very row that makes staging deploys hit ep-odd-rain
      instead of failing — and it directly contradicts BUILD-1's deferred step 2,
      which says to ADD Preview-scoped vars when collaboration work starts.
      A reader trusting this file would undo the 2026-07-27 fix.
      (3) §4 and §5 say staging uses Square SANDBOX
      (`SQUARE_ENVIRONMENT=sandbox`, sandbox app credentials). Staging actually
      connects to a separate "Froot Staging" PRODUCTION app against the real
      Square account — which is why staging token refreshes act on real tokens.
      The file's promise that "no real charges can happen from staging even by
      accident" is therefore not something this environment guarantees.
      (4) Consequently §8's verification checklist can never pass as written.
      FIX SHAPE — pick one, do not leave it as is: either correct the file to
      describe the environment that exists, or add a prominent header marking it
      ASPIRATIONAL/HISTORICAL (the plan as drafted, not the system as built).
      The header is the cheap option and removes the danger in (2) immediately.
      Related: DEBT-3 (the other doc contradicting current policy), BUILD-1,
      SEC-4 (logged from this same file's housekeeping note).

  - id: DEBT-8
    # FIRST debt row ever closed, so this field is new here — the vocabulary is
    # borrowed from the phase rows (staging -> shipped). The internal roadmap UI
    # does NOT read it yet and still lists every debt item under "Known debt —
    # recorded, not yet fixed"; see DEBT-14.
    status: shipped
    commits: [6530d8b]
    title: "Square import discards business_email — Store.contactEmail exists but is never populated"
    notes: >
      REACHED MAIN 2026-07-29 in promotion 746c1be. Status flipped staging -> shipped 2026-08-01 by DOCS-2. Debt rows carry no
      `shipped:` field (DebtItem in src/lib/roadmap.ts has none, and adding
      one type-errors the build), so the promotion date lives here.
      CLOSED 2026-07-28 as PERM-7 Task 0, committed separately (Ruling 1).
      Logged 2026-07-27. Every piece of this already exists; two lines connect
      them, and neither was written.
      `Store.contactEmail` is in the schema (prisma/schema.prisma:118, nullable)
      and is already editable by hand in both the Add Store form
      (src/app/(app)/stores/add-store-button.tsx:83) and the Edit Store dialog
      (src/app/(app)/stores/store-actions.tsx:203). GET /api/square/locations
      spreads the ENTIRE Square location object (`...loc`), so `business_email`
      reaches the client on every import. But the import dialog's POST body maps
      only name, address, city, state, timezone, phoneNumber and
      squareLocationId (src/app/(app)/stores/import-square-button.tsx:58-66) —
      the email is silently dropped.
      CORRECTION 2026-07-28 — THE SECOND HALF OF THAT SENTENCE WAS WRONG WHEN
      WRITTEN, and the original prompt inherited it as Task 0 step 2. The row
      claimed "the store CARD then renders address, phone and timezone but never
      the email, so the field is invisible even when set." The card HAS rendered
      contactEmail, behind a Mail icon, since the initial commit:
      src/app/(app)/stores/page.tsx:182-187, dated to 1cfdf76 (2026-06-26) by
      `git log -L 182,187`. This was not PERM-6 drift and not a regression —
      the claim was simply never checked against the file. There was nothing to
      build for that step; it was already shipped. Recorded rather than quietly
      dropped, because a false "it's invisible" is what made the field look
      more broken than it was.
      CHANGED UNDER YOU 2026-07-27 by PERM-6 Task 4 — read this before
      starting. GET /api/square/locations no longer spreads the whole Square
      object; it now returns an explicit allow-list (id, name, address
      {address_line_1, locality, administrative_district_level_1},
      phone_number, timezone, alreadyImported), verified against both callers
      as the fields actually consumed. `business_email` therefore NO LONGER
      REACHES THE CLIENT. This is not a regression to report — it was dropped
      knowingly, because nothing read it and the route is the org's Square data
      going over the wire. DEBT-8's first step is to add `business_email` back
      to that allow-list, one line in src/app/api/square/locations/route.ts,
      at the point a consumer actually exists. Gary's call, 2026-07-27:
      widening later is easy, narrowing after something depends on it is not.
      THE MAPPING ALONE POPULATES NOTHING — found 2026-07-28 against the
      PRODUCTION database, and it is why this row grew a third fix. Store
      .contactEmail is null on 10 of 10 production stores. The import dialog is
      the ONLY writer of these fields and it filters out already-imported
      locations (`available = locations.filter((l) => !l.alreadyImported)`),
      and there was no store-level "Resync from Square" the way there is a
      per-member one for staff. So all 9 Square-linked production stores were
      permanently unreachable by the import mapping, and PERM-7(d)'s email seed
      would have come up empty for every existing store INCLUDING Las Brisas —
      the exact store the phase was designed around.
      FIX, as landed (three parts, not two):
      (a) re-add business_email to the allow-list —
      src/app/api/square/locations/route.ts:63-83;
      (b) map business_email → contactEmail at import —
      src/app/(app)/stores/import-square-button.tsx:58-72;
      (c) NEW per-store "Resync from Square" so already-imported stores have a
      path at all — src/app/api/stores/[id]/resync-square/route.ts (new) +
      src/lib/square.ts fetchSquareLocation + the button in
      src/app/(app)/stores/store-actions.tsx. Gary's Ruling 3, 2026-07-28: a
      one-shot backfill script fixes today and nothing afterwards — the same gap
      reopens for the next store whose Square email changes, with no mechanism
      to notice. The button also gives PERM-7(d)'s drift indicator a remedy:
      see drift, click resync. Scoped to exactly the field set the import maps,
      so import and resync cannot drift apart.
      Step 2 as originally written ("surface it on the store card") required NO
      CHANGE — see the correction above.
      Independently useful, but it is also the PREREQUISITE for
      PERM-7(d), which seeds a device login's email from it — a seed cannot come
      from a column nothing populates. See DECISIONS.md 2026-07-27 for why that
      seeding is one-way and never a live sync.

  - id: DEBT-9
    title: "Two ACTIVE staff have 9 store assignments and NO primary — signed documents capture an arbitrary store"
    status: shipped
    notes: >
      CLOSED 2026-08-03 — GATE SATISFIED ON PRODUCTION. Code shipped as
      de3ba40 (Phases 1-3); flags set by Phase 4 SQL on staging and production
      2026-08-02. The gate this row carried is now discharged in full, walked
      on branch production (br-sparkling-block-a620qvg4), org
      cf888f2d-f234-48c7-8097-fd5b44b5b3dd.
      (a) /staff renders a Corporate card — "available at every location, based
      at none" — with Gary Thomas and Kelton Thomas in it and NEITHER under
      Carson. Locations collapsed to one non-clickable "All 11 locations" chip.
      (c) /staff/[id] shows the read-only Corporate badge and NEITHER store
      warning fires.
      (d) THE HALF THAT MATTERED, and it was walked SELF-SERVE, not attested —
      see the note at acknowledgments/route.ts, which records why an attested
      walk could not have evidenced it. Signed end to end as Gary Thomas,
      corporate, 11 assignments, 0 primaries. The consent step showed a STATIC
      "Store: Corporate" line with NO picker. Result:
      HrDocumentAcknowledgment.storeName = "Corporate" on all four rows
      (cmsdmc179000304jg2q3jh5iv + three siblings, signedAt 2026-08-03
      19:23:04-19:24:48), and the rendered Certificate of Acknowledgment reads
      "Store: Corporate". THAT IS THE FIRST OBSERVATION OF WHAT A DOCUMENT
      ACTUALLY STAMPS — every prior phase verified the resolver and said so.
      NON-CORPORATE CONTROL, same walk: Aaliyah Rose, 3 assignments, Meadowood
      Mall primary, manager-attested — certificate reads "Store: Meadowood
      Mall". The corporate branch did not disturb the normal path.
      THE CONDITION REGENERATED DURING THE WALK, which is the best argument
      this row ever produced for why it was not fixed by hand: a Square resync
      ran unprompted between two page loads and expanded both members from 9
      assignments to 11 (Cafe De Keva Cart, Keva Kiosk). isCorporate survived
      untouched — it lives on StaffMember, which no sync path writes. A
      hand-set primary would have been silently wrong about two new stores
      nobody assigned.
      SIDE OBSERVATION, recorded because it contradicts a live blocker
      elsewhere: the certificate header on PRODUCTION reads "Generated by Froot
      for Keva Juice", NOT "Microsoft". The HR-14 org-name issue does not
      reproduce on production. Recorded on HR-14 itself, deliberately fenced —
      it shows Organization.name is right on that row today, and says nothing
      about the rename handler's unwritten slug or the P2025 hazard.
      TWO FOLLOW-UP FIXES CAME OUT OF THE WALK and landed as 16d1006. They did
      NOT close this row — de3ba40 did — and are recorded here only so the pair
      is findable from either end. (1) api/staff/[id]/invite returned Clerk's
      rejection as the bare word "Forbidden" with no attribution, reading as a
      Froot authorization failure; root cause was Clerk's per-organization
      MEMBERSHIP LIMIT with the org at its cap, and the setting is the default
      for NEW orgs so raising it does not retroactively lift an existing one.
      (2) a comment at acknowledgments/route.ts recording that an ATTESTED walk
      cannot test the corporate guard, because isAttested short-circuits the
      disjunct — which is exactly why (d) above had to be walked self-serve.
      WHAT THIS ROW DID NOT FIX, so nobody reads the closure as wider than it
      is: DEBT-49 (no admin control sets isCorporate — it is SQL-only) and
      DEBT-50 (no in-app org switcher / duplicate Clerk identities) were both
      filed by this work and remain open.
      ────────────────────────────────────────────────────────────────
      ORIGINAL TEXT BELOW, unedited.
      COST OF DOING NOTHING: Production has zero signed documents for either
      zero-primary owner, so nothing is wrong today, and the first document
      either signs freezes an alphabetical accident into a legal record —
      LATENT (trigger: the first HR signature by a staff member with no primary
      store; four such staff on preview/staging, two on production. A second
      trigger is quieter — adding a store to a single-assignment staff member
      makes them ambiguous instantly, and the partial index does not catch it
      because zero primaries stay legal).
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's.
      RECORD HALF RELOCATED 2026-08-02 by DEBT-TRIAGE-2 → docs/MIGRATIONS.md
      § Protected indexes, "Hazard 3 — the index guarantees AT MOST one primary,
      not AT LEAST one", beside the existing Hazard 2. Nothing is deleted here:
      the record text stays in this row as the history and lands there as the
      place it fires, on the reader of the constraint. THIS ROW KEEPS ITS TASK —
      set a primary for Gary Thomas and Kelton Thomas via the chip-click path on
      /staff, both environments, Gary's own schedule.
      EVIDENCE REPAIRED 2026-08-02 by DEBT-TRIAGE-1. These are POINTERS, so they
      are repaired rather than left as dated claims; the original citations are
      preserved below and each is named here. Re-measured at HEAD cc1fffc.
      THE WORST ONE — THIS ROW QUOTES CODE THAT NO LONGER EXISTS. "WHY IT
      MATTERS" below cites src/lib/hr.ts:44-49 and quotes
      `.find(a => a.isPrimary) ?? staff.storeAssignments[0]`. THAT LINE IS NOT
      IN THE FILE. f480568 ("BUILD-2 Task 8: primaryStoreName() orders
      internally instead of trusting callers") rewrote the function to sort
      internally: it is now `const [best] = [...staff.storeAssignments].sort(...)`
      returning `best?.store.name ?? null`, and there is no `.find` in it at
      all. CORRECT CITATION: src/lib/hr.ts:65-73 — measured here, not inherited;
      the triage audit reported :65-71 and the function closes at :73. A row
      quoting absent code reads as authoritative, which is why this one is
      called out first.
      THE FUNCTION ALREADY KNOWS ABOUT THIS ROW, and the row did not know about
      the function. The comment block immediately above it (src/lib/hr.ts:56-64)
      says in terms: "this does NOT fix DEBT-9: a staff member with no primary
      still resolves to an arbitrary-but-stable store. It makes that stability a
      guarantee here rather than a coincidence." So the substance of this row is
      untouched by the rewrite — only its evidence went stale.
      "DO THIS BEFORE BUILD-2'S INDEX LANDS" IS STALE. The index landed
      2026-07-29 in prisma/migrations/20260729145504_build2_staff_one_primary_store.
      The instruction is moot and, per the REFUTED paragraph below, was never
      load-bearing anyway: a partial unique index on WHERE "isPrimary" indexes
      only TRUE rows, so zero-primary staff contribute no index entries and
      there is no ordering dependency in either direction. The chip-clicks can
      happen whenever.
      STILL EXACT AT HEAD, checked rather than assumed: the three signed-PDF
      citations src/lib/hr-signed-pdf.ts:426, :615 and :670 all resolve to the
      lines this row describes. Not every citation here drifted.
      NOTHING ELSE IN THIS ROW CHANGES — the finding, the urgency and the fix
      all stand exactly as written below.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      CORRECTED 2026-07-29 by the BUILD-2 audit. This row says
      primaryStoreName() "takes THE FIRST ROW OF AN UNORDERED QUERY — an
      arbitrary one of NINE stores, potentially a different one per request."
      The "unordered" and "different per request" halves are FALSE: both callers
      order by [{ isPrimary: "desc" }, { store: { name: "asc" } }] (see the
      correction on BUILD-2 for the file:line list), so with zero primaries the
      fallback resolves to the ALPHABETICALLY-FIRST store, deterministically,
      every request. Gary Thomas and Kelton Thomas would both stamp "Carson".
      WHAT SURVIVES, AND IT IS STILL WORTH FIXING: stable-and-arbitrary is not
      correct. "Carson" is an alphabetical accident, not a decision, and the
      first document either owner signs freezes it into a legal record. The
      urgency is unchanged; only the flapping is withdrawn.
      ALSO REFUTED — GARY'S OWN INSTRUCTION. The reason given for setting
      primaries BEFORE BUILD-2's index ("so the constraint locks in real intent
      rather than whatever row order won") does not hold. A partial unique index
      on WHERE "isPrimary" indexes only rows where the flag is TRUE, so a staff
      member with ZERO primaries contributes zero index entries and cannot
      collide. There is no ordering dependency in either direction: the
      chip-clicks may precede or follow the migration. Accepted by Gary
      2026-07-29.
      BRANCH staging IS DIFFERENT DATA, checked 2026-07-29: four ACTIVE staff
      with no primary — Aaliyah Rose (1: Carson), Chase Nyman (2: Carson, Las
      Brisas), Gary Thomas (1: South Reno), Kelton Thomas (3: Carson, Las
      Brisas, Spanish Springs). The single-assignment rows resolve correctly
      today via the fallback (one candidate, no ambiguity) so they are tidiness;
      Chase and Kelton are the real ones. TRIGGER, not "optional": a
      single-store row becomes ambiguous the instant a second store is added,
      silently, and the index does NOT catch it because zero primaries stay
      legal. Chip-clicks are Gary's, both environments, own schedule.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      Found 2026-07-27 by the BUILD-2 production pre-check (Query B), run in the
      Neon console against branch `production`. Gary Thomas and Kelton Thomas,
      both ACTIVE, each with 9 StoreStaffAssignment rows and ZERO isPrimary.
      NOT a migration blocker — a partial unique index permits zero primaries,
      and BUILD-2's Query A returned clean. A data-quality problem sitting next
      to that one, not the gate.
      WHY IT MATTERS: primaryStoreName() (src/lib/hr.ts:44-49) is
      `.find(a => a.isPrimary) ?? staff.storeAssignments[0]`. With no primary,
      `.find` returns undefined and the fallback takes THE FIRST ROW OF AN
      UNORDERED QUERY — an arbitrary one of NINE stores, potentially a different
      one per request. That value is frozen at signing time onto
      HrDocumentAcknowledgment.storeName and FormSubmission.storeName, then
      stamped into the signed PDF (src/lib/hr-signed-pdf.ts:615, :426, :670).
      Worse than the duplicate case BUILD-2 guards against on two counts: nine
      candidates rather than two, and these are the OWNER accounts — the people
      most likely to sign or countersign.
      FIX (one minute, no code, no migration): set a primary for each via the
      chip-click path on /staff, which PATCHes /api/staff/[id] with
      primaryStoreId only. Do this BEFORE BUILD-2's index lands, so the
      constraint locks in real intent rather than whatever row order won.
      ANSWERED 2026-07-27 — THEORETICAL, NOT LIVE. Query C (preserved in full
      with its result in docs/prompts/BUILD-2_default_store.md Task 1a) returned
      ZERO ROWS against branch `production`, 116ms. Neither Gary Thomas nor
      Kelton Thomas has signed anything — no HrDocumentAcknowledgment rows, no
      FormSubmission rows. So NO executed document carries an arbitrarily-picked
      store name and there is NOTHING HISTORICAL TO CORRECT. The question of
      whether to rewrite signing snapshots, which would have been Gary's call,
      does not arise.
      That makes this purely forward-looking and cheap: set both primaries
      before either account signs anything, and before BUILD-2's index lands.
      Urgency is low; the cost of leaving it is that the first document either
      owner signs freezes an arbitrary one of nine stores into a legal record.
      ────────────────────────────────────────────────────────────────
      PHASE 4 IS DONE ON BOTH ENVIRONMENTS — staging 2026-08-02
      (br-square-feather-a63z92vz), production 2026-08-02
      (br-sparkling-block-a620qvg4), two rows each, org
      cf888f2d-f234-48c7-8097-fd5b44b5b3dd. Gary Thomas
      (cmqxfyiwy000004l49ps3w1tf) and Kelton Thomas
      (cmqxfyjt1000004jtbfzj9jmz) are isCorporate on production, which is the
      only branch that ever carried the condition — 9 assignments and 0
      primaries each there, against 1 and 3 on staging. Code promoted as
      de3ba40. What remains is THIS GATE, nothing else.
      GATE ON THE PHASE 4 PROMOTION — added 2026-08-02 at the close of Phase 3
      (Gary's ruling). THIS IS A GATE, NOT A NICE-TO-HAVE. Phases 1-3 shipped
      code; the fix is not live until the flags are set on production, and
      three of the four Phase 3 items carry NO RENDERED EVIDENCE:
        (a) the /staff Corporate card and its "All N locations" chip IN PAGE
            CONTEXT — the chip component itself IS evidenced, see below;
        (c) the /staff/[id] no-primary warning and the read-only Corporate
            badge;
        (d) the acknowledgments-route server guard, and WHAT A SIGNED DOCUMENT
            ACTUALLY STAMPS — the frozen HrDocumentAcknowledgment.storeName
            rows and the rendered PDF's Store line.
      They shipped on `next build` (types + compile), a code read, and the
      isCorporate DATA PATH exercised end to end by a harness against branch
      dev (br-broad-wave-a6vpjdw0). That is real but it is not the output.
      Phase 2 verified the RESOLVER and said so explicitly; nothing anywhere
      has yet observed what a document stamps.
      WHY IT DEFERRED, so it is not read as laziness: the dev Clerk account
      reachable in the browser is a member of exactly one org, the 0-store
      twin — the fixture, the 9 stores and the test document all live in the
      other same-named org, whose admin is a DIFFERENT Clerk account on the
      SAME email. See DEBT-50. Rather than manufacture accounts, the walk was
      moved off dev.
      THE WALK RUNS ON PRODUCTION, NOT STAGING — moved 2026-08-02 by Gary's
      ruling, after staging was tried and rejected. This paragraph previously
      said staging; it was wrong, and the reasoning matters more than the
      destination. Clerk's verified-snapper-7 instance is SHARED by local and
      staging, so the membership that blocks the walk on dev blocks it on
      staging identically — auth() hands staging the same single-membership
      orgId no matter which database is behind it. The available workaround was
      to add the browser account to org_3FhYUR4l0ue7egug1I0Ig8wxOVn in the
      Clerk dashboard, and it was REJECTED on its own merits: the
      organizationMembership.created webhook may not reach staging, which would
      leave a session that is an org member with NO User row — a new failure
      mode introduced into staging in order to chase a verification — and
      hand-inserting the User row to paper over that is worse. Do not revisit
      it on the next reader's instinct; the cost is a manufactured identity in
      a shared environment.
      PRODUCTION IS ALSO THE BETTER EVIDENCE, which is what makes this a
      genuine improvement and not a retreat. Staging does NOT reproduce the
      condition: measured on preview/staging (br-square-feather-a63z92vz)
      2026-08-02, Gary Thomas has ONE assignment there and Kelton THREE — not
      nine each. The nine-store ALL_CURRENT_AND_FUTURE_LOCATIONS shape that
      DEBT-9 exists for is a PRODUCTION condition. A staging walk would have
      exercised the corporate branch against a population that never had the
      bug.
      WHAT THE WALK MUST PRODUCE, named so it cannot be waved through: the full
      four-phase self-serve ceremony (consent → review → finalize → done) as a
      CORPORATE staff member on a real acknowledgment document; the resulting
      HrDocumentAcknowledgment.storeName rows read back and equal to
      "Corporate"; the generated PDF's Store line read back and equal to
      "Corporate"; then the same walk as a NON-corporate multi-store member
      showing the picker still appears and its selection is what gets stamped.
      Every observation names its branch AND its org id (CLAUDE.md § Database
      Evidence, § Browser Evidence).
      WHAT IS ALREADY EVIDENCED, so the walk is not re-done wholesale:
      StaffLocationChips, the one component this phase rewrote, was verified
      2026-08-02 on a temporary public /menu preview — ten assertions off
      server-rendered HTML across four cases. Corporate collapses to a single
      "All 9 locations" chip with canEdit both true and false (the corporate
      branch returns BEFORE the chip loop, so no set-primary click is reachable
      for someone whose primary is deliberately unset); the unflagged control
      with the same nine assignments renders 8 chips + "+1"; ten
      "Make X the primary store" buttons across the two control cases and zero
      from corporate. The preview page was deleted at close.
      And (b) /hr/compliance IS evidenced, before and after, on branch dev:
      ADMIN org-wide totals byte-identical (staffCount=6, requiredTotal=7,
      pct=14) with corporate dropped from By Store only (Carson 4 staff → 3,
      1/5 → 1/4, 20% → 25%); both MANAGERs lose them entirely from KPIs, By
      Store and the employee table. That half needs no re-walking.

  - id: DEBT-10
    status: verified
    commits: [2877b41]
    title: "GET /api/square/team-members has no role gate — the same hole PERM-6 closed on square/locations"
    notes: >
      VERIFIED IN PRODUCTION 2026-07-29 (Gary). Promoted to main with the
      2026-07-29 merge; 2877b41 confirmed an ancestor of main. Both halves
      exercised against the real environment, which is what moves this row from
      `staging` to `verified`:
      GATE — a STORE account (Las Brisas) received 403; an ADMIN received 200.
      That is the first live proof the capability check fires, and it exercises
      exactly the tier the row was argued down to.
      ALLOW-LIST — the 200 carried exactly the allow-listed fields and nothing
      else.
      SCALE — 138 team members in the production payload, every one carrying
      email_address. This RETIRES the row's own "NOT CHECKED: whether the
      production org has Square connected" caveat below: it is connected, the
      payload was live, and the exposure was 138 employees' email addresses
      readable by any authenticated account, not a hypothetical. Environment
      named per CLAUDE.md § Database Evidence — this is a production Square API
      payload observed through the deployed app, not a Neon branch query.
      PHONE NOTE UPGRADED — correction (1) below called the phone exposure an
      INFERENCE. That was too weak. Square's documented TeamMember object
      carries phone_number, so the field genuinely exists on the object the
      route was spreading; the spread had no filter, so it went to the client
      whenever Square populated it. What is STILL not claimed is an observation
      of our own pre-fix payload — the allow-list now strips the field, so that
      observation can no longer be made. Correction (1) is left in place
      unedited as the record of the weaker wording; this paragraph supersedes
      its strength, not its caution.
      DEAD FIELD FOUND BY THE SAME PASS — `display_name` is in the allow-list
      and NOTHING EVER POPULATES IT. Square's TeamMember object has no such
      field (0 of 138 returned it; Gary confirmed against Square's API
      reference 2026-07-29), so memberName()'s given/family fallback is the
      only path that has ever run. RECOMMENDED, NOT DONE — flagged rather than
      removed silently, and re-verified against the code first: dropping it is
      FOUR sites, not one line —
      src/app/api/square/team-members/route.ts:64 (the allow-list entry),
      src/lib/square.ts:110 (the server type),
      src/app/(app)/staff/staff-buttons.tsx:14 (the client type), and the read
      at staff-buttons.tsx:233 (memberName). They must move together: drop the
      type but not the route and the build breaks. Nothing else reads
      display_name, so the change is contained. Behaviour is unchanged either
      way, which is exactly why it is a cleanup and not a fix — take it when
      something else opens these files.
      CLOSED 2026-07-28, commit 2877b41, ahead of its check-by date.
      CHECK-BY RESOLVED — the 2026-07-31 deadline was met on 07-28 and is kept
      here rather than deleted, because the row's value is partly that a dated
      owner-assigned finding was actually cleared inside its window. The
      original text follows unchanged below the FIX AS LANDED paragraph.
      FIX AS LANDED (both halves, src/app/api/square/team-members/route.ts):
      (a) GATE — `const { role } = await getUserStoreScope()` +
      `if (!can({ role }, "staff.sync.square"))` → 403, inserted between the
      orgId check and the org lookup, exactly mirroring
      src/app/api/square/locations/route.ts:33-36. This is the FIRST call site
      of staff.sync.square: the capability existed in permissions.ts:142 and
      was referenced by nothing.
      (b) ALLOW-LIST — the `...m` spread is replaced by id, display_name,
      given_name, family_name, email_address, plus the server-derived
      mapAssignedStores output (assignedStoreIds, primaryStoreId, allLocations)
      and alreadyImported. Every field verified against the sole consumer,
      src/app/(app)/staff/staff-buttons.tsx. email_address is KEPT: the dialog
      writes it to StaffMember.email (staff-buttons.tsx:253) and the resync
      fills a blank email from the same field (staff/sync-square/route.ts:66-70).
      Gary's rule, 2026-07-28: a genuinely-needed PII field means gate the
      route, not drop the field. phone_number, status, is_owner, reference_id,
      timestamps, wage_setting and raw assigned_locations are dropped — zero
      consumers each.
      REACHABILITY, ANSWERED: this was API-SURFACE ONLY, the PERM-6 Task 1
      shape — NOT a live non-admin UI path. Checked: /api/square/team-members
      is absent from proxy.ts:3-12 isPublicRoute, so auth.protect() always
      required a Clerk session (never anonymous); route.ts checked only orgId
      and squareAccessToken, so any authenticated member of a Square-connected
      org got a 200; and the only fetch in the tree is staff-buttons.tsx:167
      inside ImportStaffButton, rendered solely under isAdmin. A STORE or STAFF
      account COULD reach it by hand from any signed-in page; no button did.
      NOT CHECKED: whether the production org has Square connected. No database
      query was run on any branch this session. DEBT-8 records nine
      Square-linked stores on branch `production`, which if accurate means the
      payload was live — that is a prior session's evidence, not this one's.
      TWO CLAIMS IN THIS ROW WERE OVERSTATED, corrected 2026-07-28 rather than
      quietly inherited:
      (1) "phone number is readable" is an INFERENCE, not an observation.
      fetchSquareTeamMembers CASTS the response (square.ts:151) and the
      SquareTeamMember type (square.ts:108-118) does not declare phone_number,
      so the field passed through only if Square's response carried it. No live
      Square response was observed. The EMAIL exposure is directly verifiable
      from the type plus the consumer; the phone exposure is not. The allow-list
      closes both regardless, which is why the fix did not change.
      (2) "renders only under isAdmin (staff/page.tsx:92)" — the gate is :93,
      and there is a SECOND render site the row missed, :109/:111 (the
      empty-state block). Both are isAdmin-gated, so the conclusion held, but
      the reference was incomplete.
      OUT OF SCOPE, RECORDED NOT FIXED: POST /api/staff/sync-square gates with
      an inline `if (!isAdmin)` (sync-square/route.ts:20-21) rather than
      can({role}, "staff.sync.square") — the same capability and the same tier,
      so a consistency item, not a gap.
      PERMISSIONS_INVENTORY.md updated in the same commit: §1j SQ-5b now reads
      ADMIN via staff.sync.square, and §2 gap #7 is struck through as resolved
      in two parts (locations by PERM-6 Task 4, team-members by this row).
      ── ORIGINAL TEXT BELOW, UNCHANGED ──
      CHECK BY 2026-07-31 — OWNER: GARY. Assigned a date and an owner at
      logging (Gary, 2026-07-27) rather than at some later reconcile, because
      this is NOT a docs-staleness item: it is a LIVE PII EXPOSURE. Every
      employee's email address and phone number in the org is readable by any
      authenticated account, including STAFF. Do not let it acquire the
      17-days-unverified shape the Instagram and HR-11b blockers did.
      The fix is the SAME SHAPE as PERM-6 Task 4 — a can() gate plus a field
      allow-list, copied from src/app/api/square/locations/route.ts — so it is
      cheap while that pattern is fresh, and gets more expensive once nobody
      remembers the two-part shape. See the FIX paragraph below for the exact
      capability and call site.
      Found 2026-07-27 while fixing PERM-6 Task 4, deliberately NOT fixed there
      (Task 4 was scoped to square/locations). src/app/api/square/team-members/
      route.ts:6-11 has only auth() plus the org lookup — no can() call — so
      any authenticated org member including STORE and STAFF can read the
      org's full Square team-member list. Identical shape to the locations
      finding: it also spreads the whole Square object (`...m` at :24), so
      every field Square returns on a team member — including email_address
      and phone_number for every employee in the org — reaches the client.
      Arguably worse than the locations case, because the payload is personal
      data about staff rather than business locations.
      The inventory row was SPLIT to record this: PERMISSIONS_INVENTORY.md §1j
      SQ-5 is now SQ-5a (locations, fixed, ADMIN via stores.manage) and SQ-5b
      (team-members, still any member).
      FIX when picked up: gate at staff.sync.square (ADMIN_ONLY) — the sole
      caller is the staff import dialog at
      src/app/(app)/staff/staff-buttons.tsx:167, which already renders only
      under isAdmin (staff/page.tsx:92) — and replace the spread with an
      allow-list of the fields the dialog consumes. Same two-part shape as
      PERM-6 Task 4, so copy that route.

  - id: DEBT-11
    status: shipped
    commits: ["6f33427"]
    title: "PATCH /api/staff/[id] can 500 on a duplicated storeId in the request body"
    notes: >
      REACHED MAIN 2026-08-01 in promotion 97ed309. RE-CHECKED IN PRODUCTION as
      part of that promotion's six-check pass: staff member Tommy Thomas
      (Las Brisas) saved WITHOUT changing stores, and the assignments survived
      with the Primary star intact — the tell for the undefined-vs-[] path this
      row's drift finding protected.
      Status flipped staging -> shipped 2026-08-01 by DOCS-2. Debt rows carry no
      `shipped:` field (DebtItem in src/lib/roadmap.ts has none, and adding
      one type-errors the build), so the promotion date lives here.
      CLOSED 2026-08-01 in 6f33427. The 500 path was re-verified at HEAD before
      the fix, exactly as the row describes it: ownership validated with
      `orgStores.length !== new Set(storeIds).size` (deduped for the
      COMPARISON), then finalStoreIds built from the RAW array and handed to
      storeStaffAssignment.createMany, violating the (staffMemberId, storeId)
      unique constraint. Deduped once at the top; that value is now used
      everywhere.
      THE ROW'S OWN ONE-LINER WOULD HAVE BROKEN THIS ROUTE, which is worth more
      than the fix. It proposed `const storeIds = [...new Set(parsed.data
      .storeIds)]`, copied from POST /api/staff — but POST's schema is
      `.default([])` and this one is `.optional()`, so parsed.data.storeIds is
      legitimately undefined here and `new Set(undefined)` throws. The undefined
      is load-bearing: it means "leave assignments alone", and the route branches
      on it twice. The landed form is the ternary that preserves it. A fix
      copied from a sibling is only as good as the schemas matching.
      BEHAVIOUR DOES CHANGE, on exactly one input, and that is the point of the
      row: a PATCH body repeating a storeId goes from an unhandled 500 to a
      normal success writing one assignment. Every other input is byte-identical.
      Flagged before edit and approved by Gary (2026-08-01), because the sweep
      this landed in was otherwise contracted to behaviour preservation.
      NOT REACHABLE FROM THE UI, unchanged — the dialog holds selections in a
      Set. This is hardening against hand-made and programmatic callers.
      ── ORIGINAL TEXT BELOW, UNCHANGED ──
      Found 2026-07-27 while fixing PERM-6 Task 3, in the block being changed;
      written down rather than fixed inline because it is a robustness bug, not
      that phase's finding. src/app/api/staff/[id]/route.ts validates ownership
      with `orgStores.length !== new Set(storeIds).size` — deduping for the
      COMPARISON — but then builds finalStoreIds from the RAW array. A body
      carrying the same storeId twice passes validation and reaches
      storeStaffAssignment.createMany with two identical rows, violating the
      (staffMemberId, storeId) unique constraint: an unhandled 500.
      Not reachable from the UI — the dialog holds selections in a Set — so
      this is hardening against a hand-made or future programmatic caller, and
      PERM-7 is exactly such a caller.
      FIX: dedupe once at the top (`const storeIds = [...new Set(parsed.data
      .storeIds)]`) and use that everywhere, as POST /api/staff now does after
      PERM-6 Task 1.

  - id: DEBT-12
    title: "POST /api/staff does not validate email format; the sibling PATCH does"
    notes: >
      COST OF DOING NOTHING: A malformed address can reach the nullable email
      column via the Square import or Add Staff Member and sits there harming
      nothing — LATENT (trigger: adding `.email()` later against dirty Square
      data, which is the import path and would start 400ing rows mid-run; the
      row's own instruction to check the live Square data FIRST is what defuses
      it). Verified at HEAD: route.ts:14 is still
      `z.string().trim().max(320).nullish()`, with the asymmetry deliberately
      commented at :11.
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's.
      Found 2026-07-27 during PERM-6 Task 1, deliberately left alone. The new
      Zod schema on src/app/api/staff/route.ts types `email` as a bounded
      string but NOT .email(), while the sibling PATCH at
      src/app/api/staff/[id]/route.ts:13 validates the format properly. The
      asymmetry is now deliberate and commented in the schema: tightening it in
      PERM-6 would have started 400ing Square-imported rows (the import posts
      m.email_address straight through) on a data-quality problem that was not
      that phase's finding, and a partial staff import failing mid-run is a
      worse outcome than a malformed email in a nullable column.
      FIX when picked up: add .email() to the POST schema AND first check what
      the live Square team-member data actually contains, since the import path
      is the one that would start failing. The import surfaces per-row failures
      with their reason (staff-buttons.tsx:246-259), so the failure mode is at
      least legible — but it should be a decision, not a surprise.

  - id: DEBT-13
    status: shipped
    commits: ["ffee362"]
    title: "A MANAGER cannot see a staff member who works at their store but is homed elsewhere — /staff groups by primary store"
    notes: >
      SHIPPED — VERIFIED ON PRODUCTION 2026-08-03. Promoted in the de3ba40
      production promotion of 2026-08-02; implementing commits ffee362 and
      778cf10 (docs/DEPLOY_LOG.md names the full set). Observed on
      www.usefroot.com: /staff shows "Also works here — 2 members based at
      another store" holding Aaliyah Rose (Home store: Meadowood Mall) and
      Chase Nyman (Home store: University Village) under Carson, with the header
      reading "1 team member · 2 also work here".
      THE OBSERVATION WAS TAKEN IN A SINGLE-STORE VIEW SIGNED IN AS
      indianathomas@live.com — NOT as ADMIN, and that is what makes it evidence
      rather than a formality. This row is about what a STORE-SCOPED user can
      see; the ADMIN view is unchanged by construction, so an ADMIN session
      could not have evidenced the fix either way. Same shape as DEBT-9's
      self-serve-rather-than-attested walk: the principal you verify AS is part
      of the evidence, not an incidental detail of how it was collected.
      ORG ID NOT NAMED in the report this was recorded from, contrary to
      CLAUDE.md § Browser Evidence. What identifies the view instead is
      structural — two named staff, their two named home stores, and exact
      header counts — which is that section's own corollary (verify by a
      structural fact, not by a name) doing the work the id would have done.
      Recorded as a gap so no future reader assumes the id was checked.
      CLOSED 2026-08-02 in ffee362. One file, src/app/(app)/staff/page.tsx — no
      permissions change, no query change, no schema, no migration. The query
      was always correct; this was a render decision, exactly as the row's FIX
      DIRECTION said.
      WRITTEN BEFORE THE PUSH, so the board is briefly ahead of reality: at the
      moment this row was edited, ffee362 existed only on the local `staging`
      branch and `origin/staging` was still at 790cfc4. Gary pushed immediately
      after. The SHA is the durable fact; `status: staging` was accurate within
      minutes of being written, and this clause is here so nobody later reads
      the status as evidence of where the code was at the time.
      RE-VERIFIED AT HEAD 790cfc4 BEFORE THE CHANGE, and this row had NOT
      drifted: :78 was still `member.storeAssignments[0].store` (blame 1cfdf76),
      :118 was still `stores.filter((s) => byStore.has(s.id))` (blame e645694),
      and the `some` clause at :28 still fetched the member the render then
      dropped. All three line numbers from 2026-07-27 were exact five days later.
      WHAT LANDED — the row's second named direction, "Also works here", with
      the TRIGGER made precise: a member is listed under their home store, and
      if their home store is NOT ON THIS PAGE they are listed instead under
      every store of theirs that IS, under an "Also works here — N members based
      at another store" heading, each row carrying "Home store: <name>".
      THE TRIGGER IS THE WHOLE DESIGN, and it is why the fix needs no isAdmin
      branch. An ADMIN's `stores` is every store in the org, so a member's home
      store is ALWAYS visible to them and the new branch is unreachable — their
      page is unchanged and cannot duplicate a member, as a property of the data
      rather than a conditional someone could get wrong later. The row's FIRST
      named direction, "render under every in-scope store", cannot be taken as
      literally worded for exactly this reason: for an admin every store is in
      scope, so every multi-store member would list once per store.
      ACCEPTED COST, Gary 2026-08-02: a member whose home store IS in scope
      stays in ONE group even when they also work at another in-scope store.
      Chase Nyman (Carson + Las Brisas, no primary, so homed Carson
      alphabetically) appears to the Las Brisas manager under Carson — same
      page, one card up. Discoverable, which the bug was not.
      THE PRIMARY STAR NEVER MOVES, which is what the row's forbidden fix would
      have broken. Chips still render every assignment with the star on the real
      primary, so on Jordan's Carson row they read ★UNR · Carson · Las Brisas —
      the star keeps pointing at UNR from both cards, and the "Also works here"
      heading agrees with it instead of contradicting it.
      COUNTS STAY SEPARATE: the store header keeps "12 team members" for that
      store's own roster and appends "· 2 also work here" only when the block is
      non-empty. One merged number would make the same word cover two
      populations.
      THE TRUST HALF, which is the row's sharpest point: a non-admin's subtitle
      now states the rule the list follows — "Everyone assigned to your 2
      stores, including staff based at another location" — and it is there on a
      quiet day too. A block that only appears when someone happens to be
      visiting teaches nothing on the days it is absent, and its absence is
      indistinguishable from the bug.
      A WORSE MANIFESTATION THIS ROW NEVER STATED, found at HEAD while
      re-verifying and closed by the same fix: if EVERY in-scope member is homed
      elsewhere, `staff.length` is non-zero so the empty state at :102 does not
      render, and the filter at :118 yields nothing. The manager gets a heading
      and a blank page — no rows, no empty state, no explanation. The same bug
      at its worst.
      WHY IT SCALES, also not in the row, and the reason it was worth fixing now
      rather than later. Jordan's omission is provably an EXPLICIT isPrimary on
      an out-of-scope store, inferred from the sort order WITHOUT a query:
      assignments order [{isPrimary desc}, {store.name asc}], and Carson
      precedes UNR alphabetically, so with zero primaries he would have homed at
      Carson and been visible. Only an explicit primary at UNR omits him. And
      explicit primaries arrive in BULK — SyncStaffButton
      (staff-buttons.tsx:483) sets "store assignments and primary store to match
      Square" for every imported member — so this population grows with each
      Square import rather than one member at a time.
      NOT MEASURED, stated so nobody reads more into this than was checked: no
      database was touched this session. Jordan Pippenger remains the ONLY
      example anyone has evidenced. DEBT-9's four recorded preview/staging
      zero-primary staff were checked against the rule above and NONE is a
      second example — Aaliyah Rose and Gary Thomas are single-store, Chase
      Nyman and Kelton Thomas home at Carson alphabetically, all in scope. How
      many staff carry an explicit out-of-scope primary is UNKNOWN and needs a
      Neon console query. The rendered page was not verified either: /staff is
      behind Clerk and the case needs a MANAGER session.
      ALSO IN THIS COMMIT, both approved as riders: the roster row markup was
      extracted to one StaffRow component shared by the store roster, the "Also
      works here" block and Unassigned, removing a pre-existing near-duplicate
      of ~30 lines; and the non-admin subtitle falls back to the admin wording
      at stores.length === 0 rather than reading "your 0 stores".
      TWO OBSERVATIONS LEFT AS COMMENTS AT THEIR SITES, deliberately not filed
      as rows (Gary, 2026-08-02): Unassigned is reachable for an ADMIN only,
      since a non-admin's query requires `some` in-scope assignment; and
      StaffLocationChips truncates at 8, so a member with 9+ stores on an "Also
      works here" row may not show a chip for the store whose card they are on —
      which costs nothing, because the card heading already names it.
      RULED NOT A ROW, Gary 2026-08-02: a manager already sees out-of-scope
      store NAMES in the Locations chips at HEAD, because StaffLocationChips
      renders member.storeAssignments unfiltered. So "Home store: UNR" discloses
      nothing new, and naming the store is what makes the row make sense.
      Masking chips would be a separate change with a real cost — the Primary
      star would point at a hidden store.
      ── ORIGINAL TEXT BELOW, UNCHANGED ──
      COST OF DOING NOTHING: A MANAGER's /staff roster silently omits every
      staff member who works at their store but is homed outside their scope,
      and nothing signals the list is incomplete — LIVE. Re-derived at HEAD, not
      inherited: page.tsx:78 still groups by `member.storeAssignments[0].store`
      and :118 still renders `stores.filter((s) => byStore.has(s.id))`, so the
      query fetches the person and the render discards them; the detail page
      still loads by direct URL, which is the worst combination.
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's.
      Found 2026-07-27 during PERM-6's staging verification pass, on a session
      driven as corporate@keva.com (MANAGER, assigned Carson + Las Brisas).
      VERIFIED PRE-EXISTING, NOT A PERM-6 REGRESSION. Gary asked for this to be
      established before filing, because the bug surfaced on the night staff
      scoping changed and PERM-6's check 5 only proved a MANAGER can WRITE
      correctly — visibility is a different path and was untested that night.
      Four independent checks, all against commit d4a6bdc:
      (1) `git show --stat d4a6bdc -- 'src/app/(app)/staff/'` returns EMPTY —
      the roster page is not in the commit at all.
      (2) `git blame` on the grouping loop (page.tsx:69-80) → 1cfdf76, dated
      2026-06-26, a month before this phase.
      (3) `git blame` on the render filter (page.tsx:118) → e645694, dated
      2026-07-12. The last commit to touch the file at all is 015d23e (HR
      legal-name work), not d4a6bdc.
      (4) Its dependencies are untouched too: src/lib/auth.ts
      (getUserStoreScope) is not in the commit, and the GET handler in
      api/staff/route.ts was not modified — PERM-6's diff there is the Zod
      schema and POST only. The roster queries Prisma directly in
      getStaffData(), so that API is not even in its path.
      THE BUG: src/app/(app)/staff/page.tsx groups members by their PRIMARY
      store (`member.storeAssignments[0].store`, :78) and then renders only
      groups whose store is in the caller's scoped list
      (`stores.filter((s) => byStore.has(s.id))`, :118). The QUERY is correct —
      getStaffData fetches any member with `some` assignment in the caller's
      stores — so the member is fetched and then silently discarded at render.
      OBSERVED: Jordan Pippenger is assigned to Carson AND Las Brisas, both of
      this manager's stores, but is homed at UNR. He appears nowhere on the
      manager's /staff roster. His DETAIL page at /staff/<id> loads fine
      (staff/[id]/page.tsx:38 authorises on `some` assignment), so he is
      reachable by direct URL but not discoverable — the worst combination,
      because nothing signals he is missing.
      WHY IT MATTERS: a manager cannot see a person who works at their store.
      Not cosmetic — it is a roster with silent omissions, and the manager has
      no way to know the list is incomplete. Multi-store staff at operators
      with a shared labour pool are exactly the population affected, and
      Jordan (7 stores) is a real example in staging today.
      FIX DIRECTION: render a member under EVERY in-scope store they are
      assigned to, or add an "Also works here" line per store group. Do NOT
      simply change the grouping key to "first in-scope assignment" — that
      moves the member under a heading which contradicts the Primary star
      shown on their own row and chips. This needs NO permissions change: the
      query already returns the right people, so it is a display decision.

  - id: DEBT-14
    status: verified
    commits: [46e1b64]
    title: "The internal roadmap UI lists closed debt under \"not yet fixed\" — it ignores a debt row's status"
    notes: >
      VERIFIED IN PRODUCTION 2026-07-29 (Gary). Promoted with the 2026-07-29
      merge; 46e1b64 confirmed an ancestor of main. Note the pleasing loop: this
      row is the first to be moved to `verified` BY THE MECHANISM IT ADDED, and
      flipping it here moves it into its own "Resolved debt" section.
      CLOSED 2026-07-28, commit 46e1b64. The debt section now splits on status:
      an explicit staging/shipped/verified renders under a collapsed "Resolved
      debt" heading, everything else — including a MISSING status, which is
      every outstanding row — stays under "Known debt". 18 open / 2 resolved at
      close (DEBT-8, DEBT-10). Resolved rows keep their place on the page rather
      than being dropped, and now show their commit SHA. DEBT-18 (WITHDRAWN, no
      status) deliberately buckets OPEN: a `withdrawn` value would have to go
      into PhaseStatus, which is shared with the phase board, and Gary ruled
      that a separate decision rather than a rider on this fix. Its comment
      stands as the record. Follow-on found while fixing: DEBT-21.
      ── ORIGINAL TEXT BELOW, UNCHANGED ──
      Found 2026-07-28 while closing DEBT-8, which is the FIRST debt row ever
      resolved — so this was latent from the day the section was written and
      nothing could have surfaced it earlier. Written down rather than fixed
      inline: PERM-7 Task 0 does not touch the roadmap UI, and the PERM-2 scope
      precedent is that a hole gets fixed inline only when the phase is already
      inside the function (same rule Gary applied to DEBT-11 in Ruling 2,
      2026-07-28).
      THE BUG: src/app/(app)/internal/roadmap/roadmap-client.tsx:676-682 renders
      `debt.map(...)` unconditionally under the heading "Known debt — recorded,
      not yet fixed" (:679). Phase rows carry a `status` the UI groups on; debt
      rows never did, because none had ever been closed. DEBT-8 now carries
      `status: staging` and the UI ignores it, so a resolved item is presented
      as outstanding.
      scripts/generate-roadmap.mjs passes the field through untouched
      (normalize() at :95 preserves unknown keys), so the data is already
      there — this is purely a render change.
      FIX DIRECTION: group or filter debt on `status` the way phases are, and
      re-word the heading so it does not assert "not yet fixed" over a mixed
      list. Treat a MISSING status as open — every existing debt row omits it,
      and defaulting the other way would silently hide all thirteen.

  - id: DEBT-15
    status: shipped
    commits: ["6f33427"]
    title: "ClerkAPIResponseError is defined 5x in the built output — identity-based type guards MAY be unreliable across chunks (impact unverified)"
    notes: >
      REACHED MAIN 2026-08-01 in promotion 97ed309. RE-CHECKED IN PRODUCTION as
      part of that promotion's six-check pass: /users -> Invite with
      corporate@keva.com, an address that already has a login, returned the 409
      rendered in the dialog WITH the plus-address suggestion, not a generic
      "Bad Request". That also CONFIRMS DEBT-16's prediction — the dialog
      surfaces the server's text, which already carries the suggestion — so the
      missing client-side pre-check stays polish, not a correctness gap. TEST THE
      INVITE PATH, NOT THE STAFF FORM: the first attempt created a staff member
      with a duplicate email, a different path with no uniqueness constraint, and
      read the success as a possible bug. Recorded so it is not repeated.
      Status flipped staging -> shipped 2026-08-01 by DOCS-2. Debt rows carry no
      `shipped:` field (DebtItem in src/lib/roadmap.ts has none, and adding
      one type-errors the build), so the promotion date lives here.
      CLOSED 2026-08-01 in 6f33427, with its approved mitigation applied and its
      audit preserved below. READ THE NEXT THREE PARAGRAPHS BEFORE ACTING ON
      ANYTHING IN THIS ROW.
      WHY IT CLOSES, stated precisely, because the obvious phrasing would be
      false. It does NOT close because "the hazard was mitigated". It closes
      because (1) the identity DEPENDENCY WAS REMOVED — users/route.ts:127 no
      longer narrows on class identity at all, so the question stops applying to
      the one site that had it — and (2) THE HAZARD WAS SMALLER THAN THIS ROW
      CLAIMED, because the row read only the tail of Clerk's type guard.
      THE ROW'S MECHANISM IS INCOMPLETE, found 2026-08-01 while implementing the
      fix. This row says isClerkAPIResponseError "ends in `target instanceof
      ErrorClass`". True — but it does not START there.
      createErrorTypeGuard (@clerk/shared/dist/errors/createErrorTypeGuard.js)
      first compares `target.constructor?.kind === ErrorClass.kind`, a plain
      STRING comparison against a static, and only falls back to instanceof if
      that misses. A string comparison is immune to chunk duplication. So the
      five definitions were real and the inference drawn from them was
      overstated: the guard already survived the condition this row was filed
      about.
      TWO THEORIES ABOUT THIS ROW HAVE NOW BEEN UNDERCUT BY MEASUREMENT, and
      that is the durable content. The first was that duplicate class
      definitions CAUSED the PERM-7 staging failure — disproven 2026-07-28, same
      day it was filed, with disconfirming evidence that was already in hand and
      reasoned past. The second was that identity narrowing at this site was
      therefore fragile — undercut 2026-08-01 by reading the whole guard instead
      of its last line. Same failure mode both times: a suggestive artifact (5
      definitions; an `instanceof`) beat a cheap direct check (the SDK
      reproduction; the first four lines of the function). The artifact is never
      the measurement.
      THE FIX IS CHEAP HARDENING, NOT A BUG FIX — the row's own binding warning,
      restated on close as it required. It is NOT the fix for the PERM-7 staging
      collision failure. That failure was caused by staging running a deployment
      that predated every PERM-7 commit, and no new code was involved at all. Do
      not let the two be conflated; that conflation is what this row was demoted
      for in the first place.
      WHAT LANDED: the class is now a TYPE-ONLY import, so no ClerkAPIResponseError
      identity reaches the bundle, and the guard duck-types on the payload —
      `clerkError` plus an `errors` array plus a NUMERIC `status`. All three are
      load-bearing, per Gary's amendment (2026-08-01): `clerkError` is a field on
      the BASE ClerkError and establishes nothing about shape on its own, while
      the branch consumes errors[0].code and err.status. Verified against
      @clerk/backend that the sole construction site always sets status from the
      HTTP response, so the third check excludes no real Clerk error.
      STILL WORTH KEEPING: the SHARP RULE below (the risky shape is `instanceof`
      against a class owned by a package that is a transitive dependency of more
      than one direct dependency) and the CAVEAT ON BUCKET 2 (re-run the counts
      after any major dep bump). Both stand on their own and neither depended on
      the overstated mechanism.
      ── ORIGINAL TEXT BELOW, UNCHANGED ──
      DEMOTED FROM CAUSE TO UNVERIFIED HAZARD, 2026-07-28, same day it was
      filed. READ THIS PARAGRAPH BEFORE THE REST OF THE ROW.
      This row originally asserted that duplicate class definitions CAUSED
      PERM-7's staging collision failure. THAT WAS WRONG. The real cause was
      that staging was running a deployment predating every PERM-7 commit — the
      old blanket `catch` at users/route.ts returned `err.message`, and
      ClerkAPIResponseError.message is literally "Bad Request", which is exactly
      what the admin saw. No new code was involved at all.
      WORSE, THE DISCONFIRMING EVIDENCE WAS ALREADY IN HAND. The SDK
      reproduction run while diagnosing this returned
      `isClerkAPIResponseError(err) === TRUE` and produced the intended 409.
      That result was observed and then reasoned past, because the 5-definition
      count looked like a better story. A measurement that contradicts the
      theory outranks a suggestive artifact; this row is the reminder.
      WHAT SURVIVES, as observation only: the built server output really does
      define ClerkAPIResponseError five times — `grep -rl
      'kind="ClerkAPIResponseError"' .next/server/chunks` returns 5 files —
      because @clerk/shared is a transitive dependency of several @clerk/*
      packages and Turbopack inlines it per package chunk. @clerk/shared's
      isClerkAPIResponseError is createErrorTypeGuard(...), which ends in
      `target instanceof ErrorClass`. Whether that ever fails INSIDE the Next
      bundle is UNTESTED — it demonstrably does not fail outside it.
      IMPACT: UNKNOWN. Do not cite this row as the cause of anything until
      someone reproduces a false negative in a deployed runtime.
      The audit below stands on its own and is still useful.
      Audited across the whole of src/ at Gary's instruction ("if class identity
      is unreliable across chunks, one call site isn't the whole finding").
      THE MECHANISM: the built server output defines ClerkAPIResponseError FIVE
      separate times — `grep -rl 'kind="ClerkAPIResponseError"' .next/server/chunks`
      returns 5 files — because @clerk/shared is a transitive dependency of
      several @clerk/* packages and Turbopack inlines it into each package chunk
      rather than sharing one copy. @clerk/shared's isClerkAPIResponseError is
      createErrorTypeGuard(...), which ends in `target instanceof ErrorClass`.
      An error thrown from one chunk's copy fails the check in another's.
      AUDIT RESULT — the finding is genuinely ONE call site, established rather
      than assumed. Every `instanceof` in src/ falls into three buckets:
      (1) INTRINSICS — `instanceof Error` (18 sites) and `instanceof Date`
      (forecast-import.ts:30,50). Error and Date are per-realm globals, never
      bundled, never duplicated. SAFE.
      (2) FIRST-PARTY CLASSES — SignedRecordError (hr-signed-pdf.ts:128) and
      HrFileValidationError (hr-files.ts:31). Referenced in the build through a
      shared module namespace object (`e instanceof x.SignedRecordError`), i.e.
      ONE definition imported by many chunks, not redefined per chunk. SAFE as
      built. Prisma.PrismaClientKnownRequestError (pace-alerts.ts:118) is the
      same shape and appears in exactly ONE chunk. SAFE as built.
      (3) THIRD-PARTY CLASS FROM A SHARED TRANSITIVE DEP — only
      isClerkAPIResponseError, at src/app/api/users/route.ts:127. The only site
      with duplicated definitions, and therefore the only one where the hazard
      is even possible. NOT known to be broken — see the demotion above.
      THE SHARP RULE, worth more than the single fix: the risky shape is
      `instanceof` against a class owned by a package that is a TRANSITIVE
      dependency of MORE THAN ONE direct dependency. That is what gets inlined
      more than once. First-party classes and intrinsics are fine, and
      "instanceof is unreliable" as a blanket claim would be wrong.
      CAVEAT ON BUCKET 2: safe-as-built is a bundler outcome, not a guarantee.
      A dependency change could split Prisma's client across chunks and the
      pace-alerts P2002 guard would begin failing — degrading a benign "already
      alerted this month" return into a rethrow logged as "pace alert failed".
      Worth re-running the counts above after any major dep bump.
      FIX (approved in principle by Gary 2026-07-28, before the demotion, and
      NOT yet applied): duck-type on the payload instead of the class — Clerk
      errors carry `clerkError: true` and an `errors` array, both DATA rather
      than identity, so they survive any number of duplicated class definitions.
      Still worth doing as cheap hardening. It is NOT a fix for the staging
      failure, because that failure had a different cause; do not let the two be
      conflated again when this is picked up.

  - id: DEBT-16
    title: "The /users invite dialog has no email-collision pre-check — only the /stores device-login dialog does"
    notes: >
      COST OF DOING NOTHING: The server's 409 already carries the plus-address
      suggestion and the dialog renders it — confirmed in PRODUCTION 2026-08-01
      — so the missing pre-check costs one round trip and changes no outcome
      available to the admin — NONE. A legitimate NONE, and the row's own
      reasoning ("it does not change what the admin can ultimately do"). Note
      what this NONE does NOT cover: the DEVICE dialog's pre-check has still
      never been exercised on staging, which is untested code rather than a cost
      of leaving this row alone.
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's.
      Filed 2026-07-28 at Gary's instruction rather than built, because PERM-7
      had already grown past its size and this is an optimisation, not a
      correctness gap.
      PERM-7 put a collision pre-check in the device-login dialog
      (src/app/(app)/stores/create-device-login-button.tsx): the /stores page
      passes down every email already used by a User or PendingInvite in the
      org, so a colliding Square contact address is detected and a
      plus-addressed suggestion prefilled BEFORE the Clerk call. The /users
      invite dialog (src/app/(app)/users/user-actions.tsx) has none of this and
      relies entirely on the server's 409.
      WHY IT IS NOT URGENT (Gary's reasoning): once DEBT-15's fix lands, the
      /users dialog renders the 409's text, and that text ALREADY carries the
      plus-address suggestion. The pre-check saves a round trip and shows the
      suggestion a moment sooner; it does not change what the admin can
      ultimately do. Do not confuse this row with the DEBT-15 failure — that one
      is a correctness bug, this one is polish.
      NOTE: the PERM-7 staging pass exercised the /users dialog, not the device
      dialog, which is why the missing pre-check was visible at all. Whoever
      picks this up should verify the DEVICE dialog's pre-check works, since it
      has never been exercised on staging either.
      STILL OPEN AFTER 2026-08-01, AND UNAFFECTED BY THE DAY'S TWO RUNS.
      (1) The 409 COLLISION PATH was confirmed the same day — see DEBT-15, which
      records it: /users -> Invite with an address that already has a login
      returned the 409 rendered in the dialog WITH the plus-address suggestion.
      Note the branch, because it matters: that check ran in PRODUCTION as part
      of promotion 97ed309's six-check pass, not on staging. It confirms this
      row's premise — the dialog surfaces the server's text and the text already
      carries the suggestion — which is exactly why the missing pre-check stays
      polish.
      (2) THIS ROW IS THE PRE-CHECK, and the pre-check was NOT tested by either
      run. The invite that cleared PERM-6/PERM-7's promotion gate went through
      the /users dialog again but to a FRESH address, so no collision occurred
      there, and the DEVICE dialog was not opened at all. Do not read DEBT-15's
      green 409 as covering this row: they are the server path and the client
      path, and only the server path has run.

  - id: DEBT-17
    status: shipped
    commits: ["84437e5"]
    title: "Visiting /users can persist a role the admin never chose — the page creates User rows from Clerk membership alone, ignoring PendingInvite"
    notes: >
      REACHED MAIN 2026-08-01 in promotion 63407be. Status flipped staging -> shipped 2026-08-01 by DOCS-2. Debt rows carry no
      `shipped:` field (DebtItem in src/lib/roadmap.ts has none, and adding
      one type-errors the build), so the promotion date lives here.
      FIRST LIVE NON-ADMIN INVITE SINCE THE FIX, 2026-08-01 — CONSISTENT, NOT
      PROOF. The invite that cleared PERM-6/PERM-7's promotion gate was role
      MANAGER, and /users showed the accepted account as MANAGER, not STAFF —
      the downgrade this row describes did not happen. It does NOT discriminate
      and must never be cited as though it does: post-fix BOTH writers resolve
      role from PendingInvite, so MANAGER is what either winner produces, and
      which one won was not established. What it does rule out is the failure
      mode — had the fix regressed or not deployed, a page-won race would have
      shown STAFF.
      FIXED 2026-07-30 — DIRECTION B, exactly as decided 2026-07-28 and without
      re-litigating the fork. src/app/(app)/users/page.tsx:85-86 now resolves
      the create branch's role as
      `pendingByEmail.get(email)?.role ?? (m.role === "org:admin" ? "ADMIN" :
      "STAFF")`, mirroring the webhook's resolvedRole
      (webhooks/clerk/route.ts:109) so the two writers cannot drift. Every
      file:line in this row was re-verified before editing and NONE had drifted:
      the block is :65-98, the role guess was at :86, pendingByEmail is built at
      :46.
      TWO SILENT-NO-OP RISKS CHECKED FIRST, both clear. (1) KEY MISMATCH —
      pendingByEmail is keyed on raw p.email and the webhook deliberately matches
      `mode: "insensitive"`, but PendingInvite.email is normalised at write time
      (users/route.ts:75) and the lookup key is the already-normalised `email`
      from :74-77 (getClerkPrimaryEmail returns normalizeEmail(primary)), so
      exact match holds. The map was deliberately NOT re-keyed: its other
      consumer at :148 looks up with Clerk's raw inv.emailAddress and re-keying
      would put that at risk for no gain. (2) MISSING FIELD — the query at :37
      has no `select`, so role is present, and PendingInvite.role is the Role
      ENUM (schema.prisma:194), so unlike the webhook's :109 no `as` cast is
      needed.
      NO REPAIR STEP NEEDED — evidence, 2026-07-29/30, Gary ran both queries.
      Q2 (post-webhook signature: role STAFF holding store assignments) returned
      ZERO ROWS on branch `preview/staging` and ZERO ROWS on branch
      `production`. Q1 on branch `preview/staging` returned ONE row
      (kevajuice14@icloud.com, MANAGER vs invited STORE) which is NOT this bug:
      the page can only ever write ADMIN or STAFF, and this row's error runs
      DOWNWARD (STAFF is the floor) whereas that discrepancy runs upward. So the
      fix is code-only; no data repair.
      STILL OPEN AND DELIBERATELY NOT DONE: option C (extract a shared
      syncOrgMembership()). Direction B leaves the GET-that-writes smell in
      place, which was accepted at decision time — "C LATER IF THE SMELL EARNS A
      PHASE". Note BUILD-2's interaction below still applies to C.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      Filed 2026-07-28 during PERM-7's staging post-mortem. INDEPENDENT OF
      PERM-7 and SHIPS IN PRODUCTION TODAY — it is in no phase's diff and was
      found by tracing every writer of User.role.
      THE BUG: src/app/(app)/users/page.tsx:65-91 auto-syncs "any Clerk member
      who has no DB User record yet" by upserting a User with
      `role: m.role === "org:admin" ? "ADMIN" : "STAFF"` (:86). It never
      consults PendingInvite. The Clerk webhook, which DOES consult it
      (webhooks/clerk/route.ts, `resolvedRole = pending?.role ?? ...`), is the
      only path that knows the admin picked MANAGER or STORE — because
      POST /api/users maps every non-ADMIN role to the same Clerk role,
      `org:member` (users/route.ts:102). The app role is recoverable ONLY from
      PendingInvite.
      CONSEQUENCE: whichever writer runs FIRST wins, and the page can win. An
      admin who invites someone as STORE or MANAGER and then opens /users
      before the webhook lands persists role STAFF permanently — the webhook's
      upsert later takes its UPDATE branch, which sets only `email` and never
      role. The invite's intended role is silently discarded, and PendingInvite
      is not cleaned up either, so the two disagree with no signal.
      Direction of the error is DOWNWARD (STAFF is the floor), so this is a
      lockout/annoyance rather than an escalation — an invited manager lands
      with no manager access and nothing explains why. An org:admin invite is
      unaffected, since both writers agree on ADMIN.
      NOT TRIGGERED in the 2026-07-28 staging attempt — corrected 2026-07-28.
      An earlier version of this line said the invited account "has no User row
      at all"; that came from a mis-branched query. On preview/staging the
      webhook won the race and persisted role STORE correctly from
      PendingInvite. So this remains a REAL but UNEXERCISED race, found by
      code-reading rather than by observation. Its severity is unchanged; only
      the claim that it had been seen is withdrawn.
      FIX DIRECTION: have the page consult PendingInvite exactly as the webhook
      does, or (better) stop writing roles from a render path entirely and let
      the page display an unsynced member as pending rather than materialising
      a guess. A GET that writes is the underlying smell.
      ── AUDIT 2026-07-28 (audit only, NOT implemented; row stays OPEN) ──
      STILL REAL against current code. The block is users/page.tsx:65-98 (the
      row said :65-91 — it extends to :98 including the post-sync re-fetch);
      the role guess at :86 is exact. Sharper than the row states: the page
      ALREADY BUILDS pendingByEmail at :46 from the same PendingInvite rows the
      webhook reads, and the upsert simply does not consult it. The data is in
      hand and unused. Only an ADMIN can trigger the write (requireAdmin,
      users/page.tsx:162). Also drifted: the Clerk role mapping is
      users/route.ts:106, not :102.
      THE WINDOW — NOT MEASURED, and this is stated as structure, not timing.
      It opens when Clerk fires organizationMembership.created and closes when
      the handler's upsert commits. The event is ACCEPTANCE-time, not
      invitation-time: the handler reads membership.public_user_data.user_id
      (webhooks/clerk/route.ts:140), which exists only once someone has joined.
      So normally SECONDS. But it can persist: the handler returns 500 on
      "Failed to resolve member email" (:95) and on any unhandled DB error, and
      Svix retries on a seconds → minutes → hours backoff; and if the webhook
      is misconfigured for an environment or CLERK_WEBHOOK_SECRET is wrong, the
      window is UNBOUNDED and the page becomes the only writer by design rather
      than by race. The trigger — an admin reloading /users to see whether the
      invite was accepted — is the natural admin behaviour in exactly that
      window, which is why "unexercised" should not be read as "unlikely".
      THE WRONG ROW IS PERMANENT, CONFIRMED. webhooks/clerk/route.ts:139-156:
      `update: { email: userEmail }`. `role` appears ONLY in the create branch
      (:154). The page's STAFF row is never corrected. Recovery exists but
      requires an admin to NOTICE — the Edit User dialog sets role and
      assignments (users/[id]/route.ts:136).
      THIS ROW WAS WRONG ABOUT THE AFTERMATH — corrected 2026-07-28. It said
      "PendingInvite is not cleaned up either, so the two disagree with no
      signal." FALSE against current code. The `if (pending)` block at
      webhooks/clerk/route.ts:158-196 is NOT gated on which upsert branch ran;
      it fires whenever a PendingInvite matched. So even when the page wins the
      race, the webhook still creates the correct StoreUserAssignments (:179),
      binds the staff profile (:189), and DELETES the PendingInvite (:195).
      Two consequences pulling opposite ways: LESS BAD — store access lands
      correctly, so the victim gets the right locations with the wrong role;
      WORSE — the forensic evidence self-destructs, so there is normally no
      surviving PendingInvite to compare against. End state is a distinctive
      shape: role STAFF holding store assignments.
      OPTIONS FOR THE FIX — Gary's decision, deliberately not taken here.
      (A) Page stops writing; render unsynced Clerk members as "pending sync".
      Kills the race and the GET-that-writes smell outright. BUT the auto-sync
      is evidently a safety net for missed webhooks, and this removes it: the
      Edit dialog uses prisma.user.update({where:{id}}) and needs the row to
      already exist, so a broken webhook would leave members with no row and no
      path to one. Needs an explicit "Sync now" admin action or it deletes a
      recovery route.
      (B) Page reads PendingInvite as the webhook does. Cheapest — roughly one
      line against data already loaded at :46. Fixes the role; keeps the
      GET-that-writes. Does not reproduce the webhook's store fan-out, but the
      webhook's if(pending) block still lands those later, so it converges. If
      the webhook never lands: role-correct row, no stores, PendingInvite
      survives — strictly better than today. Risk: role resolution now lives in
      two places and can drift.
      (C) Extract the webhook body into a shared syncOrgMembership() called by
      both. One implementation, no drift; whoever wins does the whole job
      correctly and the loser's upsert is a no-op. Biggest diff, in an untested
      production path (the repo has NO tests), still leaves real writes in a
      render path, and needs idempotency care around pendingInvite.delete under
      concurrency.
      (D) Leave the write; warn when role disagrees with a surviving
      PendingInvite. No behaviour change, cheapest to reason about — but per
      the correction above the PendingInvite usually will NOT survive, so it
      detects almost nothing.
      DECISION — GARY, 2026-07-28: **B NOW, C LATER IF THE SMELL EARNS A
      PHASE.** Taken on the audit above, after the correction to the aftermath
      claim. Rationale as presented and accepted: B is roughly one line against
      data the page already loads at :46, it closes the actual harm (the wrong
      role), and it cannot regress the webhook. A is the architecturally right
      answer but silently removes the missed-webhook recovery path, which does
      not look disposable — A is only on the table bundled with an explicit
      "Sync now" admin action. D is close to worthless given the correction,
      because the PendingInvite it would compare against is usually already
      deleted.
      This row therefore stays OPEN as a decided-but-unimplemented fix, not as
      an open question. The next session picking it up implements B and does
      NOT re-litigate the fork.
      EVIDENCE IT HAS OCCURRED — MOSTLY FORECLOSED by the correction above:
      because the webhook deletes the PendingInvite, the direct
      role-vs-PendingInvite comparison survives only where the webhook NEVER
      landed. Two read-only queries were WRITTEN FOR GARY TO RUN, not run here
      (per the session's Neon-console-only rule); run on branch `production`
      first, then `preview/staging`, and label every result with its branch.
      Q1 (webhook never landed) — join "User" to "PendingInvite" on
      organizationId + lower(email) where u.role <> p.role, selecting both
      roles, p."storeIds" and both createdAt.
      Q2 (post-webhook signature, weaker) — "User" u join "StoreUserAssignment"
      a on a."userId" = u.id where u.role = 'STAFF', grouped, counting
      assignments. FALSE POSITIVES ARE POSSIBLE on Q2: an admin can legitimately
      assign stores to a STAFF user by hand via users/[id]/route.ts:136. Q2 is
      a lead, not proof.
      Full query text is in the Appendix of
      docs/prompts/DEBT-10_team_members_pii.md; nothing has been run, so NO
      branch-labelled result exists yet.
      BUILD-2 INTERACTION (2026-07-29). BUILD-2 added a defaultStoreId write
      INSIDE the `if (pending)` block at webhooks/clerk/route.ts:158-196 — the
      same block this row's aftermath correction established fires regardless of
      which writer won the race. Consequences for the decided fix: DIRECTION B
      (page reads PendingInvite) touches the page only and CANNOT disturb it —
      no change needed. OPTION C (shared syncOrgMembership()) WOULD have to
      carry the default-setting across deliberately; it is easy to lose in that
      extraction because it is a `prisma.user.update` sitting AFTER the upsert
      rather than part of it. Also sharpens this row's end-state signature:
      "role STAFF holding store assignments" now additionally carries a
      defaultStoreId when exactly one store was provisioned, which slightly
      strengthens Q2 as a lead (still not proof).

  - id: DEBT-18
    status: withdrawn
    title: "WITHDRAWN — '/users displays access an account does not have' rested on a mis-branched query"
    notes: >
      STATUS VALUE APPLIED 2026-08-01, the `withdrawn` value having been added
      to PhaseStatus in cf0b044 by Gary's ruling. Nothing else on this row is
      edited and nothing here reopens it. No `commits` field, deliberately:
      nothing was fixed, so there is no SHA to cite.
      WHY IT HAD NO STATUS UNTIL NOW, carried forward from the row comment
      removed in cde5022: the vocabulary genuinely had no value for "withdrawn",
      and adding one was a CODE CHANGE to PhaseStatus in src/lib/roadmap.ts —
      deliberately deferred as a separate decision, not taken as a rider on a
      render fix. The consequence recorded at the time was that DEBT-14's rule
      "a missing status means OPEN" was WRONG for exactly this row, which is the
      anomaly the ruling resolves. THAT ANOMALY IS NOW CLOSED: with `withdrawn`
      applied to both this row and DEBT-23, the rule in src/lib/roadmap.ts holds
      again without exception — a missing status means OPEN, full stop.
      WITHDRAWN THE SAME DAY IT WAS FILED, 2026-07-28. Its founding
      observation was false. The row claimed /users showed store access for an
      account with ZERO StoreUserAssignment rows; the query behind that was run
      against the PRODUCTION branch while being read as staging. On
      preview/staging the account HAS one assignment (#0014) and role STORE, so
      the "#0014" chip on /users was rendering ACTUAL access, correctly, from
      the members table. There was never an anomaly to explain.
      Kept rather than deleted so the retraction is visible and the row number
      is not silently reused. Nothing here should be actioned.
      If a real "intended vs actual access" concern ever surfaces, file it
      fresh — do not resurrect this reasoning, which was built to reconcile a
      contradiction that did not exist.
      ORIGINAL TEXT BELOW, RETAINED AS THE RECORD OF THE ERROR.
      Filed 2026-07-28, deliberately as an OPEN QUESTION rather than a
      diagnosed bug, because the PERM-7 post-mortem could not resolve it and
      guessing is what went wrong repeatedly that day.
      OBSERVED on staging: /users showed the account gary@keva.com as role
      "Store" with location chip "#0014". A direct query of the staging branch
      returned ZERO StoreUserAssignment rows for that account, and no User row
      at all for the accepted Clerk identity
      (user_3H9HEV5iN8vkRDMb71JDSjB37cm). So the chip did not come from actual
      access.
      TWO CANDIDATE SOURCES, neither confirmed:
      (a) the PENDING INVITES table, whose location chips come from
      PendingInvite.storeIds via storeById
      (src/app/(app)/users/page.tsx, the pendingInvites map) — this renders
      INTENDED access and is labelled "Awaiting acceptance". If this is the
      source, the finding is that an invitation Clerk reports as ACCEPTED was
      still being listed as pending, since the page filters Clerk invitations
      on `status: ["pending"]`.
      (b) the MEMBERS table, whose chips come from dbUser.storeAssignments —
      but with no User row this path renders "No locations assigned" and role
      "STAFF", which is NOT what was seen.
      (b) not matching what was observed is itself the puzzle. Do not resolve
      this from the code alone; reproduce it with a screenshot and a
      simultaneous query.
      WHY IT MATTERS BEYOND THE COSMETICS: /users is the page an admin uses to
      answer "what access does this person have?". If it can show intended
      access in a way indistinguishable from actual access, the answer is
      untrustworthy in exactly the situation where it is most consequential —
      a half-completed invite. Related: DEBT-17 (same page, writes roles from
      a render path).

  - id: DEBT-19
    status: shipped
    commits: ["3d57fb6"]
    title: "Organization NAMES are not identifying on staging — nine orgs, five called \"Microsoft\", two \"Keva Juice\""
    notes: >
      COST OF DOING NOTHING: An operator working on staging cannot identify an
      org by name — "the Microsoft org" picks out five rows, four of them empty
      shells — and a wrong-org result looks identical to a right-org result —
      LATENT (trigger: the next instruction, prompt or query result that names an
      org by NAME rather than by ID; sharpest on a WRITE, where HR-14's
      Clerk-org rename would silently rename the wrong one and leave no visible
      trace, because the slug is the part that does not update).
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's.
      WRITTEN INTO A CLOSED ROW DELIBERATELY, on Gary's ruling: a closed row's
      cost is not moot to someone deciding whether the relocation was the right
      call. THIS COST IS EXACTLY WHAT DEBT-44 INHERITS along with the cleanup
      half, and that should be legible here without reading two rows.
      CLOSED BY RELOCATION 2026-08-02 by DEBT-TRIAGE-2, on Gary's ruling.
      RELOCATION CLOSES, DELETION LOSES: nothing below is removed. The record
      text stays here as the history and now also lives where it fires.
      WHERE IT WENT → CLAUDE.md § Database Evidence, as a PRECONDITION alongside
      the branch-label rule rather than as a section of its own. That placement
      IS the argument this row makes: it is the same failure as the missing
      branch label, one layer down — an under-specified identifier that still
      RESOLVES, so nothing errors and the mistake is invisible — and splitting
      one idea across two headings is how it stops firing. The nine-org
      measurement, the five "Microsoft" rows and the HR-14 silent-rename hazard
      all went with it.
      THE CLEANUP HALF WENT TO DEBT-44, which stays open and now owns it: same
      branch, same org, and DEBT-44 already carries the store-delete cascade
      analysis that a by-ID cleanup needs. That row's own text already named
      this one as its precedent, so the two were arguing the same case from
      opposite ends.
      STATUS VALUE: `shipped`, following DEBT-31, which closed the same way — a
      documentation narrowing rather than a code change. DebtItem carries no
      value for "closed by relocation" and inventing one is out of scope
      (DEBT-41 tracks that vocabulary gap).
      Found 2026-07-28 while choosing an org for PERM-7's re-run. Verified ON
      BRANCH preview/staging (branch named per CLAUDE.md § Database Evidence).
      Nine Organization rows:
      cmqvpe2bf000004l1gwxafqk4 "Keva Juice" square=f users=1;
      cmr54z65v000105jxczpt72w1 "Microsoft" square=TRUE users=4;
      cmr54x6gp000005jx7odzwx9e "Microsoft" square=f users=0;
      cmr57tw80000004l49xf6ft71 "Microsoft" square=f users=0;
      6b545d5b-3ea1-442d-b16d-9089984af8b6 "Microsoft" square=f users=1;
      cmr55fhm1000004l5az4vu73j "Microsoft" square=f users=1;
      cmrx04zn7000004jy6mk1mmha "Keva Smoothie Company" square=f users=0;
      cf888f2d-f234-48c7-8097-fd5b44b5b3dd "Keva Juice" square=f users=2;
      cmrefoomo000004l7dq37gdgn "My Organization" square=f users=1.
      Six have zero or one user; exactly ONE has Square connected.
      THE PROBLEM: a name identifies nothing. "the Microsoft org" picks out five
      rows, four of which are empty shells. Any instruction, prompt, decision
      record or query result that names an org by NAME is ambiguous, and the
      ambiguity is silent — you get A row, just not necessarily the one meant.
      SAME SHAPE AS THE BRANCH-LABEL FAILURE the same evening (CLAUDE.md
      § Database Evidence), one layer down: an under-specified identifier that
      still resolves, so nothing errors and the mistake is invisible.
      CROSS-REF, AND THE REASON THIS IS FILED RATHER THAN NOTED: HR-14 carries
      an unresolved Clerk-org-RENAME issue (a rename reaches the DB but the slug
      is never rewritten, and delivery order is not guaranteed). Whoever
      resolves it MUST identify the target org by ID. A rename applied to "the
      Microsoft org" by name has five candidates here and would silently rename
      the wrong one — and since the slug is the part that does not update, the
      damage would not be visible in the name column afterwards.
      FIX DIRECTION: not a code fix. A convention — name orgs by ID (optionally
      "ID (name)") in every doc, prompt and query result — plus a cleanup of the
      dead single-user shells on staging, which is what makes the names
      ambiguous in the first place. Do the cleanup deliberately and by ID.
      SISTER CONVENTION, added 2026-08-01 as a POINTER ONLY — the note itself
      lives once, in PERM-7's Task 7 blocker, and is not restated here.
      `current_setting('neon.branch_id', true)` returns a real value on Neon, so
      a query can CARRY machine-verifiable branch identity instead of an
      asserted literal. Ruled by Gary the same day: select the branch id AND
      name the branch; recommended where available, not mandatory. Same family
      as this row — an under-specified identifier that still resolves.
      ── CORRECTION APPENDED 2026-08-02 by DEBT-44a. NOTHING ABOVE IS EDITED ──
      Appended rather than corrected in place on Gary's ruling: the text above
      is a CLAIM, and a claim's job is to record what was believed at the time
      (CLAUDE.md § Where documents live). Two numbers in it are wrong. The
      nine-org measurement itself is not — re-queried on preview/staging
      2026-08-02, branch id br-square-feather-a63z92vz, all nine ids and all
      nine user counts reproduced EXACTLY, five days on and after three device
      logins were created and deleted in between.
      "SIX HAVE ZERO OR ONE USER" IS SEVEN. Three orgs have zero users
      (cmr54x6gp000005jx7odzwx9e, cmr57tw80000004l49xf6ft71,
      cmrx04zn7000004jy6mk1mmha) and four have exactly one
      (cmqvpe2bf000004l1gwxafqk4, 6b545d5b-3ea1-442d-b16d-9089984af8b6,
      cmr55fhm1000004l5az4vu73j, cmrefoomo000004l7dq37gdgn). 3 + 4 = 7, plus
      cf888f2d-f234-48c7-8097-fd5b44b5b3dd at two and cmr54z65v000105jxczpt72w1
      at four = 9.
      "FIVE ROWS, FOUR OF THEM EMPTY SHELLS" IS FIVE ROWS, TWO OF THEM EMPTY.
      Five Microsoft rows is right and four-not-the-live-one is right, but only
      cmr54x6gp and cmr57tw80 are actually empty; 6b545d5b and cmr55fhm1 hold a
      User each. The ambiguity argument is UNTOUCHED — five rows still answer to
      the name, which is this row's whole point — but "empty" is wrong for half
      of them, and it is the word that makes them sound safe to delete. The
      provably-empty list now lives on DEBT-44, measured against all 45
      Organization FKs rather than against the user count alone.
      THE ERROR PROPAGATED IN THE RELOCATION, and that is worth more than either
      number. Both sentences were copied verbatim into CLAUDE.md § Database
      Evidence when this row closed on 2026-08-02, so the same off-by-one now
      lives in the precondition as well, corrected there by the same append.
      A RELOCATION COPIES ERRORS AS FAITHFULLY AS FACTS. Nothing in the
      relocation process re-derives what it moves — that is what makes it cheap
      and what makes this happen — so a number that was never checked becomes a
      number in a precondition, where it is read as settled. The lesson is not
      "check before relocating", which nobody will remember; it is that the
      relocated copy inherits the ORIGINAL's confidence level and displays a
      higher one, and a reader of CLAUDE.md had no way to know this measurement
      was one evening's count. Same family as DEBT-45 and DEBT-37: the failure
      is in what the artifact does not carry about itself.

  - id: DEBT-20
    status: shipped
    commits: ["6f33427"]
    title: "POST /api/staff/sync-square gates with an inline isAdmin check instead of can(staff.sync.square) — the capability's only other call site uses it"
    notes: >
      REACHED MAIN 2026-08-01 in promotion 97ed309. RE-CHECKED IN PRODUCTION as
      part of that promotion's six-check pass: /staff -> "Sync Locations from
      Square" as admin worked, no 403 — the capability check resolves the same as
      the inline isAdmin it replaced.
      Status flipped staging -> shipped 2026-08-01 by DOCS-2. Debt rows carry no
      `shipped:` field (DebtItem in src/lib/roadmap.ts has none, and adding
      one type-errors the build), so the promotion date lives here.
      CLOSED 2026-08-01 in 6f33427, exactly as scoped: two lines plus an import
      in src/app/api/staff/sync-square/route.ts. :20-21 was exact at HEAD and
      `isAdmin` appeared nowhere else in the file.
      BEHAVIOUR IS IDENTICAL, and this was proven rather than repeated from the
      row: getUserStoreScope derives isAdmin as `dbUser?.role === "ADMIN"`
      (auth.ts:139), while can() resolves to `isPermissionRole(role) &&
      ADMIN_ONLY.includes(role)` with ADMIN_ONLY = ["ADMIN"]
      (permissions.ts:105). The two agree on every role including null. The 403
      body and status are unchanged.
      NOT WIDENED, per the row's own instruction. The contradicted call sites in
      docs/PERMISSIONS_INVENTORY.md §3 each need Gary's ruling first, because the
      shim necessarily encodes one answer; sync-square was safe to do alone
      precisely because inline and capability already agreed.
      ── ORIGINAL TEXT BELOW, UNCHANGED ──
      Found 2026-07-28 while fixing DEBT-10, in the immediate neighbourhood of
      that change; written down rather than fixed inline because DEBT-10 was
      scoped to GET /api/square/team-members. Related: DEBT-10, which created
      the asymmetry by migrating the read half of this pair.
      NOT A SECURITY GAP AND NOT URGENT — say this first so the row is not
      read as one. Both enforcement points land on ADMIN today, so behaviour is
      identical either way. src/app/api/staff/sync-square/route.ts:20-21 does
      `const { isAdmin } = await getUserStoreScope()` then
      `if (!isAdmin) return 403`, while the sibling read it pairs with,
      src/app/api/square/team-members/route.ts, now does
      `can({ role }, "staff.sync.square")` after DEBT-10. Same capability, same
      tier, two different mechanisms — in the same user flow: the Staff page's
      "Import from Square" button reads through the migrated route and its
      "Sync Locations from Square" button writes through the unmigrated one
      (both in src/app/(app)/staff/staff-buttons.tsx, :167 and :486).
      WHY IT IS WORTH A ROW ANYWAY: PERM-1's shim exists so that PERM-5's
      per-user overrides have ONE place to hook in (src/lib/permissions.ts
      SCOPE_OVERRIDES). An inline `isAdmin` is invisible to that layer. On the
      day someone is granted staff.sync.square by override, Import will start
      working for them and Sync will not, and the failure will present as an
      inexplicable 403 on one of two adjacent buttons rather than as a missed
      migration. The cost of that is paid later and by someone else, which is
      exactly the shape worth recording now.
      SCOPE IF PICKED UP: two lines in
      src/app/api/staff/sync-square/route.ts — swap the destructure to
      `{ role }` and the check to
      `if (!can({ role }, "staff.sync.square")) return 403`. No behaviour
      change, so nothing to verify beyond `next build` and one admin-path
      smoke test of the Sync button.
      DO NOT WIDEN THE SWEEP WITHOUT A RULING. This row is deliberately about
      ONE route, not "migrate every inline role check". docs/
      PERMISSIONS_INVENTORY.md §3 lists call sites whose enforcement points
      CONTRADICT each other, and src/lib/permissions.ts's header is explicit
      that each of those needs Gary's ruling before migration because the shim
      necessarily encodes one answer. sync-square is not one of those — inline
      and capability agree on ADMIN — which is precisely why it is safe to do
      alone and why bundling it with the contradicted ones would not be.

  - id: DEBT-21
    status: shipped
    commits: ["70ee3c8"]
    title: "generate-roadmap.mjs coerces commit SHAs to strings for phases and bugs but not for debt"
    notes: >
      REACHED MAIN 2026-08-01 in promotion 63407be. Status flipped staging -> shipped 2026-08-01 by DOCS-2. Debt rows carry no
      `shipped:` field (DebtItem in src/lib/roadmap.ts has none, and adding
      one type-errors the build), so the promotion date lives here.
      FIXED 2026-07-30 in 70ee3c8 — one word, exactly as the row prescribed:
      `const debt = withStringCommits(normalize(raw.debt ?? []))`.
      IT WENT LIVE WITHIN TWO DAYS OF BEING FILED AS LATENT. This row was filed
      2026-07-28 saying "LATENT, NOT LIVE — no row triggers it today". The very
      next debt SHA recorded, 84437e5 on DEBT-17 and DEBT-22 (2026-07-30),
      triggered it: `next build` failed with "Type 'number' is not assignable to
      type 'string'" at src/generated/roadmap.ts, and a knowingly-red commit was
      created before anyone noticed. "Cheap to take whenever the generator is
      next opened" turned out to mean "cheap until the next SHA".
      THE FILED TRIGGER WAS TOO NARROW — corrected, not left standing. The row
      below says the mechanism needs an ALL-DIGIT SHA (2081401, 9743899).
      84437e5 contains an 'e'. YAML read it as SCIENTIFIC NOTATION:
      84437 x 10^5 = 8443700000. So the hazard class is wider than filed — any
      SHA shaped <digits>e<digits> also hits it, and that shape is far likelier
      than all-digits: it needs a single `e` in the right place rather than
      seven consecutive digits. The row's own prediction, "the first all-digit
      debt SHA is the one that surfaces it", was therefore wrong about the
      mechanism while being right that a debt SHA would surface it.
      The comment above withStringCommits (generate-roadmap.mjs:80-95) now
      documents BOTH shapes so the next reader does not re-derive the narrow
      version.
      VERIFIED, not assumed: with the coercion in place the build is green even
      with 84437e5 UNQUOTED in the YAML. The SHAs are left quoted as defence in
      depth, but the generator no longer depends on an editor remembering.
      ── ORIGINAL NOTES BELOW, UNEDITED (and narrower than the real hazard) ──
      Found 2026-07-28 while adding status/commits to DEBT-10, one line away
      from the change. LATENT, NOT LIVE — no row triggers it today.
      THE MECHANISM is the one the script already documents for phases
      (generate-roadmap.mjs:82-85): an all-digit short SHA such as 2081401 or
      9743899 is valid YAML for a NUMBER, so `commits` parses as a mixed
      string/number array. withStringCommits() coerces it — but it is applied to
      phases and bugs only, and debt is normalized without it:
        const phases = withStringCommits(normalize(raw.phases))
        const bugs   = withStringCommits(normalize(raw.bugs ?? []))
        const debt   = normalize(raw.debt ?? [])
      (generate-roadmap.mjs:93-95). A debt row carrying an all-digit SHA would
      therefore reach the page as a number while DebtItem.commits is typed
      string[] — the same silent shape drift the phase comment was written to
      prevent. Both SHAs on debt rows today contain letters (6530d8b, 2877b41),
      which is the only reason nothing is broken. DEBT-14's render now prints
      commits, so the first all-digit debt SHA is the one that surfaces it.
      FIX: one word — wrap the debt line in withStringCommits, as the two lines
      above it already are.
      FILED RATHER THAN FIXED INLINE: DEBT-14 was scoped to the render, this is
      the generator, and a one-word fix in a different file is still a change
      nobody asked for in that diff (the same rule applied to DEBT-11 in Ruling
      2, 2026-07-28). Cheap to take whenever the generator is next opened.

  - id: DEBT-22
    status: shipped
    commits: ["84437e5"]
    title: "hr/acknowledge loads a manager-attest target's storeAssignments with no orderBy — the only unordered primary-store pick left"
    notes: >
      REACHED MAIN 2026-08-01 in promotion 63407be. Status flipped staging -> shipped 2026-08-01 by DOCS-2. Debt rows carry no
      `shipped:` field (DebtItem in src/lib/roadmap.ts has none, and adding
      one type-errors the build), so the promotion date lives here.
      FIXED 2026-07-30. src/app/(app)/hr/acknowledge/[documentId]/page.tsx:56 now
      carries the standard
      `orderBy: [{ isPrimary: "desc" }, { store: { name: "asc" } }]`. One line,
      no migration, no behaviour change for anyone who has a primary. The
      file:line was re-verified before editing and had not drifted.
      With this, EVERY storeAssignments load in the codebase orders
      deterministically — the split this row documented is closed. Cosmetic as
      described: the persisted value was always recomputed server-side from an
      ordered query (acknowledgments/route.ts:94-102), so no signed record was
      ever affected and there is nothing historical to correct.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      Found 2026-07-29 during the BUILD-2 audit, while disproving the claim
      that primaryStoreName() runs on unordered data (see the correction on
      BUILD-2 and DEBT-9). It does not — but this one site does.
      WHERE: src/app/(app)/hr/acknowledge/[documentId]/page.tsx:54-57. The
      manager-attested branch loads the target staff member with a bare
      `include: { storeAssignments: { include: { store: true } } }` — no
      orderBy. Every other load of storeAssignments in the codebase uses
      `orderBy: [{ isPrimary: "desc" }, { store: { name: "asc" } }]`
      (src/lib/hr.ts:23, src/app/api/hr/forms/shared.ts:48,
      src/app/api/hr/documents/[id]/acknowledgments/route.ts:99,
      src/lib/hr-compliance.ts:148, src/lib/hr-signed-pdf.ts:764,
      src/app/(app)/staff/page.tsx:33, src/app/(app)/staff/[id]/page.tsx:32).
      This is the sole exception.
      That unordered array reaches the client at
      src/app/(app)/hr/acknowledge/[documentId]/page.tsx:120-124 and is picked
      over at signing-client.tsx:91 —
      `staff.stores.find((s) => s.isPrimary) ?? staff.stores[0] ?? null` —
      which pre-selects the store chip.
      COSMETIC, NOT A LEGAL EXPOSURE, and the reason matters: the value that
      gets PERSISTED is recomputed server-side. On the attested path
      acknowledgments/route.ts:172 calls primaryStoreName(staff) against its
      OWN ordered query at :94-102, and per the comment at :167-169 the
      attested path has no selector at all. So a mis-ordered pre-selection
      cannot reach HrDocumentAcknowledgment.storeName. What it can do is
      show a manager an arbitrary chip for a staff member with no primary —
      visible, briefly confusing, never written down.
      Once BUILD-2's partial unique index lands, the duplicate-primary case
      is impossible and this narrows to the zero-primary case only (DEBT-9's
      shape). It does not narrow to nothing: a staff member with no primary
      and several stores still gets an arbitrary chip here.
      FIX: add the standard orderBy to the include at page.tsx:56. One line,
      no migration, no behaviour change for anyone who has a primary.
      NOT DONE IN BUILD-2 — filed rather than fixed, per Gary 2026-07-29,
      because BUILD-2's fence is the column plus its two migrations and this
      is an HR signing surface.

  - id: DEBT-23
    status: withdrawn
    title: "WITHDRAWN — DEPLOY_LOG.md was never missing an entry; the row was built on a misread grep"
    notes: >
      STATUS VALUE APPLIED 2026-08-01, the `withdrawn` value having been added
      to PhaseStatus in cf0b044 by Gary's ruling — the "separate decision" the
      NO STATUS FIELD paragraph below was waiting on. That paragraph is left
      standing as written; it is now history rather than instruction. No
      `commits` field, deliberately: nothing was fixed, so there is no SHA to
      cite.
      WITHDRAWN 2026-07-30. THE ROW BELOW IS FALSE. docs/DEPLOY_LOG.md contains
      an entry for every production promotion on record — all SIX that meta.note
      lists: 2026-07-27 (line 7), 2026-07-26 (:69), 2026-07-25 (:91),
      2026-07-24 (:111), 2026-07-23 (:131), 2026-07-21 (:145). The 2026-07-27
      entry the row claims is missing is the FIRST heading in the file.
      HOW THE ERROR HAPPENED, recorded because the mechanism will recur:
      `grep "promotion" docs/DEPLOY_LOG.md | tail -5` was run and the LAST match
      read as the newest entry. DEPLOY_LOG is REVERSE-CHRONOLOGICAL — newest at
      the top — so `tail` returned the OLDEST entries. A row, a "second
      occurrence" claim, and a follow-up "third occurrence" remark were all built
      on that single unchecked assumption. The "third occurrence" framing was
      Gary's, offered in good faith on the strength of this row, and rests on the
      same false premise — the error originates here, not there.
      LESSON, not just an apology: `tail` on a file whose order was never checked
      is not evidence. The file's ordering is visible in one `grep -n "^## "`,
      which is what should have been run and is what settled it.
      WHAT SURVIVES: the 2026-07-29 BUILD-2 promotion genuinely had no entry at
      the time this was withdrawn — for the ordinary reason that it had just
      happened, not because of any pattern. That entry was written in the same
      commit as this withdrawal. Nothing else in the row stands.
      NO STATUS FIELD, deliberately: like DEBT-18, this is withdrawn rather than
      fixed, so it buckets OPEN on /internal/roadmap with WITHDRAWN as the first
      word of its title. Adding a `withdrawn` value to PhaseStatus is a separate
      decision (Gary, 2026-07-28) and not a rider on this.
      ── ORIGINAL TEXT BELOW, RETAINED AND FALSE ──
      Noted 2026-07-29 during the BUILD-2 audit; filed, not fixed. The most
      recent promotion heading in docs/DEPLOY_LOG.md is "2026-07-23 — PRODUCTION
      promotion (HR-8 → STAFF-1 batch)", but ROADMAP and git both record a
      2026-07-27 promotion (merge 06b1561, "Promote staging: PERM-2, PERM-3,
      BUILD-1, SQ-1 docs, roadmap dashboard", with 17dc723 recording BUILD-1,
      BUG-3 and SQ-2 verified in production). The 2026-07-21 entry already
      carries a note that its own entry was missing when the promotion happened,
      so this is the second occurrence of the same gap, not a one-off. MATTERS
      because DEPLOY_LOG is the only place the ORDER and CONTENT of production
      promotions is narrated — MIGRATIONS.md records which migrations rode which
      promotion and points AT DEPLOY_LOG for the rest. A missing entry means
      the next person reconstructing "what went to prod when" has to read git.
      Reconstructable from git log at the time of writing; gets harder, not
      easier.
      DO NOT LET FILING THIS ROW BECOME THE THIRD OCCURRENCE (Gary,
      2026-07-29). BUILD-2 itself carries two migrations
      (20260729124105_build2_user_default_store,
      20260729145504_build2_staff_one_primary_store) and is promoted to main
      separately from this row being filed. THAT promotion needs its own
      DEPLOY_LOG entry — naming both migrations, since MIGRATIONS.md's own
      history section records migrations per promotion and would otherwise have
      nothing to point at. Writing this row down is not the same as closing it.

  - id: DEBT-24
    status: shipped
    commits: [f646bf6]
    title: "meta.updated in ROADMAP.yaml was the roadmap page's fallback date, was stale, and nothing maintained it"
    notes: >
      REACHED MAIN 2026-08-01 in promotion 63407be. Status flipped staging -> shipped 2026-08-01 by DOCS-2. Debt rows carry no
      `shipped:` field (DebtItem in src/lib/roadmap.ts has none, and adding
      one type-errors the build), so the promotion date lives here.
      FILED AND FIXED IN THE SAME COMMIT 2026-07-30, on Gary's ruling. Recorded
      in full anyway: the finding is about a class of trap, and the deletion
      alone would not explain itself to a future reader.
      RULING — GARY, 2026-07-30: TAKE (a), DELETE meta.updated and let the
      fallback be "unknown". An unmaintained fallback is worse than none. Not to
      be re-litigated; option (b) below was considered and rejected.
      HOW THE LABEL RESOLVES: scripts/generate-roadmap.mjs:38-50 runs
      `git log -1 --format=%cI -- docs/ROADMAP.yaml` at build time (prebuild /
      predev hooks); :97-108 picks git -> meta.updated -> unknown and records
      WHICH, and the page states the source under the date
      (roadmap-client.tsx:321-326) in warning colour whenever it is not git
      (:350-352). It is NOT file mtime, so a fresh clone cannot turn the label
      into "last deployed" — a clone still carries history. The mechanism is
      careful; this row is about a rotting input to it, not a flaw in it.
      THE FALLBACK PATH: Vercel shallow-clones (~10 commits per the comment at
      generate-roadmap.mjs:32, not independently verified). If ROADMAP.yaml's
      last change falls outside that window `git log` exits 0 with EMPTY output
      rather than failing — the `out || null` at :45 exists for exactly this —
      and meta.updated was used instead.
      THE PROBLEM: meta.updated read 2026-07-27 while ROADMAP.yaml's last commit
      was 2026-07-29T15:43:26-07:00, and `git log -S` showed the field changed
      EXACTLY ONCE EVER — f0bcb91, 2026-07-24, the commit that created the meta
      block. Nothing wrote it; WORKFLOW.md's session-completion rules never
      mentioned it. So the fallback held a value wrong since the day it was
      written, drifting further every session.
      WHY IT MATTERED DESPITE THE DISCLOSURE: the failure was invisible in normal
      operation because the git path always wins — the staleness could only be
      discovered at the moment it was being relied on. When it fired the page
      would render "Roadmap last updated Jul 27" in the prominent line with a
      small warning beneath: a plausible date, not an obviously broken one.
      LIKELIHOOD WAS LOW, NOT ZERO. Measured over the last 20 commits,
      ROADMAP.yaml was touched at #1, 5, 6, 8, 9, 11, 12, 13, 14, 16, 17, 18, 19,
      20 — largest gap 4 commits, comfortably inside depth 10. But that is a
      property of working habits, not a guarantee: a long code-only run deferring
      the roadmap update to the end would cross it.
      REJECTED — (b) keep meta.updated and add "bump it" to WORKFLOW.md's session
      completion rules. Preserves a real fallback but relies on a manual step
      that had been skipped every session for five days; the evidence was against
      it. ALSO REJECTED, and never on the table: deriving the fallback from build
      time, which is exactly the "last updated becomes last deployed" failure the
      design avoids.
      SAFE AS A ONE-LINE DELETE, verified 2026-07-29 before doing it: the
      generator reads only raw.phases (:93), raw.bugs (:94), raw.debt (:95) and
      raw.meta?.updated (:98). Nothing anywhere reads meta.sources_retired or
      meta.note. With `updated:` gone, metaUpdatedToIso returns null at its first
      guard (:67), fromMeta is null, and the chain falls to `unknown`, which the
      page already renders (roadmap-client.tsx:341-345 and the :326 label). No
      code change was required.
      SIDE NOTE for whoever next opens the meta block: after this deletion NOTHING
      in it is read by any code. meta.note remains valuable human history (six
      production promotions with SHAs, the `--ancestry-path` re-verification, the
      SQ-2 double-SHA divergence, the rollback tag, HR's production launch date)
      and should not be deleted on the grounds that it is unread.

  - id: DEBT-25
    status: shipped
    commits: ["6b36471"]
    title: "WORKFLOW.md's session-completion rules still order you to bump the deleted meta.updated"
    notes: >
      REACHED MAIN 2026-08-01 in promotion 63407be. Status flipped staging -> shipped 2026-08-01 by DOCS-2. Debt rows carry no
      `shipped:` field (DebtItem in src/lib/roadmap.ts has none, and adding
      one type-errors the build), so the promotion date lives here.
      CLOSED 2026-07-30 in 6b36471, by REPLACEMENT rather than deletion (Gary's
      ruling this session, on the argument below). The bullet at WORKFLOW.md:88-91
      — verified un-drifted before editing, :88-91 exact — now reads:
      "do NOT add or bump `meta.updated` — the field was deleted 2026-07-30
      (DEBT-24). The `/internal/roadmap` page dates itself from this file's git
      commit date, falling back to "unknown" by design."
      WHY NOT JUST DELETE IT: this row's own named risk is a future session
      RE-ADDING `updated:` to satisfy the checklist, quietly reversing DEBT-24.
      A deletion removes the wrong instruction and leaves nothing standing
      against that; a reader who then sees the page render "unknown" has no way
      to know it is by design and will read it as a bug worth fixing with a
      fallback. A prohibition is also still a checkable line in a list whose
      premise is "not done until all are true", where a deletion merely
      shortens the list. The replacement keeps the half of the original text
      that is still true — the page dates itself from the git commit date — and
      drops the half describing the deleted field as a live fallback.
      NOT TOUCHED, and not a leftover half of this fix:
      scripts/generate-roadmap.mjs:65,151-152 still name meta.updated in its
      fallback chain. That is correct behaviour, not drift — with the field gone
      the chain resolves to "unknown", exactly as DEBT-24 recorded ("No code
      change was required"). Noted here so a later reader does not mistake it
      for this row left half-done.
      ── ORIGINAL TEXT BELOW, UNCHANGED ──
      Found 2026-07-30 during DEBT-7, which inherited the instruction and could
      not carry it out. RECORDED, NOT FIXED — DEBT-7 was a docs-only session
      whose writable set was STAGING_SETUP.md and this file.
      docs/WORKFLOW.md:88-91, item 2 of "Session completion rules", requires
      "`meta.updated` is bumped to today's date" and explains it as the fallback
      the /internal/roadmap page shows when a shallow clone hides the git commit
      date. That field was DELETED from this file on 2026-07-30 by Gary's ruling
      on DEBT-24 (f646bf6) precisely because nothing maintained it. So the one
      document that tells a session what "done" means now orders a step that
      cannot be performed, on a field that no longer exists.
      NOT THE SAME FINDING AS DEBT-3, though both are stale WORKFLOW.md: DEBT-3
      is §3 documenting the retired `migrate dev` flow. This is a different
      section and a different cause — DEBT-24 changed the world and left its
      instruction behind.
      WHY IT IS NOT COSMETIC: a checklist item that cannot be satisfied trains
      readers to skip checklist items. The likely failure is a future session
      RE-ADDING `updated:` to satisfy WORKFLOW.md, quietly reversing a ruling
      that is marked not-to-be-re-litigated.
      FIX SHAPE: delete item 2's `meta.updated` bullet from WORKFLOW.md:88-91
      and, if anything replaces it, say that the page dates itself from the git
      commit date and falls back to "unknown" by design. One-line edit; needs a
      session with WORKFLOW.md in scope. Related: DEBT-24, DEBT-3, DEBT-7.

  - id: DEBT-26
    status: shipped
    commits: ["9508d4c"]
    title: "Two code/doc comments describe a roadmap data model that has since changed"
    notes: >
      REACHED MAIN 2026-08-01 in promotion 97ed309. Status flipped staging -> shipped 2026-08-01 by DOCS-2. Debt rows carry no
      `shipped:` field (DebtItem in src/lib/roadmap.ts has none, and adding
      one type-errors the build), so the promotion date lives here.
      CLOSED 2026-08-01 in 9508d4c. Both comments corrected, exactly per the fix
      shape below. Line references had drifted only trivially — the ROADMAP.yaml
      NOTE runs :20-23, not :20-22 — and roadmap.ts:57-59, roadmap.ts:61-62,
      roadmap-client.tsx:63-67 and :670-671 were all exact at HEAD.
      (1) The header NOTE now reads `debt:` accepts id/title/status/commits/
      notes. The `open:` half is untouched and still true: adding `open:` to a
      debt row still type-errors the build.
      (2) The roadmap.ts comment now points at isResolvedDebt — and DELIBERATELY
      DOES NOT RESTATE THE STATUS SET, which is the part worth noting. Naming
      the statuses in a second place is how this comment went stale the first
      time. isResolvedDebt is now labelled in its own header as the single
      definition, and the type-side comment defers to it. That was immediately
      load-bearing: the `withdrawn` value added hours later in cf0b044 changed
      the set, and neither comment needed touching.
      ── ORIGINAL TEXT BELOW, UNCHANGED ──
      Found 2026-07-30 during DEBT-7 while checking what fields a debt row may
      legally carry. RECORDED, NOT FIXED — out of that session's writable set
      (one of the two is, but fixing it was not the task and the pair belongs in
      one row). Both are comments, not code: nothing misbehaves at runtime, and
      both mislead a reader deciding what is allowed.
      (1) docs/ROADMAP.yaml:20-22 — the header NOTE states "`debt:` accepts
      id/title/notes ONLY". False since DEBT-8 became the first resolved debt
      row: DebtItem in src/lib/roadmap.ts:61-62 carries optional `status` and
      `commits`, and DEBT-8, DEBT-10, DEBT-14, DEBT-21, DEBT-24 and now DEBT-7
      all use them. The `open:` half of that same NOTE is still correct and
      still type-errors the build — do not delete the whole note.
      (2) src/lib/roadmap.ts:57-59 — the comment above DebtItem.status says
      "NOTHING RENDERS THEM YET — the debt list is still unconditional, under a
      heading that asserts 'not yet fixed'. That is DEBT-14." DEBT-14 shipped
      (46e1b64) and is `verified`; roadmap-client.tsx:670-671 splits the list on
      exactly this field via isResolvedDebt (:63-67). The comment describes the
      bug its own field was added to enable fixing, after the fix landed.
      WHY THEY BELONG TOGETHER: both are the same mistake — a note written when
      debt rows had no status, left in place when they got one. A reader who
      trusts (1) will not add a status; a reader who trusts (2) will assume
      adding one has no effect. Together they discourage the exact convention
      the file now runs on.
      FIX SHAPE: correct the ROADMAP.yaml NOTE to list id/title/status/commits/
      notes while keeping the `open:` warning, and replace the roadmap.ts
      comment with a pointer to isResolvedDebt. Text only, no behaviour change.
      Related: DEBT-14, DEBT-8, DEBT-21.

  - id: DEBT-27
    title: "10 of 12 stores have storeNumber: null — the backfill half, split out of DEBT-5"
    notes: >
      COST OF DOING NOTHING: The degraded sort stays deterministic and the chip
      asymmetry is the best the render can do, so ten null storeNumbers cost
      nothing observable today — LATENT (trigger: stores spanning more than one
      timezone, at which point PERM-3's forecast window boundary turns on a
      name-order tiebreak nobody chose).
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's.
      Created 2026-07-30 by Gary's ruling when DEBT-5 closed on its LABEL half
      (838ad99). This row exists because the audit found the data half had no
      home: DEBT-5's notes were the only mention of a storeNumber backfill in
      this file, and DEBT-4 — the other candidate — rules on local `.env` hosts
      and `vercel env pull`, not on this. Closing DEBT-5 without splitting would
      have retired the whole subject from the open list while the data was still
      wrong. This row carries what DEBT-5's title was actually about.
      THE OPEN WORK: only Carson #0034 and Las Brisas #0014 carry a storeNumber;
      the other ten are null. Consequences, in the order they matter:
      (1) PERM-3's no-storeId fallback (`forecastWindowForCaller`, used by
      /api/forecasting/audit, whose storeId is optional) sorts the caller's
      assigned stores by storeNumber THEN name, so ten nulls degrade it to a
      name-only sort. Still deterministic, so harmless while every store is in
      one timezone — it stops being harmless the moment stores span timezones,
      since the window boundary would then turn on a tiebreak nobody chose.
      (2) The /users chips DEBT-5 just fixed now read `#0014 — Las Brisas` for
      two stores and a bare name for ten. That is consistent and readable, and it
      is the best the render can do; the remaining asymmetry is the DATA's.
      IT IS A DATA CHANGE, NOT A CODE CHANGE — gated on the migration policy in
      CLAUDE.md / MIGRATIONS.md, not on BUILD-1. Same posture as DEBT-1: Neon is
      the source of truth, SQL approval required before any mutation, and the
      backfill is the risk rather than any code replacement. Twelve rows across
      three Neon branches, which is the part to plan rather than the SQL.
      DO THE LAYOUT RECHECK HERE. DEBT-5 verified that the /users chips wrap
      rather than squash and deliberately added NO cap, on the arithmetic that
      only two chips lengthen today. This backfill is what makes all twelve
      lengthen at once, and the pending-invites list renders its chips UNCAPPED
      (src/app/(app)/users/page.tsx:299) while the members table caps at five.
      Re-look at the uncapped list once the data lands; adding a cap is a
      behaviour change and belongs to a session that has decided to make it.
      NOT the real fix for (1) — that is BUILD-2, which shipped 2026-07-29
      (493175e) for User.defaultStoreId. This backfill remains the cheaper
      interim for the sort, and is independent of it. Related: DEBT-5 (label
      half, closed), DEBT-28 (label idiom inconsistency, natural to fold in
      here), BUILD-2, DEBT-1, PERM-3's `deferred:` entry.

  - id: DEBT-28
    title: "The `#0034 — Carson` store label idiom has three variants across fourteen sites"
    status: staging
    notes: >
      ── DEBT-72a BOARD-CURRENCY BACKFILL 2026-08-17 — STATUS ABSENT -> staging.
      Prepended per preserve-and-mark; NOTHING BELOW IS EDITED.

      A DEBT ROW THAT IS SHIPPING MUST DECLARE ITSELF. This row previously carried NO
      `status:` field. On a debt row that means OPEN by design (`DebtItem` in
      src/lib/roadmap.ts:135, read by `isResolvedDebt` in roadmap-client.tsx:71;
      DEBT-14's "a missing status means OPEN", recorded in this file's header) — and
      27 of the 75 debt rows still rely on it, 28 before this flip. Omission is
      therefore not neutral
      here: it says OPEN, and this row's code is on staging. It now says so
      explicitly. This is the first live case of the debt corollary added today to
      docs/WORKFLOW.md § Session completion rules item 4.

      EVIDENCE. Work commit "893d91d" ("stores/staff: one em-dash label form across
      the five hyphen sites") returns TRUE for `git merge-base --is-ancestor 893d91d
      origin/staging` and FALSE for the same test against origin/main. It is the
      current tip of origin/staging. Per the backfill's §3c rule — on staging, not on
      main — the status is `staging`, and it becomes `shipped` on the promotion that
      carries it, like every other debt row (debt rows have no `shipped:` field).

      MEASUREMENT NOTE WORTH KEEPING. A subject-only search does NOT find this
      commit: the ID "DEBT-28" is the last line of its commit BODY and appears
      nowhere in the subject. The DEBT-72 audit predicted exactly this row as the
      case that would be missed, and the subject-only probe reproduced its blindness.
      Whoever builds DEBT-72b's gate should read this before ruling on the regex.
      Full measurement, method and the whole table:
      docs/prompts/DEBT-72a_BACKFILL.md. The rule this backfill accompanies is
      docs/WORKFLOW.md § Session completion rules item 4 — a row whose code was
      pushed to staging in a session has its status set in that session.
      Cross-refs: DEBT-72 (the promotion gate, still planned, still unbuilt),
      DEBT-72_AUDIT.md "b809e03" (which found this class), DEBT-72b (the gate
      itself, sequenced after this and deliberately not designed here).

      RULED 2026-08-17 (Gary) — THE TWO SITES THAT NEEDED A DECISION ARE
      DECIDED, AND THE ROW IS NOW PURELY MECHANICAL. Prepended per
      preserve-and-mark; nothing below is edited. The row's own HAZARD paragraph
      named two sites that "are not pure separator swaps, they are different
      sentences, so they need a decision rather than a find-and-replace". Both
      are now ruled and neither needs re-deriving.

      (1) stores/page.tsx:162 — DROP THE "Store " PREFIX. It becomes
      `#0034 — Carson`, matching the other thirteen label sites. It was the only
      site carrying the prefix, which makes it drift rather than intent. This is
      a real edit, not an exemption.

      (2) store-view-client.tsx:100 — LEAVE `Carson (#0034)` AS IT IS, and the
      reason is the durable half of this ruling: THE EM DASH FORM IS THE HOUSE
      RULE FOR LABELS, AND HEADERS ARE EXEMPT. A label sits in a list or a chip,
      where the number leads so a column scans; a header answers "which store am
      I in", where the name is what the reader came for and the number is a
      qualifier. Two different jobs, correctly rendered two ways. Recorded as an
      exemption rather than left as an outlier so the next sweep does not re-file
      it as drift — which is exactly what happened here, and cost this row three
      glyph variants when there were only ever two.

      WHAT REMAINS: five hyphen-minus sites to convert, one of which also drops
      the prefix per (1). Eight sites already carry the em dash and are
      untouched. That is five files, five edits, no decisions left in it.

      THE SEQUENCING ADVICE BELOW STILL STANDS AND IS NOT OVERRULED. This row
      says fold into DEBT-27 because only two stores currently carry a
      storeNumber, so twelve of the fourteen sites do not render the separator at
      all today and the change is invisible until the backfill lands. Doing it
      standalone is five files for no visible effect. Doing it as part of any
      session that is already open in staff/ or stores/ is free. Neither is
      wrong; what would be wrong is a dedicated session for it.

      POINTERS REPAIRED AGAIN 2026-08-17, originals preserved and nothing above
      or below rewritten. Re-measured by codepoint at HEAD 5bfda2b BEFORE any
      edit, because the ruling above cites the pre-drift numbers and would
      otherwise have pointed a find-and-replace at the wrong lines.
      FOUR CITATIONS HAD MOVED, INCLUDING BOTH SITES THE RULING DECIDES ON:
      src/app/(app)/stores/page.tsx:162 → :227;
      src/app/(app)/staff/page.tsx:127 → :262;
      src/app/(app)/staff/[id]/page.tsx:623 → :742; and
      store-view-client.tsx:100 → :113, whose path is also wrong everywhere it
      appears in this row — it is src/app/(app)/store-view/store-view-client.tsx,
      not stores/. STILL EXACT: staff-buttons.tsx:116 and
      staff-edit-actions.tsx:201.
      THE EIGHT EM-DASH CITATIONS HAD ALL DRIFTED TOO and are repaired here even
      though nothing touches them, so the next reader is not misled twice:
      user-actions.tsx:112,244,271 → :119,315,342; template-form.tsx:1014 →
      :1571; training-form.tsx:813 → :866; users/page.tsx:252,301 → :334,384;
      create-device-login-button.tsx:103 unchanged. The fifteenth reference,
      webhooks/clerk/route.ts:134 → :178.
      THE COUNTS ARE UNCHANGED — 8 em dash, 5 hyphen-minus, 1 inverted, fourteen
      label sites plus the derived-name site. Only line numbers moved, so the
      2026-08-02 correction below is still right about everything except where
      the code sits.
      THIS IS THE SECOND WHOLESALE POINTER REPAIR ON THIS ROW; the first was
      2026-08-02 at cc1fffc and repaired two. Line numbers are the least durable
      thing in this row and a third repair should be assumed, not discovered —
      re-measure by codepoint before trusting any citation here.
      THE FIVE EDITS THEN LANDED at the repaired locations, converting U+002D to
      U+2014 and dropping the lone "Store " prefix. The exemption at
      store-view-client.tsx:113 was left alone as ruled.
      ── PRIOR NOTES BELOW, UNEDITED ──
      COST OF DOING NOTHING: Three separator glyphs render the same information
      across fourteen sites and nothing misbehaves — NONE. Said plainly rather
      than inflated: this row is the clearest NONE in the list, and the row's own
      text already says so ("Text/formatting only — nothing misbehaves, and every
      variant renders the same information").
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's.
      EVIDENCE REPAIRED AND COUNT CORRECTED 2026-08-02 by DEBT-TRIAGE-1,
      re-measured at HEAD cc1fffc. All fifteen references below were checked
      individually rather than assumed; two had drifted.
      POINTERS REPAIRED, originals preserved below:
      src/app/(app)/templates/template-form.tsx:995 → :1014, and
      src/app/api/webhooks/clerk/route.ts:133 → :134. EVERY OTHER CITATION IS
      STILL EXACT — user-actions.tsx:112,244,271; training-form.tsx:813;
      create-device-login-button.tsx:103; users/page.tsx:252,301;
      staff-buttons.tsx:116; staff-edit-actions.tsx:201; staff/page.tsx:127;
      staff/[id]/page.tsx:623; stores/page.tsx:162 (still carrying the odd
      `Store #0034 - Carson` wording); store-view-client.tsx:100.
      THE TITLE SAID TWELVE SITES AND HEAD MEASURES FOURTEEN. Corrected under
      the same rule applied to DEBT-33 — a title stating a checkable fact that
      is wrong gets fixed. HOW IT WENT WRONG IS THE USEFUL PART: the "12" is the
      6 em dash / 5 hyphen / 1 parenthesised tally in the paragraph below, and
      THAT TALLY PREDATES THIS ROW'S OWN LATER ADDITIONS. The sentence beginning
      "Plus src/app/(app)/users/page.tsx:252,301 as of 838ad99, and
      src/app/api/webhooks/clerk/route.ts:133" was appended afterwards and never
      folded back into the count. A row can go stale against itself.
      THE CORRECTED SPLIT AT HEAD: 8 EM DASH (user-actions.tsx:112,244,271;
      template-form.tsx:1014; training-form.tsx:813;
      create-device-login-button.tsx:103; users/page.tsx:252,301), 5 HYPHEN-MINUS
      and 1 INVERTED as listed below and all unchanged. That is FOURTEEN LABEL
      SITES. The fifteenth reference, webhooks/clerk/route.ts:134, is counted
      separately and deliberately: it builds the same string as a derived
      User.name rather than as a label, which is the distinction the original
      text already drew and the reason it sits outside the tally.
      EM DASH IS NOW THE OUTRIGHT MAJORITY — 8 of 14, not the bare plurality of
      6 of 12 the text below describes. That strengthens the em-dash default
      rather than changing it.
      NOTHING ELSE CHANGES: still low priority, still fold into DEBT-27, and the
      two-sites-need-a-decision hazard below still stands.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      LOW PRIORITY. Logged 2026-07-30 from DEBT-5's audit, which had to establish
      the house form before it could match it and discovered there is no single
      one. Text/formatting only — nothing misbehaves, and every variant renders
      the same information.
      THE THREE VARIANTS, verified by CODEPOINT rather than by eye (they are
      indistinguishable in a diff, which is most of why the drift happened):
      EM DASH U+2014, 6 sites — src/app/(app)/users/user-actions.tsx:112,244,271;
      src/app/(app)/templates/template-form.tsx:995;
      src/app/(app)/hr/training/training-form.tsx:813;
      src/app/(app)/stores/create-device-login-button.tsx:103. Plus
      src/app/(app)/users/page.tsx:252,301 as of 838ad99, and
      src/app/api/webhooks/clerk/route.ts:133 which builds the same string as a
      derived User.name rather than as a label.
      HYPHEN-MINUS U+002D, 5 sites — src/app/(app)/staff/staff-buttons.tsx:116;
      src/app/(app)/staff/[id]/staff-edit-actions.tsx:201;
      src/app/(app)/staff/page.tsx:127; src/app/(app)/staff/[id]/page.tsx:623;
      src/app/(app)/stores/page.tsx:162 — which also differs in wording, reading
      `Store #0034 - Carson` where every other site reads `#0034 - Carson`.
      INVERTED, 1 site — src/app/(app)/store-view/store-view-client.tsx:100 puts
      the name first and parenthesises the number: `Carson (#0034)`.
      So the repo-wide split is 6 em dash / 5 hyphen / 1 parenthesised. Em dash
      is the plurality, not a consensus, and it is what the newest code uses
      (PERM-7's device-login button and the Clerk webhook). Gary ruled em dash
      for DEBT-5 on 2026-07-30, matching user-actions.tsx as the closest cousin
      of the page being fixed; that ruling was scoped to the two chips and is
      the natural default here.
      NOT FIXED, and no site above was touched — DEBT-5's writable set was two
      files and aligning the others was explicitly out of its scope.
      WHEN: fold into DEBT-27. That session is already going store-label-shaped,
      already has to re-look at these surfaces once every store carries a number,
      and this is invisible until then — two stores currently exercise the branch
      that shows the separator at all. Doing it standalone means touching six
      files to change one character each. NOTE THE HAZARD IF SOMEONE DOES IT
      ANYWAY: `stores/page.tsx:162` and `store-view-client.tsx:100` are not pure
      separator swaps, they are different sentences, so they need a decision
      rather than a find-and-replace. Related: DEBT-5, DEBT-27.

  - id: DEBT-29
    status: shipped
    commits: ["00e454a"]
    title: "The template availability window is collected, stored, exported — and never enforced"
    notes: >
      SHIPPED — VERIFIED ON PRODUCTION 2026-08-03. Promoted in the de3ba40
      production promotion of 2026-08-02; implementing commits 00e454a and
      65c20cb (docs/DEPLOY_LOG.md). Observed on www.usefroot.com:
      /templates/new reads "Recorded for reference. Not yet used to show or hide
      checklists." under the offsets and "A label for staff — checklists stay
      visible all day." under the run field, and the Preview card is GONE.
      WHAT SHIPPED IS THE COPY, NOT THE FEATURE — stated here because the row's
      title still describes an unenforced window and a reader skimming
      `status: shipped` could take it for enforcement. The availability window
      is STILL not enforced on production. This row closes on the form no longer
      claiming otherwise, exactly as the relabel half always said.
      ORG ID NOT NAMED in the report this was recorded from (CLAUDE.md
      § Browser Evidence). Lower stakes here than elsewhere — the observation is
      static form copy on a create page, which renders identically in any org —
      but recorded so the gap is not assumed to have been checked.
      CLOSED 2026-08-02 in 00e454a by DEBT-29-RELABEL — THE RELABEL HALF ONLY.
      Prompt:
      docs/prompts/DEBT-29-RELABEL_honest_copy.md. The form no longer claims the
      window works. Four strings changed and one card was deleted, all in
      src/app/(app)/templates/template-form.tsx: the Label became "When is this
      checklist run? *"; the help text became "A label for staff — checklists
      stay visible all day."; the Operational Phase help text became "Orders
      this checklist in the day and sets which shift hands off to it."; and a
      new line under the two offset inputs reads "Recorded for reference. Not
      yet used to show or hide checklists."
      THE PREVIEW CARD IS GONE, with getPhaseDescription, which had no other
      caller. It computed concrete clock times — "Store opens 8:00 AM →
      Available 07:00 AM - 10:00 AM" — for a window nothing enforces. It was
      deleted rather than reworded on the ruling that no helper text makes a
      computed clock time honest: it was a simulation of a feature that does not
      exist, and the most guarantee-shaped thing on the page.
      ALSO RELABELLED, one page over: the templates LIST badge at
      src/app/(app)/templates/templates-client.tsx:269 read "When: Store Hours".
      It now reads "Run:". Same claim, different page — leaving it would have
      made the list contradict the form, which is worse than either state alone.
      This site is NOT in the original text below; it was found by this session.
      THE COST LINE BELOW SAYS LIVE, AND THAT IS DISCHARGED BY THIS COMMIT — the
      false statement is off the screen. What remains is not LIVE: an operator
      now types two numbers that are honestly labelled as not-yet-used. The cost
      line is left as DEBT-TRIAGE-2 derived it rather than rewritten, per the
      convention that a dated claim records what was believed at the time.
      WHAT DID NOT CLOSE, and now has its own row: DEBT-48, the implement-vs-
      remove decision, AWAITING RULING, created in 00e454a. Same shape as
      DEBT-5 splitting into
      DEBT-27. Nothing was implemented, no field was deleted, no write path was
      touched, and the payload at template-form.tsx:792-794 is byte-identical —
      so the CSV export/import round trip is a verified no-op. DEBT-48 also
      carries Gary's recorded thinking on the eventual shape, which this row is
      too small to hold.
      THE OFFSETS STAY VISIBLE AND LABELLED — ruled by Gary 2026-08-02, AGAINST
      his own first instinct, which was to hide the inputs and keep the columns.
      RECORDED HERE BECAUSE A FUTURE READER WILL ARRIVE WITH THAT INSTINCT. The
      two reasons it does not work: (1) startOffset/endOffset default to 1 and 2
      at template-form.tsx:637-638 and are written on EVERY StoreHours save, so
      hiding the inputs does not stop the write — it stops the ADMIN SEEING what
      gets written, which is worse than the status quo; (2) sending null instead
      changes what the CSV export emits and breaks parity with files already on
      disk, which is the same argument that ruled DEBT-1b's "During Hours"
      mapping. Both reasons are recorded as a comment at the defaults themselves,
      because that is where someone stands when the instinct strikes.
      A SECOND COMMENT LANDED at src/app/api/templates/[id]/route.ts, extending
      the existing DEBT-1b block: PATCH spreads templateData wholesale, so both
      offsets reach the DB on every template edit, unvalidated — and because the
      route never NAMES them, a repo-wide grep for either field does not return
      that file. This corrects the original text below, which says the grep
      returns "the two write routes": only POST names them. The finding is
      unaffected — still transport, never consumption — but the write surface is
      one route wider than a grep can show.
      POINTERS REPAIRED 2026-08-02 by this session, on DEBT-TRIAGE-1's precedent
      — repaired here, originals left visible below. `prisma/schema.prisma:133`
      for the StoreHours model is now :168-177. `src/lib/messages.ts:38-66` ends
      one line early; resolvePostedForDate closes at :67.
      ONE CLAIM BELOW IS NARROWER THAN THE TRUTH, and it shaped the new wording.
      "The only functional consumer of operationalPhase is handoff-note date
      resolution" is right about the FEATURE and understates the field. phaseOrder
      is the single funnel and drives three behaviours: date resolution
      (resolvePostedForDate, consumed at handoff-messages/route.ts:127-128), the
      shift_phase recorded on the note (phaseToShiftPhase, :137), and WHICH
      templates you may post to and in what order (handoff-notes.tsx:130, :138,
      :141 — a real user-visible gate keyed on operationalPhase, just not an
      availability one). So the phase is a working SEQUENCE key while the offsets
      are inert, and the new copy states the phase's real job rather than
      implying both are decorative.
      VERIFIED NON-CONSUMPTION ON THE READ PATH TOO, which the original text does
      not cover: src/app/api/stores/[id]/templates/route.ts:23-38 is what the
      store-view "Available Checklists" list actually fetches, and it filters on
      isActive / isArchived / appliesTo only — no phase, no offsets, no
      StoreHours join.
      ── PRIOR NOTES BELOW, UNEDITED ──
      COST OF DOING NOTHING: An operator fills in a REQUIRED "When is this
      checklist available? *" field and is shown a preview that reads as a
      guarantee, and nothing in the app ever reads the three values back —
      LIVE. Re-derived at HEAD: the only five files in src touching
      startOffsetHours/endOffsetHours are the template form, the templates
      list, and the export, import and write routes — collection and transport,
      never consumption, exactly as this row describes.
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's.
      EVIDENCE REPAIRED 2026-08-02 by DEBT-TRIAGE-1, re-measured at HEAD
      cc1fffc. These are POINTERS, so they are repaired; the original citations
      stay visible below. All three are in
      src/app/(app)/templates/template-form.tsx and all three drifted by the
      same insertion, not by any change to what they cite. :917 → :936, the
      Label reading "When is this checklist available? *". :925 → :944, the help
      text "Availability calculated based on each store's operating hours".
      :954-960 → :969-979, the Preview card; the three "Example: Store opens
      8:00 AM → Available ..." template strings this row quotes are now at :974,
      :976 and :977.
      THE FINDING ITSELF IS UNCHANGED AND RE-CONFIRMED: nothing reads the three
      collected fields to gate anything. Nothing else in this row changes.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      Logged 2026-07-30 out of the DEBT-1a audit (21dd712), which set out to find
      who READS operationalPhase and found that the field's advertised purpose
      has no runtime consumer at all.
      THE PROMISE: the template form asks "When is this checklist available? *"
      (src/app/(app)/templates/template-form.tsx:917) and states "Availability
      calculated based on each store's operating hours" (:925), collecting
      operationalPhase + startOffsetHours + endOffsetHours and previewing them
      back as "Store opens 8:00 AM → Available 09:00 AM - 06:00 PM" (:954-960).
      THE REALITY: nothing reads those three to gate anything. Checklist rows are
      created in exactly two places — src/app/api/checklists/route.ts:118 (single)
      and :155 (bulk) — and neither references phase or offsets. There is no cron
      generator (only /api/cron/pace-alerts and /api/cron/sales-reconcile). No
      code path joins the offsets to the StoreHours model
      (prisma/schema.prisma:133). Store-view's "Available Checklists" heading
      labels every generated checklist; it is not a filter. A repo-wide grep for
      startOffsetHours/endOffsetHours returns only the form, the templates list,
      the export route, the import route and the two write routes — collection
      and transport, never consumption.
      THE DECISION, which is a product call and not a code cleanup: either
      IMPLEMENT the gate (join the offsets to StoreHours at generation or listing
      time) or REMOVE the claim (drop the offsets UI, or relabel it so the admin
      is not told a number does something it does not). Doing neither leaves
      operators configuring a control with no effect — and a preview sentence
      that reads like a guarantee.
      SEQUENCING AGAINST DEBT-1: the only functional consumer of
      operationalPhase today is handoff-note date resolution
      (src/lib/messages.ts:38-66), which is alias-covered — which is exactly why
      DEBT-1's blast radius is small. IMPLEMENTING the gate ends that: a dirty
      phase string would then decide when a checklist appears. Finish DEBT-1b
      before this row is resolved in that direction.
      Disconfirmed by: any call site reading startOffsetHours/endOffsetHours
      outside those six files, or any availability filter keyed on
      operationalPhase. Related: DEBT-1, DEBT-30.

  - id: DEBT-30
    title: "No DB-level constraint on Template.operationalPhase — app-layer validation is the ceiling"
    notes: >
      COST OF DOING NOTHING: Data is clean on all three branches and every
      current writer is validated, so the missing constraint costs nothing
      today — LATENT (trigger: the next write path that reaches
      Template.operationalPhase without the Zod guard, which is exactly what the
      CSV import did once already — that recurrence is why the row argues the
      CLASS is not closed even when today's holes are).
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's.
      GATE (1) CLEARED 2026-08-02, recorded by DEBT-TRIAGE-1 — THIS ROW DID NOT
      KNOW, and read as blocked when it is ready to run. DEBT-1 is
      `status: verified` at HEAD: writers plugged in c17ccc1, the backfill
      returning zero non-canonical rows on all three branches with branch-named
      before/after counts (dev 1 changed, preview/staging 2, production 1, all
      three then non_canonical_remaining = 0), promoted to production 2026-08-01
      in 63407be — docs/DEPLOY_LOG.md § "2026-08-01 (afternoon) — PRODUCTION
      promotion (DEBT-1, DEBT-2 and the 07-30 debt batch)", pushed to
      origin/main 16:27 — and verified in production by Gary the same day.
      Confirmed at HEAD by `git branch --contains c17ccc1` returning main.
      GATE (2) IS NOT A GATE AND IS NOT DONE. The hand-authored migration below
      is THIS ROW'S REMAINING WORK, not a precondition someone else clears;
      writing it up as "the gate cleared" would leave a reader thinking the
      migration exists. It does not. What changed is that nothing external
      blocks authoring it any more.
      THE OPEN QUESTION BELOW IS STILL OPEN and must be answered before the SQL
      is written: whether NULL stays legal. Nothing since 2026-07-30 has
      decided it.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      Logged 2026-07-30 out of the DEBT-1a audit (21dd712). The column is plain
      TEXT with no CHECK and no enum (prisma/schema.prisma:280;
      prisma/migrations/20260627002005_init/migration.sql:108), so every guard
      against a non-canonical value lives in application code — and the audit
      found six write paths, two of which (the Duplicate button, the CSV import)
      reach the column with no validation whatsoever.
      DEBT-1b will add Zod validation to those paths. That closes today's holes
      but NOT the class: any future route that writes this column re-opens it,
      which is precisely what happened once already — the template form has
      offered only canonical values since the initial build (1cfdf76), and the
      CSV import shipped later with no enum and re-opened the door.
      A CHECK constraint (or a Postgres enum) makes the invariant unbypassable.
      It is deliberately NOT part of DEBT-1b: that session's task order forbids
      schema changes and DDL outright, and the constraint could not be added
      before the backfill anyway — it would reject the existing rows.
      GATED ON, in order: (1) DEBT-1b's backfill reading zero non-canonical rows
      on dev, preview/staging AND production — all three, since the audit found
      different counts per branch (1 / 2 / 1); (2) a migration authored under the
      hand-authored flow in docs/MIGRATIONS.md §3 (migrate diff → read the SQL →
      db execute + migrate resolve → generate), since `migrate dev` is broken here.
      OPEN QUESTION for that session: whether NULL stays legal. It is today —
      AllDay templates write it deliberately (template-form.tsx:790) and 3 of 8
      rows per org carry it — so a CHECK must permit NULL or those rows fail the
      constraint. Related: DEBT-1, DEBT-29.

  - id: DEBT-31
    status: shipped
    commits: ["9508d4c"]
    title: "MIGRATIONS.md:184 says staging was separately seeded — disproven for Template"
    notes: >
      REACHED MAIN 2026-08-01 in promotion 97ed309. Status flipped staging -> shipped 2026-08-01 by DOCS-2. Debt rows carry no
      `shipped:` field (DebtItem in src/lib/roadmap.ts has none, and adding
      one type-errors the build), so the promotion date lives here.
      CLOSED 2026-08-01 in 9508d4c, as the narrowing this row asked for rather
      than a deletion. :184 was exact at HEAD. The line now reads that staging is
      a Neon CHILD BRANCH of production that has diverged PER-TABLE since its
      branch point, with both tables' evidence set out under it: StaffMember HAS
      diverged (the zero-primary query, which is the real observation the
      original line was built from and is kept in full), Template has NOT (same
      cuid on all three branches, identical createdAt to the millisecond).
      THE OPERATIONAL ADVICE IS INTACT, which was the constraint. Both numbered
      consequences survive word for word — a clean staging result is still not
      evidence about production, and dev is still the fair rehearsal for a
      migration while staging is the fair rehearsal for the deploy. Only the
      stated REASON changed, which is the whole point: a wrong reason is what
      lets someone conclude the opposite for a table they have not checked.
      WHILE IN THE FILE, the endpoint table gained a Branch id column —
      production br-sparkling-block-a620qvg4, staging br-square-feather-a63z92vz,
      dev br-broad-wave-a6vpjdw0. Taken from docs/prompts/DEBT-2_AUDIT.md's
      recorded identities (Q0/S0/P0), where each was measured against its
      endpoint before any query ran, NOT from memory. That closes the minor
      finding DEBT-2a filed and declined to open a row for.
      ── ORIGINAL TEXT BELOW, UNCHANGED ──
      Logged 2026-07-30 from the DEBT-1a audit (21dd712), which read all three
      branches and found the doc's claim too broad. NOT FIXED — MIGRATIONS.md was
      outside that session's writable set.
      THE CLAIM: docs/MIGRATIONS.md:184 states "`dev` inherits production's data
      shape; staging does not", with the "Which branch am I actually reading?"
      table listing preview/staging as "separately seeded".
      THE CONTRADICTION: branch preview/staging carries the SAME cuid
      (cmqx004mk001d3apdv3b6h4mj) as branch dev and branch production for the
      2026-06-27 Template row — identical id, identical createdAt to the
      millisecond. Rows are not independently seeded into the same cuid. The Neon
      console also shows preview/staging as a CHILD BRANCH of production
      (breadcrumb "production ↳ preview/staging", br-square-feather-a63z92vz),
      which is how it got them.
      WHAT THE EVIDENCE ACTUALLY SUPPORTS: the line was recorded 2026-07-29 from
      a zero-primary StaffMember query that genuinely differed between branches —
      four ACTIVE staff on preview/staging versus production's two. That is real,
      and it is evidence about StaffMember, which has DIVERGED since the branch
      point. It is not evidence that staging was seeded from scratch. Template
      shows the opposite: shared ancestry, plus staging-only divergence (a second
      org, cmr54z65v…, with 8 templates created 2026-07-11 that production has
      never had).
      THE FIX IS A NARROWING, not a deletion: staging is a Neon branch of
      production that has diverged per-table since its branch point. The
      operational advice built on top of the claim SURVIVES unchanged — a clean
      staging result is still not evidence about production, and both are still
      worth running — but the stated REASON is wrong, and a wrong reason is what
      lets someone conclude the opposite for a table they have not checked.
      DO IT: fold into whichever session next edits MIGRATIONS.md rather than
      standalone. Related: DEBT-1, DEBT-3.

  - id: DEBT-32
    status: shipped
    commits: ["597e86d"]
    title: "CLOSED — Retire the I-14b legacy phase alias and fold the three phase lists into one"
    notes: >
      SHIPPED TO PRODUCTION 2026-08-10 IN MERGE "ddaa216", inside CHK-2. Status
      corrected from `staging` by the CHK-4 bookkeeping rider, per the DEBT-54
      convention; "597e86d" is an ancestor of origin/main.
      THE FOLD HAS SINCE TAKEN ITS FOURTH READER AND HELD. CHK-3's
      expectedWindow() reads OPERATIONAL_PHASES through normalizePhase() rather
      than carrying a list of its own — the reader this row was folded ahead of
      — and CHK-4 added no fifth list. That is the row's own success condition
      observed one phase later, which is worth recording because "no new copy
      appeared" is the kind of outcome nothing reports unless someone looks.
      ── PRE-PROMOTION RECORD OF 2026-08-09 BELOW, UNEDITED ──
      CLOSURE CONFIRMED BY GARY'S STAGING PASS 2026-08-09 — org
      org_3G02wO4QlVVSWppi8aqlnSZnsDa, instance verified-snapper-7. Handoff
      notes behaved identically to pre-fold on the deployed code: opener→today,
      closer→tomorrow, ordering unchanged. The row was already closed in
      "5298d42" on measured evidence; this is the browser half confirming the
      harness, not a second closure.
      TWO CORRECTIONS TO THE BLOCK BELOW, WHICH IS OTHERWISE LEFT AS WRITTEN.
      (1) IT IS NO LONGER ONE STEP AHEAD. That block was written while both
      commits were unpushed and correctly said so; Gary has since pushed them,
      origin/staging is at "5298d42", and the pass above ran against that
      deployment. `status: staging` is now plainly true.
      (2) WHERE THE PROOF LIVES, since the block below states the method but not
      its location: the durable record is "597e86d"'s own commit message, which
      carries the full method and the 2,715-comparison count, plus the S2
      session report. THE HARNESS ITSELF WAS NOT PRESERVED — it ran from the
      session scratchpad and is gone. That is a real limit on this citation and
      it is stated rather than papered over: the numbers are re-derivable from
      the method as described, but the proof is not re-runnable as written. A
      future session wanting to re-verify rebuilds it from the commit message.
      ── CLOSURE AS WRITTEN 2026-08-09, UNEDITED ──
      CLOSED 2026-08-09 IN "597e86d", AS CHK-2'S OPENING MOVE. Both
      `"During Hours"` alias lines are deleted and the ordering map lives once,
      in src/lib/phases.ts, DERIVED from OPERATIONAL_PHASES rather than restated
      — so the fourth list this row worried about cannot drift from the
      canonical one, because it IS the canonical one. Three lists became one.
      `status: staging` IS ONE STEP AHEAD AND SAYS SO. The commit is on the local
      staging branch and has NOT been pushed — Claude never pushes — so no
      deployment is running this yet. It is recorded as `staging` because that is
      the branch it is committed to and the next promotion carries it; it is not
      a claim that anyone has observed it running. This is TPL-2's caveat rather
      than S1's inverse of it, and it is written down rather than left for a
      reader to assume the stronger meaning.
      THE BEHAVIOURALLY-INERT CLAIM WAS THIS ROW'S OWN, AND IT HELD — but it was
      PROVEN rather than trusted. The row said `phaseOrder` falls back to `?? 1`,
      the same value the alias maps to, so deleting both lines changes no output.
      HOW IT WAS PROVEN: the retired implementations were pasted verbatim from
      7031155 — including handoff-notes.tsx's separate hand-copied twin, which
      got its own comparison rather than being assumed identical to the
      messages.ts one — and run beside the shipped code over every input either
      can receive: the three canonical phases, the legacy string, null, empty,
      whitespace-padded, case variants and junk, crossed with the full source x
      target date rule across a month boundary, a year boundary, a leap day and
      both DST boundaries. 2,715 comparisons, ZERO differences. Six named cases
      were also asserted ABSOLUTELY, not merely as old-vs-new equality, since
      equality alone would still pass if both sides were wrong together:
      "During Hours" orders as mid and reports shift_phase mid; "Post to Closing"
      from an opener lands today; "Post to Opening" from a closer lands tomorrow;
      and the legacy string behaves identically in both the source and the target
      position.
      ONE THING THE ROW DID NOT ANTICIPATE, recorded because it is the near-miss:
      the obvious tidy implementation — routing phaseOrder through
      normalizePhase() — WOULD HAVE BROKEN THE INERTNESS. normalizePhase trims
      and the old maps did not, so a whitespace-padded "  Before Opening  " would
      have moved from MID to OPENING. It was rejected for that reason and the
      lookup stays untrimmed. The write paths already normalize; trimming belongs
      there, not in a read path that is claiming to change nothing.
      THE HAZARD FIRED EXACTLY AS PREDICTED AND WAS HANDLED AS INSTRUCTED. CHK-2
      scoped its eslint gate to exclude handoff-notes.tsx and said so in the
      commit message. Run on its own the file reports exactly the one DEBT-33
      baseline error and no other — so the session introduced none and, as
      required, fixed none. THE CITATION MOVED: :70:25 to :69:25, because this
      commit's edit is one line shorter than what it replaced. Corrected on
      DEBT-33.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      ABSORBED 2026-08-09 AS CHK-2'S OPENING MOVE. Plan:
      docs/prompts/CHK-1_PLAN.md §8, committed in "746918c". This is not a
      re-file and the work is unchanged — it is the same two alias lines and the
      same consolidation into src/lib/phases.ts, done at the front of a session
      that was going to open those files anyway.
      THE REASON IT MOVED IS THAT THE LIST COUNT WENT UP. DEBT-48 said the fold
      is "the natural thing to do first if this is built, since a gate should
      read one list rather than three". CHK-3's expectedWindow() reads
      operationalPhase to choose its anchor (Before Opening / During the Day /
      After Closing), which makes a FOURTH reader. Folding after that would be
      consolidating four lists instead of three, so it goes first — before the
      new consumer exists, not after.
      THE ROW STAYS OPEN AND CARRIES NO STATUS until CHK-2 ships, on the same
      reasoning as DEBT-36's and DEBT-48's 2026-08-09 riders: nothing has
      shipped, and no `status` value means "absorbed into a phase that has not
      built yet".
      THE HAZARD BELOW IS CARRIED FORWARD VERBATIM INTO CHK-2's ROW, not merely
      cross-referenced — handoff-notes.tsx holds one of DEBT-33's ten baseline
      lint errors at :70:25, so CHK-2 scopes its eslint gate to exclude that
      file AND says so in the commit message, rather than appearing to have
      fixed a DEBT-33 error it did not fix.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      COST OF DOING NOTHING: `phaseOrder` falls back to `?? 1`, the same value
      the alias maps to, so deleting both lines changes no output whatsoever —
      verified at HEAD, both lines still present at messages.ts:41 and
      handoff-notes.tsx:24 — and the standing cost is three phase lists where
      there should be one — NONE. A legitimate NONE: the row itself says the
      alias is behaviourally inert, which is why there is no hurry AND no risk.
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's.
      THE GATE CLEARED 2026-08-02, recorded by DEBT-TRIAGE-1 — THIS ROW DID NOT
      KNOW, and read as blocked when it is ready to run. "UNBLOCKED ONCE: DEBT-1
      is verified" below is satisfied on exactly the terms it set: staging → main
      WAS promoted, in 63407be on 2026-08-01 (docs/DEPLOY_LOG.md § "2026-08-01
      (afternoon)", pushed to origin/main 16:27), and production IS running the
      plugged writers rather than merely the clean data — DEBT-1's code commit
      c17ccc1 is on main, confirmed at HEAD by `git branch --contains`, and
      DEBT-1 is `status: verified` after Gary verified the promoted code in
      production the same day. This row is now free to run; nothing external
      blocks it.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      Created 2026-07-31 by the DEBT-1b task order, which put alias retirement
      explicitly OUT OF SCOPE: even with data clean and writers plugged, the
      alias stays as belt-and-braces until the fix has soaked in production.
      This row keeps it tracked without rushing it.
      WHAT RETIRES: the two hand-copied legacy mappings, "During Hours": 1, at
      src/lib/messages.ts:41 and
      src/app/(app)/store-view/checklist/[id]/handoff-notes.tsx:24. Both are
      READ-path ordering maps; neither was touched by DEBT-1b.
      THE ALIAS IS ALREADY BEHAVIOURALLY INERT — phaseOrder falls back to `?? 1`,
      which is the same value the alias maps to, so deleting both lines changes
      no output today. That is exactly why there is no hurry AND no risk: this is
      a readability change, not a behaviour change. It only stops being inert if
      some future call site treats an unknown phase differently from mid (a
      throw, or a fallback of 0).
      FOLD IN AT THE SAME TIME: DEBT-1b added a THIRD list,
      src/lib/phases.ts (OPERATIONAL_PHASES), deliberately free of Prisma and
      React imports so writers on both sides of the client boundary can share it.
      That is the natural home for the ordering map too — messages.ts pulls the
      Prisma runtime, which is the whole reason handoff-notes.tsx hand-copied its
      map in the first place. Retiring the alias without consolidating leaves
      three lists where there should be one.
      HAZARD: handoff-notes.tsx also carries one of the ten lint errors in
      DEBT-33 (:70:25, "Cannot call impure function during render"), so whoever
      opens that file will see a scoped lint failure that predates them.
      UNBLOCKED ONCE: DEBT-1 is verified — i.e. after staging → main is promoted
      and production is running the plugged writers, not merely the clean data.
      Related: DEBT-1, DEBT-30, DEBT-33.

  - id: DEBT-33
    title: "npm run lint exits 1 on a clean checkout — 10 errors, no commit gate can use it"
    notes: >
      POINTER REPAIR 2026-08-09 (CHK-2, "597e86d") — ONE OF THE TEN MOVED, AND
      NOTHING WAS FIXED. The impure-function error in
      src/app/(app)/store-view/checklist/[id]/handoff-notes.tsx now sits at
      **:69:25**, not :70:25. CHK-2's DEBT-32 fold deleted the hand-copied
      PHASE_ORDER map from that file and replaced it with an import, and the
      replacement is one line shorter — so the Date.now() call slid up by one.
      THE COUNT IS UNCHANGED AT TEN. This is a line number moving, not an error
      clearing: CHK-2 scoped its eslint gate to exclude that file precisely so it
      would neither be blocked by this error nor appear to have fixed it, and run
      on its own afterwards the file reports exactly this one error and no other.
      Repaired here rather than in the dated measurement below, which stands as
      written: `THE ELEVEN, at 48559fc` records what was true at that commit and
      is not edited. This block is the current pointer; that block is the
      history. Whoever finally clears these ten should search the file for
      `Date.now`, not for a line number — this is the second time a citation in
      this row has been invalidated by an unrelated edit to a file it names.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      COST OF DOING NOTHING: No commit gate in this repo can use
      `npm run lint` — measured at HEAD, exit 1, 32 problems, 10 errors — so
      every commit runs scoped eslint instead and a regression in an untouched
      file goes uncaught; nothing is known to be broken at runtime and every
      affected file ships today — LATENT (trigger: one of the react-hooks
      hazards actually manifesting, most likely as a pdf-viewer that does not
      update rather than as a crash; or the first session that genuinely needs a
      repo-wide lint gate and cannot have one).
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's. FIRST
      DERIVED AS LIVE AND MOVED TO LATENT BY GARY 2026-08-02 on the
      product-vs-process distinction now recorded above the `debt:` key: this
      cost falls on Gary and on Claude, not on a merchant using the app. The
      distinction is the definition, not a downgrade of the finding.
      RECORD HALF RELOCATED 2026-08-02 by DEBT-TRIAGE-2 → a comment atop
      src/components/hr/pdf-viewer.tsx, extending the existing HR-11 block: the
      HR-14 reading note. A roadmap row is never read by the person who opens
      that file, which is the only moment it matters. THE COMMIT-GATE RULE WAS
      NOT COPIED — it already lives at CLAUDE.md § Commit Gates, verified at
      HEAD, and duplicating it is how two copies drift. THIS ROW KEEPS ITS TASK:
      the ten react-hooks errors across five files in Labor, HR and the sidebar.
      THE ROW ATE ITS OWN DOG FOOD, recorded because it is the cleanest possible
      demonstration of why this row is open. Relocating that reading note into
      pdf-viewer.tsx could not pass the scoped-eslint commit gate THIS ROW
      CREATED: the file already carries three of the ten errors, so `npx eslint`
      on it exits 1 before and after. Ruled by Gary 2026-08-02 — a documented
      no-change measurement rather than a skipped check. Measured: 3 errors
      react-hooks/refs at :57 :58 :59 before, the same 3 at :75 :76 :77 after,
      exit 1 both times, zero new errors or warnings; the three statements are
      byte-identical and moved by exactly the 18 lines of the added comment. The
      other two code files in that commit gated clean at exit 0.
      TITLE CORRECTED 2026-08-02 by DEBT-TRIAGE-1: it said 11 errors, and HEAD
      cc1fffc measures 32 problems / 10 errors, exit 1 — `npx eslint .`, error
      lines counted independently of the summary. The BODY below already
      recorded the correction (the prefer-const was fixed in 89c70f7); only the
      title had gone stale, and a title stating a checkable fact that is wrong
      gets fixed. THE ROW STAYS OPEN AND THE STANDING RULE IS UNCHANGED: ten
      react-hooks errors remain, the baseline is still red, and no bare
      `npm run lint` may appear in any commit gate.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      Logged 2026-07-31 during DEBT-1b, which was ordered to add a bare
      `npm run lint` to its build-gates-commit chain and found the chain could
      never reach the commit. MEASURED, not inferred: stashing the session's
      changes and running eslint against a clean 48559fc gives the IDENTICAL
      result — exit 1, 33 problems, 11 errors. None of the eleven are in files
      DEBT-1b touched; its seven files lint clean (0 errors, 4 pre-existing
      warnings).
      TEN REMAIN, as of 2026-08-01 — the prefer-const was fixed in 89c70f7 and
      is struck from the list below. The row STAYS OPEN: the ten are react-hooks
      errors and they are the reason the gate is still banned. Measured before
      and after that commit, so the count is not inferred: src/lib/hr-signed-pdf.ts
      went 1 error to 0 (exit 1 to exit 0) and repo-wide eslint went 33 problems /
      11 errors to 32 problems / 10 errors. Exactly one moved.
      THE ELEVEN, at 48559fc:
      react-hooks/set-state-in-effect (6) —
      src/app/(app)/labor/weekly-plan-client.tsx:121:7 and :295:5;
      src/app/(app)/settings/labor/labor-settings-client.tsx:94:5, :228:19,
      :711:19; src/components/layout/sidebar.tsx:176:26.
      react-hooks/refs, "Cannot access refs during render" (3) —
      src/components/hr/pdf-viewer.tsx:57:3, :58:3, :59:3.
      react-hooks, "Cannot call impure function during render" (1) —
      src/app/(app)/store-view/checklist/[id]/handoff-notes.tsx:70:25.
      prefer-const (1) — src/lib/hr-signed-pdf.ts:373:11, the only one
      auto-fixable. DONE 2026-08-01 in 89c70f7 (`let size = 9` -> `const`).
      CAUSE: these are React Compiler rules judging code written before Next 16.
      Nothing is known to be broken at runtime — every one of these files ships
      today — but the rules are not cosmetic either; set-state-in-effect and
      refs-during-render describe real cascading-render and staleness hazards.
      READING FOR HR-14: TWO OF THE AFFECTED FILES SIT IN THE HR STAGING SURFACE
      — src/components/hr/pdf-viewer.tsx (three of the eleven) and
      src/lib/hr-signed-pdf.ts. Anyone touching HR document rendering or signed
      PDFs should read this row first; the pdf-viewer errors are about refs
      mutated during render, which is the kind of thing that shows up as a
      viewer that does not update rather than as a crash.
      STANDING-RULE CONSEQUENCE, in force now: NO BARE `npm run lint` IN ANY
      COMMIT GATE until this baseline clears. Scoped lint per commit is the
      interim rule — `npx eslint <the files this commit touches> && npm run build
      && git commit`, as DEBT-1b's three commits did. That keeps new code honest
      without blocking every commit on debt someone else left.
      DO IT: the prefer-const is done; the ten react-hooks errors are real
      refactors across five files in Labor, HR and the sidebar, and should not be
      bundled into an unrelated phase. Related: DEBT-32.

  - id: DEBT-34
    title: "A production connection string and five other prod secrets sat in froot/.env.backup"
    notes: >
      COST OF DOING NOTHING: The file was deleted and verified absent, so
      nothing is exposed today, and nothing structural changed — LATENT
      (trigger: the next deployed-environment credential written into the
      working tree by any route — a backup, an editor artifact, a
      `vercel env pull`. There is no platform setting behind the ban, DEBT-4
      records why Vercel will not accept the type change, and the only guard is
      that someone notices).
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's.
      FOUND 2026-08-01 during DEBT-2a, while that session was confirming its own
      database protocol — not by looking for it. DELETED BY GARY 2026-08-01, the
      same day, and verified absent.
      WHAT IT WAS: froot/.env.backup — untracked, gitignored (.gitignore:34,
      `.env*`), never committed, created 2026-07-26 22:00:16 PDT. Its
      DATABASE_URL pointed at ep-green-smoke-a6xthq4r-pooler, which
      MIGRATIONS.md:180 identifies as PRODUCTION. It also carried
      CLERK_SECRET_KEY, SQUARE_ACCESS_TOKEN, BLOB_READ_WRITE_TOKEN,
      HR_BLOB_READ_WRITE_TOKEN and CRON_SECRET.
      NOBODY VIOLATED ANYTHING. The file predates the 2026-07-28 total ban on
      deployed-environment credentials on disk (CLAUDE.md § Environment
      Variables) by two days. It is the exact artefact that ruling was written
      to prevent, and it was still sitting there three days after the ruling
      landed. Only the hostname and the key NAMES were read; no value was read,
      loaded or used, and the audit's query scripts pinned dotenv to .env
      explicitly so this file could not be picked up.
      WHY IT STAYS OPEN AFTER THE DELETION: one file was found and removed, and
      nothing structural changed. The ban is a rule with no platform setting
      behind it (DEBT-4 — Vercel will not accept the type change on
      DATABASE_URL_UNPOOLED), so the only guard is that someone notices. FIX
      SHAPE: a pre-commit or session-start check that greps the working tree for
      a non-dev Neon endpoint and fails loudly. Cheap, and it would have caught
      this on 2026-07-26. Related: DEBT-4, and CLAUDE.md § Environment Variables.

  - id: DEBT-35
    status: shipped
    commits: ["3d57fb6"]
    title: "The migrations folder cannot rebuild the live schema — Task.estimatedTimeMinutes"
    notes: >
      COST OF DOING NOTHING: Every environment is fine today only because none
      of them was built from these migrations, so nothing is observable —
      LATENT (trigger: the baseline squash in MIGRATIONS.md §2, or any database
      built from an empty state by `migrate deploy`. Note the trigger is worse
      than it looks: the squash regenerates from schema.prisma, so it will
      silently ADOPT the live type and erase the evidence that the ledger ever
      disagreed, rather than failing).
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's.
      WRITTEN INTO A CLOSED ROW DELIBERATELY, on Gary's ruling: a closed row's
      cost is what tells a reader whether the relocation was the right call, and
      here it is also the argument for WHERE it went — §2 is the one operation
      this bites.
      CLOSED BY RELOCATION 2026-08-02 by DEBT-TRIAGE-2, on Gary's ruling.
      RELOCATION CLOSES, DELETION LOSES: nothing below is removed.
      WHERE IT WENT → docs/MIGRATIONS.md §2 (the one-time baseline squash), as a
      ⚠️ callout directly beneath the existing one. §2 already carried a callout
      of exactly this shape — "re-append both partial indexes or the baseline is
      wrong" — and § Protected indexes already had "Hazard 1 — the baseline
      squash silently drops them", so this is the same pattern in the file that
      set it. The squash is the only operation this bites, and §2 is where
      someone stands before doing it.
      LABEL NOTE, since the DEBT-TRIAGE-2 prompt called this "Hazard 3": that
      name went to DEBT-9's relocation instead, which continues the existing
      numbered Hazard 1 / Hazard 2 series under § Protected indexes. §2 has no
      numbered hazards — its idiom is the ⚠️ callout — so this landed as a
      callout there. Same section as instructed; only the label differs, and it
      is reported rather than improvised silently.
      SHARPENED IN THE RELOCATION, and this is new reasoning rather than a
      transcription: the squash regenerates 0_init with `--to-schema-datamodel`,
      i.e. FROM schema.prisma, which says `Float?` — so the regenerated baseline
      will say DOUBLE PRECISION, match the live databases, and ERASE THE EVIDENCE
      that the ledger ever disagreed. The squash is simultaneously the operation
      that would surface this and the operation that makes it unfindable.
      Nothing fails; the discrepancy just stops existing. The relocated callout
      therefore instructs diffing the regenerated baseline against a live
      branch's actual column types BEFORE marking it applied, and treating
      estimatedTimeMinutes as one KNOWN instance rather than the only one —
      anything else from the same `db push` era has the identical signature.
      STATUS VALUE: `shipped`, following DEBT-31 and DEBT-19 — a documentation
      change rather than a code change.
      MEASURED 2026-08-01 during DEBT-2a, on branch dev, while cataloguing the
      Task table. CONFIRMS THE LONG-STANDING estimatedTimeMinutes AUDIT ITEM —
      this is corroboration of a known discrepancy, NOT a new discovery, and the
      row should be read as closing the question of whether it was real rather
      than as opening a new one.
      THE FACT, three sources and one of them disagrees: ALL THREE BRANCHES —
      dev, preview/staging and production — have Task.estimatedTimeMinutes as
      `double precision`; prisma/schema.prisma:311 says `Float?`; and the ONLY
      migration that ever creates the column —
      prisma/migrations/20260627002005_init/migration.sql:132 — says `INTEGER`.
      No migration alters it afterwards. So the schema and the live databases
      agree with each other, and the LEDGER is the one that is wrong. CONFIRMED
      ON ALL THREE BRANCHES 2026-08-01: not a local artefact, not a staging
      artefact — whatever changed the type reached PRODUCTION.
      WHY IT MATTERS: `prisma migrate deploy` from an empty database would
      produce a Task table that does not match schema.prisma. Every environment
      is fine today because none of them was built that way — but the baseline
      squash (MIGRATIONS.md §2, still not done) is exactly the operation that
      would surface it, and `migrate dev` is already broken for the same
      underlying reason (P3018, no baseline).
      CAUSE, consistent with the record: a `db push` from before that command was
      retired after the 2026-07-06 staging drift incident. Not proven, and the
      disconfirmer is cheap — a migration somewhere altering the column type
      would refute it; none exists.
      MEASURED ON ALL THREE, so nothing here is inferred: P8 on production
      returned the same 11 columns and the same 10 constraints as dev and
      staging, estimatedTimeMinutes included. Related: DEBT-3, MIGRATIONS.md §2.

  - id: DEBT-36
    status: shipped
    commits: ["4fd9152"]
    title: "CLOSED — Sections are implied, not modelled — no entity, no ordering, no as-executed record"
    notes: >
      SHIPPED TO PRODUCTION 2026-08-10 IN MERGE "ddaa216", inside CHK-1, WITH
      MIGRATION A's BACKFILL CONFIRMED ON `br-sparkling-block-a620qvg4`
      (unlinked_tasks 0, sections 32 — Gary, 2026-08-10). Status corrected from
      `staging` by the CHK-4 bookkeeping rider — which is EXACTLY THE STEP THIS
      ROW WROTE DOWN FOR ITSELF two paragraphs below ("it becomes `shipped` with
      the production promotion, like every other debt row"), so the flip is the
      row's own instruction being followed rather than a judgement call.
      ── PRE-PROMOTION RECORD OF 2026-08-09 BELOW, UNEDITED ──
      CLOSED 2026-08-09 IN "4fd9152", VERIFIED ON STAGING THE SAME DAY. The
      rider in "3ec9bc8" said the acceptance-for-now ends when S1 ships. It
      shipped, and the trigger-test passed — so this row closes on the terms it
      set itself rather than on a judgement call. `status: staging` is what
      isResolvedDebt reads (roadmap-client.tsx:71); it becomes `shipped` with
      the production promotion, like every other debt row. Debt rows carry no
      `shipped:` field.
      ALL THREE HALVES OF THE DEFECT ARE ANSWERED. (1) NO ENTITY → `Section`,
      per-template, stable ids, @@unique([templateId, name]). (2) NO ORDERING →
      explicit `sortOrder`, RECOVERED from MIN(orderIndex) rather than
      invented, so the entity reproduces the old render order by construction;
      the double heading a non-contiguous section used to produce in the
      template form is gone. (3) NO AS-EXECUTED RECORD →
      `Checklist.sectionsSnapshot`, frozen ONCE at the first task log and never
      rewritten. Evidence and the browser pass are on CHK-1.
      THE LATENT TRIGGER WAS FIRED DELIBERATELY, WHICH IS WHY THIS CLOSES
      RATHER THAN LAPSES. The block below says the acceptance "does not survive
      the trigger" and treats the first rename as the thing to fear. CHK-1's
      pass pulled it on purpose: a checklist completed post-deploy, its section
      renamed, and the print copy held the OLD name while the template showed
      the new. The trigger is now the test, exactly as the 2026-08-09 scoping
      note said it would become.
      TWO LIMITS ON THIS CLOSURE, RESTATED HERE SO IT IS NOT READ WIDER THAN IT
      IS — both were true when the row was scoped and neither changed:
      (a) SCOPE IS NAMES ONLY (Gary, 2026-08-09). Task descriptions, task
      additions and task flags STILL resolve live at print time, so a printed
      checklist from last month still re-prints with a task added yesterday.
      That is the same mechanism one layer out, it is wider than this row ever
      described, and it is DEBT-60 — not something this closure covers.
      (b) A CHECKLIST COMPLETED BEFORE THE CHK-1 DEPLOY HAS NO SNAPSHOT and
      therefore still renders LIVE section names; a rename still rewrites ITS
      headings. UNRECOVERABLE RATHER THAN UNFIXED: what those checklists were
      printed under is not written down anywhere, and fabricating it would make
      a guess indistinguishable from a record — the exact defect this row is
      about (plan §12.5). The as-executed record starts at deploy. Also stated
      at docs/prompts/CHK-1_PLAN.md §2.4, prisma/schema.prisma
      (Checklist.sectionsSnapshot) and src/app/print/checklist/[id]/page.tsx.
      `Task.sectionName` SURVIVES this row as a legacy mirror, kept written on
      every write path — TPL-1a's shape. Retiring the column is a later row and
      was never DEBT-36's business.
      ── EVERYTHING BELOW IS PRESERVED UNEDITED, INCLUDING THE TWO BLOCKS THAT
      SAY THIS ROW DOES NOT CLOSE. They were true when written; this block is
      where that expires. ──
      IN FLIGHT 2026-08-09 — CHK-1's S1 IS BUILT AND COMMITTED ("4fd9152"),
      AND THIS ROW STILL DOES NOT CLOSE. The Section entity, `Task.sectionId`,
      `TaskLog.sectionId` and `Checklist.sectionsSnapshot` all exist in code;
      the migration that creates them is HAND-AUTHORED AND UNRUN on every
      branch, so on the day this line is written the defect below is still
      live everywhere and the acceptance is still being paid. It closes when
      the backfill is proven on the three live branches and the rename trigger
      has been fired on staging — CHK-5 carries the instruction, per the block
      below, and nothing about that changes here.
      ONE THING TO CARRY INTO THE VERIFICATION, because it is this row's own
      latent trigger stated as a limitation: A CHECKLIST COMPLETED BEFORE THE
      CHK-1 DEPLOY HAS NO SNAPSHOT and therefore still renders LIVE section
      names — a rename still rewrites ITS headings. That is unrecoverable
      rather than unfixed: what those checklists were printed under is not
      written down anywhere, and fabricating it is the defect this row is
      about (plan §12.5). The as-executed record starts at deploy.
      ── SCOPING NOTE OF 2026-08-09 BELOW, UNEDITED ──
      SCOPED 2026-08-09 — THE ENTITY WORK IS NOW CHK-1, AND THIS ROW STAYS OPEN
      UNTIL IT SHIPS. The feature phase this row was parked for was scoped and
      approved: docs/prompts/CHK-1_PLAN.md, committed in "746918c". The Section
      entity — per-template, stable ids, explicit sortOrder — is CHK-1 (S1), and
      the as-executed record this row says does not exist anywhere in the system
      arrives with it as Checklist.sectionsSnapshot.
      THE ROW IS NOT CLOSED AND THE STATUS IS NOT SET, DELIBERATELY. `status`
      on a debt row means staging/shipped/verified/withdrawn (roadmap.ts
      DebtItem; isResolvedDebt in roadmap-client.tsx:71 is the single
      definition). Nothing has shipped, nothing is retracted, and there is no
      value meaning "scoped into a phase that has not built yet" — the same gap
      the header NOTE records for audit-only phases ("no status fits… a future
      'closed'/'documented' would"). Setting any existing value would move this
      row into the RESOLVED list while the defect is still live on every branch.
      It closes when CHK-1 ships, and CHK-5 carries the instruction to do it.
      WHAT THE ACCEPTANCE COSTS IS STILL BEING PAID UNTIL THEN. The 2026-08-07
      ruling below accepted renames-rewrite-history FOR NOW; "now" has a defined
      end but has not arrived. The latent trigger also stands, with one change
      worth naming: CHK-1's regression plan FIRES IT DELIBERATELY on staging —
      rename a section on a template with completed history and prove the
      historical checklist and its print copy do not move. The first rename is
      no longer something to fear; it is the test.
      TWO THINGS THIS ROW ASKED FOR THAT CHK-1 DOES NOT DO, so the closure is
      not read as wider than it is. (1) The snapshot scope is NAMES ONLY (Gary,
      2026-08-09). Task descriptions and task additions still resolve live at
      print time, which is the same mechanism one layer out and is DEBT-60.
      (2) Task.sectionName is KEPT AND KEPT WRITTEN as a legacy mirror, the
      TPL-1a shape — retiring the column is a later row, not CHK-1's business.
      ── RULING OF 2026-08-07 BELOW, UNEDITED ──
      RULED 2026-08-07 — RENAMES-REWRITE-HISTORY IS ACCEPTED FOR NOW, AND THE
      SECTION ENTITY IS INTENDED WORK RATHER THAN A DECLINED OPTION. Gary took
      the second of the two directions this row put to him, but not immediately:
      the entity is QUEUED behind the current minor-fix batch, and when it comes
      up it is to be scoped as a FEATURE PHASE together with DEBT-48's
      availability window — not as a debt cleanup and not as a rider. Both are
      template-surface product work that outgrew a debt row, and scoping them in
      one sitting is the point.
      WHAT THE ACCEPTANCE COSTS IN THE MEANTIME, stated so it is carried
      knowingly: until the entity exists, a section rename still retroactively
      rewrites the headings on every historical completed checklist and every
      print copy, and there is still no as-executed record of section names
      anywhere in the system.
      THE LATENT TRIGGER STANDS UNCHANGED and is the thing to watch: the first
      section rename on a template that already has completed historical
      checklists. If that fires before the phase is scoped it ESCALATES THE
      PRIORITY — what was accepted above is a LATENT defect, not a fired one,
      and the acceptance does not survive the trigger.
      Row stays OPEN. Title prefix changed from AWAITING RULING to RULED;
      nothing below this block is edited.
      COST OF DOING NOTHING: There is no as-executed record of section names
      anywhere in the system, and nobody has renamed one yet — LATENT (trigger:
      the first section rename on a template that already has completed
      historical checklists, which silently rewrites their headings and their
      print copies, with no indication anything changed. For an
      operational-compliance product a printed closing checklist that re-prints
      under a heading it was never executed under is the part that matters).
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's.
      RECORD HALF RELOCATED 2026-08-02 by DEBT-TRIAGE-2 → prisma/schema.prisma,
      as comments on BOTH Task.sectionName and the TaskLog model — the compliance
      fact that renames rewrite history and that there is no as-executed record
      of section names anywhere. TaskLog gets the other half because the note
      there is the ABSENCE of a section column, which is where a reader would
      look for the record and not find it. THE ENTITY WORK STAYS HERE, AWAITING
      RULING: a comment records a fact, not a decision.
      AWAITING GARY'S RULING: whether to model Section as a first-class entity
      with stable ids, accepting a migration plus UI, or to accept that section
      renames retroactively rewrite historical checklists.
      THIS BLOCK IS THE FIRST RECORD OF THAT DEFERRAL, NOT A TRANSCRIPTION OF AN
      EXISTING ONE — stated plainly because the other three AWAITING RULING rows
      are transcriptions and this one is not. Ruled by Gary 2026-08-02 in the
      DEBT-TRIAGE-1 task order: the entity work waits until after DEBT-2b, and
      until he decides whether renames rewriting historical checklists is
      acceptable. Before today the row carried only a sequencing note ("Sequence
      it after DEBT-2b") and no ruling at all, so it read as undone work rather
      than as a decision pending on him.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      FILED 2026-08-01 from DEBT-2a's findings; see docs/prompts/DEBT-2_AUDIT.md §2 and
      §6. This is the DURABLE fix for the class DEBT-2 describes, and it is
      deliberately NOT part of DEBT-2b — it is a product decision plus a
      migration plus a UI, not a debt cleanup. Do not let it ride along.
      WHAT IS MISSING: there is no Section entity. A section is a GROUP BY over
      a free-text string, recomputed independently at five render sites. So
      there is no canonical list, no rename, no reorder, and no per-section
      metadata. Section ORDER is not expressible at all — it is inferred from
      orderIndex adjacency (template-form.tsx:489), which is why a section whose
      tasks are non-contiguous renders two identical headings from data that has
      not drifted at all.
      THE FINDING THAT MAKES THIS MORE THAN COSMETIC — CHECKLIST RENAMES REWRITE
      HISTORY. Checklist does NOT snapshot task data: api/checklists/route.ts:122
      and :156 store only ids, and TaskLog carries no section. So renaming a
      section retroactively rewrites the headings on EVERY historical completed
      checklist and its print copy. There is no as-executed record of section
      names anywhere in the system. A printed closing checklist from last month
      will re-print today under whatever the section is called today, with no
      indication anything changed. For an operational-compliance product that is
      the part worth fixing, and an entity with stable ids is what fixes it.
      SCOPE NOTE: an entity also subsumes the ordering limitation above, and
      makes DEBT-2b's step 3 (one empty-value string across five sites)
      unnecessary rather than merely consistent. Sequence it after DEBT-2b, not
      instead of it. Related: DEBT-2, DEBT-30.

  - id: DEBT-37
    title: "columnForStatus has no board column for `withdrawn` and silently falls through to Planned"
    notes: >
      COST OF DOING NOTHING: Unreachable today — verified at HEAD that
      BOARD_COLUMNS still has no `withdrawn` column, and that the only two rows
      carrying the status are DEBT rows, which render through BugsAndDebt/DebtRow
      and never reach columnForStatus — LATENT (trigger: the first PHASE, not a
      debt row and not a bug, given `status: withdrawn`. It will render under
      Planned, the most wrong of the three available answers, and it will fail
      quietly as a card in the wrong column rather than as an error).
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's.
      FILED 2026-08-01 by Gary's instruction, from the DEBT-SWEEP session that
      added the `withdrawn` status (cf0b044). Found during that work, stated in
      the session report, and filed rather than fixed BECAUSE a report is not
      the repo: an observation that lives only in a transcript does not exist.
      THE FACT: BOARD_COLUMNS (src/lib/roadmap.ts) maps planned/in_progress ->
      Planned, staging -> In staging, shipped/verified -> In production. It
      claims no column for `withdrawn`. columnForStatus loops those columns and
      returns "planned" when nothing matches, so a withdrawn PHASE would render
      on the board as Planned — the most wrong of the three available answers,
      since withdrawn is the opposite of upcoming.
      WHY IT IS UNREACHABLE TODAY, which is why this is a row and not a bug:
      columnForStatus is called from exactly one place
      (roadmap-client.tsx, the board grid) and only ever over `phases`. The two
      rows carrying `withdrawn` are DEBT rows, which render through
      BugsAndDebt/DebtRow and never touch columnForStatus. No phase has the
      status and none is expected to.
      THE TRIGGER THAT MAKES IT REACHABLE is therefore specific and worth
      naming: the first time a PHASE (not a debt row, not a bug) is given
      `status: withdrawn`. That is a plausible thing to want — a planned phase
      that gets abandoned is exactly the case the value was invented for — and
      it will fail quietly, as a card in the wrong column, not as an error.
      DELIBERATELY OUT OF SCOPE OF THE RULING THAT CREATED IT. Gary's
      `withdrawn` ruling (2026-08-01) scoped the work to the type, isResolvedDebt
      and a one-word label, and said to report-and-skip anything larger. The
      honest fix here is a fourth board column (or an explicit exclusion from
      the board), which is UI beyond that scope. Filed at full size instead of
      being half-done inside a sweep.
      FIX SHAPE when picked up: either add a Withdrawn column to BOARD_COLUMNS,
      or have the board filter withdrawn phases out explicitly rather than let
      them land in Planned by fallback. Prefer whichever makes the fallthrough
      impossible to reach silently — the defect is the silence, not the column
      count. Related: DEBT-18, DEBT-23, DEBT-26.

  - id: DEBT-38
    status: shipped
    commits: ["7d984be"]
    title: "CLOSED — A code-carrying production promotion went unlogged for three days — fast-forward promotions leave no artifact that prompts a DEPLOY_LOG entry"
    notes: >
      SECOND GENUINE RECURRENCE, 2026-08-16 — FIVE CONSECUTIVE UNLOGGED
      FAST-FORWARD PROMOTIONS. Prepended per preserve-and-mark; nothing below is
      edited. Session:
      docs/prompts/BUG-7_CLOSURE_AND_DEPLOY_LOG_RECONCILIATION.md + Addendum A.
      The five retroactive docs/DEPLOY_LOG.md entries were written in that
      session; this row records the mechanism.
      THE FIVE, from `git reflog show main --date=iso`, given as the instant MAIN
      MOVED (Pacific) with the tip commit's own authored time beside it, because
      the difference between those two columns is this row's own finding:
        882d6c3  main moved 2026-08-11 10:24:36 PT  ·  tip authored 09:26:53 PT
        ec42265  main moved 2026-08-11 16:27:24 PT  ·  tip authored 16:11:02 PT
        b853787  main moved 2026-08-12 12:33:26 PT  ·  tip authored 11:58:03 PT
        ce036f9  main moved 2026-08-12 21:50:11 PT  ·  tip authored 21:01:07 PT
        06dc830  main moved 2026-08-14 08:05:16 PT  ·  tip authored 2026-08-13
                                                       21:20:08 PT
      main@{0} through main@{4} are all "merge staging: Fast-forward"; the last
      real merge is f318d2e at main@{5}. Forty-three commits reached production
      across the five, in ranges 11 + 2 + 13 + 7 + 10.
      THE MECHANISM IS END-OF-WORK-BLOCK PUSHING, NOT A LOGGING LAPSE, and
      filing it as a logging lapse would put the remedy in the wrong place. Four
      of the five promotions moved main within an hour of their own last commit —
      finish the block, promote, stop. That is a working RHYTHM, and a rhythm
      does not get fixed by resolving to remember something. A fast-forward
      leaves no merge commit, so nothing in the flow ever pauses to prompt an
      entry: there is no artifact, on the platform or in git, between "push" and
      "the next session".
      FOUR-PLUS-ONE, NOT FIVE — AND THE CORRECTION MATTERS MORE THAN THE FACT.
      The fifth promotion is the exception to the rhythm: 06dc830 was authored
      2026-08-13 21:20:08 PT and sat overnight, reaching main 2026-08-14
      08:05:16 PT, about 10 h 45 m later. The session prompt that commissioned
      this entry asserted all five were end-of-block and dated the fifth "Aug 13
      evening". IT HAD READ A COMMIT TIME AS A PROMOTION TIME. That is the
      CLAUDE.md § Database Evidence timestamp trap in a form that section does
      not name — not UTC-versus-local this time, but authored-versus-applied,
      two Pacific timestamps that are both real, both correctly transcribed, and
      ten hours apart. It produced a tidier story than the truth (five identical
      end-of-block pushes) and nothing about it looked wrong. Caught by running
      `git reflog show main --date=iso` rather than inferring promotion times
      from `git log`. THE REFLOG IS THE AUTHORITY ON WHEN MAIN MOVED; a commit
      time is the authority on nothing but itself.
      FIVE CONSECUTIVE VIOLATIONS OF THE --no-ff RULE THIS ROW ITSELF ADDED IN
      7d984be, and therefore five production states with no one-line rollback.
      Each of the five DEPLOY_LOG entries written this session has to carry
      "FAST-FORWARD, not a merge — `git revert -m 1` does not apply" over a
      hand-assembled reverse-order revert list of 11, 2, 13, 7 and 10 commits —
      which is precisely the artefact this row's LATENT cost line below predicted
      ("exactly what you do not want to be assembling under pressure"), now
      produced five times.
      THE ROW STAYS `shipped`, AND THE REASON IS THE FINDING. 7d984be is in
      production and WORKFLOW.md §2 still says `git merge staging --no-ff
      --no-edit` with the DEPLOY_LOG add+commit inside the command block. The
      mechanism is present, correct, and was not defeated — IT WAS NOT RUN. A
      fast-forward is only possible if `--no-ff` was not passed, so these five
      promotions did not go through §2 at all. Reopening this row would imply the
      remedy is wrong; it is not. What is missing is a step that makes the
      promotion procedure READ the document, which is the same shape as the
      failure recorded in docs/DECISIONS.md 2026-08-16 and is filed as DEBT-72
      rather than absorbed here — one contained change per row.
      NOT A THIRD OCCURRENCE OF DEBT-23'S REASONING, which stays withdrawn and
      stays false. As with the first recurrence below, this was established from
      the reflog, independently, and the DEPLOY_LOG heading order was read with
      `grep -n "^## "` rather than with `tail` — the file is
      reverse-chronological and its newest heading is a STAGING entry newer than
      all five of these promotions, which is exactly the trap that produced
      DEBT-23 and would have put five Aug-11-to-14 entries above an Aug-15 one.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      SHIPPED TO PRODUCTION 2026-08-07 in merge "fad9207" — AND ITS FIRST
      EXERCISE FOUND A GAP IN ITSELF, NOW FIXED. The same promotion that
      carried this fix was the first run under it, and it worked: "fad9207" is
      a real merge commit with two parents, where the two promotions before it
      had to record "FAST-FORWARD, not a merge — git revert -m 1 does not
      apply" over hand-assembled 32- and 28-commit revert lists.
      BUT the one-line rollback §2 promised does not run. Tested rather than
      assumed on the day: `git revert -m 1 fad9207` conflicts on
      docs/DEPLOY_LOG.md — 37 of 38 files revert cleanly — and it will do so on
      every future promotion, because §2 has the log entry committed on main
      AFTER the merge while the promotion set also touches that file. The
      corrected three-line recipe, plus the rule that the log is KEPT rather
      than reverted, is now in WORKFLOW.md §2 under "Rolling a promotion back";
      the measurement lives in the "fad9207" DEPLOY_LOG entry.
      A SECOND GAP, same section, same day: §2's command block read checkout /
      pull / merge / push / checkout, with the DEPLOY_LOG add+commit only in
      the prose beside it — so copying the block literally pushed without
      committing the entry, which is this row's own failure mode. The commands
      are now in the block.
      THE ROW CLOSES WITH BOTH GAPS FIXED, NOT HIDDEN. It is filed as shipped
      because the fix is in production and works; the corrections above are
      recorded here rather than in a new row because they are defects in this
      row's own remedy, found by using it, and a reader asking "did DEBT-38
      actually work" needs both halves in one place.
      ── ORIGINAL CLOSURE NOTES BELOW, UNEDITED ──
      CLOSED 2026-08-07 — RULED, AND BOTH FIX DIRECTIONS WERE TAKEN. The row
      offered (a) a DEPLOY_LOG step inside WORKFLOW.md §2 and (b) `--no-ff` on
      promotions, and its own last word was that they are not exclusive. Gary
      ruled BOTH, applied in "7d984be": §2 now writes the docs/DEPLOY_LOG.md
      entry BETWEEN the merge and the push — a step OF the promotion rather than
      a thing remembered afterwards — and the promotion merge is
      `git merge staging --no-ff --no-edit`, which restores the merge commit as
      the artifact that prompts the entry, restores `git log --merges` as a
      reconstruction tool, and makes `git revert -m 1 <merge-sha>` the rollback
      again.
      WHY BOTH RATHER THAN THE CHEAPER ONE ALONE: (a) by itself leaves the
      reconstruction and rollback halves broken — `git log --merges` would still
      return none of these promotions — while (b) by itself restores the
      artifact without saying what to do when you see it. Each covers the
      other's gap, which is what "not exclusive" meant.
      TIMING, AND IT IS WHY THIS WAS RULED NOW RATHER THAN LATER: at ruling time
      staging was SEVENTEEN commits ahead of main. This row's stated trigger is
      THE NEXT PRODUCTION PROMOTION, and a delta that size means the trigger is
      close. The fix lands BEFORE it fires rather than after — which was the
      entire argument for not carrying this row another cycle.
      STATUS IS `staging`, NOT `shipped`: "7d984be" is committed on the staging
      branch and has been neither pushed nor promoted. Flip to `shipped` when it
      reaches main — and note that under the fix itself, that promotion will
      write its own DEPLOY_LOG entry. First use of the mechanism is the
      promotion that carries it.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      COST OF DOING NOTHING: DEPLOY_LOG is complete today — the missing entry
      was written 2026-08-01 — and the mechanism that lost it for three days is
      entirely untouched — LATENT (trigger: the next production promotion, which
      will be a fast-forward, will create no merge commit, and so will produce no
      artifact prompting anyone to write the entry. A second consequence rides
      the same trigger: rollback stays a hand-assembled reverse-order revert
      range instead of a one-line `revert -m 1`, which is exactly what you do not
      want to be assembling under pressure).
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's.
      AWAITING GARY'S RULING: fix direction (a) a DEPLOY_LOG step inside
      WORKFLOW.md §2, or (b) `--no-ff` on promotions — or both. Title prefixed
      2026-08-02 by DEBT-TRIAGE-1 so the row stops reading as backlog; the
      deferral is the row's own ("the choice is Gary's"), transcribed, not new.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      FILED 2026-08-01 by DOCS-2, on Gary's rider. The promotion 746c1be
      (2026-07-29 08:24, twenty-one commits carrying PERM-6, PERM-7, DEBT-8,
      DEBT-10 and DEBT-14) reached production with NO docs/DEPLOY_LOG.md entry
      and stayed that way for three days. Its entry was written 2026-08-01, at
      the same time as the entries for 63407be and 97ed309.
      HOW IT WAS FOUND, which is the uncomfortable part: from
      `git reflog show main`, NOT from DEPLOY_LOG and NOT from ROADMAP. Neither
      document contained anything that pointed at it. It surfaced only because
      DOCS-2 was independently reconstructing every promotion from git and the
      reflog showed five pushes where the documents accounted for two. Nothing
      in the normal reading path would have surfaced it.
      NOT EVIDENCE FOR DEBT-23'S REASONING, stated explicitly because the
      temptation to resurrect it is real. DEBT-23 claimed a missing 2026-07-27
      entry and a "second occurrence" pattern; BOTH were false and it was
      WITHDRAWN — the entry it called missing was the first heading in the file,
      and the error was a `tail` on a file whose reverse-chronological order was
      never checked. That row stays withdrawn and its reasoning stays false.
      THIS row is the FIRST genuine recurrence, established independently from
      the reflog rather than inherited from that argument. DEBT-23's surviving
      instruction — "Writing this row down is not the same as closing it" — is
      what this row exists to honour.
      THE MECHANISM IS A PROCESS GAP, NOT AN ATTENTION LAPSE, and this is the
      part that matters. FIVE of the seven pushes to main since 06b1561 were
      FAST-FORWARDS: 17dc723, 746c1be, 18220bb, 493175e, 63407be and 97ed309 all
      moved main without creating a merge commit. WORKFLOW.md §2's
      `git merge staging --no-edit` produces no merge commit when main has not
      moved independently, so there is (1) no merge commit to serve as the
      artifact that prompts someone to write the entry, and (2) nothing for
      `git log --merges` to return when reconstructing after the fact — it finds
      NONE of these promotions. The older entries in DEPLOY_LOG all have merge
      SHAs and `git revert -m 1` rollbacks precisely because main used to
      diverge. It stopped diverging, and the habit that depended on that
      silently stopped working.
      CONSEQUENCE: this WILL recur until something changes. It is also why every
      recent DEPLOY_LOG entry has to spell out a reverse-order revert range
      instead of a one-line `revert -m 1`.
      FIX DIRECTION IS AN OPEN QUESTION FOR GARY, not a decision taken here.
      Two candidates: (a) a step in WORKFLOW.md §2 that makes writing the
      DEPLOY_LOG entry part of the promotion itself rather than a thing
      remembered afterwards, or (b) `--no-ff` on promotions, restoring the merge
      commit as both the artifact and the rollback handle. (b) also restores
      `git log --merges` as a reconstruction tool and makes `revert -m 1` work
      again; (a) is cheaper and touches no git behaviour. They are not exclusive.
      WORKFLOW.md was deliberately NOT edited by DOCS-2 — the choice is Gary's.
      Related: DEBT-23 (withdrawn, and not the basis for this row), DEBT-24.

  - id: DEBT-39
    title: "Two writers create User rows and only ONE derives the device name — a page-won acceptance race skips PERM-7 Task 7 permanently and silently"
    notes: >
      COST OF DOING NOTHING: Clerk collects no name on any observed path, so
      whichever writer wins the race the visible damage is a blank name rather
      than a person's — LATENT (trigger: `first_name`/`last_name` made REQUIRED
      in the Clerk dashboard, or any new sign-up path that collects a name.
      CHECK IT AS ONE TRIGGER WITH DEBT-40 AND PERM-7 TASK 7 — the same
      configuration change unmasks all three at once, and after it this race
      starts putting a person's name on a device account).
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's. LIMIT
      STATED RATHER THAN GLOSSED: nobody has measured which writer actually wins
      in practice, so a device account showing a blank name TODAY is possible
      and unobserved. That does not change the verdict — blank is not a person's
      name — but it is the part of this row nobody has checked.
      RECORD HALF RELOCATED 2026-08-02 by DEBT-TRIAGE-2 → docs/DECISIONS.md,
      appended to the PERM-7 Task 7 section as "The reopening condition — and it
      fires in the Clerk dashboard, not in this repo". That is the point of the
      move: the trigger is a settings change made by a person standing in the
      Clerk dashboard, and nobody standing there reads ROADMAP.yaml. It is
      written as ONE trigger unmasking two things, per this row's own
      instruction. THIS ROW KEEPS ITS TASK — mirror NAME as DEBT-17 mirrored
      ROLE, so either winner of the race produces the same row.
      FILED 2026-08-01, on Gary's PERM-7b ruling, which was given and never
      applied because that session stopped at part one. The finding is from
      PERM-7b's audit; the filing is PERM-7c's.
      THE RACE. Two writers can create the User row for an accepted invite, and
      whichever runs first wins. (1) The Clerk webhook,
      src/app/api/webhooks/clerk/route.ts — its upsert create branch at :151-153
      derives the device name (`deviceName ?? …`, deviceName built at :126-137)
      when the invite is role STORE with exactly one store. That is PERM-7 Task
      7. (2) The /users page, src/app/(app)/users/page.tsx:84 — its create
      branch writes `name: [pub.firstName, pub.lastName].filter(Boolean).join("
      ") || null` with NO deviceName derivation at all.
      WHY IT IS PERMANENT AND SILENT. The webhook's UPDATE branch is
      `update: { email: userEmail }` (route.ts:146) and deliberately never
      writes name — create-only is the Ruling 4 guard that stops later Clerk
      events resetting a corrected name (DECISIONS.md 2026-07-28). So if the
      page creates the row first, the webhook arrives second, takes the update
      branch, and Task 7 never runs for that account — not late, never. Nothing
      logs it, nothing retries it, and the resulting name is a plausible value
      rather than an obviously missing one.
      DEBT-17 IS THE PRECEDENT AND THE INCOMPLETE FIX. That row is the same race
      on the same two writers, found for ROLE. The fix (84437e5, direction B)
      mirrored ROLE between them — users/page.tsx now resolves role from
      PendingInvite exactly as the webhook's resolvedRole does, so the two
      cannot drift. It left NAME unmirrored. One field of a two-field race was
      closed and the other was not noticed.
      CURRENTLY MASKED, AND THIS IS WHY IT IS DEBT RATHER THAN A BUG ROW. The
      page can only write a name Clerk gives it, and Clerk gives it none: the
      sign-up flow renders only REQUIRED fields, first/last are merely enabled,
      and no name is collected on any observed path including OAuth — the
      unreachability finding recorded in full on PERM-7's Task 7 blocker
      (three staging runs, 2026-08-01). So today the page writes null, the
      webhook writes the store name if it wins, and null if it does not, and the
      visible damage is a blank name rather than a person's name.
      ONE ROOT CAUSE, ONE REOPENING CONDITION, shared with that closure: two
      writers with unmirrored fields, masked by a Clerk configuration that
      collects no name. If first/last are ever made REQUIRED, or a sign-up path
      is added that collects a name, BOTH unmask together — Task 7's override
      becomes reachable-and-untested AND this race starts putting a person's
      name on a device account, which is the exact hazard the convention exists
      to prevent. Check them as one trigger, not two.
      FIX DIRECTION, not taken here: mirror NAME as DEBT-17 mirrored ROLE — give
      the page the same PendingInvite-driven derivation, so either winner
      produces the same row. Direction C (stop the GET from writing at all)
      remains the deliberately-untaken option and would close both races
      outright.
      Related: DEBT-17 (the same race, role half, fixed), PERM-7 Task 7 (the
      derivation and its unreachability closure), DEBT-40 (the comments that
      still describe the unreachable path as live).

  - id: DEBT-40
    title: "Code comments and DECISIONS.md assert a name is typed at sign-up — no configured Clerk instance collects one"
    notes: >
      COST OF DOING NOTHING: The code implements the ruling correctly and only
      the comments' factual premise is false, so nothing misbehaves — LATENT
      (trigger: the next editor of the Clerk webhook's name derivation, or of
      DECISIONS.md § PERM-7 Task 7, who takes the comment at face value and
      demotes the override to a fill-the-blank fallback. That is precisely the
      misreading the decision record was written to prevent, which is what makes
      a false premise inside it more dangerous than a false premise anywhere
      else).
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's.
      FILED 2026-08-01 by PERM-7c, which found it while re-verifying PERM-7 Task
      7 and could not fix it — that session was scoped to ROADMAP.yaml.
      THE COUNTERFACTUAL ASSERTIONS, all stating or presupposing that a human
      types a name at sign-up: src/app/api/webhooks/clerk/route.ts:113-115
      ("Clerk owns the name, and it is whatever a human types at sign-up"),
      :120-121 ("keeps the name they typed"), :123-125 ("It OVERRIDES the typed
      name rather than filling a blank one"); docs/DECISIONS.md:8 (the heading
      itself, "OVERRIDES the sign-up form"), :15-16, :19-21, :33-34, :38 ("An
      admin who renames the account wins permanently; the sign-up form does
      not").
      WHY THEY ARE FALSE AS WRITTEN: neither configured instance collects a
      name. Clerk's sign-up flow renders REQUIRED fields, `first_name`/
      `last_name` are merely ENABLED, and three staging runs on 2026-08-01 —
      including one deliberately routed through Google OAuth — presented
      username and password only. Full evidence on PERM-7's Task 7 blocker.
      WHAT IS NOT FALSIFIED, and must survive any rewording. The ruling STANDS:
      override-not-fill is the correct design (DECISIONS.md 2026-07-28, Gary),
      and the code IMPLEMENTS it correctly — the override is real, create-only,
      and narrowly conditioned. Only the factual claim that the form PRESENTS
      the field is wrong. A future editor must not read this row as licence to
      demote the override to a fallback; that is the exact misreading the
      decision record was written to prevent.
      SAME SHAPE AS DEBT-26 — comments describing a model that has since
      changed, except here the model never held rather than having drifted.
      FIX DIRECTION: reword to describe the mechanism and its reason rather than
      the imagined human — the derivation asserts the store's name over ANY
      value Clerk supplies, whether or not the form currently offers a way to
      supply one — and cite the reopening condition so the wording survives a
      configuration change. Do not delete the "Tommy Thomas" counterexample: it
      is the hazard the convention exists for and remains valid as a hazard.
      SAVED PROMPTS STAY WRONG BY DESIGN — docs/prompts/PERM-7_store_device_
      logins.md and PERM-7b_task7_name_derivation.md both instruct the tester to
      type a name. A saved prompt is a claim wholesale and is never edited
      (CLAUDE.md § Where documents live). The rows are the authority.
      Related: DEBT-26 (same shape, fixed), DEBT-39 (same root cause and the
      same reopening condition), PERM-7 Task 7.

  - id: DEBT-41
    status: shipped
    commits: ["13e96fb"]
    title: >
      CLOSED — A blocker entry can only be LIVE or RESOLVED — there is
      no value for the half-closed state two entries are actually in
    notes: >
      SHIPPED TO PRODUCTION 2026-08-10 IN MERGE "ddaa216". Status corrected from
      `staging` by the CHK-4 bookkeeping rider, per the DEBT-54 convention;
      "13e96fb" is an ancestor of origin/main.
      THE THIRD STATE HAS BEEN USED IN ANGER SINCE, WHICH IS THE THING THIS ROW
      ACTUALLY WANTED. DEBT-66 was rescoped rather than closed on 2026-08-10 —
      the git-push path cleared, the dashboard-redeploy path still open — and
      that is precisely the half-closed shape the row was filed about. A vocabulary
      addition that nothing uses is indistinguishable from one nobody needed.
      ── PRE-PROMOTION RECORD OF 2026-08-08 BELOW, UNEDITED ──
      VERIFIED 2026-08-08 BY GARY ON STAGING — CODE VERIFIED VIA THE TPL-2
      WALKTHROUGH; PANEL RENDERING PER THE "13e96fb" SESSION'S BROWSER EVIDENCE.
      Read the two halves separately, because they carry different weight. The
      code is on staging and exercised: "13e96fb" and "cc837db" reached origin
      with "198d040"/"332e6fb", and the TPL-2 pass ran against a build carrying
      this row's BlockerEntry union, so the type change is live and does not
      break the app. THE PANEL ITSELF WAS NOT RE-OPENED IN THIS PASS. The counts
      below — 16 live · 2 narrowed across 11 blocked phases · 12 resolved, the
      dashed border and the NARROWED marker — REST ENTIRELY ON THE "13e96fb"
      SESSION'S OWN BROWSER READING, recorded further down this row, and are not
      independently re-confirmed here.
      WRITTEN THIS WAY DELIBERATELY (Gary, 2026-08-08). The first draft of this
      rider asserted a staging panel reading with those exact counts. They are
      identical to the ones already in this row, which is precisely the shape of
      a figure transcribed from a document and re-reported as a fresh
      observation — CLAUDE.md § Database Evidence's relocation lesson, one
      surface over. Asked directly, Gary confirmed the panel was not opened. A
      rider claiming a check that did not happen would have been worse than no
      rider, because it would have looked like corroboration.
      WHAT WOULD ACTUALLY CLOSE THIS: open /internal/roadmap on staging, name
      the ORGANIZATION ID and the Clerk INSTANCE per CLAUDE.md § Browser
      Evidence, and record the counts seen. Until then the panel evidence for
      this row has exactly one source, taken pre-push on a local build.
      CLOSED 2026-08-08, commit "13e96fb". The third state exists and both
      exemplars carry it. BlockerEntry is now
      `string | {resolved: true, text} | {narrowed: true, text}` — a bare string
      still means LIVE, and the new state is a SEPARATE UNION MEMBER precisely
      so that `resolved: false` has nowhere to be written. The panel reads
      16 live · 2 narrowed across 11 blocked phases · 12 resolved, where it read
      18 live before; the "Phases blocked" tile is unchanged at 11, because a
      narrowed entry STILL BLOCKS and both numbers come from one predicate.
      BUILD-1 `blockers[0]` and IG-1 `blockers[2]` were flagged, not rewritten —
      their text is byte-identical, verified by re-running P-4's whole-tree
      identity check across all 30 blocker entries, which permitted exactly two
      diffs (both `live -> narrowed`) and zero text diffs. The panel was read in
      a browser at those counts before the commit, not inferred from the data.
      STATUS IS `staging` AND THAT IS ONE STEP AHEAD OF THE TRUTH AT WRITING:
      "13e96fb" is committed on the local `staging` branch and NOT PUSHED, so
      the value becomes true on Gary's push and nothing else in this row depends
      on it. Stated rather than implied — DEBT-14 closed the same way, and the
      legend's `staging` ("merged to `staging`, on Vercel preview") is the
      nearest true value once the push lands. Claude never pushes (CLAUDE.md
      § Git Rules).
      CORRECTION APPENDED 2026-08-08 — THE CAVEAT ABOVE IS NOW FALSE AND THE
      STATUS IS SIMPLY TRUE. Gary pushed; "13e96fb" and its row commit "cc837db"
      are both on origin/staging, confirmed by `git merge-base --is-ancestor`
      against origin/staging after a fetch rather than by assuming the push
      succeeded. Preserved rather than deleted: the caveat was correct when
      written and expired exactly as designed.
      THE NAME IS `narrowed`, NOT `half` OR `partial`, and the reason is in the
      two exemplars: neither is half of anything. Each is an entry that still
      blocks on strictly LESS than it originally asserted — BUILD-1's step 1
      closed with step 2 deferred, IG-1's env-var half settled with the callback
      registration outstanding. `narrowed` names the direction of travel and
      stays honest when the surviving remainder is a tenth rather than a half.
      IT CARRIES NO SECOND FIELD naming what remains. Both entries already state
      it in their own prose, and a required `remains:` would force that prose to
      be paraphrased into a place that then drifts from it — DEBT-26's shape.
      COUNTED SEPARATELY, NEVER EXCLUDED, AND STILL BLOCKING — the three
      answers this row's history argued for. Folding narrowed into live would
      have kept the count safe and kept it dishonest, which is the state this
      row spent a week recording; excluding it would have been the one direction
      the scheme exists to prevent. So `liveBlockers` and `blockingBlockers` are
      now two functions rather than one widened one. THAT SPLIT FOUND TWO REAL
      UNDER-REPORTS THAT WERE NOT IN THE DESIGN: the Staging → Production panel
      would have printed "No recorded gate — ready to promote" over a narrowed
      gate, and PhaseCard's badges would have dropped a narrowed entry from the
      collapsed card entirely. Both are fixed in the same commit. Neither is
      reachable today — there are zero staging phases, and both narrowed entries
      sit on `shipped` rows — which is exactly why renaming the function rather
      than widening it was worth the churn: the name is what surfaced them.
      WHAT THIS DOES NOT UNDO: exemplar (a) is still relocated. PERM-6's 11.9k
      coverage record is still behind a collapsed disclosure and this change
      does not bring it back — the state arrived after the workaround, not
      before it. The row closes because the vocabulary gap is closed, not
      because that price was refunded.
      THE STANDING PROHIBITION SURVIVES THIS CLOSURE and now lives in three
      places (the type, the YAML header, this row): never write `resolved:
      false`, and never write `narrowed: false`. The absence of a flag carries
      LIVE, and that default is what makes the whole scheme safe. A third state
      is not a licence for a fourth — the gap this type had was a real state
      with no name, and the fix was a name and a render treatment.
      NOT DONE HERE, DELIBERATELY: DEBT-42's `deferred` and `open` are still
      bare `string[]`. Widening them is still gated on its own trigger (the
      first surface that COUNTS either field), and doing it as a rider on this
      commit is precisely the sweep-creep that left exemplar (a) relocated
      instead of fixed. DEBT-14's adjacent question — a ruled-LATENT debt row
      rendering identically to an untriaged one — is a different type on a
      different panel and would mean touching PhaseStatus, which is shared with
      the phase board; out of scope by the same argument Gary used on DEBT-14
      itself.
      ── RULING AND HISTORY BELOW, UNEDITED ──
      RULED 2026-08-07 — THE STANDING TRIGGER FIRED, SO THIS ROW STOPS BEING A
      DECISION AND BECOMES WORK. Its own instruction was WATCH FOR MORE EXAMPLES
      BEFORE ACTING, and after exemplar (a) was relocated away rather than
      solved, the condition was exactly ONE NEW half-closed exemplar. That
      exemplar now exists: IG-1's THIRD blockers entry, `blockers[2]`, dated
      2026-08-06, which opens "SHRUNK TO ITS SURVIVING HALF 2026-08-06 — STILL
      LIVE, but half of what it asserted is now answered". It is SELF-DESCRIBED
      as half-closed, it is carried as live, and — unlike exemplar (a) — it was
      not routed around to make a count come out right. It is the clean new
      instance the row demanded.
      SO, PER THIS ROW'S OWN INSTRUCTION, THE THIRD BlockerEntry STATE IS NOW
      DESIGN WORK. Scope as ruled: SMALL, LOW PRIORITY, and it gets its OWN
      SESSION rather than riding along — a vocabulary change to a type the whole
      board reads is precisely the thing that gets half-done inside a sweep,
      which is how exemplar (a) ended up relocated instead of fixed. The cost
      being paid meanwhile is unchanged and still small: one internal panel
      over-counting live blockers by one, in the deliberately-safe direction.
      THE DO-NOT-FIX-BY-ADDING-`resolved: false` INSTRUCTION IS UNCHANGED and is
      the first thing that session reads. The absence of the flag already
      carries "live"; a third state needs its own name and its own render
      treatment, or it destroys the default that makes the scheme safe.
      Row stays OPEN, title prefix changed from AWAITING RULING to RULED. The
      title's "two entries" stays dated-at-filing prose, corrected by the blocks
      below rather than rewritten — the same treatment it already had.
      COST OF DOING NOTHING: The blockers panel counts BUILD-1's half-closed
      entry as live, so /internal/roadmap over-reports by one in the
      deliberately-safe direction — LATENT (trigger: a SECOND live half-closed
      exemplar, which is also this row's own standing condition for designing a
      third state; exemplar (a) was relocated away rather than solved, so the
      count is one).
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's. FIRST
      DERIVED AS LIVE AND MOVED TO LATENT BY GARY 2026-08-02 on the
      product-vs-process distinction recorded above the `debt:` key. The
      argument for LIVE was real and is kept here rather than discarded: Gary
      opened /internal/roadmap on 2026-08-01, read this very count, and
      concluded a session had failed — so the number demonstrably changes what
      he believes. It is still a cost to HIM and to the tooling, not to a
      merchant using the app, and that is the line the label now carries.
      RECORD HALF RELOCATED 2026-08-02 by DEBT-TRIAGE-2 → one paragraph on
      BlockerEntry in src/lib/roadmap.ts: NEVER ADD `resolved: false`. That is
      where the person about to add it is standing, and it is the one action
      this row most needs to prevent. The reasoning goes with it — the value is
      already carried by the flag's absence, and introducing it destroys the
      "a missing flag means live" default. THIS ROW STAYS AWAITING RULING and
      its standing instruction is unchanged: watch for one NEW example.
      AWAITING GARY'S RULING: nothing until a NEW half-closed exemplar appears;
      then whether a third BlockerEntry state is worth designing. Title prefixed
      2026-08-02 by DEBT-TRIAGE-1 so the row stops reading as backlog; the
      deferral and its trigger are the row's own ("KEEP AS-IS... accepted by
      Gary 2026-08-01", "WATCH FOR MORE EXAMPLES BEFORE ACTING"), transcribed,
      not new. The prefix is added to a title this row's own text already flags
      as dated-and-deliberately-unrewritten ("two entries"); prefixing it does
      not touch that claim.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      FILED 2026-08-01 by P-4, on Gary's ruling, from the audit that added the
      `resolved` flag. Filed rather than fixed because inventing a third state
      was not in that session's scope and because two examples is thin evidence
      for a vocabulary change — but it is real evidence, and an observation that
      lives only in a session report does not exist (the DEBT-37 precedent).
      THE FACT: BlockerEntry (src/lib/roadmap.ts) is `string | {resolved: true,
      text}`. A bare string is live, a flagged entry is closed, and there is
      nothing in between. Two of the nineteen entries at filing time are in
      between, and neither fits:
      (a) PERM-6's second `blockers` entry, "WHAT THE CLEARED GATE DOES NOT
      COVER". It is not a promotion gate — the gate above it is cleared — but it
      records four things still unexercised (the webhook's rejecting branch,
      Task 1, the STAFF role, and the reason the phase stays `shipped` and not
      `verified`). It is a live COVERAGE note wearing the blockers field.
      (b) BUILD-1's sole entry, "CHANGED SHAPE — HALF CLOSED, HALF DEFERRED":
      step 1 of a two-step fix is done and step 2 is deliberately deferred until
      collaboration work starts.
      BOTH ARE CARRIED AS LIVE, deliberately, which is the safe direction —
      under-claiming resolution can only over-report, and over-reporting is
      recoverable in a way that a hidden gate is not. The cost is that the panel
      reads 11 live when the honest answer is 9 live and 2 half-closed.
      DO NOT FIX THIS BY ADDING `resolved: false`. That is the same value the
      absence of the flag already carries, and it would invite marking entries
      false wholesale, which destroys the "a missing flag means live" default
      that makes the scheme safe. If a third state is wanted it needs its own
      name and its own render treatment.
      WATCH FOR MORE EXAMPLES BEFORE ACTING. Two is enough to file and not
      enough to design against.
      EXEMPLAR (a) SHARPENED 2026-08-01 (evening) by PERM-6c, and NOT REMOVED.
      PERM-6's second blockers entry no longer records four things unexercised:
      the webhook's rejecting branch, POST /api/staff and the STAFF role were
      all closed inside the entry that day. What survives is the fourth item
      alone — the reason the phase stays `shipped` and not `verified`. It is now
      a PURE status rationale in the blockers field, which is a CLEANER instance
      of this row's problem and not a weaker one: there is no live gate left in
      it to justify carrying it as live, and it is still carried as live. The
      count cost is unchanged.
      THE OTHER WAY OUT, weighed and not taken: move that rationale to `notes`
      and mark the entry resolved, which would fix the count with no new
      vocabulary. Rejected by Gary 2026-08-01 because it would leave THIS ROW
      WITH ONE EXEMPLAR, and this row already says two is thin. Fixing the
      symptom by destroying the evidence is how a vocabulary problem never gets
      fixed.
      EXEMPLAR (a) IS GONE — RESOLVED BY RELOCATION 2026-08-01 (later the same
      day) by PERM-6d, and HOW it went matters more than that it went: THE
      VOCABULARY PROBLEM WAS NOT SOLVED. PERM-6's second blockers entry needed a
      third state and got a different FIELD instead. Its status rationale moved
      to PERM-6's `notes`, the entry became `resolved: true`, and the panel now
      reads 10 live across 5 phases and 9 resolved — while BlockerEntry gained
      nothing at all. A half-closed entry still has nowhere to say so; one entry
      simply stopped being half-closed, by having its two halves filed in two
      different fields.
      THE COST, NAMED: an 11.9k-char coverage record went behind a collapsed
      disclosure to make one count come out right. The entry's WHAT REMAINS
      UNCOVERED list — four things still unexercised — is the only live content
      left in it, and it is now a click away instead of on the page. A pointer
      to those four was added at the top of PERM-6's `notes`, so the fold costs
      visibility and not findability. That is a mitigation, not a fix.
      THIS IS THE "OTHER WAY OUT" ABOVE, TAKEN. The paragraph beginning "THE
      OTHER WAY OUT, weighed and not taken" records Gary rejecting exactly this
      move on 2026-08-01, because it would leave this row with one exemplar. He
      reversed that the same day, on evidence rather than on reflection: he
      opened /internal/roadmap, saw PERM-6 under "what's stopping promotion",
      and concluded the session had failed. Preserving the evidence cost more
      than the evidence was worth. That paragraph is dated prose and is
      deliberately not edited — nor is this row's TITLE, which still says "two
      entries" and is corrected by this block rather than rewritten, the same
      treatment PERM-6 gave its own O/0 spelling.
      BOTH WORKED EXAMPLES ARE STILL DESCRIBED HERE; ONLY ONE IS STILL LIVE.
      (a) stands unedited above, as filed and as sharpened: a blockers entry
      that was not a promotion gate, that held four unexercised coverage gaps
      AND a status rationale at filing and then only the rationale, carried as
      LIVE throughout because the type had no other value to carry it as.
      (b) BUILD-1's sole entry, "CHANGED SHAPE — HALF CLOSED, HALF DEFERRED", is
      the one remaining LIVE instance.
      DOES THIS ROW STILL STAND ON ONE LIVE EXEMPLAR? YES — and PERM-6d's
      reading is that it stands slightly better than it did, not worse. This
      row's argument was never "two entries are stuck"; it was "the type has no
      value for a real state". (b) is the cleaner of the two instances and the
      one that CANNOT be relocated away: step 2 of BUILD-1's fix is a genuinely
      deferred gate, so there is no `notes` sentence to move it into and no
      honest way to flag that entry resolved. And (a) did not stop being a data
      point — it became a worked example of what the missing state COSTS, which
      is the paragraph above. Evidence of a workaround's price is evidence.
      RECOMMENDED AND RULED — KEEP AS-IS (recommended by PERM-6d, accepted by
      Gary 2026-08-01). NOT CLOSED: nothing was fixed, and closing a row because
      its symptom was routed around is how a problem returns unrecognised. NOT
      DOWNGRADED either — a DebtItem carries no severity field to downgrade
      (src/lib/roadmap.ts), so a downgrade could only be prose softening the
      title, and the title is dated-at-filing prose this block already corrects.
      This row's own standing instruction is unchanged and is still the right
      one: WATCH FOR MORE EXAMPLES BEFORE ACTING. It now needs one NEW example
      rather than none before a third state is worth designing.
      Related: P-4 (added the flag), DEBT-14 (the same vocabulary problem one
      level up, where `withdrawn` eventually had to be added to PhaseStatus for
      exactly this reason — see DEBT-18/DEBT-23).

  - id: DEBT-42
    title: >
      `deferred` and `open` keep resolved entries in the array
      forever, the same defect P-4 fixed for `blockers`
    notes: >
      RULED 2026-08-07 — NOTHING WAS PENDING HERE, AND THE PREFIX WAS THE ERROR.
      No call was owed on this row: the deferral and its trigger were already
      ruled by Gary on 2026-08-01, explicitly, as FLAG AND NOT FIX. What
      happened on 2026-08-02 is that DEBT-TRIAGE-1 prefixed the title AWAITING
      RULING so the row would stop reading as backlog — and over-corrected. A
      row that was DECIDED was made to read as a row AWAITING a decision, and it
      then sat in the awaiting-ruling set for five days asking for a call that
      had been made a day before the prefix was applied.
      WHAT ACTUALLY STANDS, UNCHANGED: this is a LATENT RECORD, and its trigger
      is the first surface that derives a COUNT from `deferred` or `open` — a
      summary tile, a panel headline, a dashboard number. At that moment it
      becomes the P-4 bug again with the same failure direction, and the fix is
      already designed and cheaper than P-4's was. Until then no headline count
      reads either field and the cost is a slightly wrong badge on one collapsed
      card.
      PREFIX REMOVED AS MISLABELLED — that is the whole of this ruling. The row
      stays open, stays latent, and is not waiting on Gary. Note the lesson for
      the next relabelling pass: a prefix that says a row needs a DECISION is
      itself a claim, and applying it to a row that has already been decided
      costs exactly what it cost here.
      COST OF DOING NOTHING: No headline count reads `deferred` or `open` —
      verified at HEAD, both are still `string[]` at roadmap.ts:84-85 — so the
      three resolved entries sitting in those arrays cost a slightly wrong badge
      on one collapsed card and nothing else — LATENT (trigger: the first
      surface that derives a COUNT from either field, a summary tile, a panel
      headline or a dashboard number, at which point this becomes the P-4 bug
      again with the same failure direction and the fix is already designed).
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's.
      AWAITING GARY'S RULING: nothing until a surface COUNTS `deferred` or
      `open`; that event is the trigger, and the fix is already designed. Title
      prefixed 2026-08-02 by DEBT-TRIAGE-1 so the row stops reading as backlog;
      the deferral and its trigger are the row's own ("on Gary's explicit ruling
      to FLAG AND NOT FIX", "WHAT WOULD CHANGE THAT"), transcribed, not new.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      FILED 2026-08-01 by P-4, on Gary's explicit ruling to FLAG AND NOT FIX.
      THE FACT: `deferred` and `open` are still `string[]` (src/lib/roadmap.ts)
      and carry no way to mark an entry closed, so resolved entries stay in the
      array exactly as blocker entries did before P-4. Three exist today, found
      by reading all 21 entries in full:
      PERM-2 `deferred[0]` opens "RESOLVED 2026-07-27 by PERM-6 Task 1";
      PERM-6 `open[0]` opens "RESOLVED AND MOVED 2026-07-27";
      PERM-7 `open[0]` opens "RESOLVED 2026-07-28";
      and SEC-1 `open[0]` opens "HALF-CLOSED 2026-07-27", which is DEBT-41's
      third state rather than a clean closure.
      WHY IT WAS RULED NOT WORTH FIXING NOW, and this is the part to re-read
      before reopening it: NO HEADLINE COUNT READS EITHER FIELD. `deferred` gets
      a card badge and a DetailList; `open` gets a DetailList only — no badge,
      no count, no summary tile. The cost is therefore a slightly wrong badge on
      one collapsed card, not an untrustworthy answer to "what is stopping
      promotion". That asymmetry is the whole reason `blockers` was worth a
      migration and these two are not.
      WHAT WOULD CHANGE THAT: any new surface that COUNTS either field — a
      summary tile, a panel headline, a dashboard number. The moment a number is
      derived from these arrays, this becomes the P-4 bug again with the same
      failure direction, and the fix is already designed and proven: widen the
      type to BlockerEntry, migrate only the closed entries, run the whole-tree
      identity check.
      THE FIX IS CHEAPER THAN P-4's WAS. BlockerEntry, both helpers and the
      shared DetailList already accept the object form — `string[]` is
      assignable to `BlockerEntry[]`, which is why DetailList already renders
      the "resolved" marker for any entry that carries the flag. Widening the
      two fields is a type change plus a four-entry migration; no new mechanism
      is needed.
      Related: P-4, DEBT-41 (SEC-1's entry is an example of both).

  - id: DEBT-43
    status: shipped
    commits: ["06befd2", "04da67f"]
    title: >
      Every `border-[var(--color-*)]` in the app silently renders as the default
      border colour — an UNLAYERED `*` rule in globals.css overrides every
      border-color utility
    notes: >
      READ THIS FIRST — THE FILE TAILWIND SCANS IS docs/ROADMAP.yaml, NOT
      src/generated/roadmap.ts. Hoisted to the top of this row 2026-08-05 by
      Gary's ruling. The row stated the WRONG file first and corrected itself
      several paragraphs down, and that ordering has now cost twice: once inside
      this row, where two attempts were spent neutralizing the generated file
      and changed nothing, and once OUTSIDE it, when a 2026-08-05 session brief
      repeated the wrong claim as its own premise — it read the top of the row
      and stopped. A ROW THAT MISLEADS A SKIMMER MANUFACTURES FALSE PREMISES
      DOWNSTREAM, so the correction now sits where the skimmer is.
      THE CORRECTED RULE, in the order it should be read:
      (1) TAILWIND SCANS docs/ROADMAP.yaml — the TRACKED file, this prose, in
      source control. Prose in a row's notes can create real CSS selectors out
      of ordinary English words. Neutralized in 5da1008 by
      `@source not "../../docs"` (src/app/globals.css:10); confirm that line
      still exists before assuming a new row's prose is harmless.
      (2) TAILWIND DOES NOT SCAN src/generated/roadmap.ts. It is gitignored
      (.gitignore:47) and Tailwind 4 honours .gitignore. Proven, not inferred:
      with 5da1008 applied that file still contains two `--color-*` occurrences
      and dev runs clean.
      (3) GREPS RUN THE OTHER DIRECTION, and for them the original rule stands —
      src/generated/roadmap.ts carries every row's notes as string literals, so
      ANYONE GREPPING src FOR EVIDENCE ABOUT A DEBT ROW MUST EXCLUDE IT. Both
      files say whatever the row says; only one of them is scanned, and it is
      not the one the rule further down names.
      RE-VERIFIED 2026-08-05 while adding the `rulings:` list to this file:
      emitted CSS byte-identical across the change — 78,350 bytes and 1,031
      unique class selectors before and after, zero added, zero removed. The
      exclusion is doing its job, measured rather than assumed.
      ── PRIOR NOTES BELOW, UNEDITED ──
      SHIPPED — VERIFIED ON PRODUCTION 2026-08-03. Promoted in the de3ba40
      production promotion of 2026-08-02; implementing commits 1dbe9ca, 06befd2,
      475e425, 04da67f, 790cfc4, 5da1008 and 24894fa — SEVEN, where this row's
      `commits` field names two, so treat docs/DEPLOY_LOG.md as the authority on
      the set. Observed on www.usefroot.com: borders render their intended
      colours across /staff and /templates. The universal reset is wrapped in
      `@layer base`, so the border-color utilities win.
      ORG ID NOT NAMED in the report this was recorded from (CLAUDE.md
      § Browser Evidence). Least consequential of the three flips taken the same
      day — this is a global stylesheet observation with no org-scoped data in
      it — but recorded rather than waived, since the value of that precondition
      is that it is not judged case by case.
      FIFTH CORRECTION 2026-08-02, recorded by DEBT-29-RELABEL, and it is the
      one with a running cost: THIS ROW'S OWN PROSE BROKE `npm run dev` FOR THE
      WHOLE APP, and had done since 1dbe9ca (2026-08-01 22:20) — the commit that
      FILED this row. Every route 500s, not one page: confirmed cold at 65c20cb
      (rm -rf .next, then /, /sign-in and /templates all 500). Gary did not know;
      it was found by accident while rendering an unrelated form. FIXED in
      5da1008 — `@source not "../../docs"` in globals.css.
      THE CONTAMINATION RUNS IN A THIRD DIRECTION, AND THIS ROW NAMES THE WRONG
      FILE. The rule below says "src/generated/roadmap.ts is in the measurement
      path… It is scanned by Tailwind". IT IS NOT. That file is gitignored and
      Tailwind 4 honours .gitignore. The file Tailwind actually reads is the
      TRACKED docs/ROADMAP.yaml — this row's notes, in source control. Proven,
      not inferred: with 5da1008 applied, src/generated/roadmap.ts still contains
      two `--color-*` occurrences and dev runs clean. The cost of the wrong name
      is measurable — neutralizing the generated file changed nothing and burned
      two attempts before the real source was found.
      SO THE RULE THIS ROW CARRIES NEEDS WIDENING. "Exclude
      src/generated/roadmap.ts when grepping src for evidence" is still right for
      GREPS. For TAILWIND it points at the wrong file: the scanner never saw the
      generated copy and always saw docs/ROADMAP.yaml. Both files say whatever
      the row says; only one of them is scanned.
      WHAT THE FIX COSTS, measured cold both sides: the emitted stylesheet is NOT
      byte-identical — 78,944 → 78,296 bytes, ten selectors removed, zero added,
      and all ten are prose artifacts with no real usage in src (four ellipsis-
      mangled quotations of this row, `.border-warning` which this row already
      documents as a phantom, and `.bottom-10` `.top-10` `.lowercase` `.ring`
      `.static` — ordinary English words in these notes). Each was checked
      individually against className usage; real classes are untouched.
      AND THE WARNING COUNT BELOW IS WRONG TOO, which is the fifth correction's
      own small echo: this row says "two CSS-optimizer warnings". It was THREE at
      65c20cb, and is 0 after 5da1008.
      THE PATTERN IS THE FINDING, AND THIS ROW ALREADY SAYS SO — that is why this
      is filed here rather than as a new row. Five corrections now: generated-file
      contamination in both directions, lines-versus-occurrences, the
      wrong-population regex, and now the wrong scanned file plus a wrong warning
      count. The four before this one were all about MEASURING the defect. This
      one is different again: the row's own text became a defect, in an
      environment nobody was measuring.
      ── PRIOR NOTES BELOW, UNEDITED ──
      CLOSED 2026-08-02 in 06befd2, by the one-line fix this row named and
      deliberately did not apply: globals.css:61 wrapped in `@layer base`. Four
      lines changed, zero call sites edited, no Tailwind config touched.
      MECHANISM RE-VERIFIED INDEPENDENTLY AT HEAD before the change, because
      this row's own history says to. Fresh `rm -rf .next && npm run build`,
      brace-depth walk of the emitted chunk with the enclosing at-rule chain
      recorded per block. BEFORE: `*{border-color:var(--color-border)}` at depth
      0 with no enclosing at-rule — unlayered — and all 25 matching
      `.border-\[var\(--color-*\)\]` blocks at depth 1 inside `@layer utilities`
      (11 of those at depth 2 inside `@layer utilities > @supports(color-mix)`,
      the opacity-modifier variants). AFTER: the universal rule at depth 1
      enclosed by `@layer base`, merged into Tailwind's own base block — there
      is exactly one `@layer base` block and one `@layer utilities` block — and
      the 25 utility blocks unchanged at depth 1 in `@layer utilities`.
      EMITTED LAYER ORDER, which is what makes this a fix rather than a
      relocation: properties (byte 17) -> theme (1,922) -> base (9,441) ->
      components (13,095, a bare statement) -> utilities (13,096). `base`
      precedes `utilities` on first appearance, so utilities win. The `*` rule
      lands at byte 13,042, inside base.
      SCOPE REPRODUCED EXACTLY, unit = OCCURRENCES (`grep -o`), excluding
      src/generated/roadmap.ts — both stated because this row requires both:
      560 / 468 / 92 / 45.
      THE 468 `--color-border` OCCURRENCES ARE UNAFFECTED, before and after.
      They ask for the value the universal rule already forced, so they got it
      by accident before and get it by the cascade now. No edit, no visual
      change, in either direction. This is the largest class and it is a no-op.
      A SECOND CLASS OF NO-OP, found while measuring and not previously
      recorded: the 4 `--color-input` occurrences (ui/input.tsx, ui/select.tsx,
      ui/textarea.tsx, dashboard/sales-performance-card.tsx). `--color-input`
      and `--color-border` both compile to `#d7cbc1` — verified in the emitted
      chunk, not inferred from the source tokens — so those four render
      identically before and after. The number of occurrences that change
      VISIBLY is therefore 88, not 92.
      THE FILE-COUNT DISCREPANCY THIS ROW CARRIED AS UNEXPLAINED IS CLOSED, in
      the row's favour. `grep -rl --exclude-dir=generated` returns 106 files at
      HEAD, matching the 106 recorded here; the 108 was the outlier. The 92
      wrong-coloured occurrences sit in 43 files, not 44.
      SCOPE WAS WIDER THAN THIS ROW EVER STATED, and this is the one substantive
      correction the fix session made to it. The 560 counts only ARBITRARY
      `border-[var(--color-*)]` utilities. NAMED border-color utilities were
      masked by the identical mechanism and are unmasked by the identical fix:
      18 of them exist in src (17 `border-transparent`, 1 `border-white`),
      across ui/switch.tsx, ui/badge.tsx and 9 feature files. Three of the 18
      are dead (see below), so 15 change. Total occurrences that change visibly:
      88 + 15 = 103. ANYONE RE-MEASURING THIS CLASS OF BUG MUST GREP FOR NAMED
      UTILITIES TOO — an arbitrary-value regex silently under-reports it, which
      is what happened here for the row's whole life.
      THAT IS A FOURTH MEASUREMENT ERROR ON THIS ROW, AND THE PATTERN IS NOW THE
      FINDING (Gary, 2026-08-02, ruling on whether to back the change out). The
      first three were the generated-file contamination in both directions and
      lines-versus-occurrences; this one is different in kind and worse in
      consequence. Those three miscounted the population. THIS ONE MISDEFINED IT:
      the 560 / 468 / 92 / 45 figures are exactly right and were reproduced to
      the occurrence, and they answer a NARROWER QUESTION than the row's title
      asks. The title says "every `border-[var(--color-*)]`"; the mechanism
      paragraph correctly says the rule "overrides every border-color utility in
      the application". Only the second is the real scope, and NO PRIOR
      MEASUREMENT ON THIS ROW EVER COUNTED THE NAMED UTILITIES — not the audit,
      not DEBT-TRIAGE-1's correction, not DEBT-TRIAGE-2's recount. Three sessions
      re-measured the same regex more carefully each time and none asked whether
      the regex matched the claim.
      SO THE RULE THIS ROW ALREADY CARRIES IS NOT ENOUGH. "State your unit and
      exclude the generated file" guards against counting the population wrong.
      It does not guard against counting the wrong population. ADD: state what
      the regex EXCLUDES, and check that against the sentence the number is being
      used to support. A precise count of the wrong set is more dangerous than a
      rough count of the right one, because precision is what stops the next
      person re-deriving it — the 560 was quoted forward three times BECAUSE it
      was exact.
      RULING, GARY 2026-08-02: LEAVE IT ON STAGING, do not back it out. The
      number grew because the row's scope was always understated, not because
      the change did more than was intended — the named utilities were masked by
      the same rule and are unmasked by the same fix. Every one of the 15 is an
      element shedding an unintended grey ring, which is the wanted direction.
      BADGE BEHAVIOUR, RECORDED SO THE VISUAL PASS READS IT AS EXPECTED. All
      three soft badge variants (success/warning/info, ui/badge.tsx:14-16) carry
      BOTH `border-transparent` AND `border-[var(--color-*-border)]`. In the
      stylesheet `.border-transparent` (byte 33,664) is emitted AFTER the three
      arbitrary utilities (30,418 / 31,977 / 32,698) at equal specificity inside
      the same layer, so on a naive reading transparent wins and badges would go
      borderless. THAT READING IS WRONG AND WAS BRIEFLY REPORTED AS FACT before
      being caught. `cn()` is `twMerge(clsx(...))`, and tailwind-merge strips
      `border-transparent` as a conflicting border-color class before it ever
      reaches the DOM — verified by running twMerge over the actual cva strings,
      not by reading its docs. So: soft badges show a GREY border today and
      their INTENDED COLOURED border after. Solid badges (default/secondary/
      destructive, :10-12) carry `border-transparent` alone, which the universal
      rule also beat — they show an unintended grey 1px ring today and are clean
      after. ui/switch.tsx:13 is the same solid case. Both are improvements.
      THE STYLESHEET IS NOT THE DOM — the reasoning error this row exists to
      teach, committed once more inside the session that fixed it. The
      byte-offset measurement was real, correctly taken, and answered a question
      nobody was asking: which class wins IF BOTH REACH THE ELEMENT. One of them
      never does. Same family as the row's own "a suggestive artifact beat a
      cheap direct check" — the artifact was the compiled CSS this time instead
      of the browser, and the cheap direct check was one node invocation. It was
      caught only because the finding was being written up for a decision rather
      than filed as a row.
      TIDY-UP LANDED SEPARATELY IN 04da67f, behaviour-neutral, gated on its own:
      the three dead `border-transparent` deleted from badge.tsx:14-16, and a
      comment added above the `body` rule. NEITHER WAS A FIX AND NEITHER IS A
      ROW — that was the point of triaging them (Gary, 2026-08-02: "that is a
      better outcome than a fix or a row"). The badge tokens were dead, not
      wrong; twMerge output was verified byte-identical with and without them
      for all three variants BEFORE deleting. What goes away is not a defect but
      the READING that cost this session a wrong finding, sitting in the source
      waiting for the next person to make it.
      REMAINING UNLAYERED RULE, deliberately not wrapped: `body` at
      src/app/globals.css:65 is still at depth 0 unlayered after 06befd2,
      carrying background, color, font-family and -webkit-font-smoothing. Same
      hazard one element wide. layout.tsx:14 applies `min-h-full antialiased` to
      `<body>` today — `antialiased` already collides with the rule's own
      `-webkit-font-smoothing: antialiased` and is harmless only because both
      set the same value, so the collision is LIVE and merely benign rather than
      hypothetical. A future `bg-*`, `text-*` or `font-*` on that element would
      silently lose. Cost NONE with no named trigger, so it is a comment at the
      site rather than a row (Gary, 2026-08-02) — the person who would hit it is
      standing in globals.css, whereas a row is only read by someone already
      reading the roadmap. The comment shipped in 04da67f and names the live
      collision, since that is the detail that makes it worth having.
      GATE: eslint does not cover CSS in this repo — `npx eslint
      src/app/globals.css` returns "File ignored because no matching
      configuration was supplied", and the config is eslint-config-next
      core-web-vitals + typescript with no CSS plugin. Verified rather than
      assumed, per the prompt. Gated on `npm run build` alone. The two
      CSS-optimizer warnings in the build log are byte-identical before and
      after: they are Tailwind failing to compile the literal string
      `border-[var(--color-*)]` out of src/generated/roadmap.ts, i.e. THIS ROW'S
      OWN PROSE reaching the compiler. The contamination this row documents is
      now also a standing build warning.
      GARY'S VISUAL PASS IS THE REMAINING WORK, not a blocker on the fix: 88
      arbitrary plus 15 named occurrences across 43-plus files begin rendering
      what they always asked for. Highest-traffic first — ui/radio-group.tsx:23
      (every radio indicator ring turns primary orange, unconditional),
      ui/checkbox.tsx:15 (checked/indeterminate gain a primary border),
      ui/badge.tsx and ui/switch.tsx as above, then
      store-view/checklist/[id]/checklist-execution-client.tsx (8),
      internal/roadmap/roadmap-client.tsx (6) and hr/page.tsx (6, all primary).
      ── CORRECTED-MECHANISM NOTES BELOW, AS THEY STOOD AT CLOSURE ──
      COST OF DOING NOTHING: 45 of the 92 wrong-coloured sites are
      `--color-primary`, the selected-state affordance app-wide, so a merchant
      using the app sees the wrong border on selection today — LIVE. The one
      product-facing row of the three; the scope is what makes it so, not the
      original 11-element observation.
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's.
      SCOPE RE-MEASURED AT HEAD 2026-08-02 and the numbers below are EXACT:
      560 occurrences, 468 invisible no-ops, 92 wrong-coloured, 45 primary.
      A THIRD MEASUREMENT ERROR ON THIS ROW, caught before it was written down
      and recorded because the pattern is now the finding. DEBT-TRIAGE-2's first
      recount returned 549 / 81 / 42 and looked like drift against the 560 / 92 /
      45 above. It was not drift: it had counted grep LINES where this row counts
      OCCURRENCES, and a line carrying two utilities counts once either way it is
      wrong. Re-run with `grep -o` it reproduces 560 / 92 / 45 exactly.
      THE PATTERN: this row's scope numbers are unusually easy to get wrong, and
      have now been got wrong three separate times in three different ways — the
      generated-file contamination in both directions (recorded below), and now
      lines-versus-occurrences. ANYONE RE-MEASURING MUST STATE THEIR UNIT AND
      EXCLUDE src/generated/roadmap.ts. Neither is optional on this row.
      OPEN DISCREPANCY, deliberately not resolved: the file count. This row says
      106 files; HEAD measures 108 by `grep -rl ... --exclude-dir=generated`. The
      occurrence counts reconcile exactly and the file count does not. Not
      material to anything above, and left open rather than papered over —
      whoever fixes this row should find out why before trusting either number.
      MECHANISM CORRECTED 2026-08-02 by DEBT-TRIAGE-1. The OBSERVATION below is
      right and its EXPLANATION is false, and both fixes it proposes would have
      failed. Everything in this block was measured at HEAD cc1fffc against a
      fresh `npm run build`, not inherited from the triage audit — which had
      itself inherited P-4's numbers and found them wrong. The TITLE was
      corrected too: it carried the false claim itself, and a title stating a
      checkable fact that is wrong gets fixed (the DEBT-33 rule). The original
      read "Every `border-[var(--color-*)]` on /internal/roadmap silently
      renders as the default border colour — the arbitrary utility emits no
      border-color"; the second clause was false and the first understated the
      scope to one page.
      THE FALSE CLAIM, struck: "Tailwind cannot tell whether an opaque var()
      inside the BARE border-[…] utility is a width or a colour", so the
      border-color declaration is never emitted. FALSE, and refuted by the
      compiled stylesheet on its face — one grep would have done it. In
      .next/static/chunks at HEAD, with brace depth and enclosing at-rule
      measured rather than eyeballed: .border-\[var\(--color-warning\)\] emits
      border-color:var(--color-warning) at depth 1 inside @layer utilities, and
      .border-\[var\(--color-warning-border\)\] emits
      border-color:var(--color-warning-border) the same way. THE DECLARATION IS
      EMITTED. The dev chunk agrees — universal rule at :4462 depth 0, utilities
      at depth 1.
      THE REAL MECHANISM: `* { border-color: var(--color-border) }` at
      src/app/globals.css:61 is UNLAYERED — source brace depth 0, since the
      `@theme inline` block closes at :59 — while `@import "tailwindcss"` puts
      every utility inside `@layer utilities` at depth 1. Unlayered normal
      author declarations outrank every layered one regardless of specificity,
      so that one 0-0-0 rule overrides every border-color utility in the
      application.
      BOTH PROPOSED FIXES FAIL, for the same reason.
      `border-l-[var(--color-warning)]` emits border-left-color and is still
      layered; the unlayered shorthand expands into four longhands INCLUDING
      border-left-color, so it loses. `border-warning` emits
      border-color:#efa201 and is also layered, so it loses too. Both were
      measured in the compiled chunk at depth 1.
      THE ROW'S PROSE CONTAMINATED TWO INDEPENDENT MEASUREMENTS, IN OPPOSITE
      DIRECTIONS. `.border-warning{border-color:#efa201}` really is in the
      compiled chunk — solely because the string sits in THIS ROW'S OWN NOTES,
      which `prebuild` writes into src/generated/roadmap.ts (:1327 at the time
      of writing) where Tailwind's content scanner reads it as source. Zero real
      usages exist in src. That is the first direction: the row manufactured
      evidence that its own suggested fix was already in use. The second
      direction inflated the scope count — a raw grep over src returned 96
      wrong-coloured sites, four of which were this row quoting itself,
      INCLUDING THE ONLY `border-l-[var(--color-*)]` "usage" in the repo, which
      was the row citing its own proposed fix. DEBT-TRIAGE-1 caught the first
      contamination and then fell for the second in the same session, which is
      why this is recorded as its own paragraph rather than as a footnote.
      THE RULE THAT FALLS OUT OF IT, and it should be legible without reading
      the rest of this row: A GENERATED FILE CONTAINING THE ROW'S OWN TEXT IS IN
      THE MEASUREMENT PATH. src/generated/roadmap.ts is generated from
      docs/ROADMAP.yaml by scripts/generate-roadmap.mjs on every `predev` and
      `prebuild`, is gitignored, and carries every debt row's notes as string
      literals. ANYONE GREPPING src FOR EVIDENCE ABOUT A DEBT ROW MUST EXCLUDE
      src/generated/roadmap.ts. It is scanned by Tailwind, it is searched by
      every default grep, and it says whatever the row says.
      THE REASONING ERROR, which is worth more than the fix. A plausible
      mechanism was inferred from a correct observation and never tested against
      the one artifact that would have refuted it in a single grep. The browser
      measurement was real and right; the explanation invented for it was never
      checked. SAME FAMILY AS DEBT-15 AND DEBT-23. DEBT-15's own closing text
      names the shape — "a suggestive artifact beat a cheap direct check... The
      artifact is never the measurement" — and DEBT-23 was withdrawn for a row
      built on a misread grep. This is the third instance, and in all three the
      cheap direct check was available and skipped.
      FIX DIRECTION, DELIBERATELY NOT APPLIED: one line — wrap globals.css:61 in
      `@layer base`. That puts the reset below utilities in the cascade and
      every existing call site starts working with NO call-site edits at all.
      The row as originally written would have sent someone to edit every call
      site for no effect. DO NOT APPLY IT HERE — a visual change across 92 sites
      gets its own session, and Gary will confirm the diagnosis in a browser
      before authorising it.
      SCOPE, re-measured at HEAD excluding src/generated/roadmap.ts: 560
      occurrences of `border-[var(--color-*)]` across 106 files in src. 468 are
      `border-[var(--color-border)]` — the same value the universal rule already
      forces, so they are invisible no-ops in either direction and need no edit
      even after the fix. 92 CARRY A DIFFERENT TOKEN AND RENDER THE WRONG
      COLOUR, of which 45 are `--color-primary` — the selected-state affordance
      app-wide, which is the real user-facing cost and is named nowhere in the
      original text below. Zero side-named `border-l/t/r/b-[var(--color-*)]`
      exist in real source. This row's own "11 elements on /internal/roadmap" is
      a small fraction of that: it is the count for the one page that happened
      to be open when the row was filed.
      EVIDENCE LIMIT ON THIS CORRECTION: static analysis of the compiled
      stylesheet plus the cascade-layer rule, NOT a browser measurement. Gary
      will confirm in the browser before authorising the fix. Note the
      asymmetry, because it is what makes the falsification safe to act on
      already: the refuted half needs no cascade reasoning at all — "the
      declaration is missing" is contradicted by the stylesheet directly.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      FOUND 2026-08-01 by P-4 while verifying that page in a browser, and filed
      rather than fixed: it is a pre-existing design-system defect, it predates
      P-4 and is untouched by it, and fixing it is a Tailwind-config question
      rather than a roadmap-page one.
      THE FACT, measured in the running dev server rather than inferred: 11
      elements on /internal/roadmap carry `border-[var(--color-warning)]` and
      ZERO of them render warning-coloured — all resolve to `--color-border`.
      The panel's own outer section carries `border-[var(--color-warning-border)]`
      and fails the same way. A freshly injected probe element with
      `border-l-2 border-[var(--color-warning)]` also came back
      `--color-border`, so it is the utility and not any one call site.
      NOT A MISSING VARIABLE, which is the obvious wrong diagnosis and was ruled
      out first: `getComputedStyle(document.documentElement)` returns `#efa201`
      for `--color-warning`. The custom property resolves fine. What is missing
      is the `border-color` declaration itself — Tailwind cannot tell whether an
      opaque `var()` inside the BARE `border-[…]` utility is a width or a
      colour, so the roadmap page asks for a colour and gets nothing. Sibling
      utilities that name the property are unaffected, which is why
      `text-[var(--color-warning-text)]` works on the same page and the headline
      really is orange.
      LIKELY FIX, unverified: `border-l-[var(--color-warning)]`, or better the
      theme-generated `border-warning` — the tokens live in `@theme inline` in
      src/app/globals.css:30-33, so Tailwind 4 should already emit named border
      utilities for them. Check what is generated before editing call sites.
      SCOPE IS WIDER THAN THIS PAGE. The count above is /internal/roadmap only
      because that is the page that was open. Grep the whole app before
      estimating; every `border-[var(--color-…)]` anywhere is a candidate.
      WHAT IT DOES NOT BREAK, recorded so nobody re-opens P-4 over it: P-4's
      live-vs-resolved distinction does not depend on the border. It is carried
      by the word "resolved" on the entry and by the muted text colour, both of
      which were verified working in the same session. That is the design
      surviving the bug rather than luck — the marker was made load-bearing
      precisely because two of PERM-6's closed entries say "STAYS OPEN" in their
      own text and colour alone could never be trusted to carry the meaning.
      Cosmetic only: no count, no state, no data is affected.

  - id: DEBT-44
    title: >
      Staging carries the PERM-6/PERM-7 verification fixtures — test principals,
      a staff member and a stale ADMIN invite — and nothing owns cleaning them up
    notes: >
      COST OF DOING NOTHING: Staging fixtures sit on top of nine Organization
      rows of which five are called "Microsoft", so any query or instruction that
      names a principal by NAME resolves to A row silently — a cost already paid
      once, in a session spent working out which org was meant — LATENT
      (trigger: the next session that queries or mutates staging by name rather
      than by ID; or the next session that leaves fixtures unnamed, which removes
      the one property making any of this cleanable). The stale ADMIN
      PendingInvite for Taylin@keva.com is the sharp exception and is NOT covered
      by this verdict: whoever accepts it becomes an unscoped ADMIN of the org.
      Gary was actioning it 2026-08-01; this session has no database access and
      cannot confirm it is gone.
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02, not the audit's. FIRST
      DERIVED AS LIVE AND MOVED TO LATENT BY GARY 2026-08-02 on the
      product-vs-process distinction recorded above the `debt:` key: staging
      ambiguity costs whoever is working on staging, not a merchant using the
      app.
      RECORD HALVES RELOCATED 2026-08-02 by DEBT-TRIAGE-2, to two places because
      they fire in two places. (a) The fixture-naming convention — name every
      test principal for the phase that made it → CLAUDE.md § Staging
      Verification, as its own sub-section, since it binds the session CREATING
      fixtures rather than the one cleaning them up. (b) The store-delete cascade
      hazard → a comment at handleDelete in
      src/app/(app)/stores/store-actions.tsx, which is where someone is standing
      when it matters. NOTE THE FILE: DEBT-TRIAGE-2's prompt said
      stores/page.tsx; THIS ROW WAS RIGHT and the prompt's guess was not — the
      row says only "/stores' handleDelete" and the function is in the button
      island at store-actions.tsx:143, per CLAUDE.md's own *-actions.tsx
      convention. The comment also records the swallowed failure, which is what
      makes the cascade dangerous rather than merely destructive.
      THIS ROW ABSORBS DEBT-19's CLEANUP, 2026-08-02 by Gary's ruling. DEBT-19
      closed on relocating its naming convention to CLAUDE.md § Database
      Evidence, and its second half — deleting the dead single-user Organization
      shells on branch preview/staging that make the five "Microsoft" rows
      ambiguous — lands here: same branch, same org, and this row already carries
      the cascade analysis that cleanup needs. Do it BY ID, and per the analysis
      below delete the PRINCIPALS, not the stores.
      FILED 2026-08-01 (evening) by PERM-6c on Gary's instruction, and
      DELIBERATELY NOT CLEANED IN THE SAME SESSION.
      ON BRANCH preview/staging, org cmr54z65v000105jxczpt72w1:
      (1) StaffMember "PERM-6b Staff" (id cmsbe40q9000104l7fm3t6bp4), created
      through /staff -> Add Staff Member for the PERM-6c run.
      (2) The User created by accepting its self-service invite — id
      cmsbeaqn3000504l7uy2dd3wl, email indianathomas+staff@gmail.com, role
      STAFF, username perm-6-staff, one assignment (Carson), default store
      Carson, createdAt 2026-08-02 06:04:19.167 UTC, bound to (1). This is the
      principal that produced PERM-6 entry (1) item (4)'s two 403s; the identity
      is settled on that entry, from the DB row and from the "Self-service
      login" badge the /staff profile now renders. Delete both together — the
      binding is the point of the fixture.
      (3) The three PERM-7 device logins — indianathomas+store@gmail.com,
      +store2@ and +store3@ — IF ANY SURVIVE. Nobody has checked and this row
      does not claim they do. Each was role STORE, one store (Carson), named
      "#0034 — Carson"; see PERM-7's Task 7 closure.
      (4) A stale PendingInvite for Taylin@keva.com, role ADMIN, ZERO stores.
      UNRELATED to any run above, origin unknown, noticed while querying.
      GARY IS ACTIONING THIS ONE IMMEDIATELY, 2026-08-01 — it is not part of the
      deferred cleanup and should not wait on it. The reasoning is kept because
      it is why it was separated rather than filed with the rest: it is the only
      fixture here with a privilege consequence, since whoever accepts it
      becomes an ADMIN of this org. Zero stores means it grants no assignment,
      which is not a mitigation — ADMIN is unscoped.
      NOT ON THE LIST, deliberately: the scratch store "PERM-6b"
      (cmsbe0giw000004l7e3zhm22n) was DELETED during the run — that deletion was
      the test. Nothing to clean up there.
      WHY THIS IS DEBT AND NOT HOUSEKEEPING: DEBT-19 is the precedent. Nine orgs
      on this branch, five of them called "Microsoft", and the cost was a
      session spent working out which one. Test principals accumulate the same
      way and fail the same way — a query resolves to A row, silently. Every
      fixture above is named for the phase that made it, which is the only
      reason this is cleanable at all; the next session that leaves fixtures
      unnamed removes that.
      AUDITED AT THE DATABASE 2026-08-02 by DEBT-44a. Gary ran every query in
      the Neon console; this session had no database access and wrote no query
      result it did not receive pasted back. ON BRANCH preview/staging, branch
      id br-square-feather-a63z92vz carried by the queries themselves per
      § Database Evidence. Session prompt:
      docs/prompts/DEBT-44a_staging_cleanup.md.
      FIXTURES (1), (2) AND (3) ARE CONFIRMED GONE AT THE DATABASE. StaffMember
      cmsbe40q9000104l7fm3t6bp4 = 0 rows, User cmsbeaqn3000504l7uy2dd3wl = 0,
      the three indianathomas+store/2/3@ accounts = 0. No StaffMember or User
      anywhere on the branch still carries a phase name. All five referential
      orphan checks returned 0, so nothing outlived its principal.
      FIXTURE (4) — THE TAYLIN INVITE — WAS NOT GONE, and the gap between the
      page and the table is the finding. PendingInvite cmrdtbfjg000004l8yzo9loqb,
      Taylin@keva.com, role ADMIN, zero stores, created 2026-07-09 18:00:35.692,
      org cmr54z65v000105jxczpt72w1. Absent from /users and present in the
      database.
      "IT IS GONE" WAS GARY'S, AND IT WAS A UI OBSERVATION THE DATABASE
      CONTRADICTS. Recorded as provenance rather than as a fault: the prompt
      that carried the claim also instructed that it be verified at the database
      precisely because absence from a page is not absence from a table, and
      that instruction is what caught it. The same prompt's other three UI
      observations all held.
      IT IS NOT STALE CLUTTER, IT IS AN ARMED ADMIN GRANT, and the mechanism is
      worse than "someone might accept the original invitation". The webhook
      resolves the app role as `pending?.role ?? roleMap[membership.role] ??
      "STAFF"` (webhooks/clerk/route.ts:108) — the PendingInvite role BEATS the
      Clerk membership role — and the lookup that finds the row is deliberately
      case-INSENSITIVE (:103-105), so the mixed-case address is no protection.
      Any future invitation to that address in this org, AT ANY ROLE, makes the
      acceptor an unscoped ADMIN of the org that has Square connected and 297,382
      rows behind it. See DEBT-46 for why it was unreachable and unremovable
      through the product.
      THE PRECEDENCE FIRES ON THE CREATE BRANCH ONLY, and the sentence above is
      too broad read on its own — corrected here the same day rather than left
      to be over-applied. resolvedRole is consumed by a user.upsert whose UPDATE
      branch sets `{ email }` and nothing else (:145-155): role is written only
      when no User row exists for that clerkUserId. So the grant lands on a
      principal the database has never seen — a first-time acceptor, a new Clerk
      account on the same address, or an identity whose User row was deleted
      while its Clerk user survived, which is this row's own re-provisioning
      trap and DEBT-47's missing user.deleted. It does NOT re-role an existing
      member. The Taylin row satisfied that condition: the webhook DELETES the
      PendingInvite at the end of its consumption block (:217), so a surviving
      PendingInvite is itself proof the invitation was never accepted and no
      User row was ever created from it. The conclusion is unchanged and the
      delete was right; the REASON needed the condition attached, because the
      same sentence applied to a row whose invitee is already a member would be
      wrong — see DEBT-46's second instance, where it is.
      DELETED 2026-08-02, AND CONFIRMED ABSENT BY INDEPENDENT INVENTORY RATHER
      THAN BY RETURNING. The branch-guarded DELETE ran in the Neon console,
      which reported "Statement executed successfully" WITHOUT displaying the
      RETURNING row — so no RETURNING output exists and none is claimed. What
      stands in its place is a separate all-invites query run afterwards on
      br-square-feather-a63z92vz, returning exactly ONE PendingInvite on the
      whole branch, and it is not this one. THAT IS THE STRONGER PROOF, not a
      fallback: RETURNING is the deleting statement reporting on itself, while
      an independent read afterwards is a different query answering a different
      question. On this row of all rows — where the whole finding was that a UI
      observation and the table disagreed — the provenance of the proof is worth
      as much as the proof. Recorded on Gary's instruction.
      THE CLERK ASYMMETRY, ESTABLISHED FROM CODE, AND IT CHANGES THE ORG
      CLEANUP FROM A DELETE INTO A TWO-SIDED SEQUENCE. There is no
      `organization.deleted` handler and no `user.deleted` handler; the complete
      set at HEAD is organization.created (:41), organization.updated (:54),
      organizationMembership.created (:62), user.updated (:223) and
      organizationMembership.deleted (:243). So deleting a Clerk org removes
      nothing from the database, and deleting a Clerk user removes nothing
      either. Worse in the other direction: organizationMembership.created
      UPSERTS the Organization on clerkOrgId (:71), so a database row deleted
      while its Clerk org survives REGENERATES with a NEW cuid the moment anyone
      is added — and organization.updated does a bare `update` (:56), so a
      rename against a deleted row throws P2025, the webhook 500s and Svix
      retries. A cleanup that undoes itself and leaves a latent webhook failure
      is worse than no cleanup (Gary, 2026-08-02). The general fix is DEBT-47;
      the ordering below is the workaround while it does not exist.
      ORDER RULED 2026-08-02 (Gary), CONDITIONAL — CLERK FIRST, THEN DATABASE,
      and only for orgs whose Clerk side is ALSO being deleted. Clerk-first has
      no window: deleting the Clerk org fires organizationMembership.deleted per
      member, which on a zero-user org fails its `if (org && user && …)` guard
      and is a clean no-op (:264-266), after which the org can be neither
      re-created nor renamed and the database row is inert. Database-first opens
      both regeneration vectors for as long as the two steps are apart.
      "DO NOT CLEAN THESE UP" IS AN ACCEPTED ANSWER and is the ruling wherever
      the Clerk organization is being KEPT: there the database row is unsafe to
      delete in EVERY order, because the regeneration vector stays live
      permanently.
      NO ORG HAS BEEN DELETED AND NONE IS AUTHORISED. Gary is checking which
      Tier 1 clerkOrgIds exist on the dev Clerk instance before deciding.
      THE INVENTORY, 2026-08-02 on preview/staging, from a query that enumerated
      the FK list from pg_catalog rather than by hand — 45 constraints, 44
      RESTRICT and 1 CASCADE (PendingInvite), matching prisma/schema.prisma
      exactly with no drift. Nine orgs, and DEBT-19's nine ids and nine user
      counts reproduced exactly five days on.
      TIER 1, provably empty — every one of the 45 direct FK counts zero AND
      both assignment tables zero AND Square not connected:
      cmr54x6gp000005jx7odzwx9e "Microsoft" (org_3G00QYrKl7f4OBUeES31RWvnG76);
      cmr57tw80000004l49xf6ft71 "Microsoft" (org_3FzvLEsjLt90c2YivLbz9blQCfy);
      cmrx04zn7000004jy6mk1mmha "Keva Smoothie Company"
      (org_3GtAAT6xIp9v3v3flohP7anbdB4).
      TIER 2, one User row each and nothing else — NOT AUTHORISED, and they need
      a THIRD step nobody has ruled on, because organizationMembership.deleted
      leaves the User row standing and User is RESTRICT:
      cmqvpe2bf000004l1gwxafqk4 "Keva Juice"; 6b545d5b-3ea1-442d-b16d-9089984af8b6
      "Microsoft"; cmr55fhm1000004l5az4vu73j "Microsoft";
      cmrefoomo000004l7dq37gdgn "My Organization".
      READ THIS BEFORE USING THE UI ON A TIER 2 ORG: DELETE /api/users/[id]
      removes the Clerk account GLOBALLY, not just this org's membership — the
      route says so itself at users/[id]/route.ts:175-177 (UM-1). The /users
      Remove button on a shared account revokes it everywhere it is a member.
      TIER 3, not candidates: cmr54z65v000105jxczpt72w1 (live — Square AND
      Instagram connected, 297,382 direct child rows, excluded by its DATA and
      not by its name); and cf888f2d-f234-48c7-8097-fd5b44b5b3dd "Keva Juice",
      which is not a shell on any reading — 9 stores, 11 checklists, 8 templates,
      4 staff, 2 users, 1 pending invite.
      THE TWO UUID-SHAPED IDS STAY OFF THE LIST UNTIL PROVENANCE IS KNOWN
      (Gary's ruling). Organization.id is @default(cuid()) at HEAD and `git log
      -S` finds no commit where it was otherwise; gen_random_uuid() appears
      nowhere in the schema or in any migration; git history begins one day
      before 20260627002005_init, so there is no earlier era to inherit from.
      The mechanical point is sharper: the init DDL is `"id" TEXT NOT NULL` with
      NO database-level default — Prisma mints the cuid in the CLIENT — so any
      writer bypassing Prisma Client must supply its own id, and a UUID is what
      a person or tool picks when it has to invent one. 6b545d5b carries
      independent corroboration: `slug` is NULL, and it is the only org on the
      branch where that is true, while every code path that creates an
      Organization sets a slug unconditionally (:47 and :77). That is good
      enough to say it did NOT come through the webhook and NOT good enough to
      say what did. Nothing in scripts/ creates an Organization with a supplied
      id. Unresolved, deliberately.
      gary@keva.com IS NOT A CONTRADICTION — SETTLED 2026-08-02. The prompt
      reported THREE assignments (Carson, Las Brisas, Meadowood Mall) against
      PERM-6's recorded count of ONE, and the query confirmed three. PERM-6's
      record was CORRECT WHEN TAKEN: StoreUserAssignment has exactly two writers
      — the webhook's createMany from pending.storeIds (:180), which produced the
      one PERM-6 saw on the day the user was created, and PATCH
      /api/users/[id]'s `storeAssignments: { deleteMany: {}, create: … }`
      (users/[id]/route.ts:162-165), a FULL REPLACE from the /users edit dialog,
      which produced the three. Two true readings of different moments. Neither
      row needs correcting. WHAT IS WORTH KEEPING: an assignment count in a row
      is a READING, not a property — StoreUserAssignment has no createdAt, so
      nothing in the table can date the change, and the only reason this was
      reconstructible at all is that there are just two writers.
      "PERM-6b FLAGGED gary@keva.com" WAS GARY'S, FROM MEMORY, AND IS
      UNSUPPORTED. Recorded because the correction is the durable part. A search
      of docs/ finds no such flag in PERM-6b's prompt, PERM-6c's, or the PERM-6
      row. The nearest real finding is PERM-6's Task 3 delta check
      (ROADMAP.yaml:1815-1818) — Jordan Pippenger, a STAFF MEMBER with SEVEN
      StoreStaffAssignment rows, a different table and a different principal —
      and "Meadowood Mall" appears in the repo exactly once, in that line. Two
      findings fused in recall into one that was never filed. Same family as
      DEBT-45: an observation that lives only in a memory does not exist either.
      CLEANING UP IS NOT FREE — read before deleting. Deleting a STORE cascades
      its assignments — StoreUserAssignment.store AND StoreStaffAssignment.store
      are both onDelete: Cascade (prisma/schema.prisma:185 and :267) — and takes
      goals and caches with it, and /stores' handleDelete swallows the failure.
      That analysis is why PERM-6c created a throwaway store instead of deleting
      a real one, and PERM-6's entry (1) records the second cascade firing for
      real on the fixture in (1). Delete the PRINCIPALS, not the stores — and
      delete the Clerk side too, since a User row removed while its Clerk user
      survives is a re-provisioning trap rather than a cleanup.
      ONE MORE FIXTURE, ADDED 2026-08-05: the gary@kevajuice.com PendingInvite
      from the F4 test still shows Pending on staging /users. It was consumed or
      errored; the row lingers. Trash icon, ten seconds, next time anyone is on
      staging. Arrived as R8 on the 2026-08-05 pending-rulings sheet and was
      moved here by Gary's ruling the same day — it is a chore with no options
      and no decision in it, so it belongs with the fixtures rather than in the
      decision log. NOT the Taylin@keva.com ADMIN invite above, which is the
      sharp one; this is an ordinary leftover.

  - id: DEBT-45
    status: shipped
    commits: ["7d984be"]
    title: >
      CLOSED — Audit-only sessions leave no artifact: the DEBT-TRIAGE
      audit's 22 per-row assessments exist nowhere in the repo
    notes: >
      SHIPPED TO PRODUCTION 2026-08-07 in merge "fad9207" — the CLAUDE.md rule
      travelled in the same merge as DEBT-38's WORKFLOW.md fix, both from
      "7d984be".
      IT HAD ALREADY FIRED ONCE BEFORE PROMOTING, which is the useful thing to
      record: the TYPE-1 investigation session reported findings and wrote no
      artifact, with this rule already in the tree. The next session paid the
      debt — docs/prompts/TYPE-1_AUDIT.md, written at 2d5b93d and re-verified
      by reading the code rather than transcribing the transcript, which is how
      three of TYPE-1's claims turned out to be wrong. So the BACKSTOP half
      works and the AUTOMATIC half — the standing line in Gary's prompt
      template, which lives outside this repo — is still what this row said it
      was: the harder and more important one, and not enforceable from here.
      ── ORIGINAL CLOSURE NOTES BELOW, UNEDITED ──
      CLOSED 2026-08-07 — RULED YES. Audit-only sessions MUST write their
      findings to docs/prompts/<NAME>_AUDIT.md BEFORE reporting, and the report
      SUMMARISES that file rather than replacing it. The rule is in CLAUDE.md
      § Where documents live, commit "7d984be" — placed there rather than in
      WORKFLOW.md because that section already governs docs/prompts/ and already
      draws the pointers-vs-claims line an audit file sits on. DEBT-37's rule —
      an observation that lives only in a transcript does not exist — now
      operates at SESSION scale, which is exactly what this row argued for.
      THE SECOND HALF — WHAT MAKES IT AUTOMATIC RATHER THAN REMEMBERED — IS
      ANSWERED OUTSIDE THIS REPO: a standing line in Gary's prompt template, so
      the instruction arrives WITH the session instead of waiting to be recalled
      by it. That half cannot be enforced from here and deliberately leaves no
      repo artifact.
      SO CLAUDE.md IS THE BACKSTOP, NOT THE WHOLE FIX, and the rule says so in
      its own text rather than leaving a future reader to assume the repo has it
      covered. It catches the sessions the template misses. This row named the
      automatic half as the harder question and the reason it was filed rather
      than fixed; the honest closure is that the two halves landed in two
      places, only one of which this repo can see.
      THIS ROW'S SCOPE NOTE STILL HOLDS AND WAS NOT QUIETLY DROPPED: nothing was
      reconstructed. The missing DEBT-TRIAGE audit stays missing, and the 22
      assessments now in the rows remain DEBT-TRIAGE-2's own, derived at HEAD
      and labelled as such.
      STATUS IS `staging`, NOT `shipped`: "7d984be" is on the staging branch,
      unpushed and unpromoted. Flip on promotion.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      COST OF DOING NOTHING: DEBT-TRIAGE-2 had to re-derive all 22 cost
      assessments from scratch because the audit that produced them left nothing
      behind, and the same hole will swallow the next audit — LATENT (trigger:
      the next audit-only session that ends without writing its findings to a
      file; this is a process and tooling cost, not a product one).
      DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 2026-08-02.
      AWAITING GARY'S RULING: whether audit-only sessions must write a
      docs/<PHASE>_AUDIT.md, and if so what makes that AUTOMATIC rather than
      REMEMBERED. Both halves are the question — the second is the harder one and
      the reason this is filed rather than fixed. Recorded as a question, not a
      decision, on Gary's instruction 2026-08-02.
      FILED 2026-08-02 by DEBT-TRIAGE-2, from the hole it fell into.
      THE FACT: docs/prompts/DEBT-TRIAGE_records_vs_tasks.md is 111 lines and
      ends at "REPORT BACK, part one". It is the INSTRUCTION. The audit's actual
      output — a verdict, a destination, a fold-target and a cost-of-doing-
      nothing line for each of 22 rows — was reported into a chat transcript and
      never written to the repo. The relocation destinations survived only
      because Gary re-typed them into the DEBT-TRIAGE-2 prompt by hand; the 22
      cost lines did not survive at all, and DEBT-TRIAGE-2 derived its own at
      HEAD instead. Those are marked DERIVED-AT-HEAD-BY-DEBT-TRIAGE-2 on every
      row precisely so the provenance is not silently laundered into "the
      audit's".
      IT IS SYSTEMIC, NOT ONE SESSION'S LAPSE, which is the reason for a row.
      DEBT-37's rule — "an observation that lives only in a transcript does not
      exist" — was written about a SINGLE observation found in passing during
      DEBT-SWEEP. This is the same rule at whole-session scale: an audit is
      nothing BUT observations, so a session whose entire product is analysis has
      the largest possible exposure to it and no habit protecting it.
      THE COUNTER-EXAMPLE PROVES IT IS SOLVABLE, and is the reason this is worth
      asking rather than merely lamenting: DEBT-1a and DEBT-2a DID write their
      findings out, as docs/prompts/DEBT-1_AUDIT.md (28k) and
      docs/prompts/DEBT-2_AUDIT.md (27k), and both are still cited by live rows
      today — DEBT-35 reads its three-branch column evidence out of DEBT-2_AUDIT,
      and DEBT-31 took its branch ids from it. Those two sessions did the right
      thing because their prompts told them to. Nothing structural distinguishes
      them from the triage session except that instruction.
      SO THE REAL QUESTION IS THE SECOND HALF. A convention that depends on each
      prompt remembering to ask for it has the same failure mode as the
      DEPLOY_LOG gap in DEBT-38: a habit with no artifact behind it, working
      until the one time nobody remembers. Candidates, none chosen: a line in
      WORKFLOW.md § Session completion rules; a rule in CLAUDE.md § Where
      documents live, which already governs docs/prompts/ and already draws the
      pointers-vs-claims line an audit file would sit on; or making it part of
      whatever DEBT-38 lands on, since both are "the process produced no
      artifact" in different clothes.
      SCOPE NOTE — this is NOT a request to write the missing DEBT-TRIAGE audit
      retrospectively. That analysis is gone and reconstructing it from memory
      would manufacture exactly the kind of authoritative-looking claim DEBT-9
      and DEBT-43 were corrected for. The 22 assessments now in the rows are
      DEBT-TRIAGE-2's own, derived at HEAD, and labelled as such.
      Related: DEBT-37 (the single-observation version of this rule, and its
      precedent), DEBT-38 (a process gap with no artifact behind it), DEBT-24.

  - id: DEBT-46
    title: >
      A PendingInvite row is both unreachable and unremovable through the
      product — /users renders from Clerk's list, and the revoke that should
      delete the row compares email case-sensitively and reports success when it
      deletes nothing
    status: shipped
    notes: >
      CLOSED 2026-08-03 — THE MANUFACTURING PATHS, NOT THE ORPHANS. Read that
      line before the rest: this row closes on what MADE orphans and what LIED
      about it, NOT on the orphans themselves. Two live rows remain unreachable
      and unremovable through the product, and DEBT-52 carries them.
      THE REASON GIVEN FOR CLOSING WAS WRONG AND IS RECORDED HERE RATHER THAN
      QUIETLY DROPPED. The proposal was that Phase 1 fixed "unremovable", which
      is FALSE for the rows that are actually stuck: the revoke route declines
      to delete an orphan by design (404 when Clerk has no such invitation, 409
      when its status is not pending, no delete on either path), and it is
      unreachable for one anyway since it is keyed on a Clerk invitationId that
      an orphan does not have. What Phase 1 fixed is narrower — the button no
      longer MANUFACTURES orphans and no longer reports success when it deletes
      nothing. This row closes instead on the split-out clause in its own text
      below ("the lifecycle question ... has outgrown this row and should be
      split out then"), and on keeping its now-stale paragraphs readable as
      history rather than as current claims.
      SHIPPED: 426f07c — the revoke route deletes the DB row BEFORE the Clerk
      invitation, matches case-insensitively, returns 404 when Clerk has no such
      invitation and 409 naming the status when it is not pending, and
      RevokeInviteButton reads res.ok and renders the error it used to discard.
      The count is REPORTED, not failed: under DB-first ordering a 404-on-zero
      would return before the revoke and leave a dashboard-created invitation
      live while reporting "not found", so this row's own "return 404 on zero"
      suggestion was ruled against (R5) and the reasoning lives at the route.
      d098530 — both role writers resolve PendingInvite by NORMALISED email,
      newest createdAt wins, with a console.warn naming both row ids on a
      collision and the matching orderBy on the webhook, so the two writers
      cannot resolve one collision differently; also fixed RemoveUserButton,
      which carried the identical fire-and-forget defect twenty lines away on
      the more destructive action. 736ac99 — Clerk's list endpoints return the
      first TEN by default, so /users was showing at most ten pending
      invitations and the rest were invisible, which on that page means
      unrevokable; /users and GET /api/users now drain via fetchAllClerkPages.
      58589c9 — the session brief,
      docs/prompts/DEBT-46_pending_invite_case_and_orphans.md.
      NOT SHIPPED, and carried by DEBT-52: the orphan surface. No page lists a
      PendingInvite on its own merits, so a row whose Clerk invitation is
      revoked, expired or accepted-without-webhook still renders nowhere. Two
      exist: kevajuice14@icloud.com on preview/staging
      (br-square-feather-a63z92vz) and the inherited karson@keva.com on
      preview/main (br-purple-rain-a6m62xww). The production copy of the latter
      was revoked and its deletion MEASURED — karson_rows = 0 on
      br-sparkling-block-a620qvg4. Also unshipped: two Clerk list calls still
      capped at ten (staff-termination.ts:73, users/[id]/route.ts:135), both
      deferred with severity analysis on DEBT-52, and the /staff/[id] deadlock
      on DEBT-51.
      736ac99 IS NOT BEHAVIOUR-VERIFIED AND CANNOT BE HERE. The truncation
      symptom needs more than ten SIMULTANEOUSLY PENDING invitations in one org;
      below that threshold the fixed and unfixed code render byte-identical
      pages. Staging is nowhere near it — one PendingInvite row on the whole
      branch. Reproducing it would take eleven test principals. It rests on the
      Clerk type contract and review. The warnAbove log line is the substitute
      for a test: zero firings against anything measured on 2026-08-03, and a
      real count the first time an org crosses. A clean staging pass proves
      absence of REGRESSION, not that the truncation is fixed.
      THE CASE TRAP TURNED OUT TO BE EMPTY. Measured 2026-08-03 across all three
      branches — the two below plus production (br-sparkling-block-a620qvg4) —
      mixed_case_rows = 0 everywhere, and ci_sibling_count = 1 on every row that
      returned it, so the collision the fix guards against has no instance
      either. The paragraphs below saying production and preview/main "have
      never been examined" were true when written and are superseded by that
      measurement. d098530 is therefore HARDENING, not remediation: the danger
      that actually materialised was orphaning, which no amount of
      case-normalisation touches. TWO OF THIS ROW'S PARAGRAPHS ARE STALE BY
      DESIGN AND KEPT UNEDITED — "FIX DIRECTION, not yet ruled" (ruled: R1
      newest-wins, R3 DB-delete-first, R5 report the count) and the one-branch
      measurement above.
      ——— ORIGINAL TEXT BELOW, UNEDITED ———
      COST OF DOING NOTHING: A pending invitation can carry a live role grant
      that no page displays and no button can revoke, and the admin who tried to
      revoke it was told it worked — LIVE (trigger: already fired, on
      preview/staging, on the org with Square connected; the stale ADMIN invite
      in DEBT-44 sat invisible from 2026-07-09 to 2026-08-02 and was found by a
      query, not by the product). Sharpest on ADMIN, because the webhook prefers
      the PendingInvite role over the Clerk membership role, so an invisible row
      silently escalates the NEXT invitation to that address.
      FILED 2026-08-02 by DEBT-44a, from the Taylin invite in DEBT-44 (4).
      Established from code at HEAD; the database evidence is on DEBT-44.
      IT IS ONE DEFECT, NOT THREE, and that is why it is one row: unreachable
      AND unremovable is what makes it dangerous. Either half alone is
      survivable — an invisible row you can still revoke by address is tidy-up,
      and a visible row whose revoke fails is a bug someone notices. Together
      they make a live grant that nothing in the product can see or delete.
      THE THREE PATHS THAT READ PendingInvite.email, TWO OF WHICH DISAGREE WITH
      THE THIRD:
      (1) webhooks/clerk/route.ts:103-105 — case-INSENSITIVE, deliberately, and
      commented "older rows may hold mixed-case emails". This is the path that
      CONSUMES the row, so the grant always lands.
      (2) users/page.tsx:46 — builds `new Map(pendingInviteRecords.map(p =>
      [p.email, p]))` and looks it up at :157 with `inv.emailAddress`. Case
      SENSITIVE. A mixed-case row never enriches its Clerk invitation.
      (3) api/users/invitations/[invitationId]/route.ts:29-31 — `deleteMany({
      where: { organizationId, email: invitation.emailAddress } })`. Case
      SENSITIVE. On a mixed-case row it matches nothing.
      AND THE RENDER IS CLERK-DRIVEN, NOT DATABASE-DRIVEN, which is the half
      that makes it invisible rather than merely mislabelled. users/page.tsx:156
      maps over `pendingInvitations.data`, fetched at :25 with `status:
      ["pending"]`; the database rows at :37 are ONLY a lookup map. There is no
      code path anywhere that lists PendingInvite rows on their own. So a row
      whose Clerk invitation is revoked, expired or accepted-without-webhook
      renders NOWHERE — not as stale, not as orphaned, not at all.
      THE PART THAT MADE A REAL FAILURE INVISIBLE, and Gary asked for it
      recorded by name: the revoke route IGNORES deleteMany's count. Prisma
      returns `{ count: 0 }` rather than throwing, and the route returns
      `{ success: true }` at :34 regardless. An admin clicked revoke, was told it
      worked, and nothing was deleted. A destructive operation that cannot
      distinguish "deleted the row" from "matched nothing" reports the same
      success for both.
      THE MIXED CASE IS LEGITIMATE AND DATED, so this is a real class and not
      one malformed row. POST /api/users normalises to lowercase at write time
      (users/route.ts:102, via normalizeEmail in lib/clerk.ts:4-7), but that
      landed in 3c7d0a0 on 2026-07-22 — thirteen days AFTER the Taylin row was
      created on 2026-07-09. Every PendingInvite written before that commit may
      hold mixed case, which is exactly the class (1)'s comment was written for
      and exactly the class (2) and (3) mishandle.
      FIX DIRECTION, not yet ruled: normalise on read in all three paths, or
      backfill `lower(email)` and add a case-insensitive uniqueness constraint,
      or both. Whatever is chosen, the render must be able to show a
      PendingInvite that has no matching Clerk invitation — otherwise the row
      stays invisible even after the case bug is fixed, since revoked and
      expired invitations leave the same orphan. Checking deleteMany's count and
      returning 404 on zero is separable and smaller than either.
      A CHECK WHOSE COUNT AND DETAIL CAN DISAGREE, recorded here on Gary's
      instruction rather than left in a transcript, because it is the kind of
      thing that gets trusted later. DEBT-44a's own confirmation query returned
      `n = 1` with an EMPTY detail string for this very row: the detail
      expression concatenated `array_length(p."storeIds", 1)`, which is NULL for
      an empty array, and NULL || text is NULL, so string_agg had nothing to
      aggregate. Zero stores — the thing that made the invite notable — is what
      blanked the evidence describing it. A second, broader check in the same
      query is the only reason the id was recovered. Use
      `coalesce(array_length(...), 0)` in any re-run, and treat a check whose
      count and whose detail can disagree as a defective check.
      THE CASE TRAP HAS EXACTLY ONE KNOWN INSTANCE AND IT IS NOW DELETED —
      measured 2026-08-02 on branch preview/staging (br-square-feather-a63z92vz)
      by an all-invites query carrying `email <> lower(email)` as a column. After
      the Taylin delete the whole branch holds ONE PendingInvite:
      cmr07sljf000004l5yhwqzzme, kevajuice14@icloud.com, mixed_case_unrevokable
      FALSE. Recorded because a measured negative is evidence and usually goes
      unwritten — the trap is narrower than it looked.
      DO NOT READ THAT AS "THE CLASS IS CLOSED" (Gary, 2026-08-02). The query
      looked at ONE BRANCH. Every PendingInvite written before 3c7d0a0
      (2026-07-22) is still in scope on `production` and `preview/main`, neither
      of which has been examined, and the same three read paths are deployed
      there. One branch measuring clean is one branch.
      SECOND LIVE INSTANCE OF THE PRECEDENCE — NOT THE CASE BUG, AND NOT
      DANGEROUS, and it is the reason this row's scope is wider than the case
      trap that produced it. That surviving row is the one DEBT-17 already found
      and set aside: its Q1 on this branch returned kevajuice14@icloud.com with a
      User at role MANAGER against a PendingInvite at role STORE, correctly ruled
      NOT DEBT-17's bug because that error runs DOWNWARD (STAFF is the floor)
      while this discrepancy runs UPWARD (ROADMAP.yaml:4448-4451, verified at the
      row 2026-08-02 rather than from recollection). Role STORE, created
      2026-06-30, still pending after five weeks, in
      cf888f2d-f234-48c7-8097-fd5b44b5b3dd — the org whose provenance is unknown
      (DEBT-44). Direction is downward, so it grants nothing and is benign; it is
      the same mechanism as Taylin wearing the opposite sign.
      AND IT IS NARROWER THAN IT FIRST READS, per the create-branch correction on
      DEBT-44: because a User row for that address ALREADY EXISTS at MANAGER,
      re-adding that same Clerk identity hits the upsert's UPDATE branch and
      changes no role at all. STORE lands only if the User row is gone or the
      Clerk identity is new. So this instance is armed but not aimed — which is
      exactly why it is worth recording rather than actioning: it is what the
      mechanism looks like when it is NOT an escalation, and the pair
      (Taylin ADMIN-upward, this one STORE-downward) is the clearest available
      statement of what the precedence actually does.
      SCOPE WIDENED BY IT, DELIBERATELY, AND THE CALL IS ARGUABLE. This lands
      here rather than on a row of its own (Claude's recommendation, Gary's
      ruling 2026-08-02) because the defect a future reader needs to find is one
      thing — a PendingInvite can carry a live role grant that outlives its
      invitation and that the product does not surface — and splitting it across
      two rows fragments exactly the thing that took a query to see. The
      ARGUMENT AGAINST is real and is kept: this row's title is about a row that
      is unreachable AND unremovable, and kevajuice14 is neither — its case flag
      is FALSE, so the revoke button would work on it. What it shares is the
      payload, not the mechanism. If a third instance turns up that is removable
      and visible and still stale, the lifecycle question — nothing expires a
      PendingInvite, nothing reconciles one against Clerk, and Clerk invitations
      expire on their own while the database row does not — has outgrown this row
      and should be split out then.
      Related: DEBT-44 (the row this was found in, the delete, and the
      create-branch condition on the precedence), DEBT-47 (the other half of the
      Clerk/database asymmetry), DEBT-39 (unmirrored writers — the same family,
      one layer over), DEBT-17 (same page, /users, and the row that first saw
      kevajuice14 and correctly declined it).

  - id: DEBT-47
    title: >
      The Clerk webhook has no organization.deleted and no user.deleted handler,
      and organization.updated bare-updates — so deletion never reaches the
      database and a deleted row makes the webhook 500 on retry forever
    notes: >
      UPGRADED 2026-08-04: the consequence is no longer hypothetical. The
      production database (br-sparkling-block-a620qvg4) carries a User row
      (user_3Fq6iuvkhK4TSsmdaJ8yMMcT4rf, STAFF, kevajuice14@icloud.com,
      created 2026-07-03) whose Clerk identity no longer exists — deleted in
      Clerk, row orphaned, still counted by every org-scoped query (the
      live org shows 6 DB rows vs 5 Clerk members). The 2026-08-04 Clerk
      cleanup (blankettegirl user + My Organization org + Keva Smoothie
      Company org) added three more orphans by the same mechanism, tallied
      in DEBT-57. COST OF DOING NOTHING: LIVE — member counts and rosters
      silently disagree with Clerk today.
      ── ORIGINAL TEXT BELOW, UNCHANGED ──
      COST OF DOING NOTHING: Cleaning up an organization or a user requires a
      hand-sequenced two-sided dance that the product cannot perform and nothing
      records, and getting the order wrong silently regenerates what was deleted
      — LATENT (trigger: the next attempt to delete an Organization or User row
      whose Clerk identity survives, which is DEBT-44's staging cleanup and any
      future tenant offboarding; also fires unprompted the first time anyone
      renames a Clerk org whose database row is missing).
      FILED 2026-08-02 by DEBT-44a. Established from code at HEAD, from reading
      every handler in src/app/api/webhooks/clerk/route.ts.
      THE COMPLETE HANDLER SET is five events: organization.created (:41),
      organization.updated (:54), organizationMembership.created (:62),
      user.updated (:223), organizationMembership.deleted (:243). There is NO
      organization.deleted and NO user.deleted. Deletion is the only lifecycle
      verb the mirror does not implement.
      THREE CONSEQUENCES, and the second is the one that bites:
      (1) DELETION DOES NOT PROPAGATE. Deleting a Clerk org leaves the
      Organization row; deleting a Clerk user leaves the User row.
      organizationMembership.deleted fires per member and unlinks
      StaffMember.userId and deletes StoreUserAssignment rows (:264-266) — but
      it does NOT delete the User row, so even removing every member leaves the
      org undeletable, since User is one of the 44 RESTRICT FKs.
      (2) THE DATABASE ROW REGENERATES. organizationMembership.created UPSERTS
      the Organization on clerkOrgId (:71), explicitly because Clerk does not
      guarantee webhook ordering. So a row deleted while its Clerk org survives
      comes back the moment anyone is added — with a NEW cuid, the same
      clerkOrgId and the same name. The cleanup undoes itself, and the
      replacement is HARDER to reason about than what was deleted, because its
      id no longer matches anything on any row.
      (3) A RENAME AGAINST A DELETED ROW NEVER STOPS FAILING.
      organization.updated does a bare `prisma.organization.update` (:56), not
      an upsert. With no row it throws P2025, the handler 500s, and Svix retries.
      This one needs no cleanup to trigger it — it fires the first time anyone
      renames an org whose row is missing for any reason.
      WHY IT IS FILED RATHER THAN FIXED: DEBT-44a was an audit session with no
      code changes in scope, and the fix is a design question rather than a
      patch. Deletion semantics are not obvious — whether organization.deleted
      should cascade, refuse, or soft-delete is a product ruling, and with 44
      RESTRICT FKs a naive cascade handler would be the most destructive code
      path in the app, reachable from a Clerk dashboard button. Related: HR-14's
      Clerk-org-rename issue, which lives on the same handler this row calls out
      at (3).
      THE WORKAROUND, WHICH IS NOT THE FIX, and it is recorded on DEBT-44
      because that is where someone will be standing: delete the CLERK side
      first, then the database row, and only where the Clerk org is being
      deleted too. Where the Clerk organization is being KEPT there is no safe
      order at all and the ruling is do not clean it up. That is a live
      constraint on DEBT-44's Tier 1, and the reason its Tier 2 needs a third
      step nobody has authorised.
      Related: DEBT-44 (the cleanup this blocks, and the ordering ruling),
      DEBT-46 (the other half of the Clerk/database asymmetry), DEBT-39
      (unmirrored writers), HR-14 (the rename issue on handler (3)).

  - id: DEBT-48
    status: shipped
    commits: ["746918c", "d089a7c", "c05365a"]
    title: >
      CLOSED — Implement the checklist availability window, or remove
      the fields — the half of DEBT-29 that a relabel could not settle
    notes: >
      ── CLOSURE RIDER 2026-08-10 (CHK-5/S5), THE ONE THE S5 SKELETON OWES THIS
      ROW PER PLAN §10. PREPENDED; NOTHING BELOW IS EDITED. ──
      THE ROW CARRIED NO `status` FIELD AT ALL UNTIL NOW, WHICH IS THE ONLY
      THING ACTUALLY BEING FIXED HERE. Its own text has said "THIS ROW'S JOB IS
      DONE" since 2026-08-09 and its two open questions were answered there — so
      it read as closed to a human and as untriaged to anything that filters on
      status. That is the DEBT-54 bookkeeping class the CHK-4 rider corrected on
      DEBT-32, one row over.
      DISCHARGED IN THREE PLACES, hence three commits: "746918c" scoped it (the
      approved CHK-1 plan, which is what the row was holding its thinking FOR),
      "d089a7c" built the engine that made the offsets mean something (CHK-3,
      in production via merge "ddaa216"), and "c05365a" put the (i) explainer
      beside the fields (CHK-4), which is what discharges this row's own warning
      — "State it, or someone will implement them as enforcement later".
      THE ANSWER TO THE ROW'S TITLE QUESTION, PLAINLY: implement, not remove.
      The fields are EXPECTATIONS and never gates (R3), and nothing in the
      product is ever hidden by a window.
      ONE THING THE CLOSURE DOES NOT CLAIM: CHK-4 is in_progress, not
      staging-verified, so the explainer has landed in code and has not been
      read on a screen by anyone. That is CHK-4's gate to clear, not this row's
      — this row closes on the thinking being scoped and built, which it is.
      ── THE 2026-08-09 SCOPE NOTE IS NEXT, UNEDITED ──
      SCOPED 2026-08-09 — THIS ROW'S JOB IS DONE, AND IT SAID SO ITSELF. The
      scope note below reads "THIS ROW'S JOB IS TO HOLD THE THINKING UNTIL GARY
      SCOPES IT AS ONE." He scoped it: docs/prompts/CHK-1_PLAN.md, approved
      2026-08-09, committed in "746918c". The window is CHK-3 (the engine) and
      CHK-4 (the surfaces).
      BOTH RECORDED OPEN QUESTIONS ARE ANSWERED, and by which ruling is worth
      stating rather than leaving to be matched up. (a) "What does OVERDUE do
      across day boundaries?" → R1: overdue is a live state within the
      operational day ONLY; at day close an unfinished checklist becomes MISSED
      — closed, not actionable, permanently recorded — and a finished one may
      record completed-late. The row's own guess was right: "there is probably a
      point where an unfinished checklist stops being actionable and becomes a
      compliance record." (b) "What do the OFFSETS mean once the window is
      soft?" → R3: they are EXPECTATIONS, never gates, and nothing is ever
      hidden by a window. The row's warning — "State it, or someone will
      implement them as enforcement later" — is discharged by the (i) explainer
      CHK-4 ships beside the fields.
      THE OVERDUE-NOT-HIDDEN THINKING WAS PROMOTED FROM THINKING TO SPEC, which
      is precisely what the 2026-08-07 block below forbade doing without Gary
      scoping the phase first. He did. The two operating scenarios it argues
      from are now regression checks: the late opener's tasks stay completable
      (CHK-4 verifies nothing is ever hidden), and the 11am employee sees
      "Opening — Overdue" rather than an untouched list.
      ONE THING THE THINKING DID NOT ANTICIPATE, and it is the finding that
      shaped CHK-3: a checklist nobody starts HAS NO ROW. /store-view lists
      templates, not checklists, so MISSED had nothing to be written onto and
      day close must MATERIALIZE the miss. Neither this row nor DEBT-36 could
      see it from where they sat.
      THE ROW STAYS OPEN AND CARRIES NO STATUS, for the reason set out on
      DEBT-36's 2026-08-09 rider: no `status` value means "scoped into a phase
      that has not built yet", and any existing one would move a live defect
      into the RESOLVED list. It closes when CHK-4 ships; CHK-5 carries the
      instruction.
      SEQUENCING AGAINST DEBT-32 WAS HONOURED, and tightened. The note below
      says the fold is "the natural thing to do first if this is built, since a
      gate should read one list rather than three". CHK-3's expectedWindow()
      reads operationalPhase to pick its anchor, making a FOURTH reader — so the
      fold is CHK-2's opening move, before the new consumer is written.
      ── RULING OF 2026-08-07 BELOW, UNEDITED ──
      RULED 2026-08-07 — PARKED AS THE FEATURE PHASE THIS ROW'S OWN SCOPE NOTE
      DEMANDED. Neither direction the row put up (build the window, or remove
      the two offset columns) was taken as a debt-sized job, because the row is
      right that the subject has outgrown one: per-template availability with
      overdue states and a day-boundary policy is a feature phase. It gets
      scoped as one, TOGETHER WITH DEBT-36's Section entity, after the current
      minor-fix batch — two pieces of template-surface product work parked on
      the same terms and scoped in the same sitting.
      GARY'S OVERDUE-NOT-HIDDEN THINKING STANDS EXACTLY AS IT IS RECORDED BELOW
      — AS THINKING, NOT AS SPEC. Parking the row does not promote it. The block
      below still says nobody should implement from it without Gary scoping the
      phase first, and the two questions it leaves open — what OVERDUE does
      across day boundaries, and what the OFFSETS MEAN once the window is soft —
      are still unanswered inputs to that scoping rather than settled design.
      THE THIRD OPTION IS SPUN OUT AND IS NO LONGER THIS ROW'S CARGO. Making the
      offsets OPTIONAL and BLANK by default is now DEBT-59, filed 2026-08-07 on
      Gary's explicit ruling. It left this row because its whole value is doing
      it WHILE NO GATE EXISTS: it is small, it is doable now, and parking it
      inside a feature phase that has not been scoped yet would have parked it
      indefinitely — which is precisely the outcome that turns 1 and 2 into real
      windows on every existing template the day a gate ships.
      Row stays OPEN, title prefix changed from AWAITING RULING to RULED.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      COST OF DOING NOTHING: The form is now honest — it says the phase is a
      label and says the offsets are not yet used — so nobody is misled and
      nothing is broken; what is missing is a capability, not a fix — NONE.
      A legitimate NONE: DEBT-29 closed the harm on 2026-08-02, and what is left
      is a product decision that can wait indefinitely without anything
      degrading. DERIVED AT HEAD BY DEBT-29-RELABEL 2026-08-02, not
      DEBT-TRIAGE-2's — this row did not exist when that pass ran.
      CREATED 2026-08-02 by Gary's ruling when DEBT-29 closed on its RELABEL half.
      Same shape as DEBT-5 splitting into DEBT-27: closing DEBT-29 whole would
      have retired the subject from the open list while the capability question
      was still open and while three columns still collect values nothing reads.
      Prompt for the session that closed the other half:
      docs/prompts/DEBT-29-RELABEL_honest_copy.md.
      THE STATE AT HEAD, so nobody re-derives it: Template.operationalPhase,
      startOffsetHours and endOffsetHours are all collected, stored, exported and
      imported. The phase is a WORKING sequence key — it drives handoff-note date
      resolution, the note's shift_phase, and which templates you may hand off to
      (src/lib/messages.ts:38-67; handoff-notes.tsx:130, :138, :141). The two
      offsets are read by NOTHING. No code path joins either to StoreHours
      (prisma/schema.prisma:168-177), at generation or at listing.
      AWAITING GARY'S RULING: whether to build the window, or to remove the two
      offset columns and keep the phase as the label it already is.
      A THIRD OPTION, surfaced by DEBT-29-RELABEL and explicitly kept here rather
      than folded into the relabel: make the offsets OPTIONAL and blank by
      default, so templates stop carrying an invented 1/2 window nobody chose.
      Today they default to 1 and 2 (template-form.tsx:637-638) and are written
      on every StoreHours save, so the day a gate goes live those unchosen
      defaults become real windows for every existing row. That is a change to
      what the form writes, which is why it belongs to this decision and not to a
      copy change.
      SEQUENCING AGAINST DEBT-1 AND DEBT-32, carried over from DEBT-29 and still
      binding on the IMPLEMENT direction only: today a dirty phase string can at
      worst misorder a handoff note. Implementing the gate makes it decide WHEN A
      CHECKLIST APPEARS. DEBT-1 is `status: verified` — writers plugged in
      c17ccc1, backfill clean on all three branches, promoted to production
      2026-08-01 in 63407be — so the soak it demanded has happened; this is a
      pointer to the reason, not an open blocker. DEBT-32 (retire the I-14b alias,
      fold the three phase lists into one) is the natural thing to do first if
      this is built, since a gate should read one list rather than three.
      ────────────────────────────────────────────────────────────────
      GARY'S THINKING ON THE EVENTUAL SHAPE — RECORDED 2026-08-02 AS THINKING,
      NOT AS A RULING, AND NOT AS A SPEC. It is here so it is not lost. Nobody
      should implement from it without Gary scoping the phase first.
      THE SHAPE: availability is PER TEMPLATE, not per org, and a closed window
      makes a checklist OVERDUE — visible, flagged, still completable — never
      hidden.
      WHY, from two real operating scenarios. (1) An opener arrives late and
      misses the window. Under hard enforcement the morning checklist disappears
      and the cash-drawer count and petty-cash audit go undone. Nobody skipped
      them; the system withdrew them. For a compliance product a task that
      VANISHES is worse than one left unfinished, because unfinished is visible
      and vanished is not. (2) An owner-operator ignores the opening checklist
      because they can. The 11am employee opens the app and sees it untouched —
      is it theirs? was it skipped? should they do it four hours late? With no
      enforcement at all the list stops meaning "what you need to do" and starts
      meaning "everything that exists".
      OVERDUE-NOT-HIDDEN ANSWERS BOTH: (1) keeps the tasks completable, and (2)
      gives the 11am employee "Opening — overdue, not completed", which is an
      answer rather than a puzzle.
      PER TEMPLATE because the two scenarios differ BY CHECKLIST, not by org. A
      cash-drawer count is genuinely time-bound; a deep-clean list is "do this
      today". One org-wide switch forces one answer for both. Per-template also
      makes the whole question lower-stakes: with overdue-not-hidden the worst
      case is a longer list, not a missed audit.
      TWO QUESTIONS THIS SHAPE RAISES, UNANSWERED, recorded so they are not
      rediscovered. (a) What does OVERDUE do across day boundaries? A closing
      checklist unfinished at 11pm is overdue; at 6am is it still overdue, or
      closed out and recorded as missed? Without an answer the opening crew
      inherits a growing pile from previous shifts. There is probably a point
      where an unfinished checklist stops being actionable and becomes a
      compliance record. (b) What do the OFFSETS mean once the window is soft?
      Today they read as a hard boundary. Under overdue-not-hidden they describe
      an EXPECTED start and end, not an allowed one — the same two numbers
      carrying a different meaning. State it, or someone will implement them as
      enforcement later.
      ────────────────────────────────────────────────────────────────
      SCOPE NOTE, AND IT IS THE POINT OF THIS ROW: this has outgrown a debt row.
      Per-template availability with overdue states and a day-boundary policy is
      a FEATURE PHASE, not a cleanup. THIS ROW'S JOB IS TO HOLD THE THINKING
      UNTIL GARY SCOPES IT AS ONE. Do not let it ride along with a debt session.
      Disconfirmed by: any availability filter keyed on operationalPhase or the
      offsets appearing in the codebase — at which point the IMPLEMENT direction
      was taken and this row describes history. Related: DEBT-29 (the relabel
      half, closed), DEBT-1, DEBT-32, DEBT-30.

  - id: DEBT-49
    title: >
      No admin control sets StaffMember.isCorporate — the DEBT-9 designation is
      SQL-only, so a corporate hire cannot be marked without a Neon console
    notes: >
      COST OF DOING NOTHING: The two known corporate staff are set by SQL in
      DEBT-9 Phase 4 and every read path honours the flag, so nothing is wrong
      for anyone who exists today; the gap opens only when a THIRD corporate
      person is hired and no one in the product can mark them — LATENT (trigger:
      the next corporate hire, or any store-homed employee moving to corporate.
      Until then the flag is correct for 100% of rows).
      CREATED 2026-08-02 by Gary's ruling deferring DEBT-9 Phase 3d. The reason
      for the defer was scope, not doubt: DEBT-9 Phase 0 finding 4 added the
      whole self-serve signing path to that session, which put HR-11 ceremony
      re-verification on the critical path, and the toggle has no users today.
      Prompt: docs/prompts/DEBT-9_corporate_work_location.md.
      THE ASSESSMENT IS THE CONTENT OF THIS ROW — measured at head during
      DEBT-9 Phase 0, recorded so it is not re-derived. Two things make this
      bigger than a checkbox, and both are easy to miss on a second look:
      (1) THE GATE ASYMMETRY. The natural home, StaffEditActions, renders for
      ADMIN *and* in-scope MANAGER — src/app/(app)/staff/[id]/page.tsx:545 gates
      it on canSeeNotes (:87, ADMIN || MANAGER). But the brief requires this
      control to match the PRIMARY-STORE CHIP path, which is ADMIN-only:
      src/app/(app)/staff/page.tsx:266 passes canEdit={isAdmin}. PATCH
      /api/staff/[id] admits managers wholesale (route.ts:29-32) and documents
      the asymmetry it already lives with at :94-98. So isCorporate would be the
      FIRST field in that route needing its own per-field role gate. The
      recommendation at the time — not ruled on — was a separate ADMIN-gated
      control rendered NEXT TO StaffEditActions rather than a field inside the
      manager-visible dialog, precisely to avoid adding a per-field gate to a
      shared route.
      (2) THE STALE-STAR RULE, genuinely undecided. Flagging someone Corporate
      while they still hold an isPrimary assignment leaves a starred chip on
      /staff/[id] (page.tsx:501-507) that primaryStoreName() now ignores — the
      UI would assert a home store the signed record contradicts. Whoever builds
      this must pick one: clear isPrimary on flagging, refuse the flag while a
      primary is set, or render the star suppressed for corporate staff. The
      Zod schema (route.ts:7-17), the dialog state (staff-edit-actions.tsx:56)
      and BOTH transaction branches (route.ts:125-153) have to agree on whichever
      is chosen — the branch that replaces assignments wholesale and the
      light-touch branch that only moves the flag.
      NOT A BLOCKER ON ANYTHING. DEBT-9's read paths, surfaces and data all ship
      without it; this row is purely the write affordance.
      Disconfirmed by: an isCorporate control appearing in staff-edit-actions.tsx
      or beside it with an ADMIN check in PATCH /api/staff/[id]. Related: DEBT-9
      (the parent), BUILD-2 (the primary-store chip path this must match).

  - id: DEBT-50
    title: >
      No in-app organization switcher — once an active org is set there is no
      way to change it, and two orgs answer to the name "Keva Juice"
    notes: >
      RESTATED 2026-08-04 after the audit + fix + evidence cycle. The row's
      two halves now stand as: (A) DUPLICATE IDENTITIES — mechanism ruled
      (see DECISIONS.md 2026-08-04): no live duplicates on production, the
      dev pair is a known artifact of dev's open configuration, the
      measured production "duplicates" are fossils (one cross-instance, one
      DEBT-47 ghost). Production minting paths closed by F4 (3784c34) and
      restricted mode (F3 ruling). The Task 5 invite guard remains an
      optional hardening, deprioritised. (B) ORG SWITCHER / MULTI-ORG —
      unchanged: not supported by the schema (clerkUserId @unique + required
      organizationId), sized L (Option 1, composite uniqueness) in the
      2026-08-04 audit, and MUST NOT ship before the DEBT-55 sweep; the F1
      guard (5695aab) is the prerequisite that made the current state safe.
      This half stays planned.
      The row's title and Disconfirmed-by clause were wrong (tested the
      switcher, not the duplicates) — corrected with this restatement.
      ── ORIGINAL TEXT BELOW, UNCHANGED ──
      COST OF DOING NOTHING: Nobody is blocked from working, because a session
      that lands on the right org stays on it; the cost lands on whoever needs
      to CHANGE org — they must leave the app for the Clerk account portal or
      sign out — and on anyone verifying anything in a browser, who has no way
      to confirm in-app which org they are looking at — LATENT (trigger: named
      and it already fired once. Any user belonging to 2+ orgs, which on dev is
      gary@kevajuice.com today; and any browser verification session, which is
      how this was found).
      FOUND 2026-08-02 during DEBT-9 Phase 3, by walking into it. A ceremony
      walk was started against org_3FhMmIWVjja5HYpsou8n6rVtZn2 while the fixture
      under test lived in org_3FhYUR4l0ue7egug1I0Ig8wxOVn. /staff returned 200,
      rendered a plausible roster, and passed the hr module gate — both orgs
      carry hr in activeModules. Nothing errored. The session was simply looking
      at the wrong company. Prompt:
      docs/prompts/DEBT-9_corporate_work_location.md.
      CORRECTION, SAME SESSION, AND IT CHANGES WHAT THIS ROW IS FOR. The first
      draft of this row said the session was on the wrong org and needed a
      switcher. Measured afterwards in the page via window.Clerk, that was
      wrong: the signed-in account had membershipCount = 1. THERE WAS NO SWITCH
      TO MAKE. The actual condition is DUPLICATE CLERK ACCOUNTS SHARING AN
      EMAIL — two accounts both `gary@kevajuice.com`,
      user_3FhMl9WeyNaGexqdaCEa5gEkU6P (admin of the 0-store org) and
      user_3FhYUMgQoq9CFsnypYy3in9ajCg (admin of the 9-store org) — each a
      member of exactly one of the two same-named orgs. A switcher would NOT
      have resolved that incident; nothing in-app could have, because the
      account genuinely could not reach the other org.
      SO THE (a) HALF BELOW IS THE VALUABLE ONE, and this correction is why. A
      persistent active-org display would have shown the session was somewhere
      unexpected within seconds. The (b) switch half remains worth building on
      its own merits and would not have helped here. Recorded rather than
      quietly rewritten because the wrong diagnosis was ARRIVED AT from real
      evidence and held for several minutes — the roster was genuinely the
      wrong org's, which made "you're on the wrong org, switch" feel complete.
      THE STATE AT HEAD: src/app/(app)/layout.tsx:20 renders Clerk's
      <OrganizationList> ONLY inside `if (!orgId)`. Once Clerk has an active
      org that branch is unreachable, and nothing else in the app renders an
      OrganizationSwitcher — grepped at head, the only other setActive hits are
      unrelated useState calls in labor settings and the roadmap client. The
      sidebar shows the user's name and email and a Sign out button; it does
      NOT show which organization is active.
      TWO PARTS, and the cheap one is worth more than the expensive one.
      (a) DISPLAY the active org name somewhere persistent — the sidebar, next
      to the user block. Cheap, and it is what turns a silent wrong-org session
      into an obvious one. (b) SWITCH orgs in-app, via Clerk's
      <OrganizationSwitcher>. More involved: it needs a decision about what
      happens to in-flight state on switch, and every page is org-scoped.
      Doing (a) alone would have prevented the incident above.
      DISPLAY IS NOT ENOUGH ON ITS OWN, and this is the part not to lose: BOTH
      dev orgs are named "Keva Juice". A sidebar showing the NAME would have
      read "Keva Juice" in the wrong org too. Whatever is displayed must
      disambiguate duplicates — the org id, or a slug, or a store count. This
      is the same lesson as the branch-vs-branch-id rule in CLAUDE.md § Database
      Evidence: a name is not an identifier.
      RELATED CLEANUP, not this row: the duplicate orgs AND the duplicate
      accounts. DEBT-44 owns deleting dead org shells on staging; dev has the
      same condition and worse (four orgs, two named "Keva Juice", two empty;
      plus two Clerk accounts on one email). Cleaning dev is not tracked
      anywhere yet — but note that cleanup would NOT close this row, because a
      single-org user today is a two-org user tomorrow.
      MEASURED AT HEAD on branch dev (br-broad-wave-a6vpjdw0), 2026-08-02, so
      the next reader does not re-derive it:
        org_3FhYUR4l0ue7egug1I0Ig8wxOVn  "Keva Juice"  9 stores  6 staff
          ADMIN gary@kevajuice.com   user_3FhYUMgQoq9CFsnypYy3in9ajCg
          ADMIN taylin@keva.com      user_3G9oexUgTFFOEkH4Eu6UKLXdp4O
        org_3FhMmIWVjja5HYpsou8n6rVtZn2  "Keva Juice"  0 stores  1 staff
          ADMIN gary@kevajuice.com   user_3FhMl9WeyNaGexqdaCEa5gEkU6P
      Only the FIRST org can exercise anything store-shaped. Any future browser
      verification on dev should assert its org id before trusting a screen.
      CORRECTION APPENDED 2026-08-06 (R5, ruled) — THE FIRST ORG ABOVE IS NOT A
      DEV ORG. The block is left byte-unchanged because the MEASUREMENT is
      sound: those rows really are on branch dev, and the branch is named on the
      line, exactly as the house rule requires. What is wrong is the inference
      drawn from them. org_3FhYUR4l0ue7egug1I0Ig8wxOVn is a PRODUCTION Clerk org
      (Keva Juice, 5 members — production Clerk dashboard 2026-08-04, and
      production SQL on br-sparkling-block-a620qvg4 joining Organization
      cf888f2d-f234-48c7-8097-fd5b44b5b3dd to that clerkOrgId). Dev was forked
      FROM production and inherited its Organization rows verbatim, clerkOrgId
      included, so that string is present in the dev DATABASE while the Clerk
      org itself lives on the production INSTANCE. Clerk-side truth wins over a
      DB row.
      WHAT SURVIVES UNCHANGED: the incident, the duplicate-accounts diagnosis,
      and the (a)-over-(b) conclusion. The browser was on the dev instance
      throughout — the same session recorded membershipCount = 1 via
      window.Clerk, and the DEBT-9 gate independently records that the
      browser-reachable account was NOT a member of org_3FhYUR… and that adding
      the membership was considered and REJECTED. Nothing production-side was
      touched on 2026-08-02.
      WHY THIS IS THE ROW THAT GETS THE CORRECTION: this block is the SOURCE of
      the same misattribution in CLAUDE.md § Browser Evidence. Correcting that
      passage while leaving its source intact would re-seed the error from here
      the next time someone reads the row. Both now carry the same standing
      lesson — NAME THE INSTANCE AS WELL AS THE ORG ID. A Clerk org id
      identifies an instance, not a database branch; a row carrying it proves
      only that some fork once held that row.
      Disconfirmed by: an OrganizationSwitcher, or a persistent active-org
      display that distinguishes two same-named orgs, appearing in the app
      shell. Related: DEBT-44 (org shell cleanup), CLAUDE.md § Browser Evidence
      (the precondition written the same day this was found).

  - id: DEBT-51
    title: >
      An orphaned PendingInvite soft-deadlocks /staff/[id] — the "Invite pending"
      badge hides the re-invite button and that page has no revoke control
    notes: >
      COST OF DOING NOTHING: A staff member whose self-service invite row is
      orphaned can never be re-invited from their own page — the button that
      would do it is hidden by the very row that is stuck — LATENT, and only
      for rows carrying a staffMemberId (trigger: any orphaned staff invite;
      zero measured today, since the one live orphan on preview/staging
      (kevajuice14@icloud.com) has staffMemberId null and the production row
      karson@keva.com was revoked 2026-08-03).
      CREATED 2026-08-03 by Gary's ruling during DEBT-46 Phase 0. Deferred on
      COST, not doubt — see the reasoning below. Prompt:
      docs/prompts/DEBT-46_pending_invite_case_and_orphans.md.
      THE MECHANISM, measured at head during DEBT-46 Phase 0.
      src/app/(app)/staff/[id]/page.tsx:92 reads invitePending with a
      findFirst on staffMemberId — NO Clerk call, so it is the one
      database-driven PendingInvite surface in the app and the only place an
      orphan renders at all. It renders as a warning badge (page.tsx:505) and,
      at self-service-actions.tsx:170, it GATES the "Invite to self-service"
      button: `email && !hasLogin && !invitePending`. So the orphan is not
      merely visible-but-stale, it actively blocks the recovery action. The
      page carries no revoke or delete control, so the only exits are accepting
      the dead invitation or terminating the member (staff-termination.ts:60
      deletes by staffMemberId and does clear it).
      WHY DEFERRED RATHER THAN FOLDED INTO DEBT-46 PHASE 3. Two reasons, and
      the second is the one that decided it.
      (1) PHASE 3 RELIEVES IT AS A SIDE EFFECT. Phase 3 lists orphaned
      PendingInvite rows on /users with a delete action; deleting the orphan
      there clears the row, which un-hides the button here. So an admin who
      knows about /users is never permanently stuck — the deadlock is on this
      page, not in the data.
      (2) THE FIX COSTS A CLERK CALL ON A PAGE THAT MAKES NONE. Making
      /staff/[id] self-sufficient means classifying the row against Clerk's
      invitation list — the same classification Phase 3 builds — which adds a
      Clerk round-trip to a staff detail page that currently makes zero, on
      every staff view. That is a real per-load cost for a case with no
      measured instances.
      WHAT A FIX WOULD LOOK LIKE, so it is not re-derived: either pass the
      classification down from a shared helper (cheapest if Phase 3 already
      built one and something upstream is already fetching invitations), or
      gate the button on `!invitePending || inviteIsOrphaned` with the orphan
      determination made server-side. Do NOT simply drop the badge — it is
      correct information; the defect is that it gates an action.
      Disconfirmed by: /staff/[id] offering a way to clear or ignore a stuck
      invite, or the re-invite button ceasing to be gated on a row whose Clerk
      invitation is not pending. Related: DEBT-46 (the parent — Phase 3 is the
      /users half of the same design gap), HR-7 (which created the
      staffMemberId invite path this deadlocks on).

  - id: DEBT-52
    title: >
      No surface lists an orphaned PendingInvite — a row whose Clerk invitation
      is gone still carries a role grant nothing can show, and expiry makes one
      with nobody touching anything
    notes: >
      COST OF DOING NOTHING: Two orphans exist right now and neither is visible
      in the product; each carries the app role its invite was written with, and
      that role BEATS the Clerk membership role on acceptance — ACTIVE, not
      latent (trigger: already fired twice, and expiry fires it again on every
      invitation that is neither accepted nor revoked before it lapses).
      CREATED 2026-08-03 by Gary's ruling scoping DEBT-46 Phase 3 to step 1
      only. Step 1 (pagination) SHIPPED; this row is the remainder, deferred for
      fresh attention rather than for doubt. Prompt:
      docs/prompts/DEBT-46_pending_invite_case_and_orphans.md.
      STILL UNREACHABLE AND STILL UNREMOVABLE — DEBT-46 CLOSED WITHOUT FIXING
      EITHER FOR EXISTING ROWS, and this paragraph is the reason that closure is
      not a claim of safety. DEBT-46's argument was that unreachable AND
      unremovable together are what make an orphan dangerous. Phase 1 fixed the
      revoke button MANUFACTURING orphans and fixed it reporting success when it
      deleted nothing. It did NOT make an existing orphan removable. The route
      reads the invitation first and returns 404 when Clerk has none or 409 when
      the status is not pending, and on both paths it declines to delete the row
      BY DESIGN — deleting on an error return would be a hidden destructive side
      effect. It is unreachable anyway: the route is keyed on a Clerk
      invitationId, and the only control that supplies one renders on pending
      invitations only. So for the two live orphans BOTH halves of DEBT-46's
      danger condition remain true today, and work item (c) below is the only
      planned path to removal. Until it ships, the sole way to delete an orphan
      is by hand in the Neon console, which is how karson@keva.com was cleared
      on production.
      THE DESIGN GAP. /users builds its pending list from CLERK
      (getOrganizationInvitationList, status pending) and uses the database rows
      only as a lookup map — so a PendingInvite whose Clerk invitation is
      revoked, expired or accepted-without-webhook renders NOWHERE. /stores
      reads the rows but only flattens them into takenEmails, where an orphan
      silently reserves an address and steers the admin to a plus-addressed
      variant for a reason it never states. /staff/[id] is the sole
      database-driven surface and shows only a boolean badge, for rows carrying
      a staffMemberId (DEBT-51). Rows written by POST /api/users — the dangerous
      shape, ADMIN with zero stores — have staffMemberId null and NO surface at
      all.
      WHY THE GRANT MATTERS: the webhook resolves the app role as
      `pending?.role ?? roleMap[membership.role] ?? "STAFF"`, so the row wins.
      It fires on the upsert's CREATE branch only — a first-time acceptor, a NEW
      Clerk account on the same address, or an identity whose User row was
      deleted while its Clerk user survived. It does NOT re-role an existing
      member; the broader claim is wrong and has been stated wrongly before.
      FOUR WAYS AN ORPHAN IS MADE, measured or code-derived 2026-08-03, one of
      them now closed:
      (1) revoke-first-delete-second on a case-mismatched row — CLOSED by
      426f07c, which reversed the order and made the delete insensitive;
      (2) a revoke performed in the CLERK DASHBOARD, which never reaches
      /api/users/invitations/[invitationId] — the webhook handles five event
      types and NONE of them are invitation events, so nothing in Froot reacts;
      (3) /users auto-sync creating the User without deleting the PendingInvite
      (page.tsx) — only the webhook deletes, so any acceptance the webhook does
      not complete strands the row, and merely LOADING /users is enough;
      (4) EXPIRY, which needs no user action at all: Clerk drops the invitation
      from the pending list, nothing deletes the row, and the grant goes
      invisible while everyone is asleep. This is the strongest argument for the
      row and it is the one nobody can be trained out of.
      THE TWO LIVE ORPHANS, both named by branch per CLAUDE.md § Database
      Evidence: preview/staging (br-square-feather-a63z92vz), org cf888f2d…
      (org_3FhYUR4l0ue7egug1I0Ig8wxOVn), kevajuice14@icloud.com STORE 1 store
      createdAt 2026-06-30 — a User exists for that address, so it was accepted
      and the row survived, which is path (3); Clerk logs FIVE invitations to it
      (3 accepted, 2 revoked), the revoked pair being path (2)'s own record.
      preview/main (br-purple-rain-a6m62xww) inherits karson@keva.com from
      production. The production copy was revoked 2026-08-03 and its deletion
      MEASURED (karson_rows = 0 on br-sparkling-block-a620qvg4) — that one is
      closed, and it is the reason this row is not urgent.
      THE WORK, in order. (a) classify every PendingInvite against Clerk by
      fetching all four statuses (pending/accepted/revoked/expired) — ONE call,
      not an extra one, since the pending list becomes a filter over the same
      data, and step 1's fetchAllClerkPages already drains it; (b) an "orphaned
      invites" section on /users rendered only when non-empty, showing role,
      stores, createdAt and WHICH of the four statuses explains it; (c) DELETE
      /api/users/pending-invites/[id], keyed on PendingInvite.id and NOT on a
      Clerk invitation id which by definition may not exist, 404 on count zero
      like 426f07c; (d) ONE-WORD HARDENING that (c) is what makes necessary —
      webhooks/clerk/route.ts delete -> deleteMany, because an admin deleting an
      accepted-bucket orphan in the seconds before the webhook fires would throw
      P2025 and Svix would retry that 500 forever.
      NOT A SUBSTITUTE: handling organizationInvitation.* webhook events would
      close paths (2) and (3) at the source, but it is unconfirmed that Clerk
      emits an EXPIRY event at all — and expiry is the path with no user action
      behind it. The UI surface is required either way.
      TWO CLERK LIST CALLS ARE STILL CAPPED, deferred by Gary 2026-08-03 with
      the severity analysis attached so it is not re-derived. Both are the same
      defect DEBT-46 Phase 3 step 1 fixed on /users and GET /api/users — Clerk's
      list endpoints return the first TEN by default — and both are three-line
      changes against the helper that now exists, src/lib/clerk.ts
      fetchAllClerkPages. Deferred because both fail in survivable directions,
      not because they are wrong:
      (i) src/lib/staff-termination.ts:73 — getOrganizationInvitationList with
      NO limit, so terminating a staff member revokes their pending Clerk
      invitation only if it sits in the first ten. Past that the invitation
      stays live and redeemable AFTER termination. LESS SEVERE THAN IT READS:
      the PendingInvite row is deleted by id regardless, so the role grant is
      already gone and an acceptor lands STAFF via roleMap, and the status gate
      catches an accepted-later invite (the catch block there says so). Residual
      is a terminated person obtaining a STAFF login — real, but not an
      escalation. Trigger: >10 pending invitations in the org at termination.
      (ii) src/app/api/users/[id]/route.ts:135 —
      clerk.users.getOrganizationMembershipList with NO limit. This lists the
      ORGS A USER belongs to, not the members of an org, so the threshold is a
      user in more than ten organizations. Past it the membership is not found
      and the route returns 409 "User has no Clerk membership in this
      organization" for a user who has one. FAILS CLOSED — it refuses the role
      edit rather than mis-granting, which is the right direction to fail, and
      is why this is the lower priority of the two. Note DEBT-50: multi-org
      users already exist here, so the trigger is not exotic, only distant.
      MEASUREMENT HAZARD INHERITED FROM DEBT-46, kept because this row will
      re-run these queries. DEBT-44a's confirmation query on the Taylin row
      returned `n = 1` with an EMPTY detail string: the detail concatenated
      `array_length(p."storeIds", 1)`, NULL for an empty array, and
      `NULL || text` is NULL, so `string_agg` aggregated nothing. Zero stores —
      the thing that made the invite notable — is what blanked the evidence
      describing it. Use `coalesce(array_length(...), 0)`, and treat any check
      whose count and whose detail can disagree as defective.
      Disconfirmed by: /users showing a PendingInvite whose Clerk invitation is
      not pending, with a way to delete it; and both call sites above draining
      through fetchAllClerkPages. Related: DEBT-46 (the parent, CLOSED
      2026-08-03 on the manufacturing paths only), DEBT-51 (the /staff/[id]
      half, which this relieves as a side effect), DEBT-50 (the multi-org
      condition behind (ii)'s trigger), DEBT-44 (where this was found, and the
      create-branch condition on the precedence), DEBT-17 (which first saw
      kevajuice14 and correctly declined it), DEBT-47 (the other half of the
      Clerk/database asymmetry).

  - id: DEBT-53
    status: shipped
    commits: ["5695aab"]
    title: >
      Cross-org privilege escalation in getCurrentUser() — wrong-org User row
      was admitted with its role
    notes: >
      SHIPPED TO PRODUCTION 2026-08-04 in 999cbdc; 5695aab is named in that
      promotion's DEPLOY_LOG entry and is an ancestor of origin/main. Status
      corrected from `staging` by the 2026-08-06 bookkeeping sweep — the row
      recorded its staging verification and was never advanced when the same
      commit went to prod the following day.
      VERIFIED ON STAGING 2026-08-04 (Gary). Deployed SHA confirmed
      3784c34c511f898087af57ce9ffa3820db18f583. Guard-path test: a
      two-membership identity (indianathomas+f1test, dev instance) with its
      User row in org cmr431pps000004lgngxtm5ku ("Microsoft") and session
      under the dev Keva Juice org was refused — /users, /settings, /staff,
      /stores all bounced to /dashboard, store lists empty, and the Vercel
      runtime log shows "[auth] cross-org User row refused:
      clerkUserId=user_3HSdr0ylU1kkaYGlEnMQWQA8qx5 ..." firing on /stores
      (307) and /dashboard. Happy path unchanged for single-org users. Test
      account deleted after.
      COST OF DOING NOTHING: LATENT, one dashboard click from LIVE — a Clerk
      identity with memberships in two orgs was admitted to the second org
      carrying the first org's role, and isAdmin short-circuits store
      scoping, so an ADMIN of org A saw every store, staff member, count and
      message in org B. Trigger: adding any second org membership to any
      existing identity — a single Clerk-dashboard action, no code change.
      MECHANISM: clerkUserId is @unique on User, so exactly one row exists
      per Clerk identity globally. auth.ts:67 resolved that row by
      clerkUserId alone; requireAdmin() (:74) tested dbUser.role without
      comparing dbUser.organizationId to the session org.
      FIX: a wrong-org row is treated as ABSENT — the same shape
      getActiveStaffSelf() has used since HR-7. Mismatch fails closed with a
      console.warn naming both org ids (Gary's ruling), so a lockout is one
      log grep, not a silent bounce loop.
      WHY IT IS SAFE: the change introduces no new throw — it only turns a
      non-null dbUser into null, which all 171 call sites already handle
      because findUnique could always return null. Enumerated at fix time:
      94 requireAdmin/requireManagerOrAdmin sites (52 API files -> 403, all
      wrapped; 7 page sites -> redirect), ~60 getUserStoreScope sites
      (null-total: empty scope, can() denies role:null), 17 direct sites.
      CANNOT CAUSE A WRONG-ORG WRITE: both User writers upsert on
      clerkUserId; the UPDATE branch writes email alone.
      PRE-PROMOTION LOCKOUT CHECK RUN 2026-08-04, branch
      br-sparkling-block-a620qvg4: live org
      cf888f2d-f234-48c7-8097-fd5b44b5b3dd holds all 6 real User rows; every
      other org's rows are dev-instance fossils or deleted-vanity-org rows
      (see DEBT-57). No production user sits on a wrong-org row. SAFE TO
      PROMOTE.
      KNOWN LOCKOUT CLASS, named not assumed away: a User row left stale by
      an org move — membership.deleted leaves the row carrying the old org
      and no writer can correct it. Before this fix such a user was silently
      served the old org's role (this bug firing on a single-membership
      user); now they degrade to STAFF-level with no in-app repair. Own row:
      DEBT-56.
      NOT SWEPT: 21 page-level findUnique({ clerkUserId }) sites with the
      same missing guard — UI-only in effect (every gate behind them refuses
      independently) but the reason a mismatched user still sees an
      overpromising sidebar. Own row: DEBT-55, which carries the site list.

  - id: DEBT-54
    status: shipped
    commits: ["3784c34"]
    title: >
      accept-invite failed toward sign-up — the one flow that can mint a
      duplicate identity
    notes: >
      SHIPPED TO PRODUCTION 2026-08-04 in 999cbdc, the same promotion as
      DEBT-53; 3784c34 is an ancestor of origin/main. Status corrected from
      `staging` by the 2026-08-06 bookkeeping sweep.
      VERIFIED ON STAGING 2026-08-04 (Gary), same deployed SHA as DEBT-53.
      Three tests: (1) real invite link for an existing dev-instance account
      (gary@kevajuice.com) landed on /sign-in with __clerk_ticket intact;
      (2) regression test — the same ticket loaded at /accept-invite with NO
      __clerk_status parameter landed on /sign-in (before this commit that
      URL went to /sign-up); (3) Clerk's own reused-ticket error also
      directs to sign-in. Invitation revoked after.
      COST OF DOING NOTHING: LATENT, armed by any invite to an existing
      address — every __clerk_status except the single literal "sign_in"
      sent the invitee to the sign-up form, the one flow that can mint a
      second Clerk identity for an email that already has one. Triggers:
      rehires, re-invites, link rewriters that strip parameters, future
      Clerk status values.
      MECHANISM: route.ts:28 was status === "sign_in" ? "/sign-in" :
      "/sign-up" — an exact-equality allow-list of one value; absent,
      malformed and future statuses all fell to sign-up. This is DEBT-50
      hypothesis H2.
      FIX: default inverted — /sign-up only on the explicit "sign_up"
      status; everything else including absent goes to /sign-in. The
      failure direction now points at the flow that cannot create an
      identity. Ticket passthrough unchanged (set after dest,
      destination-agnostic; both targets read it off the query string).
      RELATED, NOT DONE: the invite guard (DEBT-50 audit Task 5) remains
      open as an option, deprioritised — see the DEBT-50 mechanism ruling:
      with F4 shipped and production restricted mode ON, no active
      duplicate-minting path remains on production.

  - id: DEBT-55
    title: >
      20 page-level findUnique({ clerkUserId }) lookups lack the org guard
      getCurrentUser() now has
    notes: >
      RULED 2026-08-06 — R1 (b): LEAVE LATENT. The central guard is fixed
      (DEBT-53/F1) and the 20 remaining sites are UI-only; nothing needs the
      sweep until multi-org work starts, at which point it is a hard
      prerequisite for DEBT-50's org-switcher half. The row stays OPEN — this
      is a decision not to schedule it, not a closure.
      THE STANDING NOTE LANDED IN CLAUDE.md § Page Conventions, not as a
      comment at a call site. Reasoning, since R1 offered both: the failure
      mode is a NEW page copying the pattern, and the author of a new page has
      no particular reason to open layout.tsx or any one of the 20 — but every
      session reads CLAUDE.md, and § Page Conventions is where someone starting
      a page is already standing. A comment at the most-copied site is
      complementary rather than redundant and remains worth adding the next time
      that file is open for another reason; it was not taken here because this
      session was docs-only apart from R4's registry line.
      ── ORIGINAL TEXT BELOW, UNCHANGED ──
      COST OF DOING NOTHING: LATENT — UI-only today (every gate behind
      these pages refuses independently via the DEBT-53 guard), but they
      are why a cross-org session still renders an overpromising sidebar,
      and any future page that trusts one of these lookups for data
      inherits the pre-F1 bug. HARD PREREQUISITE for DEBT-50's org-switcher
      half. The sweep is mechanical: findUnique -> findFirst with
      organizationId: org.id, or route through getCurrentUser(). Exemplars
      already in-repo: src/app/api/checklists/[id]/task-log/route.ts:36,
      src/app/api/users/[id]/route.ts:201.
      THE SITE LIST, RE-DERIVED AT APPLICATION TIME 2026-08-04 rather than
      carried over — the F1+F4 fix session's list was never written to a
      file, so it was re-grepped at head (user.findUnique against
      clerkUserId across src/app/**/*.tsx) and the count MATCHED the fix
      session's exactly: 20 pages + layout.tsx = 21. All paths relative to
      src/app/:
        (app)/layout.tsx:32
        (app)/hr/page.tsx
        (app)/items/page.tsx
        (app)/inventory/adjustments/page.tsx
        (app)/inventory/alerts/page.tsx
        (app)/inventory/counts/page.tsx
        (app)/inventory/counts/[id]/page.tsx
        (app)/inventory/expected/page.tsx
        (app)/inventory/ingredients/page.tsx
        (app)/inventory/ingredients/deleted/page.tsx
        (app)/inventory/ingredients/duplicates/page.tsx
        (app)/inventory/orders/new/page.tsx
        (app)/inventory/purchase-orders/page.tsx
        (app)/inventory/purchase-orders/new/page.tsx
        (app)/inventory/purchase-orders/[id]/page.tsx
        (app)/inventory/recipes/page.tsx
        (app)/inventory/recipes/[id]/page.tsx
        (app)/inventory/reports/page.tsx
        (app)/inventory/sales-items/page.tsx
        (app)/inventory/storage-areas/page.tsx
        (app)/inventory/vendors/page.tsx
      NOTE THE SHAPE OF THAT LIST: 18 of the 21 are inventory pages, so the
      sweep is one module plus three stragglers, not a scatter. layout.tsx
      is the one that matters most — it is what renders the sidebar every
      other page inherits. Its lookup spans two lines (findUnique on :32,
      the clerkUserId where-clause on :33), which is why a single-line grep
      for both terms returns 20 and not 21.
      (app)/layout.tsx IS DONE — fixed 2026-08-04 in PERM-5B's session
      (commit 2e75029), leaving 20. It came out of the list first because it
      is the one that renders the overpromising sidebar observed during F1
      verification, and because PERM-5B was already editing that file to
      thread the override prop. A wrong-org row is now treated as ABSENT,
      matching getCurrentUser(); org.id was added to the org select to
      compare against. The remaining 20 are all pages, 18 of them
      inventory — the sweep is unchanged in shape, one entry shorter.

  - id: DEBT-56
    title: >
      organizationMembership.deleted leaves User.organizationId pointing at
      the departed org — no writer can ever correct it
    notes: >
      RULED 2026-08-06 — R2 (c): LEAVE AS-IS, revisit when multi-org work
      starts. Two grounds, and the second is the load-bearing one. (1) The
      console.warn in getCurrentUser() makes any occurrence a single Vercel log
      grep, so the condition is observable rather than silent. (2) The choice
      between the two fix directions — move the row on membership.created, or
      delete it on membership.deleted — is a DESIGN INPUT to the multi-org
      phase, not a standalone job: deletion's FK enumeration only has a right
      answer once that phase has decided what a User row means across orgs.
      Fixing it early would commit that phase to one of its own options.
      The row stays OPEN. Deciding not to schedule is not closure.
      ── ORIGINAL TEXT BELOW, UNCHANGED ──
      COST OF DOING NOTHING: LATENT, consequence CHANGED by DEBT-53 — a
      user moved between orgs used to be silently served the OLD org's role
      in the new org (the F1 bug firing on a single-membership user); now
      they degrade to STAFF-level with no in-app repair, and the
      console.warn names them in Vercel logs. Needs a RULING, not a patch:
      either move the row on membership.created when the identity has
      exactly one membership, or delete the row on membership.deleted
      (cleaner, but drops defaultStoreId and every FK off User — schema
      consequences need enumeration first). Found by the F1 fix session
      2026-08-03.

  - id: DEBT-57
    title: >
      Production DB fossil rows: cross-instance + deleted-Clerk-identity
      orphans, measured 2026-08-04
    notes: >
      APPROVED 2026-08-06 — R3 (a). AWAITING ITS SESSION; the cleanup was NOT
      done when the ruling was recorded, and this row is not closed by the
      approval. It is the only item on the board with LIVE cost — production
      member counts and rosters disagree with Clerk today — which is what moved
      it ahead of R1 and R2, both of which were left latent the same day.
      SESSION SHAPE, per the ruling and the house rule for destructive work:
      read-only verification SQL first (every result carrying db + branch id),
      then hand-authored DELETEs presented for approval and run by GARY in the
      Neon console, then re-verify. Nothing about this runs from a dev machine —
      see CLAUDE.md § Environment Variables. Prefer RETURNING and an asserted
      row count, per § Database Evidence's destructive-statement corollary: the
      ids below were resolved on production and MUST be re-resolved on the
      branch being written to, never carried across.
      ── ORIGINAL TEXT BELOW, UNCHANGED ──
      COST OF DOING NOTHING: LIVE — org member counts and rosters disagree
      with Clerk today (live org: 6 DB rows vs 5 Clerk members). Measured
      on br-sparkling-block-a620qvg4 (queries recorded in the DEBT-50
      audit; every result carried db + branch id):
      (1) User cmr564cr1... / user_3Fq6iuvkhK4TSsmdaJ8yMMcT4rf (STAFF,
      kevajuice14@icloud.com) — Clerk identity deleted, row orphaned
      (DEBT-47 ghost) — sits INSIDE the live org and inflates its roster.
      (2) User cmqvpgn4f... / user_3FhMl9WeyNaGexqdaCEa5gEkU6P
      (gary@kevajuice.com, ADMIN) under org cmqvpe2bf... — the DEV
      instance's Keva Juice org, cross-instance contamination.
      (3) After the 2026-08-04 Clerk cleanup: blankettegirl@gmail.com's
      User row + "My Organization" org row (cms3o8i9u...), and
      gary@keva.com's User row + "Keva Smoothie Company" org row
      (cmr595zt9...), all now pointing at deleted Clerk objects.
      Also: Organization row cmr431pps... ("Microsoft") with 0 users —
      the dev org row itself, cross-instance.
      FIX SHAPE: read-only verification query first, then hand-authored
      DELETEs presented for approval per house rule (additive-only governs
      schema, not data cleanup — but every mutation is still
      approve-before-run). Check FKs off each User row before deleting.
      Do NOT fold into DEBT-47's handler work — this is the backlog, that
      is the faucet.

  - id: DEBT-58
    title: >
      Home-screen standalone mode shipped unverified on hardware — no iPad
      has launched the web clip, and the device accounts' sign-in method is
      unknown
    notes: >
      CORRECTION 2026-08-10 (CHK-4 bookkeeping rider, "c05365a") — ONE SENTENCE
      BELOW IS NOW FALSE AND THIS ROW DOES NOT CLOSE. THE ROW IS LEFT OPEN
      DELIBERATELY; only its branch claim is corrected.
      WHAT EXPIRED: "They are NOT on origin/main, so this is on the staging
      preview and not in production." Both "c473b72" and "28bbd8a" ARE on
      origin/main as of the 2026-08-10 promotion (merge "ddaa216"), confirmed by
      `git merge-base --is-ancestor` after a fetch. The standalone manifest is on
      production.
      WHY THAT MATTERS AND WHY IT CHANGES NOTHING ABOUT THE VERIFICATION OWED.
      The sentence it corrects already said the cost line was unaffected,
      "since the trigger is a store device, not a branch" — and that reasoning
      still holds exactly. What moved is the DISTANCE to the trigger: the web
      clip is now servable from www.usefroot.com rather than only from a preview
      URL, so the first iPad that installs a tile from the real domain hits
      untested behaviour. THE THREE UNTESTED CLAIMS BELOW ARE STILL UNTESTED and
      the sign-in-method question is still unanswered. This is the row getting
      MORE urgent, not less, and the DEBT-54 flip explicitly does not apply to it
      — a promotion moves code, it does not run an iPad checklist.
      COST OF DOING NOTHING: LATENT until pushed, LIVE the moment it reaches
      a store device (trigger: a counter iPad whose staff sign in with a
      SOCIAL provider. In a standalone clip that button navigates
      cross-origin and iOS may hand the callback back to Safari rather than
      the clip, stranding a half-finished sign-in on the device that is
      supposed to be the simplest thing in the store).
      FILED 2026-08-06 by the iOS home-screen icon session, which shipped it.
      "Both commits are UNPUSHED at filing" was struck 2026-08-06 by the
      bookkeeping sweep: c473b72 and 28bbd8a are both on origin/staging. They
      are NOT on origin/main, so this is on the staging preview and not in
      production — which leaves the cost line above exactly as written, since
      the trigger is a store device, not a branch.
      WHAT IS VERIFIED, and it is only the plumbing: the rendered <head>
      carries rel="apple-touch-icon" 180x180, apple-mobile-web-app-title
      "Froot", and rel="manifest"; /apple-icon.png, /icon.png and
      /manifest.webmanifest all return 200 unauthenticated on local dev.
      Every one of those is a server-side fact. NOTHING about how iOS
      actually behaves has been observed — no iPad was involved.
      THE THREE UNTESTED CLAIMS, in the order they will bite:
      (1) standalone clips get their own cookie jar, so staff sign in again
      inside the clip even when Safari is signed in — believed one-time,
      unconfirmed across relaunch and reboot;
      (2) the social-provider hop above — <SignIn> is embedded and
      same-origin, so email/password and email-code should stay inside the
      clip, but WHICH METHOD THE PERM-7 DEVICE ACCOUNTS USE WAS ASKED AND
      NEVER ANSWERED, and it is the whole of the risk;
      (3) no back button — any page with no in-app way back is a dead end,
      recoverable only by force-quit.
      THE REVERT IS ONE COMMIT, which is why it shipped this way: the icon
      fix (c473b72) changes no behaviour and the standalone decision is
      isolated in the manifest (28bbd8a). Dropping the second commit
      restores today's Safari-chrome launch and keeps the icon.
      DO IT: answer the sign-in-method question, then run the iPad checklist
      in the session report against staging. Delete the existing home-screen
      tile first — iOS caches the web-clip icon on the BOOKMARK, permanently,
      so a stale tile will show the old screenshot no matter what the server
      sends and reads exactly like a failed deploy.
      Related: PERM-7 (the device accounts this is for).

  - id: DEBT-59
    status: shipped
    commits: ["2ccca7d"]
    title: >
      CLOSED — Template availability offsets default to 1 and 2 and are written
      on every save — every existing template carries an invented window nobody
      chose
    notes: >
      DONE 2026-08-10 IN "c05365a" — THE COPY RETIRED, AND THE ROW STILL STAYS
      CLOSED. The 2026-08-09 pointer below said the sentence would be replaced by
      CHK-4 "and by no earlier one". CHK-4 replaced it.
      WHAT IS ON SCREEN NOW: "Optional. Sets when this checklist is expected — it
      never hides it. Leave blank for no expected window." — plan §6.2's wording,
      unchanged from the proposal.
      THE PRESERVATION RULE WAS HONOURED IN A PLACE THE PLAN DID NOT ANTICIPATE.
      Plan §6.2 named two comment regions to prepend to; THREE were prepended to,
      because a third had also gone false. DEBT-29's block above the availability
      Select still reads "no code path joins these values to StoreHours" — true
      when written, false since CHK-3's expectedWindow(). It is left standing with
      a note, and the note says which half survived: the join exists and
      deliberately does not gate visibility, so the sentence DEBT-29 forbade is
      still forbidden; only the REASON moved, from "there is no such code path" to
      "there is one and it is an expectation, not a filter".
      WHAT DID NOT CHANGE, exactly as this row required: no ` *` on either label,
      blank stays blank, the columns stay nullable, the CSV round-trip untouched,
      and availabilityType's helper ("A label for staff — checklists stay visible
      all day") is byte-identical. DEBT-29's visible-and-labelled decision was
      EXTENDED, not overturned — CHK-4 added an (i) Popover beside the fields
      rather than hiding them.
      THE NINE CHECKS ARE OWED AGAIN AND ARE NOT YET RUN.
      docs/prompts/DEBT-59_AUDIT.md §7, second re-run, because template-form.tsx
      was touched a second time. They are in CHK-4's staging walkthrough.
      ── THE 2026-08-09 POINTER IS BELOW, UNEDITED ──
      UPDATE 2026-08-09 — THE COPY THIS ROW SHIPPED RETIRES IN CHK-4, AND THE
      ROW STAYS CLOSED. This is a pointer, not a re-opening: DEBT-59 is
      `shipped` and stays `shipped`. Plan: docs/prompts/CHK-1_PLAN.md §6.2,
      committed in "746918c".
      WHAT RETIRES AND WHEN. The helper text this row wrote — "Optional.
      Recorded for reference — not yet used to show or hide checklists. Leave
      blank if no window has been decided." (template-form.tsx:1056) — becomes
      FALSE the moment CHK-4 ships, because that is the session where the
      offsets gain their ruled meaning as an EXPECTED WINDOW (Gary's R3). It is
      replaced there, by that session, and by no earlier one. A copy change is
      earned by the phase that makes it true — which is this row's own argument,
      applied to this row's own sentence.
      WHAT DOES NOT CHANGE. The ` *` stays off both labels; blank stays blank;
      the columns stay nullable; the CSV round-trip is untouched. And DEBT-29's
      visible-and-labelled decision still stands — CHK-4 ADDS an (i) explainer
      beside the fields rather than hiding them, which is the same reasoning one
      step further on.
      THIS ROW'S HANDOVER WAS HONOURED. It said the feature phase "has to decide
      what an offset means before it can decide what those rows are worth", and
      that every pre-gate value should be treated as unreliable regardless of
      what it says. CHK-3 does exactly that: the expected window is computed
      from whatever offsets a template carries, with NO backfill and no
      value-matched repair of the 1/2 rows this row froze the set of. An
      operator who never chose a window still has blanks, and blanks can never
      go overdue (R3).
      ── SHIPPING RECORD OF 2026-08-07 BELOW, UNEDITED ──
      SHIPPED TO PRODUCTION 2026-08-07 in merge "fad9207". The form stopped
      manufacturing a 1/2 window nobody chose, on production, before any
      availability gate exists to turn those unchosen values into real
      behaviour — which was this row's entire argument for doing it now rather
      than parking it inside a feature phase.
      NO SCHEMA CHANGE TRAVELLED WITH IT, as the audit established: both
      columns were already nullable with no @default since the init migration,
      so there is nothing on the production database to verify and no rollback
      consideration beyond the code itself.
      ── STAGING VERIFICATION BELOW, AS RECORDED BEFORE THE PROMOTION ──
      VERIFIED 2026-08-07 by Gary on staging: all nine manual checks passed,
      including clear-to-blank (not 0), unrelated-edit preservation, and
      export/import blank round-trip.
      CLOSED 2026-08-07 IN "2ccca7d" — THE WRITE IS STOPPED. The form no longer
      manufactures a value: state initialises from `initialData ?? null`, the
      inputs render blank and map "" back to null, both labels lost the ` *`
      they never earned, and the helper copy says blank is allowed without
      saying what blank MEANS — which would have promised the gate DEBT-29
      stripped this box of promising.
      THE ROW OVERSTATED THE WORK, AND THE AUDIT IS WHY THIS WAS ONE FILE.
      Full findings: docs/prompts/DEBT-59_AUDIT.md, written at head ac59d64.
      Three claims below did not survive it. (1) "a form default plus a nullable
      column" — BOTH COLUMNS WERE ALREADY NULLABLE with no `@default`, since
      prisma/migrations/20260627002005_init. No migration, no SQL, no schema
      limb at all. (2) the CSV parity edge listed as IN SCOPE — the export
      already writes "" for null (export/route.ts:41) and the import already
      reads "" back as null (import/route.ts:31-38, :139-140). Symmetric in both
      directions, always was, and neither file was touched. (3) "rows carrying
      exactly 1/2 were almost certainly never chosen" — see the ruling below.
      Every write path was mapped by reading, not grepping, because PATCH
      /api/templates/[id] writes these columns through an unnamed spread that a
      field-name grep does not return — the trap its own comment records at
      :51-55. All four persisted null faithfully already. The form was the only
      manufacturer.
      ONE DEFECT THE ROW DID NOT HAVE, AND IT WAS THE CORROSIVE HALF: `?? 1` /
      `?? 2` fired not only on create but every time a template ALREADY HOLDING
      NULL was opened for an unrelated edit. Renaming a template or adding a
      task converted a genuine blank into a written 1/2. The affected population
      was growing on its own, without anyone touching the window.
      A SECOND, SMALLER ONE: `Number("")` is 0, so clearing an offset wrote 0,
      not null. Before this commit there was no way to express blank through
      this form at all — which is why the row was a code change and not copy.
      EXISTING ROWS ARE DELIBERATELY UNTOUCHED — GARY'S RULING 2026-08-07, taken
      against this row's own lean toward a backfill. Two reasons. The seed
      script scripts/import-keva-templates.ts wrote a DELIBERATE 1/2 to Opener
      Checklist and Closer Checklist (its eight templates carry five distinct
      combinations including three explicit nulls), so a value-matched backfill
      is known-lossy before it runs, and renames and duplicates leak past any
      name-based carve-out. And the deliberate/invented distinction is weak in
      BOTH directions: NO PRE-GATE OFFSET WAS CHOSEN AGAINST A DEFINITION OF
      WHAT AN OFFSET MEANS — an operator typing 3 and 4 into a field labelled
      "not yet used" did not choose a window either. The feature phase should
      treat every pre-gate value as unreliable regardless of what it says.
      WHAT THIS ROW DID AND DID NOT RETIRE, stated rather than implied. The
      MECHANISM is retired in full. The HARM in the COST line below is retired
      FORWARD ONLY: rows already carrying 1/2 still convert the day a gate
      reads them. What the fix buys on those is that it FREEZES THE SET as of
      "2ccca7d" — previously it grew with every unrelated edit — so the count is
      now fixed and countable instead of moving. The blast-radius query is
      preserved in the audit §4 (anchored per CLAUDE.md § Database Evidence) and
      was NOT run: this session queried no database. HANDED TO DEBT-48's
      scoping, which has to decide what an offset means before it can decide
      what those rows are worth.
      ── ORIGINAL NOTES BELOW, UNEDITED ──
      COST OF DOING NOTHING: Nothing reads the two offsets today, so nothing
      misbehaves now — LATENT, but with an unusual shape worth naming: the
      trigger does not merely EXPOSE the defect, it CONVERTS it into data. The
      day any availability gate reads these fields, an unchosen 1/2 becomes a
      REAL window on every existing template row at once, retroactively and
      silently, and a chosen 1/2 is then indistinguishable from an inherited
      one. DERIVED AT HEAD 90d8293 by this session.
      FILED 2026-08-07 ON GARY'S EXPLICIT RULING, spun out of DEBT-48 when that
      row was parked as a feature phase. SIZE: S. Size is recorded in prose
      because `debt:` rows accept id/title/status/commits/notes ONLY — `size:`
      is a `phases:` field and adding it to a debt row type-errors the build,
      the same way `open:` does (see the header NOTE at the top of this file).
      THE WORK: make Template.startOffsetHours and Template.endOffsetHours
      OPTIONAL and BLANK by default, so a template that never chose a window
      does not carry one.
      THE STATE AT HEAD 90d8293, so nobody re-derives it:
      src/app/(app)/templates/template-form.tsx:637-638 defaults them to 1 and 2
      (`initialData?.startOffsetHours ?? 1`, `?? 2`) and they are written on
      every save. Nothing reads them back — no availability gate exists
      anywhere, verified by DEBT-48 at head. The comment immediately above them
      (:631-636) records why the inputs stay VISIBLE and labelled rather than
      hidden, and that reasoning is untouched by this row: hiding the inputs was
      rejected because it would not stop the write, only stop the admin seeing
      it. This row stops the write instead.
      DOING IT BEFORE ANY GATE EXISTS IS THE WHOLE VALUE, and it is the only
      reason this left DEBT-48 rather than waiting for that phase. Now, it is a
      form default plus a nullable column. After a gate ships, it is a
      data-correction problem on live templates with no way to tell an operator's
      deliberate 1/2 from a default nobody picked — the information needed to fix
      it correctly does not exist once the window is load-bearing.
      IN SCOPE, NOT A SURPRISE TO BE FOUND MID-SESSION: the CSV export/import
      parity edge from DEBT-29's reasoning. Emitting null changes what the export
      writes, and files already on disk were written under the old shape, so the
      import side has to accept both.
      THIS IS DELIBERATE WORK GARY ORDERED, not a triage-rule violation. It is
      filed as a row rather than fixed inline or left as a comment because it is
      a code change with a data consequence and a hard ordering constraint
      against another phase — which is what a row is for.
      Related: DEBT-48 (the parked feature phase this was cargo on), DEBT-29
      (the relabel that surfaced it as a third option), DEBT-32.

  - id: DEBT-60
    title: >
      Printed and historical checklists render LIVE task text — the as-executed
      hole one layer out from DEBT-36, which CHK-1's snapshot deliberately does
      not cover
    notes: >
      COST OF DOING NOTHING: The same defect DEBT-36 describes for section
      headings, on the task text itself — a printed closing checklist from last
      month re-prints today with a task added yesterday, and with any
      description edited since, and nothing indicates it changed — LATENT
      (trigger: the first task edit or task addition on a template that already
      has completed historical checklists. Unlike DEBT-36's trigger this one is
      ROUTINE — editing a task is ordinary template maintenance, not a rare
      administrative act — so it is materially more likely to fire than the
      rename ever was). DERIVED AT HEAD "746918c" by the CHK-1 planning session.
      FILED 2026-08-09 ON GARY'S RULING, out of CHK-1's scope decision. Found
      while auditing DEBT-36's compliance argument; recorded here rather than
      absorbed because absorbing it roughly doubles CHK-1's schema and its print
      rewrite, and the two ship independently.
      THE FACT, so nobody re-derives it: print/checklist/[id]/page.tsx:30 loads
      `template.tasks` fresh on every render and groups them at :43-51. Nothing
      about the task list is frozen. A task DELETED from a template is the one
      case that is safe, and only by accident — api/templates/[id]/route.ts:96-101
      refuses to delete a task carrying TaskLog history because of the RESTRICT
      FK. Additions and text edits have no such guard.
      WHAT CHK-1 DOES AND DOES NOT COVER. It ships Checklist.sectionsSnapshot,
      frozen once on the first TaskLog write, holding [{sectionId, name,
      sortOrder}] — SECTION NAMES ONLY (Gary, 2026-08-09). Task descriptions,
      task additions and task flags are explicitly out.
      FIX SHAPE when picked up: a second Json column on Checklist
      (`tasksSnapshot`), written by the SAME single writer at the same instant
      as sectionsSnapshot, so there is one freeze point and not two that can
      disagree. Print reads it when present and falls back to the live join when
      null. Additive; no migration risk beyond the column.
      Disconfirmed by: a tasksSnapshot column appearing on Checklist, or print
      resolving task text through anything other than the live template join.
      Related: DEBT-36 (the same mechanism, one layer in), CHK-1.

  - id: DEBT-61
    title: >
      Template.frequency is collected, exported and rendered but read by no
      generation path — every template is a daily template
    notes: >
      ── RIDER 2026-08-10 (CHK-5/S5, "5190fd9"). THIS ROW STAYS OPEN. PREPENDED;
      NOTHING BELOW IS EDITED. ──
      THE EXCLUSION NOW HOLDS AT THREE GATES AND IS VISIBLE AT THE THIRD.
      Materialisation (CHK-3, "d089a7c"), closing (CHK-3's defect fix,
      "1c907d2"), and now the operations report, which filters every row through
      `dayCloseAppliesTo(template.frequency)` before it counts anything. All
      three ask the same predicate; none of them re-derives it.
      WHAT REMAINS ON THIS ROW, RESTATED NOW THAT THE CONTAINMENT IS COMPLETE —
      because the containment is thorough enough to be mistaken for the fix, and
      the disconfirmation clause below has now fired three times without this
      row closing. Two things, and neither is a filter:
      (1) GENERATION-TIME FREQUENCY AWARENESS. Bulk generate still creates a
      Weekly template's checklist EVERY day. Nothing consumes those rows now —
      day close leaves them open and the report drops them — so what was noise
      is now litter: a Weekly template silently accrues one Pending row per
      store per day, forever, and every one of them is a row a human could open.
      The three gates stop the fiction being COUNTED; they do not stop it being
      CREATED.
      (2) HONEST WEEKLY LIFECYCLE SEMANTICS. A non-Daily checklist somebody
      genuinely started is now NEVER closed — it keeps `closedAt: null` and
      reads `overdue` for as long as it exists, because overdue is derived and
      missed is the only terminal state day close writes (CHK-3's rider states
      this and counts it as `frequencyLeftOpen`). That is the right trade today
      and it is not a resting place.
      THE REPORT SAYS SO IN PLAIN WORDS AND DOES NOT CITE THIS ROW ON SCREEN:
      "Only daily checklists are tracked … For those templates, 'no misses'
      means 'not tracked yet' — not 'all done'", carrying the count of rows the
      exclusion dropped from the range being viewed. An operator should not have
      to know what DEBT-61 is; a number they can see is what keeps the exclusion
      from reading as a clean bill of health.
      FIX SHAPE IS UNCHANGED and is still a schema addition plus a form control
      plus the generation filter — a Weekly template needs a day-of-week, a
      Monthly one a day-of-month, neither collected today.
      ── THE 2026-08-09 FILING IS NEXT, UNEDITED ──
      COST OF DOING NOTHING: Bulk generate creates a checklist for every active
      template every day regardless of frequency, and on-demand creation asks no
      question at all, so a Weekly template behaves exactly like a Daily one —
      LATENT TODAY, LOUD THE DAY CHK-3 SHIPS (trigger: day-close materialisation.
      An unfiltered job would file a Weekly checklist as MISSED six days a week
      and a Monthly one as missed ~29, turning the operations report into noise
      on its first day). DERIVED AT HEAD "746918c" by the CHK-1 planning session.
      FILED 2026-08-09 ON GARY'S RULING. The trigger is already handled — CHK-3
      materialises misses only for `frequency = "Daily"`, and CHK-5's report
      states the exclusion on its face so "no weekly misses" is never read as
      "weekly checklists were all done". THAT IS A CONTAINMENT, NOT A FIX, which
      is why this row exists rather than a comment.
      THE FACT: `frequency` is String @default("Daily") on Template. Every
      reference is collection or display — template-form.tsx, the CSV export and
      import, templates-client.tsx:184 (duplicate), templates/[id]/page.tsx:69
      and print/template/[id]/page.tsx:88 (both render it as a label), and
      api/templates/route.ts:117 (`|| "Daily"` on create). NEITHER creation path
      in api/checklists/route.ts reads it: the single create at :130 and the bulk
      loop at :155-170 filter on isActive and appliesTo only.
      NOTE THE SHAPE OF THE DEFECT — it is a promise on a form, not a bug in a
      path. The operator picks Weekly, the value persists, the value prints back
      at them on the template page, and nothing anywhere honours it. That is the
      TPL-1 pattern exactly (a field wired end to end with the behaviour
      omitted), and it is worth saying so: TPL-1's lesson was that the CONTROL
      is the fix and the default was the symptom. Here the control exists and
      the CONSUMER is missing.
      FIX SHAPE when picked up: frequency-aware generation — a Weekly template
      needs a day-of-week, a Monthly one a day-of-month, neither of which is
      collected today, so this is a schema addition plus a form control plus the
      generation filter, not a one-line predicate. Sequence it AFTER CHK-3, which
      is the first thing that will care.
      Disconfirmed by: any code path in api/checklists or the day-close cron
      branching on Template.frequency. Related: CHK-3, CHK-5, TPL-1.
      RIDER 2026-08-09, PREPENDED-STYLE (nothing above is edited) — THE
      CONTAINMENT IS NOW CODE, IN "d089a7c", AND THE DISCONFIRMATION CLAUSE HAS
      FIRED WITHOUT THIS ROW CLOSING. The day-close job branches on
      Template.frequency: `materializesMisses()` in
      src/lib/checklist-lifecycle.ts, called at the materialisation site in
      api/cron/checklist-day-close/route.ts, which carries a comment pointing
      here rather than re-deriving the reasoning. So the clause above is
      satisfied by a filter that PREVENTS THE NOISE and still reads nothing
      about WHEN a Weekly template is due — which is the whole row. The trigger
      it was written against is defused; the defect is untouched. Read the
      clause as disconfirming "no code anywhere looks at frequency", which was
      the honest test on the day it was written, not as this row's exit
      condition. The exit condition is unchanged: frequency-aware generation, a
      day-of-week and a day-of-month that nobody collects yet.
      Also note what the containment does NOT cover: a non-Daily checklist that
      somebody DID start still closes normally at day close, Completed or
      Missed. Only the create-a-row-for-one-nobody-started step is skipped.

  - id: DEBT-62
    title: >
      The archived preview/main Neon branch still answers queries with
      month-stale data — a fossil that returns clean, plausible, wrong results
    notes: >
      COST OF DOING NOTHING: Nothing in the product reads it and no deploy
      targets it, so no code misbehaves — the cost is entirely to EVIDENCE, and
      it has already been paid once — LATENT-BUT-FIRED-ONCE (trigger: any query
      run across "all branches". It fired on 2026-08-09, during the CHK-1
      planning session's own duplicate precheck).
      FILED 2026-08-09 ON GARY'S INSTRUCTION, from the CHK-1 planning session.
      Size: S — it is a console deletion, not code.
      THE MEASUREMENT, per CLAUDE.md § Database Evidence: preview/main
      `br-purple-rain-a6m62xww` — `_prisma_migrations` tail ends at
      `20260708100000_f1_goal_plans` (2026-07-08), TPL-1a ABSENT, a month stale.
      Taken by Gary 2026-08-09. The branch stopped receiving deploys when
      DATABASE_URL was scoped Production-only (BUILD-1) and has been ARCHIVED
      SINCE 2026-08-04. The three live branches are unchanged: dev
      `br-broad-wave-a6vpjdw0`, preview/staging `br-square-feather-a63z92vz`,
      production `br-sparkling-block-a620qvg4`.
      HOW IT FIRED, recorded because the failure is instructive and the fix is
      not. The CHK-1 precheck was run on four branches and all four returned
      duplicate_groups 0. The fourth result was read as evidence that the plan's
      inherited "three branches" phrasing (copied from TPL-1 and TPL-2) was
      stale, and two verification instructions were rewritten to say FOUR — plus
      a paragraph asserting, labelled as inference, that the branch receives
      `prisma migrate deploy`. All of that was wrong, and every observation
      behind it was real. Corrected in docs/prompts/CHK-1_PLAN.md §0a, which
      keeps the whole arc rather than the answer.
      THE LESSON GENERALISES PAST THIS BRANCH AND IS THE REASON THIS ROW IS
      WORTH FILING: a branch ANSWERING a query proves it EXISTS, not that it is
      LIVE. That is § Database Evidence's "A ROW ID DOES NOT IDENTIFY A BRANCH"
      one turn further on, and the guard that caught it was a STRUCTURAL fact
      about the branch — the `_prisma_migrations` tail — not a fact about the
      row, which is § Browser Evidence's "verify by a structural fact, not by
      its name" applied to a database branch.
      FIX SHAPE: delete the branch in the Neon console. GARY'S, NOT CLAUDE'S —
      it is a destructive action against a hosted environment and Claude does not
      touch deployed infrastructure. Sequence it at CHK phase close, so the
      branch is not deleted out from under a session that might still be citing
      it.
      Disconfirmed by: the branch no longer appearing in the Neon project.
      Related: BUILD-1 (the scoping that stranded it), DEBT-4.

  - id: DEBT-63
    status: shipped
    commits: ["5190fd9"]
    title: >
      CLOSED — /reports omits Non-Compliant from its own totals — three status
      buckets summed as if they were all of them
    notes: >
      ── CLOSED 2026-08-10 IN "5190fd9" (CHK-5/S5). PREPENDED; NOTHING BELOW IS
      EDITED. ──
      FIXED THE WAY THIS ROW ITSELF PREFERRED: the total is now the ROW COUNT
      (`checklists.length`), not a sum of named buckets, so the omission cannot
      be reintroduced by whoever adds a sixth status. The five buckets are also
      a PARTITION by construction — `isMissed()` is asked first and the stored
      status is consulted only for what it leaves — so the tiles sum to the
      total exactly and now agree with the per-store table, which counted
      `sc.length` all along and was already right (this row's own CHK-3 rider).
      Missed joins both the tiles and the table; the Missed number comes from
      the predicate in src/lib/checklist-lifecycle.ts and never from
      `status === "Missed"` (DEBT-26).
      ONE CORRECTION TO THIS ROW, AND IT IS A CORRECTION TO A CLAIM, NOT A
      SCOPE CHANGE. The row states that a Non-Compliant checklist "is counted in
      NO bucket on /reports" and that "Non-Compliant matches none of the three".
      THAT HALF IS NOT TRUE AND NEVER WAS: `const nonCompliant = ...` and its
      own KPI tile have been in reports/page.tsx since the original platform
      build "1cfdf76" — established by `git log -L` on that line at "dc90ff6",
      not by re-reading. What was live was the DENOMINATOR half: the `sub` line
      rendered `of ${completed + inProgress + pending} total checklists`, which
      dropped Non-Compliant from the total while the tile beside it displayed
      the count. So the page under-reported its total and overstated completion
      rate against it — the row's headline consequence — for a reason one clause
      narrower than the row gives.
      WHY THE MISREADING IS WORTH RECORDING RATHER THAN QUIETLY DROPPING: the
      row was DERIVED FROM READING, and it says so honestly ("from reading, not
      from a query"). The reading stopped at the three `filter` lines it cited
      (:31-33) and inferred the absence of a fourth from their symmetry. The
      fourth was on the next line. This is the DEBT-37 class the row itself
      names, one level up — an inference presented in the register of a
      measurement — and it cost nothing here only because the fix shape it
      proposed was right for the real defect anyway.
      THE SECOND, SMALLER FINDING BELOW STANDS AND IS ALREADY CLOSED ELSEWHERE:
      checklists/page.tsx:117's `?? STATUS_STYLES.Pending` fallthrough became
      reachable when Missed shipped, and CHK-4 mapped Missed in "c05365a" while
      deliberately keeping the fallback as the net for the next status.
      R1 RECONCILIATION, STATED ON THE PAGE'S FACE IN THE TILE SUBTITLES:
      Non-Compliant is a LIVE-DAY verdict written by submit; day close rewrites
      it to Missed along with everything else unfinished, because once the day
      is shut a partial submission is a miss like any other. So Non-Compliant
      trends toward today's rows and Missed accumulates behind it. The one
      exception is a non-Daily row, which day close leaves open by design, so
      its Non-Compliant verdict persists — DEBT-61.
      ── THE 2026-08-09 FILING IS NEXT, UNEDITED ──
      COST OF DOING NOTHING: A checklist submitted with critical tasks
      incomplete is counted in NO bucket on /reports and is silently absent from
      the "of N total checklists" denominator, so the page under-reports its own
      total and overstates completion rate against it — LIVE, not latent,
      whenever any Non-Compliant row exists (trigger already met in principle:
      the status has a writer and a render site; whether any row currently
      carries it was NOT measured — this session queried no database).
      DERIVED AT HEAD "746918c" by the CHK-1 planning session, from reading, not
      from a query.
      FILED 2026-08-09. INDEPENDENT OF THE CHK PHASE — found while mapping the
      checklist surfaces, and filed separately BECAUSE it is not CHK's to fix:
      it is wrong today, at HEAD, with no new feature involved.
      THE FACT: api/checklists/[id]/submit/route.ts:34-38 writes one of three
      statuses — Completed, Non-Compliant, In Progress. reports/page.tsx:31-33
      counts Completed, In Progress and Pending, and :95 renders the total as
      `completed + inProgress + pending`. Non-Compliant matches none of the
      three. /checklists DOES render it (checklists/page.tsx:14), so the two
      pages disagree about what exists.
      WHAT CHK MUST NOT DO, and this is why the row is filed rather than left as
      a note: CHK-4 adds `Missed` to those same counts. Adding one status to a
      sum that already drops another WIDENS the defect while looking like
      maintenance — the reviewer sees a status being added and reads the
      omission as intentional. CHK-4's regression plan says to add Missed; this
      row says the sum was already wrong before it got there.
      A SECOND, SMALLER THING IN THE SAME AREA, recorded so it is not
      rediscovered: checklists/page.tsx:117 falls back to `STATUS_STYLES.Pending`
      for any unmapped status, so an unmapped value renders as "Not Started" —
      the most wrong of the available answers, silently. Exactly DEBT-37's
      class. Not currently reachable there (all four written statuses are
      mapped) and it becomes reachable the moment Missed ships unmapped, which
      is why CHK-4 maps it.
      FIX SHAPE: count Non-Compliant explicitly on /reports, or derive the total
      from the row count rather than from a sum of named buckets — prefer the
      latter, since it makes the omission impossible to reintroduce. The defect
      is the hand-summed denominator, not the missing bucket.
      Disconfirmed by: /reports deriving its total from anything other than
      `completed + inProgress + pending`. Related: DEBT-37 (the same silent
      fallthrough class), CHK-4.
      RIDER 2026-08-09 (CHK-3, "d089a7c") — ONE FACT THIS ROW DOES NOT HAVE, AND
      IT MATTERS BECAUSE THE TWO HALVES OF THE PAGE MOVE IN OPPOSITE DIRECTIONS.
      The per-store table at reports/page.tsx:36-41 does NOT use the hand-summed
      denominator: `total` is `sc.length`, every row in scope whatever its
      status, and `rate` is completed/total. So the moment Missed rows exist —
      which is the moment CHK-3's cron first runs, BEFORE CHK-4 touches any
      counting — the four tiles under-report by MORE while the store table's
      rate drops by the same rows. Same page, one number too low, another too
      high, from the same cause. Materialised misses add rows that never existed
      before; stale Pending rows flipped to Missed leave the tiles but stay in
      the table's denominator. The fix shape above (derive the total from the
      row count) fixes the tiles and leaves the table already correct, which is
      further reason to prefer it.
      THE CHK-3 SESSION DID NOT TOUCH THIS — it must not touch any rendering —
      and it is stated here rather than only in that session's report so it is
      not rediscovered as a regression when the numbers move on staging.

  - id: DEBT-64
    title: >
      Labor silently ignores overnight and one-sided StoreHours and infers from
      sales instead — and day close will read the same rows the other way
    notes: >
      COST OF DOING NOTHING: A store whose real hours cross midnight has those
      hours entered, saved, and displayed on /stores — and labor plans its week
      as if they had never been set, from trailing sales, with nothing on any
      screen saying so. LIVE the moment such a store is configured; LATENT only
      because no store had hours at all before 2026-08-09. It gets worse rather
      than staying flat: CHK-3 makes day close read the SAME rows under the
      OPPOSITE rule, so one store ends up judged on two different windows by two
      subsystems that both look correct.
      DERIVED AT HEAD "597e86d" by the CHK-2 session, from reading
      labor-plan.ts:195-207 and from running the explicit-hours branch for the
      first time in its life against dev br-broad-wave-a6vpjdw0.
      FILED 2026-08-09. IT EXISTED ONLY IN S2's REPORT UNTIL THIS COMMIT —
      named as a ROW in that session's triage and never written to the board.
      DEBT-37's rule, firing on the session immediately after the one that
      recorded it firing on S1. Filed here rather than absorbed into CHK-3
      because the fix is in labor-plan.ts, which CHK-3 must not touch, and a
      rider on a phase row dies when that phase closes.
      THE FACT: labor-plan.ts:202 accepts an explicit window only when
      `e > s` — `open = s != null && e != null && e > s ? {...} : inferredOpen[wd]`.
      A 07:00-02:00 store gives s=7, e=2, so `e > s` is false and it falls
      through to sales inference. One-sided hours fail identically: CHK-2's
      editor deliberately allows a day with an opening time and no closing time
      (times are independently nullable, and inventing the missing half is what
      DEBT-59 forbids), and `e != null` then fails. MEASURED, not reasoned:
      with Sat set to 08:00 and no close, labor returned 13 open hours for that
      day — the inferred figure, unchanged from before hours existed — while
      Mon-Thu correctly took their explicit 12.
      THE FALLBACK IS NOT THE DEFECT; THE SILENCE IS. Falling back to inference
      for an unusable window is defensible on its own. What is not defensible is
      that an operator who typed real hours gets a plan built from something
      else with no indication anywhere — the same shape as DEBT-59's lesson, one
      subsystem over: a value nobody chose, presented as if chosen.
      WHAT CHK-3 MUST NOT DO, and this is the half that makes this urgent rather
      than merely wrong: plan §3.5 rules that for day close `closingTime <=
      openingTime` means the close instant is D+1 plus buffer — overnight is
      handled EXPLICITLY and correctly. So after CHK-3 ships, one store is
      judged on its real window by day close and on an inferred one by labor.
      S3 must not "resolve" that by copying labor's `e > s` check into
      expectedWindow(); the plan's rule is the right one and this row is the
      other side being wrong.
      FIX SHAPE: treat `e <= s` as a window crossing midnight (end + 24) rather
      than as invalid, matching the day-close rule so the two subsystems read
      one meaning; and decide explicitly what a one-sided day means — infer the
      missing half, or ignore the row — rather than having it fall out of a null
      check. Either way, surface WHICH stores are running on inference, since
      that is the part no screen says today. Prefer aligning to §3.5, so there
      is one definition of a store's operating window rather than two.
      Disconfirmed by: labor-plan.ts accepting a window where `e <= s`, or any
      surface naming the stores whose hours were set and then ignored.
      Related: CHK-2 (shipped the editor that makes this reachable), CHK-3
      (§3.5, the opposing rule), DEBT-59 (values nobody chose).
      RIDER 2026-08-09 — THE OPPOSING RULE IS NOW BUILT, IN "d089a7c", AND THE
      TWO ANSWERS ARE LIVE THE MOMENT MIGRATION B DEPLOYS. Nothing above needs
      correcting; this records only that the "gets worse rather than staying
      flat" clause has come due. src/lib/checklist-lifecycle.ts reads
      `closingTime <= openingTime` as a D+1 close and a one-sided row as a
      partial window, and it says so at the single definition site with a
      pointer to this row — so the disagreement is written down in the code
      rather than waiting to be discovered by whoever notices two numbers. S3
      did NOT copy labor's `e > s` check, which is what this row asked of it.
      The fix shape and the exit condition are unchanged and both are still in
      labor-plan.ts.
      One thing the day-close side supplies for free when this is picked up: the
      four-value dayCloseSource (hours / closed-day / no-hours / no-close-time)
      is exactly the "which stores are running on inference" signal this row
      says no screen carries. Labor needs its own equivalent, but the shape is
      already decided rather than open.

  - id: DEBT-65
    status: shipped
    commits: ["5190fd9"]
    title: >
      CLOSED — Bulk generate creates checklists for ARCHIVED templates that no
      store surface will ever show — and day close now files them as Missed
    notes: >
      ── CORRECTION RIDER 2026-08-10, AFTER A STAGING CENSUS. PREPENDED;
      NOTHING BELOW IT IS EDITED, INCLUDING THE CLOSURE RIDER THAT IS NOW
      PARTLY WRONG. ──
      THE FIRST FIX WAS AIMED AT THE CONTROL NOBODY USES, AND THE MEASUREMENT
      THAT FOUND THIS WAS GARY'S, NOT THE SESSION'S. Measured on staging
      `br-square-feather` 2026-08-10 and reproduced on dev
      `br-broad-wave-a6vpjdw0` the same day: ZERO templates have
      isArchived=true; FIVE are isActive=false; all 107 staging checklist rows
      sit under an isArchived=false/isActive=true template. The cleanup DELETE
      below therefore matched nothing — correctly, and THAT IS THE FINDING, not
      a failure of the query.
      WHAT IT PROVES ABOUT THE UI, WHICH IS THE PART THAT GENERALISES. Both
      flags have real controls and they are SEPARATE: Archive writes isArchived
      alone (templates-client.tsx:314), Deactivate writes isActive alone (:308).
      AT KEVA, "ARCHIVING" IS PERFORMED WITH DEACTIVATE — five to zero. So an
      isArchived-only gate is correct and INERT: it guards a state no operator
      has ever created. Gary's ruling was operator-shaped ("archiving stops
      generation"), and satisfying it required reading what the operator's
      archive ACTION writes. The first fix read the WORD instead.
      SECOND RULING, GARY 2026-08-10: gate the single-create path on BOTH flags.
      Shipped — api/checklists/route.ts scopes that lookup with
      `isActive: true, isArchived: false`, matching bulk generate (which has
      honoured isActive all along, which is exactly why the census shows zero
      rows under inactive templates) and the crew list. Three filters, one rule.
      TWO SENTENCES IN THE CLOSURE RIDER BELOW ARE OVERSTATED, CORRECTED HERE
      RATHER THAN EDITED OUT: it says the bulk loop "generated a checklist for
      [an archived template] at every store", present tense, as though such rows
      existed. NONE EVER DID on any measured branch. That gate is insurance
      against a reachable state, not a fix to a live condition — the live
      condition was the single-create path's missing isActive check, which the
      rider filed as an unruled tidy-up and which was in fact the operative half
      of the ruling.
      AND ONE FACT THE ORIGINAL FILING GOT WRONG: "Both writers set one flag
      only" is incomplete. api/templates/import/route.ts:166 sets BOTH —
      `{ isArchived: true, isActive: false }` — on the import replace path. So
      the flags are independent at two writers and coupled at a third, which is
      a sharper version of this row's own "two flags with one meaning"
      diagnosis. Related: DEBT-67, filed for the tab labels that put the
      disagreement in front of operators.
      ── CLOSED 2026-08-10 IN "5190fd9" (CHK-5/S5). PREPENDED; NOTHING BELOW IS
      EDITED. ──
      GARY'S RULING, 2026-08-10, VERBATIM AND RECORDED AS THE ROW'S ANSWER —
      it was the S5 entry gate and the session stopped on it until it arrived:
      "archiving stops generation entirely; archived templates generate nothing,
      materialize nothing, and appear in no report going forward; existing rows
      from archived templates are (a) deleted as fiction where never-started, or
      (b) kept as history where human-touched".
      WHAT SHIPPED: `isArchived: false` on BOTH generation paths in
      api/checklists/route.ts, so the three applicability filters — bulk
      generate, the crew list at api/stores/[id]/templates/route.ts:24, and
      CHK-3's day-close job — now read one rule. Materialisation and the report
      were already covered: the cron has filtered `isActive: true, isArchived:
      false` since "d089a7c", and CHK-5's /reports/operations reads only rows
      that exist, so stopping generation is the whole of "appears in no report
      going forward".
      THE SINGLE-CREATE PATH WAS WIDER THAN THIS ROW KNEW, AND THAT IS THE ONE
      CORRECTION TO THE ANALYSIS BELOW. The row names the bulk filter
      (`isActive: true`, no isArchived). The SINGLE create at
      api/checklists/route.ts filtered on ORGANIZATION ALONE — neither flag —
      so it would instantiate an archived template, and an inactive one, for any
      caller holding a template id. Not reachable from the UI, which lists
      through the crew-list filter; "the UI does not offer it" is not a gate.
      Both paths now carry `isArchived: false`.
      WHAT WAS DELIBERATELY NOT DONE: the single-create path still does not
      check `isActive`. The ruling was about archiving, and fixing an unruled
      second flag inside it would be exactly the "two flags with one meaning"
      confusion this row identified, resolved by guesswork. Filed as its own
      triage item in the CHK-5 report.
      EXISTING ROWS ARE GARY'S TO EXECUTE, NOT THIS COMMIT'S. The (a)/(b) split
      is presented as surgical org-scoped SQL in the CHK-5 session report — a
      SELECT that counts first, then a DELETE with RETURNING, per CLAUDE.md
      § Database Evidence's rule that a statement composed against one branch
      executes cleanly against its sibling. NOTHING WAS RUN FROM THE SESSION.
      Measured dev `br-broad-wave-a6vpjdw0` 2026-08-10: 0 templates archived and
      0 checklist rows from archived templates, so dev has no cleanup to do; the
      staging and production counts are owed from the Neon console.
      ── THE 2026-08-09 FILING IS NEXT, UNEDITED ──
      COST OF DOING NOTHING: An archived template still gets a checklist for
      every store every time an admin presses Generate, and /store-view never
      lists it, so nobody can complete it. LATENT SINCE FOREVER as an invisible
      Pending row; LOUD THE DAY MIGRATION B DEPLOYS, because CHK-3's day-close
      job closes every existing row for a past day — including these — as
      MISSED. An operator who archived a template last month then sees it in the
      operations report as a store's miss, for work nobody was ever shown.
      DERIVED AT HEAD "09a0898" by the CHK-3 session, from reading the three
      applicability filters side by side. Not measured against any database.
      THE FACT: api/checklists/route.ts:150 selects bulk-generate templates with
      `{ organizationId, isActive: true }` and NO isArchived filter.
      api/stores/[id]/templates/route.ts:24 — the list staff actually see —
      selects `{ isActive: true, isArchived: false }`, and CHK-3's
      api/cron/checklist-day-close/route.ts copies that stricter filter
      deliberately so it can never MATERIALISE a miss for a template a store was
      never shown. The two filters have disagreed all along; the cron makes the
      disagreement produce a row with a status on it.
      AND THE TWO FLAGS ARE INDEPENDENT, which is what makes this reachable
      rather than theoretical: archiving does NOT clear isActive. Both writers
      set one flag only — api/templates/[id]/route.ts:31 (single) and the bulk
      patch at api/templates/route.ts:47-52, driven by
      templates-client.tsx:314's Archive button. So `isArchived && isActive` is
      the NORMAL state of an archived template, not a corrupt one.
      WHY CHK-3 DID NOT FIX IT, stated so the omission is not read as an
      oversight: the one-line filter would change WHICH templates generation
      covers, and that is a product decision on an admin button with no ruling
      behind it. S3's business was the engine. It is filed rather than left in
      that session's report because a triage that NAMES a row and does not FILE
      it is how CHK-3's own row came to carry two items from S2 (DEBT-37).
      FIX SHAPE: add `isArchived: false` to the bulk-generate filter so the
      three applicability tests read one rule. Consider whether archiving should
      also clear isActive, or whether isActive should stop being consulted
      separately at all — two flags with one meaning is the shape of the
      problem. Decide, too, what happens to checklists already generated for an
      archived template: leaving them is defensible, and doing nothing is only
      defensible once it is chosen.
      Disconfirmed by: bulk generate filtering on isArchived, or any surface
      rendering an archived template's checklist. Related: CHK-3 (the writer
      that makes it visible), CHK-5 (the report it shows up on), DEBT-61 (the
      other generation filter that is not there).

  - id: DEBT-68
    title: >
      Hand-authored FK constraints may disagree with schema.prisma's implied
      onDelete — one instance found and fixed (Template.typeId), the sweep
      across every other hand-authored FK is unowned
    notes: >
      COST OF DOING NOTHING: every `prisma migrate diff` run against a table
      whose FK drifted emits a DROP CONSTRAINT + re-ADD pair that RELAXES the
      live constraint, and the next session that pastes a generated diff into
      a migration without reading it ships a semantic change nobody ruled —
      in the found instance, TPL-1a's delete-blocked-while-in-use (the manage
      dialog's 409 depends on ON DELETE RESTRICT) would have silently become
      SET NULL, i.e. deleting a TemplateType would quietly uncategorise its
      templates instead of refusing.
      THE CLASS: migrations here are hand-authored (migrate dev is broken —
      docs/MIGRATIONS.md §3), and hand-written SQL states onDelete
      explicitly, while schema.prisma relations often omit the annotation and
      fall back to Prisma's DEFAULTS — SetNull for optional relations,
      Restrict for required ones. Wherever an author wrote something other
      than the default and did not annotate the schema, the schema
      misdescribes every live database, and migrate diff's job is precisely
      to "correct" that. This is MIGRATIONS.md's "the schema misinforms a
      reader" hazard with a generator attached.
      THE INSTANCE, FOUND 2026-08-10 by HR-20's own diff at HEAD "683d33a":
      schema.prisma's Template.templateType relation had no onDelete →
      implied SetNull; TPL-1a's migration
      (20260808103000_tpl1a_template_type_entity) created
      Template_typeId_fkey ON DELETE RESTRICT, applied on all three branches.
      HR-20's first generated diff opened with the DROP/re-ADD pair. FIXED
      schema-side in "0a745c3": onDelete: Restrict annotated with a comment
      naming the drift; no database was touched — the annotation makes the
      schema describe what every branch already is.
      FIX SHAPE (the sweep, deliberately NOT run by HR-20 — Gary, 2026-08-10,
      "don't run the sweep now, just file it"): for each hand-authored
      migration's ADD CONSTRAINT ... FOREIGN KEY, compare its ON DELETE / ON
      UPDATE against what schema.prisma implies for that relation; annotate
      the schema wherever they differ. A clean full-schema
      `migrate diff --from-config-datasource --to-schema` on dev emitting NO
      constraint churn is the done criterion — and note migrate diff is blind
      to partial indexes (MIGRATIONS.md), so this sweep proves FKs only, not
      indexes.
      Disconfirmed by: a full-schema diff on dev that emits no DROP
      CONSTRAINT for untouched tables. Related: DEBT-35 (the ledger's own
      drift class, closed into MIGRATIONS.md), the MIGRATIONS.md
      "hand-authored FK onDelete" section HR-20's docs commit adds.

  - id: DEBT-67
    title: >
      The templates "Active" tab counts !isArchived, not isActive — five
      deactivated templates sit under Active wearing an Inactive pill, while
      Archived reads 0
    notes: >
      COST OF DOING NOTHING: An operator who retires a template cannot tell from
      the page whether they retired it. LIVE, MEASURED, NOT LATENT — staging
      `br-square-feather` 2026-08-10 (Gary): five templates isActive=false, zero
      isArchived=true. All five render in the ACTIVE tab, each showing an
      "Inactive" pill, while the ARCHIVED tab reads "Archived (0)". Reproduced
      on dev `br-broad-wave-a6vpjdw0` the same day (8 templates, all four cells
      of the 2x2 measured).
      THE FACT: templates-client.tsx:244 renders `Active
      ({templates.filter(t => !t.isArchived).length})` and :250 renders
      `Archived ({...t.isArchived...})`; the view filter at :108 and :121
      switches on isArchived alone. So the tab named for one flag is counted by
      the other. The per-card pill at :374-375 reads isActive, which is why the
      two disagree ON THE SAME ROW, in the same viewport.
      THE FOUR WRITERS, since the row above and DEBT-65 each had this partly
      wrong: Archive → isArchived only (:314); Unarchive → isArchived (:320);
      Deactivate → isActive only (:308); Activate → isActive (:311); and
      api/templates/import/route.ts:166 sets BOTH on the replace path. Two
      independent flags, four controls, three tab/pill readers, no agreement.
      WHY THIS IS NOT DEBT-65 AGAIN. DEBT-65 was about GENERATION reading the
      wrong flag, and it is closed — all three applicability filters now read
      `isActive: true, isArchived: false`. This row is about the page LYING to
      the operator about which state they put a template in. The engine is now
      right and the label is still wrong, which is the more durable half: the
      generation bug produced no rows on any branch, and this one is on screen
      at Keva today.
      FIX SHAPE: decide whether the tabs mean "not archived" or "active" and
      make the label say it — either rename to "Current / Archived" (cheapest,
      no behaviour change), or split the list three ways so Active, Inactive and
      Archived are each their own count. Prefer settling whether the two flags
      should remain independent AT THE WRITER first; two flags with one meaning
      is DEBT-65's diagnosis and it survives that row's closure.
      DELIBERATELY NOT FIXED IN CHK-5 (Gary's ruling 2026-08-10, "file a row,
      don't fix now"): S5's prompt forbids touching template-form.tsx and this
      is its sibling, template-form.tsx's file has been touched twice this phase
      already, and a label change here would owe the DEBT-59 nine checks a third
      time. Sequence it with a session that is opening that file anyway.
      Disconfirmed by: the Active tab counting isActive, or the tabs being
      renamed to match what they filter. Related: DEBT-65 (the same two flags,
      at the generation end), DEBT-59 (the nine checks this file owes).

  - id: DEBT-66
    title: >
      `main` is red at "7ab7106" — dashboard redeploys fail in `vercel-build`
      with NO database env attached, and the next production promotion is
      blocked until that is understood
    notes: >
      COST OF DOING NOTHING: Nothing is down. Production SERVES normally from an
      earlier Ready deployment, so no merchant or operator is affected today —
      LATENT (trigger: THE NEXT PROMOTION TO PRODUCTION. CHK-3 carries this as
      half of its promotion blocker, and every CHK row behind it inherits the
      same gate. The trigger is not hypothetical — it is the next thing this
      track does).
      THE FACTS, MEASURED 2026-08-09 rather than relayed. Three dashboard-
      initiated redeploys of branch `main` at commit "7ab7106" failed the same
      way (13:34:50, 13:44:51, 15:13:14 PDT — timeline in
      docs/prompts/CRON-DIAG_findings.md §5), all `errorCode
      BUILD_UTILS_SPAWN_1`, `Command "npm run vercel-build" exited with 1`, all
      in `buildStep`. The build log of the third
      (froot-4608w2c0v-indianathomas-2483s-projects.vercel.app, 22:13:15Z) reads,
      consecutively — `[prisma.config] DATABASE_URL_UNPOOLED is not set —
      falling back to DATABASE_URL (pooled)`, then `Error: The datasource.url
      property is required in your Prisma config file when using prisma migrate
      deploy.`, then exit 1.
      WHAT THAT PAIR OF LINES PROVES, AND IT IS MORE THAN EITHER ALONE.
      prisma.config.ts:24 sets `url: DATABASE_URL_UNPOOLED ?? DATABASE_URL`, so
      `datasource.url` can only be undefined when BOTH are absent. The warn
      names the first; the error proves the second. THE BUILD HAD NO DATABASE
      ENVIRONMENT AT ALL — this is not a wrong URL, not a pooler problem, and
      not a migration that failed: `prisma migrate deploy` never opened a
      connection, so nothing ran against any database and the failure is before
      the first statement. In particular it is NOT the intermittent Neon
      advisory-lock P1002 (BUG-3) that a `vercel-build` failure would otherwise
      suggest, and the CRON-DIAG note recommending that be ruled out first is
      answered here.
      AND THE VARS THEMSELVES ARE FINE. Production runtime reads `DATABASE_URL`
      through the Neon adapter and production is serving, so the Production row
      exists and holds a working value. What differs is ATTACHMENT to this
      deployment, not the variable — which is the BUILD-1 scoping class: a
      dashboard redeploy of `main` appears not to receive env the way a
      git-push deploy does, and the original git-pushed production deploy of
      this same commit built clean.
      FIRST MOVES WHEN PICKED UP, cheapest first: compare the env set of a
      failing dashboard redeploy against the last successful git-push production
      deployment; check what the redeploy was created FROM and whether the
      "existing build cache" path carries an empty env snapshot; confirm with
      `vercel env ls` (METADATA ONLY — `vercel env pull` is banned in this repo,
      CLAUDE.md § Environment Variables) that `DATABASE_URL` is present in the
      Production scope and not branch-scoped to something `main` does not match.
      Then re-promote by GIT PUSH, which is the path known to work, rather than
      by dashboard redeploy.
      Disconfirmed by: a dashboard redeploy of `main` reaching `X migrations
      found` in its build log, or any `main` build whose `Datasource "db"` host
      line appears at all. Related: BUILD-1 (the split that put `migrate deploy`
      inside the Vercel build), BUG-3 (prisma.config.ts, and the failure class
      this one is NOT), DEBT-4 (env scoping), CHK-3 (the promotion it blocks).
      ── RULING 2026-08-10, PRESERVE-AND-MARK: NOTHING ABOVE IS EDITED ──
      GARY RULED THIS DOES NOT BLOCK A GIT-PUSH PROMOTION, AND THE ROW STAYS
      OPEN, RESCOPED TO THE DASHBOARD-REDEPLOY PATH. The reasoning is already in
      the evidence above and only needed reading as a scope rather than as a
      severity: every measured failure was a DASHBOARD redeploy, and the
      git-pushed production deploy of the same commit "7ab7106" built clean. So
      the defect is in how a redeploy is CONSTRUCTED, not in `main`, not in the
      code, and not in the env rows — which the last sentence of "AND THE VARS
      THEMSELVES ARE FINE" already said. One line of the title is now wrong in
      consequence and is deliberately left standing: "the next production
      promotion is blocked until that is understood" was the honest call on
      2026-08-09 and is superseded, not mistaken.
      WHAT THE RULING ADDS TO THE PRODUCT, and it is the durable half:
      PROMOTIONS HAPPEN BY GIT PUSH ONLY. That was already this row's own
      recommended first move ("re-promote by GIT PUSH, which is the path known
      to work"); it is now the rule. A dashboard redeploy of `main` is the thing
      not to reach for, and if anyone does reach for it, this row is why it
      fails.
      WHAT THE RULING DOES NOT DO: it does not diagnose the redeploy path, so
      the exit condition and the disconfirmation clause above are untouched.
      Anyone picking this up still starts at the env-set comparison. The cost of
      leaving it is now bounded to "a dashboard redeploy is not available as a
      recovery tool" — which matters most in exactly the situation where someone
      would reach for it, so the bound is not as comfortable as it sounds.

  - id: DEBT-76
    title: >
      `scripts/verify-hr8-compliance.ts` has been RED for days and nobody
      noticed — 9 of 38 checks fail on a clean tree
    notes: >
      FOUND 2026-08-20 during OVL-S2, by running every verify script as that
      session's definition of done. NOT CAUSED BY THAT SESSION — proved by
      stashing the whole working tree and re-running: the same 9 checks fail at
      807abf6 with no OVL-S2 code present. Filed rather than fixed because it is
      HR compliance semantics in a labor session, and diagnosing it properly is
      its own sitting.
      WHY IT MATTERS MORE THAN NINE CHECKS. A verify script that is permanently
      red stops being a signal. The next person who runs the suite sees a
      failure, remembers "oh, hr8 is always like that", and the habit
      generalises to the script that is failing for a real reason. This repo's
      whole verification posture rests on the suite being green by default.
      THE FAILURES CLUSTER INTO TWO SHAPES, and each has an obvious suspect —
      both of them DELIBERATE behaviour changes that shipped without the fixture
      being updated alongside:
      (1) "s2: docA complete via all-required-acked (pending-record)" and the
      percentages that cascade from it (s2 pct 33 -> 0, summaries s2, store A
      67% -> 50%, manager scope). Suspect: HR-11n checkpoint retirement
      (migration 20260815150000_hr11n_checkpoint_retirement). The fixture
      asserts a document completes when every REQUIRED CHECKPOINT is acked even
      though the record itself is pending — exactly the rule a checkpoint
      retirement would change.
      (2) "s3: docB needs-resign (record pinned to v1)", s3 pct, and the rollup
      needs-resign count. Suspect: 20260816180000_hr_document_version_requires_
      reacknowledgment. Note that the ADJACENT check still passes — "needs-resign
      carries prior signed version v1" — so the version pinning works and it is
      the STATE the fixture expects that has moved.
      SO THE LIKELY VERDICT IS A STALE FIXTURE, NOT A PRODUCTION DEFECT, and the
      first job of whoever picks this up is to CONFIRM that rather than assume
      it. If it is the code, HR compliance percentages are wrong on a live
      dashboard and this row is the wrong severity. If it is the fixture, the
      fix is to re-derive the expected values from the post-HR-11n rules and say
      in the script's header which ruling each expectation encodes — the reason
      it drifted is that the expectations are bare numbers with no citation.
      Disconfirmed by: the suite going green with no change to src/, which would
      mean an environment-dependent fixture rather than a stale one.


# PENDING RULINGS — open decisions awaiting Gary's call. Added 2026-08-05.
#
# A DECISION LOG, NOT A SECOND DEBT TABLE. An entry belongs here only if the
# next step is a DECISION rather than work. Anything with an obvious answer and
# only effort in the way is a debt row or a chore note on an existing row — R8
# on the source sheet was exactly that (a leftover staging invite) and was moved
# to DEBT-44 by Gary's ruling rather than filed here.
#
# Accepted fields: id, title, status, asked, question, options, recommendation,
# ruling, ruled, links. NOTHING ELSE — src/lib/roadmap.ts types this list and an
# unknown field type-errors the build, the same way adding `open:` to a debt row
# does.
#
# status: open | ruled | deferred
#   open     — awaiting a call. Counted in the page's headline number.
#   ruled    — decided. `ruling` and `ruled` (the date) are then present.
#   deferred — consciously not deciding yet. NOT counted as open, because a
#              deferral IS a decision; it just isn't the final one.
#
# `ruling:` AND `ruled:` ARE ABSENT UNTIL A CALL IS MADE — never present-and-
# empty. Absence already carries "not yet decided", and an empty string carrying
# the same value is the mistake src/lib/roadmap.ts documents at length for
# `resolved: false` on blocker entries: a field that duplicates what absence says
# invites filling it in wholesale, which destroys the default that makes the
# scheme safe.
#
# NO `commits:` FIELD, deliberately. A ruling is a decision, not work — the SHA
# belongs on whatever row the ruling creates, and a ruling that produced no
# commit is the normal case, not a gap.
#
# THE ID GAP AT R7 IS REAL AND INTENTIONAL. The source sheet
# (docs/prompts/Froot_pending_rulings_sheet.md) carried an R7 asking whether to
# keep the untracked promotion runbook in the repo. That closed in 1218be3
# BEFORE this list was built, so it was never imported. The ids here match the
# sheet one-for-one and were not renumbered to close the hole: renumbering would
# break the correspondence with a document in docs/prompts/, which is a claim
# and is never edited (CLAUDE.md § Where documents live).
#
# NOT MIGRATED, and it is R6's subject: SIX DEBT ROWS ALREADY OPEN WITH
# "AWAITING RULING —" IN THEIR TITLES (DEBT-36, 38, 41, 42, 45, 48). That is a
# pending-rulings convention of its own, predating this one, living inside the
# debt table. Whether those six move here is R6's question and is not answered
# by building this list. Until it is ruled, one list linking to the other is the
# honest state — R6 carries the links.
#
# RULED 2026-08-07 (R6) — THE PARAGRAPH ABOVE NOW DESCRIBES A STATE THAT NO
# LONGER EXISTS, and is kept for its reasoning rather than its facts. The six
# were ruled directly from their rows and NONE of them moved here. No debt row
# carries "AWAITING RULING" any more, so the competing convention is retired by
# exhaustion rather than by migration, and there is no second list to link to.
# New DECISIONS go here; the six stay debt rows because what remains on each is
# work or a latent record.
rulings:
  - id: R1
    status: ruled
    asked: 2026-08-05
    ruled: 2026-08-06
    title: >
      Schedule the DEBT-55 org-blind lookup sweep, or leave it latent
    question: >
      Twenty pages — eighteen inventory, plus /hr and /items — still look up the
      user by Clerk id without checking the org, the same gap F1 closed
      centrally. UI-only today: every gate behind them refuses independently.
      It is why a cross-org session still sees an overpromising sidebar, and it
      is a HARD PREREQUISITE for the org-switcher half of DEBT-50.
    options:
      - >
        (a) Schedule the sweep soon — it is mechanical, one session, mostly one
        module.
      - >
        (b) Leave open until something needs it: the switcher, or a new page
        copying the pattern.
      - >
        (c) Fold it into the next inventory-touching session as a rider.
    recommendation: >
      (b), with one addition — a comment in CLAUDE.md or at the pattern's
      most-copied site saying new pages must use the org-guarded lookup, so the
      surface stops growing. The defect is latent, the centre is fixed, and no
      phase currently needs it. A ruling of (a) produces a sweep session prompt;
      (b) leaves the row open and turns into a comment-placement decision; (c)
      is a note on the row.
    ruling: >
      (b) LEAVE LATENT. The central guard is fixed and the remaining 20 sites
      are UI-only; nothing needs them until multi-org work starts. The standing
      note that new pages must use the org-guarded lookup is added to CLAUDE.md
      § Page Conventions — chosen over a call-site comment because the failure
      mode is a NEW page copying the pattern, and its author reads CLAUDE.md
      rather than any particular one of the 20. DEBT-55 stays open.
    links: [DEBT-55, DEBT-50]

  - id: R2
    status: ruled
    asked: 2026-08-05
    ruled: 2026-08-06
    title: >
      Direction for the DEBT-56 org-move stale row — webhook repair, deletion,
      or leave it
    question: >
      When a member is removed from an org, their DB row keeps pointing at the
      old org forever and no writer can correct it. Post-F1 such a person
      degrades to STAFF with no in-app repair; before F1 they silently kept
      their old role, which was worse. This needs a direction, not a patch.
    options:
      - >
        (a) The webhook moves the row on membership.created when the identity
        has exactly one membership.
      - >
        (b) The webhook deletes the row on membership.deleted — cleaner, but it
        drops defaultStoreId and every FK hanging off User, so it needs an
        enumeration first.
      - >
        (c) Leave as-is: the console.warn makes any occurrence one log grep, and
        org moves are rare in a one-org world.
    recommendation: >
      (c) for now, revisited when multi-org work starts — at which point (a)
      versus (b) becomes a design input to that phase rather than a standalone
      fix. Write that into the row. Ruling (a) or (b) instead means an
      audit-first session, since the FK enumeration has never been done.
    ruling: >
      (c) LEAVE AS-IS, revisit when multi-org work starts. The console.warn
      makes any occurrence one log grep, and the choice between moving the row
      and deleting it is a design input to that future phase rather than a
      standalone job — deciding it early would commit the phase to one of its
      own options. Noted on DEBT-56, which stays open.
    links: [DEBT-56]

  - id: R3
    status: ruled
    asked: 2026-08-05
    ruled: 2026-08-06
    title: >
      Approve the DEBT-57 production fossil cleanup — the only item on the
      source sheet with a live cost
    question: >
      Production holds DB rows pointing at deleted Clerk objects: the
      kevajuice14 ghost STAFF row INSIDE the live org, which is why the rosters
      disagree with Clerk today (six rows against five members); the gary@
      dev-org row; the four rows orphaned by the 2026-08-04 cleanup
      (blankettegirl, "My Organization", "Keva Smoothie Company", and
      gary@keva.com's row beneath it); and the "Microsoft" dev org row.
    options:
      - >
        (a) Approve the cleanup session — read-only verification SQL, then
        hand-authored DELETEs presented for approval and run by Gary in the Neon
        console.
      - >
        (b) Leave documented.
    recommendation: >
      (a), this week. It is the one item here with a cost paid by a user of the
      product rather than by the process — wrong member counts on production —
      it is small, and the evidence is already gathered. A ruling of (a)
      produces a session prompt: read-only verify, approved DELETEs, re-verify,
      branch ids throughout.
    ruling: >
      APPROVED (a). It is the only board item with LIVE cost — production member
      counts disagree with Clerk today — which is why it was approved on the same
      day R1 and R2 were left latent. The cleanup was NOT performed when this
      ruling was recorded: DEBT-57 stays open and awaits its own session
      (read-only verify, hand-authored DELETEs approved and run by Gary in the
      Neon console, re-verify, branch ids throughout).
    links: [DEBT-57, DEBT-47]

  - id: R4
    status: ruled
    asked: 2026-08-05
    ruled: 2026-08-06
    title: >
      square.manage — the registry grants it to ALL roles while every Square
      route enforces ADMIN
    question: >
      The registry and the enforcement disagree, so one of them is wrong.
      PERM-5's Session C reported it and deliberately left the Square routes
      unmigrated rather than resolve it unilaterally.
    options:
      - >
        (a) The registry is wrong — change square.manage to ADMIN_ONLY. It
        matches reality, and Square connect/disconnect/sync are org-level
        destructive actions; SEC-1 already treats disconnect as ADMIN.
      - >
        (b) The enforcement is wrong — widen access. There is no case for this.
    recommendation: >
      (a), emphatically. The Square routes then become migratable in a future
      sweep with zero baseline change. The registry edit rides the next docs or
      code session; nothing about it needs a session of its own.
    ruling: >
      (a) THE REGISTRY IS WRONG. square.manage changed ALL -> ADMIN_ONLY in
      src/lib/permissions.ts to match the enforcement every Square route already
      applies, and which SEC-1 and the L-2 seam both assume. APPLIED 2026-08-06,
      one line plus its comment. NO BEHAVIOUR CHANGED — nothing calls
      can(_, "square.manage"), verified at head, and it is not in
      ENFORCED_CAPABILITIES so it was never deniable from the /users grid. The
      Square routes remain unmigrated with their inline requireAdmin(); what
      changed is that migrating them is now a zero-baseline-change sweep rather
      than a decision. Recorded on PERM-5's open list and on L-2.
    links: [PERM-5, SEC-1, L-2]

  - id: R5
    status: ruled
    asked: 2026-08-05
    ruled: 2026-08-06
    title: >
      CLAUDE.md § Browser Evidence cites an org id that belongs to production,
      in a passage describing a dev org
    question: >
      The incident report motivating the "name your org id" rule describes
      org_3FhYUR… as a DEV org with nine stores and six staff. That id is
      PRODUCTION's. Two readings fit: the incident was on dev and the report
      pasted the wrong id, which is a label fix; or the session was on
      PRODUCTION while believing it was dev, which is a historical finding with
      a trace behind it.
    options:
      - >
        (a) Ten-minute read-only look at the DEBT-9 Phase 3 records first — what
        did that session DO on that org, observe or write, and do the counts
        (nine stores, six staff) match production-at-that-date or dev? Then fix
        the passage accordingly.
      - >
        (b) Correct the id and move on.
    recommendation: >
      (a). If the counts match dev it is the first reading and a one-line fix;
      if they match production it is the second, and whatever that session did
      needs a trace. Discriminating evidence exists, so use it rather than
      picking the cheaper reading. A ruling of (a) produces a short read-only
      look-see prompt.
    ruling: >
      Reading (i): the browser was on dev throughout; nothing production-side
      was touched. The passage's error was labelling a production-originated row
      (inherited by the dev branch at fork) as a dev-instance org. Corrected in
      CLAUDE.md and DEBT-50 by appended correction; the standing lesson added is
      to name the INSTANCE as well as the org id.
    links: [DEBT-9]

  - id: R6
    status: ruled
    asked: 2026-08-05
    ruled: 2026-08-07
    title: >
      Disposition of the six debt rows already carrying AWAITING RULING in
      their titles
    question: >
      DEBT-36, 38, 41, 42, 45 and 48 each open with "AWAITING RULING" and have
      been pending across several cycles. They predate the source sheet's
      visibility, so it offers no per-item options for them; this entry asks how
      to handle the set rather than any one of them.
    options:
      - >
        (a) Extend the analysis with per-item options and a recommendation for
        each, so the six join the same sitting.
      - >
        (b) Rule them directly from the rows without further input — they are
        marked decisions-not-work and have been in view longer than this list
        has existed.
      - >
        (c) Explicitly defer the six another cycle with a dated note, so they
        stop being silently pending.
    recommendation: >
      (a). The point of a single sitting is that the desk ends clear, and six
      unexamined items leave it half-done. Note that (a) or (c) also raises a
      second question this list does not answer — whether the six should MOVE
      into this list or stay as debt rows. That is worth its own session rather
      than a side effect of either ruling.
    ruling: >
      (b) EXECUTED, WITH FRESH EVIDENCE — and this SUPERSEDES the lean toward
      (a) above. The six were ruled DIRECTLY from the rows, on a verbatim
      extraction of all six taken at HEAD 90d8293 (report dated 2026-08-06),
      rather than through per-item option cards. (a)'s whole worry was that six
      UNEXAMINED items leave the desk half-done; the extraction is what removed
      "unexamined". The rows were read in full, at head, before the call was
      made, so building options-and-recommendation apparatus over the same
      evidence would have been ceremony rather than examination.
      ALL SIX DISPOSITIONS ARE RECORDED ON THE ROWS THEMSELVES, dated
      2026-08-07, which is where a reader of any one of them is standing.
      DEBT-38 CLOSED, both fix directions taken, commit "7d984be". DEBT-45
      CLOSED, ruled YES, same commit. DEBT-41 converted from a decision into
      WORK — its standing trigger fired. DEBT-42 relabelled: nothing was ever
      pending on it and the prefix was the error. DEBT-36 and DEBT-48 parked, to
      be scoped together as one feature phase after the current minor-fix batch.
      One new row, DEBT-59, was filed off DEBT-48 for the availability-offset
      defaults.
      THE SECOND QUESTION THIS ENTRY RAISED IS MOOT — whether the six should
      MOVE into this list or stay as debt rows. It was only ever a live question
      while a competing pending-rulings convention was running inside the debt
      table; after today NO debt row carries "AWAITING RULING", so there is
      nothing left to move and the competing convention is retired by
      exhaustion rather than by migration. Every one of the six stays a debt row,
      because what remains on each of them is work or a latent record, not a
      decision. New DECISIONS go in this list, by the convention above the
      `rulings:` key.
      THE PANEL ENDS AT 0 AWAITING A CALL, which is what this entry was really
      asking for — and it is the last of the six to close, deliberately, since a
      meta-question about a set cannot honestly be settled before the set is.
    links: [DEBT-36, DEBT-38, DEBT-41, DEBT-42, DEBT-45, DEBT-48]
