# Froot — Phase Roadmap & Status

> **Single source of truth for build status.** At the end of every phase, update this table
> (status, commit hash, notes) and commit it along with the phase's code.
> Last updated: 2026-07-12

| Order | Phase | Size | Status |
|---|---|---|---|
| 1 | I-1 Sales item sync + Ingredient library | M–L | ✅ Done (7-4/7-5, v2 rebuild: SalesItem vs Ingredient split, units.ts, CSV import) |
| 1b | I-1b Ingredient parity, lifecycle & duplicates | M | ✅ Done 7-6 (lifecycle, bulk edit, duplicate finder, cost log) |
| 2 | I-2 Vendors, POs & Receiving | M–L | ✅ Done (full PO lifecycle, receiving updates costs; receipts stamp receivedAt so purchases land in the right inventory period) |
| 3 | I-3 Storage areas | S–M | ✅ Done (commit 432a30e) — areas + count sheets, bulk assignment, unassigned triage, re-pointed to ingredientId |
| 4 | I-4 Physical counts | M | ✅ Done (commit eb816db) — count workflow, weigh-to-count, offline-tolerant entry, finalize, corrections + audit trail, count summary |
| 5 | I-5 Sales sync + COGS & analytics | M–L | ✅ Done 7-6 (b3cb378) — Square sales sync (daily/hourly/per-item caches), inventory-period math, Reports suite under Inventory: Summary, Item Sales, Periods/COGS with GL breakdown + negative-usage flags, Valuation, Turnover, Vendor Spend |
| — | D-1 Dashboard redesign (added in pack v2.3) | M | ✅ Done 7-6 (1b8160f) — Sales Performance (today vs same weekday last year, hourly pace), Monthly Goal with extrapolation, real Shift Checklist, mock-backed Team Messages / Corporate Update / Instagram |
| 6 | I-6 Recipes + needs-attention queue + adjustments | L | ✅ Done 7-6 (4947def) — recipes w/ read-time cost engine + loop detection, triage queue, prep/batch items, transfers/loss/adjustments (backdatable, grouped), Variance + Profitability report tabs; fixture: `npx tsx scripts/seed-razz-fixture.ts` |
| 7 | I-7 Reorder points & alerts | M | ✅ Done 7-7 (1020b9e…5380324, 6 commits) — expected-inventory engine (`/inventory/expected`, consumption-mode depletion, degraded mode w/o sales), per-store pars/reorder points + weekly usage on Ingredients, low-stock alerts (`/inventory/alerts` + sidebar badge, 14d staleness guard hardcoded — org-configurable later), Cart Builder + Smart Cart (`/inventory/orders/new`, fill-to-par / N×usage → one draft PO per vendor), vendor minimums/delivery days/standing adjustments (auto-attach at receive, in COGS GL), PO history grouping + CSV + invoice attach (Blob, no OCR) + price-change confirm; fixture: `npx tsx scripts/seed-i7-fixture.ts --seed/--verify --org <id>`; deferred: emailing orders to reps, alert-badge live refresh |
| — | F-1 Forecasting: sales-data foundation | S–M | ✅ Done 7-8 (b7feecb) — GoalPlan/DailyGoal schema (migration `20260708100000_f1_goal_plans`), resumable LY backfill (`POST /api/forecasting/backfill`, ~2-week chunks, verified against real 2025 Square history), nightly 3-day reconciliation cron (`vercel.json` → `/api/cron/sales-reconcile`, verified 9/9 Keva stores); actuals reuse I-5 `SalesPeriodCache`; **⚠ deploy: set `CRON_SECRET` env var in Vercel**; exit test (penny-reconcile a full month vs Square Dashboard) pending Gary's spot-check after first full backfill |
| — | F-2 Goal engine + Forecasting page | M | ✅ Done 7-8 (11f0b47) — weekday-aligned basis (−364d, calendar-date + weekday-avg fallbacks), % scaling with month-exact rounding residuals, `/forecasting` (Goal Settings panel + 12-month calendar, actual-vs-goal tinting, managers read-only, admin writes enforced server-side in every route); fixture: `npx tsx scripts/verify-goal-engine.ts` (15 checks green) |
| — | F-3 Forecasting overrides + import | M | ✅ Done 7-8 (11f0b47) — day-edit dialog (`isOverride`, survives % recalcs), month-total edit redistributed by LY weekday weights, CSV/XLSX import (daily `date,amount` or monthly `month,amount` shapes, server-side parse, preview → commit, raw file to Vercel Blob), "remaining days only" mid-year raise + opt-in override reset; see `FORECASTING.md` |
| — | F-4 Dashboard live pacing | S | ✅ Done 7-10 (96bc048, cd711d7) — Monthly Goal card is plan-driven with goal-weighted extrapolation (`MTD ÷ MTD-goal × month goal`; run-rate fallback, formula now shared via `src/lib/pacing.ts`); Square order webhooks (`/api/webhooks/square`, HMAC-verified, ACK-then-resync of the affected store-day; lazy sync + nightly reconcile stay as backstop); "All locations" dashboard mode (`/api/dashboard/rollup`: summed totals, goal-weighted projection over summed DailyGoal rows, sortable store-ranking table w/ on/behind-pace pills, admin vs assigned-store scoping); fixture: `npx tsx scripts/verify-f4-rollup-webhook.ts` (14 checks green); **⚠ deploy: set `SQUARE_WEBHOOK_SIGNATURE_KEY` in Vercel (per env) + add the webhook subscription in each Square app (prod + Froot Staging) — notification URL `${NEXT_PUBLIC_APP_URL}/api/webhooks/square`, events order.created/updated + payment.created/updated; see FORECASTING.md** |
| — | F-5 Forecasting polish | S | ✅ Done 7-10 (cb3f1bb) — goal-edit audit log (every goal mutation → AuditLog w/ before→after, `GET /api/forecasting/audit` + Edit history panel on /forecasting; admins all stores, managers assigned only), CSV export (`GET /api/forecasting/export`, round-trips through the importer, Export CSV button), behind-pace alerts (daily cron `/api/cron/pace-alerts` 15:00 UTC, threshold `PACE_ALERT_THRESHOLD_PCT` default 90%, pace through yesterday via shared month-goal/pacing helpers, one alert per store-month via PaceAlertLog, migration `20260710220000`; recipients = org admins + assigned managers; email sender is a swappable interface w/ console default — real provider deferred); fixture: `npx tsx scripts/verify-f5-polish.ts` (18 checks green); **⚠ deploy: cron registers from vercel.json automatically (needs existing `CRON_SECRET`); optional `PACE_ALERT_THRESHOLD_PCT` env; alerts log to console until an email provider is wired in `src/lib/notify.ts`** |
| — | I-14 Team Messaging Phase 1 | M–L | ✅ Done 7-9 — TeamMessage/MessageAttachment/MessageReaction/MessageRead/CorporateUpdate schema (migration `20260709090000_i14_team_messaging`), `/messages` store feed (typed composer, photo/doc/YouTube attachments via Blob, emoji reactions, manager resolve/reopen, 15-min edit window, soft delete, cursor pagination, mark-read), checklist handoff notes (top+bottom "Leave a note for the next shift" composer w/ Post-to picker, server-side date resolution by operationalPhase, "Notes from the last shift" banner marks read), dashboard Team Messages + CORPORATE UPDATE boxes live via one `/api/dashboard/comms` call (PREVIEW/sample labels removed, corporate box collapses when none active), bare-bones admin corporate composer on `/messages`; roster sync = existing Square staff import; Phase 2+ deferred: acknowledgments, Twilio notifications, shortage→inventory alerts |
| — | I-14b Shift handoff carry-over & acknowledge | S–M | ✅ Done 7-11 (cde3b86…4c0a1de, 5 commits) — cross-day carry-over now resolves at read time with `postedForDate <= day` (a Saturday closer note for a Sunday-closed store surfaces on Monday's opener automatically), explicit Acknowledge lifecycle (`acknowledgedAt/acknowledgedByUserId` on TeamMessage, migration `20260711165333_add_handoff_ack_expiry`; viewing still only marks read — banner + dashboard clear on Acknowledge everywhere), org-configurable auto-expiry (`Organization.handoffNoteExpireDays` default 2, `expiresAt` stamped at post; legacy rows keep the 7-day createdAt backstop), store-wide notes ("Everyone (store-wide)" composer option, `postedToTemplateId` null; date rule = last-slot semantics), dashboard "NOTES FOR THIS SHIFT" card above the Shift Checklist box (via `shiftNotes` on `/api/dashboard/comms`, store-local day, no template filter so notes to retired templates are never dropped; collapses when empty), stale styling (>2d unacked) in the banner, Acknowledged/Expired badges in the feed, `PHASE_ORDER` fixed to canonical "During the Day" (+ legacy "During Hours" alias — one live row still has it); verified: 14-check DB round-trip incl. closed-day carry-over, expiry, ack (script run ad hoc, not committed) |
| — | IG-1 Instagram integration | M | ✅ Done 7-10 (b1fce8b) — org-level connect via **Instagram API with Instagram Login** (`graph.instagram.com`, scope `instagram_business_basic`; requires a Professional account; Basic Display API is dead), `Organization.instagram*` fields (migration `20260710235000_instagram_integration`), long-lived token w/ lazy 7-day-window refresh, Settings card (connect / on-off switch / disconnect behind AlertDialog / reconnect nudge), conditional sidebar item, `/instagram` grid page (permalink link-outs; IG can't be iframed), dashboard strip live via `/api/instagram/feed` (~60 min in-memory cache, stale-on-error, never calls IG on page load; `media_url` never persisted); **⚠ deploy: set `INSTAGRAM_APP_ID` + `INSTAGRAM_APP_SECRET` (optional `INSTAGRAM_REDIRECT_URI`) in Vercel per env; register `${NEXT_PUBLIC_APP_URL}/api/instagram/callback` as the OAuth redirect URI in the Meta app; until Meta App Review grants Advanced Access for `instagram_business_basic`, only IG accounts with a role on the Meta app can connect — start App Review early**; deferred: posting/comments/insights/Stories, Vercel-cron refresh backup |
| — | HR-0 HR, Training & Compliance foundation | S–M | ✅ Done 7-12 (0233558…8aec033, 4 commits) — full forward-compat HR schema (15 models + 3 enums incl. the HR-0a-confirmed checkpoint/acknowledgment pair w/ signing-time snapshots + fileHash version pin, migration `20260712120000_hr0_hr_training_compliance_schema`), server-side availability gate `hrModuleAvailable()` (`HR_MODULE_AVAILABLE` env flag OR `HR_INTERNAL_ORG_IDS` allowlist — `/hr` 404s while unavailable), per-org `activeModules` "hr" toggle (Settings Integrations card w/ Switch → `POST /api/hr/toggle`, admin-only), Billing add-on card, gated sidebar HR item (hidden until available AND active); no feature code reads the HR models yet; **⚠ deploy: `HR_MODULE_AVAILABLE=true` set on Vercel Preview only — leave production unset until HR launch**; next: HR-1+ (documents CRUD), HR-3 private Blob route |
| — | HR-1 Staff directory + detail shell | S | ✅ Done 7-12 (f85c062…b7d854b, 3 commits) — `/staff` gains a gated Compliance column (stub `getStaffComplianceSummary()` in `src/lib/hr.ts` returns `pct: null` → muted "—" w/ tooltip, never 0%; HR-8 replaces with the real batched rollup) and display-name links to new `/staff/[id]` — both only when `hrModuleAvailable(orgId) && activeModules.includes("hr")`, page byte-identical otherwise; `/staff/[id]` (availability + toggle gate → notFound, non-admins scoped to their assigned stores) renders header (Square badge, member-since, store chips) + tabs Overview (real data) / Documents / Training / Notes / Compliance as read-only shells labeled HR-4/5, HR-6/7, HR-2, HR-8; `/hr` landing now links to the staff directory; read-only phase — no writes, no new API routes |
| — | HR-2 Manager notes | S | ✅ Done 7-12 (30f6e26…2d3d5bb, 3 commits) — Notes tab on `/staff/[id]` is live (first HR phase that writes): add-note box (textarea + 5-category select) + list w/ color-coded category chips, author/timestamp footer, edited marker, empty-state CTA, client-side category filter chips; edit is author-only (dialog), delete is author-or-ADMIN behind AlertDialog; API `POST /api/staff/[id]/notes` + `PATCH/DELETE /api/staff/[id]/notes/[noteId]` share one guard (`access.ts`): 401 → `hrModuleAvailable` 404 → `requireModule("hr")` 403 → `requireManagerOrAdmin` 403 → org 404 / store-scope 403 (mirrors the page's `getStaffMember`); STORE/STAFF never see the tab and never get notes fetched; **no migration** — `ManagerNote.authorUserId` deliberately has no FK to User (notes survive author deletion; authors stitched via a second query, "Former user" fallback); Zod validates the 5-value category + non-empty body; notes are text-only by design (files live in HR-4/5) |
| — | HR-3 Reference Library + private HR file service | M | ✅ Done 7-12 (1a03dca…5161e1e, 3 commits) — the load-bearing HR file layer: dedicated **PRIVATE** Blob store `froot-hr` (iad1, store_7H3toifj0cxlzbAO; connected to all envs w/ `HR_BLOB_` prefix → `HR_BLOB_READ_WRITE_TOKEN`, documented in CLAUDE.md) — uploads via `src/lib/hr-files.ts` `uploadHrFile` (`access:'private'` + random suffix, PDF/PNG/JPG/DOC/DOCX ≤25 MB, sha256 `fileHash`), delivery via `getHrFileDownloadUrl` (cached 10-min read delegation → 5-min presigned GET; `streamHrFile` proxy fallback kept), authorization is the per-kind `canReadHrDocument` policy (Reference ⇒ any org member; HR-4 kinds stubbed) — **verified against the live store: raw blob URL w/o session → 403, signed URL → 200, unauthenticated app routes → sign-in redirect**; `/hr/documents` Reference Library (any role reads/downloads; grouped by category w/ chips, client filter chips, skeleton loader, role-aware empty states) + ADMIN Add/Edit/Archive (`POST /api/hr/documents`, `PATCH /api/hr/documents/[id]` soft archive `isActive:false` — no hard delete, no blob deletion; shared guard `access.ts`: 401 → availability 404 → module 403 → admin 403); Document Library card on `/hr`; public task-attachment store untouched, **no dep bump** (`@vercel/blob` 2.5.0 already ships private access + presign), **no migration**; single-version per doc (re-upload/versioning UI lands w/ HR-4) |
| 8 | M-1 Keva data migration (optional) | M | ⬜ Unblocked (I-6 done) |
| 9–11 | N-1/N-2/N-3 Nutrition | M | ⬜ Not started |
| 12 | X-1 Activation & QA | S | ⬜ Not started |
